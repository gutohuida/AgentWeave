# For contributors — builds from local source.
#
# Usage (from hub/ directory):
#   make docker-up
#   or: docker build . -t agentweave-hub:latest && docker compose up -d

services:
  hub:
    build: .

# Override root .gitignore — allow src/lib/ (not a Python lib dir)
!src/lib/

node_modules/
dist/

# tsc build-mode artifacts (emitted by `tsc -b` / `npm run build`)
*.tsbuildinfo
vite.config.js
vite.config.d.ts

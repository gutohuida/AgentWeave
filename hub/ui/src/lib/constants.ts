/**
 * Shared client-side constants. The values here must be kept in sync with the
 * backend `Session.id` generation rules and the placeholder string the Hub
 * dashboard uses to mean "no session selected".
 */

export const NEW_SESSION_ID = '__new__'

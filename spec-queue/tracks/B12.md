# B12 — Single-finding specs

**Findings:** F65, F130, F330, F363, F278
**Decisions:** D-B12-1 and D-B12-2 (F330), D-B12-3 (F363); F363 also depends on F354 (B11), which R1 treats as an assumption
**Changes:** `openspec/changes/a-specification-is-read-in-results-that-fit/`, `openspec/changes/a-refused-first-send-leaves-no-exploration-behind/`, `openspec/changes/a-file-path-is-not-redacted-as-a-credential/`
**Status:** R3 done — parked for operator

## R1 — explore and propose (2026-09-24)

Read at worktree HEAD `ce086b6` (the bundle-opening commit on top of `404c7d5`; it touched no product
code). The ROUNDS row also lists F213, but the bundle table moved it to B6, so it is not handled here.
In severity order: F130 (B), F363 (B), then F65, F330 and F278 (C).

### F130 (B) — already fixed; no change

ROUNDS.md's condition was *"if Round 3c finds no contract"*. Round 3c found the contract: its entry
reads *"A repair, not a spec track: `conversation-checkpoint`'s *A checkpoint is anchored on its
predecessor rather than regenerated* already says generation 'SHALL NOT re-read the whole
conversation from its start'"* (`scripts/drive/FINDINGS.md:10084`). It was fixed on 2026-09-22 in
Round 3 (group 3c) with `checkpoints.boundary_of(anchor)`, which `compute_envelope` and
`runs_to_cover` both use. The tests are `test_a_checkpoint_over_an_empty_span_keeps_the_boundary_where_it_was`
and `test_an_empty_checkpoint_written_before_the_fix_is_read_through_its_predecessor`
(`test_checkpoint_record.py`). Its status line says *"fixed … [Round 3, 2026-09-22]"* (`:9988`).
The one residual the entry names is that a legacy NULL row can cost one more empty checkpoint. That
is self-healing and needs no spec. **Needs:** nothing. ROUNDS' single-finding row should drop it.

### F363 (B) — still open; change `a-specification-is-read-in-results-that-fit`

Re-verified. The route has no size bound (`hub/hub/api/v1/agent_actions.py:1374-1481`). `include` is
still only `requirements|full` (`:1377`), and a `spdoc-` id still reaches `validate_spec_path`
(`:1401-1404` → `spec_manifest.py:72`), which answers *"path must begin with 'spec/'"*. Measured
on this repository's own corpus: the default view of
`spec/capabilities/agent-conversation-workspace/spec.html` is over 72,000 characters before its
envelope, with 47 requirements (30.7 KB of statements and 41.9 KB of criteria).

**One claim in the finding changed shape.** *"`list_evidence` accepts that id"* is not quite right.
`list_evidence_for_agent` (`agent_actions.py:1248-1275`) reads `document` **only** when
`identifier` is given, and then resolves it by path (`_resolve_requirement`, `:1154-1164`, which
would 404 on an id). Without `identifier`, `document` is ignored silently, so the call "accepts" the
id by not looking at it. The ids agents actually hold come from tasks (`spec_document_id`,
`mcp_server.py:754`, `:859`). `run_task_binding.spec_document_for_task` (`:464-477`) exists
because a builder tried the id and was refused. **Candidate new finding (not filed, since R1 does
not edit FINDINGS.md):** `list_evidence`'s `document` is silently ignored when `identifier` is
empty.

**The F354 dependency.** `spec-queue/tracks/B11.md` did not exist when R1 ran. **Assumption
stated:** R1 assumes nothing about F354's answer. The change is designed so that either version skew
between `mcp_server.py` (spawned from the working tree) and the running Hub degrades to today's
behaviour, never to a refusal (design D5). The skew table:

- **New tool, old Hub:** the extra `identifiers` query parameter is ignored. An `include=outline`
  gets a 422, which is why the tool's default must stay `requirements` (task 1.11).
- **Old tool, new Hub:** everything works.

So F354 decides only *when* IMPL's commit may land on the branch `:8000` runs, not the design. If B11
recommends pinning the server the Hub loaded, D5 becomes belt and braces.

**D-B12-3 — how the read is bounded.**

- (a) **A budget enforced by the Hub, with continuation by requirement identifier.** Recommended.
  An agent cannot see the harness ceiling, so a bound it chooses is chosen wrong. Identifier-keyed
  continuation matches how agents already reason (`FR-n`) and also serves a builder that only
  needs its task's requirements.
- (b) A `max_chars` or page parameter chosen by the agent. It fails invisibly, because the agent
  sees a spill notice and not a size.
- (c) Only add `outline`. The default read shrinks, but criteria reads still spill.

Recommended: (a), at 40,000 serialised characters. R2 must confirm Claude Code's MCP ceiling. R1
believes it is 25 k tokens by default (`MAX_MCP_OUTPUT_TOKENS`), but did not verify.

**Route under raise (design D6).** `read_document` raising `OSError` or `UnicodeDecodeError` is an
unhandled 500 today (`spec_documents.py:136-142`). R1 left the choice of owning it to R2; the
recommendation is a 409 naming the reason.

### F65 (C) — changed shape; the state it describes can no longer be born; no change

The finding's status line (*"still ends … on the first refusal rather than after three attempts"*)
is inverted relative to the code. The operator's chosen fix (2026-08-26) was to make "no evidence to
review" **terminal on the first refusal**. Today it is counted like every request-level refusal
and given up after `DELIVERY_ATTEMPT_LIMIT = 3` (`hub/hub/inbound_queue.py:222`;
`turn_scheduler.py:596-608`, where `gives_up = attempts >= DELIVERY_ATTEMPT_LIMIT` and the entry is
withdrawn with `abandoned_reason` and a `queue_entry_abandoned` event).

**"Queued as Q4-SPEC": no such change exists.** `openspec/changes/` has no Q4-SPEC directory and
no archived change carries F65. `FINDINGS.md:3166` (F53's D-4 note, 2026-09-22) already records
that *"that queue label is historical"*. The same label on F65 is historical too.

**What closed the defect: every path that could queue a review with no commit to check out now
refuses before queueing.**

1. **Loops and flows** (F65's own origin: a loop briefing). `decide_firing`
   (`scheduler.py:1617`) asks `commit_for_task_review` before resolving a reviewer and reports the
   task as `unstaffed` with the reason (`scheduler.py:1956-1979`). This was added by `3e07726`
   (2026-08-28, *"a firing does not stage a review it cannot start"*), two days after F65 was filed.
   The loop path (`scheduler.py:3747`) and the flow path (`:3417`) both take their selections from
   `decide_firing` (`:3235`). Tests: `test_a_review_needs_something_to_review.py:158-225`.
2. **The operator's `POST /agent/trigger`** refuses 409 before anything is queued
   (`agent_trigger.py:1522-1529`).
3. **The divergence restaff of a failed review** (`run_divergence.py:483`) follows a review turn
   that ran, so evidence naming a commit existed. Evidence rows are never deleted: `grep` finds no
   `delete(RequirementEvidence…)` in `hub/hub`, and `mark_artifact_removed` is dead and in any case
   keeps the row (`requirement_evidence.py:864-885`).

So the entry F65 describes can arise today only as a leftover from before 2026-08-28. Such a
leftover is given up after three passes, and until then it blocks archiving its agent with a 409
that names it and offers the remedy (`agents.py:2799-2832`, F180).

**Needs:** a ledger close on F65, not a spec. The close should cite `3e07726`, the route refusal,
and the three-attempt give-up, and should note that the operator's "terminal on first refusal" is
now moot because nothing can produce the entry. **Optionally**, add one regression test that
`decide_firing` never queues an entry with `review_task_id` set for an evidence-less task. The
existing tests assert the selection and the task status, and R2 should check whether any of them
asserts that the queue is empty. R2 should also re-check claim 3 (any other `new_entry(...,
review_task_id=…)` site: R1 found `agent_trigger.py:1582`, `run_divergence.py:267/483` and
`scheduler.py:3417/3747` only).

### F330 (C) — still open; change `a-refused-first-send-leaves-no-exploration-behind`

Re-verified at `hub/ui/src/components/agents/NewConversationSurface.tsx:83-117`: the create happens
first, the trigger second, and a refusal throws with no cleanup. `POST /project/documents` writes
the file before its commit (`spec.py:1461` → `spec_service.py:238`). A file under `spec/` without a
row is not inert, because discovery and adoption find it. `RequestModel` refuses unknown fields
(`schemas/common.py:21`, F116). That makes the shipping order a hard constraint: a new bundle
against the old Hub would refuse every explore send (design D5). So the change ships as two
commits, with the UI only after the operator restarts `:8000`.

**D-B12-1 — what happens to a document created for a refused send.**

- (a) **The request that created it discards it** (file, events, index rows and the row), gated on
  the document still being `exploring`, having no later event, and being named by no task or loop.
  The function is reachable from no route. Recommended.
- (b) The composer archives it through the phase machine
  (`spec-document-authority` `:1891`). This keeps "no deletion", but the row and the file remain,
  so F330's *"in the repository and in the spec list"* is only half fixed. It also does nothing
  when the tab closes or the network drops.
- (c) Create it at dispatch. It needs a queue-entry column (a migration), gives no path to a
  queued first send, and still has to compensate for a file written before a rolled-back dispatch.

The argument for (a) against `:1891`'s *"rather than a separate deletion path"*: that requirement
governs a document someone has seen. This one is the refused request undoing its own write. R2 is
asked to challenge it.

**D-B12-2 — the document cannot be created (naming exhausted, write error).**

- (a) **Refuse the send and say why.** Recommended. The composer restores the text on any throw
  (`Composer.tsx:216-217`), so nothing is lost. Sending without the document is exactly the
  symptom the explore control was built to stop (`NewConversationSurface.tsx:72-76`).
- (b) Today's behaviour: send anyway, silently without a document. It is pinned by the vitest case
  *"still starts the conversation when the document cannot be created"*, which (a) replaces.

**Route under raise:** design D2 tabulates every raise (mint, create, file write, commit, dispatch
refusal) and what each leaves behind. A failed file removal answers with the original error and
leaves a file with no row, which is no worse than today.

**Out of scope, stated in the spec:** input answered `200 queued` and later given up after three
delivery failures. The operator was told it was accepted, and the document is retired through its
phase.

### F278 (C) — still open; change `a-file-path-is-not-redacted-as-a-credential`

Re-verified by calling `hub.runner_events.redact_secrets` at HEAD:
`/Users/operator/code/agentweave/hub/main.py` becomes `<redacted>.py`, and
`/workspace/proj/.agentweave/worktrees/beta/src/app.py` becomes `/workspace/proj/.<redacted>.py`.
The pattern is unchanged (`runner_events.py:57-60`). No decision is needed.

The fix keeps the pattern and decides in the replacement. A catch-all match with at least three
`/` segments, each an ordinary lowercase or camel-case word, is kept, and any segment of 32 or more
characters inside it is still redacted. Measured:

- every credential in the existing tests is still redacted;
- the AWS-style `wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY` is still redacted;
- `…/v1/tokens/<40 hex>` keeps its path and loses the value;
- the residual is 1 survivor in 1,146,498 random base64 strings containing `/`;
- two-segment paths were rejected, because 3 of 790,914 random strings survived under that rule.

**A test pins the defect:** `test_write_paths_on_run_events.py:139-148` asserts that the path is
*absent* from `payload["input"]`. Task 1.5 rewrites it to keep proving read-before-redact
ordering, with a path that still redacts. The CLI twin
(`src/agentweave/diagnostics.py:43`) has no `/` in its class and is untouched.

### What was written

- The three change directories above, each with `proposal.md`, `design.md`, `tasks.md`,
  `test-guide.md`, a spec delta and `.openspec.yaml`. All three pass
  `openspec validate <name> --strict`.
- Deltas: `spec-document-authority` (2 ADDED), `agent-composer` (1 ADDED), `agent-stream-events`
  (*Structured payload safety*, MODIFIED).
- No product code, tests, FINDINGS, ROUNDS, APPROVALS or DECISIONS were edited.

### Open for R2

- F363: the MCP ceiling (D1), the `outline` size, whether FastAPI ignores the undeclared query
  parameter, D6's `OSError`, and B11's F354 answer if it exists by then.
- F330: the complete list of refusals on each side of the creation point; whether a
  non-request-level dispatch refusal can reach the caller as an error; whether the spec list hides
  archived documents; and the challenge to D3.
- F278: re-run the residual measurement.
- F65: check whether any test asserts that no entry is queued for an evidence-less review
  selection, and re-grep the `review_task_id` sites.

## R2 — independent comparison (2026-09-24)

Re-derived at the worktree's HEAD. 31 load-bearing claims checked against the code; 9 disagreed or
were incomplete. `spec-queue/tracks/B11.md` still does not exist; B11's F354 change
(`openspec/changes/an-agents-tool-server-is-the-one-its-hub-loaded/`) does. All three changes pass
`openspec validate --strict` after the edits.

### Claims that disagreed with the code, and what changed

| # | Claim (round) | What the code says | Changed |
|---|---|---|---|
| 1 | F363 D1: Claude Code spills at 25k tokens (`MAX_MCP_OUTPUT_TOKENS`) | Read from the installed CLI (2.1.280, `claude.exe`): a result is **spilled** to `tool-results/` above `min(maxResultSizeChars=1e5, C$=50000)` = **50,000 characters** of text. `MAX_MCP_OUTPUT_TOKENS` (default 25,000 tokens, about 100,000 characters) is a separate, larger **truncation** limit. A remote flag can override the spill threshold per tool | D1 rewritten with both limits. The 40,000 budget stands, with 10,000 characters of headroom; the budget now counts the whole response |
| 2 | F363: the default view of the largest document is over 72 KB | 66,559 characters by `json.dumps`, 64,450 as fastmcp 3.1.0 sends it (compact `pydantic_core.to_json`). Three documents in `spec/` exceed 50,000 | Numbers corrected in the proposal and design; conclusion unchanged |
| 3 | F363 D3: outline size unknown | Largest outline over the 34 documents: 13,199 characters | Recorded |
| 4 | F363 D2/D3: continuation by identifier reaches every requirement | `requirement_view` returns unindexed requirements with an identifier from the document's identity block, which can be `None` (`spec_reading.py:164-189`); they could never be asked for again | `identifiers` also matches `key`; `remaining_identifiers` names the key when the identifier is null. Spec sentence and task 1.12 added |
| 5 | F363 D6: `read_document` raising `OSError` is the only unhandled raise | Also `UnicodeDecodeError` (a `ValueError`) and `ProjectPathError` from `resolve_relative` (`project_workspace.py:78-80`); all three are 500 today | Decided: all three answer 409, after the precedent at `spec.py:197-205`. Task 1.13 added |
| 6 | F363 Context: an agreement test covers `include` (`test_mcp_tool_schemas.py:86`) | No test covers `read_spec_document`; the route's values are inside a `pattern=` string | Context corrected; task 1.11 now makes the values an importable constant first |
| 7 | F363 task 1.3: declare requirements out of identifier order | Identifiers are minted in declaration order, so a first submission cannot be out of order | Task 1.3 uses 12+ requirements (string vs numeric order) and a reordered second revision |
| 8 | **F330 D3: discarding the document is compatible with the main spec except for `:1891`** | The discard deletes `spec_document_events` rows. `spec-document-authority` *Every change to a document is recorded as an attributed event* says "A recorded event MUST NOT be edited or removed" (scenario *History cannot be rewritten*). R1 did not weigh it | **D-B12-1 reversed** (below). Design D3, proposal, spec delta, tasks 1.2/1.2b/1.7/2.2/2.3 and the test guide rewritten |
| 9 | F330 D3: discard only when `withdraw_refused_entry` returns `True` | `False` means the scheduler withdrew it first (`inbound_queue.py:443-447`); the route raises the refusal either way, so the composer never learns the path | Retire in the F108 branch regardless; task 1.2b |

Two smaller F330 additions: D1's creation point now lists **every** route refusal
(`agent_trigger.py:1415-1565`, all before `new_entry`), and `start_exploration` is also refused
with `session_mode="resume"` and with `review_task_id`. D2 gains two rows: an environment-level
refusal answers `200 queued` (no `refusal`, `:1654-1658`), and an unexpected raise after the commit
leaves a still-queued entry with its document, which is correct.

### D-B12-1 reversed

R2 recommends **(d): nothing committed is ever deleted.** Route-level refusals are all before the
creation point, and a failed commit rolls the rows back while the route removes its own file. That
covers F330's recorded case (F329's 500 at the commit) and its reproduction (an archived agent).
Only a dispatch refusal after the commit remains, such as an agent with no row
(`agent_trigger.py:676-687`, not checked by the route) or a runner with no adapter (`:731-738`).
There the route **archives** the document through `spec_lifecycle.transition` as the operator's act
(`exploring → archived` is permitted for a document with no requirements or tasks,
`spec_lifecycle.py:320-341`). Archived documents are left out of the spec tree's current view
(`specNavigation.ts:53`, `:103-104`) and listed under *Archived* in the browser
(`SpecDocumentBrowser.tsx:136-149`). So the file stays in `spec/changes/` for that rare case only.

- (a) R1's delete remains available, but it needs MODIFIED deltas to two main-spec requirements
  (append-only events, and `:1891`'s "rather than a separate deletion path"). R2's answer to the
  question R1 left open: the "deletion path" argument alone might hold, but the append-only rule does
  not bend to it.
- (b) The composer archives, and (c) create at dispatch: rejected for R1's reasons, which still hold.

### What stands

- **F130**: already fixed. `boundary_of` (`checkpoints.py:158`) is used by `runs_to_cover` (`:200`)
  and `compute_envelope` (`:410`), and both named tests exist (`test_checkpoint_record.py:443`,
  `:462`). A ledger close, not a spec.
- **F65**: R1's verdict stands. Every `new_entry` site was re-grepped: `review_task_id` is set only at
  `agent_trigger.py:1582`, `run_divergence.py:267` (reached from `:483`) and `scheduler.py:3417` and
  `:3747`. The scheduler's gate (`scheduler.py:1956-1979`) refuses before a review is selected. **No
  existing test asserts the queue stays empty** for an evidence-less review selection
  (`test_a_review_needs_something_to_review.py` asserts selection and task status only), so R1's
  optional regression test is worth adding when F65 is closed.
- **F363**: D-B12-3 (a Hub-enforced 40,000-character budget, continuation by identifier) stands, now
  on the correct ceiling. D4 (the id in `path`) stands: ids are `spdoc-` + 12 hex (`utils.py:20-22`,
  `spec_lifecycle.py:213`). D5 stands: FastAPI ignores undeclared query parameters on a route that
  declares each with `Query` (`agent_actions.py:1374-1379`). **F354**: B11's change recommends
  pinning the tool server at Hub start and orders its IMPL before F363's `mcp_server.py` half; this
  agrees with R1's stated assumption, and nothing in F363's design changes.
- **F278**: every code claim holds (three callers: `runner_events.py:177`, `:206`,
  `scheduler.py:94`; the pinning test at `test_write_paths_on_run_events.py:139-148`). The residual
  was re-measured with an independent implementation: 0 in 685,660. D1 item 1 decided (a named
  `entropy` group, test `m.lastgroup`). Noted: all-capitals segments (`README`) stay redacted, and
  `jobs.py:61`'s separate redactor has no `/` and is untouched.
- **Collisions**: none. B11's change and `the-shell-judge-reads-a-word-whole` mention
  `mcp_server.py` but touch no function these changes touch.

## R3 — independent comparison (2026-09-24)

Re-derived at the worktree's HEAD, weighted to F330's reversed decision and F363's F354 ordering.
22 load-bearing claims checked; 2 disagreed. All three changes pass `openspec validate --strict`.

### F330: is R2's archive path reachable, and what does each refusal return?

| Refusal (composer's real request: `agent`, `message`, `overrides`, `NewConversationSurface.tsx:98-106`) | Route returns | Left behind (after the change) | Operator sees |
|---|---|---|---|
| Any route-level refusal, `agent_trigger.py:1415-1565` (archived agent, workspace, closed conversation, overrides…) | its 4xx, before `new_entry` (`:1567`) | nothing: creation is after these | the sentence, text restored (`Composer.tsx:216-217`) |
| Naming exhausted / `PhaseError` at creation | 409 with `code` | nothing | the sentence, text restored |
| Commit fails (`:1586`, F329's case) | the raise (500) | nothing, once the route removes its file | generic error, text restored |
| Request-level dispatch refusal naming this entry (F108, `:1611-1641`) | the refusal's status | the document, **archived**, file kept | the sentence, text restored; nothing new in the current tree; the placeholder under *Archived* |
| Environment-level dispatch refusal (no `request_level`, e.g. no runner bound, `:689-696`) | 200 `queued` (`turn_scheduler.py:674-681` builds no `refusal`) | the document, correctly (input still queued) | the conversation opens with its document |
| Unexpected raise after the commit | 500 | document and queued entry, correctly | an error for input that will run (true of every send today) |

**The F108 row is reachable from the composer only by a race.** Every `request_level=True` raise in
`trigger_agent_directly` was enumerated: `:656` and `:708` are restated by the route before
creation; `:444`, `:459` and `:832-921` need a review entry (D1 refuses `start_exploration` with
`review_task_id`); `:930` and `:942` need a `work_dir` (checked by the route first); `:738` and
`:1173` cannot fire, since a runner's `cli` is validated against `RUNNER_CLIS = ("claude", "codex")`
(`db/models.py:311`), both in `SUPPORTED_RUNNERS` (`runner_commands.py:60`) and both built.
`:686` (no `Agent` row) cannot be named by a composer that picks from rows (no route deletes an
`Agent`) but is reachable from the API, which is how tasks 1.2/1.2b stage it. So the composer
reaches the archive only when the agent is archived between the route's check and the dispatch.
**R2's decision stands** (no delete; archive after commit): it is cheap, right for the race, and
catches any request-level refusal added later. But F330's recorded case (F329's commit 500) and its
reproduction (an archived agent) are closed by D1 and the commit compensation, not by the archive.
Written into design D3 as a reachability paragraph.

### Claims that disagreed, and what changed

| # | Claim | Code | Changed |
|---|---|---|---|
| 1 | F330 D3: the retire is `transition` + commit + broadcast | The operator's own archive, `POST /documents/phase` (`spec.py:1606-1629`), also calls `spec_service.rerender_phase` (`spec_service.py:788-813`), which rewrites the file's visible status. Without it the row says `archived` and the file still says `exploring` | The retire now mirrors that route: transition, `rerender_phase`, commit, broadcast. An `OSError` from the rewrite still commits the phase (the row is the authority). Design D3, task 2.2, task 1.2's assertion, and one spec scenario line |
| 2 | F330 (implicit): the archive branch is how F330 gets fixed | Race-only from the composer (above) | Reachability paragraph added to D3; no decision changed |

### What stands

- **F330**: D1's creation point and full refusal list, D2's rows, `withdraw_refused_entry`'s `False`
  case (`inbound_queue.py:440-455`), the `exploring → archived` guard (`spec_lifecycle.py:320-341`:
  operator-only, refuses a document with requirements or tasks), the two-event guard
  (`create_document` writes `created`; `save_document` writes one content event; the reindex writes
  no document event), and D5's two-commit shipping order (`RequestModel` refuses unknown fields).
- **F363 vs F354**: B11's `an-agents-tool-server-is-the-one-its-hub-loaded` pins `mcp_server.py` at
  Hub start and recommends: its IMPL, then `:8000`'s restart, then F363's `mcp_server.py` half
  (its `design.md:113-114`). F363's design (header note and D5) prefers the same order and shows it
  is safe without the pin. Consistent. The pin does not affect task 1.11: only the test imports the
  route's constant, and `mcp_server.py` keeps its stdlib + fastmcp import rule. D6's 409s agree with
  `spec.py:195-206` (that route's 400 for `ProjectPathError` is for a caller's path; D6 says so).
- **F278**: the pattern (`runner_events.py:57-60`) and the pinning assertion
  (`test_write_paths_on_run_events.py:139-148`) are as the change says.
- **F65 / F130**: re-confirmed (evidence in Final).

## Final — what the bundle reached, and why

**Parked awaiting the operator's approval. IMPL has not started.**

### Decisions

| # | Question | Recommended answer | Why |
|---|---|---|---|
| D-B12-1 | F330: what happens to a document created for a first send that is refused? | **Nothing committed is deleted.** The trigger route creates the document itself, after all its own refusals and in the same transaction as the message, so a refusal before the commit or a failed commit leaves nothing (the route removes the one file it wrote). A refusal from the dispatch after the commit **archives** the document as the operator's act, just as the operator's own archive does. | Deleting would erase recorded document events, which `spec-document-authority` forbids ("A recorded event MUST NOT be edited or removed"), and would be the separate deletion path the same spec rules out. The archive case is rare: from the real composer only an agent archived mid-request reaches it. |
| D-B12-2 | F330: the document cannot be created (names exhausted, write error). Send anyway? | **Refuse the send and say why.** | A first explore turn without its document is the exact failure the explore control exists to prevent (`NewConversationSurface.tsx:72-76`); the composer restores the text on any refusal, so nothing is lost. |
| D-B12-3 | F363: how is an agent's spec read kept inside what its harness accepts? | **The Hub enforces a 40,000-character budget on the whole response and names the requirements still to ask for** (by identifier, or key when there is none), plus an `outline` view. | Claude Code spills an MCP result to a file above 50,000 characters (read from the installed CLI); three documents in `spec/` already exceed that; an agent cannot see the ceiling, so a size it chose would be chosen wrong. |

### Changes

| Change | Findings | What it does | Size |
|---|---|---|---|
| `openspec/changes/a-specification-is-read-in-results-that-fit/` | F363 | Budgets `read_spec_document`'s response, continues by requirement identifier or key, adds `outline`, accepts a document id, answers an unreadable file with 409 instead of 500. | M: route, `spec_reading.fit_view`, tool signature; about 13 tests |
| `openspec/changes/a-refused-first-send-leaves-no-exploration-behind/` | F330 | `POST /agent/trigger` gains `start_exploration`; the composer makes one request; a refused send leaves no document (or an archived one after a dispatch refusal). | M: route, two `spec_service` helpers, composer; two commits, the UI one after `:8000` restarts |
| `openspec/changes/a-file-path-is-not-redacted-as-a-credential/` | F278 | The run-event redactor keeps ordinary file paths of three or more segments and still redacts long credential-like segments inside them. | S: one function and a test rewrite |

### Open for the operator

1. **D-B12-1**: archive, never delete (recommended). *If delete:* the change must add MODIFIED
   deltas to two `spec-document-authority` requirements (append-only events; "rather than a separate
   deletion path"), and the file would also vanish from `spec/changes/`.
2. **D-B12-2**: refuse the send (recommended). *If "send anyway":* a creation failure becomes a
   notice on a 200, and the existing vitest case stays.
3. **D-B12-3**: Hub-enforced 40,000-character budget with identifier continuation (recommended).
   *Agent-chosen `max_chars`:* fails invisibly, since the agent sees a spill notice, not a size.
   *Only `outline`:* the default read shrinks, but criteria reads still spill.
4. **Order with B11 (F354)**: recommended B11's pin IMPL, then your `:8000` restart, then F363's
   `mcp_server.py` half. F363 is also safe without it (its D5), so B11 is not a hard blocker.
5. **F330's UI commit** waits for your `:8000` restart past its Python commit: a new composer on an
   old Hub would have every explore send refused (unknown field).

### Findings that did not need a change

- **F130: already fixed** (Round 3, 2026-09-22). `checkpoints.boundary_of`
  (`hub/hub/checkpoints.py:158`) is used by `runs_to_cover` (`:200`) and `compute_envelope`
  (`:410`); pinned by `test_a_checkpoint_over_an_empty_span_keeps_the_boundary_where_it_was` and
  `test_an_empty_checkpoint_written_before_the_fix_is_read_through_its_predecessor`
  (`hub/tests/test_checkpoint_record.py:443`, `:462`). Needs a ledger close; ROUNDS' single-finding
  row should drop it.
- **F65: needs only a ledger close.** The entry it describes (a queued review with no commit to
  check out) can no longer be created: `decide_firing` asks `commit_for_task_review`
  (`scheduler.py:1970`) before staffing and reports the task unstaffed (added by `3e07726`,
  2026-08-28); `POST /agent/trigger` refuses 409 before queueing (`agent_trigger.py:1527`); the
  divergence restaff (`run_divergence.py:483`) follows a review that ran, and evidence rows are never
  deleted. The only queue sites setting `review_task_id` are `agent_trigger.py:1582`,
  `run_divergence.py:483` and `scheduler.py:3417`, `:3747`. A pre-2026-08-28 leftover is given up
  after `DELIVERY_ATTEMPT_LIMIT = 3`. "Queued as Q4-SPEC" is a historical label; no such change
  exists. Optional: one regression test that an evidence-less review selection queues no entry (no
  existing test asserts the queue stays empty).
- **Candidate new finding (not filed):** `list_evidence`'s `document` argument is silently ignored
  when no `identifier` is given (`agent_actions.py:1248-1275`), so a call passing a document id
  "succeeds" by not reading it. Surfaced by R1 while re-verifying F363; worth filing on its own.

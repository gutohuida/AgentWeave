# B6 — Spec-flow drift, corpus writes and audit reads

**Findings:** F129, F132, F216, F217 · F206, F208, F205 · F211, F169 · F213
**Decisions:** F217 drift branch basis (moved out of D13); two raised in R1 — what unwatched evidence watches (sub-question of F217), and who authors a capability document (F206's merge route); plus one small product call per change (demotion reason, proposal clean-up shape)
**Changes:** `openspec/changes/drift-watches-the-files-its-evidence-is-about/`, `openspec/changes/drift-is-scanned-and-answered-on-the-document/`, `openspec/changes/the-corpus-is-indexed-arranged-and-adopted-from-the-app/`, `openspec/changes/a-documents-rigor-history-and-retired-requirements-are-on-screen/`, `openspec/changes/a-pending-proposal-can-be-withdrawn/`
**Status:** R2 done

## R1 — explore and propose (2026-09-24)

Read on HEAD `404c7d5` (the worktree's branch head `ce086b6` adds only the bundle scaffolding).
Sources: `hub/hub/api/v1/spec.py`, `hub/hub/requirement_evidence.py`, `hub/hub/requirement_gate.py`,
`hub/hub/spec_service.py`, `hub/hub/spec_documents.py`, `hub/hub/spec_adoption.py`,
`hub/hub/spec_rigor.py`, `hub/hub/worktrees.py`, `hub/hub/api/v1/agent_trigger.py`, the spec UI
(`hub/ui/src/api/spec.ts`, `components/spec/*`, `main.tsx`), the three touched main specs, and the
other bundles' change directories as they appeared during this round. One throwaway test measured
the whole-tree footprint (below); it was deleted.

### Each finding, re-verified

| Finding | Slice | Verdict on `404c7d5` | Evidence |
|---|---|---|---|
| F129 (B) | b | **Still open** | `grep -rn "spec/drift" hub/ui/src` → nothing. `POST /spec/drift/detect` (`spec.py:956-974`) is still the only writer of `RequirementDrift`; no agent route or MCP tool reaches drift. |
| F132 (C) | b | **Still open** | `requirement_gate.py:60-62` remedy *"resolve the drift candidate"*; `resolve` still has no client; `detect_drift` still skips evidence with an open candidate (`requirement_evidence.py:1131`). |
| F216 (C) | b | **Already fixed** | `a7b2df1` (Round 4c): `list_drift` carries `created_at`, `requirement: {identifier, document}`, `evidence: {summary, locator, actor, actor_kind}` (`spec.py:1000-1058`). The drift change reads these fields. |
| F217 (C) | b | **Still open; decided here as a recommendation** | `detect_drift` compares against `footprint.branch` (`:1140-1152`); the docstring's deferral is unchanged (`:1089-1095`). Task release keeps the branch (`worktrees.py:987-993`), so the comparison keeps succeeding and finds nothing. |
| F206 (B) | c | **Still open** | None of `spec/reindex`, `documents/arrange`, `spec/adopt`, `documents/adopt`, `…/merge` appears in `hub/ui/src`. |
| F208 (D) | c | **Already fixed** | `568f868` (Round 4b): the arrange 409 names `POST /spec/reindex` and the `home` input (`spec.py:1338-1348`); `test_a_refusal_says_what_would_work.py`. |
| F205 (C) | c | **Already fixed** | `58477dc` (UI-1): `SpecPhaseBar.tsx:146-160` offers Archive on `exploring`/`proposed`; the dialog shows the refusal. |
| F211 (C) | d | **Still open** | `rigor-history` and `spec/requirements` have zero hits in `hub/ui/src`. |
| F169 (C) | d | **Already fixed** | `58477dc` + `f473510` (UI-1 and its review): `TaskDetailDrawer` renders `approval_report`; `taskApprovalReportAndPending.test.tsx`. |
| F213 (D) | single | **Still open** | `propose_edit` (`spec_service.py:290-399`) diffs against stored content only; no withdraw route (`spec.py:590-694`). |

### What R1 found beyond the ledger

These are not in `FINDINGS.md` (R1 may not write it). The orchestrator should decide whether to file
them.

1. **A footprint watches the whole tree** (the defect `read_footprint`'s docstring defers,
   `requirement_evidence.py:519-522`; F215's entry measured 44 candidates from one commit). Measured
   in R1: operator evidence with `locator: "ledger.py"`, then a commit changing only `other.py` →
   one candidate with `observed: {"other.py": …}`. **Carried** in the first drift change, because
   making drift reachable without it delivers a flood.
2. **Rejecting a proposal in the app leaves it on screen.** `reject_proposal_route` broadcasts
   nothing (`spec.py:669-694`), `useSpecMutation` does not invalidate `specProposals`
   (`api/spec.ts:244-256`), and `refetchOnWindowFocus` is off (`main.tsx:8-15`); the comment at
   `api/spec.ts:164-166` claims reject broadcasts. Read, not driven. **Carried** in the proposal change.
3. **The app records every rigor change with an empty reason.** The Enforcement select posts on
   change with no reason (`SpecPhaseBar.tsx:62-70`, `:184`), while the spec requires the reason to be
   recorded (`spec-document-authority/spec.md:959-962`). **Carried** in the audit change.
4. **A drift candidate can be answered twice**; the second answer overwrites the first and its
   fingerprint (`resolve_drift`, `requirement_evidence.py:1190-1222`, no state check). **Carried** in
   the second drift change.
5. **Only one unit of a multi-unit proposal can be accepted without a resubmission.** Accepting one
   changes the digest, and every sibling is refused as stale on its accept
   (`test_accepting_a_second_proposal_against_the_same_digest_is_refused_as_stale`). The spec calls
   proposals *"individually acceptable"* (`spec-document-authority/spec.md:1080-1086`). The proposal
   change's supersession makes the agent's resubmission replace the stale siblings, but does not
   change the compare-and-swap. **Not carried** — a design question for the operator.
6. **Reindex and arrange write files before the database commit** (`spec.py:1255-1265`,
   `:1375-1379`): an `OSError` mid-rerender leaves files written and digests rolled back. **Not
   carried**; flagged for R2 in the corpus change's D6.
7. **A drift candidate is never superseded when its requirement is reworded**, though the model
   comment says it is (`models.py:2643-2644`). **Not carried**; noted in the first drift change.
8. **A capability document has no authoring path in the product**: an agent's write is refused
   (`spec_service.py:159-162`) and the operator's `PUT …/content` (`spec.py:470-532`) has no client.
   This is why merge is a decision, below.

### Decisions

**F217 — the drift branch basis** (moved here from D13). Options, with what the code says about each:

| | Option | What it breaks | What it releases |
|---|---|---|---|
| A | Keep the branch basis; state the asymmetry on the drift surface and in the spec | nothing | honesty only; agent evidence is never watched after merge, and the whole-tree flood stays |
| B | Switch to `main` once `reachable_from_main`, whole tree | floods — an agent commit's whole tree differs from `main` in every file anyone else merged | agent evidence watched, drowned |
| **C** | **Narrow the footprint to the files the evidence is about, then switch to `main` once reached** | evidence that names no file and changed nothing off `main` watches nothing (today it watches everything); tests relying on the whole tree are rewritten; one migration | agent evidence watched where the product is; one unrelated commit raises nothing |

**Recommended: C.** The docstring's objection to switching (*"drift would flip bases underneath an
open candidate"*) does not survive narrowing: `reachable_from_main` only moves to `True`
(`refresh_reachability`'s filter, `:1028`), an open candidate is skipped before any comparison
(`:1131`), and a resolution's fingerprint is `{path: {was, now}}` (`:1221`), which a clean merge
reproduces exactly on `main`.

**Sub-question raised in R1 — what does evidence that names no file watch?** Recommended: nothing,
listed as unwatched with the reason and the remedy (name a file or commit in the locator). The
alternative (the whole tree, as today) keeps the flood for exactly that evidence. No operator loses a
signal: nothing reaches drift today.

**Legacy footprints** (recorded before the change, whole tree): recommended not scanned, listed as
`recorded_before_watching`. Same reasoning.

**F129's scan cadence**: recommended manual (F129's own argument, and `spec.py:963-964`).

**F206's merge route — who authors a capability document?** Options: M1 leave merge API-only for now
(recommended); M2 a payload editor dialog (rejected: the operator hand-editing JSON inverts the
product's premise); M3 agents draft capability content that always lands as proposals, the merge
recorded as the operator's on accept (the likely eventual answer; changes *"capability documents are
written by the operator"* to *accepted by*, so it wants its own exploration).

**Smaller product calls, one per change:** a demotion in the app requires a reason (recommended
yes); proposal clean-up is W3 — de-duplicate and supersede at submission, plus an operator
withdraw (recommended over either alone).

The decisions interact in one place: F217's answer and the unwatched sub-question decide what the
second drift change's strip shows. Under A the strip also says agent evidence is watched only on its
branch; under C it does not need to.

### Changes written

All five pass `openspec validate <name> --strict`. Order: 1 before 2 (hard dependency); 3, 4, 5
independent of each other and of 1–2. Stopping after any one leaves complete work.

| # | Change | Findings | What it does | Size |
|---|---|---|---|---|
| 1 | `drift-watches-the-files-its-evidence-is-about` | F217 + whole-tree defect | `watched_files` (locator path, named commit's diff, branch diff since leaving `main`); `EvidenceFootprint.watched_from` + migration; basis = `main` once reached; `unwatched` in `GET /spec/drift`; `footprint_view` reports what is watched. Backend only | M |
| 2 | `drift-is-scanned-and-answered-on-the-document` | F129, F132 (reads F216's fields) | `SpecDriftPanel` on the document: open candidates, three answers, Scan, unwatched line; `document`/`state` filters; answer-once 409; detect/resolve broadcast; gate remedy names the operator and where | M |
| 3 | `the-corpus-is-indexed-arranged-and-adopted-from-the-app` | F206 (4 of 5 routes) | Corpus strip in the document browser (index state, Rebuild with the home question, Adopt all); Adopt on untracked tree rows; Place under… on the document; reindex broadcasts | M |
| 4 | `a-documents-rigor-history-and-retired-requirements-are-on-screen` | F211 | Rigor history under Enforcement; a rigor change is confirmed with a reason, required for demotion; retired requirements listed with the work still linked (the detail route's caller). UI only | S |
| 5 | `a-pending-proposal-can-be-withdrawn` | F213 + reject-refresh defect | Repeat creates nothing (`already_pending`); a revision supersedes the same proposer's earlier one; operator Withdraw route and button; twins marked; reject/withdraw broadcast and invalidate | S–M |

Tests planned in each `tasks.md` group 1, each marked with why it fails today. UI tests that read a
route's payload use the route's order: drift (`created_at` ascending), rigor history (ascending,
reversed by the component on purpose), requirements (`identifier` string sort, re-sorted numerically
on purpose), proposals (`created_at` ascending, twin marker compares against earlier rows). Every
change's design has a *what each route answers when what it calls raises* section.

### Cross-bundle collisions (for the orchestrator and R2)

- **B5 `a-footprint-names-the-line-of-work-its-commit-is-on`** (F165/F166) edits the same functions
  as change 1: `_take_footprint`, `read_footprint`, `restamp_run_footprints`, `footprint_root`. The
  two are semantically independent (B5's is *which branch string*, B6's is *which files*), but they
  touch the same lines and both feed `detect_drift` (B5's fix gives review-checkout footprints a real
  branch where today they read `"HEAD"` and are skipped). Implement them in sequence; the second's
  R-round must re-read against the first.
- **B5 `the-coverage-bar-takes-the-evidence-decision-it-asks-for`** (F215) owns the evidence list for
  active requirements and edits `SpecCoverageBar.tsx`. Change 2 touches one string there; change 4
  deliberately does not re-list active requirements' evidence (its preamble), and gives
  `GET /spec/requirements/{identifier}` a caller only for retired requirements, which nothing else
  shows. B5's proposal already lists F211 and drift as B6's.
- **B5 `a-document-moves-forward-only-through-its-checks`** (F207) edits `SpecPhaseBar.tsx`'s Approve
  path; change 4 edits its rigor control. Different regions, same file.
- F215's entry carries the 44-candidate measurement; the fix is change 1, not B5.

### Not resolved in R1

- Whether items 5 and 6 of *What R1 found* deserve findings of their own.
- The reject-refresh defect (item 2) was read, not driven; change 5's task 1.11 is the test that
  would confirm it with a real query client.

## R2 — independent comparison (2026-09-24)

Re-derived on the worktree head (product code unchanged since `404c7d5`). About 45 load-bearing
claims checked against the code. All five changes still pass `openspec validate --strict`.

### R1's "already fixed" verdicts: all four stand

| Finding | Checked | Verdict |
|---|---|---|
| F216 | `list_drift` returns `created_at`, `requirement {identifier, document}` and `evidence {summary, locator, actor, actor_kind}` (`spec.py:977-1058`). `a7b2df1` is an ancestor of HEAD; the ledger says fixed | fixed |
| F208 | The arrange 409 names `POST /spec/reindex` and the `home` input (`spec.py:1338-1348`). `568f868` is in HEAD; the ledger says fixed | fixed |
| F205 | Archive is offered on `approved`, `exploring` and `proposed` (`SpecPhaseBar.tsx:146-160`). `58477dc` is in HEAD; the ledger says fixed | fixed |
| F169 | `TaskDetailDrawer` keeps `approval_report` from both approving paths (`:366`, `:406`), and `taskApprovalReportAndPending.test.tsx` exists. `58477dc` and `f473510` are in HEAD; the ledger says fixed | fixed |

### Claims that disagreed with the code, and what changed

| # | Change | R1 claim | Code | Fix |
|---|---|---|---|---|
| 1 | drift-watches | `refresh_reachability` is called only by `POST /spec/drift/detect` | It is also called after every in-app integration that merged (`task_transition_service.py:908-913`) | Context corrected. This helps: a merge made by the product flips the basis with no scan |
| 2 | drift-watches | Reason 1 for a safe flip is that reachability "only moves one way" | `restamp` can write `False` over `True` (`requirement_evidence.py:966-976`), but only together with a new commit and new `entries` | Restated as "at most one flip per baseline". The conclusion holds |
| 3 | drift-watches | D3 compares against main when reachable, else as today. The order was unstated, and the MODIFIED spec said a footprint naming no line of work "SHALL raise nothing" | A reachable footprint with `branch == ""` (a detached review checkout, or a non-tip commit) would be skipped before the basis check | D3 checks reachability first. Spec sentence rewritten; a scenario and task 1.12 added |
| 4 | drift-watches | `record` takes `main_branch` from "the route" | There are two callers: `spec.py:833` and the agent's `agent_actions.py:1207` | Task 2.2 names both |
| 5 | drift-watches | `unwatched` has two reasons | Evidence with no footprint row is inner-joined out of `detect_drift`, and neither reason lists it | Third reason `no_footprint`, task 1.11, and change 2's D3 remedy text |
| 6 | drift-watches | Task 1.7 "FAILS today only if…, record which" | It passes today: branch basis, same `moved`, matching resolved fingerprint | Made a control that asserts the main basis is in use |
| 7 | corpus | The home select comes from `useSpecDocuments` | `build_index` files only documents that are on disk **and** tracked (`spec_documents.py:259-269`). A row whose file is gone gets `home_missing`, and the question loops | Select from `useSpecList` entries that have a `document_id`, plus a "nothing to index" answer; task 1.10 |
| 8 | corpus | Adopt, then shape the corpus | Nothing in `spec_adoption.py` writes `spec/index.json` (`write_index` has two callers, reindex and arrange). An adopted document stays unindexed, and arrange answers 404 for it | After an adopt, the strip leads with Rebuild; task 1.11 |
| 9 | proposals | Only a reject leaves a stale row | An accept refused as stale **commits** `stale`, then answers 409 (`spec.py:642-649`) with no broadcast. `useSpecMutation` invalidates only on success. Same defect | D5: the refusal path broadcasts and mutations invalidate on settled; task 1.13 |
| 10 | proposals | The twin marker compares `proposed_payload` | Key order in a JSON column is not guaranteed | Compared by a key-sorted serialisation |

### What stands (checked, agreed)

- **Change 1's premises.** Footprints cover the whole tree (`read_footprint` `:539`, restamp `:969`), and `detect_drift` is the only reader of `entries`. Reasons 2 and 3 for a safe flip hold: open candidates are skipped, and the fingerprint is `{path: {was, now}}`. Integration is a real `git merge` (`task_integration.py:517`), so `is_reachable_from` becomes true after it.
- **F217: C is still recommended.** The options table is complete: A leaves the flood and B floods. The sub-decisions are consistent with C and with each other: evidence that names no file watches nothing and says so, and legacy rows are not scanned.
- **Change 2.** `resolve_drift` has no state check (`:1190-1222`). The resolve route hard-codes 422 (`spec.py:1078-1081`), so task 2.1's `http_status` mapping is needed. `spec_updated` with `path: null` is already sent by `spec/adopt` (`:1415`), and both UI consumers guard on it (`api/spec.ts:168`, `:196`).
- **Change 4.** Every route's shape, order and 404/422 path is as stated (`spec.py:376-398`, `:536-559`, `:738-819`; `spec_rigor.py:181-187`). The app sends `reason: ''` on every rigor change (`SpecPhaseBar.tsx:62-70`; `api/spec.ts:305`).
- **Change 5.** `propose_edit` never reads pending proposals (`spec_service.py:290-399`). Reject does not broadcast (`spec.py:669-694`). The `useSpecEvents` comment claiming it does is false (`api/spec.ts:163-167`).
- **Unchanged recommendations.** Merge M1, a required reason for demotion, and W3.

### Collisions with B5

- **`a-footprint-names-the-line-of-work-its-commit-is-on`.** Change 1's design now has a
  **"Builds on B5"** section; B5 lands first. Change 1 assumes three pieces of B5:
  - **D1:** `""` is the only spelling of "unknown", and the `"HEAD"` guard stays for one release.
  - **D2:** `read_footprint(root, *, at, task_branch)` and `line_of_work`. Change 1's keyword-only
    `locator`, `actor_kind` and `main_branch` go beside `task_branch`.
  - **D3:** a run directory that is gone is read at the task tip in `workspace.root`. Rule 3's
    `merge-base` still answers there.

  B5's D4 does not interact. B5 asked for a flood check on its named-branch operator footprints, and
  change 1 is what answers it: those footprints watch only their files, and once reachable they
  compare against main anyway. Both changes take the next free migration revision (B5 names `0106`).
- **`SpecCoverageBar.tsx` / `useSpecEvents`.** Change 2 edits one string in the coverage bar. B5's
  F215 change adds a `specEvidence` invalidation to `useSpecEvents`, next to the lines that changes
  2, 4 and 5 add. All the additions are independent lines. Noted in change 2's Risks.
- **`SpecPhaseBar.tsx`.** B5's F207 change edits the Approve `onError` (around `:136-145`). Change 4
  edits `onRigor` and the select (`:62-92`, `:178-215`). Different regions. Noted in change 4's Risks.

### What each changed route returns when what it calls raises

Each design's section matches the code. The one added case is accept's stale-refusal path (row 9).

The **reindex/arrange ordering hazard is confirmed**: `write_index` is a plain `write_text`
(`spec_documents.py:345`), `rerender_corpus` calls `write_document` without a try
(`spec_service.py:881`), and the commit comes afterwards (`spec.py:1265`, `:1379`). An `OSError`
therefore gives a 500 with files written and digests rolled back. It is recorded as a candidate and
not carried.

### Candidates, not filed (R1's eight, plus two from R2)

1. A footprint watches the whole tree. Carried in change 1.
2. A reject in the app leaves the row on screen. Carried in change 5.
3. The app records every rigor change with an empty reason. Carried in change 4.
4. A drift candidate can be answered twice. Carried in change 2.
5. Only one unit of a multi-unit proposal can be accepted without a resubmission, because the compare-and-swap covers the whole document. Not carried: an operator design question.
6. Reindex and arrange write files before the DB commit. Confirmed by R2; not carried.
7. A drift candidate is never superseded when its requirement is reworded (`models.py:2643-2644`). Not carried.
8. A capability document has no authoring path (the merge decision). Not carried.
9. **(R2)** An accept refused as stale leaves the row on screen. Carried in change 5 with item 2.
10. **(R2)** Evidence with no footprint row is never scanned and is not reported. Carried in change 1 (`no_footprint`).

# B4 — Permission judging

**Findings:** F362, F403, F402, F401, F283, F230, F284
**Decisions:** D4, D5
**Changes:** `openspec/changes/the-shell-judge-reads-a-word-whole/`, `openspec/changes/a-drive-or-a-home-variable-names-a-directory-by-itself/`, `openspec/changes/the-permissions-pill-shows-the-posture-the-run-gets/`, `openspec/changes/an-ask-me-card-says-what-workspace-only-would-decide/`
**Status:** R1 done | R2 done | R3 done | R4 done — operator review 2026-09-24: 2 approved, 2 revising; R4 revised both, awaiting an independent R5

## R1 — explore and propose (2026-09-24)

**Method.** Read `CLAUDE.md`, `spec-queue/tracks/README.md`, ROUNDS.md's D4/D5/S3/S10 rows, every
finding's full entry in `scripts/drive/FINDINGS.md`, `DECISIONS.md` f375-* (2026-09-21 late evening),
the archived `2026-09-22-a-word-without-a-separator-can-still-leave` (the F375 precedent) and
`2026-09-13-a-url-is-not-a-path` (whose D3, D8 and D10 decided or left open most of the rows moved
here), the `agent-run-sandboxing` and `agent-configuration` main specs, `.claude/rules/mcp-server.md`
and `db-migrations.md`, and the code at `ce086b6` (HEAD `404c7d5` plus the bundle-opening commit,
no product change). The two unarchived changes (`an-at-mention-an-agent-wrote-reads-no-file`,
`pressing-run-names-the-reason-that-held`) touch none of this.

Measured three ways: `hub.mcp_server._decide` in-process with `AW_WORKSPACE_DIR` a scratch
`…/Temp/b4ws` (Windows, `py -3.11`); real Git Bash 5.2.37 (`globskipdots on`); and a **prototype** of
the new rule 6 as an out-of-repo pytest plugin swapping `_judge_word` after collection (session
scratch, `/tmp/b4proto/b4plugin.py`; nothing in the repo edited), run over the eight test files that
reach the judge and over a 76-command sweep.

### Findings re-verified

| Finding | State at `ce086b6` | Evidence |
|---|---|---|
| **F362 (B)** | **open, changed shape** | Still refuses `ls test/*.test.js` (`'/*.test.js'`), `2>/dev/null`, `npm install @types/node` (`'/node'`), `git show HEAD:src/a.py`, `sed -E 's/(foo)/\1/'`, `python -c 'print(1/2)'`, `mkdir -p src/{a,b}`, PowerShell `Get-ChildItem src\*.py`. Cited lines moved: `_PLAIN_RELATIVE_RE` is `mcp_server.py:1038-1042` (was `:966`), `_ABSOLUTE_PATH_RE` `:1023-1026` (was `:957-960`), rule 6 `:1261-1267`. **Not Windows-only**: the POSIX branch of the regex also opens a candidate at every `/`. The `'/script'` item (`grep '</script>'`) has a different mechanism (the `<` trim then rule 5) and stays refused on purpose |
| **F403 (B)** | **open** | `cp notes.md .{,.}` and `{.,.}.` allowed; Git Bash expands them to `. ..` / `.. ..`, and `cp notes.md .{,.}/` put the file in the parent. `_lex` (`:1397-1481`) has no brace handling. **New:** `.{,.}/x` and `src/{a,..}/../y` are refused today only by rule 6's false tail reading; the F362 fix alone would let them through (prototype: both flip to allow). So F403 must ship with or before F362 |
| **F402 (B)** | **open** | `Copy-Item notes.md Z:` allowed: `_words` (`:1484-1499`) trims `:` (`_WORD_TRIM`, `:1054`), judged word `Z`. Also `Z:foo` and `-Destination:Z:` (value trimmed to `Z`). `_where("Z:")` → outside; `_where("C:")`, `_where("C:foo")` → inside for a C: workspace, which is PowerShell's meaning |
| **F401 (B)** | **open** | `cp notes.md $HOME`, `"$HOME"`, `${HOME}` (reaches the judge as `${HOME`), `--target-directory=$HOME`, PowerShell `$env:TEMP`, `-Destination:$HOME`, and `..$x` all allowed; rule 3 is gated on a separator (`:1241`). `git commit -m "$(cat <<'EOF' …)"` lexes to the single word `$(…` — the fact that rules out refusing every bare expansion |
| **F283 (B)** | **open** | Catalog `default="acceptEdits"` on both providers (`model_catalog.py:231`, `:271`); `DEFAULT_PERMISSION_MODE = "acceptEdits"` (`:344`) imported nowhere; spawn default `workspace` (`runner_commands.py:74`, `:238-242`). Pill falls to `control.default` (`ComposerModelControls.tsx:140`, `AgentOutputPanel.tsx:393-395`). **Also** the settings select hard-codes "Built-in default (Edit files)" (`AgentSettingsControls.tsx:199`), and two tests pin the wrong display (`test_permission_approver.py:1051-1052`; `composerPermissionDefault.test.tsx:132-136`). The main spec's *"The default is unchanged"* scenario (`agent-run-sandboxing`) has been false since `72afb3c` |
| **F230 (C)** | **open** | `approve_tool_call` routes `manual` to `_ask_operator` without `_decide` (`:1700-1707`); `PermissionRequestResponse` (`permissions.py:31-45`) and the row (`models.py:1592-1626`) carry no boundary |
| **F284 (C)** | **open** | Same card; `describe`/`requestKind` (`PermissionRequestCard.tsx:21-59`) show only tool and path |

None already fixed; none retired.

### Decisions

**How D4 and D5 interact.** D4's first half decides who the shell judge serves. With the default at
`workspace` (as the spawn already is) the judge decides **every** unattended Claude command, so its
false refusals cost every agent (F362's 262), and a stricter D5 would cost every agent too. D4's
second half puts the judge's answer on the "Ask me" card, so the judge must not *overclaim* either:
an allow over `cp x $DEST` must not read "inside". The three recommendations below are built to be
one position: **default `workspace`; fix the judge's false refusals first; refuse only bare
expansions that name a directory by themselves; show the judge's answer on the card as advice, with
an allow reason that admits what it did not check.**

**D4 (first half) — one built-in default.** Options: (a) `workspace` for Claude, the display derived
from the spawn; (b) `acceptEdits` — Bash then prompts headlessly with nobody to answer, so unattended
runs edit but cannot execute (`runner_commands.py:22-28, 62-73`; 14/14 tests under `workspace` vs
none, 2026-08-13); (c) `workspace` for Codex too — would accept a Codex sandbox escalation whose
`cwd` is inside (`codex_appserver.py:280-283`) where today the default declines it (`:291`),
unmeasurable since Codex is undrivable. **Recommended (a)**, Codex's display kept truthful at "Edit
files". Built as `the-permissions-pill-shows-the-posture-the-run-gets`: one `posture_at_rest`
function read by `build_command` and served per agent as `permission_mode_at_rest`.

**D4 (second half) — does the manual card show the workspace verdict?** Options: (a) no; (b) yes, the
approver's own `_decide` answer, advisory; (c) "Ask me" auto-refuses outside calls (a new posture,
contradicts *"the operator is the boundary where no posture is checking"*); (d) Hub-side
classification from `Run.workspace_dir` (cannot read shell; a second opinion). **Recommended (b).**
Built as `an-ask-me-card-says-what-workspace-only-would-decide`. It found a real hazard:
`PermissionRequestCreate` is `extra="forbid"` (`schemas/common.py:32`), and `:8000` spawns the new
approver before its Hub restarts, so a new field would 422 and deny **every** "Ask me" card until the
restart. The design retries once without the field on a 422 (no row exists after a validation 422,
so no duplicate card).

**D5 — how strict about bare `$VAR` / `$(…)`?** Options: (a) accept the disclaimer; (b) refuse every
separator-less expansion — refuses `echo $x`, loops, and every Claude Code commit (the heredoc's
`$(…` word); (c) command-aware destination positions — a new layer in a word-based judge, defeated by
`bash -c`; (d) refuse a bare reference to the variables that name a home, working or temp directory
(`HOME`, `PWD`, `OLDPWD`, `USERPROFILE`, `TMPDIR`, `TMP`, `TEMP`, `APPDATA`, `LOCALAPPDATA`; bash
`$N`/`${N}`, PowerShell `$HOME`/`$PWD`/`$env:N`, `%N%`) and a `..` directly before an expansion, as
uncheckable, leaving everything else allowed and stated as unjudged. **Recommended (d)**: it is F375's
`~` rule applied to the shorthand's other spellings (`$HOME` = `~`, `$PWD` = `~+`, `$OLDPWD` = `~-`),
and it costs nothing an agent needs. Built in
`a-drive-or-a-home-variable-names-a-directory-by-itself` together with F402 (same rule 4, same
requirement).

### What was written

| Change | Findings | What it does |
|---|---|---|
| `the-shell-judge-reads-a-word-whole` | F362, F403 | Bash brace expansion modelled in the lexer (bounded, refused as uncheckable past 256); rule 6 judges pieces split at shell metacharacters, quotes, `@`, `:`, `(` and a leading glued option, plus the quote-removed word — never a name's tail; a glob component that can match `..` is judged as `..`; `/dev/null` and the std streams allowed in bash; `user@host:` and `host:port/` refused as network. Prototype (without the brace step): exactly 9 rows move in 455 tests — X4/X5/X6 to allowed, N5/N6/G3/E11/Z1/Z2 refused with a truer reason; H11 (F301's `python -c`) needed the quote break, E11 the quote-removed reading. Modifies the shell-path requirement (its "reading from the middle" sentence) and adds two |
| `a-drive-or-a-home-variable-names-a-directory-by-itself` | F402, F401 | PowerShell keeps a drive word's colon and judges `Z:`, `Z:foo`, `-Destination:Z:`, `Temp:`; D5 (d) in rule 4. Modifies the separator-less requirement |
| `the-permissions-pill-shows-the-posture-the-run-gets` | F283 | `posture_at_rest`; `permission_mode_at_rest` on the agents list; pill and settings read it; Claude catalog default `workspace`; dead constant deleted. Modifies `agent-configuration`'s default-posture requirement and restates `agent-run-sandboxing`'s stale default requirement. UI bundle |
| `an-ask-me-card-says-what-workspace-only-would-decide` | F230, F284 | Approver sends `_decide`'s verdict (Codex: `_within`), stored in a new column (migration 0106), shown on the card as advice; the allow reason admits unchecked expansions; 422 retry for an un-restarted Hub. Adds one requirement |

All four pass `openspec validate --strict`. Each is independent except the first, which must not be
split (its brace step prevents a regression of its rule-6 step). Tests planned in each `tasks.md`
name the files and why each fails today; the card's UI test uses the list route's newest-first order
(`permissions.py:73`) and asserts DOM order.

**Every changed route, when what it calls raises:** `approve_tool_call` has no `try` around
`_decide` (`:1708`), so every judge addition is string work, `fnmatch` and `_judge_path` (total) — with
a totality test (brace/bracket bombs). The agents list's new field is a pure mapping (unknown →
`null`). `open_permission_request` stores the verdict; a malformed one is a 422, which the approver
retries without the field.

### Unresolved, for R2/R3

- `fnmatch.fnmatchcase` and the brace expander must be confirmed total on pathological input
  (recursion depth on `{{{{…`).
- Change 1's D5 widening: `scp a host:x/y` becomes allowed (consistent with `a-url-is-not-a-path`'s
  N3); R2 should weigh refusing `name:relative/path`.
- Change 1's device set: four names, or `/dev/null` only (the only one F362 measured).
- Change 3 keeps the requirement name *"Introducing an enforced posture does not change existing
  runs"* while its text now states the default; a RENAMED delta may be cleaner.
- Migration number `0106` is also claimed by three other bundles' changes
  (`a-checkpoint-is-handed-over-once-and-says-where-it-went`, `a-footprint-names-the-line-of-work-its-commit-is-on`,
  `worker-spend-counts-against-the-budget`); renumber at build.
- B11's `an-agents-tool-server-is-the-one-its-hub-loaded` (F354) would make a run's MCP server the
  copy its Hub loaded. If it ships first, the card change's 422 retry (the un-restarted-Hub window)
  is no longer needed, and every "reaches `:8000` on edit" note in all four changes becomes "on
  restart". R2 should check the order.
- `hub_client` is read from agent config in the list route by the same key the trigger uses
  (`agent_trigger.py:1056`) — R2 should confirm `agent_meta` carries it for runner-bound agents
  (`agents.py:534`).
- F362's "68 `/`-rooted fragments" were not re-derived individually (the `:8000` transcripts were
  not reopened); the sweep covers the shapes the finding names.

## R2 — independent comparison (2026-09-24)

**Method.** Code at worktree HEAD `115f740` (no product change since `ce086b6`). 31 load-bearing
claims opened at their `file:line`; five measured in-process (`_decide`/`_words` with
`AW_WORKSPACE_DIR` a scratch dir, `py -3.11`), `fnmatch` totality measured, and bash brace
behaviour checked in Git Bash 5.2.37. No pytest run was needed. R1's prototype was not re-run; its
regressions were re-derived by reading rule 6 against the proposed piece split.

### Claims that disagreed with the code, and what changed

| # | Claim (change) | What the code / a measurement shows | Changed |
|---|---|---|---|
| 1 | Expanding *unquoted* braces is enough to keep brace escapes refused under the new rule 6 (`the-shell-judge…` D1) | A quoted pattern handed to an inner shell is never expanded (`_lex` recurses only into `$(…)`), is split at its commas by `_words`, and is refused today only by the tail reading: `bash -c "cp n .{,.}/x"` and `sh -c 'cp n {,..}/x'` → deny `'/x'` (Bash tool), the same from the PowerShell tool. The new rule 6 reads `.}/x` as a name inside → **allowed**. `bash -c 'cp n .{,.}'` is allowed today (F403 one level down) | D1 gains an **inner-shell brace reading**: an argument with a literal brace is also judged as expanded, the brace counterpart of D2's quote-removed reading. Spec scenario "A quoted brace is literal" replaced by two (a quoted brace is judged as expanded; harmless quoted braces stand). Task 1.3 rewritten, 2.1b added. Cost named: a file literally named `.{,.}` is refused |
| 2 | A letter sequence `{x..y}` can be replaced by one representative (D1) | Bash's letter range runs over ASCII codes: `echo {Z..a}` prints `Z [`, a backslash, `] ^ _`, a backtick, `a` (measured). The backslash is a Windows separator, so `{Z..a}..` has the alternative `\..`, which the judge refuses today and a representative `Z..` would allow | Integer sequences keep the representative; letter sequences expand in full (at most 58). Task 1.6 row |
| 3 | The `:` piece break is safe (D2 step 3) | PowerShell `Copy-Item x Z:foo\bar` and `-Destination:Z:foo\bar` are refused today by the tail `\bar` (measured). Split at `:` they become `Z` + `foo\bar`, both inside → **a write to another drive allowed** | In the PowerShell reading a `:` after a lone leading letter is not a break; the piece `Z:foo\bar` resolves on drive Z. Task 1.7 rows (Windows). Change 2's design cross-references it |
| 4 | Totality over "5000 `{`" is a test concern only (D1) | A recursive expander passes Python's 1000-frame limit; `approve_tool_call` has no `try` (`:1708`). **If the judge raises**: FastMCP returns a tool error, never the allow string (`:1710-1711`), so the call is *not allowed*, but no reason reaches the model and `_report_decision` (`:1709`) never runs | D1 requires an iterative expander or a depth cap; new **D6**: `approve_tool_call` catches a judge exception and denies with a reason, reported like any refusal, with no return annotation (the `.claude/rules/mcp-server.md` trap). Proposal step 5, task 2.2b |
| 5 | 256 alternatives per argument bounds the cost (D1) | Each alternative costs a `realpath` (twice with `continues`, `_judge_path` `:1142-1158`), and bash is read twice for `$'…'` (`:1580-1583`); 100 arguments × 256 is ~50,000 `realpath` calls | Added a 1024 per-command bound |
| 6 | `-Destination:Z:` judges `Z:` under the proposed `_words` trim (change 2 D1) | Measured `_words` → `-Destination:Z`; the piece does not fullmatch `_PS_DRIVE_RE`, so the old trim applies and rule 4 sees `Z` | D1 now keeps the colon when the piece is `_COLON_OPTION_RE` + a drive; task 2.1 says so |
| 7 | A directory variable is "fullmatched and not followed by a name character" (change 2 D2) | Self-contradictory; a fullmatch misses `cp x $HOME.bak` and `$PWD..` (siblings of home and of the workspace, separator-less, allowed today by rule 4 `:1243-1258`) and `${HOME-x}` | Prefix match not followed by a name character, plus `${NAME` with any operator; spec text and scenario updated; task 1.4 rows |
| 8 | The list route reads `hub_client` "as the trigger does" (change 3 D2) | The trigger's `config` is `launchability.get_agent_config` (`agent_trigger.py:718`), which adds the **session-wide `hub_client` fallback** (`launchability.py:476-479`); the list route merges only the per-agent entry over `Agent.config` (`agents.py:533-534`). A project with a top-level `hub_client: "cli"` would show `workspace` over runs spawned `acceptEdits` | Extract `effective_hub_client(meta, session_data)` for both; task 1.3 row, task 2.3 |
| 9 | Keep the requirement's name (change 3, R1's open item) | *"Introducing an enforced posture does not change existing runs"* states the opposite of the new text; nothing outside `openspec/specs` cites it | RENAMED to *"The built-in posture a run receives is the posture shown for it"* (RENAMED + MODIFIED; `--strict` passes) |
| 10 | Computing the verdict in `_ask_operator` is safe (change 4 D1) | `_ask_operator`'s only `try` is around the request (`:1636-1647`); a raising `_decide` would stop the operator being asked | The verdict is computed under its own `try`, `None` on failure; task 1.3b |
| 11 | The Codex verdict restates `_within` (change 4 D1) | Two restatements of one check can drift | `decide_approval`'s workspace branch calls the same helper; equivalence test in task 1.5. Only the sandbox methods reach the operator (`codex_appserver.py:274-277`) and the Codex row is written directly (`agent_trigger.py:2793-2807`), so it has no 422 hazard |

### Asked of R2, answered

| Question | Answer |
|---|---|
| Are the brace expander and `fnmatch` total on adversarial input? | `fnmatch.fnmatchcase("..", p)` measured total on 3.11 (unbalanced `[`, `.[!]`, `.[z-a]`, `.[` + backslash, 5000-star patterns in 0.035 s or less). The expander is not written yet; the design now requires it to be iterative or depth-capped (row 4) |
| What does the judge return if it raises? | Not an allow, but a silent tool error with no reason and no record; D6 makes it a reported deny (row 4) |
| Order against B11's `an-agents-tool-server-is-the-one-its-hub-loaded` (F354) | No B4 change waits on it. Until B11 is built **and** `:8000` restarts onto it, `mcp_server.py` edits reach `:8000`'s next run from the working tree; after that, only on restart. Change 4's 422 retry is **kept** either way (building B11 does not force the restart, and the retry also covers a verdict the route rejects); once both change on one restart it is inert, not wrong. Notes added to change 1's Risks and change 4's D2 |
| Does F362 also affect the POSIX branch? | Yes: `_ABSOLUTE_PATH_RE`'s POSIX alternative (`:1023-1026`) opens at any `/` |
| Would fixing F362 alone let `.{,.}/x` and `src/{a,..}/../y` through? | Yes, by reading: both are refused today only by the tail reading D2 removes. It also holds one level down (row 1) |
| Does the settings page hard-code "Built-in default (Edit files)"? | Yes, `AgentSettingsControls.tsx:199`; pinned by `agentPermissionDefault.test.tsx:77` and `composerPermissionDefault.test.tsx:132-136`, and `test_permission_approver.py:1051-1052` pins the catalog default. The catalog default is display-only (no Python code reads a control's `.default`; `render_control_args` renders overrides only, `model_catalog.py:453-487`), so changing it moves no run |
| Is the 422 retry for an un-restarted `:8000` sound? | Yes: `PermissionRequestCreate` is a `RequestModel` (`extra="forbid"`, `schemas/common.py:32`) taken as the route's body, so the 422 is raised before the handler runs and no row exists; `HubAPIError.status_code` carries it (`mcp_server.py:80-92`, `:187-197`) |
| Do the changes respect `.claude/rules/mcp-server.md`? | Stdlib only: every addition is `re`/`fnmatch`/`os`/`tempfile`. No return annotation on `approve_tool_call` (stated in change 1 D6 and change 4 task 2.3). "Reaches `:8000` on edit" matches the rule |
| Does D4's `posture_at_rest` agree with B3's D5 list of posture reads? | On five of seven: `default_permission_mode` and `runtime_overrides` sit above it in the pill exactly as in the spawn (`agent_trigger.py:760-768`); `yolo` and `hub_client` are its inputs; `read_only` moves the boundary, not the posture. Two stay outside as named residuals in change 3's Risks: `Runner.flags` appended raw after `--permission-mode` (`runner_commands.py:285-286`), and `config["env_vars"]` carrying `AW_PERMISSION_POSTURE=operator`, which survives a non-`manual` spawn (`agent_trigger.py:1183`, `:1195-1196`) and which the approver honours (`mcp_server.py:1699`). An agent `request_agent` creates (B3) has its own row with no default and no `yolo`, so it reads `workspace` like any fresh Claude agent |
| Does `agent_meta` carry `hub_client` for runner-bound agents? | By key, yes, but without the session-wide fallback (row 8) |
| Collisions | No other unarchived change touches `_judge_word`, `_lex`, `_words`, `_read_command`, `_decide`, `_ask_operator` or `approve_tool_call`. `the-codex-models-offered-are-the-ones-its-cli-lists` (B7) rewires `model_catalog`'s readers but leaves `controls` alone: a textual rebase with change 3 only. Migration `0106` is still shared with three other bundles' changes: renumber at build |

### What stands

D4 (a) and (b), D5 (d), and every finding's re-verification stand. R1's nine prototype row moves
stand as far as reading can check them (the X4/X5/X6 flips and the N5/N6/G3/E11/Z1/Z2 reasons follow
from the piece rule). Open questions carried to R3 and the operator unchanged: change 1's `host:x/y`
widening (recommend allow) and its device set (recommend the four names); change 2's `PWD` in the
set (recommend include it). New for R3 to re-derive: the inner-shell brace reading's cost on real
agent commands (inline JSON with more than 256 alternatives is refused).

## R3 — independent comparison (2026-09-24)

**Method.** Worktree HEAD `b66f6a6` (no product change since `ce086b6`). 33 load-bearing claims
opened at their `file:line` across the four changes. The judge was attacked for inputs the final
rule set (R2's D1-D6) would **allow** where today's code refuses: 30 commands measured through
`_decide` in-process (`py -3.11`, `AW_WORKSPACE_DIR` a scratch dir), R2's piece split re-derived
over the candidates with `_where` per piece, and bash behaviour checked in Git Bash 5.2.37. No pytest
run.

### Regressions found in R2's rule set, and fixed

| # | Input | Today | Under R2 | Fix (where) |
|---|---|---|---|---|
| 1 | Bash tool on Windows: `python w.py 'Z:foo\bar'`, `python w.py Z:foo/bar`, `powershell -c "Copy-Item x Z:foo\bar"` | deny `'\\bar'` / `'/bar'` | **allow**: the bash reading keeps the `:` break, pieces `Z` + `foo\bar` both inside (measured) | the drive exception is keyed on the **platform**, not the dialect (change 1 D2 step 3; spec sentence + scenario; task 1.7b) |
| 2 | `dd if=x of=c:~/y` (also `git show HEAD:~/x`) | deny `'/y'` | **allow**: pieces `c` + `~/y`, both inside; bash expands `~` after `:` in an assignment-shaped word (Git Bash: `echo of=c:~/y` prints `of=c:/c/Users/huida/y`) | a piece (or a drive's remainder) beginning with `~` is `_UNCHECKED` (change 1 D2 step 5; task 1.7b) |

Candidates that stayed refused under R2's rules (no change needed): PowerShell
`FileSystem::C:\Windows\x` and `FileSystem::..\x`; `cp x [.][.]/y` (bash never lets `[.]` match a
leading dot; measured literal); `-o~/y` and `@~/x` (now refused by row 2's rule); `/out:..\x`
(rule 5, absolute); integer brace ranges (digits and a sign only); the 256/1024 bounds (they refuse,
never allow); `~user/x`, `\~/y` (rule 3).

### Escapes allowed today that no round had named

| Input | Today | Decision |
|---|---|---|
| `bash -c 'cp n .\./x'` (Bash and PowerShell tools; the inner bash writes `../x`, measured) | **allow** (plain word, rule 5) | closed in change 1 D7: a word holding a backslash is also judged with an inner shell's escapes removed, before rule 5 (task 1.7c) |
| PowerShell `Copy-Item x Microsoft.PowerShell.Core\FileSystem::C:\Windows\x` | **allow** (`_PLAIN_RELATIVE_RE` admits `:` after the first separator) | closed in change 1 D7: a word with `::` is not plain (task 1.7c) |
| `powershell -c 'Copy-Item x Z:'`, `python w.py Z:` from the Bash tool on Windows | **allow** | change 2 D4: drive words judged in bash on a Windows host |
| `dd of=c:~`, `PATH=a:~` (separator-less) | **allow** | change 2 D4: a `~` after a colon is `_UNCHECKED` |
| `cp x ..*`, `cp x .{,.}*` (bash 5.1 matches `..`) | **allow** | change 2 D4: a separator-less glob beginning with `..` is judged as `..`; `.*` deliberately left (often a quoted regex) |
| `bash -c "cp n \$'\\x2e\\x2e'"` (an inner ANSI-C `..` with no separator) | **allow** | named residual in change 1; not closed |

### Other claims that disagreed with the code

| # | Claim (change) | What the code shows | Changed |
|---|---|---|---|
| 3 | The pill and the settings select are the places the posture is shown (change 3 D3) | `NewConversationSurface.tsx:201-218` renders a second `<Composer>` with **no** `effectiveControls` (`Composer.tsx:96` defaults to `{}`), so the first message of every conversation shows the catalog default: wrong today even for an agent whose default is "Ask me" | one UI helper `postureAtRest(agentRow)` read by both composer hosts; spec sentence + scenario; task 1.4b |
| 4 | R2's `effective_hub_client(meta, session_data)` reproduces the spawn (change 3 D2) | `get_agent_config` applies the session-wide `hub_client` to the per-agent entry **before** laying it over `Agent.config` (`launchability.py:474-485`), so the session-wide value beats `Agent.config`'s; a merge-then-fallback helper inverts that | the helper is the whole merge, lifted unchanged (`agent_config(...)`); task 1.3 row |
| 5 | The at-rest argv equals the explicit-override argv (change 3 task 1.2) | override flags are spliced at `runner_commands.py:216`, the default near the end; `yolo` emits `--dangerously-skip-permissions` (`:276`) while an explicit `bypassPermissions` renders `--permission-mode bypassPermissions` | compare flag/value multisets; `yolo` asserted separately |
| 6 | The bound's cost is "inline JSON with more than 256 alternatives" (R2) | re-derived: the inner-shell reading expands the whole quoted argument, so spaces do not help; nine two-key JSON objects in one quoted argument exceed 256 | named in change 1 D7 and its residuals as the one new refusal of ordinary work; splitting at whitespace first rejected as unsound (`'{a," b",..}/x'`) |

### Asked of R3, answered

| Question | Answer |
|---|---|
| D6 against the `approve_tool_call` annotation trap | Safe: the `try` wraps only `_decide` (`mcp_server.py:1708`) and yields the same dict, so one `json.dumps` exit remains and no annotation is needed; `except Exception` covers `RecursionError`. `test_response_carries_no_structured_content` (`test_permission_approver.py:907-925`) already guards the wire. D6 must not wrap the "Ask me" path (change 4 asks anyway). Noted in change 1 D6 |
| Do the pill, settings and run agree through one helper wherever the posture is shown? | After fix 3, yes: server `posture_at_rest` (spawn and list), UI `postureAtRest` (both composers and the settings label). `grep permission_mode hub/ui/src` finds no other display |
| Change 4's claims | Stand: `_ask_operator`'s only `try` is the request (`:1636-1647`); `PermissionRequestCreate` has no verdict field (`agent_actions.py:1013-1018`); the list is newest-first (`permissions.py:73`); Codex's workspace branch is `_within(cwd or grantRoot)` (`codex_appserver.py:280-283`) |

### What stands

Change 1's D1-D6 (with the two fixes), change 2's D1-D3, change 3's D1 and D4, change 4 whole. All
four changes pass `openspec validate --strict` after R3's edits.

## Final — what the bundle reached, and why

The bundle is **parked awaiting operator approval**; no implementation has started. Four changes in
`openspec/changes/`, each passing `openspec validate --strict`.

### Decisions

| # | Question | Recommended answer | Why |
|---|---|---|---|
| D4a | One built-in default posture for a Claude run: `acceptEdits` ("Edit files") or `workspace` ("Workspace only")? | **`workspace`**, stated in one function (`posture_at_rest`) that the spawn and every display read. Codex keeps "Edit files", which is what its runs really get | The spawn has used `workspace` since `72afb3c` (`runner_commands.py:74`); only the display says "Edit files" (`model_catalog.py:231`). `acceptEdits` prompts for Bash with nobody to answer, so unattended runs could edit but never run anything (measured 2026-08-13: 14/14 tests ran under `workspace`). Switching Codex would accept sandbox escalations it declines today, which cannot be measured while Codex is undrivable |
| D4b | Should an "Ask me" card show what "Workspace only" would decide? | **Yes, as advice**: the approver's own `_decide` verdict, shown as a warning ("Workspace only would refuse this: …") or a muted note. The operator still decides | The Hub already computes the boundary, while the operator, deciding in seconds, was shown only the tool and path (F230, F284). It is the same function as the unattended answer, so there is one boundary. Auto-refusing under "Ask me" would be a new posture |
| D5 | How strict about bare expansions with no separator (`$VAR`, `$(…)`)? | **Refuse only those that name a directory by themselves**: `$HOME`, `$PWD`, `$OLDPWD`, `$USERPROFILE`, `$TMPDIR`, `$TMP`, `$TEMP`, `$APPDATA`, `$LOCALAPPDATA` at a word's start (bash `$N`/`${N…}`, PowerShell `$HOME`/`$PWD`/`$env:N`, `%N%`); `..` before an expansion (`..$x`); a `~` after a colon (`of=c:~`); a glob beginning with `..` (`..*`). **Everything else stays allowed** (`echo $x`, loops, `$(dirname $PWD)`, the commit heredoc) | It is F375's `~` rule applied to the shorthand's other spellings (`$HOME` = `~`, `$PWD` = `~+`). Refusing every bare expansion refuses every Claude Code commit (the heredoc lexes to one `$(…` word). A command-aware destination check is a new layer that `bash -c` defeats |

### Changes

| Change | Findings | What it does | Size |
|---|---|---|---|
| `the-shell-judge-reads-a-word-whole` | F362, F403 | The shell judge stops refusing the tail of an ordinary word (`src/*.py`, `@types/node`, `HEAD:src/a.py`, regexes, `2>/dev/null`): a non-plain word is read as the pieces a shell could take from it. Brace patterns are judged as expanded, including quoted ones an inner shell expands, with bounds (256 per argument, 1024 per command, refused past them). A glob that can match `..` is judged as `..`. `user@host:` and `host:port/` are network addresses. On Windows a drive colon never divides a drive; a `~` after a colon is uncheckable; an inner shell's backslash escapes and PowerShell `Provider::path` are read too (R3). A judge that raises becomes a reported deny (D6) | Medium: `mcp_server.py` + tests; reaches `:8000`'s next run on edit |
| `a-drive-or-a-home-variable-names-a-directory-by-itself` | F402, F401 | `Z:`, `Z:foo`, `-Destination:Z:` and `Temp:` judged as the drive they name (PowerShell on any host, Bash on a Windows host too); D5's refusals in rule 4 | Small: `mcp_server.py` + tests |
| `the-permissions-pill-shows-the-posture-the-run-gets` | F283 | `posture_at_rest` read by the spawn and served as `permission_mode_at_rest`; both composers (the conversation's and the new-conversation surface's) and the settings select read it through one UI helper; the catalog's Claude default becomes `workspace`. No run changes | Small to medium: Python + UI bundle |
| `an-ask-me-card-says-what-workspace-only-would-decide` | F230, F284 | The approver sends `_decide`'s verdict with each "Ask me" request (Codex: the same `_within` helper its posture uses); it is stored (new column) and shown on the card as advice; an allow reason says when a command names values decided at run time; one retry without the field on a 422 from an un-restarted Hub | Medium: approver, route, migration, UI bundle |

Order: change 1 must not be split (its brace step prevents a regression of its piece step); the
others are independent. Change 4's allow wording reads best after change 2. Its migration number
(`0106`) is shared with three other bundles' changes: renumber at build.

### Open for the operator

1. **D4a**: recommended `workspace` for Claude. If `acceptEdits` instead: `posture_at_rest` returns
   it, unattended Claude runs lose Bash again, and the shell judge only governs runs that chose
   "Workspace only" (changes 1 and 2 still hold).
2. **D4b**: recommended yes, as advice. If no: drop change 4 whole and close F230/F284 as by design
   with a line in the posture documentation.
3. **D5**: recommended the narrow set above. If (a), accept the disclaimer: change 2 keeps only its
   drive part. If (b), refuse every bare expansion: not buildable under the default posture (it
   breaks every commit). Sub-choices, recommended first: include `PWD` in the set (yes, it is `~+`);
   leave a separator-less `.*` allowed (yes, it is often a quoted regex; residual: `chmod -R x .*`
   on bash 5.1 reaches the parent).
4. **Change 1's two open questions**: allow `scp a host:x/y` (no user, no port) as a relative path
   (recommended, consistent with how `example.com/x` is read), or refuse it as network; the bash
   device set `/dev/null`, `/dev/stdin`, `/dev/stdout`, `/dev/stderr` (recommended) or `/dev/null`
   alone.
5. **Accept the one new refusal of ordinary work** that change 1 introduces: a quoted JSON array of
   about nine or more objects with commas is refused as too many brace alternatives (write it to a
   file instead). Recommended: accept. The alternative, splitting at whitespace first, would let a
   brace escape through.
6. **Two posture residuals stay outside `posture_at_rest`, on purpose.** Both are operator-written
   configuration, not a built-in default. (i) A runner's `Runner.flags` are appended raw after
   `--permission-mode` (`runner_commands.py:285-286`), so a flag naming a posture wins over what the
   pill shows. (ii) An agent's `env_vars` can carry `AW_PERMISSION_POSTURE=operator`; the spawn only
   ever sets that variable for "Ask me" and never clears it (`agent_trigger.py:1195-1196`), and the
   approver honours it (`mcp_server.py:1699`), so a "Workspace only" run silently becomes "Ask me".
   Recommended: leave both as named residuals now; a later change could have the spawn strip
   `AW_PERMISSION_POSTURE` from `env_vars` and refuse a posture flag in `Runner.flags`.
7. **Tell-first**: every `mcp_server.py` edit (changes 1, 2, 4) reaches `:8000`'s next agent run from
   the working tree, committed or not; change 4's migration and change 3's Python half reach it on
   the next restart; the UI bundles on the next reload.

### Findings that did not need a change

None. All seven (F362, F403, F402, F401, F283, F230, F284) were re-verified open by all three rounds
(at `ce086b6` and `b66f6a6`); none was already fixed, retired or a duplicate.

## R4 — revise after the operator's review (2026-09-24)

**Input.** The Opus review (`tracks/reviews/B4-2026-09-24.md`, "Change 1" and "Change 2"). The
operator's answers are in `APPROVALS.md` `## 2026-09-24`:

- expand glob matches against the filesystem, bounded;
- refuse a directory variable inside a quote handed to an inner shell;
- extend the variable list;
- add the PowerShell spellings;
- `$(dirname $PWD)` refused, which is accepted;
- `host:x/y` allowed.

**Scope:** the two REVISING changes only. The two APPROVED ones were not touched.

**How.** The round re-derived both changes from `hub/hub/mcp_server.py` at `b7d976a`: `_lex`,
`_words`, `_read_command`, `_judge_word`, `_where`, `_judge_path`, `_expands`, `_decide`,
`approve_tool_call`. It also read `worktrees.py`, `_symlink_shared_dependencies`.

It measured three ways:
- `_decide` in-process in `py -3.11`, with `AW_WORKSPACE_DIR` set to
  `testbed/scratch/b4-r4/ws`, which holds a real junction `up` pointing to a sibling `out`;
- Git Bash 5.2.37 (`globskipdots on`, `extglob off`);
- Windows PowerShell 5.1.

The scratch was deleted afterwards. No `hub/` or `src/` file was edited, and no Hub was started or
called.

### Per review item

**C1 HIGH: a glob through a link.** *Code:* no glob character is in `_PLAIN_RELATIVE_RE`, so every
glob word reaches rule 6. There `_ABSOLUTE_PATH_RE` is the only thing that refuses it, and it
reads a tail, not the link.

*Measured:*
- In Git Bash, `cp n u*/` copied `n` into `out`.
- The judge refuses `cp n u*/` as `'/'` today, and `u?/x` and `[u]p/x` as `'/x'`.
- **`cp n u*` and `cp n up` are allowed today.** A separator-less link name was already an escape
  before R3.

*Changed:*
- Change 1 gains **D8, `_glob_links`**. Every glob piece, in every reading, is expanded one
  directory at a time with `os.scandir`. It matches a relaxed pattern: brackets and extglob groups
  become `*`, case is folded, and `**` recurses only when the command names `globstar`. Each matched
  **link** is judged by `_judge_path`. A non-link entry of an inside directory is inside by
  construction, so only links cost a `realpath`.
- `DirEntry.is_symlink()` is **False for a junction** on Python 3.11 (measured), so the test also
  reads the reparse-point attribute.
- `fnmatch` does not match `[[:alpha:]]p` to `up`, but bash does (both measured). That is why the
  brackets are relaxed.
- The budget is 8192 entries per `_decide`, refused past it.
- Change 2 gains **D10**: a separator-less name that `lexists` is judged by where it resolves, and
  so is each suffix of a glued short option run (`cp -tup n`, allowed today, measured). A
  separator-less glob goes through `_glob_links`.
- Change 1 carries a **MODIFIED delta on "A posture exists in which the workspace boundary is
  enforced per tool call"**. It adds a scenario for a glob matching a link, one for a glob too large
  to examine, and a sentence scoping a shell command to the paths its text names.

*Tests:*
- Change 1's 1.4c: `u*/`, `u?/x`, `[u]p/x`, `[[:alpha:]]p/x`, `bash -c 'cp n u*/'`, and the extglob
  `@(u)p/x`. Each fails against R3 as written. Each passes today only by the tail, so the tests
  assert the resolved target in the reason, which fails today.
- Change 2's 1.4f: `cp n up`, `u*`, `-tup`, `{up,x}`. Each fails today.
- The fixture uses `os.symlink` on POSIX and `_winapi.CreateJunction` on Windows.

**C1 MEDIUM: extglob.** *Code:* `(` ends an outer argument (`_ARGUMENT_ENDS`), so the outer
`@(..)/x` is refused as `..`. The inner, quoted form is a single word.

*Measured:* `bash -O extglob -c 'cp n @(..)/x'` is refused today by `'/x'`.

*Changed:* D3 treats `[@?*+!](` up to its matching `)` as one glob unit, with no breaks inside it.
The unit is `..`-capable when an alternative begins with `.`. For D8 each group counts as `*`.

*Test:* 1.4b fails against R3 (piece `..)/x`, inside).

**The `case` arm.** Refused today (`'/../evil.sh'`) and allowed after, because `)` is kept not a
break. That protects the regex back-references F362 counted. *Not closed*, and put to the operator
as change 1's Open Question 1, recommended **accept**. It executes and cannot write: a redirect
meets `>`, and an argument meets whitespace. The alternative costs are stated.

**C1 MEDIUM: `user@host:`, spec against design.** *Code:* R3's D5 was step 1 of rule 6, which only
words holding a separator reach.

*Measured:* `git clone git@github.com:repo` and `scp n user@host:file` are allowed today.

*Resolved one way:* **refuse** the form, consistently.
- D5 now runs on the whole word after rule 2 and before rule 3.
- `_SCP_ADDRESS_RE` requires a dotted host, an IP or `localhost`. A `[^:]+` host caught
  `docker pull alpine@sha256:…` (allowed today, measured), `x@npm:y` and `x@workspace:*`. A dotless
  alias (`user@myserver:`) is read as a path, the same call as `host:x/y`.
- `_HOST_PORT_RE` requires a separator after the port and a host of two or more characters, so
  that a Windows `C:1/x` is not read as a host.
- R3's ADDED requirement is folded into the MODIFIED network requirement.

*Tests (1.5):* each separator-less row fails today and against R3.

**C1 MEDIUM: `host:x/y` against the network SHALL.** *Changed:* a **MODIFIED delta on "A network
address in a shell command is decided as a network address"**. It states which schemeless forms
are addresses, that other colon forms are judged as paths (a revision and a path are the same
text), and that an allow is no statement that no address was named. It adds four scenarios.

**C1 MEDIUM: Windows rules untested in CI.** *Chose a Windows job, not a monkeypatched constant.*
The Windows rows are true only because of `ntpath` resolution: `_where` via `commonpath` raising on
another drive, `realpath` through junctions, and `\` as a separator. On Linux,
`_where("Z:foo\\bar")` is a relative file inside, so a monkeypatched constant could only show which
string reached `_judge_path`, not that the call is refused.

*Changed:*
- Change 1's D9 adds `hub-judge-windows` (`windows-latest`, the two judge test files, installed
  through `-c ../constraints-dev.txt`), and one named key, `_DRIVE_LETTERS`.
- The CLI `test` job already runs on `windows-latest`.

**C1 LOW: bounds per `_decide`.** *Code:* `_decide` makes up to four top-level `_read_command`
calls (two dialects × two readings), each recursing into nested substitutions.

*Changed:*
- One `_Budget` per `_decide`, passed everywhere: 256 alternatives per argument, 1024 in total,
  and 8192 glob entries. The backslash levels are bounded by log2 of the longest run.
- A per-`_decide` memo of word judgements and expansions. The `c` and `utf8` readings of the same
  text are then charged once. Without the memo, an unknown tool would get about 256 usable
  alternatives.
- Past a bound, the call is denied with `_TOO_MANY`, reported, and never allowed.

*Measured:* `realpath` costs about 85 µs, a directory entry about 9 µs, `lexists` 10 to 18 µs. The
worst case is about 0.25 s.

*Test:* 1.6 checks that the memo charges once.

**C1 LOW: `/dev/null` in pieces on Windows.** *Measured:*
- `python -c "open('/dev/null','w')"` is refused today (tail).
- msys converts whole arguments: `/dev/null` and `x=/dev/null` reach Python as `nul` and `x=nul`.

*Changed:* on a drive-letter host D4 exempts only a whole word or a piece after `<`/`>`.

*Test:* 1.7d fails against R3's D4.

**C1 "checked and holding" (D6).** Re-read and still true. The "What each changed route returns"
section is rewritten to cover `_glob_links`: it catches `OSError`/`ValueError` per listing and
entry, is iterative, and ends a link cycle at the budget.

**C2 HIGH: the `$HOME` bypass one quote down.** *Measured:* `bash -c 'cp n $HOME'`,
`sh -c "cp n \$HOME"` and `powershell -c 'Copy-Item x $HOME'` (from Bash) are all allowed today.

*Changed:*
- D2 matches `_LITERAL_DOLLAR` too, and the spec sentence "…quoted literally or escaped… SHALL
  stand" becomes a SHALL refuse.
- Deeper quoting is covered by change 1's D7, which now removes backslash escapes **level by
  level**, each level judged as a word through every rule. On POSIX,
  `bash -c 'bash -c "cp n \$HOME"'` reaches rule 4 as `\$HOME`; on Windows `\` is a separator and
  rule 3 refuses it today.

*Tests:* 1.4d fails today and against R3. 1.5a asserts the accepted costs (`echo '$HOME'`,
`grep '$HOME'`, a commit heredoc naming `$HOME`).

**C2 MEDIUM: the `$(dirname $PWD)` control.** *Code:* `_read_command` re-reads the nested text, and
the word `$PWD` is found there. *Changed:* the control becomes
`$(git rev-parse --show-toplevel)`, and `$(dirname $PWD)` moves to the refused costs (1.5a).

**C2 MEDIUM: the PowerShell spellings.** *Measured:* these are allowed today: `${env:TEMP}`,
`${HOME}`, `$variable:HOME`, `$global:HOME`, `$env:HOMEDRIVE$env:HOMEPATH`, `$PROFILE`, `$PSHOME`.
*Changed:* the pattern is `[$␀]\{?(?:(?:env|variable|global|local|script|private|using):)?NAME`, in
any case.

**C2 LOW: the heredoc cost.** It is named in change 2's design, "Costs the operator accepts",
together with the `git commit -F` way round.

**C2 operator: extend the list.** Each name was decided with a reason in change 2's D2. The measured
Git Bash environment showed that msys keeps some Windows names' case and capitalises others. So
bash matches a Windows name in its Windows spelling or in all capitals, never in lower case (a
user's `$tmp` stands).

*Added:*
- `HOMEPATH`, `HOMEDRIVE`, `SystemDrive`, `PUBLIC`;
- `OneDrive`, `OneDriveConsumer`, `OneDriveCommercial`;
- `ProgramData`, `ALLUSERSPROFILE` (writable by users, persists for all);
- `SystemRoot`, `windir`;
- `ProgramFiles`, `ProgramW6432`, `CommonProgramFiles`, `CommonProgramW6432`;
- `XDG_CONFIG_HOME`, `XDG_DATA_HOME`, `XDG_CACHE_HOME`, `XDG_STATE_HOME`, `XDG_RUNTIME_DIR`;
- PowerShell `$PSHOME` and `$PROFILE`, and cmd's `%CD%`.

*Left out:* `XDG_DATA_DIRS` and `XDG_CONFIG_DIRS` (lists), and tool-specific or user variables
(open-ended; a named residual).

*Test:* 1.4b.

### Holes of the same kind the review did not name (all measured)

1. **A separator-less link name**: `cp n up`, `Copy-Item n up`, `cp -tup n`, `cp n u*`. Allowed
   today. Change 2 D10.
2. **An expansion that leaves `..`**: `cp n $(true)..`, `$x..`, `.$x.`, `` `true`.. ``, and
   `bash -c 'cp n $x..'`. Allowed today; Git Bash prints `..` for each, and PowerShell passes `..`
   for `$x..`. Change 2 D3 now refuses a word that is `..` once every expansion is removed.
3. **A directory variable after a colon**: `dd if=n of=c:$HOMEPATH`. Allowed today; `ls -d
   c:$HOMEPATH` lists home. Change 2 D2.
4. **Two levels of backslash escapes**: `bash -c 'bash -c "cp n .\\./x"'`. Allowed today, and Git
   Bash prints `../x`. R3's one-level D7 still allows it. Change 1 D7 is now level by level.
5. **A separator-less `user@host:`**, which R3's D5 could never see (above).

Also checked, and not holes:
- Win32 trailing-dot and space normalisation: `.. \x`, `... \x`, `. .\x` all failed to open
  (measured), so none is a parent.
- An outer `@(..)` is split at `(` by the lexer and refused.
- `$(echo)../x` inside quotes is rule 3's (it has a separator and a literal `$`).

**Found, not this bundle's to fix:** the Hub links `node_modules`, `.venv` and `venv` from the
project checkout into every worktree (`SHARED_DEPENDENCY_DIRS`). Every path through them resolves
outside and is refused today. D8 and D10 extend that verdict to globs through them and to their bare
names, so `grep -r foo --exclude-dir=node_modules .` (allowed today, measured) would be refused in
such a worktree. No project worktree on this machine holds such a link now: all four LoopEngine
worktrees were checked read-only, and LoopEngine has no `node_modules`. It is recorded as open
questions in both designs, recommending a finding.

### MODIFIED deltas added (grep of `openspec/specs/` for requirements made false)

- Change 1:
  - `agent-run-sandboxing` "A posture exists in which the workspace boundary is enforced per tool
    call": its scenario "Traversal and links cannot escape" was false under R3 for globs. New.
  - "A network address in a shell command is decided as a network address": `host:x/y` and the
    schemeless forms. New, replacing R3's ADDED requirement.
  - "A path in a shell command…": already MODIFIED, now extended.
- Change 2: "A word with no path separator…" was already MODIFIED. It is revised: the literal `$`
  is now refused (the old SHALL stand is gone), and link names, glob matches and a `..` left by an
  expansion are added.

Also grepped, and not made false: `agent-capability-plane` (the Hub URL with a scheme stays
allowed), "Every permission decision is answered" (D6 strengthens it), and `workspace-isolation`
"Expensive shared dependency directories are linked" (unchanged, but see the finding above).

### Validate

`openspec validate the-shell-judge-reads-a-word-whole --strict` and
`openspec validate a-drive-or-a-home-variable-names-a-directory-by-itself --strict` are both valid.

### Left for the operator (recommended answer first)

1. **Change 1, the `case` arm.** Accept the residual and keep `)` not a break. The alternative,
   breaking at `)` followed by `.` or `~`, refuses regexes ending `(…).*`.
2. **Change 1, the device set.** The four names. This is unanswered from the Final section.
3. **Both changes, the linked dependency directories.** Build as written and file a finding. The
   boundary, not this bundle, should decide how the Hub's own shared links count.
4. **Order.** Change 2 now builds after change 1. It is no longer independent.

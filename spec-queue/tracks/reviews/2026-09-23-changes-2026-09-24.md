# The 2026-09-23 day window's two changes: Opus adversarial review, 2026-09-24

The operator requires an adversarial pass before any row is marked APPROVED. An Opus subagent ran this one. It took measurements with Haiku on `claude` 2.1.280 in a throwaway directory, since deleted. The daily-review session wrote the review down here.

**What the operator decided (2026-09-24, daily review):** both changes were approved with their defaults. After the review:
- `an-at-mention-an-agent-wrote-reads-no-file` is **REVISING**. A fresh round re-derives it with this review as input.
- `pressing-run-names-the-reason-that-held` had its fixes applied and was approved.

## 1. `an-at-mention-an-agent-wrote-reads-no-file` (F409, severity A): REVISE

**Measured:**
- A baseline `@<outside>` expands, and the file is attached.
- `\@<outside>` does not expand.
- U+FE6B (`﹫`) does not expand.
- An outside-path `@import` in the working directory's CLAUDE.md is not loaded under `-p`, so it is not a second route in.
- `--tools ""` (the titler's argv since Round 4, `conversation_titles.py:74-78`) **still expands**. D7 is still needed for the titler.

**The single choke point holds, re-derived:**
- There are exactly three `-p` sites: `runner_commands.py:287`, `worker.py:139` and `conversation_titles.py:78`.
- `trigger_agent_directly` has one caller (`turn_scheduler.py:408-411`).
- Nothing uses stream-json or stdin input, except Codex app-server, which gets the same `prompt`.
- The only UI-built message that carries agent text is Start work (`tasks.ts:475`).

**HIGH: a bypass. The answer half of the question echo carries agent text and is not escaped.**
- `ask_user` options are mandatory (`schemas/questions.py:33`), and the tool tells the agent "the label is what comes back to you" (`mcp_server.py:369-371`).
- The app answers with `answer: typed || labels.join(', ')` (`AgentOutputPanel.tsx:960`). That answer is stored as `row.answer`, and `answer_labels` is stored too (`models.py:961`).
- D4 escapes only `row.question` (`questions.py:229`, `:233`).
- **Exploit:** an agent offers the option `Use @/home/u/.ssh/id_rsa`. The operator clicks it, and the operator-origin echo expands the file on that agent's next turn.
- **Fix:** in `_batch_delivery_text`, neutralise `row.answer` whenever `row.answer_labels` is non-empty. A typed answer comes from the Composer, whose `@` picker must keep working, so leave it as it is.
  - Add a row to test 1.6: labels set gives an escaped answer; a typed answer is unchanged.
  - Rewrite the scenario to: "an answer the operator typed is not neutralised; an answer that is an offered option is".

**MEDIUM: an existing SHALL is contradicted, with no MODIFIED delta.**
- `agent-flows/spec.md:813` says "the loop's message itself is **delivered unchanged**", and the paragraph at `:782-785` says the same kind of thing.
- Under D3 plus Q1, a loop message containing `@` arrives as `\@`.
- **Fix:** add a MODIFIED delta that exempts file-mention neutralisation, or make the ADDED requirement say it supersedes.

**MEDIUM: the ADDED requirement contradicts itself on Q1.**
- It says "Text the operator wrote SHALL reach the run unchanged", yet an operator-authored job message is neutralised (Q1, accepted).
- **Fix:** restrict that sentence to text the operator sends directly (the composer or chat). State that a scheduled job's message is neutralised as a whole, and add a scenario.

**LOW:**
- **Line numbers have drifted since R3.** Tasks should cite function names, not lines.

  | Cited as | Now at |
  |---|---|
  | `runner_commands.py:283` | `:287` |
  | job `new_entry` sites | `scheduler.py:3404`, `:3736` |
  | `conversation_titles.py:71` | `:78` |
  | `tasks.ts:446-465` | `:462-481` |
  | `agent_trigger.py:1091` | `:1140` |
  | `agent_trigger.py:1498` | `:1567` |

- **Test 1.7** must allow the titler's new `--tools ""`.
- **`spec_turn_notice` interpolates `path`** (`launchability.py:369-373`), and `validate_spec_path` (`spec_manifest.py:61-82`) allows spaces and `@`. Adoption makes on-disk names reachable, so neutralise `path` in the notice. It is cheap.
- **B5's evidence change adds an `evidence` origin** with its own `format_turn_prompt` branch. D3's default-deny covers it only if the neutralise test stays `origin_type != "operator"` rather than per branch. Say so in both changes.
- **B3's full-names change adds no `@`,** so there is no interaction.
- **B7's worker changes keep both command builders.** Whichever of the two lands second must keep `restore_file_mentions` ahead of `model_validate` in `_interpret`.

**Tests fail if the change is reverted:** 1.1, 1.3, 1.5, 1.6, 1.7, 1.9, 1.10 and 1.11 each fail on today's code. 4.1, 4.2a and 4.2b check the CLI transcript. The 3.1 probes are drive-only.

## 2. `pressing-run-names-the-reason-that-held` (F400, F373): APPROVE WITH FIXES

**Verified:**
- `run_job` is at `jobs.py:1372-1520`.
- The guard order (`scheduler.py:335-375`) matches the spec's total order.
- `_do_fire_job` has exactly ten exits, matching the design's table.
- The identity trap is real, so the `int` copy is required.
- The MODIFIED delta keeps all six scenarios.
- The two moved assertions (`test_board_agent_role.py:385`, `:420`) are the only phrase pins.
- On a raise, the route still answers the failed run as a 500 (`:1509-1515`), as before.

**MEDIUM: D3 contradicts itself on the re-ask gate.**
- The "Two gates change" bullet says the busy re-ask runs "where `not answered_by_row`".
- Task 1.14 has a concurrent tick's `in_progress` row, so `wrote_row` is true, and the task expects the busy 409. Under that gate the re-ask is skipped, `_loop_in_flight_decision` returns None, and the route answers 500. That is today's bug.
- The sketch (design lines 211-217) and task 2.3 make the re-ask unconditional instead.
- **Fix:** delete the `not answered_by_row` wording. The `skipped` and `failed` arms already return first, so an unconditional re-ask below them is correct for both 1.9 and 1.14.

**LOW:**
- **A race that shifts from a stale answer to a 500:** the firing decides "in flight", then the task finishes before the route re-asks. Today, with an earlier skipped row, that answers a stale 409; after the change it answers 500 "Failed to fire job". Note it in Risks.
- **Stale line citations:** `run_job` was `:1326`, and the guard was `:312-350`.
- **Rebases:** coordinate with the ones `an-agent-can-be-paused-and-keeps-its-input` (1.7) and `run-id-in-an-event-always-names-a-run` already declare.

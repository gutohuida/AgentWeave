# B7: Opus adversarial review, 2026-09-24

The operator requires this adversarial review before any row is marked APPROVED. An Opus subagent ran it at HEAD `c1c0fa4`, with `mode=ro` reads of `:8000`. The daily-review session wrote it down here so the next round can take it as input.

**Operator (2026-09-24, daily review):**
- Change 3 is **REVISING**, and a fresh round re-derives it with this review as input. Its three open questions are answered:
  - (a) a dismissal made while the budget is exhausted **persists** until the operator acts; raising the budget does not override it;
  - (b) **F422** (the titler pays again every turn) **is fixed first**, so exhaustion does not write one `budget_exhausted` row per turn;
  - (c) the wording "excludes 3 turns" on `:8000`'s label is fine.
- Changes 1 and 2 were approved as they are.
- Changes 4 and 5 had their fixes applied and were approved.

## 1. `a-runner-choice-names-its-model` (F268): APPROVE

- **Coverage.** There are exactly three runner selects: `AgentSettingsControls.tsx:253`, `ProjectSettingsPanel.tsx:171` and `:279`. The catalog covers both runner CLIs.
- **LOW: the label format is not pinned.** The design shows `Claude Code — Opus 5.5 (claude)`; B7.md Final shows `name · provider · model`. Runners the Hub creates are already named `label — model label` (`agents.py:713`), so a `name — model` label would print the model twice. Pick one format and add a test on a Hub-created runner.
- **LOW: the checkpoint-runner select can name the wrong model.** When `project.checkpoint_model` is set, that model is the one that runs (`checkpoint_trigger.py:153`), not `runner.model`. This is cosmetic.

## 2. `an-estimate-that-misses-turns-says-so` (F62): APPROVE

- `_aggregate_columns` (`usage_accounting.py:86-96`) matches the design, so the count comes out the same everywhere.
- On `:8000`, 3 of 348 turns have no cost, all `unavailable`, so the label will read "excludes 3 turns".
- **LOW:** seed test 1 from `_accounting_from_dimensions` output (`runner_parsing.py:582`), the real Codex path.
- **This change and change 3 affect each other.** Once worker cost joins the total, "excludes N turns" undercounts, because unpriced worker calls are not counted. Whichever of the two lands second should define `unpriced_calls` (calls that were spawned with `cost_usd_micros IS NULL`), put it in the label, and MODIFY this change's sentence "A monetary figure summed over turns…".

## 3. `worker-spend-counts-against-the-budget` (F240): REVISE

**HIGH: the final warning at exhaustion cannot fire as designed.**
- D3a changes `:192` to `not policy.automatic or await budget_blocked()`, but that branch then calls `needs_final_warning(policy, …)`, which returns False whenever `policy.automatic` (`checkpoint_policy.py:235`).
- So the scenario "a reading at the final-warning level raises the final warning" can never fire, and test 10d fails against the design as written.
- **Fix:** when the budget blocks, build `effective = dataclasses.replace(policy, mode="offered")`. The class is frozen and `automatic` is derived from `mode` (`:41-56`). Use `effective` for both tests and for `needs_final_warning`.

**HIGH: three MODIFIED deltas are missing.**
- `conversation-checkpoint`, "Crossing the threshold warns before it spends": *"Where checkpointing is configured to act alone, the threshold SHALL still generate and hand over"*, and the scenario "Acting alone still acts". B8, approved today, also MODIFIES this requirement, so whichever change is archived second must rebase its text onto the other's.
- `agent-flows`, "A flow generates the author's handover briefing" (`spec.md:516-547`): *"SHALL generate … whenever"*. Exhaustion is a new exception, alongside the existing no-notes and no-runner exceptions.
- `conversation-lifecycle`, "Title generation is a project setting" (`:97-130`): "When generation is selected, the title SHALL be produced…". Add the exhaustion exception.

**MEDIUM: the "Interaction with B8" text is stale.** B8's approved decline sits after the dismissed/final branch and before notes (`:236`), not at `:187-190`. Behaviour stays compatible in either landing order, but the claim "a handed-over conversation never reads the budget" is false.

**MEDIUM: test 10e cannot catch a read that is not lazy.** Put the warning check first in the `:192` condition, and have 10e assert that the patched `project_budget_state` is called 0 times.

**LOW:**
- **Backfill vs. runtime normaliser.** `_accounting_from_dimensions` adds cache only when `input_tokens` is not null (`runner_parsing.py:106`); the SQL adds it whenever output is not null. Use a `CASE` on input so the two agree. On `:8000` the total re-measures exactly at **962,599** tokens.
- **Keep the existing CHECK.** The outcome-CHECK rebuild in migration `0106` must keep `ck_worker_invocations_failure_explained`, per the `0047` precedent. Test 13 should assert that a non-ok row with no error is still refused.

**Checked and sound:**
- Every `run_worker` caller gets an initiator.
- Snapshot, PATCH and `project_budget_state` agree.
- No route answers 500 on a decline.

**Operator answers (these feed the round):** (a) the dismissal persists; (b) F422 is fixed first; (c) the wording is fine.

## 4. `a-model-alias-is-a-model-choice` (F221): APPROVE WITH FIXES (applied)

- Every door that accepts a model goes through `ProviderDescriptor.model`, and the readers that resolve an id already handle aliases. There is no price table.
- **LOW:** add a runner-registry MODIFIED to "A runner's model is drawn from the catalog": "a declared model **or declared alias**".
- **LOW:** test 1 parametrises two routes (`/runners`, `/agents`), not three. Add PATCH `/runners` explicitly.

## 5. `the-codex-models-offered-are-the-ones-its-cli-lists` (F267, F174): APPROVE WITH FIXES (applied)

- The real `C:\Users\huida\.codex\models_cache.json` matches every assumption:
  - its keys are `fetched_at`, `etag`, `client_version` and `models`;
  - every entry has `slug`, `display_name`, `visibility` (`list` or `hide`), `priority` and `context_window`.
- **LOW:** parse with `json.loads(path.read_bytes())`, which detects UTF-8 on any platform.
- **LOW:** do not memoise a failed read keyed on mtime alone. Key on `(mtime_ns, size)`, or re-read after a failure.
- **LOW:** a Codex run on a model the cache no longer lists loses its context window (`runner_parsing.py:402`). Fall back to the literal for window lookups only.

# B3: Opus adversarial review, 2026-09-24

The operator requires an adversarial pass before any row is marked APPROVED. Two Opus subagents ran
this one: part 1 at HEAD `d0da83d` (changes 1-3) and part 2 at `d2b9c32` (changes 4-8). The
daily-review session recorded their results here.

**Operator (2026-09-24, daily review):**
- **The pause change is REVISING.** A fresh round re-derives it, with this review as input. The
  operator's inputs:
  - archiving a paused agent **withdraws its queued input**;
  - a pause **takes precedence** over anything that starts or wakes a turn: a declared reviewer's
    review, and a divergence retry or escalate, wait until the agent is resumed.
- **`request_agent` drops `hub_client`** from the copied config, for least authority.
- **The field-split change (F366) is fixed and approved:** its REVISE verdict was only for the missing
  MODIFIED delta and for `status` being required.
- **D8 is accepted as designed:**
  - a review staged late by a divergence restaff stays "You moved";
  - a manual Run press reads "Loop X moved";
  - the 38 old rows keep "You moved".
- **Agents setting holder, priority and description at task creation** is filed as a finding
  (F443).
- **F77:** a refusal only; the retired-requirement removal ships in this change.
- All other changes had their fixes applied and were approved.

## Part 1

### 1. `agents-no-longer-register-themselves`: APPROVE WITH FIXES

The deletion is sound, and no data is lost: on `:8000` (mode=ro), all 8 agents are
`self_registered=0`, and `project_sessions` has 0 rows.

- **HIGH: the test inventory is incomplete.** These tests build rows or SQL with the dropped column
  and will break:
  - `test_dashboard_truth.py:43-48,120,608,651`: `Agent(self_registered=...)` raises TypeError.
  - `test_operator_is_told_the_truth.py:139-219`: the test at `:183` asserts the exemption being
    removed. Delete it or invert it.
  - `test_migrations.py:724`, `:3597`, `:3602`: raw `INSERT ... self_registered`.

  **Fix:** add these to task 2.7, and widen the test guide's grep to `hub/tests`.
- **MEDIUM: the migration shape.**
  - Follow `0013_drop_agent_pilot_columns.py`: `batch_alter_table("agents", recreate="never")`, with
    a guard on each column. A recreate would rebuild a table carrying `ck_agents_lifecycle` and
    `ix_agents_project_name`.
  - On `:8000`, `self_registered` is `BOOLEAN NOT NULL` with no server default (measured, SQLite
    3.45.1), and `engine.py:230` swallows a failed upgrade and starts anyway. If this migration
    fails, every agent INSERT after it fails NOT NULL.
  - Add a migration test seeded from `:8000`'s real `CREATE TABLE agents`, followed by an ORM
    insert.
- **MEDIUM: the e2e harness hides the failure.** `.claude/skills/e2e-loop/e2e.py:124-133` swallows
  the register `RuntimeError`, then PATCHes, so the failure surfaces as a confusing 404.
  - Switch it to `POST /agents`, in the gitignored mirror `.agents/skills/e2e-loop/e2e.py` too.
  - `docs/reference/hub-api.md:62` also describes PATCH as "Update self-registered agent…".
- **LOW: re-fixturing changes what the tests mean.** Registered test agents were self-registered
  `poll` rows, which got the launchability exemption and the job skip. Re-run the 19 files and read
  what fails; do not just substitute a fixture.

### 2. `request-agent-models-the-new-agent-on-one-the-operator-made`: APPROVE WITH FIXES

The config keys the code actually reads are `runner`, `model`, `cli`, `yolo`, `read_only`,
`env_vars` and `hub_client`.
- Dropping `yolo` closes the path to `--dangerously-skip-permissions` (`runner_commands.py:276-277`).
- Grants and `default_permission_mode` are not copied.

Findings:
- **MEDIUM: D5 omits `config["hub_client"]`, which decides posture.**
  - The operator's `hub_client` "moves" the permission posture (`agent_trigger.py:1045-1056`).
  - With no MCP server, the default becomes `acceptEdits` (`runner_commands.py:81,236-241`).
  - **Fix (operator: drop it):** leave `hub_client` out of the copy, and add a test.
- **LOW: a waiting setting is copied.** `env_vars` can carry `QUESTION_WAIT_ENV`
  (`agent_trigger.py:580`). Document it, or strip it.
- **MEDIUM: no task for the paused-template refusal.** It is written only in design text. Add a task
  (conditional on which lands second).

### 3. `an-agent-can-be-paused-and-keeps-its-input`: REVISE

The lock argument holds. The only caller of `trigger_agent_directly` is `_attempt_turn`, and the
seven delivery and firing sites are complete.

- **HIGH: a paused agent would be told it is waiting on a usage limit.**
  - `agents_held` returns a bare set of names, and `_roster_availability` sets
    `held=name in held_agents` (`scheduler.py:1194`).
  - The rung-3 sentence reads "is waiting for its provider's usage limit to reset"
    (`scheduler.py:1337-1338`), and the tail reads "waiting for a usage limit" (`:1352`).
  - The seven-site count misses these sentence sites.
  - **Fix:** `agents_held` returns a map from name to kind (or add a separate `agents_paused`), plus
    `AgentAvailability.paused` and a "paused" clause. Task 2.2 must list `hold_sentence`,
    `hold_busy_reason`, `hold_coalesce_reason`, `_rung_3_clause_text` and `_rung_3_tail`. Several of
    them take `ProviderHold.hold_until`, which a pause does not have.
- **HIGH: shipped SHALLs are contradicted, and the change has only an ADDED requirement.** Add
  MODIFIED deltas, plus a precedence sentence in the ADDED requirement: "a pause takes precedence
  over any requirement that starts or wakes a turn; that input is queued" (operator: yes). The
  contradicted SHALLs:
  - `agent-flows` :242-247 (the undeclared-reviewer choice);
  - `agent-flows` :851 ("held" means the allowance hold);
  - `agent-flows` :1026-1032 (an exhaustive five-clause list); check against :1069;
  - `agent-loops` :793-823 (the busy guard) and :1463 (coalesce names "the time the hold ends");
  - `agent-capability-plane` :995 ("SHALL be woken");
  - `run-task-binding` :284 (`retry`/`escalate` "SHALL start … a run").
- **Operator input: archiving a paused agent withdraws its queued input.** `archivable`
  (`agent_lifecycle.py:25-60`) refuses today while input is queued.
- **LOW: the waiting reason is hidden.** `_attempt_turn` returns the hop-budget and
  conversation-unavailable reasons before the hold check (`turn_scheduler.py:336-350,373`), and the
  status route checks hop budget first (`inbound_queue.py:136`). Check pause first.
- **LOW: a paused assignee's flow task reads "in flight" with no reason.** Under B1's attendance rule,
  "refused" needs `delivery_attempts>0`, which a pause never sets.

## Part 2

### 4. `a-flows-own-moves-are-recorded-as-the-flows` (D8): APPROVE WITH FIXES

The core claims hold. The scheduler stages only at `scheduler.py:906/908`, and `origin_type="job"`
entries are created only at `:3404`/`:3736`. Reading the cause from delivered entries fires in
production.

- **LOW: S13's text disagrees on the signature.** S13's design (`a-flow-stages-its-review-in-the-dispatch/design.md:285`)
  rebases onto `enter_selected_task(..., origin, job_id)`, but D8's task 2.3 defines only `job_id`.
  Align the two.
- **LOW: task 1.5's source scan will trip.** It scans for `origin="job"`, which the MCP
  `task_history` docstring will contain. Scan for the `ORIGIN_JOB` symbol, or exclude docstrings.
- **LOW: a line number is wrong.** `new_entry` is at `inbound_queue.py:24`, not `:50`.
- **LOW: add a cross-reference.** `task-lifecycle-governance/spec.md:580-587`, "The system may cause a
  transition without becoming an actor", needs a sentence saying a job move acts as the operator.
- **B3.md:328 is now stale.** It says S13's text never mentions the collision. S13's R2 added a
  Collisions paragraph, a governance sentence and task 1.14c, consistent with F327-scope-b and D8.

### 5. `a-message-to-the-operator-is-told-where-the-operator-reads` (F77): APPROVE WITH FIXES

- **MEDIUM: a third stale reference to the retired backstop.**
  `agent-conversation-workspace/spec.md:803` defines "waiting on the operator" as including "an
  undismissed unasked-question flag". Add a MODIFIED delta that drops that clause.
- **LOW: an adjacent change.** `why-queued-input-waits-is-told-truthfully` changes
  `create_message_for_actor` to return `(msg, held)`. There is no conflict: this change's branch
  raises before that point.
- Checked: `create_message_for_actor` is the single door (`agent_actions.py:218`, `messages.py:346`),
  and `.lower()` against the reserved names covers case variants.

### 6. `a-claude-run-is-told-its-agentweave-tools-by-their-full-names` (F139): APPROVE WITH FIXES

The server key is verified: `mcpServers.agentweave` (`runner_commands.py:252-256`), with
`--allowedTools mcp__agentweave__*` (`:262`).

- **MEDIUM: a spec scenario claims more than the change does.**
  - "A run whose harness prefixes injected tools → each AgentWeave tool is named with that prefix" is
    unscoped.
  - A first run has `access_path=="mcp"` but `described_path` `cli` (`launchability.py:305-311`), and
    its HTTP rendering labels operations bare (`agents.py:1484`).
  - **Fix:** scope the scenario to "a run described as having the injected surface", or emit the
    host-`SendMessage` sentence in the HTTP rendering whenever the server is injected.
- **LOW: the preamble wording disagrees with control 1.2.** The unknown-harness scenario says "may
  prefix", but control 1.2 asserts "they are prefixed". Align them.

### 7. `an-agent-updates-a-task-with-what-its-tool-carries` (F366): REVISE, then fixed and approved (operator)

The argument holds; the review asked for these fixes:
- **HIGH: add a MODIFIED delta.** In `task-lifecycle-governance`, the scenario *"Naming a reviewer in
  the same request succeeds"* (`spec.md:445-448`) must be scoped to the operator. Rewrite the
  paragraph at `:358-361` ("The agents' HTTP task route accepts an assignee today…").
- **MEDIUM: make `status` optional in MCP `update_task`.** Today it is required (`mcp_server.py:337`),
  so adding `requirement_ids` forces a status restatement, and restating `blocked` is a 403 for an
  agent.
- **LOW: widen the 403 sentence.** It should also name requirement links.
- **Operator:** agents can still set holder, priority and description when they create a task
  (`POST /agent-actions/tasks`). This is filed as F443, to be decided later.

### 8. `the-operator-can-rename-a-task` (F125): APPROVE WITH FIXES

- **MEDIUM: renaming a blocked task would fail.** `useUpdateTask` requires `status` and always sends
  it (`hub/ui/src/api/tasks.ts:366-378`), so renaming a `blocked` task restates `blocked` without
  `blocked_reason`, and the route answers 422. **Fix:** a separate `useRenameTask` that sends only
  `{title}`, following the `useLandTask` precedent.
- **LOW: `"title": null`.** Refuse it as blank.
- **LOW: where the agent refusal sits.** It belongs in the pre-write check, not "beside description",
  which runs after the transition (`tasks.py:1408`).
- **LOW: bundle skew.** A committed bundle reaches `:8000` before its Python does, so a rename there
  answers 422 until the restart. This follows the `2026-09-14-ui` night-build rule.

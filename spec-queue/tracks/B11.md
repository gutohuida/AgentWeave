# B11 — The remainder decisions

**Findings:** F308, F305, F354, F385, F20, F307, F349, F53, F281, F389, F146, F157, F134, F183, F177, F248, F133, F21, F382, F322, F339, F340
**Decisions:** D13 (less F217, which moved to B6), one recommendation per finding
**Changes:** `openspec/changes/an-agents-tool-server-is-the-one-its-hub-loaded/`, `openspec/changes/the-app-window-keeps-the-operators-preferences/`, `openspec/changes/a-dialog-takes-the-keyboard-when-it-opens/`, `openspec/changes/input-the-hub-accepted-is-answered-as-accepted/`, `openspec/changes/a-loop-that-is-gone-lets-go-of-its-document/`, `openspec/changes/charters-are-named-once-and-an-empty-one-says-so/`, `openspec/changes/a-name-a-caller-chooses-reaches-its-own-resource/`, `openspec/changes/a-run-records-that-its-calls-were-allowed/`
**Status:** R1 done | R2 done | R3 done — operator review 2026-09-24: 7 approved, 1 revising (`APPROVALS.md`; review: `tracks/reviews/B11-2026-09-24.md`)

## R1 — explore and propose (2026-09-24)

Read at worktree HEAD `ce086b6` (the bundle-opening commit on `404c7d5`; it touched no product code).
Every finding's full ledger entry, every `DECISIONS.md` row naming one, the specs each touches, and
the unarchived changes (including the ones other bundles opened tonight) were read first. `:8000`'s
database was read only through a `mode=ro` URI, for five counts cited below. No product code, test,
ledger or decision file was edited.

### Summary

| # | Finding | Re-verified | Recommended answer | Spec? |
|---|---|---|---|---|
| 1 | F354 live agents run MCP from the working tree | open | **Pin the server the Hub loaded** | change 1 |
| 2 | F385 pywebview private mode | open, both halves | **Persist the window profile, and let an unset theme follow the system** | change 2 |
| 3 | F20 deep-link fallback | open; a second instance found | **Canonicalise the address; fix the CLI's `view=` link** | no-spec (UI round) |
| 4 | F307 dialog initial focus | Tab half fixed (UI-1); this half open | **Focus moves in on open; a confirmation opens on Cancel** | change 3 |
| 5 | F349 remainder | open (post-commit 500) | **Answer accepted as accepted, and retry the start in the Hub** | change 4 |
| 6 | F133 queue status recomputes the reason | open | **One read-only selection shared by scheduler and status** | no-spec (Round-3 style), sequenced with B1 |
| 7 | F146 operator-posted blocking question | open | **Refuse `blocking: true` on the operator route** | no-spec (Round-3 style) |
| 8 | F53 a loop's archive releases adopted tasks? | tasks half open; a third defect found | **A successor adopts a gone loop's unfinished tasks; archive stays pure** | change 5 |
| 9 | F157 `spec_document_id` on non-loop jobs | open | **Refuse it on create, as PATCH does** | change 5 (shared) |
| 10 | F134 an empty charter | open | **Refuse blank content; render a legacy empty row honestly** | change 6 |
| 11 | F183 charter name uniqueness | open | **409 at both doors, unique index, rename old duplicates** | change 6 (shared) |
| 12 | F177 runner sequence column | open; changed shape (ties are systematic) | **No migration: make the order total, `(created_at, name, id)`** | no-spec |
| 13 | F248 `/worktrees/conflicts` shadows an agent | open; **a class of three** | **Reserve the shadowed words + a route-table test** | change 7 |
| 14 | F281 shell writes recorded how? | open; changed shape | **Not recorded; already specified as out of scope; fix one comment** | no-spec (comment) |
| 15 | F389 event per allow? | open | **No event per allow; a per-run count** | change 8 (lowest priority) |
| 16 | F305 windows re-ask decided questions | open | **One channel: `decisions_for_user` holds only `DECISIONS.md` ids** | no-spec (harness) |
| 17 | F308 CI drift alarm | **already fixed** (`f7c732b`, on `master`) | **Close; add the job to `check-build`** | no-spec (harness) |
| 18 | F21 retire? | open; **the premise is challenged** | **Do not retire yet: one probe first** | decision + probe |
| 19 | F382 watch-only? | open, undiagnosed | **Watch-only, plus two tripwires** | no-spec |
| 20 | F322 park with F325? | unchanged | **Park with F325** | none |
| 21 | F339 stay parked? | unchanged | **Stay parked; but its doc row is reachable today, split it out** | none (+ a docs fix) |
| 22 | F340 stay parked? | unchanged | **Stay parked** | none |

### How the items group, and how they interact

- **What reaches a live agent or the live app from this checkout** — F354, F385, F20, F307. F354's
  answer decides when any `mcp_server.py` edit reaches `:8000`'s agents; this bundle's own changes
  touch none of that file, but **B1's `why-queued-input-waits-is-told-truthfully` and B12's
  `a-specification-is-read-in-results-that-fit` both edit it** (each notes the hazard).
- **Answers that describe what happened** — F349, F133, F146. All three are a route whose reply
  disagrees with the Hub's state. F349 and F133 both sit on the queue; B1's
  `why-queued-input-waits-is-told-truthfully` rewrites the same `get_queue_status` (its D3/D4), so
  F133 must land in that change or strictly before it. F349's change edits `messages.py:318`, which
  B1's change also edits (`create_message_for_actor`); different lines, sequence either way.
- **Who holds a document** — F53, F157: one change.
- **What a charter or a name may be** — F134, F183, F248, F177. F177's order defect also applies
  to charters (measured below), and F183's uniqueness gap also applies to runners (change 6, Open
  Question 1). F248's reserved words change the agent-name rule CLAUDE.md says to change in three
  places.
- **What a record can prove** — F281, F389, and the parked F339/F340. One consistent answer: record
  what the Hub observes (F389's count), and label what it cannot observe as outside the record
  (F281, already a SHALL). F339's measurement (Claude Code confines `acceptEdits`) narrows F281.
- **The loop's own harness** — F305, F308, F382. F305 is how every answer in this bundle gets
  recorded without being re-asked (ROUNDS.md item 6 routes D13 through `daily-review`).
- **Parked** — F21, F322, F339, F340.

### 1. F354 — live agents run MCP from the working tree (open)

**Re-verified.** `agent_trigger.py:1141-1144` still names `Path(__file__).resolve().parents[2] /
"mcp_server.py"` on every spawn; it reaches Claude as `--mcp-config` (`runner_commands.py:250-259`)
and Codex as `config.mcp_servers` (`codex_appserver.py:969-972`). `mcp_server.py` imports only the
stdlib and `fastmcp` and never reads its own location (`grep`), so a copy is the same program.
It is the only repository file spawned per turn (`grep -rn sys.executable hub/hub`).

**Options.** (a) pin the bytes at Hub start and spawn a content-addressed copy outside the repo;
(b) a rule never to edit `mcp_server.py` in the checkout `:8000` runs (the status quo, which has
blocked F363 since 2026-09-14); (c) run `:8000` from elsewhere (contradicts CLAUDE.md's
"by intent"); (d) `python -m hub.mcp_server` (still reads the working tree).

**Recommended: (a).** It is the only option under which the Hub's routes and its tool server
change on the same event, a restart. Written as `an-agents-tool-server-is-the-one-its-hub-loaded`.
A pin that cannot be written refuses the turn through the context file's existing
`TriggerAgentError` 409 path (`agent_trigger.py:1110-1118`); it never falls back.

**For B12 (F363), explicitly:** B11 recommends pinning. Once that change is built **and `:8000` has
been restarted onto it**, an edit to `mcp_server.py` in the checkout reaches `:8000`'s agents only at
`:8000`'s next restart, together with the routes it calls, like every other Hub file. So F363 needs
no version-skew analysis for the pinned case. Recommended order: this change's IMPL → the operator's
`:8000` restart → F363's `mcp_server.py` half. B12's own D5 shows F363 degrades gracefully if it
ships first, so this is a preference, not a gate. Until the restart, `:8000` still spawns the
working tree's file, so an uncommitted edit there still reaches live agents.

### 2. F385 — pywebview private mode (open)

**Re-verified.** `cli.py:1025-1027` passes neither `private_mode` nor `storage_path`; pywebview
6.2.1's `start()` defaults `private_mode=True` (measured with `inspect.signature`). The store holds
far more than the theme: `grep localStorage.setItem hub/ui/src` finds the open project, the sidebar
layout, model favourites, spec presentation, panel tabs and **unsent composer drafts**.
`configStore.ts:49` still defaults to `'light'`; nothing reads `prefers-color-scheme`.

**Options.** Persistence only; system default only; both; neither. **Recommended: both**, as
independent halves of `the-app-window-keeps-the-operators-preferences`. The profile goes under the
CLI's own `HUB_DIR / "window"` (`cli.py:242`), so a future reset (`R4`) can find it. The credential
stays in `sessionStorage` (`configStore.ts:37-40`), and `index.html` is served without validators
(`main.py:573-579`), so a persistent HTTP cache cannot pin a stale bundle.

### 3. F20 — deep-link fallback (open, no-spec)

**Re-verified.** Navigation reads only `window.location.search` (`useWorkspaceNavigation.ts:11`);
the SPA route serves `index.html` for any path (`main.py:573-579`); `replaceState` targets a bare
`?…` so the unknown **pathname survives** in the address bar (`useWorkspaceNavigation.ts:37`).
Unknown `tab` values coerce to Overview silently (`navigation.ts:372-374`). **A second instance, in
the product itself:** the CLI builds its launch URL as `/?project=<id>&view=overview`
(`src/agentweave/cli.py:306`, since `ab53cf4`); no parser reads `view`, and it only works because
Overview is the default.

**Options.** (a) alias `/projects/{id}/{tab}` paths; (b) canonicalise: replace the address with `/`
plus the resolved destination's own query, dropping what was not understood; (c) a notice for an
unknown link; (d) retire as by-design. **Recommended: (b), plus fixing `cli.py:306` to `tab=`.** The
app window has no address bar, so a notice would reach only browser users, and (b) makes the address
state what is on screen. No spec: *"Conversation navigation is URL-backed"* already implies it.
Tests: `useWorkspaceNavigation.test.tsx`, starting at `/projects/p/tasks?project=p&tab=tasks&x=1`,
asserts `location.pathname === '/'` and `location.search === '?project=p&tab=tasks'` after
resolution (fails today: the pathname stays); `tests/test_cli.py` asserts `_hub_project_app_url`
carries `tab=overview`.

### 4. F307 — dialog initial focus (Tab half fixed, this half open)

**Re-verified.** UI-1 fixed the first Tab (`useDialogFocus.ts:58-68`) and left initial focus to D13
in its own comment. The effect records the opener (`:28`) and never moves focus. Seven components
use the hook; three are confirm-only (Cancel first in the DOM), three `autoFocus` an input, one is
the task drawer.

**Options.** Focus Cancel; focus the destructive button; focus the panel; leave it. **Recommended:
focus moves in; a confirmation opens on Cancel, chosen by an explicit mark, not DOM order.** Written
as `a-dialog-takes-the-keyboard-when-it-opens`. **R1 also inferred a restore defect**, not measured:
React applies `autoFocus` in the commit, before the hook's effect records `returnFocusTo`, so for the
three `autoFocus` dialogs the recorded element is the input that unmounts, and closing leaves focus
on `<body>`. The change's task 1.6 measures it before D3 builds on it.

### 5. F349 remainder (open)

**Re-verified.** After the entry commit (`agent_trigger.py:1587`), `persist_event` (`:1595`) and
`schedule_agent` (`:1600`) are unguarded; `schedule_agent` catches only `TriggerAgentError`
(`turn_scheduler.py:424`). Nothing retries a queued entry on a timer. Four more routes have the same
post-commit shape: `messages.py:318`, `agents.py:2257`, `questions.py:212`, `inbound_queue.py:253`
(read, not measured). And the existing deferred-drain backstop loses a pair whose drain raises
(`run_reconciliation.py:184-187`, `:205`).

**Options.** (a) answer accepted and retry in the Hub; (b) answer accepted without a retry (still
false in effect); (c) keep 500 and add a retry (invites duplicate sends); (d) schedule inside the
transaction (a spawn cannot roll back). **Recommended: (a)**, written as
`input-the-hub-accepted-is-answered-as-accepted`: one never-raising `schedule_accepted`, a bounded
1 s / 5 s / 30 s retry, then the existing deferred set as backstop.

### 6. F133 — queue status recomputes the reason (open, no-spec)

**Re-verified.** The status route's token-budget branch asks `all(entry.origin_type != "operator")`
over every queued entry (`inbound_queue.py:140-144`). The scheduler asks whether any operator entry
is in the **selected turn**: the controlling entry's conversation, within the hop budget, of a
compatible kind (`turn_scheduler.py:340-376`). An operator entry in another conversation, behind an
autonomous head, satisfies the route and not the scheduler, so the route stops saying why.

**Options.** Mirror the selection in the route (narrow; keeps two copies), or factor the pure part of
the selection out of `_attempt_turn` (`controlling`, `selected`, `initiator` from entries, budget and
cap; the conversation-lifecycle check stays in the scheduler) and have both call it. **Recommended:
the shared function**, as a Round-3-style fix with no spec. **Sequencing:** B1's
`why-queued-input-waits-is-told-truthfully` rewrites `get_queue_status` (its D3, D4); land F133 as a
task inside that change's IMPL or strictly before it, never in parallel. Tests (in the route's own
order, `sequence` ascending): an autonomous entry in conversation A, then an operator entry in B,
budget exhausted → `waiting_reason == "token budget exhausted"` (fails today: `null`); reversed (the
operator's entry first) → the reason is **not** the budget one, because the scheduler would start it.
The reversed case is what fails if the test's order were the one the route never returns.

### 7. F146 — an operator-posted blocking question (open, no-spec)

**Re-verified.** `POST /questions` passes `created_by_run_id=None` (`questions.py:316`);
`_asking_run_has_ended` returns False for an unset asker (`:51-52`), so a blocking operator-posted
question is "still waiting" forever and its answer is never delivered. **No product caller uses this
route:** no UI hook posts to it (`hub/ui/src/api/questions.ts` has none), the MCP server calls only
`/questions/batch` and `/questions/{id}` (`mcp_server.py:396`, `:422`, `:488`, `:528`), and the CLI
has none. Only tests and drive harnesses do.

**Options.** (1) refuse `blocking: true` (422); (2) deliver (needs a discriminator from legacy rows);
(3) a sentinel asker (a migration). **Recommended: (1).** It is the only option that cannot recur,
and it changes nothing any product surface sends. Round-3-style, no spec. Update
`_asking_run_has_ended`'s docstring, which cites the operator route as a case it serves. Tests
(`hub/tests/test_questions.py`): `blocking: true` → 422 naming `blocking` (fails today: 201);
`blocking: false` still delivers on answer (control).

### 8–9. F53 and F157 — who holds a document (change 5)

**F53 re-verified:** `2239f38` fixed the claim; `_adopt_document_tasks` still adopts only
`loop_id IS NULL` (`jobs.py:266`) and neither archive door clears anything. **R1 found a third
defect:** `spec_tasks.materialise` picks the owning loop by document alone, with no `archived_at`
filter, no project filter and no ordering (`spec_tasks.py:176-180`), so since `2239f38` a later
approval can stamp new tasks with an archived loop. Read, not measured (the change's task 1.4).

**F53 options.** (a) done; (b) clear `loop_id` at archive for never-started tasks (needs a backfill
for loops already archived, two doors, and erases the link when nobody takes the work); (b′) a
successor claiming the document adopts every non-terminal task a **gone** loop owns; (c) an operator
reassign control. **Recommended: (b′)**, which also releases LoopEngine-style held tasks from the
staffing gate's "unreachable" test (`scheduler.py:1165-1180`) and recovers tasks already stranded in
real databases the first time the document is claimed again.

**F157 re-verified:** `create_job` reads `spec_document_id` only inside the opt-in branch
(`jobs.py:746-763`); PATCH refuses it (`:989-1011`). No test asserts the silent drop: the ledger's
`test_jobs_crud.py:537` cite has moved, and only a docstring at `:602` mentions it. **Recommended:
refuse on create**, with the `work_needs_evidence` wording (`:621-631`). The MCP loop tools always
opt in (`purpose` defaults to `""`, `mcp_server.py:681`), so no product surface changes behaviour.

Written together as `a-loop-that-is-gone-lets-go-of-its-document`.

### 10–11. F134 and F183 — charters (change 6)

**Re-verified.** `content: str` is unconstrained on create and update (`schemas/charters.py:13,18`);
the renderer tests the row (`agents.py:2108-2118`). No name check at either door
(`charters.py:18-34`, `:62-80`); `ix_charters_project_name` is plain (`models.py:366`). The agent
precedent moved: the 409 is at `agents.py:682` now (the entry cites `:612-617`). **Measured on
`:8000`** (`mode=ro`): 0 blank-content charters, 0 duplicate charter names.

**F134 options.** Refuse at the API; render as "no charter" and count it missing; a third statement.
**Recommended: refuse at both doors, and render a pre-existing empty row with its own sentence and
`missing: "charter content"`** (a guard for rows the schema never saw). **F183 options.** API check
only; API check plus a unique index. **Recommended: both**, with the index migration renaming old
duplicates (`X (2)`, oldest keeps the name), following migration `0063`'s reasoning for agents.
Written as `charters-are-named-once-and-an-empty-one-says-so`. Open for the operator there: runner
names have the same gap (`ix_runners_project_name` is plain; `runners.py` checks none).

### 12. F177 — a runner sequence column (open; changed shape; no-spec)

**Re-verified:** `runners.py:82` still orders by `created_at` alone. **Changed shape, measured on
`:8000`:** ties are not only a 15.6 ms coincidence. Every seeded project's default runner pair shares
one `created_at` exactly (3 of 3 projects), and **all nine seeded charters share one** (3 of 3), so
both lists fall back to name order for every seeded row. `charters.py:44` orders by `created_at`
alone too. No consumer reads "the first runner" (`grep -rn "runners\[0\]" hub/ui/src` finds none).

**Options.** (a) a `sequence` column (the precedent for `Conversation`, `Checkpoint`,
`TaskTransition`; a table rebuild for a presentation list); (b) a total order with no migration:
`order_by(created_at, name, id)`; (c) declare name order. **Recommended: (b), for runners and
charters,** because no correctness decision reads either order; the ledger's "needs a migration"
premise does not hold. If a consumer ever reads the first row, (a) is the precedent to follow then.
No spec. Tests (`hub/tests/test_runners_api.py`, `test_charters_api.py`): two rows with an identical
`created_at` inserted in reverse-name order list by name; renaming one moves only that one.

### 13. F248 — a class of three (change 7)

**Re-verified, and widened.** A scan of every `@router.<method>("…")` in `hub/hub/api/v1` for a
literal declared beside a `{param}` at the same depth finds three pairs where the parameter is a
caller-chosen name: `GET /worktrees/{agent}` vs `conflicts` (the finding), `GET /queue/{agent}` vs
`settings` (`inbound_queue.py:80` before `:199`), and `GET /tasks/{task_id}` vs `board` and `boards`
(`tasks.py:943`, `:1009` before `:1234`; `TaskCreate.id` accepts any `^[a-zA-Z][a-zA-Z0-9_-]{0,63}$`,
`schemas/tasks.py:17`). The UI calls `/queue/${agent}` (`api/queue.ts:40`). Measured on `:8000`: no
agent is named `conflicts` or `settings`, and no task id is `board` or `boards`.

**Options.** (a) reserve the words, plus a test that walks `app.routes` so the next collision fails
the suite; (b) move the literal routes (UI callers, and a bundle/backend skew on `:8000`); (c) move
the parameter routes (impossible for `/tasks/{task_id}` without moving the most-used route);
(d) reorder (swaps the victim). **Recommended: (a)**, written as
`a-name-a-caller-chooses-reaches-its-own-resource`.

### 14. F281 — shell writes recorded how? (open; changed shape; no-spec)

**Re-verified:** `written_paths("Bash", …)` still returns `()`. **Changed shape:** (i) the corpus
already decided it. `agent-run-sandboxing` (`spec.md:413` onward) says *"Scope is part of the
requirement … It SHALL NOT be described, labelled or surfaced as a complete account … a shell
command … [is] out of scope"*. (ii) F339 measured Claude Code refusing a shell redirect outside the
workspace under `acceptEdits` (2.1.269), so "an outside write is possible" no longer holds for that
posture. (iii) Operator-approved calls are rows in `permission_requests`, listable since F231
(UI-1). What remains uncovered is full access, by the operator's choice.

**Recommended: not recorded; close F281 as decided by the existing requirement.** One residual: the
column comment at `hub/hub/db/models.py:1214` says *"Read an empty list as 'nothing left this run's
boundary'"*, which a shell write contradicts; reword to "no file tool wrote outside this run's
boundary". A Round-0-style comment fix, no test.

### 15. F389 — event per allow? (change 8)

**Re-verified:** `record_permission_decision` persists refusals only (`agent_actions.py:991-1009`);
Codex reports refusals only (`codex_appserver.py:1083-1100`). **Recommended: no event per allow
(the spec's refusal rule already forbids it as a refusal record), and instead a per-run count**,
written on first sight and flushed at the run's end like `outside_write_record.py`, so the write load
is at most three rows per run, which matters while `database is locked` is live (F349, F292). Written
as `a-run-records-that-its-calls-were-allowed`, marked lowest priority in B11: nothing is broken
today. If the operator prefers to accept the gap, close F389 and drop the change.

### 16. F305 — windows re-ask decided questions (open, no-spec harness)

**Re-verified:** the day window still inherits the night's `decisions_for_user`
(`.claude/loops/day-window.md:175-178`, step 3), and nothing reconciles it with `DECISIONS.md`.
`arm-cycle.ps1:249` resets the list at each arming, which shortens a stale entry's life to one
handoff but does not stop it crossing night → day. `STATE-night.json` carries one entry tonight.

**Options.** (a) the composing window clears an inherited entry whose question `DECISIONS.md` has
decided (two channels, reconciled by judgement); (b) one channel: a window that raises a decision
writes an `OPEN` row in `DECISIONS.md` (its contract already allows windows to add rows) and
`decisions_for_user` carries only that row's id; the review page lists `OPEN` rows read from the
file. **Recommended: (b)**, F305's own "cleaner and larger". Harness work, no spec: edit
`day-window.md` step 3 and D-5, `night-window.md`'s decision rule, `arm-cycle.ps1`'s limit line
(`:256`), and the `autonomous-prep`/`autonomous-session` skills' description of the field. A check
belongs with it: at compose, every id in an inherited `decisions_for_user` must name an `OPEN` row,
and the window drops any that does not, saying so in its log.

### 17. F308 — CI drift alarm (already fixed)

**Fixed `f7c732b`** (2026-09-22), and **armed**: `.github/workflows/upstream-drift.yml` is on
`master` and `origin/master` (`git branch -a --contains f7c732b`), so GitHub reads its schedule. It
has not run yet (`gh run list --workflow upstream-drift.yml` returns nothing); its first scheduled
firing is Monday 2026-09-28 07:00 UTC. **D13's row is stale.** One residual the fix itself names:
its result *"is read when it fires"*, and nothing reads it: the `check-build` skill covers
`ci.yml`, `publish.yml` and `hub-image.yml` only (`grep -rn upstream-drift .claude/` finds nothing).
**Recommended: close F308; add the drift job to `check-build`** (no-spec harness line). Worth a
`gh run list` check after 2026-09-28 to confirm the first run happened.

### 18. F21 — retire? (open; the premise is challenged)

The proposed answer was "retire: not fixable inside AgentWeave", on the ground that whether Claude
Code defers MCP tool schemas behind a search step is the harness's behaviour. **Two things R1 found
weaken that:**

- **The harness has a setting for it.** The installed `claude` (2.1.280) reads an
  `ENABLE_TOOL_SEARCH` environment variable (strings in the binary: an unset or `auto` value means a
  10%-of-context threshold, a number sets the percentage, and a `standard` mode logs
  `result=false`). The Hub already sets its own variables in the run's environment
  (`agent_trigger.py:1184-1187`). Semantics **unverified by a run**.
- **The Hub does not isolate the run's MCP configuration.** `build_command` passes `--mcp-config`
  without `--strict-mcp-config` (`runner_commands.py:250-259`; `grep strict-mcp hub/hub` finds
  nothing), so a run may also load servers from the operator's own configuration, which count toward
  the threshold that triggers deferral. Whether any such servers reach a headless run on this machine
  is **unverified** (the user-level `mcpServers` in `~/.claude.json` is empty; account-level
  connectors were not checked).

And `agent-tool-surface` *"An invocable tool surface is the default, not an opt-in"* already requires
that a newly created agent's runs have a tool surface *"the agent can actually call"*. So F21 may be
a breach of a shipped requirement with a fix in `runner_commands.py`.

**Recommended: do not retire yet.** Run one probe (Haiku, per the cheap-model rule) with three arms:
the default argv; `--strict-mcp-config`; `ENABLE_TOOL_SEARCH` set to disable deferral. For each,
read the `system/init` line's tool list for `mcp__agentweave__record_evidence` and whether a search
tool is offered. If an arm makes the Hub's tools directly callable at an acceptable context cost, F21
becomes a small spec change on `agent-tool-surface`. If none does, retire it as proposed. Also worth
a separate finding whatever the probe shows: a Hub run may inherit the operator's own MCP servers.

### 19. F382 — watch-only? (open, undiagnosed)

**Re-verified:** narrowed to `test_stop_endpoint_marks_run_stopped_and_broadcasts_run_stopped`
(2026-09-22); candidate (a) is removed by `f9e6dee`; candidate (b) stands: `_stoppable_pty`'s fake
read blocks in `released.wait()` with no timeout (`hub/tests/test_agent_trigger.py:1932-1935`). CI's
hub job runs `--timeout=300 --timeout-method=thread` (`.github/workflows/ci.yml:143`); the loop
windows' local full-suite runs do not.

**Recommended: watch-only, with two tripwires** (no-spec): bound the fixture's wait
(`released.wait(timeout=30)`, then fail loudly), and have the windows' local full-suite command pass
the same `--timeout=300 --timeout-method=thread` as CI, so a recurrence dumps a stack instead of
freezing silently. Close F382 if the tripwired suite runs clean for two weeks.

### 20. F322 — park with F325 (unchanged)

`codex_appserver.py:280-283` still decides a workspace-posture command by its `cwd` alone; `network`
appears only in the full-access grant (`:118`). Codex is undrivable, and `DECISIONS.md` keeps F325
open by the operator's choice (`f325`). **Recommended: park with F325**, reopening together.

### 21. F339 — stay parked (unchanged), with one split

The `DEFERRED` row (`DECISIONS.md` `f299-f301`, 2026-09-21) parks the 1b question until a
non-MCP runner exists; `RUNNER_CLIS` is unchanged (`models.py:311`). **But the doc half is reachable
today:** `docs/reference/permission-postures.md:33` says `acceptEdits` is *"Not checked"*, and
`acceptEdits` is the "Edit files" posture any operator can pick for any Claude run
(`model_catalog.py:215`). The parking reason (no runner reaches the approver-less path) covers
`:39`, not `:33`. **Recommended: stay parked for the 1b question; split the `:33` row into a no-spec
docs fix**, after re-measuring on the installed 2.1.280 with F339's own script (one Haiku turn),
worded as *"checked by Claude Code, not by the Hub"* with the version it was measured on.

### 22. F340 — stay parked (unchanged)

`harness_has_honoured_mcp` is unchanged and `runner_parsing.py` still has no `system` branch.
**Recommended: stay parked** per 2026-09-21. Note for its reopening: F354's pin removes this
checkout's likeliest cause of the Hub's own server failing to start (a half-written
`mcp_server.py`), which is the case F340 says the Hub records nothing about.

### Changes written (all `openspec validate --strict` pass)

| Change | Findings | What it does | Size |
|---|---|---|---|
| `an-agents-tool-server-is-the-one-its-hub-loaded` | F354 | Pin `mcp_server.py` at Hub start; spawn the pinned copy; refuse, never fall back | S, no migration, no UI |
| `the-app-window-keeps-the-operators-preferences` | F385 | Persistent WebView profile under `~/.agentweave/hub/window`; unset theme follows the system | S, CLI + a UI bundle |
| `a-dialog-takes-the-keyboard-when-it-opens` | F307 | Focus into the dialog on open, Cancel on confirmations by mark, opener recorded first | S, UI bundle |
| `input-the-hub-accepted-is-answered-as-accepted` | F349 | Never-raising post-commit scheduling at five routes, with a bounded retry | M, no migration |
| `a-loop-that-is-gone-lets-go-of-its-document` | F53, F157 (+ materialise) | Successor adopts a gone loop's unfinished tasks; materialise stamps only a live loop; create refuses a stray document | S–M, no migration |
| `charters-are-named-once-and-an-empty-one-says-so` | F134, F183 | Blank content refused; empty row rendered honestly; name unique with an index | M, one migration |
| `a-name-a-caller-chooses-reaches-its-own-resource` | F248 (+ two instances) | Reserve `conflicts`, `settings`, `board`, `boards`; a route-table test | S, no migration |
| `a-run-records-that-its-calls-were-allowed` | F389 | A per-run `{allowed, refused}` count, first sight + flush | M, one migration, lowest priority |

Each change's `tasks.md` names its failing-first tests and why each fails if the fix is reversed,
and each `design.md` states what its route returns when the function it calls raises.

### Found in passing, for the ledger (not filed here: R1 does not edit `FINDINGS.md`)

1. **`spec_tasks.materialise` can stamp new tasks with an archived loop** (`spec_tasks.py:176-180`).
   Carried by change 5.
2. **F248's class has two more instances**: `GET /queue/settings` and `GET /tasks/board(s)`. Carried by
   change 7.
3. **Seeded runners and charters tie on `created_at`** in every project (measured on `:8000`), so the
   charter list has F177's defect too. Carried by F177's no-spec fix.
4. **The CLI's launch URL carries `view=overview`**, a parameter nothing parses (`cli.py:306`).
   Carried by F20's no-spec fix.
5. **Closing an `autoFocus` dialog may leave focus on `<body>`** (inferred, not measured). Carried by
   change 3, task 1.6.
6. **A Hub run may load the operator's own MCP servers** (`--mcp-config` without
   `--strict-mcp-config`). Unverified whether any exist for a headless run here. See item 18.
7. **Runner names have no uniqueness rule**, the same gap as F183. Change 6, Open Question 1.
8. **The drift alarm has no reader** (`check-build` omits it). Item 17.
9. **F339's `:33` doc row is reachable today** through the "Edit files" posture. Item 21.

### Could not resolve

- **F21's probe** needs a real `claude` turn and was not run (R1 starts no Hub and spends no turn).
  The recommendation depends on it.
- **F307's restore defect and F248's two new rows** are read, not measured; both changes measure them
  first.
- **Migration numbering**: changes 6 and 8 each need a migration, and other bundles also plan
  `0106`. Both say "the next free revision at IMPL time".

## R2 — independent comparison (2026-09-24)

Read at the worktree's HEAD; product code untouched. Route order was rebuilt from the **built app**
(`app.routes`, imported with the worktree's `hub/` on `PYTHONPATH`), and `:8000` was read only
through `mode=ro` (two queries). 41 load-bearing claims were checked; 8 disagreed with the code.

### Claims that disagreed, and what changed

| # | Claim (R1) | What the code shows | Changed |
|---|---|---|---|
| 1 | F248: a reserved task id is refused by "the `TaskCreate.id` validator" with a 422, "no new exception path" | The agent door has its own `AgentTaskCreate.validate_id` (`agent_actions.py:127-132`) and then builds `TaskCreate(**body.model_dump())` **inside** the handler (`:233`). A `TaskCreate`-only fix makes MCP `create_task` with id `board` answer **500** | change 7: one shared check that both models call; task 1.4 now covers both doors |
| 2 | F248: the other literal/parameter pairs are Hub-minted `runner-…`, `charter-…`, `job-…` | The `app.routes` walk finds only `runners/launchability*` beside `{runner_id}`. No charter or job pairs exist | proposal corrected; D2's test now keys on a segment match plus an allowlist of minted parameters, not on the names `{agent}`/`{task_id}` |
| 3 | F349 D3: move `persist_event` into a `try` and continue | `persist_event` commits on the **route's** session (`utils.py:72`). After a failed commit, the refusal branch reuses that session (`withdraw_refused_entry`, `:1625`) and gets `PendingRollbackError`. A rollback expires the rows the answer reads (`MissingGreenlet`, the trap `turn_scheduler.py:402-405` names) | change 4 D3: capture plain values after `:1587`, then roll back in the `except`; new task 1.9 |
| 4 | F349: four more routes have the post-commit shape | **Six.** Also `accounting.py:78`, where one raise also skips every later agent in the loop, and the runner rebind at `agents.py:2693`. `checkpoints.py:334` is out of scope: nothing is committed before it | change 4 D4 and task 1.6 widened |
| 5 | F354 task 2.2: `from … import pinned_server_path` at module top | Task 1.6 patches `hub.tool_server.pinned_server_path`. A name bound by `from … import` would not see that patch, so the test would fail against a correct fix | change 1 task 2.2: import the module and call `tool_server.pinned_server_path()` |
| 6 | F177 (b) `order_by(created_at, name, id)`, tested by "two tied rows inserted in reverse-name order list by name (fails today)" | **It passes today.** SQLite reads through `ix_runners_project_name` / `ix_charters_project_name` and sorts by `created_at` in a temp B-tree, so ties already come out in name order (`EXPLAIN QUERY PLAN` on `:8000`: `SEARCH … USING INDEX ix_charters_project_name`; `:8000`'s seeded charters list alphabetically). (b) writes the defect down as the rule. It does not restore creation order | **recommendation reversed** (below) |
| 7 | F307 D3: the restore defect is "inferred, not measured" | Confirmed from source. React DOM 18.3.1 focuses `autoFocus` in `commitMount` during the layout phase (`react-dom.development.js:11029-11030`), before any `useEffect` runs, so `returnFocusTo` is the input. On close `AgentCreateDialog` returns `null`, and the cleanup focuses a detached node | change 3: D3 reworded. Task 1.6 still runs it |
| 8 | F389 D1: operator-answered refusals are counted | Only if `note()` runs before the `_operator_already_refused` early return (`agent_actions.py:989-990`) | change 8 round log |

### Claims that stand (checked, agreed)

F354 spawn site, stdlib-only server, only spawn site (`grep "mcp_server"` over `hub/hub` and
`src/`). There is one non-Hub importer, `src/agentweave/mcp/server.py`: out of scope, noted. F385:
pywebview `start()` has `private_mode=True, storage_path=None` (measured by signature), and
`configStore.ts:49` defaults to `'light'`. F20: `replaceState(target)` keeps the pathname
(`useWorkspaceNavigation.ts:37`), and `cli.py:306` sends `view=overview`. F53: adoption, both calls,
`:734` commits before the conflict check, materialise has no `archived_at` filter, the partial index
is `0090`. F157, F134, F183: schemas and both doors; the seed paths only insert into a project with
no charters, and the manifest's names are distinct, so the unique index cannot make seeding raise.
F133: route `:140-144` against scheduler `:340-376`. F146: `created_by_run_id=None` (`:316`),
`:51-52`. F281: `agent-run-sandboxing` `:413-418` already scopes shell writes out;
`models.py:1214`'s comment is still worded as R1 says. F382: the fixture's unbounded wait. F21:
`--strict-mcp-config` exists in 2.1.280 and the Hub never passes it.

### The items R1 left for R2

- **F248's two new rows: confirmed** from the built app's registration order: `/queue/settings`
  before `/queue/{agent}`, and `/tasks/board`, `/tasks/boards` before `/tasks/{task_id}`. No parameter
  route hides a later literal. `:8000`: no agent `conflicts`/`settings`, no task `board`/`boards`.
- **F307's restore defect: confirmed from React's source** (row 7). The run-level check is still
  task 1.6.
- **F177's "ties in every seeded project": confirmed** (`mode=ro`): 3 of 3 projects have one tied
  default-runner pair, and in each project all 9 charters share one `created_at`. The cause is the
  seed adding every row in one flush (`project_lifecycle.py:298-320`, `db/engine.py:280-289`).
- **F281 "already decided by `agent-run-sandboxing`": confirmed** (`spec.md:413-418`: *"SHALL NOT
  be described … as a complete account"*, and *"a shell command"* is named out of scope).

### F177: the recommendation reversed, from (b) to a rowid tiebreak

**Recommended now: `order_by(created_at, rowid)` for runners and charters**, with
`literal_column("<table>.rowid")`. No migration.

- It restores what the ledger asked for, creation order. `rowid` is assigned at insert as
  max+1, so among the rows that exist it is insertion order. Migration `0063` already relies on
  `rowid` for the same "oldest first" question. The Hub is SQLite-only.
- The failing-first test is real: insert `ZB` and then `ZA` with an identical `created_at`. Today the
  list gives `ZA, ZB`. Fixed, it gives `ZB, ZA`. Renaming `ZA` to `AA` does not move it.
- **Visible effect on `:8000`:** seeded charters change from alphabetical order to manifest order,
  and each project's default runner pair changes to seed order. Worth one line in the UI round's
  notes.
- **Caveat:** SQLite's `VACUUM` may renumber the rowids of a table without an `INTEGER PRIMARY KEY`.
  In practice it copies them in order, but that is not guaranteed. The Hub does not `VACUUM`. If it
  ever must, option (a), a `sequence` column, is the precedent.
- If the operator would rather declare name order ((c)), then (b) is the right code. In that case the
  tests are regression pins, not failing tests, and the round must say so.

### F133: concrete enough for B1's `why-queued-input-waits-is-told-truthfully` (its task 2.0)

Land this **strictly before** that change. Its task 2.0 stops if this is not there.

- **Where:** `hub/hub/inbound_queue.py`, beside `can_start`, `queued_entries` and `project_limits`.
  Both the scheduler and the status route already import from this module.
- **What:** a pure function, `select_turn(entries, hop_budget, cap) -> TurnSelection`, with fields
  `controlling`, `selected`, `initiator` (`"operator"` | `"autonomous"`) and `blocked`
  (`"hop budget exhausted"` | `"queued entry has no conversation"` | `None`). It is the body of
  `turn_scheduler.py:335-377` with **one substitution**: `selected` filters on
  `controlling.conversation_id` instead of `conversation.id`. The two are equal whenever the
  scheduler goes on to start a turn, because the lifecycle check requires it.
- **Scheduler:** `_attempt_turn` calls `select_turn`, then keeps its own conversation lookup and
  lifecycle check (`:342-351`) and the rest unchanged. This is a behaviour-neutral refactor, and the
  existing `test_turn_scheduler*` cases must pass unchanged.
- **Route:** `get_queue_status` loads entries through `queued_entries(...)` and the limits through
  `project_limits(...)`, so its order and cap are the scheduler's, not a copy of them. The hop branch
  becomes `selection.blocked == "hop budget exhausted"`. The token-budget branch becomes
  `selection.initiator == "autonomous" and budget exhausted`, and keeps its place after the provider
  hold, as in the scheduler (`:388-395`). The provider hold still reads the **whole** queue
  (`operator_would_probe(entries, hold)`), as both do today. "Conversation is unavailable" stays
  scheduler-only (out of F133).
- **Tests** (`hub/tests/test_inbound_queue*.py`, entries given ascending `sequence`, the route's own
  order):
  - An autonomous entry in conversation A, then an operator entry in B, with the budget exhausted:
    `waiting_reason == "token budget exhausted"`. This fails today, because the route falls through
    to the probe or the fallback.
  - The control is the reverse: the operator's entry is in A and comes first. The reason is not the
    budget one. It passes both before and after the fix.
- **Collision:** B1's D3 reads `selection.selected` for the checkout-holder check. B3's pause reason
  and B2's "already running" sentence sit in other branches of the same route, per B1's own
  collisions list.

### F21: the probe that decides it, and the default meanwhile (probe not run)

**Evidence strengthened, not run:** this very session shows claude.ai account connectors (Gmail,
Google Calendar, Google Drive, Claude Docs) among its deferred tools. A Hub-spawned `claude` under the
same login can load them too, because `build_command` passes `--mcp-config` without
`--strict-mcp-config` (`runner_commands.py:250-259`). They add tools toward the deferral threshold,
so arm B below is materially different from arm A on this machine.

**The probe** is one throwaway trial-Hub project (never `:8000`), on Haiku per the cheap-model rule.
It uses F21's own shape: *"implement a one-line change, run its test, then record evidence for FR-1
with `record_evidence`"*. Each of the three arms runs twice, six turns in all:

| Arm | Argv / environment | Read from the stream-json `system/init` line | Read from the run |
|---|---|---|---|
| A | today's `build_command` argv | whether `mcp__agentweave__record_evidence` is in `tools`, whether `ToolSearch` is, and the MCP server list | `ToolSearch` calls before the first `record_evidence`, the evidence rows written, and the first-turn input tokens |
| B | A + `--strict-mcp-config` | same | same |
| C | A + `ENABLE_TOOL_SEARCH=false` in the run's environment (then `standard` if `false` is not honoured) | same | same |

**Decision rule:**
- If B or C lists `record_evidence` directly, and both of that arm's runs write evidence with zero
  `ToolSearch` calls at under a +10% first-turn token cost, F21 becomes a small spec change on
  `agent-tool-surface` (*"An invocable tool surface is the default"*): pass that flag, or set that
  variable, in `runner_commands.py` or `agent_trigger.py:1184-1187`.
- If only B does, a finding also goes on file: runs inherit the operator's own servers.
- If no arm does, retire F21 as not fixable inside AgentWeave.

**Default meanwhile:** F21 stays **open, not retired**, with no product change. The existing
explicit tool-naming notice stays. Record the probe as the one thing that closes it.

### Cross-bundle consistency

- **F354 ordering (B12's F363, B4):** consistent. B12 orders this pin's IMPL, then the `:8000`
  restart, then F363's `mcp_server.py` half, as a preference. B4 says no change of its own waits on
  the pin and keeps its 422 retry either way. Neither gates on B11, and B11 does not gate them.
- **F53's third defect (materialise stamping an archived loop) against B10 and B2:** no conflict.
  B10's `a-loop-is-stopped-archived-and-delegated-from-its-own-tab` adds events and a tab over the
  same two archive doors and changes no task ownership. B2's firing changes touch neither
  `spec_tasks.materialise` nor `loop_id`. B1's attendance change alters the `queued` half of the
  staffing gate's `reachable` test, and change 5's D1 alters the `loop_id in live` half:
  independent.
- **Collisions in `openspec/changes/`:** none found beyond those R1 and the other bundles already
  record. No other change carries F21, F177 or F248.

### The six no-spec items: what each fix round does

| Finding | The fix round, in one line |
|---|---|
| F20 | Canonicalise the address to `/` + `serializeDestination(resolved)`. Compare the pathname as well as the search (today's `target !== currentSearch()` skips a canonical query on a non-root path). Change `cli.py:306` to `tab=overview`. Tests: `useWorkspaceNavigation.test.tsx` (the pathname becomes `/`) and `tests/test_cli.py`. |
| F146 | Answer `POST /questions` with `blocking: true` with a 422 naming `blocking`. Update `_asking_run_has_ended`'s docstring. The dead `HttpTransport.ask_question` (`transport/http.py:418`, marked DEAD at `:390`) still sends `blocking`: drop the parameter or the method with it. Tests: 422 fails today (201); `blocking: false` still delivers. |
| F133 | Factor `select_turn` into `hub/hub/inbound_queue.py`; the scheduler and `get_queue_status` both call it; land before B1's change (spec above). |
| F305 | Make `decisions_for_user` carry only `DECISIONS.md` `OPEN` row ids. Edit `day-window.md` (`:175-178`, `:340`, `:600`), `night-window.md` (`:19`, `:307`), `arm-cycle.ps1` (`:249`, `:256`) and the `autonomous-prep`/`autonomous-session` skills. At compose, drop any inherited id that is not `OPEN`, and log it. |
| F177 | `order_by(created_at, rowid)` in `runners.py:82` and `charters.py:44`, with the ZB/ZA tie test (fails today). |
| F281 | Close as decided by `agent-run-sandboxing`. Reword the `models.py:1214` comment to "no file tool wrote outside this run's boundary". No test. |

### Further notes for the fix rounds (not new findings)

- **F382:** the unbounded `released.wait()` appears twice (`test_agent_trigger.py:1935` **and**
  `:2089`). Bound both.
- **F308, F339, F340, F322:** nothing R2 found changes R1's answer.

**Validation:** `openspec validate <name> --strict` passes for all eight changes after the edits.

## R3 — independent comparison (2026-09-24)

Read at the worktree's HEAD; product code untouched. The route table was rebuilt from the built app
(`app.routes`) with a whole-path matcher, `:8000` was read once through `mode=ro`, and the installed
pywebview's source was read. 40 load-bearing claims were checked; 5 disagreed with the code.

### Claims that disagreed, and what changed

| # | Claim (R1/R2) | What the code shows | Changed |
|---|---|---|---|
| 1 | F248: the shadowed words are exactly `conflicts`, `settings`, `board`, `boards` | A fifth. `GET /agent/sessions/{agent}` (`agent_trigger.py:3257`) is registered before `GET /agent/{agent}/chat` and `/agent/{agent}/conversations`; `/agent/sessions/chat` matches both and the first wins. An agent named `sessions` loses its chat history and conversation list, both read by the UI (`api/agentChat.ts:167`, `:381`). R2's walk compared one differing segment; this pair differs in two | change 7: `sessions` reserved (D1, D2, proposal, tasks 1.1, 1.2, 1.6); D2 states the whole-path rule. `:8000`: no agent named `sessions` |
| 2 | F349 D4: the six other sites need only `schedule_accepted` | Five of the six write an event on the route's session **between** the commit and the schedule (`messages.py:285`, `:295`; `agents.py:2250`, `:2252`; `questions.py:207`; `inbound_queue.py:248`; `accounting.py:64`). A locked write there answers 500 and never schedules: F349's defect under its likeliest cause | change 4: new D3a, one never-raising `persist_accepted_event` on its own session for every post-commit event at all seven sites (it also makes R2's per-route rollback unnecessary); task 1.6 widened, 2.3a added |
| 3 | F349: "four other routes" (proposal, task 2.4), "five routes" (D5) | Six sites plus the trigger, per R2's own D4 | change 4: counts corrected; D2's pending-retry dict is also the strong task reference (`main.py:526`'s bare `create_task` is not one) |
| 4 | F133: `select_turn` goes in `inbound_queue.py` as the body of `turn_scheduler.py:335-377` | That body calls `_entry_kind` (`turn_scheduler.py:69`), and `turn_scheduler` imports from `inbound_queue` (`:18`), so `inbound_queue` cannot import it back | F133's plan: `_entry_kind` moves into `inbound_queue.py` with `select_turn`; `turn_scheduler` imports it |
| 5 | F308: "add the drift job to `check-build`" as a one-line list edit | `check-build` reads the last 40 runs of all workflows (`SKILL.md` Step 1) and filters non-`ci.yml` workflows by the tag argument. A weekly run falls out of 40 runs, and a tag filter never matches a scheduled run | F308's step (Final) fetches the drift workflow's own latest run, shown regardless of tag |

### The four re-derivations this round was asked for

- **F354: does the pin change what a live agent loads?** Yes. The Hub never runs the server itself:
  it hands `[sys.executable, <path>]` to the runner CLI (Claude `--mcp-config`,
  `runner_commands.py:250-260`; Codex `config.mcp_servers` per `thread/start`,
  `codex_appserver.py:969-972`), and the CLI starts a new Python **per turn**, which reads the file at
  that moment (`agent_trigger.py:1141-1144`). So the path decides everything. Pinned, every turn
  after the pinning Hub's start loads the pinned bytes; a running turn keeps its process either way.
  `main.py:22` imports `agent_trigger` at module top, so the pin is taken at start. One real
  difference, checked: the script's own directory becomes `sys.path[0]` (today `hub/hub/`, pinned the
  digest directory). No `hub/hub/*.py` shadows a standard-library name and the server imports nothing
  from its directory, so the program is the same. Residual: `fastmcp` still loads from the Hub's
  interpreter at each spawn (an environment change, not a checkout edit). Recorded in change 1's
  round log.
- **F349 across the six sites:** rows 2 and 3 above. `checkpoints.py:334` stays excluded (nothing is
  committed before it). No collision with B9's `an-event-is-announced-only-once-its-write-is-committed`
  (that one is about `commit=False` events inside a caller's transaction).
- **F177, every list that shows runners or charters:** only two lists have an order,
  `runners.py:82` and `charters.py:44`. The other `select(Runner)` sites build id-keyed dicts for
  lookup (`agents.py:405`, `:1695`, `runners.py:93` launchability), and the UI sorts neither list
  (`grep sort` over `hub/ui/src` finds none for runners or charters), so the route order is the
  screen order. Both tables have string primary keys, so each has an implicit `rowid`; the Hub is
  SQLite-only and no migration has rebuilt either table (`grep batch_alter_table`). R2's reversal to
  `order_by(created_at, rowid)` stands. One constraint to carry: change 6's unique-index migration
  must create and drop indexes, not rebuild `charters` in batch mode, or it renumbers the rowids the
  order reads. Change 6's "oldest keeps the name" already uses `created_at, rowid`: consistent.
- **F133 against B1's final `why-queued-input-waits-is-told-truthfully`:** consistent. B1's task 2.0
  requires `controlling`, `selected`, `initiator` from entries, budget and cap, and its D3 feeds
  `selected` to `resolve_bound_task`; `select_turn` returns exactly those. R2's one substitution
  (`controlling.conversation_id` for `conversation.id`) holds: the scheduler goes on only when the
  looked-up conversation has that id (`turn_scheduler.py:343-351`). `queued_entries` orders by
  `sequence` (`inbound_queue.py:92-94`), the order the route reads today
  (`api/v1/inbound_queue.py:109-117`). The scheduler's `if not selected` branch is unreachable (the
  controlling entry always qualifies); keep it as a guard. Correction: row 4 above. The route's order
  of checks stays B1's (running, hop, pause/provider hold, token budget, launchability, workspace,
  holder, fallback).

### Claims that stand (checked, agreed)

F385: pywebview 6.2.1 sets `IsInPrivateModeEnabled` from `private_mode` whatever `storage_path` is
(`platforms/edgechromium.py:81`, `winforms.py:743-750`), so both kwargs are needed, as change 2 says.
F53's third defect (`spec_tasks.py:176-180`, no `archived_at` filter). F20 (`cli.py:306` sends
`view=overview`). F382 (unbounded `released.wait()` at `test_agent_trigger.py:1935` and `:2089`).
F146 (no UI or CLI poster of `blocking`). F21 (`--strict-mcp-config` never passed). B3's F139 change
is about the host's built-in tools, not MCP deferral: no overlap with F21.

**Validation:** `openspec validate <name> --strict` passes for all eight changes after the edits.

## Final — what the bundle reached, and why

B11 is 22 leftover questions from D13. Ten findings are carried by eight spec changes (F53 and
F157 share one, F134 and F183 share one). Six need only a no-spec fix round (F20, F133, F146, F177,
F305, F382). Two close with a small step (F308, F281). Four stay open or parked (F21 until its probe,
F322, F339 with a docs fix split out, F340). **IMPL is not started; the bundle is parked awaiting
the operator's approval.** Every answer below is a recommendation until it is recorded in
`DECISIONS.md`.

### Decisions

| # | Question | Recommended answer | Kind | Why |
|---|---|---|---|---|
| 1 | F354: live agents run the tool server from the working tree | **Pin it**: copy `mcp_server.py` at Hub start to a content-addressed file outside the repo and spawn that; refuse the turn (409) if the copy cannot be written, never fall back | change 1 | The runner starts a new Python per turn from the path the Hub names (`agent_trigger.py:1141-1144`), so an uncommitted edit reaches `:8000`'s next turn today. Pinned, the tools and the routes change together, at a restart |
| 2 | F385: the app window forgets everything on close | **Persist the window's profile** (`private_mode=False`, `storage_path=~/.agentweave/hub/window`) **and let an unset theme follow the system** | change 2 | pywebview defaults to private mode; the store holds the open project, layout and unsent drafts, not only the theme. The credential stays in `sessionStorage` |
| 3 | F20: an unknown deep link | **Canonicalise the address, and fix the CLI's launch link** | no-spec fix | The fix round replaces the address with `/` plus the resolved destination's query (comparing the pathname too) and changes `cli.py:306` from `view=overview` (nothing parses it) to `tab=overview`; tests in `useWorkspaceNavigation.test.tsx` and `tests/test_cli.py` |
| 4 | F307: a dialog does not take focus when it opens | **Focus moves into the dialog; a confirmation opens on Cancel**, chosen by an explicit mark | change 3 | Only the first Tab was fixed (UI-1). React focuses `autoFocus` before the hook records the opener, so closing those dialogs also drops focus to `<body>` (confirmed from React 18.3.1's source) |
| 5 | F349: accepted input answered 500 | **Answer accepted as accepted, and retry the start inside the Hub** (1 s, 5 s, 30 s, then the existing deferred set) | change 4 | After the commit, an event write or `schedule_agent` can raise at the trigger and six other sites; the input is queued but the caller hears 500 and nothing retries |
| 6 | F133: queue status recomputes why input waits | **One read-only turn selection shared by the scheduler and the status route** | no-spec fix, **before** B1's change | The fix round moves `select_turn(entries, hop_budget, cap)` and `_entry_kind` into `hub/hub/inbound_queue.py`, and `_attempt_turn` and `get_queue_status` both call it. Failing test: autonomous entry in A then operator entry in B, budget spent, gives `token budget exhausted` (not today); the reverse order is the control |
| 7 | F146: an operator-posted blocking question is never delivered | **Refuse `blocking: true` on `POST /questions`** | no-spec fix | The fix round answers 422 naming `blocking`, updates `_asking_run_has_ended`'s docstring, and drops `blocking` from the dead `HttpTransport.ask_question`. No product surface posts to that route |
| 8 | F53: a gone loop keeps its document's tasks | **A successor loop that claims the document adopts the gone loop's unfinished tasks**; archive stays as it is; materialise stamps only a live loop | change 5 | No backfill needed, and it recovers tasks already stranded. Materialise today picks any loop for the document, archived ones included (`spec_tasks.py:176-180`) |
| 9 | F157: a non-loop job silently drops `spec_document_id` | **Refuse it on create**, as PATCH already does | change 5 | No product surface sends it; the silent drop is the only defect |
| 10 | F134: an empty charter | **Refuse blank content at create and update; render an old empty row honestly** | change 6 | 0 such rows on `:8000`; the guard is for rows the schema never saw |
| 11 | F183: two charters with one name | **409 at both doors, plus a unique index**; the migration renames old duplicates (`X (2)`; the oldest by `created_at, rowid` keeps the name) | change 6 | Follows migration `0063` for agents. 0 duplicates on `:8000` |
| 12 | F177: runner (and charter) order is not creation order | **`order_by(created_at, rowid)`**, no migration | no-spec fix | The fix round changes `runners.py:82` and `charters.py:44`, with the failing test "insert `ZB` then `ZA` with one `created_at`, expect `ZB, ZA`". Ties are systematic (every seeded runner pair and all nine seeded charters share one `created_at`) and SQLite breaks them by name. Visible effect: seeded charters list in manifest order, not alphabetical |
| 13 | F248: a route word shadows a chosen name | **Reserve the shadowed words** (agents `conflicts`, `settings`, `sessions`; task ids `board`, `boards`) **plus a test that walks the route table** | change 7 | Five words, not one (R2 found three, R3 found `sessions`). Moving routes would touch UI callers and `:8000`'s bundle order |
| 14 | F281: how are shell writes recorded? | **They are not, by design: close F281** | ledger close + comment fix | The fix round rewords `models.py:1214` to "no file tool wrote outside this run's boundary". `agent-run-sandboxing` already says the record is not a complete account and names shell commands out of scope |
| 15 | F389: an event per allowed call? | **No; a per-run allowed/refused count instead** (lowest priority) | change 8 | Nothing is broken; the count gives the record a denominator at no more than three writes per run. If the gap is acceptable, close F389 and drop change 8 |
| 16 | F305: the loop windows re-ask decided questions | **One channel**: `decisions_for_user` carries only ids of `OPEN` rows in `DECISIONS.md` | no-spec fix (harness) | The fix round edits `day-window.md`, `night-window.md`, `arm-cycle.ps1` and the two autonomous skills; at compose, a window drops and logs any inherited id that is not `OPEN` |
| 17 | F308: the CI drift alarm | **Close as fixed** (`f7c732b`, on `master`) **and add the drift job to `check-build`** | ledger close + harness step | See "F308's remaining step" below |
| 18 | F21: retire "agents reach for tool search"? | **Not yet: run one probe first**; F21 stays open meanwhile | decision + probe | The Hub may be able to fix it (`--strict-mcp-config`, `ENABLE_TOOL_SEARCH`); see "F21's probe" below |
| 19 | F382: the suite freeze | **Watch only, with two tripwires** | no-spec fix (tests + harness) | The fix round bounds `released.wait()` (`timeout=30`, then fail) at `test_agent_trigger.py:1935` and `:2089`, and gives the windows' local full-suite run CI's `--timeout=300 --timeout-method=thread`. Close after two clean weeks |
| 20 | F322: Codex workspace commands judged by `cwd` alone | **Park with F325** | stays parked | Codex is undrivable; F325 is open by the operator's choice |
| 21 | F339: `acceptEdits` confinement | **Stay parked** for the approver-less question; **split out the docs row** | stays parked + no-spec docs fix | The docs fix rewords `permission-postures.md:33` ("Not checked") to "checked by Claude Code, not by the Hub", with the version, after one Haiku re-measure with F339's script. The "Edit files" posture reaches that row today |
| 22 | F340: the Hub records nothing when its own tool server fails | **Stay parked** | stays parked | Unchanged since 2026-09-21. Change 1 removes this checkout's likeliest cause (a half-written `mcp_server.py`) |

### Changes

| Change | Findings | What it does | Size |
|---|---|---|---|
| `an-agents-tool-server-is-the-one-its-hub-loaded` | F354 | Pin `mcp_server.py` at Hub start; spawn the pinned copy; refuse, never fall back | S, no migration, no UI |
| `the-app-window-keeps-the-operators-preferences` | F385 | Persistent WebView profile under `~/.agentweave/hub/window`; an unset theme follows the system | S, CLI + UI bundle |
| `a-dialog-takes-the-keyboard-when-it-opens` | F307 | Focus into the dialog on open, Cancel on confirmations by mark, opener recorded first | S, UI bundle |
| `input-the-hub-accepted-is-answered-as-accepted` | F349 | Never-raising post-commit events and scheduling at the trigger and six other sites, with a bounded retry | M, no migration |
| `a-loop-that-is-gone-lets-go-of-its-document` | F53, F157 | A successor adopts a gone loop's unfinished tasks; materialise stamps only a live loop; create refuses a stray document | S–M, no migration |
| `charters-are-named-once-and-an-empty-one-says-so` | F134, F183 | Blank content refused; an empty row rendered honestly; names unique with an index | M, one migration |
| `a-name-a-caller-chooses-reaches-its-own-resource` | F248 | Reserve `conflicts`, `settings`, `sessions`, `board`, `boards`; a route-table test | S, no migration |
| `a-run-records-that-its-calls-were-allowed` | F389 | A per-run allowed/refused count | M, one migration, lowest priority |

**Order that matters.** F133's fix lands before B1's `why-queued-input-waits-is-told-truthfully`
(that change's task 2.0 stops otherwise). Change 1's IMPL, then an operator restart of `:8000`, then
B12's F363 `mcp_server.py` half (a preference, not a gate). Changes 6 and 8 each take the next free
migration number at IMPL, and change 6's migration must not rebuild `charters` (F177's order reads
its `rowid`).

### F21's probe, and the rule that decides it

One throwaway project on a **trial Hub** (never `:8000`), Haiku per the cheap-model rule, with F21's
own task: *"implement a one-line change, run its test, then record evidence for FR-1 with
`record_evidence`"*. Three arms, each run twice (six turns):

| Arm | What changes | Read |
|---|---|---|
| A | today's argv | from the stream-json `system/init` line: whether `mcp__agentweave__record_evidence` is in `tools`, whether `ToolSearch` is, and which MCP servers loaded. From the run: `ToolSearch` calls before the first `record_evidence`, evidence rows written, first-turn input tokens |
| B | A + `--strict-mcp-config` | the same |
| C | A + `ENABLE_TOOL_SEARCH=false` in the run's environment (`standard` if `false` is not honoured) | the same |

**Decision rule.** If B or C lists `record_evidence` directly, and both of that arm's runs write the
evidence with zero `ToolSearch` calls at under +10% first-turn tokens, F21 becomes a small spec
change on `agent-tool-surface` (*"An invocable tool surface is the default"*): pass that flag or set
that variable (`runner_commands.py`, or the run environment at `agent_trigger.py:1184-1187`). If only
B does, also file a finding: Hub runs inherit the operator's own MCP servers (this machine's account
connectors). If no arm does, retire F21 as not fixable inside AgentWeave.

### F308's remaining step

The alarm is built and armed (`.github/workflows/upstream-drift.yml`, weekly, Monday 07:00 UTC; first
firing 2026-09-28). Nothing reads its result. In `.claude/skills/check-build/SKILL.md`:

- fetch the drift workflow's own latest run
  (`https://api.github.com/repos/gutohuida/AgentWeave/actions/workflows/upstream-drift.yml/runs?per_page=1`),
  because a weekly run falls out of Step 1's 40-run page and the tag filter never matches a
  scheduled run;
- show it as its own row regardless of any tag argument, as `ci.yml` is shown;
- print "not fired yet" when there is no run, and flag it stale when the latest run is more than
  8 days old;
- name it in the skill's description.

After 2026-09-28, one `gh run list --workflow upstream-drift.yml` confirms the first run happened.

### Open for the operator

Each needs an answer before IMPL. The recommended answer comes first.

1. **Approve the 22 answers above** (record them in `DECISIONS.md`). A "no" withdraws that change or
   fix round and leaves the finding open.
2. **F354: pin the tool server?** Yes (recommended). *No* keeps today's rule never to edit
   `mcp_server.py` in the checkout `:8000` runs, which B12's F363 then works around. *Run `:8000` from
   another checkout* instead contradicts CLAUDE.md's "by intent" arrangement.
3. **F177: creation order or name order?** Creation order via `rowid` (recommended). *Name order*
   declares today's behaviour correct; the fix is then `order_by(created_at, name, id)` and its tests
   are regression pins, not failing tests.
4. **F21: spend six Haiku turns on the probe?** Yes (recommended). *No* keeps F21 open with no change;
   *retire now* drops a fix the Hub may own.
5. **Change 5: does an in-progress task adopted from a gone loop keep its assignee?** Keep it
   (recommended; it ends the staffing gate's "unreachable" hold). *Return it to pending* restarts the
   work under the successor.
6. **Change 6: fold runner names into the uniqueness rule?** They have the same gap
   (`ix_runners_project_name` is plain). Fold them in (recommended, same migration), or file
   separately.
7. **Change 8 (F389): build it or accept the gap?** Build it, lowest priority (recommended).
   Accepting the gap closes F389 and drops the change.
8. **Change 4's open question: a failed refusal withdrawal still answers 500.** Leave it
   (recommended; the caller is owed the refusal, and a retry refuses again).

### Findings that did not need a change

- **F308**: fixed in `f7c732b` (on `master` and `origin/master`); only the `check-build` step remains.
- **F281**: already decided by `agent-run-sandboxing` (`spec.md:413-418`: the record is not a complete
  account, and a shell command is out of scope). Close it, with the one comment fix.
- **F322, F340**: parked (F322 with F325; F340 per 2026-09-21). **F339**: parked for its
  approver-less question; only its docs row moves.
- **F21**: neither fixed nor retired; open until the probe.

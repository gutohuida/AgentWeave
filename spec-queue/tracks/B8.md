# B8 — Checkpoint cutover identity

**Findings:** F293, F294
**Decisions:** none in `DECISIONS.md` terms. R1 made five design choices (D1–D5 in the change's `design.md`), each a recommendation the operator may overturn
**Changes:** `openspec/changes/a-checkpoint-is-handed-over-once-and-says-where-it-went/`
**Status:** R1 done

## R1 — explore and propose (2026-09-24)

### What the code showed (HEAD `404c7d5`)

`cut_over` (`hub/hub/checkpoint_cutover.py:63-200`) has one guard against a second handover. It
refuses when `predecessor.lifecycle == "archived"` (`:97-103`), and its refusal ends *"unarchive it
first"*. The guard reads the lifecycle at `:97` and writes it at `:144` (`archive`), then commits
at `:145`. Between the two it awaits `archivable` (`:105`). Nothing records that a checkpoint was
handed over. `Checkpoint` (`hub/hub/db/models.py:1668-1804`) has no such column, and
`InboundQueueEntry` has no checkpoint id. `unarchive` (`hub/hub/conversations.py:479-482`; route
`hub/hub/api/v1/agent_chat.py:623-635`) is never refused and resets `lifecycle` to `open`. Two
callers exist: the route (`api/v1/checkpoints.py:370-404`, refusal → 409 at `:391-394`) and the
automatic trigger (`checkpoint_trigger.py:319-336`, refusal → `cutover_refused` on
`checkpoint_ready`). `take_checkpoint` holds an in-process claim (`_checkpoint_claims`,
`checkpoints.py:27-28, 167-186`), and nothing guards the cutover.

### Each finding re-verified

- **F293 — still open.** A throwaway test used the suite's own fixtures on its file-backed SQLite:
  `cut_over`, then `unarchive` + commit, then `cut_over` again with the same checkpoint. It produced
  **two** `handoff` successors (`conv-2997cf6e3357`, `conv-2ecad0277068`). The shape is unchanged
  since filing. The only line drift: the guard is still at `:97`.
- **F294 — still open.** Two sessions loaded the conversation and checkpoint, then waited on an
  `asyncio.Barrier(2)`, then ran `cut_over` concurrently. Both returned successors
  (`conv-9433b9e6954f`, `conv-e632a122f613`). Without the barrier the second read came after the
  first commit and was refused. So the barrier is the deterministic test, and task 1.2 keeps it.
- **A shape R1 adds to F293, not a new finding.** The app's own Handoff (`writeCheckpoint`,
  `hub/ui/src/store/checkpointOperationStore.ts:37-75`) always takes a **new** checkpoint and then
  cuts it over. An operator who unarchives and presses Handoff therefore forks the line with a
  *different* checkpoint. A per-checkpoint column alone would not close that. This drives D2.
- `git log -S "cut_over_to_conversation_id"` finds nothing, so the column has never existed.
  Operator's `:8000` database, read with `mode=ro`: head `0105`, 16 checkpoints, **0** cutover
  deliveries, **0** `handoff` conversations. The trial database has 0 checkpoints.

### Why this is the third ask, and why the first two did not land

1. **F126** (filed 2026-08-30, `b039d47`) recommended shape (2), the column. Night N-3 shipped
   shape (1), the lifecycle guard (`3142a91`), because *"a migration fails the day-window
   carve-out's second condition"*. It also noted that shape (1) as worded needed the very link the
   column adds.
2. **F293** (2026-09-06 D-2, `6f58c27`) re-recommended the column. F294, from the same drive, showed
   the column alone races. The night log (`523e66d`) and `APPROVALS.md:1598-1605` carried the note
   *"one change answers both … Neither is specced."* No window ever proposed it:
   `git log --all --grep` finds no proposal commit. The night works severity A first (five A's were
   unproposed), and Merge Week O5 froze new change directories from 2026-09-22 to 2026-09-28.
3. **`ROUNDS.md` S2** (`eeadf70`, 2026-09-22) scheduled it. This bundle is that track. The common
   cause each time was the migration, which unattended windows may not make.

### Design choices, with the recommendation

| # | Question | Options | Recommended | Why |
|---|---|---|---|---|
| D1 | Where does the handover identity live? | (a) `Checkpoint.cut_over_to_conversation_id`; (b) `Conversation.handed_over_by_checkpoint_id` | **(a)**, `String(64)`, nullable, no FK | The checkpoint listing can then answer "where did it go". The successor side is already answerable via `lineage_id`. An FK would need a batch rebuild of `checkpoints` (constraint names pinned by `0088`), and SQLite does not enforce FKs here |
| D2 | "At most once" per checkpoint, or per conversation? | (a) per checkpoint; (b) partial unique index `checkpoints(conversation_id) WHERE cut_over_to_conversation_id IS NOT NULL` | **(b)** | (a) leaves the UI's own Handoff after an unarchive able to fork the line, and leaves the trigger-vs-banner race (`checkpoint_trigger.py:327-333` offers C1 while the trigger cuts over C2). The lineage requirement says a line is linear. Chains are unaffected, because the index keys on the predecessor's id |
| D3 | How are concurrent presses serialised? | (a) in-process claim like `_checkpoint_claims` (F294's own rec.); (b) compare-and-set `UPDATE … WHERE cut_over_to_conversation_id IS NULL` + D2's index | **(b)** | The migration exists anyway. The claim is the column written conditionally, not an extra field to read and race on. Migration `0104` and `refused_capability.py:216-230` are the precedent. The locking argument (pysqlite legacy mode, `engine.py:36-40`, sets no pragmas) comes from reading only; task 1.2 is its measurement |
| D4 | Keep refusing a conversation archived by hand? | (a) keep; (b) allow now that the column exists | **(a)**, reordered | `archivable` returns None for an archived conversation (`conversations.py:395-396`), so allowing it skips the run/queue checks. The spent and handed-over checks now run first, so *"unarchive it first"* is said only where it is true |
| D5 | Backfill legacy rows? | (a) yes, from delivered entries; (b) no | **(a)** | Exact, not heuristic: the delivered content is `_DELIVERY_PREAMBLE` + `# Checkpoint {id}` (`checkpoint_generation.py:285`). The first handover per conversation wins, so a database holding an F126-era fork still gets its index. It writes 0 rows on both local databases |

### What each route returns when what it calls raises

The compare-and-set miss and the `IntegrityError` are converted to `CutoverRefusedError` **inside**
`cut_over`, after a rollback. So the route still answers **409**, and the trigger still reports
`cutover_refused`. The rollback is safe because neither caller has anything pending (route
`:378-384` only reads; trigger `generate_checkpoint` commits at `checkpoint_generation.py:664`).
Task 1.8 pins that the trigger's checkpoint survives the rollback. A lock-timeout
`OperationalError` stays a 500, as today; R1 does not widen it.

### What was written

`openspec/changes/a-checkpoint-is-handed-over-once-and-says-where-it-went/`: `proposal.md`,
`design.md` (context table, the three asks, D1–D5, the migration checklist per
`.claude/rules/db-migrations.md`, route-raise analysis), `tasks.md` (10 tests first, 1.1–1.5, 1.8 and 1.10
fail today; 1.6/1.7 controls; 1.9 moves one assertion deliberately; a mutation pair in 1.4 showing
which guard catches which race; migration tests incl. a pre-existing-fork database and F329 parity),
`test-guide.md`, and one ADDED requirement in `specs/agent-conversation-handoff/spec.md`.
`openspec validate … --strict` passes.

### Cross-references and collisions

- **B2 (F235)** is about checkpoint *visibility*, the read grant. It is not carried here. B2 may
  touch `CheckpointSummary` too, and that is a textual merge only. F126's own remainder (the
  cutover banner filters on `trigger === 'context_pressure'`) is also left out. It is UI behaviour,
  and the new field is what such a banner would read.
- **Migration number:** B3's `agents-no-longer-register-themselves` also names `0106` (its
  `proposal.md:73`). Whichever is built second takes `0107`.
- `scripts/drive/t_d2_cutover_guard.py` probe 1 asserts the old *"unarchive it first"* text on a
  spent checkpoint. Task 4.4 adapts it in the re-drive.

### For R2 to attack

- The D3 locking argument. It rests on pysqlite's implicit-`BEGIN` behaviour and on SQLite's busy
  handler releasing the shared lock between retries, and nothing in R1 measured it.
- Whether D2 (b) over-refuses a workflow the operator uses (design Open Question 1).
- Whether any caller of `cut_over` other than the two found stages writes before calling it.

# B9 — SSE tells the client what it lost

**Findings:** F251, F253, F335
**Decisions:** none from `DECISIONS.md`; two questions raised for the operator (B9-Q1, B9-Q2)
**Changes:** `openspec/changes/an-event-is-announced-only-once-its-write-is-committed/`, `openspec/changes/a-live-view-that-fell-behind-is-told-and-catches-up/`, `openspec/changes/every-event-the-hub-sends-reaches-the-app/`
**Status:** R1 done | R2 done | R3 done — operator review 2026-09-24: 3 approved (`APPROVALS.md`; review: each change's design.md `## Operator review, 2026-09-24`)

## R1 — explore and propose (2026-09-24)

Read against the worktree's HEAD `ce086b6`. `git diff --stat 404c7d5 HEAD -- hub/ src/` is
empty, so every citation holds for `404c7d5` too. No verdict in `spec-queue/DECISIONS.md` names
F251, F253 or F335. No other open change carries them. Of the other bundles' changes, only
`a-loop-is-stopped-archived-and-delegated-from-its-own-tab` (B10) and
`a-checkpoint-is-handed-over-once-and-says-where-it-went` (B8) touch broadcasts. Both broadcast
already-allowlisted kinds after commit, or add a payload field, so neither collides with B9's
files. Once B9's registry lands, a new kind either of them adds must be registered (the test makes
that so).

### Each finding re-verified

**F335 (D) is still open, unchanged in shape.** `resolve_divergences_for_task` stages
`resolved_at` (`hub/hub/run_divergence.py:93-94`) and an event row with
`persist_event(..., commit=False)` (`:97-103`). It then calls `sse_manager.broadcast` at once
(`:104`). Its only caller is `apply_transition` (`task_transition_service.py:711`), whose caller
commits. The docstring's argument that the broadcast "needs no such care" (`:82-83`) is the defect.
An AST scan of every function that calls `persist_event(commit=False)` (`run_divergence.py:102`,
`task_transition_service.py:776`, `:797`) finds this the **only** one that broadcasts. A second
heuristic scan looked for broadcasts with no commit earlier in the same function. It flagged
`projects.py:317/379/604/641`, `checkpoints.py:403` and `agent_chat.py:467`. Each of those runs after
a service call that commits (read: `delete()` at `projects.py:628-641`, `cut_over` at
`checkpoints.py:385-403`), so none is F335's shape. Not found by the scan and noted:
`new_session_request` (`agents.py:3039-3040`) broadcasts before its own `persist_event` commits. But
nothing on that path rolls back (the message committed at `:3036`), so this is not F335's shape and
it was left alone.

**F253 (B) is still open, unchanged.** `asyncio.Queue(maxsize=256)` is at `sse.py:43` and `:63`.
Both `except asyncio.QueueFull: pass` blocks are still there (`:91-94`, `:100-103`). Both stream
generators (`api/v1/events.py:95-107`, `:144-153`) yield only queue items. The client's only
catch-up is `onSseReconnect` (`useSSE.ts:134`), used at `:411` (invalidate everything) and at
`api/agents.ts:560` (the agent-output reconciliation poll). A drop never triggers it.

**F251 (B) is still open, and has grown.** A re-measurement by AST, not grep, found these numbers:

- The Hub broadcasts **63** kinds: 59 as literals, plus 4 through `_broadcast_run_lifecycle`
  (`agent_trigger.py:1916`, bounded by `_RUN_LIFECYCLE_EVENTS`, `:1878`, asserted at `:1897`).
- The allowlist `useSSE.ts:21-68` has **46** names.
- **18** kinds are dropped. That is the finding's 17 plus `queue_agent_held`
  (`agent_trigger.py:2547`), which is new since the finding was filed.
- The mirror defect, not in the finding: **`job_deleted` is allowlisted and handled**
  (`useSSE.ts:40`, `:523`), **but nothing in `hub/hub` broadcasts it**. Archiving a job broadcasts
  `job_archived` (`jobs.py:1290`), which is dropped, so the jobs list never refreshes on archive.
  `useSSE.test.tsx:128` tests the kind that is never sent. This is an F190-shaped fixture: an event
  the server never emits.
- The handlers that never run are unchanged: `api/checkpoints.ts:45-51`, `api/agentChat.ts:184-190`,
  and six sentences in `lib/eventSummary.ts`. The offer banner (`AgentOutputPanel.tsx:605-618`)
  still depends on `useCheckpoints`.
- The coupling to F335 is real. `eventSummary.ts:149` renders *"{count} open divergence(s) on
  {task} resolved"*.

### The design questions, with options and recommendations

**F335: how to stop announcing a write that can roll back.** The options were:

- (A) Defer the announcement to the commit of the session that staged it. Publish on the root
  transaction's `after_commit`, and drop it on the root's `after_transaction_end`.
- (B) Make every caller of `apply_transition` broadcast after committing.
- (C) Stop broadcasting the kind live.
- (D) `create_task` the async broadcast from the listener.

**Recommended: A.** B spreads the rule across callers, and the next `commit=False` announcement
repeats the defect. C drops a true live signal. D reorders frames and makes tests depend on timing.

R1 **measured** the SQLAlchemy event order on 2.0.50 with aiosqlite, rather than assume it:

- A close without commit fires no `after_rollback`.
- A savepoint release fires `after_commit` with `in_nested_transaction() == True`.

So the listener must skip nested commits, and clearing must hang off `after_transaction_end`. The
listener runs on the loop thread.

Two traps are named in the design:

- The existing 1.8a spy is on `broadcast`, which the deferred path bypasses. The new tests spy on a
  single sync `SSEManager.publish` funnel, and a control proves the spy sees a true resolution.
- An exception in `after_commit` propagates out of a commit that has **already landed**. That would
  be a 500 for a write that happened (`JSONServerSentEvent` serialises in its constructor). So each
  publish is caught and logged.

The operator's PATCH route keeps the wire order `run_divergence_resolved` before `task_updated`
(`tasks.py:1470` commit, `:1472` broadcast). A test pins that order from the real route.

**F253: how to tell the client.** The options were:

- (A) A `stream_gap` frame.
- (B) Close the slow subscriber so the client reconnects.
- (C) Event IDs and replay.
- (D) Block, or grow the queue.

**Recommended: A**, with one change from the finding's sketch: **the stream writes the gap, not the
next put**. A gap queued by the next broadcast is never delivered after a burst followed by silence,
and it competes for a full queue. B loops a throttled tab through reconnects and puts nothing on
screen. C is out of proportion. D stalls the broadcasters.

The gap carries **no `project_id`**. It is stream metadata, like `connected`, because the operator
queue mixes projects. The client answers it instance-wide: `invalidateQueries()` plus the existing
reconnect listeners. The Activity feed shows one line whatever project is selected. The two route
generators are folded into one `stream_frames`, so the gap is written once and can be unit-tested
without a stalled socket.

**F251: what the app does with a kind (B9-Q1, for the operator).** The options were:

- (A) A runtime allowlist generated from a server registry.
- (B) No runtime filter, with the generated vocabulary kept as a TypeScript union type.
- (C) No filter and no generation.

**Recommended: B.**

- A reintroduces F251 whenever the committed bundle lags the Hub. It does lag: `/health`'s
  `ui_stale` exists for exactly that.
- C lets a handler for a kind that is never sent (`job_deleted`) compile.
- B blocks both failure directions. Nothing is dropped by omission, and `tsc` (run by
  `npm run build`) rejects a `case` or `===` against a kind that does not exist.

The server side is the same under every option: `hub/hub/sse_events.py`, plus an AST test that each
broadcast kind is registered and each registered kind is broadcast. At runtime an unregistered kind
is warned about, never raised on, for the same after-commit reason as F335.

**How the three interact.** F335 must land first, or in the same commit as F251. F251's tasks carry
this as prerequisite 0.4, checked against F335's test 1.1. F253 does not depend on the other two,
because its client branch runs before the allowlist test. If F335 has landed, F253's counter sits
in F335's `publish` funnel. If F253 has landed, F251's registry lists `stream_gap` as a stream
frame. Order to build: **F335 → F253 → F251**. Stopping after any one leaves complete work.

### What was written

- `an-event-is-announced-only-once-its-write-is-committed` (F335). Backend only, small.
- `a-live-view-that-fell-behind-is-told-and-catches-up` (F253). Backend and UI, small to medium.
- `every-event-the-hub-sends-reaches-the-app` (F251). A registry, a generator, the UI dispatcher,
  and D4's per-kind invalidation table. Medium.

Each change has `proposal.md`, `design.md`, `tasks.md`, `test-guide.md`, and an ADDED delta to
`local-project-workspace`. All three pass `openspec validate --strict`.

Every planned test states why it fails on today's code. Two are controls that make the others
non-vacuous: F335 1.2 is a true resolution seen by the same spy, and F251 1.2 injects `job_deleted`.

The UI tests follow the order the stream really emits: `connected`, events, then the gap. F253's
1.7 also covers the other order its design permits.

### Noticed, not in scope (for the ledger's owner to file)

- `OverviewPage.tsx:101` shows the last ten buffered events of **every** project, with no filter
  for the selected project. It is the one live consumer without `ActivityLog`'s filter, and it
  becomes more visible once F251 admits 18 kinds.
- `new_session_request` is broadcast before its event row commits (`agents.py:3039-3040`). Nothing
  rolls back there, so it is harmless today.

### Not resolved in R1

- D4's per-kind invalidation table (F251) was derived from payloads and query keys, not driven. R2
  should check each row against the payload each route sends.
- Whether `checkpoint_due` and `agent_requested` read acceptably through `summaryForEvent`'s
  default branch is left to R2 and the human check.

## R2 — independent comparison (2026-09-24)

Read against the worktree at `8c54aa0`. There has been no `hub/` change since `404c7d5`. Each claim
below was re-derived from the code, not from R1's text. 31 load-bearing claims were checked: 27
stand and 4 changed the proposal. No decision recommendation was reversed.

### Re-measured independently

| What | R1 said | R2 measured | How |
|---|---|---|---|
| Broadcast kinds | 63 (59 literal + 4 via `_broadcast_run_lifecycle`) | **63**: 59 literal, plus one non-literal site (`agent_trigger.py:1916`, bounded by `_RUN_LIFECYCLE_EVENTS` at `:1878` and asserted at `:1897`) | An AST walk of every `.broadcast(`/`.publish(` call in `hub/hub`, using R2's own script outside the repo |
| Allowlist | 46 | **46** (`useSSE.ts:21-68`) | Parsed from the array |
| Dropped | 18, including `queue_agent_held` | **18**, the same names. `queue_agent_held` is at `agent_trigger.py:2547` | Set difference |
| Allowlisted but never broadcast | `job_deleted` | **`job_deleted` only.** Archiving a job sends `job_archived` (`jobs.py:1290`, payload `{"id"}`) | Set difference |
| SQLAlchemy event order (F335 D1) | Measured on 2.0.50 | **Re-run, same result.** Commit fires `after_commit` then `ate(root)`. Rollback fires `after_rollback` then `ate(root)`. Close fires `ate(root)` only. A savepoint release fires `after_commit` with `in_nested_transaction()==True`, plus `ate(nested)`. A second `commit()` fires `after_commit` again, so popping the pending list is what stops a republish. A raise in `after_commit` **does** propagate out of `commit()`, so D2's `try/except` is required, not just defensive | A scratch script: aiosqlite, `async_sessionmaker(expire_on_commit=False)` |
| sse_starlette sends one frame at a time (the premise of F253 D2) | Assumed | **Confirmed** in 3.3.2 `_stream_response`: `async for data in body_iterator: await send(...)`. The generator waits at `yield` while a send blocks. So drops pile up there, and the check after the yield sees them | `inspect.getsource` |

### Claims that stand (checked at file:line)

**F335**
- The broadcast at `run_divergence.py:104` runs before the commit, after `persist_event(commit=False)` at `:97-103`.
- Its only caller is `task_transition_service.py:711`. That call runs only for `ORIGIN_ACTOR` transitions (`:700`).
- The three `commit=False` sites are as R1 listed.
- F319's rollback runs on the same session (`turn_scheduler.py:448`). Its `ate(root)` clears a pending announcement before the refusal's own commit at `:476`.
- D3's order holds. `_commit_and_render` commits at `tasks.py:1470` and broadcasts `task_updated` at `:1472`. The listener fires inside the awaited commit, so it goes first.
- The old spy (`test_a_refused_review_leaves_nothing_behind.py:828-835`) is on `broadcast`.

**F253**
- Both `QueueFull: pass` blocks are there (`sse.py:91-94`, `:100-103`).
- Both generators are there (`events.py:95-107`, `:144-153`).
- The app uses only the operator stream (`useSSE.ts:249`).
- The catch-ups are at `useSSE.ts:411` and `api/agents.ts:560`.
- `test_reconnecting_gets_a_clean_queue_with_no_stale_events` exists (`test_operator_events.py:312`).

**F251**
- `npm run build` is `tsc && vite build` (`package.json:8`).
- `useCheckpoints`' listener (`api/checkpoints.ts:45-51`) never receives its kinds today, and it feeds the offer banner (`AgentOutputPanel.tsx:617`).
- `useProjectConversations` (`api/agentChat.ts:184-190`) refreshes on any payload with a `conversation_id`. Every checkpoint kind's payload carries one: `checkpoint_trigger.py:222/280/331/335/341`, `checkpoints.py:195/263/403`, `checkpoint_handover.py:278`.
- The six sentence lines in `eventSummary.ts` are where R1 said.
- `useSSE.test.tsx:128` uses `job_deleted` as its fixture.

### D4's per-kind table, row by row against payloads and query keys

R2 compared each row with the payload the Hub sends and the query keys the UI reads.

| Kind | R1 row | R2 verdict |
|---|---|---|
| `job_archived` | `jobs`, `jobs/<id>`, `loops` | Stands. The payload is `{"id"}` and the keys are at `api/jobs.ts:164/173`. Archiving a job also archives its loops (`jobs.py:1280-1285`) |
| `project_deleted` | `['projects']` | Stands. It is broadcast under the deleted project's own id (`projects.py:641`) |
| `agent_requested` | `agents`, `['projects']` | Stands. It creates an agent row and a conversation (`agents.py:2236`). Its `conversation_id` also triggers `useProjectConversations` |
| `question_declined` | `questions`, `status` | Stands. The payload is `{id, agent}` (`questions.py:588`) |
| `task_blocked`/`task_unblocked` | `tasks`, `status`, `questions` | Stands. No `task_updated` accompanies either one, and `status` carries `task_counts` (`status.py:62`). The `questions` entry repeats what the accompanying question events usually do, and it does no harm |
| `run_diverged`/`run_divergence_resolved` | `tasks`, `status` | Stands, but **the reason is corrected**. The badge reads `task.has_open_divergence` from the tasks list (`TaskCard.tsx:396`), not a divergence query. `useDivergences` (`api/tasks.ts:447`) has no consumer, so it is deliberately not invalidated |
| `review_unstaffed` | `tasks`, `status` ("a divergence may restaff") | **Changed to `loops`, `loops/<loop_id>`.** `_emit_review_unstaffed` writes only its event row and "does not touch the job" (`scheduler.py:2427-2478`), so nothing on a task changes. The place it shows is the loop card's stall reason (`scheduler.py:2070-2086`), and the payload has `loop_id` |
| `queue_agent_held`/`queue_agent_paused` | `agents`, `queue/<agent>` | Stands. Both payloads carry `agent` (`agent_trigger.py:2536`, `turn_scheduler.py:490`) |
| `worktree_released` | `worktrees`, `worktree-conflicts` | Stands. The keys are at `api/workspace.ts:116/156` |
| Checkpoint and conversation kinds, `new_session_request` | No central case | Stand |

### What disagreed with the code, and what was changed

| # | Claim | Code | Change |
|---|---|---|---|
| 1 | F251 D4: `review_unstaffed` invalidates `tasks`, `status` | It writes no task state (`scheduler.py:2439-2443`) | The row is split out as `loops`, `loops/<loop_id>`. The `run_diverged` row's reason is rewritten (design D4) |
| 2 | F251 D3: the generated union is the `EVENT_KINDS` keys | F253's `ActivityLog` would compare `event.type === 'stream_gap'`. That is TS2367 if the union lacks the kind | The union also carries stream frames the app dispatches (`stream_gap`), never `connected` (design D3) |
| 3 | F251 task 2.6: the `tsc` fallout is unknown | Exactly two dead `case` labels will fail `tsc`. One is `job_deleted` (`useSSE.ts:523`). The other is `question_not_asked` in `eventBelongsToTimeline` (`api/agents.ts:400`). That function is called only from the live listener (`:418`), and nothing broadcasts that kind, so its "old rows" comment is wrong there | Both are named in task 2.6, with the reason `eventSummary.ts:125`'s copy stays |
| 4 | F251 D1: the AST walk reads "the kind argument" | The kind is argument 2 of `defer_broadcast` but argument 1 of `broadcast` | Stated in design D1.1 |

Minor citation fixes:
- F253 design: `ActivityLog.tsx:136-144` is now `:136-146` (the check is at `:139`).
- The Overview path is `components/overview/OverviewPage.tsx`.

**R1's open question on `checkpoint_due` and `agent_requested` in the feed.** `EventRow` titles each
row with the kind name. It shows a summary only when the summary differs from that name
(`EventRow.tsx:81-82`). Neither payload has `error`, `message`, `summary` or `title`, so each row
reads as the actor plus the kind name, like any kind with no sentence. This is recorded in F251
design D5 and left to the human check.

### Planned tests

With its fix reversed, each planned test would fail as its task states.

- **F335:** 1.1 and 1.2 share one spy on the single `publish` funnel, so 1.1 cannot pass vacuously. 1.3 pins the order the real route sends.
- **F253:** 1.6 uses the order `stream_frames` emits: `connected`, then the event, then the gap. 1.7 uses the other order the design permits.
- **F251:** 1.5 builds its payloads from the shapes above, stamped the way `SSEManager` stamps them.

**What each route returns when its callee raises.** A raise in `after_commit` was measured to
propagate out of a commit that has already landed. F335 test 1.5 covers it. F253 and F251 change no
route's error path.

### Collisions

None. Of the 49 unarchived changes, those that mention broadcasts add only kinds that are already
registered (`spec_updated`, `loop_stopped`, `checkpoint_ready`). As R1 said, B8's and B10's changes
touch only payloads or ordering. None of them carries F251, F253 or F335.

### Observations (not filed)

- **R1's Overview claim is confirmed.** `components/overview/OverviewPage.tsx:101` renders
  `getBufferedEvents().slice(-10)`. That is the module buffer fed by the instance-wide operator
  stream (`useSSE.ts:186`). It has no `project_id` filter, and it shows on a project's Overview tab
  (`App.tsx:388-391`). Two more problems on the same strip:
  - `agent_output` is allowlisted, so streamed chunks from any project will usually fill all ten
    pills.
  - Its warning dot tests `severity === 'warning'` (`:209`), but the Hub writes `warn`. So only
    `context_warning` ever shows amber.
- `useDivergences` (`api/tasks.ts:447`) is a hook no component mounts.

## R3 — independent comparison (2026-09-24)

Read against the worktree (no `hub/` change since `404c7d5`). 24 claims re-checked; 20 stand, 4
changed the proposal. No recommendation reversed; one new operator question (B9-Q2).

### Re-measured

| What | R3 measured | How |
|---|---|---|
| Kinds broadcast / allowlisted / dropped / dead | **63 / 46 / 18 / `job_deleted` only**; one non-literal site (`agent_trigger.py:1916`) | R3's own AST script (outside the repo) |
| `tsc` fallout of typing `SSEEvent.type` as the union | **Two**, as R2 said: `useSSE.ts:523` `job_deleted`, `api/agents.ts:400` `question_not_asked`. (`run_interrupted` at `agents.ts:401` is real: `run_reconciliation.py:134`) | Every `case '…'` / `.type === '…'` in non-test files that use `SSEEvent`, against the 63 |
| `commit=False` sites | Still three (`run_divergence.py:102`, `task_transition_service.py:776`, `:797`); only the first broadcasts | grep |
| Test sessions | Real `async_session_factory()` sessions, no outer savepoint (`conftest.py:766`), so the `in_nested_transaction()` skip cannot make F335's tests vacuous | grep |

### What disagreed, and what changed

| # | Claim | Code | Change |
|---|---|---|---|
| 1 | F335 D4: the AST guard (rule 1) is enough, and B10's note says "whichever lands second converts to `defer_broadcast`" | Converting a post-commit broadcast **in place** stages it after the function's last commit. `get_session` closes without committing (`db/engine.py:166-168`), `after_transaction_end` clears the list, and the frame is silently never sent. `update_job`'s trailing `job_updated` (`jobs.py:1167`, after `_hand_job_to_scheduler`) is that shape, and it is one of the four functions B10 hands over | **Rule 2 added** (a `defer_broadcast` must precede the function's own last `.commit()`), with a snippet-based negative control (task 1.6b). Both failure messages now spell out the remedy for whoever lands second: stage with `defer_broadcast` above the commit, in today's wire order, and spy on `publish`. Rule 1 was **not** narrowed (B10 asked for this check) |
| 2 | F335 task 1.6: fails "on nothing else" | False if B10 lands first: then `update_job`, `archive_job`, `archive_loop`, `set_loop_control` fail too | 1.6 made conditional; task 2.4b converts them when B10 is first. No existing test spies on those broadcasts (`test_jobs.py` mentions `broadcast` only in a docstring), so no spy breaks |
| 3 | F335 spec: the deferred form is for writes that "leave the commit to the caller" | The guard also covers functions that commit themselves (B10's four) | Requirement text widened to match the guard: every function staging with `commit=False` announces deferred, staged before the commit it reports |
| 4 | F251 0.4: F335 "on the tree" keeps F335's false line out of the app | `:8000` serves `hub/hub/static/ui` from disk (`main.py:19`), so a bundle commit reaches it on reload, **before** the process restarts onto F335. `ui_stale` compares bundle with `hub/ui/src` (`main.py:199-255`), not with the process, so nothing notices | New F251 design section and **task 0.5**: the bundle commit waits for the operator's confirmation that `:8000` was restarted onto F335 (**B9-Q2**). F253 and F335 were checked for the same skew: both safe (F253 design gained the two-way skew note; F335 design says when it takes effect) |

Also made order-independent (the F335 → F253 → F251 order is recommended, not required, except
F251 after F335): F253 task 2.3b (if F251 landed first, register `stream_gap` in `STREAM_FRAMES`
and regenerate, or its `=== 'stream_gap'` is TS2367); F251 tasks 2.1 and 2.4 (register `stream_gap`
and keep F253's branch ahead of the generic dispatch with `continue`, so a gap is dispatched once).

### Stands (checked at file:line)

`run_divergence.py:93-104` (stage, then broadcast before commit) · `broadcast` has no `await`
(`sse.py:71-103`), so the sync `publish` funnel is sound · both `QueueFull: pass` blocks
(`sse.py:91-94`, `:100-103`) · `fireReconnect` (`useSSE.ts:141`) and the invalidate-all hook
(`:410-414`) · `ActivityLog`'s project filter (`:139`) and history dedupe (`:117`) · `dispatchEvent`
reads `severity` from the payload (`useSSE.ts:184`) · `job_archived` payload `{"id"}`
(`jobs.py:1290`) · B10 adds no new kind (`loop_stopped`, `loop_archived` are already sent,
`scheduler.py:3187`, `loops.py:184`) and no `useSSE` edit · `a-loops-outstanding-mail-is-mail-not-yet-delivered`
adds no broadcast · no other unarchived change stages with `commit=False` next to a broadcast.

All three changes pass `openspec validate --strict`.

## Final — what the bundle reached, and why

The Hub's live event stream (SSE) has three defects that each make the app show something other
than the truth: it announces a write that is then rolled back (F335), it silently drops events for a
slow tab (F253), and the app throws away 18 of the 63 kinds the Hub sends (F251). Three small
changes fix them. **IMPL is not started; the bundle is parked awaiting approval.**

### Decisions

| # | Question | Recommended answer | Why |
|---|---|---|---|
| D-F335 | How to stop announcing a write that may roll back | Defer the announcement to the session's commit (`defer_broadcast`, published by an `after_commit` listener, cleared when the root transaction ends) | Measured on SQLAlchemy 2.0.50 by R1 and R2: rollback and close-without-commit both end the root transaction, so nothing leaks. The alternatives either spread the rule over every caller or lose a true live signal |
| D-F335-guard | How to keep the shape from coming back | Two AST rules: no direct broadcast in a function that stages with `commit=False`; no `defer_broadcast` after a function's own last commit | Rule 2 closes the silent-drop trap R3 found in converting B10's functions |
| D-F253 | How a tab that fell behind is told | A `stream_gap` frame written by the stream (not queued by the next event), with no `project_id`; the app treats it like a reconnect | A gap queued behind a burst is never delivered; closing the stream loops a throttled tab; replay is out of proportion |
| B9-Q1 | Keep a client allowlist, or not | **No runtime filter**; the Hub's vocabulary is generated into a TypeScript type (option B) | A generated allowlist re-drops every new kind whenever the committed bundle lags the Hub; with the type, a handler for a kind that is never sent fails `npm run build` (the `job_deleted` bug) |
| B9-Q2 | May F251's bundle reach `:8000` before `:8000` runs F335's fix | **No**: the bundle commit waits for the operator's restart (task 0.5) | A bundle reaches `:8000` on reload, before a restart; until then a refused review shows a false "divergence resolved" line |

### Changes

| Change | Findings | What it does | Size |
|---|---|---|---|
| `an-event-is-announced-only-once-its-write-is-committed` | F335 | `SSEManager.publish` funnel, `defer_broadcast`, two commit listeners, one call site, AST guard | Small, Hub only |
| `a-live-view-that-fell-behind-is-told-and-catches-up` | F253 | Per-subscriber drop count, one shared `stream_frames` generator that writes `stream_gap`, client catch-up and a feed line | Small–medium, Hub + UI |
| `every-event-the-hub-sends-reaches-the-app` | F251 | Server registry `sse_events.py` with AST tests, generated TS vocabulary, no client filter, per-kind invalidations, `job_deleted` → `job_archived` | Medium, Hub + UI |

Build order **F335 → F253 → F251**; stopping after any one leaves complete work. F251 must not
start its fix before F335 is in the tree (task 0.4) and must not commit its bundle before `:8000`
runs F335 (task 0.5). F253 is independent and safe under either skew. B10's loop change collides
only through F335's guard; whichever lands second converts four functions, and the guard's message
says how.

### Open for the operator

1. **B9-Q1: drop the app's event allowlist?** Recommended: **yes, keep the vocabulary only as a
   generated type.** Answering "keep a generated allowlist" makes F251 tasks 2.4-2.5 keep the filter
   and 1.6 assert an unknown kind is dropped, and accepts that a Hub kind added without a bundle
   refresh is dropped again.
2. **B9-Q2: gate F251's bundle on a `:8000` restart?** Recommended: **yes** (one restart you would
   make anyway to pick up F335, and one question from the builder). Answering "accept the window"
   drops task 0.5; until the next restart a review refused at delivery on `:8000` can put one false
   "divergence resolved" line in the Activity feed, while the task board stays correct.

### Candidate finding for the operator (observed, not filed)

**The Overview tab's activity strip shows every project's events, and its warning colour never
fires for the Hub's warnings.** `components/overview/OverviewPage.tsx:101` renders
`getBufferedEvents().slice(-10)`, the instance-wide buffer fed by the operator stream
(`useSSE.ts:186`), with no `project_id` filter, on a single project's Overview tab
(`App.tsx:388-391`). It is the one live consumer without `ActivityLog`'s filter, and `agent_output`
chunks from any project will usually fill all ten pills. Its amber dot tests
`severity === 'warning'` (`:209`), but the Hub writes `"warn"` (21 `severity="warn"` sites in
`hub/hub`), so only `context_warning` shows amber. It grows more visible once F251 admits 18 kinds
and F253 adds `stream_gap` (`"warn"`). Suggested as its own finding; no B9 change carries it.

### Findings that did not need a change

None. F251, F253 and F335 are all still open, in the shape R1 described (F251 has grown by one kind,
`queue_agent_held`, plus the dead `job_deleted` handler). Also noticed and left alone:
`new_session_request` broadcasts before its event row commits (`agents.py:3039-3040`), but nothing
rolls back on that path; `useDivergences` (`api/tasks.ts:447`) has no consumer.

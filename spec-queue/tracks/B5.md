# B5 — Evidence, propose gates and merge truth

**Findings:** F215, F207, F113, F166, F358, F165, F242, F141
**Decisions:** D12 (F242 read_only mid-task; F141 conflict result persisted; F165 unknown-branch spelling)
**Changes:** `openspec/changes/evidence-is-decided-after-the-run-that-recorded-it/`, `openspec/changes/the-coverage-bar-takes-the-evidence-decision-it-asks-for/`, `openspec/changes/a-document-moves-forward-only-through-its-checks/`, `openspec/changes/a-footprint-names-the-line-of-work-its-commit-is-on/`, `openspec/changes/isolation-does-not-change-under-held-work/`, `openspec/changes/the-approval-preview-asks-the-gates-merge-question/`
**Status:** R3 done — parked for operator

## R1 — explore and propose (2026-09-24)

Worked in `.claude/worktrees/spec-tracks` at `ce086b6` (product code identical to HEAD `404c7d5`).
Read `review_turn.verdict_evidence_sentence` (F357, `1b58a2b`) as asked: it now tells an ungranted
reviewer to `ask_user` the operator to decide evidence by id (`review_turn.py:178-235`), which makes
F215 sharper — the product now *routes* the operator to a decision no screen can take.

### Each finding, re-verified against the code

| Finding | Verdict | Evidence |
|---|---|---|
| **F215** (B) | **still open** | No `spec/evidence` string anywhere in `hub/ui/src` or `hub/hub/static/ui` (grep). `SpecCoverageBar.tsx:22-26` still renders *"nobody has decided about it yet"*. Neither decision route broadcasts `spec_updated` (`spec.py:892-926`, `agent_actions.py:1309-1349`), so even a decision by curl leaves open views stale. |
| **F207** (C) | **still open** | `set_phase` calls `spec_lifecycle.transition` directly (`spec.py:1607`); `transition` checks phase map, actor and `explore_closed_at` only (`spec_lifecycle.py:270-356`). **Wider than filed:** `proposed → approved` runs no completeness check on any route, a `proposed` document stays writable (`spec_service.py:165-169` refuses only `approved`), and adoption places documents at `proposed` from file metadata (`spec_adoption.PHASES`, `:44-50`). |
| **F113** (B) | **still open** | `spec_service.propose` returns completeness findings (`:775-779`) and only then calls `transition`, whose `explore_not_closed` arrives as 409 (`spec.py:1569-1573`). Pinned by `test_spec_documents_api.py:323-335`. |
| **F358** (B) | **still open** | `decide` (`requirement_evidence.py:677-737`) never asks whether the recording run is live. `run_liveness.run_is_live` (`:69-71`) exists and its registry entry is popped only after the finalize block that restamps footprints (module docstring `:22-28`) — exactly the window. Duplicate remedy still says *"commit it first"* (`:146-152`) against the briefing's *"You do not need to `git commit`"* (`launchability.py:441`). |
| **F165** (B) + addendum | **still open, and measured** | `_branch_at` (`:565-578`) answers `""` for a non-tip commit; the ordinary arm (`:531-535`) and `restamp_run_footprints` (`:968`) write `"HEAD"` for a detached checkout — **measured** with git 2.49 (the addendum's one unmeasured link). `integration_targets` keys on both as distinct branches (`task_integration.py:285`); `detect_drift` skips both (`:1140-1144`). Review checkouts are detached by construction (`worktrees.py:211-216`), so every reviewer's evidence is keyed `"HEAD"`. |
| **F166** (C) | **still open** | `footprint_root` fallbacks unchanged (`:299-348`). Reachable in the product: `release_task_workspace` runs on `rejected` as well as `approved` (`task_transition_service.py:726-736`) and only approval is gated on a live turn, so rejecting mid-turn removes a live run's directory. `test_evidence_footprint_root.py:982-1013` pins the released case landing on the agent's own checkout. |
| **F242** (B) | **still open, second door** | `PATCH /agents/{name}` merges `config` unguarded (`agents.py:2648-2659`); **also** `POST /agents/register` merges config for an existing name (`:2300-2308`). |
| **F141** (C) | **changed shape, still open** | F156's fix (Round 3) made the preview say *"whether it merges cleanly is checked at approval"* (`tasks.py:1138-1145`) — no longer a false promise, but still nothing remembers or predicts the conflict. `would_conflict`'s own docstring says the probe *"costs nothing and changes nothing"* (`task_integration.py:430-433`), which undercuts the preview's reason for not running it (`tasks.py:1090-1092`). |

### Measurements taken

- **What the operator decision route returns when integration raises** (a scratch test outside the
  repo made `retry_integration` raise): `POST /spec/evidence/{id}/decision` → **200**, body
  `review_state: accepted`, stored row `accepted`. The wrap in
  `integrate_what_was_waiting_for_this_evidence` (`task_integration.py:700-706`) holds.
- **git 2.49, detached checkout:** `rev-parse --abbrev-ref HEAD` → `HEAD`; `git branch --points-at
  <sha>` → `(HEAD detached at <sha>)` when the checkout is detached at that commit;
  `git for-each-ref --points-at <sha> refs/heads` → nothing. So **`_branch_at` returns the string
  `(HEAD detached at …)` as a branch** when an operator's checkout is detached at the commit they
  name. New, latent; carried in the F165 change (D2, task 1.1).

### D12 — the three questions, options and recommendations

**F242 — may `read_only` flip mid-task?** (a) refuse while the agent holds work — a live turn, or
an assigned task not approved/rejected; (b) pin the workspace scheme per task at provisioning
(reverses task 4.7's precedence, and a read-only agent would then write in a task checkout); (c)
snapshot held checkouts at the flip (leaves F242's consequences 2 and 3). **Recommended (a).**

**F141 — is the gate's conflict result persisted?** (a) no — the preview runs the gate's own
`would_conflict` live; (b) persist as a `refused` integration row (a rejected transition writing a
row, stale on rebase, silent before the first approve); (c) preview reads the last refusal (needs
(b)'s storage plus a freshness rule); (d) branch-vs-main in `/worktrees/conflicts` (largest).
**Recommended (a)**: it answers before the first approve, cannot go stale, stores nothing.

**F165 — unknown-branch spelling?** (a) one spelling `""`, the branch resolved wherever git can say
(tip → task branch containing it → sole containing branch → `""`), and an idempotent data
migration `HEAD → ''`; (b) normalise at read (two stored spellings forever); (c) `NULL` (more churn,
no extra meaning). **Recommended (a).**

**How they interact.** Only F165 and F141 touch each other: the preview (F141) probes
`merge_targets`, whose reduction F165's change fixes, so under (a)/(a) the preview inherits the fix
with no extra work. F165's resolution moves reviewers' footprints out of the `"HEAD"` bucket into
the task branch, which exposes an observation-order hazard (a later review of an older commit would
displace newer work) — so the F165 change also makes the reduction keep the **descendant** commit
per line of work. F242 does not interact: it decides which directory a turn runs in, never how a
footprint names its branch.

### What was written — six changes, each ships alone

| Order | Change | Findings | Size |
|---|---|---|---|
| 1 | `a-document-moves-forward-only-through-its-checks` | F207, F113 | S-M (backend + one UI handler) |
| 2 | `evidence-is-decided-after-the-run-that-recorded-it` | F358 | S (backend) |
| 3 | `the-coverage-bar-takes-the-evidence-decision-it-asks-for` | F215 (S5a) | M (UI + two broadcasts) |
| 4 | `a-footprint-names-the-line-of-work-its-commit-is-on` | F165, F166 (D12 q3) | M (backend + data migration) |
| 5 | `isolation-does-not-change-under-held-work` | F242 (D12 q1) | S (backend) |
| 6 | `the-approval-preview-asks-the-gates-merge-question` | F141 (D12 q2) | S-M (backend + drawer) |

3 renders 2's refusal and field but does not depend on it. 6 inherits 4's reduction through
`merge_targets` but does not depend on it. All six pass `openspec validate --strict`.

**Cross-bundle collisions found and declared in the designs:** B6's
`drift-watches-the-files-its-evidence-is-about` edits the same footprint functions as change 4
(different fields, same signatures and drift basis — recommended order: change 4 first); B6's drift
and rigor-history changes edit/mount beside `SpecCoverageBar.tsx` (change 3 reads
`GET /spec/evidence`, not F211's routes, so it does not become F211's first caller); B3's
`agents-no-longer-register-themselves` deletes the second door change 5 guards (B3 already records
it).

### Noticed in R1, not carried — for the orchestrator to file or route

1. **A read-only agent assigned a writing task writes into the operator's checkout, flip or no
   flip.** F242's consequences 2 and 3 do not need the flip: `resolve_turn_workspace`
   (`worktrees.py:776-826`) gives `read_only` precedence over the task, and no worktree means no
   snapshot. Change 5 closes only the mid-task door (its Open Question 1).
2. **The approval gate's conflict probe is unwrapped.** `requirement_gate._check_mergeable`
   (`:434-436`) calls `would_conflict` → `task_integration._git` (`:136-145`), a bare
   `subprocess.run(..., timeout=60)` that raises on timeout or `OSError`. What the approval route
   answers then was not traced (change 6 asks R2 to).
3. **F358's cross-run residue.** A superseded row from an *earlier* run still cannot be retired by
   its author, and a new turn with uncommitted changes still meets the duplicate check at the
   turn-start commit (change 2, Open Questions 1-2).

### Open for R2

- Change 1: does any of the seven test helpers that walk `/documents/phase?to=proposed` carry an
  incomplete fixture?
- Change 2: D2's digest guard against `requirement_coverage`'s staleness; Open Question 2.
- Change 4: D3's seam; drift candidates D2 adds on an existing fixture, combined with B6's basis.
- Change 5: assigned vs provisioned tasks.
- Change 6: call `_merge_situation` directly or extract a helper; time the probe on this repo.

## R2 — independent comparison (2026-09-24)

Checked **about 40 load-bearing claims** against the code at `9880b73` (product code unchanged since
`404c7d5`). All six changes still pass `openspec validate --strict`. The claims that disagreed, and
what changed:

| Change | Claim that disagreed with the code | What R2 changed |
|---|---|---|
| 1 `a-document-moves-…` | "Same checks at approval" — `import_not_approved` is a finding about *another* document; the settled task-dependencies contract preserves and reports it when approval races a reopen (`archive/2026-08-21-task-dependencies/design.md:215-224`), pinned by `test_spec_task_dependencies.py:267-330` (asserts **200**). D3 would reverse it and fail that test | D1 leaves `import_not_approved` out at `approved` (still reported at `proposed`); spec delta gains the exception and a scenario; control test 1.7a; design Open Question 0 asks the operator whether to reverse the contract instead |
| 1 | (R1 asked) are the helpers' fixtures complete? | **No.** None states `scope.non_goals` (`spec_completeness.py:177`) and four carry no criteria: `test_spec_declared_tasks.py:87`, `test_spec_task_dependencies.py:67,302`, `test_task_spec_document_context.py:152`, `test_spec_criteria_reach_the_task.py:207`. Task 2.3 now names each |
| 1 | "F205's dead `proposed → archived` edge — B6" | Already fixed (`SpecPhaseBar.tsx:146-160`); line removed. The explore sentence becomes one constant |
| 2 `evidence-is-decided-…` | D1 placed "after the value check, before the grant check" — contradicts its own reason and test 1.5 (an ungranted agent on a live row would get 409, not 403) | Placed after all three existing refusals (value, grant, self-acceptance) |
| 2 | D2 re-applies the footprint with `_apply_footprint(…, existing)` | That call writes `outside_workspace_writes = None` ("not observed") over what `capture_footprint` recorded (`requirement_evidence.py:392-429`); D2 now passes `outside_writes_for_run` |
| 2 | Open Question 2 (new-turn duplicate) left to R2 | Decided (a) as **D5**: on the duplicate path only, an agent whose checkout is dirty records a new row — **but only in a checkout the Hub snapshots** (task/agent worktree). Review turns and turns in the project checkout pass `worktree=None` (`agent_trigger.py:1033`, `:1341`), so a dirty reviewer checkout (the observed `.pyc` case) would otherwise mint real duplicates. Tests 1.12, 1.12a, 1.13; spec scenario |
| 3 `the-coverage-bar-…` | The *newest* label "is the one a merge takes" | False: the merge reduces per task, across requirements, per branch, by `observed_at` (`task_integration.py:240-287`); the list is per requirement by `produced_at`. Relabelled *latest* = most recently recorded, in design, spec and test guide. F190's order test survives |
| 4 `a-footprint-names-…` | D4 gives `integration_targets` an optional root | `merge_targets`' docstring states `integration_targets` stays a pure DB query (`task_integration.py:385-392`), and every merging caller already uses `merge_targets` (gate `:417`, transition service `:883`, prerequisites `task_workspace.py:242`). **D4 moved into `merge_targets`**; the preview's governed path (the one direct `integration_targets` caller, `tasks.py:1122`) switches to it — shared with change 6's D1, whichever lands first. Test 1.11; spec scenario |
| 4 | Migration `0106` | 0106 is named by four other parked changes; now "the next free revision" |
| 6 `the-approval-preview-…` | Only the probe loop is wrapped | `_merge_situation`'s own `is_repository`/`branch_exists` and `task_branch_tip` raise the same way; the wrap covers them. Sharing decided: rename `_merge_situation` → `merge_situation` (one call site, `:618`), preview calls it and falls back to today's listing on `None`. Test 1.5a |

**Stands, re-derived:** every route/function citation in F207/F113 (`set_phase` → `transition` only;
`transition` has exactly two callers, both operator-credentialed); the registry is popped after the
restamp on both transports (`agent_trigger.py:2461`→`2706`, `:3134`→`3250`); D2's digest guard
matches coverage's staleness test exactly (`requirement_coverage.py:192`); the agent-plane list
already spreads `_evidence_view`; neither decision route broadcasts, and both `spec_updated`
consumers tolerate a missing `path`; F242's two doors are the only writers of `read_only`.

**R1's measured claims, re-measured:** git 2.49 `rev-parse --abbrev-ref HEAD` on a detached checkout
→ `HEAD` (confirmed). `git branch --points-at`: detached at a branch **tip** gives two lines, so
`_branch_at` answers `""`; detached at a **non-tip** commit gives the single line `(HEAD detached at …)`,
which `_branch_at` returns as the branch (confirmed, and the non-tip case is the reachable one). The
decision route's 200 under a raising integration: the wrap at `task_integration.py:700-707` is as R1
read it; not re-run.

**Decisions (D12):** none reversed. F242 keeps **(a) refuse under held work**, and R2 keeps
"assigned, not terminal" over "provisioned": turning isolation *on* for a read-only agent has no
checkout to count, yet its earlier turns' edits sit uncommitted in the operator's checkout (test
1.5a added). F141 keeps **(a) the preview probes live** — measured at 0.03-0.16 s on this repository.
F165 keeps **(a) one spelling `""`**.

**For B6's R3 — the footprint change's pieces B6 builds on:** D1, D2 (`line_of_work(root, commit, *,
task_branch=None)`; `read_footprint(root, *, at=None, task_branch=None)`), the restamp reading
`Run.task_id` once, and D3 (read at `agentweave/task/<id>`'s tip in `workspace.root`) are
**unchanged**. D3's seam is now named: `read_evidence_footprint(...)`, called by `_take_footprint` and
`capture_footprint`, wrapping `read_footprint` — B6's new keyword arguments pass through it. **D4
moved** (from `integration_targets` to `merge_targets`); B6 marks D4 unrelated. The migration number
is now "next free".

**The approval route when the gate's git calls raise (R1 left untraced):** `TimeoutExpired` (after
60 s per call) or `OSError` from `task_integration._git` leaves `evaluate` →
`task_transition_service.transition()` → no registered handler (`main.py:532-553` handles only
`TransitionRefusedError`, `TaskBindingError`) → **bare 500 on every approval surface**, nothing
committed. Not carried by any change here (it needs a decision: refuse as unknown, or approve and let
the merge record its failure) — **candidate finding**, with R1's other unrouted one (a read-only agent
given a writing task writes into the operator's checkout; change 5's Open Question 1, recommended
(d) file it).

## R3 — independent comparison (2026-09-24)

Checked **about 35 load-bearing claims** against the code at `d0ef6f1` (product code unchanged since
`404c7d5`), working each one out again from the entry points rather than from R2's notes. All six changes pass
`openspec validate --strict`. **One targeted run** (a scratch test, deleted; nothing left in
`hub/tests/`) settled the decision route's answer under a raising merge.

| Change | Claim re-derived | Verdict | What R3 changed |
|---|---|---|---|
| 2, 3 | "The decision route answers **200** when integration after the decision raises" (R1 measured, R2 read) | **Wrong.** With an approved task actually waiting and `retry_integration` raising, the wrapper rolls back (`task_integration.py:702-707`), which expires every loaded row; the route then reads `evidence`/`review` to build its answer and raises `MissingGreenlet` at `spec.py:926` → `:1110`: **a bare 500**, with the decision committed (`accepted`). The agent plane reads `evidence.id` after the same call (`agent_actions.py:1354-1357`) and fails the same way. R1's scratch had nothing waiting, so it never reached the raise | Change 2 gains **D6**: both routes build their response before integrating. Spec requirement + scenario, test 1.14 (a stub must *load* the task through the session, or it opens no transaction and passes today). Change 3: corrected both of its "200" claims; its broadcast must use the id captured before integration (test 1.1a) |
| 2 | D2 revises "the row `duplicate_of` returns" when it is the same run's | **Cannot fire in D5's own case.** `duplicate_of` returns the **oldest** match (`requirement_evidence.py:236-244`). A new turn starts at the previous turn's snapshot, where the previous run's row already sits. So a second record in that turn finds the previous run's row, skips D2, and D5 mints a third row | D2 looks up **this run's** matching row first. Test 1.7b; spec scenario |
| 2 | D5 applies when "the footprint root" is under the task/agent roots | Keyed on the wrong directory when the run's directory is gone: `footprint_root` then falls back to the agent's *own* checkout (`:343-348`), which the turn's snapshot does not commit | D5 keys on `Run.workspace_dir` (written from `effective_work_dir`, `agent_trigger.py:1264`, the directory `_execute_run` snapshots) |
| 4 | D4 keeps the incumbent where the incoming commit "is an ancestor" | `merge-base --is-ancestor X X` succeeds. Two footprints of the **same** commit would keep the older row, which changes the `evidence_id` the gate's refusal names (`requirement_gate.py:448-452`) where no ancestry is at stake | *Proper* ancestor. Control 1.5c |
| 4 | D4 in `merge_targets` fires from the real approval path | **Holds, traced**: `PATCH …approved` → `transition` → `evaluate` → `_merge_situation` → `merge_targets` (`requirement_gate.py:417`); on acceptance `integrate_task` → `merge_targets` (`task_transition_service.py:714`, `:883`); `retry_integration` → `integrate_task` (`:801-824`); prerequisites (`task_workspace.py:242`). No merging caller reads `integration_targets` | Test 1.5b pins it through the route (1.5 only called the function) |
| 1 | R2's seam: `import_not_approved` filtered out at `approved` | **Right seam.** Passing no approved paths flags every import, and faking them lies to a pure function. Side effect now stated: an import *added after* proposing to a never-approved document also passes, and is recorded as `document_not_approved` by the same code as the race | D1 paragraph; no test change |
| 1 | Does anything else reach `approved` unchecked? | **No.** `transition` has two callers (`spec.py:1607`; `spec_service.py:781`, which only targets `proposed`), and the only `.phase =` writer is `spec_lifecycle.py:339`. Adoption can register a document *at* `approved` (`spec_adoption.py:235-240`) but creates no tasks (`materialise_quietly`'s one caller is `spec.py:1624`). That is B6's registration question | Stated in D1 |

**Re-derived, still holds:** F165's three writers of the unknown branch (`requirement_evidence.py:531-533`,
`:968`; `detect_drift` `:1143`); `_branch_at`'s single-line rule (`:565-578`); `duplicate_of` ignores
digest; the snapshot runs only for a non-review isolated workspace (`agent_trigger.py:1033`, `:1341`,
`:2400-2408`) and uses the same porcelain predicate D5 asks (`worktrees.py:857-887`); the list route's
order (`produced_at`, id); `_merge_situation` has one call site; `task_integration._git` is unwrapped
(`:136-145`) and the app has exactly two exception handlers (`main.py:532`, `:544`), so R2's
approval-surface 500 stands; change 5's refused-whole claim holds because `get_session` never commits
on exit (`db/engine.py:166-169`). No other unarchived change touches the decision routes (grep).

**Decisions (D12):** none reversed.

**B6's footprint seam:** change 4's D1-D3 are **unchanged**: `""` only, `line_of_work(root, commit, *, task_branch=None)`,
`read_footprint(root, *, at=None, task_branch=None)`, `read_evidence_footprint(...)`. D4's only edit
(proper ancestor) is inside `merge_targets`, which B6 does not touch.

## Final — what the bundle reached, and why

Bundle B5 covers three things: how evidence is recorded and decided, what gates a specification
document's move to proposed and approved, and what approval actually merges. It produced six changes
that can each ship alone, plus recommended answers to D12's three questions. **Nothing is built yet: the
bundle is parked until the operator approves it.**

### Decisions

| # | Question | Recommended answer | Why |
|---|---|---|---|
| D12-1 (F242) | May an agent's `read_only` (isolation) setting change while it holds work? | **No. Refuse while the agent has a live run or an assigned task that is not approved/rejected; allow otherwise.** Both directions, both doors (`PATCH /agents/{name}`, `POST /agents/register`) | Changing it mid-task strands work either way. Turning isolation off sends the next turn into the operator's checkout, away from the task branch. Turning it on starts a fresh task checkout that cannot see earlier turns' uncommitted edits in the operator's checkout (`worktrees.py:763-773`, `agent_trigger.py:1033`). Pinning the setting per task would let a read-only agent write. Snapshotting at the change would still leave the next turn's harm |
| D12-2 (F141) | Is the approval gate's conflict result stored? | **No. The approval preview runs the gate's own conflict probe live** (`would_conflict` over `merge_situation`'s targets) | This answers before the first approve and after a refusal, never goes stale, and stores nothing. Measured at 0.03-0.16 s on this repository. Storing it would make a refused transition write a row that goes stale on the next rebase and says nothing before the first approve |
| D12-3 (F165) | How is "unknown branch" spelled on a footprint? | **One spelling, `""`, and the branch resolved wherever git can say** (tip → task branch containing it → the only branch containing it → `""`), plus a data migration that is safe to re-run, `HEAD → ''` | Today `""` and `"HEAD"` are two buckets to the merge and one to drift (`task_integration.py:283-286`, `requirement_evidence.py:1143`). Every reviewer's evidence is keyed `"HEAD"`, and an operator naming a non-tip commit can get the branch `(HEAD detached at …)` (measured, git 2.49) |
| F113 status (S6) | Should `POST /documents/propose` report an open exploration as a 409, or inside its 200 `blocking` list? | **Inside the 200 `blocking` list**, with every completeness finding | The route then has one shape for "not yet". The UI never offers Propose while exploration is open. One test changes on purpose |
| Change 1, Q0 | Should approval refuse an import whose source document was reopened, or never approved? | **No.** Keep the settled task-dependencies contract: approve, and record the reference that no longer resolves | Pinned by `test_spec_task_dependencies.py:267-330`, and `materialise` already records `document_not_approved`. Answering yes would change that requirement and its test |

### Changes

| Change | Findings | What it does | Size |
|---|---|---|---|
| `a-document-moves-forward-only-through-its-checks` | F207, F113 | The phase route runs the same completeness checks as `propose` before `proposed` **and** `approved` (409 `document_incomplete` with the full list; `import_not_approved` exempt at approval). `propose` reports an open exploration in its `blocking` list. The app shows an approval refusal | S-M (backend + one UI handler) |
| `evidence-is-decided-after-the-run-that-recorded-it` | F358, **plus the 500 R3 found** | A decision waits (409 `recording_run_live`) while the recording run is live. Recording again in the same run revises that run's own undecided row. An agent's changed checkout in a new turn is not a duplicate, and the agent is not told to commit. Views carry `recording_run_live`. **Both decision routes return the decision they saved even when the merge after it fails (D6)** | S-M (backend) |
| `the-coverage-bar-takes-the-evidence-decision-it-asks-for` | F215 | Accept/Reject on each evidence piece in the coverage bar's rows; both decision routes broadcast `spec_updated` | M (UI + two broadcasts) |
| `a-footprint-names-the-line-of-work-its-commit-is-on` | F165, F166 | One unknown spelling and a resolved branch (`line_of_work`). A released run's evidence reads the task branch's tip (`read_evidence_footprint`). Within one branch, approval merges the descendant commit (in `merge_targets`). Data migration | M (backend + migration) |
| `isolation-does-not-change-under-held-work` | F242 | The setting change is refused under held work, on both doors, and the whole request body is refused | S (backend) |
| `the-approval-preview-asks-the-gates-merge-question` | F141 | The preview calls the gate's `merge_situation` (renamed to public) and probes each target, naming conflicting paths before approve. Any git failure falls back to today's hedged sentence | S-M (backend + drawer) |

**Order.** Changes 1 and 5 are independent. **Change 2 goes before change 3.** R3 found that change 3's broadcast test 1.1a relies on change 2's D6; if 3 must ship first, it builds its own response before integrating. Change 4 before change 6 is preferred, because 6 inherits 4's reduction through `merge_targets`; whichever lands first switches the preview's governed path to `merge_targets`. Change 4 also goes before B6's `drift-watches-the-files-its-evidence-is-about`.

**Migration number.** Change 4's migration takes **the next free revision at build time**. Head is
`0105` today, and `0106` is already named by four other parked changes
(`a-checkpoint-is-handed-over-once-and-says-where-it-went`, `an-ask-me-card-says-what-workspace-only-would-decide`,
`worker-spend-counts-against-the-budget`, `agents-no-longer-register-themselves`). Whichever lands
second renumbers and bumps the head assertions in `test_migrations.py` and `test_project_persistence.py`.

### Open for the operator

1. **D12-1 (F242):** refuse the isolation change while the agent holds work? *Recommended: yes.* "Pin per task"
   replaces change 5's D1 and reverses task 4.7's precedence (a read-only agent would write in a task
   checkout). "Allow freely" drops change 5 and closes F242 as accepted behaviour.
2. **D12-2 (F141):** the preview probes live and nothing is stored? *Recommended: yes.* "Store it" replaces
   change 6's D1 with a `refused` integration row and means checking every reader that filters on
   `outcome`. "Read the last refusal" needs that row plus a rule for when it goes stale.
3. **D12-3 (F165):** one spelling `""`, with a data migration that runs on `:8000`'s data? *Recommended: yes.*
   "Normalise at read" drops the migration but keeps two spellings forever. "Fix only the operator case"
   drops D1 and leaves reviewers in their own `"HEAD"` bucket.
4. **F113:** `propose` reports an open exploration inside its 200 `blocking` list? *Recommended: yes.*
   Answering no keeps the 409; change 1's D2 is dropped and the rest stands.
5. **Change 1, Q0:** approval does not refuse a stale import? *Recommended: no refusal.* Refusing
   changes the task-dependencies requirement and its pinned test.
6. **Two candidate findings for the ledger.** No change here carries them; the orchestrator files them:
   - **When the approval gate's git calls raise, every approval surface returns a bare 500.**
     `task_integration._git` is an unwrapped `subprocess.run(timeout=60)`. `is_repository`,
     `branch_exists`, `task_branch_tip` and `would_conflict` all use it from `evaluate`, and the app
     handles only `TransitionRefusedError`/`TaskBindingError` (`main.py:532-553`). A timeout or
     `OSError` makes operator `PATCH /tasks/{id}`, the agent plane and MCP answer 500 after up to a
     minute per call, with nothing committed. This needs a decision: refuse because the answer is unknown, or approve and let the
     merge record its failure. *Recommended: refuse, with a stated "could not ask git" reason.* The
     gate exists so nothing is approved that might not merge, and the operator can retry.
   - **A read-only agent assigned a writing task writes into the operator's checkout, even with no setting change.**
     `takes_task_workspace` gives `read_only` precedence over the task (`worktrees.py:763-773`), and
     nothing snapshots a turn that is not isolated. Nothing in the app sets `read_only` today.
     *Recommended: file it now, and refuse assigning a writing task to a read-only agent if the app ever
     offers the setting.*
   - Not a candidate, because change 2's D6 already fixes it: R3's decision-route 500. It still needs a
     finding id so the ledger names what change 2 closes.

### Findings that did not need a change

None of the eight findings was already fixed or a duplicate, and each is carried by exactly one change
above. **F141 changed shape** after F156's fix: the preview no longer promises a clean merge (it says
approval checks it), but it still predicts nothing, so it stays with change 6. **F205**'s dead
`proposed → archived` edge, which R1 touched in passing, was already fixed (`SpecPhaseBar.tsx:146-160`)
and is not carried here.

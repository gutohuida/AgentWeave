# 2026-09-15 night window

Newest entry at the BOTTOM. Playbook: `.claude/loops/night-window.md`.

Armed 2026-09-15T22:55:03+01:00 on `autonomous/2026-09-14-daily` (parent master@f28bc31), model opus.

## Iteration 1 — 23:10, compose (reconciled): the queue two firings wrote was yesterday's, already built

**Reconciliation.** Branch `autonomous/2026-09-14-daily` at `69ce883` matches STATE. But the tree was
dirty: two compose firings (23:00 and 23:05, Sonnet) had rewritten STATE-night.json's queue and
exited without a log entry or a commit, each leaving a background `pytest hub/tests` running (pids
24456 and 23112, both orphaned, both killed this firing; their partial output is in the untracked
`.claude/autonomous/tmp/`). The queue they wrote was 2026-09-14's `ORDER:` line, verbatim: R1..R3,
IMPL and DRIVE for `who-owns-a-loops-queue`, then `a-late-answer-is-delivered`. **Every item was
already done on the 2026-09-14 night:** the reachability half was proposed as
`a-task-nothing-will-move-holds-nobody` (`9e140e1`), implemented `4b59ee0`, driven and archived
`0cdbdf0`; `a-late-answer-is-delivered` was implemented `d16a76a`, driven and archived `a099af2`
(`openspec/changes/archive/2026-09-15-a-late-answer-is-delivered`). This firing was routed to
Opus for "R1" of a change that exists. Discarded that queue.

**Why it happened, and the fix (in this commit).** `APPROVALS.md`'s newest section is still
`## 2026-09-14`: 09-15 had no daily window, and the operator's decisions that day went into
`DECISIONS.md` instead. The playbook read "newest section" with no date check, which the 09-14
section's own text had warned about. `.claude/loops/night-window.md` step 2 now applies a section
only if its heading is the date the window armed, and tells compose to read `DECISIONS.md`'s newest
`## Decided` entries too.

**What tonight actually has.**
- APPROVALS: no 2026-09-15 section → the default backlog-first queue applies.
- Unarchived changes: only two. `an-unstaffed-review-names-its-holders` (0/24) is **not
  buildable**. It argues from option (e), and F352-free was decided (f) on 2026-09-15 (`26ded6e`),
  so its rung-3 half needs a re-derivation round, which is day-window work. That's a note to
  tomorrow, not work tonight. `a-refusal-names-a-remedy-that-works` (0/23) **is buildable**. The operator
  approved the split and said "build the F353 half now" (DECISIONS.md `### 2026-09-15, later` and
  F352-free row; split `cba4ae2`). Its one verification round ran at `accba6e` (READY TO BUILD WITH
  FIXES, all five applied). Retires F353, F334, F365, F367 (all B). No migration, no
  `mcp_server.py`, no UI.
- Tree green: `1988118` recorded the full Hub suite at 4397 passed / 0 failed, and
  `git diff 1988118 HEAD -- hub/hub src hub/tests tests` is empty. So the inherited tree is the
  one measured green. I didn't re-run it (the two orphaned runs were duplicates of each other and
  never finished).
- No Hub is listening on any 80xx port right now.

**Queue:** the change split into firing-sized items: groups 1+2, group 3, group 4 in two halves,
then gate, drive, archive, and close-out. Ids carry the `-impl`/`-gate`/`-drive`/`-archive` suffixes,
so the driver routes them to Sonnet.

## Iteration 2 — groups 1+2: own_review_remedy, and error_summary fits its column

Branch and `git log` matched STATE; tree was clean. Built `a-refusal-names-a-remedy-that-works`
tasks 1.1–2.4 (`openspec/changes/a-refusal-names-a-remedy-that-works/tasks.md`):

- **1.1** `own_review_remedy(task)` in `hub/hub/scheduler.py`, beside `_wedged_review_reason`.
  Public (no leading underscore) for group 4's later `agent_trigger` import, not wired yet. Asserts
  `task.status` is `completed` or `under_review`.
- **2.1** `JOB_RUN_ERROR_SUMMARY_CHARS = 500` and `fit_error_summary(text)` in
  `hub/hub/db/models.py`, beside `JobRun`; `@validates("error_summary")` applies it (the first
  `@validates` in `hub/hub`, per D2); the column now reads the constant; `JobRunResponse` in
  `hub/hub/schemas/jobs.py` imports it for `max_length`; `_stall_run_to_increment`
  (`scheduler.py:961`) now compares against `fit_error_summary(stall_reason)`. Re-grepped
  `error_summary\s*=` first — still the nine sites D2 names, all model-level, no new site.
- **2.2** `_wedged_review_reason` now trims its quoted title (one character at a time, re-`repr`ing
  each attempt) until the sentence fits 500 characters, appending `…`.
- New test file `hub/tests/test_a_refusal_names_a_remedy_that_works.py`, 7 tests covering 1.2, 2.3,
  2.4. **All four named mutations applied and verified to make their test fail, then reverted**:
  swapping `own_review_remedy`'s two branches (both new tests failed on the swapped sentence);
  disabling `JobRun`'s `@validates` (the 600-char test failed on `600 == 500`, the raw string was
  stored); skipping `_wedged_review_reason`'s trim loop (the fit test failed on `552 <= 500` — the
  unmutated sentence at a 32-char reviewer/256-char title measures 552, close to D2's own 551
  estimate for the same shape).

Ran `hub/tests/test_a_refusal_names_a_remedy_that_works.py`, `test_loop_stall_ticks_in_place.py`,
`test_scheduler.py`, `test_job_reaches_the_scheduler.py`: 80 passed (one known `RuntimeError: Event
loop is closed` thread-exception warning in `test_scheduler.py`, unrelated — a teardown artifact,
not a failure). `ruff check` and `black --check --target-version py311` clean on every touched file
(`black` reformatted one import-wrap line in `models.py`, applied). Did not run `mypy` against the
touched `hub/` files — CI's lint set only points `mypy` at `src/`, which none of this touches; that
step belongs to 5.1 later, over the finished change.

Ticked 1.1, 1.2, 2.1, 2.2, 2.3, 2.4 in `tasks.md`, each recording its mutation and the observed
failure. Next: group 3 (once per task, F365).

## Iteration 3 — 23:37, group 3: once per task (F365)

Branch and `git log` matched STATE; tree was clean. Built
`a-refusal-names-a-remedy-that-works` tasks 3.1–3.3 (`tasks.md`). Re-grepped
`_review_unstaffed_already_stands` first, as instructed — it had moved to `scheduler.py:2120`
(groups 1–2 added ~40 lines above it), not the `:2067-2069` the task's cited line numbers warned
were stale.

- **3.1** Added `.where(EventLog.data["task_id"].as_string() == task_id)` to the query and dropped
  the now-redundant `data.get("task_id") == task_id` from the Python-side check. **Carried REV item
  10** (missed in the first split pass): the docstring claimed "a condition that cleared and
  returned is news again," which stays untrue even after this fix — there is no "cleared" event, so
  a task whose newest record still reads the same reason looks unbroken-standing whether the
  condition was continuous or recurred. Rewrote the docstring to state the narrower rule actually
  implemented (newest-for-this-task, not newest-for-any-change) and name the open gap explicitly
  rather than repeat the untrue claim.
- **3.2 / 3.3** New tests in `hub/tests/test_a_refusal_names_a_remedy_that_works.py`, with their own
  `_flow`/`_operator_completed`/`_evidence` fixtures (adapted from
  `test_a_flow_names_what_it_cannot_staff.py`'s fixture (b) shape) and a `live_scheduler` fixture
  (copied from `test_a_review_nobody_is_doing.py`) so `POST /jobs/{id}/run` reaches a real firing.
  3.2: two operator-completed, single-agent-roster tasks, evidenced from setup so both reach the
  exclusion branch immediately, five firings — exactly one `review_unstaffed` row per task. 3.3: task
  `a` starts refused at the commit gate (no evidence at all — a different sentence than the
  exclusion branch); between firings, evidence recorded for `a` by a second roster agent that did
  not work it resolves the commit gate *and* widens `a`'s exclusion (via
  `agents_that_recorded_evidence_for`) to the full two-agent roster, flipping its reason to the
  exclusion-branch text; task `b`, worked by the same author and never evidenced, stays unchanged.

  **Both mutations applied and verified to make their test fail, then reverted** (dropping the
  `task_id` filter and restoring the old loop-newest check): 3.2's task `a` recorded 5 events
  instead of 1 (one new row every firing — each firing's freshly-written row for the *other* task
  always shadows the next check, reproducing F365's measured shape almost exactly). 3.3's task `b`
  recorded 2 events instead of 1 (its unchanged reason stopped being suppressed once the check
  started comparing against task `a`'s just-written row instead). Also re-ran
  `test_an_unchanged_wedge_is_recorded_once_not_once_per_tick`
  (`test_a_review_nobody_is_doing.py`, one task, five firings) against the same mutant: it still
  passed, confirming why a single-task fixture never caught this.

Ran the full related set — `test_a_refusal_names_a_remedy_that_works.py`,
`test_a_review_nobody_is_doing.py`, `test_a_flow_names_what_it_cannot_staff.py`,
`test_scheduler.py` — 112 passed (same known `RuntimeError: Event loop is closed` thread-exception
teardown warnings as iteration 2, unrelated). `ruff check` clean; `black --check --target-version
py311` reformatted the new test file's line-wrapping once, applied and re-verified green. Ticked
3.1–3.3 in `tasks.md`, each recording its mutation and observed failure. Next: group 4a (the
refusals, 4.1–4.5, F353/F334).

## Iteration 4 — group 4a: the refusals (F353/F334)

Branch and `git log` matched STATE; tree was clean. Built `a-refusal-names-a-remedy-that-works`
tasks 4.1–4.5 (`tasks.md`).

- **4.1** `_guard_reviewer_is_not_the_author` (`task_transition_service.py:443-465`), both raise
  sites now share one `remedy` string chosen once, by `actor.is_operator`, before either branch:
  operator gets *"Land it, on the task, to review it yourself, or dispatch a different agent's
  review turn (POST /agent/trigger with review_task_id)."*; agent gets *"None of the task tools
  you are offered reassigns a task; the operator can move it on."* Both refusal sentences now open
  *"Cannot move task {id} to 'under_review' with {assignee!r} as its holder: …"* — none of "is
  assigned to", "still assigned", "holding it", "held by", or "Left as is…" survive. Measured
  length at a 64-char id and 32-char name, all four combinations (branch × actor kind): 392, 346,
  468, 422 — all under 500 (the fit test itself is group 4b's 4.10). Rewrote the docstring's
  *"`actor` is deliberately unread"* paragraph: actor now drives the remedy, not the refusal.
- **4.2** `review_dispatch_refusal` (`agent_trigger.py:504-521`): `own_review_remedy` imported at
  module level from `...scheduler` (no cycle — `scheduler` imports nothing from `agent_trigger`);
  both `return` statements now end with `own_review_remedy(task)`, and the completer branch's
  "clear the assignee" clause is gone. Confirmed `task.status` is always `"completed"` or
  `"under_review"` at both call sites (the function's own status guard at `:487-493` filters to
  exactly those two before either branch is reachable), so `own_review_remedy`'s internal assert
  cannot fire here.
- **4.3–4.5** Three new tests in `test_a_refusal_names_a_remedy_that_works.py`. 4.3: an operator
  PATCH on a completed task held by its author, 403, asserts "Land it" and "review_task_id" in
  `response.json()["detail"]`, none of "clear the assignee"/"approves"/"name that agent as the
  assignee". 4.4: an agent PATCH through `/agent-actions/tasks/{id}` with a minted run token
  (`hash_run_token`, `aw_run_` prefix — the token in the mutation-run had to be corrected to that
  prefix or auth itself 401s before the guard is ever reached), same fixture, asserts "None of the
  task tools you are offered reassigns a task", none of "clear the assignee"/"Assign a different
  reviewer"/"no agent can"/"changes who holds"/"API". 4.5 (F334's shape) reuses
  `test_a_refused_review_leaves_nothing_behind.py`'s `_leg_a` fixture directly by import rather
  than rebuilding it — that fixture already reproduces F334 exactly (a review queued behind the
  reviewer's own running turn, evidence recorded for the task by that agent before the turn ends,
  then `DELIVERY_ATTEMPT_LIMIT` delivery attempts through `schedule_agent`), and is separately
  pinned on `GUARD_WORDS = "recorded evidence for this task"`, which my wording keeps. Learned
  along the way: `enter_selected_task` always calls `apply_transition` with `operator()`
  (`scheduler.py:878`, unchanged) — staffing is a Hub-internal act, never the reviewer's own — so
  every delivery pass reads the *operator* remedy, not the agent one; the test asserts only what
  F334 actually measured (no "is assigned to", the task's `assignee` stays `None` through every
  pass because the staged assignee is rolled back with the refused transition before
  `entry.waiting_reason` is recorded).

  **All three named mutations applied and verified to make their test fail, then reverted.** 4.3
  and 4.4 share one mutation shape (swap `if actor.is_operator` to `if not actor.is_operator`,
  which is what "restore the old sentence" and "ignore `actor`" collapse to here, since `actor` is
  read for nothing else): 4.3 failed on `'Land it' in detail` (got the agent sentence instead),
  4.4 failed on `'None of the task tools...' in detail` (got the operator sentence instead). 4.5's
  mutation (restored `"it is assigned to {assignee!r}, which recorded evidence…"` in the evidence
  branch) failed on `"is assigned to" not in waiting_reason` — all five recorded delivery passes
  read *"it is assigned to 'rr-reviewer'"*.

  Ran `test_a_refusal_names_a_remedy_that_works.py` (12 passed), `test_reviewer_is_not_the_author.py`
  + `test_a_refused_review_leaves_nothing_behind.py` + `test_a_task_waits_while_its_run_waits.py`
  (96 passed), `test_agent_trigger.py` + `test_review_turn.py` + `test_review_divergence.py` (85
  passed) — same known thread-exception teardown warning, unrelated. `ruff check` and `black
  --check --target-version py311` clean on every touched file. Ticked 4.1–4.5 in `tasks.md`, each
  recording its mutation and observed failure. Next: group 4b (4.6–4.10 — old-sentence test
  updates, dispatch-route tests, the D9 duplication fix, the 500-char fit test).

## Iteration 5 — group 4b: old-sentence tests, dispatch-route tests, D9's once-only remedy, the fit test

Branch and `git log` matched STATE; tree was clean. Built `a-refusal-names-a-remedy-that-works`
tasks 4.6–4.10 (`tasks.md`). Re-grepped every cited line first, as instructed — all five comment
sites and all six wording assertions were still at the lines the task named, unmoved by group 4a.

- **4.6.** Confirmed the six wording assertions unchanged (they pin fragments D1/D4 keep:
  "could not staff this step", "has worked on this task", "is the one that completed this task",
  "review it yourself", and the two "completed" absence checks). Amended the five comments stating
  the pre-D4 remedy in the present tense: `api/v1/tasks.py:1261-1272`,
  `schemas/tasks.py:123-128`, `test_reviewer_is_not_the_author.py:374-378`'s docstring (all three
  described the mechanism — PATCH reassigning, or clearing to `None` — rather than quoting a
  sentence the guard no longer says), `agent_trigger.py:853-859` ("already names both remedies"
  → "already names the remedy for whichever actor is asking"), and
  `task_transition_service.py:401-406` ("they clear or reassign `assignee` first" → "the
  refusal's own remedy (design D4, below) names how"). **Found one more, not on the split's own
  list**: `test_the_evidence_names_the_author.py:434` still asserted the evidence branch's
  pre-D4 wording ("No agent is recorded as completing it"), broken by group 4a and invisible to
  its own related-test run because that file was never in it. Surfaced only once I ran the
  broader suite for this task's own verification. Fixed to assert the new fragment ("with no
  completer recorded"); recorded in `tasks.md` as a gap in the split's own audit method (built
  from the design doc's before/after wording, not a fresh grep of every test touching the
  guard's message) rather than silently patched.
- **4.7/4.8.** Two new tests on `review_dispatch_refusal` (the `POST /agent/trigger` route's own
  precheck, distinct from the PATCH-path guard groups 4.1-4.5 covered): `test_the_dispatch_routes_author_refusal_names_land_it`
  (completed task, own author dispatched to review it: 403, "Land it", not "clear the assignee")
  and `test_the_dispatch_routes_completer_refusal_never_names_land_it` (under_review task nobody
  currently holds, dispatched to its completer: 403, approve/reject/revision_needed, never "Land
  it"). *Mutations applied and verified to fail, then reverted*: 4.7 (appended the old clause to
  `own_review_remedy`'s completed branch) failed on `"clear the assignee" not in detail`; 4.8
  (`own_review_remedy` always returns the completed sentence) failed on `"approve" in detail`.
- **4.9.** The verification round's named duplication defect, fixed at both sites
  (`agent_trigger.py`'s `review_dispatch_refusal` and `trigger_agent_directly`'s own D9 check).
  Re-reading the task's own wording caught a subtlety my first pass missed: it isn't just "make
  'let the review in flight finish' end its own sentence" — the *"Reassign the task if …"* prefix
  drops out entirely (matched by the "and not 'Reassign'" assertion the task names), because
  reassigning was never actually the guard's remedy and duplicating "decide it yourself" was the
  narrower bug groups pointed at. Both sites now read `f"Task {id} is already under review by
  {assignee!r}. Let the review in flight finish. {own_review_remedy(task)}"`. Two tests —
  `test_the_precheck_names_the_remedy_exactly_once` (site 1, via the route) and
  `test_the_dispatch_itself_names_the_remedy_exactly_once` (site 2, calling
  `trigger_agent_directly` directly so the route's own precheck doesn't intercept first).
  *Mutations applied and verified to fail, then reverted*: (a) restoring the old sentence at site
  1 (no `own_review_remedy` appended) failed on `"approve" in detail`; (b) restoring the original
  duplicated concatenation failed on `detail.count("decide it yourself") == 1` (got 2) — exactly
  the defect the split's own verification round predicted, and exactly what a looser assertion
  (words present, "Reassign" absent) would have missed, as the task warned.
- **4.10.** Two tests: `test_the_guards_sentence_fits_at_worst_case_ids` calls
  `_guard_reviewer_is_not_the_author` directly for all four combinations (F70 completer / F306
  evidence-author branch × operator/agent actor) at a 64-char task id and 32-char agent name,
  asserting `len(message) <= 500` and the remedy fragment survives whole;
  `test_the_dispatched_refusal_at_worst_case_ids_also_fits` reaches the same guard through the
  real dispatch route (`enter_selected_task` → `TransitionRefusedError`), reading the length off
  the route's own 403 response rather than `JobRun.error_summary` (which D2 already fits
  separately — reading from there would hide exactly this class of bug, per the task's own
  instruction). *Mutation applied* (lengthened the evidence branch to something in the shape of
  the pre-fit wording, measured 681 chars at these ids): failed on `len(message) <= 500`.
  Reverted.

Ran the full 18-test file (18 passed), then the broader related suite plus this file — 13 files,
363 passed — twice: once surfacing the 4.6 gap above (1 failed: `test_the_evidence_names_the_author.py`),
once clean after the fix. Same known `RuntimeError: Event loop is closed` thread-exception
teardown warning throughout, unrelated (pre-existing, `test_scheduler.py`). `ruff check` and
`black --check --target-version py311` clean on every touched file. Ticked 4.6–4.10 in `tasks.md`,
each recording its mutation and observed failure. Committed `7343200`, pushed. Next: 5.1-5.2, the
gate — CI's lint set (already run piecemeal above; re-run as the full documented command set) and
the full `hub/tests/` suite backgrounded with counts recorded.

## Iteration 6 — 5.1-5.2: the gate, and one more stale-wording gap

Branch and `git log` matched STATE; tree was clean (one untracked leftover dir from prior
iterations' killed background runs, `.claude/autonomous/tmp/`, unrelated). Ran CI's full documented
lint set over exactly its paths — `ruff check src/ hub/ tests/` (all checks passed), `black --check
--target-version py311 src/ hub/hub/ hub/tests/ tests/` (577 files unchanged), `mypy src/` (no
issues, 22 files). **5.1 clean.**

**5.2, first full run** (`py -3.11 -m pytest hub/tests/ -q`, backgrounded, tracked via the harness's
own background-task notification rather than a manual `&`-and-poll — cancelled a duplicate manual
background launch from the same command to avoid a second suite fighting the first over the test
database): **1 failed, 4414 passed, 86 skipped, 3130.22s (52:10)**. The failure —
`test_review_dispatch_staffs_the_task.py::test_naming_the_author_as_its_own_reviewer_is_refused_before_the_turn`
— asserted the pre-4.1 guard wording (`"the agent recorded as completing it"`), which group 4a's
message rewrite (iteration 4, `4515728`) replaced with `"the agent recorded as completing this
task"` plus an actor-chosen remedy. Same class of gap as the 4.6 finding in iteration 5
(`test_the_evidence_names_the_author.py`): a test pinned to the guard's exact sentence, sitting in
a file never in group 4a's own related-test run because it exercises the *dispatch* path
(`trigger_agent_directly`) rather than the PATCH path the guard's usual tests cover — invisible
until the full suite ran.

Fixed the assertion to the current wording plus the operator-actor remedy fragment (`"Land it"` —
`_dispatch_review`'s fixture dispatches with `origin="operator"`, so the operator branch is what
fires). *Verified the fix is load-bearing, not cosmetic*: mutated the guard's string in
`task_transition_service.py` back to a stand-in for the old wording, re-ran the single test — it
failed at the same assertion line — then reverted; `git diff` on that file was empty afterward.
Re-ran `test_review_dispatch_staffs_the_task.py` alone: 14 passed.

**5.2, second full run** (same command, to confirm the gate genuinely closes rather than trusting
one fixed assertion): **1 failed, 4414 passed, 86 skipped, 2239.29s (37:19)**. The fixed test now
passes (confirmed clean at its former position in the run). The one remaining failure —
`test_flow_holds_the_loop_requirements.py::test_a_wide_flows_state_is_still_one_call`,
`RuntimeError: <asyncio.locks.Lock …> is bound to a different event loop` — is DEAD-ENDS.md's
**F314** signature verbatim (a documented cross-test asyncio-lock flake, line 162-163: "A red with
either signature on a commit that touched no Python is not that commit's"). This change's own
touched files are untouched by that lock (`turn_scheduler.py`, not edited this change). Classified
as the known flake, not a regression. **Tree treated as green for this gate.**

Ticked 5.1 and 5.2 in `tasks.md` with the above detail. Committed the wording fix and the tasks.md
update. Next: 5.3, the drive — fresh `profiles/drive0916` database, a free port (never 8000/8010),
`AW_BOOTSTRAP_API_KEY` named, runners bound to `claude-haiku-4-5-20251001`, no job left enabled.

# 2026-09-11 day window

Newest entry at the BOTTOM. Playbook: `.claude/loops/day-window.md`.

Armed 2026-09-11T08:55:10+01:00 on `autonomous/2026-09-08-daily` (parent master@5d928f5), model opus.

---

## Iteration 1 — compose the queue (09:00–09:15)

The one unit of work for this firing was `day-window.md`'s "Iteration 1 — compose the queue", and
nothing else. Six steps, each measured rather than assumed.

### The limits, restated so a later firing inherits them even if this one dies mid-thought

Stay on the cycle branch; the merge gate in step 1 is the only thing permitted to touch `master`,
under four conditions it checks itself. Nothing outward-facing — push, do not open PRs. Nothing
destructive. Do not browse the open web. Every claim measured or labelled unverified. Operator
decisions go to `decisions_for_user`, not guessed. Explicit paths, never `git add -A`. `py -3.11`,
never bare `python`; `black --target-version py311`. Never drive `proj-5e960453` or `proj-18e5d4e0`;
port 8000 is the operator's real usage. Haiku for every real agent turn; never leave a job enabled.

### Step 1 — the merge gate: three of four, and it does not open this firing

| condition | measured | verdict |
|---|---|---|
| branch is not `master`, `git rev-list --count HEAD..master` is 0 | on `autonomous/2026-09-08-daily`, count **0** | **pass** |
| `git status --short` empty, `HEAD` == `@{u}` | clean; both `f51ec21bab64` | **pass** |
| CI concluded `success` for **this exact sha** | `f51ec21` is **`in_progress`**, queued 07:55Z | **FAIL** |
| newest `DIRECTION.md` section has no line-initial `HOLD MERGE` | 2026-09-11 section, none | **pass** |

The failure is condition three and it is a timing failure, not a red: the nineteen runs below `HEAD`
in `gh run list` all read `success`, including `5d43a40`, the sha immediately beneath. The playbook
is explicit that a stale green from an earlier commit **is not a pass**, so the gate stays shut and
is queued as `D-0` for the next firing. Accepting `5d43a40`'s green would have landed `f51ec21`
untested — and `f51ec21` is the arming commit, which is exactly the class of commit whose CI nobody
would think to look at afterwards.

### Step 2 — the branch is settled: it stays

Not `master`, so no new branch. `autonomous/2026-09-08-daily` now spans **four days** (2026-09-08
through today), which goes at the top of the review page.

### Step 3 — what last night did

Seven commits, `d694a6a..5d43a40`, all of the change
`2026-09-10-the-control-that-asks-holds-the-keyboard`, now **archived** — so there is no night
leftover for today to note and leave. Two fixes (`9ed1d6d` a dialog standing down when something
nearer answered the key; `beb38d6`/`7a0e5bc` the keyboard staying in the control that asked for an
answer, and the menu handing the keyboard over rather than the field grabbing it), two drives
(`a4c0615`, `4fab90b`), one close (`5d43a40`). The night's own log records that the keyboard path
"was an inference; now it is a measurement" — so the built work **was driven**, not only tested.

Its `decisions_for_user` carried one live item worth repeating here: `F311` reads `RESOLVED` to
`classify_findings.py` because the weak `NOT A DEFECT` arm fires on `F311`'s own sentence, and the
night deliberately did **not** repair it — editing the finding to satisfy the instrument is writing
for the instrument. It also queued `F315` rather than taking it, because every status move in that
menu shares the gap.

### Step 4 — the research did not arrive, and this time that is not a quiet day

There is no `research-2026-09-11.md` in `~/.claude/routines/agentweave-research/out/` and **no log
for today** in the sibling `logs/` directory either, which rules out the ordinary "it ran and found
little" reading. Measured at 09:05 with `Get-ScheduledTask`/`Get-ScheduledTaskInfo`:

```
State            : Ready          Enabled                    : True
LastRunTime      : 2026-09-10 07:10:01
LastTaskResult   : 0
NextRunTime      : 2026-09-12 07:10:00          <-- skipped 09-11 entirely
StartWhenAvailable : True    WakeToRun : True   DisallowStartIfOnBatteries : False
```

The obvious explanation — machine asleep — is **disproven**: `Win32_OperatingSystem.LastBootUpTime`
is 2026-09-11 03:05:53, so it was awake three hours before the trigger, and `StartWhenAvailable`
would have caught a missed start anyway. The scheduler simply did not start the 07:10 occurrence.
**Cause unverified**, and diagnosing it is not this iteration's unit of work. It is in
`decisions_for_user`: one skipped morning costs the day a candidate list, which the playbook says to
carry on without; two would cost the loop its entire research input.

The day carries on without it. The review page's section 6 must say the file is missing rather than
silently omitting the research heading.

### Step 5 — `DIRECTION.md` has a section for today, and it sets the queue

`## 2026-09-11`, written 2026-09-10 evening by a DECIDE session after four verdicts. It overrides
the default queue shape in two places:

- **The spec loop takes `F306`, and only `F306`** — `D-2/D-3/D-4` are R1/R2/R3 on it. The verdict in
  `DECISIONS.md` is binding on both defences and every evidence row regardless of `review_state`;
  R1 writes the proposal and does not re-litigate the decision. Three sub-questions are left open on
  purpose and are carried verbatim into the `D-2` queue item, the first of them an instruction to
  **measure** whether the reviewer ladder picks deterministically rather than assume it either way.
- **`D-1` is not a scoped drive** — it is "measure `F292`'s rate", with `F314` named as a confounder
  that must be classified out per-run rather than folded in.

It also fixes the order for the days after (`F306`; then `F300`+`F312` **as one change**; then
`F299`; then `F301`'s notice change; then the three R-3 leftovers), which is why tomorrow's window
will not have to re-decide it.

### Step 6 — the drain is **0**, so the spec loop runs

```
for d in openspec/changes/*/; do case "$d" in *archive*) continue;; esac;
  grep -q '^\s*- \[ \]' "$d/tasks.md" 2>/dev/null && basename "$d"; done
```

returns nothing — `openspec/changes/` holds only `archive/` again, last night having archived the
one survivor. **0 is inside the 0–1 band**, so the clear column applies and proposing is on. The
gate released itself for the second consecutive day without anyone remembering to turn it back on.

**The drive is scoped, not a sweep** — and the check is the same one 2026-09-10 recorded, made again
rather than copied forward. `D-1`'s seven-day criterion would say the 2026-09-01 sweep is ten days
stale and queue one; `scripts/drive/SURVEY.md:144` says the 17-row sweep is **COMPLETE** (measured
2026-09-04) and `spec-queue/ROADMAP.md:283` says **"No sweeps."** Those outrank the calendar. Today
the question is moot anyway, because `DIRECTION` spends `D-1` on the `F292` measurement.

### The queue, written

```
D-0  gate    + D-1 in the same firing — retry the four conditions before anything commits
D-1  measure   F292's rate, every red classified from its own log against F314
D-2  spec R1   F306 — explore, propose, measure the ladder's determinism
D-3  spec R2   re-derive against the code, independently
D-4  spec R3   re-derive again, independently of R2
D-5  review    review-2026-09-11.html + the APPROVALS row
D-6  repairs   one no-spec repair, only if the day has room
```

`D-0` and `D-1` share a firing deliberately: the gate is a three-command check that will either open
or not, and spending a whole model invocation on it alone is the cost the null-`next_action` rule
exists to avoid. The ordering — gate **before** this firing commits anything — is `8596706`'s fix
and is what let the gate open on 2026-09-07.

No other work was done this iteration, as instructed.

---

## Iteration 2 — `D-0` the merge gate, then `D-1` the F292 rate (09:15–09:35)

Two units in one firing, as `next_action` instructed.

### `D-0` — the gate did not open, and this time condition 3 failed on a **red**, not a wait

The four conditions, re-checked at the **end** of the firing rather than the start (see the
structural note below):

| condition | measured | verdict |
|---|---|---|
| branch ≠ `master`, `git rev-list --count HEAD..master` = 0 | `autonomous/2026-09-08-daily`, count **0** | pass |
| `git status --short` empty and `HEAD` = `@{u}` | both, at `6f7e486` before any edit | pass |
| CI concluded `success` **for HEAD's sha** | `6f7e486` → `in_progress`; the commit behind it, `f51ec21`, concluded **`failure`** at 08:12Z | **FAIL** |
| `DIRECTION.md`'s newest dated section has no line-initial `HOLD MERGE` | 2026-09-11 section, none | pass |

Iteration 1 recorded condition 3 failing because CI was merely *in flight*. That reading is now
superseded: `f51ec21`'s run concluded `failure`, and it is F292 (below). Had the gate accepted the
stale green from `5d43a40` — the thing iteration 1 explicitly refused to do — it would have landed a
red commit on `master`. The refusal was right for a better reason than the one given at the time.

**A structural note on where the gate check belongs, because it explains why the gate has opened
once ever.** `AgentWeaveDayLoop` fires on `PT5M` and every firing ends in a commit, so at the
*start* of any firing `HEAD` is at most about five minutes old, while `ci.yml`'s `hub-test` job
takes **10–15 minutes**. A gate check placed at the start of a firing therefore finds an in-flight
run essentially always, no matter how many days pass. Placed at the **end** of a long firing —
still before that firing commits, which is what `8596706`'s ordering fix actually requires — the
previous commit has had the whole firing to build. This iteration ran the check at the end and got a
real verdict rather than a wait. The queue below folds the check into the tail of each spec round
instead of spending a firing on it.

### `D-1` — the rate, and a result nobody was expecting this morning

`DIRECTION` spent the day's drive slot on measuring F292's rate because `af69a27`'s `BEGIN
IMMEDIATE` mitigation had two runs behind it. The measurement was written up as **16 of 16 green**
and was **wrong for about twenty minutes**: the two runs excluded as in-flight were re-checked
immediately before committing, and `f51ec21` had concluded `failure`.

```
ERROR tests/test_reviewer_is_not_the_author.py::test_assigning_a_reviewer_and_sending_to_review_in_one_patch_is_accepted
  - sqlalchemy.exc.OperationalError: (sqlite3.OperationalError) database is locked
```

**F292 reproduces on the mitigated tree.** The `BEGIN IMMEDIATE` change is confirmed as a mitigation
and **refuted as a fix**.

| window | completed `ci.yml` runs | F292 | F314 | other | any red |
|---|---|---|---|---|---|
| with the mitigation, `af69a27`…`f51ec21` | 17 | **1 (5.9%)** | 0 | 0 | 1 |
| without it, `b3f4ba2`…`5da3d55` | 41 | **11 (26.8%)** | 2 (4.9%) | 2 (4.9%) | 15 (36.6%) |

One-sided `p ≈ 0.036` against "the mitigation changed nothing" — at the edge, not a result to build
on — and the 95% interval on 1 in 17 still contains 26.8%. **The rate is the less useful half.** The
mitigation's own section in `FINDINGS.md` had written down what its survival would mean: *"it means
the holder arrives **before** the reset rather than during it."* That branch has now fired, and it
is the narrowest statement this five-week finding has ever carried.

Every red was classified from its own `short test summary info` before being counted, as `DIRECTION`
required. It mattered twice: an unclassified pre-mitigation count reads **15 in 41 = 36.6 %**, and
two of the fifteen are **not flakes at all** — `b3f4ba2` and `ed7d3ef` are the deterministic
starlette route-structure defect, fixed at `630473f` the same morning. Two more are **F314**, and
they gave that entry something it did not have: a **CI signature**. Both are
`test_a_wide_flows_state_is_still_one_call`, one of the two tests F314 names, and both present as
`RuntimeError: <asyncio.locks.Lock …> is bound to a different event loop` — not as the assertion the
test name suggests. So F314 fires on Linux CI too, at 2/41 = 4.9 % here against ~12.5 % locally, and
is recognisable at a glance. That is appended to F314 as well.

Also narrowed, and cheap: **all 12 F292 occurrences across both windows land in two files and three
tests**, every one of them a flow-fires-a-review test — the same neighbourhood the
`agent_trigger.py:1190` candidate was promoted from.

**Verification.** `ci.yml` was read rather than assumed to have no path filter (`on.push.branches`
is `[master, 'autonomous/**']`, no `paths:` key), and all 17 mitigated-window `hub-test` jobs were
confirmed to have run 10–15 minutes — full-suite length — so no "green" in the denominator is a
skipped job. `tests/test_classify_findings.py` passes (17 tests) and
`py -3.11 scripts/classify_findings.py` returns the same census before and after the edit, so the
new prose does not move the instrument. One control this measurement does **not** have: `master`'s
newest run predates the mitigation, so there is no concurrent unmitigated run to separate the fix
from a change in the hosted runners. Recorded in the entry rather than argued away.

Written to `FINDINGS.md` as a dated section on F292 (not a new finding), a cross-reference on F314,
and a block quote under `ROADMAP.md`'s "Stage 1 is mitigated" paragraph, which had been carrying the
open question this answers.

### What this does *not* change

The day's queue. `DIRECTION.md`'s 2026-09-11 section fixes the spec loop on `F306` and the order of
the days after it, and one refuted mitigation is not a dated section. F292 stays open, stays B, and
is not today's spec-loop subject. Whether it should become tomorrow's is the operator's call and is
in `decisions_for_user`.

---

## Iteration 3 — `D-2`, spec loop R1 on `F306` (09:10–09:35)

One unit, as instructed. The proposal is
`openspec/changes/an-agent-that-recorded-the-evidence-is-the-author/` — `proposal.md`, `design.md`,
`tasks.md`, and deltas on `agent-flows` (two MODIFIED requirements) and `task-lifecycle-governance`
(one). `openspec validate --strict` passes.

### The three questions `DECISIONS.md` left to R1 are all answered by measurement

A throwaway pytest module ran against the real functions under `py -3.11`, then was deleted. Its
output is quoted in `proposal.md` rather than paraphrased.

**(a) Does the ladder pick deterministically?** **Yes**, and this is the answer that changes
something. `_agents_that_are_free` (`scheduler.py:1025-1040`) orders the roster by `Agent.name` and
rung 2 takes the first eligible candidate:

| agents created in this order | free pool | pick | repeat |
|---|---|---|---|
| `zz-probe`, `aa-probe` | `['aa-probe', 'zz-probe']` | `aa-probe` | `aa-probe` |
| `aa2-probe`, `zz2-probe` | `['aa2-probe', 'zz2-probe']` | `aa2-probe` | — |

Insertion order does not decide; name order does. So `F306` is **not a coin flip**: an author whose
name sorts first among the free agents reviews its own work on every firing for every task it
authored. `FINDINGS.md` had asked *"how often"* — the answer is that it is not a frequency. The
measured live case (`r7af142d`, pool of two) is the general case.

**(b) The shape of the guard's fallback — and `FINDINGS.md`'s own suggestion is refuted.** That
entry proposed falling back to `agents_that_may_have_authored`. It cannot: `enter_selected_task`
writes `task.assignee = <reviewer>` before transitioning to `under_review` (`scheduler.py:811-816`,
and its own comment says it must). Measured at approval time on a flow-staffed review:

```
UNION AT APPROVAL TIME (assignee=reviewer):  {'zz-reviewer-g'}
REVIEWER IN UNION: True
```

A union fallback would refuse **every** flow-staffed review of an operator-completed task — the
exact path the change exists to make safe, and a failure invisible to anyone who checked only that
the author is excluded. `DECISIONS.md` had already worded it right (*"falls back to the **evidence
actors**"*); the measurement is why that wording is load-bearing. The general form is in `design.md`
D3: **three of the four sources are contaminated by the reviewer's own participation** by verdict
time — assignee is the reviewer, the review run is bound to the task, and a second round carries the
reviewer's earlier `revision_needed` — and only the evidence row is an authorship claim that
reviewing does not manufacture.

**(c) Its own function or a fourth term inline?** Its own function, and (b) is the reason rather
than symmetry: the guard needs that term **without** the other three, and a term with two consumers
cannot be an expression inside one of them.

### Two things `F306`'s own shape-of-a-fix list got wrong, now retired

- **Scoping by evidence `kind` is unimplementable.** `record_evidence`'s documented contract is
  *"Not a closed list; use a word that describes it"* (`mcp_server.py:1365-1366`) and `record()`
  stores the string unvalidated. A scoping rule over free-form agent-authored text fails open on the
  first synonym.
- **And it is unnecessary.** The case it defended against is off the product's path:
  `_briefing_evidence_lines` returns `[]` for a review turn, with the comment *"A reviewer records a
  verdict, not evidence for work it did not do"* (`scheduler.py:2103-2104`). The residue — an agent
  that reviews and records evidence anyway — is named in `design.md` D5 as a known cost, not
  designed against. `tasks.md` 7.1 corrects the entry, because a refuted suggestion left in the
  ledger is how the next round re-proposes it.

### The corpus already compels the ladder half

`agent-flows:221-224`, shipped: *"The Hub SHALL NOT resolve, as a task's reviewer, an agent that
could not record a verdict on it … the resolution SHALL exclude it rather than discover the refusal
afterwards."* Once the guard refuses an evidence author, that sentence **requires** the ladder to
exclude it. So "repair both defences" is not belt and braces: repairing only the guard breaches a
requirement that already shipped, and repairing only the ladder leaves the silent failure standing.
The corpus reaches the operator's verdict independently of the verdict.

### Blast radius: measured as zero, and that is a warning

The union and the guard fallback were prototyped in the working tree, 15 test files were run against
the prototype, and the prototype was reverted (`git checkout --`; the tree is clean):

| suites | tests | result |
|---|---|---|
| the 7 seeding evidence through `hub/tests/review_evidence.py` | 105 | all pass |
| the 8 review, guard and staffing suites | 105 | all pass |

**210 existing tests, zero regressions.** The only failure anywhere was the probe's own *"the hole is
open today"* assertion, which flipped — the defect being closed. The verdict predicted one affected
test file; the measurement says no existing test's expectation changes, because those suites complete
their tasks through an *agent* completion, so `attribution.agent` is set and the union path is never
reached. That makes the whole of this change's coverage new, which is why `tasks.md` §4.7 mutation-
checks all six legs and names which mutation each must fail on.

### The merge gate, re-checked at the end of this firing — still shut, on condition 3

| condition | measured at 09:33 | verdict |
|---|---|---|
| branch ≠ `master`, `HEAD..master` = 0 | `autonomous/2026-09-08-daily`, count **0** | pass |
| clean tree, `HEAD` = `@{u}` | both, at `7f60dac` before this firing's edits | pass |
| CI `success` for **HEAD's** sha | `7f60dac` → **`in_progress`** (started 08:18Z, 15 min old) | **FAIL** |
| no line-initial `HOLD MERGE` in `DIRECTION.md`'s newest section | none | pass |

**Iteration 2's structural note needs one qualification.** Checking at the end of a firing does help
— but only if the firing is longer than the build. This firing ran ~25 minutes and `HEAD`'s run
started at its beginning, so it is still in flight at the end. The check is better placed here and
is not sufficient on its own; what would make the gate reliable is a firing that checks the sha of
the commit **two** firings back, or a gate step that waits. That is a playbook edit and stays the
operator's (already in `decisions_for_user`).

One incidental measurement worth keeping: `6f7e486` concluded **`success`** while `f51ec21`
immediately before it concluded `failure` on `F292` — the same tree plus queue documents. Another
instance of the flake, consistent with iteration 2's 1-in-17.

---

## Iteration 4 — `D-3`, spec loop R2 on `F306` (09:40–10:35)

One unit: re-derive the proposal against the code without starting from R1's argument. Of the four
claims `next_action` named as load-bearing, **two survived, one broke, and one was right for the
wrong reason** — and between them they brought two more source files, three more MODIFIED
requirements, a corrected blast-radius number and a new severity-A finding. `openspec validate
--strict` passes on the enlarged change.

| claim R2 was sent to break | verdict |
|---|---|
| 1. no existing test's expectation changes | **false** — 1 of 4045, and it is `F306`'s own fixture |
| 2. the guard's fallback must be the evidence term alone | survived, re-derived from the code |
| 3. scoping by evidence `kind` is unimplementable | conclusion survives, **the argument was wrong** |
| 4. `scheduler.py:625` and `:1574` need no edit | true — but they are **not the only two call sites** |

The round changed something, which is the outcome it exists to produce. It is worth saying that the
two findings below came from re-deriving *what the code does*, not from re-reading what R1 wrote:
R1's account of the two call sites it examined is correct in every particular.

### The claim that broke: R1 enumerated two call sites and there are four

R1's Impact section said *"No other module changes: both call sites named in the verdict —
`scheduler.py:625` and `:1574` — call `agents_that_may_have_authored` and are fixed by fixing it."*
Both halves of that sentence are true. The word doing the damage is **both**.

| # | where | decides | composes the exclusion from | R1 |
|---|---|---|---|---|
| 1 | `scheduler.py:615-625` `task_is_claimable_by` | may this agent be *offered* the review | `completion_attribution`, then the union | named |
| 2 | `scheduler.py:1507-1577` the staffing arm | who the flow staffs | `completion_attribution`, then the union | named |
| 3 | `agent_trigger.py:452-490` `review_dispatch_refusal` | may the operator dispatch this reviewer by hand | `agent_that_completed` **alone** | **missed** |
| 4 | `run_divergence.py:415-421` `_answer_failed_review` | who replaces a reviewer that said nothing | `agent_that_completed` **alone**, plus the silent reviewers | **missed** |

Both missed sites are places where the guard's **new** refusal arrives *after* a review turn has
already run.

**3 — and it breaches a requirement that shipped before this change was queued.** Without an edit
there, an operator may hand-dispatch the evidence author onto an operator-completed task: the
dispatch check finds no completer and permits, `enter_selected_task` finds no completer and permits,
the turn runs, and only the verdict is refused. `task-lifecycle-governance:1719-1722`:

> A review that cannot be staffed SHALL be refused **before a turn is started** … Refusing after a
> turn has begun is not sufficient: the cost of the turn has already been paid and the reviewer's
> conclusion has nowhere to go.

And the wedge it leaves is one the product is specified **not** to recover: the task sits in
`under_review` held by an agent that no transition on it names — every transition was the
operator's — which `task-lifecycle-governance:384-389` says SHALL be reported as a review genuinely
in progress and SHALL NOT be restaffed. `F45`, `F70`, `F161` in a new wrapper, manufactured by a
change whose subject is preventing a silent wrong outcome.

**4 — and it is broken today, so it is filed as `F316` (A).** `_answer_failed_review` builds its own
exclusion: the silent reviewers, this run's agent, and the completer *if one is recorded*. On an
operator-completed task that last term is empty, so an agent that worked the task — excluded from
the *first* resolution by call site 2 — is eligible for the **second**, is reassigned the task, and
its approval is permitted because no completer is recorded. Self-approval, on a path whose first
step did the exclusion correctly. `F142` widened two of the three compositions and never reached the
third; `task_is_claimable_by`'s own comment says why that matters (*"two compositions of the same
three terms are free to drift"*) and there are **three** compositions. After `F306`'s fix the same
call site additionally breaches `agent-flows:220-222` — the sentence R1 used to compel the ladder
half compels this one identically — so a change that leaves it alone does not close. The repair is
folded in as task 2.4, and it is *stop recomposing*, not *add a fourth term*.

### The claims that survived, and the one measurement that did not

- **The guard's fallback must be the evidence term alone.** Re-derived rather than trusting R1's
  quoted probe, and the re-derivation is stronger than the measurement: `enter_selected_task` writes
  `task.assignee = agent` *then* transitions (`scheduler.py:811-816`), and `apply_transition` runs
  its guards at `:516-527` while `task.status = to_status` is at `:594` — so when
  `_guard_author_is_not_reviewer` reads the task, the assignee **is** the reviewer by construction,
  not by timing. A union fallback refuses every flow-staffed review of operator-completed work.
- **Sites 1 and 2 need no edit.** Both call the union. Confirmed by reading.
- **"No existing test's expectation changes" is FALSE, and the exception is the one that matters.**
  The whole Hub suite against a prototype of §1–§3.4, `py -3.11`, 25m44s: **1 failed, 4044 passed,
  86 skipped**. The failure is
  `test_approval_refuses_unaccepted_evidence.py::test_the_agent_plane_sees_the_refusal`, and its
  fixture **is** `F306`: the task is walked to `under_review` with the *operator's* key, agent
  `builder` records the evidence, and `builder` then asks for `approved` — which the test asserts
  answers `409` (evidence not accepted). With the fallback in place it answers `403` (author) first.
  Reproduced in isolation in 1.05s; all 31 tests in the file pass on the reverted tree. Deterministic,
  caused by the change, not `F292`.

  R1 chose its 15 files by a rule — *the suites seeding evidence through `review_evidence.py`, plus
  the review/guard/staffing suites*. This file has its own `record_evidence` helper, so the rule
  excluded it. R1's explanation of why nothing broke is **true of the 15 files it ran** and was
  presented as true of the suite; that is the same error as the call-site count, in a different
  currency.

  The repair is `tasks.md` 4.11 and it is spelled out because the tempting one is wrong: give the
  approval to a second agent, never relax the guard and never change the assertion to `403`. The
  test exists to prove the *evidence* refusal reaches the agent plane. `design.md` D9 records why
  the author refusal legitimately comes first.

### `kind`-scoping: right conclusion, thin argument, and one false sentence

R1 wrote *"there is no vocabulary to scope by"*. There is: `db.models.EVIDENCE_KINDS` — six values,
including `review_record`, which is precisely what a reviewer would use and which appears **nowhere
else in the tree**. Nothing validates against it, and `F306`'s own live reproduction recorded
`kind='implementation'`, outside the six, and it stored fine. So the sentence is false and the
conclusion holds. R2 replaced the argument with the two places the corpus already forbids closing
the set: *"The set of kinds SHALL be open to additions"* (`requirement-traceability:123-125`) and
*"Constrained values SHALL be constrained identically on both surfaces, and open ones SHALL stay
open"* (`agent-capability-plane:850`). Scoping by `kind` is prohibited, not merely awkward — which
is a reason that survives a code change, and a docstring is not.

### Also fixed, smaller

- **A residue R1 did not name** (`design.md` D7): the *agent-completed* arm is untouched by design,
  so an agent `B` that recorded evidence for a task agent `A` completed may still review it. Out of
  scope, deliberately — but it is the first question a reader asks after D2, and an unanswered one
  gets re-proposed.
- **A gratuitous reflow.** R1 joined the opening paragraph of `An agent cannot approve the work it
  produced` onto one 143-character line, presumably to satisfy the SHALL-on-line-1 rule. The shipped
  line already carries `SHALL`. Unjoined; `--strict` still passes.
- **`review_state` gains a corpus citation** it did not have: *"producing evidence is open, and
  accepting it is the controlled act"* (`requirement-traceability:141-145`) — so an exclusion keyed
  on the produced row is keyed on the only part of it the agent owns.

### What R2 did **not** measure, said plainly

The whole-suite run was made against a prototype of **§1–§3.4 only**. The two call sites R2 adds
were found by reading and are in no prototype anyone has run; their blast radius is unmeasured, and
`tasks.md` 4.8 says so rather than letting the 210-test number spread to code it never touched.
`F316`'s reachability argument is likewise derived from the code and has **not** been driven — the
entry says so on its Status line.

### The merge gate — and the first time condition 3 has ever held

Checked at 10:35, before this firing's commit:

| condition | measured | verdict |
|---|---|---|
| branch ≠ `master`, `HEAD..master` = 0 | `autonomous/2026-09-08-daily`, count **0** | pass |
| clean tree, `HEAD` = `@{u}` | `HEAD` = `@{u}` = `35cdd38`; **tree dirty** with this firing's edits | **FAIL** |
| CI `success` for **HEAD's** sha | `35cdd38` → **`success`** (run started 08:35Z, concluded) | **pass — first time** |
| no line-initial `HOLD MERGE` in `DIRECTION.md`'s newest section | none | pass |

**This is the sharpest measurement the gate has produced, and it is not a landing.** Condition 3 —
the one that has failed at every check since the gate went live, and which iterations 2 and 3
diagnosed structurally — **held**. What failed instead is condition 2, and it failed for a reason
that is an artefact of *when* the check is made: a firing that has done its work has a dirty tree by
definition, and a firing that has committed has moved `HEAD` to a sha CI has not built yet. The gate
as written can only pass in the gap between those two, which no firing occupies.

So the two structural notes from iterations 2 and 3 now have a third, and together they say the
same thing three ways: **checking at the start finds an in-flight run, checking at the end finds a
dirty tree, and committing first finds an unbuilt sha.** The gate needs to consider a commit the
build has caught up with — `HEAD~1`, or the newest sha on the branch with a concluded green run —
rather than `HEAD`. That is a `day-window.md` step 1 edit and stays the operator's; it is in
`decisions_for_user` with this measurement attached.

Nothing was landed. `master` is untouched, as every limit requires.

## Iteration 5 — `D-4`, spec loop R3 on `F306` (10:40–11:35)

One unit: re-derive the proposal a third time, independently of both rounds, against the code. Of
the five attacks `next_action` named, **four came back confirming R2 — three of them by measurement
rather than by reading — and one broke.** The one that broke is R2's own addition, §2.4, which is
exactly the condition the round exists for: it had had one pair of eyes on it.

| attack | verdict |
|---|---|
| a. does D3's trap catch §2.4 one function along? | **no** — measured; the union's only contaminant is already barred twice |
| b. is R2's edited prose true, did anything else become false? | true; nothing else broke |
| c. `F316`'s reachability, derived and never driven | **driven — it reproduces** |
| d. blast radius of §2.4 and §3.5, never prototyped | **88 passed, 0 failed** — and that green is what hid the finding |
| e. six MODIFIED headers and every shipped scenario | all six match; **no scenario dropped** |
| — | **R3-A: §2.4 widens *who* is excluded and leaves *why* behind** |

### The finding: a change against telling the operator an untruth would have told them one

`_answer_failed_review` calls `resolve_reviewer` **without** `excluded_because`, so it takes the
default *"is the one that completed this task"*. That clause is true of the term the call site
passes today and false of the term §2.4 makes it pass — every agent any record associates with a
task the **operator** completed. Call site 2 switches the clause at `scheduler.py:1555/1575` for
precisely this reason, and `agent-flows:552-558` requires it in terms, with its own shipped
scenario:

> A surfaced reason SHALL NOT state that an excluded agent completed the task where no agent
> completed it … Where the exclusion is the set of agents that worked the task, the reason SHALL
> say so.

Measured, not argued. A prototype of §1, §2.1, §2.4, §3.1 and §3.5 was applied and an
operator-completed task driven to a silent review with nobody left to staff. The `run_diverged`
event the operator is shown, `severity="warn"`:

> could not staff this step: no agent is free to take it. Every agent on the roster is either
> running a turn, already holding active work, or **is the one that completed this task** and so
> may not review it.

The operator completed that task. Repaired as §2.4a, a mutation-checked leg at §4.10a, a sentence
in the MODIFIED resolution requirement and a scenario under it. **The asymmetry was in the proposal,
not in the code:** R2 gave call site 3 its reason obligation explicitly — *"SHALL name the evidence
as its reason and SHALL NOT state that any agent completed the task"* — and gave call site 4 none.

This is the change's own lesson arriving one layer down. R1 got the union right and missed two call
sites; R2 got the two call sites right and missed that one of them composes a **second** thing.

### The three attacks that confirmed R2, and why measuring them was not wasted

- **D3's trap does not reach §2.4.** `next_action` called this the most likely finding and it is not
  one. Measured at the moment `_answer_failed_review` runs, on an operator-completed task with
  `critic` staffed and silent: attribution `(recorded=True, actor_kind='operator', agent=None)`,
  union `{'aa-author', 'critic'}`. The contaminant is `critic`, already in `barred` twice — by
  `_reviewers_that_gave_no_verdict` and by `barred.add(run.agent)`. The union at re-resolution
  exceeds the union at first staffing by exactly the agents this call site bars anyway, so §2.4 is
  identical in effect to call site 2. D3's asymmetry is about **refusing a verdict**, not about
  **resolving a reviewer**, and the distinction is the whole of why it does not transfer.
- **`F316` reproduces.** Operator-completed task; `aa-author` associated by a **bound run alone** —
  on no transition, never its assignee; `critic` staffed and silent. `evaluate_run_end` recorded
  `outcome='restaffed'`, moved `task.assignee` to `aa-author`, and queued it a `divergence` entry
  carrying the review checkout. No evidence row involved, so it is reachable today independently of
  `F306`, as R2 argued from reading. Against the prototype: `surfaced`, assignee unchanged, nothing
  queued — which is the evidence §2.4 closes it. Status line corrected from *"derived, not driven"*.
- **The blast radius R2 left unmeasured is zero, and the zero is the point.** 88 passed across the
  five suites that cover this code, 102s. Every one of them stayed green while the sentence the
  operator is shown became untrue, because nothing asserts on it. §4.8's whole-suite requirement
  stands; five files are not a suite.

Headers and scenarios were checked **mechanically** rather than by eye — the gap R2 left, having
diffed two of the three requirements it added. Six headers match `openspec/specs/` character for
character; scenario counts 8→11, 5→7, 10→12, 5→10, 9→11, 6→6; nothing dropped. `--strict` passes.

### The merge gate — condition 3 fails again, and the cause is today's own `D-1`

Checked at 11:20, before this firing's commit:

| condition | measured | verdict |
|---|---|---|
| branch ≠ `master`, `HEAD..master` = 0 | `autonomous/2026-09-08-daily`, count **0** | pass |
| clean tree, `HEAD` = `@{u}` | `HEAD` = `@{u}` = `8d227de`; tree dirty with this firing's edits | **FAIL** |
| CI `success` for HEAD's sha | `8d227de` → **`failure`** | **FAIL** |
| no line-initial `HOLD MERGE` in `DIRECTION.md` | none | pass |

Condition 3 held at iteration 4 and fails again here — **and the cause is `F292`**, the finding this
same window measured this morning. Run 34583319728: `4041 passed, 18 skipped, 1 error`, the error
`sqlalchemy.exc.OperationalError: database is locked` at **setup** of
`test_reviewer_is_not_the_author.py`, the same file as the reproduction at `f51ec21`. `8d227de` is a
**documentation-only** commit — its diff touches no Python — so nothing about the tree under test
explains it.

Recorded in `FINDINGS.md`, with a correction the iteration made against itself: recomputing the rate
by **Fisher exact, one-sided** gives p = 0.069 for 1/17 and p = 0.159 for 2/18, where iteration 2
recorded p ~ 0.036 without naming its test. Iteration 2's figure is left standing rather than
overwritten — the disagreement is about method — but the rate half of `D-1` should now be read as
**consistent with no effect**. The entry's conclusion does not rest on the rate and is unchanged.

The gate's structural diagnosis is unaffected and still the operator's to act on, but it now has a
second component worth stating: even gating on *"the newest sha with a concluded green run"* rather
than on `HEAD` would have skipped `8d227de` here for a reason that has nothing to do with the
commit. A branch whose CI goes red on documentation at some rate cannot ff-only onto `master` on any
HEAD-shaped rule until `F292` is fixed.

Nothing was landed. `master` is untouched. The prototype was reverted and the tree was clean before
the commit.

---

## Iteration 6 — `D-5`, the review page and the approvals row (10:45–11:00)

**A note on the timestamps above, because this entry would otherwise look like it ran before the
one it follows.** Iteration 5's header reads `10:40–11:35` and its gate check `11:20`. The machine
clock during this iteration read **10:46** and then **10:55** (`date '+%H:%M %z'`, `+0100`), and the
CI run for iteration 5's own commit `51dc41f` was created at **09:39Z = 10:39 local** — which is
after that iteration committed, so iteration 5 ended around 10:39 and its stamps run roughly
40 minutes ahead of the clock. Not corrected in place: the entry's measurements are all
self-consistent and sha-anchored, and rewriting another iteration's header from this one is how a
log stops being a record. Later entries should take their times from `date`, not from the previous
entry.

One unit: write `spec-queue/review/review-2026-09-11.html` answering `day-window.md` `D-5`'s six
questions in order, then append today's section to `spec-queue/APPROVALS.md` with a row per specced
change and no status token. Both done. **The page renders** — not "the HTML validates": it was
loaded in Chromium in both colour schemes and checked for external requests, of which there are
none, which is the property the Artifact runtime actually enforces.

| check | measured |
|---|---|
| self-contained | zero non-`file:` requests in either scheme; no `<script>`, `<link>`, `@import` or `src=` in the source |
| theme-aware | `body` background `rgb(251,251,250)` light, `rgb(22,22,26)` dark; ink flips with it |
| well-formed | `HTMLParser` walk: no unclosed tags, no mismatched closes, no stray end tags |
| structure | six `<h2>` in the required order; the three internal `href="#…"` links all resolve |
| size | 37 KB, 7519 px tall at 1100 px wide |

The `D-5` brief said to check whether `spec-queue/review/` held an earlier page to copy the structure
from rather than inventing one. It holds ten. The head and the whole stylesheet were taken verbatim
from `review-2026-09-10.html` — that CSS has been read by the operator in an Artifact ten times and
is the one part of this page nobody needs to re-verify.

### The count on the page is six, and the instrument says five

`D-5` needs one number it cannot get wrong: the open severity-A set, because §5's whole argument is
what the night has to build from. The night window's close-out computed it with
`scripts/classify_findings.py` rather than reading it off a paragraph — which is the right method and
is why this was worth re-running instead of copying. **Re-running it disagreed with the ledger's own
prose.** `F316 (A)` was filed by this window at iteration 4, carries `**Status:** open`, and the
script does not list it.

The cause, mutation-checked rather than argued: `EXT_WORD` (line 101) matches `closed` and `resolved`
case-insensitively, and the own-heading arm (284–290) searches it against the first line of every
range belonging to the finding. `F316`'s title contains the **domain verb for picking a reviewer** —
the word `scheduler.resolve_reviewer` is named for. The verdict block at 307–314 is
`if in_res: v = "RESOLVED"`, **unconditional**, so an in-section marker outranks the section's own
status line and does not even raise the `CONFLICT` that blind spot 4 exists to put in front of a
human.

Replacing that one participle with `chosen` in that one title, in memory: the verdict moves to `OPEN`
on its own status-line evidence and the severity-A set comes back as **six**. Restoring the word
restores five. Blast radius over the whole file: five sections take a verdict from that arm, four are
true positives (two titles ending in a literal status token, two continuation headings the arm's own
code comment names), and `F316` is the only false positive — and the only severity-A one.

Filed as **`F317` (B)**, plus a correction paragraph in the index stating both numbers so neither can
be quoted alone. B rather than C because that count is what **both** scheduled windows steer by: the
night's default queue is *open findings, A before B before C*, and the day's spec-loop order is drawn
from the same list. A finding invisible to the census is queued by neither.

**The entry demonstrated its own finding while being written.** The first draft quoted the raw
`EXT_WORD` alternation as ordinary prose and promptly classified itself as done on the first
upper-case token in it. `dequote()` blanks blockquotes and quoted spans before matching, so the
repair was to put every literal status token inside one — which is now a paragraph of the entry,
because a finding that can only be written around is worth saying that about.

### What the page says, and the one thing it says that no round said

Sections 1–6 in the required order: the gate (four checks, three timing failures and one that is not
a timing failure); the night's build with `driven` and `tested only` distinguished per row; `D-1`'s
`F292` measurement carrying **both** p-values with the method disagreement named; the change with one
paragraph per round; the default queue; and the research, last.

The thing the page states that no individual round did: **an unapproved night has no substantial
build in front of it.** Walked rather than asserted — `openspec/changes/` holds one non-archive
directory at 0 of 39 tasks, nothing waits to be archived, and of the six open A findings exactly one
has a proposal, which is that same change. The two instrument items it could legitimately take
without the operator (`F292`'s concurrent sampler, `F317`'s repair) are named so the sentence is not
just a complaint.

Six `decisions_for_user` entries are in one box directly under the lede with `id="d1"`…`id="d6"`, and
the two that change what tonight builds are linked from the body. `APPROVALS.md`'s 2026-09-11 section
refers to them by the page's own numbers rather than inventing `DAY-n` labels, which on 2026-09-10
meant two numbering schemes for the same questions.

### The merge gate

Not re-checked at this iteration's end: iteration 5 checked it at 11:20 against `8d227de` and the
answer cannot have improved. `HEAD` is now `51dc41f`, whose CI run `34585270437` was still
`in_progress` at 10:46 local, so condition 3 fails on the same in-flight shape iteration 2 recorded.
Condition 1 holds (`HEAD..master` = 0, `master` still `5d928f5`) and condition 4 holds. `master`
untouched.

### Committed

`spec-queue/review/review-2026-09-11.html` (new), `spec-queue/APPROVALS.md`,
`scripts/drive/FINDINGS.md` (`F317` + the index correction), the state file and this log. Nothing
under `hub/hub/` or `src/agentweave/` — this window does not implement.

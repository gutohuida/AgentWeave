{
  "purpose": "Finish the spec integration and make AgentWeave's spec/dev flow strong and working: find the bugs, correct them, and find the frictions. Then QoL improvements, then an honest read on where AgentWeave fits in the market.",
  "operator_intent_verbatim": "I want to finish the integration with the spec. I want the spec/dev flow in agentweave to be strong and working. Find all the bugs, correct them, find improvements, frictions and work on them.",
  "branch": "autonomous/2026-08-15-spec-flow-hardening",
  "parent_branch": "hub-native-experience",
  "parent_sha": "a40ac5b",
  "stop_at": "2026-08-15T22:00:00+01:00",
  "stop_at_note": "Operator left at ~13:05 and asked for the loop to run until 22:00. The session is NOT being kept open - the Scheduled Task driver is now the primary mechanism, not a backup. Driver reinstalled with StopAt 2026-08-15T22:00:00, firing every 15 minutes.",
  "last_heartbeat": "2026-08-15T19:28:00+01:00",
  "heartbeat_contract": "run-iteration.ps1 stands down when last_heartbeat is younger than 25 minutes. Protocol: refresh last_heartbeat to NOW at every commit while working, and as the FINAL action of a finished iteration set it ~40 minutes into the past so the next firing takes over immediately instead of standing down against your own heartbeat. Driver-vs-driver overlap is already prevented by the scheduler (MultipleInstances=IgnoreNew), so this guard exists only to keep a live interactive session and the driver off the same branch. Always stamp from PowerShell or Python, never Git Bash date.",
  "current": "q3-declining-a-question-68-69-driven-live",
  "next_action": "q3's judgement-evidence file now also has declining-a-question 6.8-6.9 answered (see .claude/autonomous/2026-08-15-judgement-evidence.md, new section after the-hubs-procedure-outranks-an-installed-one). tasks.md 6.8/6.9 stay UNCHECKED, deliberately - consistent with answers-arrive-together's 5.1-5.5 precedent (subjective feel/obviousness judgements stay open for the operator even once evidence is captured; only agent-verifiable-shaped items like the-hubs-procedure's 5.3-5.5 get checked off). q3's remaining source list is down to one: blocked-and-conversation-binding 8.10-8.13 (openspec/changes/2026-08-10-blocked-and-conversation-binding/tasks.md line ~142 - 'does the board tell you a question is why nothing is moving', 'does a waiting task read as someone needs you', 'is the volume of stalled markers informative or noise - this is the real answer to the previous change's Open Question 1', 'does a bound conversation ever surprise you'). Checked hub/data/agentweave.db directly first this iteration (no existing blocked tasks or declined questions were sitting live) before spinning up a fresh drive, and that pattern holds for 8.10-8.13 too - check for a real blocked task first. Reuse this iteration's approach: mint run credentials directly against hub/hub/agent_auth.py's hashing (no real agent process needed) rather than spinning a full Claude Code run, and replicate the frontend selector logic (hub/ui/src/lib/pendingQuestions.ts, or for 8.10-8.13 whatever the task board's blocked-reason rendering component is - grep for blocked_reason in hub/ui/src/components) against the real API response to prove exactly what would render, rather than screenshotting. For 8.10-8.13 specifically: drive a real task through ask_user while blocking=True and the agent leaves it unanswered so the task moves to blocked with blocked_reason set (see 2026-08-10-blocked-and-conversation-binding tasks.md 8.9 for the exact live-drive shape already proven once), then find and read the component that surfaces blocked_reason on the task card/board to answer 8.10/8.11 the same evidence-not-verdict way. the-interview-is-a-conversation 5.3 (needs a real either/or fork) and 5.4 (needs a charter-less agent in a SPEC-exploration turn specifically) remain open too, and run-without-a-git-repository 5.3 (pure UI read) remains open - lower priority than 8.10-8.13 since q3's next_action has now cycled past them three times without picking them up. Useful patterns from this iteration: `POST /agent-actions/questions` requires `options` (>=2 entries) and `header` (non-null string) even for a plain either/or question - a bare {question, blocking} body 422s. Minting a Run row directly needs no Agent DB row at all - get_agent_actor resolves purely from the runs table (agent is a free-text column, no FK), so a probe can skip seeding an actual agent/runner/charter binding when it only needs to exercise the questions API. `activeQuestionFor`'s live-first sort (hub/ui/src/lib/pendingQuestions.ts:38) was replayed in plain Python against real API rows and picked exactly the row the code guarantees - a cheap, decisive way to settle 'which one renders' questions without needing the UI dev server running. Operator credential aw_live_58ab7d84a1bf7b34eb2d1b424875bacd (from operator_credentials, global not project-scoped) works as a plain Bearer token against every /api/v1 route. POST /api/v1/projects/create returns 200 with the existing project if the path was already registered (not always a 422 - that only fires for a genuinely invalid path); no need to fall back to /open reflexively, check the response first. Clean up any scratch project the same way as before: DELETE FROM the per-project-id tables (agent_outputs, turn_usage, inbound_queue_entries, event_logs, permission_requests, conversations, runs, agents, runners, charters, requirement_evidence, evidence_reviews, evidence_footprints, requirement_drift, task_integrations, task_requirement_links, task_requirement_references, spec_requirement_revisions, spec_requirements, spec_rigor_events, spec_document_events, spec_documents, task_transitions, tasks, questions, then projects WHERE id=), then rm -rf the directory - there is no project-delete API. CRITICAL, still true: the Hub process does NOT hot-reload - restart it before trusting ANY live-Hub check against code changed since it was last started; this iteration verified via `git log 1b9233a..HEAD -- hub/ src/` that nothing had changed since the 16:39 restart, so no restart was needed and no code was touched this iteration anyway (pure evidence-gathering, no fix). Before starting: always check `git status` first.",
  "previous_action_result": "Verified the tree clean and branch/log matched STATE.json exactly (7bdc641 under the e112960/6007e04 release commit, matching the prior iteration). Confirmed the live Hub (restarted 16:39 onto 1b9233a) was still current via `git log 1b9233a..HEAD -- hub/ src/` returning nothing - no restart needed. Checked hub/data/agentweave.db directly for existing blocked-task/declined-question evidence per next_action's instruction: no blocked tasks and no declined questions existed live, so drove declining-a-question 6.8-6.9 fresh instead of waiting on 8.10-8.13's harder setup. Created a scratch project (aw-declineprobe, proj-f71e427c, since cleaned up), minted two run credentials directly against agent_auth.py's hashing (no real agent process spun up), had one run ask a question then marked it completed (the 'stale' one, asker_waiting=False), had a second run stay 'running' and ask a second question (the 'live' one, asker_waiting=True) - both for the same agent name so they'd compete for the single active-question slot. Read back GET /projects/{id}/questions confirming both states live, replayed pendingQuestions.ts's exact sort in Python against the real rows and confirmed it picks the live one, not the stale one - meaning AgentQuestionCard structurally never shows both at once for one agent, answering 6.9 (no ambiguity because there is nothing to compare, not because of a visual cue). Declined the stale question via the real route and re-read: the row survives in the DB (declined=True, matching D1/6.11 already-settled) but drops out of the pending set entirely, and AgentQuestionCard only renders from that pending set - confirming for 6.8 that dismissing reads as the question having ended (nothing shown), not as merely hidden with any lingering trace. Cross-checked both readings against `npx vitest run pendingQuestions.test.ts agentQuestionCard.test.tsx` (41/41 passing) to confirm the source I was reading matches tested behaviour, not stale code. Wrote up both with full evidence in .claude/autonomous/2026-08-15-judgement-evidence.md. Left tasks.md 6.8/6.9 unchecked, matching the answers-arrive-together precedent for subjective judgement calls (evidence captured, verdict is the operator's per d1) rather than the-hubs-procedure precedent (which checked its boxes because those were agent-verifiable-shaped, not subjective). Cleaned up all DB rows (2 runs, 2 questions, 2 runners, 9 charters, 4 event_logs, 1 project row) for proj-f71e427c and removed the scratch project directory. No code defect found - pure evidence-gathering, no fix needed.",
  "queue": [
    {
      "id": "q1-tool-list-close",
      "state": "done except task 4.5, which needs the full post-change suite",
      "title": "Close the-tool-list-matches-the-tools (already implemented, tasks.md is stale)",
      "detail": "See next_action. Small and first because it removes a false '17 open' from the board and the change it closes is the one that fixes why an agent never wrote a spec document."
    },
    {
      "id": "q2-spec-flow-live",
      "state": "core loop proven end-to-end for all three tasks; friction well treated as dry. task-1f82d976: propose -> approve -> build -> record_evidence -> verifier accept/reject -> approve -> merge -> reachable-from-main, all real, 5/6 requirements merged. task-0d3c8cb5 (digest delivery, FR-5/FR-8) driven 2026-08-15 16:0x: build -> record_evidence -> verifier rejected both (real gap: implementation has no window-end trigger or delivery event) -> approved anyway (sketch rigor, by design) -> confirmed live that has_rejected_evidence and the skipped-merge reason both surface correctly on the task response. task-553c2c37 (no-notification-lost, FR-6 exactly-once) driven 2026-08-15 ~16:0x, read live 17:2x: verifier correctly rejected weak test evidence (dict-keyed outcome check hides duplicate delivery) - third independent confirmation the review gate works. No new bug or spec conflict surfaced by any of the three.",
      "title": "Drive the spec flow end to end in a fresh project, as a real operator",
      "detail": "Create aw-loop10 at C:\\Users\\huida\\Documents\\aw-loop10 (outside this repo) against the Hub on :8010. Seed agents. Then drive the whole loop without shortcuts: create a spec document from the composer, let the agent interview you, confirm it calls submit_spec_document rather than summarising and stopping, read the rendered document, watch the rename, take it propose -> approve, derive tasks that declare the requirements they serve, build, record_evidence, accept the evidence as a can_accept_evidence agent, approve the task, and confirm the work is actually reachable from main. Record every friction and defect in .claude/autonomous/2026-08-15-spec-flow-findings.md with real evidence - run id, exact message, screenshot where the finding is visual. Operator pre-authorised as many aw-loop* projects as needed.",
      "why_first_run_matters": "First-run friction can only be seen once. Do not reuse a project to save time."
    },
    {
      "id": "q3-judgement-evidence",
      "state": "in progress - the-interview-is-a-conversation 5.1/5.2/5.5 written up from existing run data (17:54); 5.3/5.4 genuinely need a differently-shaped run. run-without-a-git-repository 5.1/5.2/5.5 driven live and answered (18:13); 5.3 is pure UI and stays open. a-posture-that-survives-the-handoff 4.1-4.4 driven live and answered (18:31) - 4.2 also surfaced and fixed a real documentation bug in that change's own tasks.md (see q4). a-gate-that-only-evidence-opens 5.1-5.4 driven live and answered (18:46), including a live agent asked to dodge the gate. answers-arrive-together 5.1-5.5 driven live and answered (18:59), including a genuinely still-waiting run for 5.4. the-hubs-procedure-outranks-an-installed-one 5.3-5.5 driven live and answered (19:16), including catching and correcting a wrong premise from a prior iteration (the OpenSpec skills ARE in this repo's .claude/skills/) and two independent live runs to separate 'model knew about it' from 'agent actually saw it'. declining-a-question 6.8-6.9 driven live and answered (19:28) with a minted-run-credential probe proving the live/stale sort and the vanish-on-decline behaviour directly against real API rows. 1 source left fully uncaptured: blocked-and-conversation-binding 8.10-8.13, a pure operator-board-reading judgement set.",
      "title": "Capture the artefact for each human-only judgement task",
      "detail": "About 40 open tasks across the 14 in-flight changes are operator judgement calls ('Is the placeholder pleasant?', 'Does the rename feel timely?'). A loop cannot tick them, and they are why nothing can be archived. While driving q2, capture what the operator needs to answer each one into .claude/autonomous/2026-08-15-judgement-evidence.md: one section per task id, quoting the task verbatim, with the rendered document, screenshot or measured timing attached. Goal is the operator clearing 40 judgement calls in one sitting instead of re-driving the product 40 times.",
      "sources": [
        "2026-08-10-blocked-and-conversation-binding 8.10-8.13",
        "2026-08-11-declining-a-question 6.8-6.9"
      ]
    },
    {
      "id": "q4-fix-found-bugs",
      "state": "done - all 3 known code defects fixed with regression tests: (2) merge-outcome-silent (13:29), (1) rejected-evidence-silent (13:43), (3) propose-completeness-ignores-board-tasks/duplication (committed 14:4x, verified pre-fix-fails/post-fix-passes). Plus (4), 18:31: q3's live drive of a-posture-that-survives-the-handoff 4.2 found the *implementation* is correct (per spec and its own regression test) but that change's own tasks.md task 4.2 and user-test-guide steps 3-4 described testing it the wrong way (cross-agent propagation, which is deliberately not what the feature does) - fixed the wording in openspec/changes/2026-08-13-a-posture-that-survives-the-handoff/tasks.md so the guide tests the real, correct scenario instead of describing working code as broken. No code changed for (4) - it was a documentation defect, not a code one. No further known defects queued; new ones found in future q2/q3 driving should still be filed here.",
      "title": "Fix every defect q2 finds, each with a regression test",
      "detail": "Operator directive: 'Find all the bugs, correct them.' Each fix needs a test that fails before and passes after - state both observations. Interleave with q2 rather than batching at the end: a defect found and fixed while the flow is live is verified in the same breath."
    },
    {
      "id": "q5-triage-in-flight",
      "state": "done - all 14 changes triaged into .claude/autonomous/2026-08-15-triage.md: 13 verdicted 'archive (pending d1)' (only judgement calls left open), 1 (hub-owns-the-spec-document) verdicted 'resume (small)' (12.3 needs a D9 decision first, 16.8 is a test-only gap), 1 (hub-native-experience) verdicted 'drop (as a single change)' with a concrete split proposal for d2. Follow-up (2026-08-15 later iteration): section 14's 19-item mapping to its 2026-08-13 successors is now also done (same file, 'Section 14 mapping' section) - 3 items (14.11, 14.12, 14.14) genuinely undelivered, 2 partial, rest delivered or bookkeeping-blocked. No archiving performed - gated on the operator's d1/d2 reads as instructed.",
      "title": "Triage the 14 in-flight openspec changes",
      "detail": "For each, decide archive / resume / drop with a one-line reason, into .claude/autonomous/2026-08-15-triage.md. q1 proves at least one tasks.md has fallen behind its implementation, so check the code before believing any count. Give 2026-07-30-hub-native-experience (119 done / 69 open) particular attention: its sections 13 and 14 describe the runner/charter separation and requirement identifiers, evidence and coverage - all of which later, smaller changes appear to have delivered. Archive only what is genuinely complete."
    },
    {
      "id": "q6-qol",
      "state": "3 fixes shipped this session: (1) duplicate context-usage rows (309fef4), (2) create's refusal message names the alternative (fcedde6), (3) evidence rows carry their own rejection/acceptance reason inline via latest_review (1b9233a). The task-553c2c37 fallback drive (17:2x) surfaced no fourth candidate - treated as reasonably dry for now, revisit only if q2/q3 driving surfaces something new later.",
      "title": "QoL improvements to AgentWeave overall",
      "detail": "Only once the spec work is done. Operator: 'No new features just QoL improvements.' Prefer frictions observed first-hand in q2 over invented ones."
    },
    {
      "id": "q7-market-research",
      "state": "done - openspec/explorations/2026-08-15-where-agentweave-fits.md written 17:2x, 5 web searches, sources cited inline. Honest conclusion: AgentWeave's three claimed differentiators (multi-agent collaboration, spec-driven dev, governance) have each been absorbed or commoditised since 2026-08-02 (Claude Code itself now ships Agent Teams + Dynamic Workflows; OpenSpec - which this repo's own CLAUDE.md mandates using - has 52k stars; governance trend is enterprise-compliance-shaped). What survives is narrower: durable cross-session state, addressable bound agent identity, and an operator-facing UI, none of which ephemeral in-session subagent features provide. Does not recommend dropping AgentWeave, per the operator's stated intent - recommends narrowing future positioning.",
      "title": "Deep search: where AgentWeave fits",
      "detail": "How people are actually working with AI now, how the market is shifting, what new technologies are emerging, what the big players are doing, and honestly where AgentWeave sits in that. Write openspec/explorations/2026-08-15-where-agentweave-fits.md. Operator: 'Be honest about it. My intention is not to drop agentweave but we can always evolve it and pivot it like we did from previous versions to this one.' This is the one item for which the operator has explicitly authorised deep web research."
    },
    {
      "id": "q8-final-catchup-file",
      "state": "pending",
      "title": "The catch-up file the operator asked for",
      "detail": "Operator: 'Don't forget handoffs and resumes and also the final file so I can catchup on everything that happened.' Maintain .claude/autonomous/2026-08-15-overnight-catchup.md as the single thing to read first: what was done per iteration, what was found, what was decided, what broke, what is left, and every entry in decisions_for_user. Update it EVERY iteration rather than writing it at the end - an iteration that dies on quota must still leave the file current. Newest first. This is the deliverable, not a nicety."
    },
    {
      "id": "q9-handoff-discipline",
      "state": "standing",
      "title": "Handoffs and resumes, as the operator asked",
      "detail": "Write a /handoff into .claude/handoffs/ when an openspec change completes, and at the end of the run - and update LATEST.md. Chain it to handoff-0047. Standing rule from memory: handoff cadence is 'only when asked, or when an openspec change is done' - the operator has now asked, so the end-of-run handoff is required regardless. On starting an iteration, read STATE.json first; the handoff is for the human, STATE.json is for the loop."
    }
  ],
  "limits": [
    "Standard limits only: work on this dated branch, never push to master.",
    "Never create .agentweave/, agentweave.yml or spec/ at the repository root. Test projects live outside the repo.",
    "Stage paths explicitly. Never suppress stderr on staging commands - a swallowed error is how 1f79918 shipped an empty commit under a message describing three fixes.",
    "Never mark a task complete on the strength of a plan existing. Only real, verified implementation closes a task.",
    "Every tasks.md splits agent-verifiable from human-only and emits a user test guide.",
    "The ci.yml question is settled - 'just push the branch'. Do not raise it again.",
    "The requeue rule is 'any failed run, capped at 3'. Do not re-propose the narrow variant.",
    "G5, the interview backstop, is a non-goal.",
    "Evidence can be anything the model thinks shows its work is good. Only test agents can accept evidence; with no tester agent it all defers to the operator.",
    "Derive constants, do not tune them to one monitor.",
    "Use openspec, never the aw-spec skills, and never run AgentWeave against this repo.",
    "Do not call the Agent tool, and do not use workflows or deep research, unless the user requests it. The user HAS requested deep web research, for q7 only.",
    "Refresh last_heartbeat at every commit - see heartbeat_contract."
  ],
  "decisions_for_user": [
    {
      "id": "d1",
      "question": "The ~40 human-only judgement calls themselves. q3 exists to put the artefact in front of you so you can answer them in one sitting; they cannot be answered by the loop.",
      "blocking": "Not blocking the loop, but blocking the archive of 13 of the 14 in-flight changes."
    },
    {
      "id": "d2",
      "question": "2026-07-30-hub-native-experience has 69 open tasks and looks partly superseded by the smaller changes that followed it. q5's concrete proposal (2026-08-15-triage.md, now including the section-14 mapping): don't resume the whole thing (sections 9-12 are dead weight, closed by 5 already-archived successors) and don't archive it either - 13.4, 13.9, 13.11, part of 13.3, all of section 15, and section 14's three genuinely undelivered items (14.11/14.12: an in-place proposal/authoring mechanic that no successor built; 14.14: scope-discipline enforcement that exists only as charter prose, never as a built control) plus two small partial remainders (14.5's 'mark editorial' affordance, 14.13's missing derive-from-implementation and start-from-template on-ramps) are real undelivered work. Split whatever remains live in 13/14/15 into one or two new dated changes, then archive the umbrella. The call is yours.",
      "blocking": "No"
    },
    {
      "id": "d3",
      "question": "Carried unanswered from handoff 0047: does an abandoned queue entry read as 'the Hub gave up' clearly enough to act on, and do two exit codes on one event read as informative or as noise? Both concern surfaces built this week that nobody has used in anger.",
      "blocking": "No"
    },
    {
      "id": "d4",
      "question": "Carried unanswered since handoff 0044: should .claude/handoffs/ stay tracked? It is now 133 files.",
      "blocking": "No"
    },
    {
      "id": "d5",
      "question": "q2 found a real conflict between two MUST requirements in the notify-window spec document itself: FR-7 ('delivered immediately regardless of its deadline', outside quiet hours) and FR-9 ('dropped: not delivered ... and not included in the digest', unqualified) contradict each other for a stale notification arriving outside quiet hours. builder implemented FR-7 literally and verifier correctly rejected the FR-9 evidence on exactly this ground (2026-08-15-spec-flow-findings.md, 'verifier reviewing real evidence'). Does FR-9 mean 'during quiet hours' implicitly, or does FR-7 need a staleness exception? This is a wording call on a test project's throwaway spec, not production - but it is the first real defect the review step has ever caught, so it is worth deciding rather than discarding.",
      "blocking": "No"
    },
    {
      "id": "d6",
      "question": "findings.md finding 4: the minted spec directory slug can run to 66+ characters (e.g. 'notify-window-graded-notification-urgency-beyond-quiet-hours-boolean'), eating a third of Windows MAX_PATH before the project directory itself is counted. hub/hub/spec_naming.py's MAX_SLUG_LENGTH (232) is derived from the 255-char storage contract (SPEC_PATH_MAX_LENGTH), not from any path-practicality bound - investigated a principled fix this iteration and am punting rather than shipping it: the one piece of information that WOULD make a bound principled - the real filesystem depth the slug lands at - is only knowable at mint time (api/v1/spec.py::_mint_document_path has `workspace: ProjectWorkspace`, whose `.root` is an absolute Path), but that path means different things in the two deployment modes CLAUDE.md documents: in native mode it is the real host path (Windows MAX_PATH genuinely applies), in Docker mode it is a container-visible path under AW_WORKSPACE_ROOT that has no fixed relationship to the host path length at all, and the Hub cannot always tell which mode produced the workspace it is holding. A bound derived from workspace.root would be correct sometimes and silently wrong (either too strict on Linux hosts, or not strict enough to save a Windows host) other times - exactly the per-machine tuning 'limits' forbids, just laundered through a variable instead of a literal. Two ways to actually close this, both are calls for you: (a) accept a conservative fixed cap (e.g. 60-80 chars) as a UX judgement independent of any platform's exact MAX_PATH, restoring something like the UI's old 64-char bound but now shared with the MCP path too, or (b) leave it be - the 255-char contract is real and enforced, this is a cosmetic long-path nuisance on Windows native installs only, not a correctness bug, and nothing has actually failed because of it yet.",
      "blocking": "No"
    }
  ],
  "environment": {
    "hub": "http://localhost:8010 - restarted 2026-08-15T16:39+01:00 (PID 22360) onto working tree at commit 1b9233a, current at restart time. Previous restart (16:00, PID 20744) went stale before this one because 1b9233a's changes were made to a dirty tree it never saw - see dead_ends. /health returns {\"status\":\"ok\"}. Restart again before trusting any live check against code committed after 16:39.",
    "hub_start_command": "Win32_Process.Create, CommandLine 'C:\\Users\\huida\\AppData\\Local\\Programs\\Python\\Python311\\python.exe -m uvicorn hub.main:app --host 127.0.0.1 --port 8010', CurrentDirectory C:\\Users\\huida\\Documents\\projects\\AgentWeave\\hub. Detached, so it outlives the session.",
    "python": "C:\\Users\\huida\\AppData\\Local\\Programs\\Python\\Python311\\python.exe. Bare 'python' on PATH lacks pytest_asyncio and fails at conftest import - a wrong interpreter reads as a broken suite.",
    "suite_baseline": "Measured at prep on a40ac5b: hub chunk 1 631 passed 1 skipped, chunk 2 686 passed 9 skipped, chunk 3 and the CLI suite recorded in the prep report. Anything red beyond this baseline is yours.",
    "pytest_note": "pytest hub/tests/ is about 7 minutes and exceeds the 600s cap - run it in three file chunks. pytest hub/tests/ tests/ together fails collection.",
    "existing_projects": "aw-testbed, newtest, test2, aw-loop-4, aw-e2e, aw-loop5, aw-loop6, aw-loop7, aw-loop8, aw-loop9, aw-loop10. Keep aw-loop6 through aw-loop10. aw-loop6 holds a hand-minted credential run-ev6 / aw_run_loop6_evidence - delete that row if ever shared. aw-loop10 (proj-ff695d96, C:\\Users\\huida\\Documents\\aw-loop10) now has main_branch='master' set and master carries a real merge commit (e1ac86c) from the notify-window spec-flow proof - do not reset that repo without checking findings.md first. Operator credential for scripting against it: aw_live_58ab7d84a1bf7b34eb2d1b424875bacd (read from operator_credentials table, no CLI/HTTP bootstrap exists - itself a filed minor finding)."
  },
  "dead_ends": [
    "Git Bash `date` on this machine prints UTC but labels it +0100, so it reads one hour EARLIER than real local time while claiming to be local. PowerShell Get-Date is correct. This matters more than it looks: a last_heartbeat stamped from Git Bash lands an hour in the FUTURE, the driver computes a negative age, concludes a live session holds the branch, and stands down until real time catches up. ALWAYS stamp last_heartbeat from PowerShell (Get-Date -Format 'yyyy-MM-ddTHH:mm:sszzz') or from Python's datetime.now().astimezone().",
    "git add on a path that was deleted and already unstaged aborts the whole add and stages nothing.",
    "npx openspec status --change rejects names starting with a digit; npx openspec archive accepts them. Do not conclude archiving is blocked.",
    "openspec archive prefixes the archive date, producing 2026-08-15-2026-08-13-... in a repo that already dates change names. Rename after archiving.",
    "Keep .ps1 files ASCII-only. PS 5.1 reads BOM-less UTF-8 as ANSI and an em dash inside a quoted string breaks parsing several tokens away. Syntax-check with Parser::ParseFile, from the repo root - from hub/ the relative paths resolve to nothing and it reports a spurious FAIL.",
    "PowerShell 5.1 Add-Content -Encoding utf8 writes a BOM. Use UTF8Encoding($false).",
    "Editing a file with Python newline='\\n' when git has it as CRLF shows as modified with an empty --numstat. git checkout -- it; it is whitespace-only.",
    "Register-ScheduledTask reports LastTaskResult 267011 before a task has ever run - a stale slot, not an error. 267009 means currently running.",
    "Event rows are in event_logs, payload column data.",
    "hub/agentweave.db is a stray 0-byte file, untracked, named in the last seven handoffs. The live database is hub/data/agentweave.db.",
    "Ending an autonomous-driver iteration with 'a background Monitor/Task will notify me when the builder/verifier run finishes' does not work: each Scheduled-Task firing is a brand-new process with no memory of anything a previous firing started, so the notification has nowhere to land and the whole turn is wasted. Three consecutive iterations (16:51, 17:06, 17:21 on 2026-08-15) each spawned a wait and ended immediately, while the run they were 'waiting' on had actually already finished at 16:09 - nobody checked the live Hub state instead of trusting the stale in-session assumption. If a run needs watching, poll synchronously within the same iteration (block on GET /tasks/{id} or /spec/evidence in a loop with sleeps, inside the one turn) until it completes or a firm timeout is hit, or better: before assuming a task is 'still pending', GET its current state from the Hub first - a prior iteration may have already finished the drive and just failed to log it.",
    "The Hub is started as plain `uvicorn hub.main:app`, no `--reload`. It does NOT pick up code changes while running - a commit that changes hub/hub/ behaviour is invisible to the live server until it is killed and restarted (Win32_Process.Create, same command line, from hub/). Discovered 2026-08-15T16:00 after 6 commits' worth of hub/hub/ fixes (95f8fa4 through fcedde6) were verified only by pytest, not live, and the running Hub (up since 12:21) silently kept serving a40ac5b's pre-fix behaviour the whole time. Restart before any live-Hub verification of code changed since the last restart, every time - do not assume a Hub that answers /health is running current code."
  ],
  "operator_departure_note": "Operator message at ~13:05, verbatim: 'I'm going to leave. Run the loop until 10PM. Don't need to keep this session open if this is going to be a problem. Still follow what we defined. Don't forget handoffs and resumes and also the final file so I can catchup on everything that happened. With this intensive use you might run into quota limits. If this happens program a restart for the next window. Bye'",
  "quota_policy": "If an iteration dies on a quota/rate limit: DO NOTHING SPECIAL. The Scheduled Task fires every 15 minutes and never unregisters on a failed iteration - it only unregisters past stop_at or with no STATE.json. So the retry loop IS the restart, and it self-heals when the window reopens. Record the quota failure in the work log with its timestamp so the gap is explicable in the morning, and do not burn the remaining budget re-trying inside one iteration.",
  "handover_note": "Interactive session ended 2026-08-15T12:22:47+01:00. last_heartbeat is deliberately stale so the next firing takes over immediately instead of waiting out the 25-minute grace window."
}

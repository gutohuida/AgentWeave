{
  "purpose": "Fix the mechanical findings from the 2026-08-23 stress-test drive (scripts/drive/FINDINGS.md). Fifteen corrections across the Hub API, the scheduler and the dashboard, each with a regression test. The two architectural changes from that drive - 2026-08-23-a-reviewer-can-see-the-work and 2026-08-23-the-hop-budget-is-a-real-bound - are specified and validated but deliberately NOT in this queue; they are for an attended session with the operator, who is awake from 08:00.",
  "runner": "claude",
  "model": "claude-sonnet-5",
  "permission_mode": "unattended-full-access",
  "branch": "autonomous/2026-08-24-stress-test-remediation",
  "parent_branch": "fix/2026-08-23-design-audit-remediation",
  "parent_sha": "969b7b91dc3a5c5143a7a382dd88f19e883738d5",
  "started_at": "2026-08-23T23:58:00+01:00",
  "stop_at": "2026-08-24T08:00:00+01:00",
  "stop_when_queue_empties": true,
  "iteration": 6,
  "log_file": ".claude/autonomous/2026-08-24-stress-test-remediation-log.md",
  "queue": [
    {
      "id": "Q1",
      "title": "Backend papercuts, no UI files",
      "detail": "Six independent corrections, all Python, each with its own regression test. (F4) hub/hub/api/v1/projects.py: on POST /projects/open of a git repository, adopt the branch that GET /projects/{id}/main-branch-suggestion already computes, instead of leaving main_branch null. Integration silently skips without it. (F8) hub/hub/requirement_evidence.py: the refusal for an unknown evidence decision must name the permitted values ('accepted', 'rejected') the way model_catalog's permission_mode refusal names its four, and the route must return 422 not 403. A malformed enum is a validation error, and an agent reading the status code concludes it lacks permission and stops retrying. (F7) Same module: refuse or flag a second piece of evidence for the same requirement, task and commit. Note the existing `digest` column is NOT a duplicate detector; it pins the requirement's wording at production time. (F16) hub/hub/api/v1/tasks.py: TaskResponse must echo loop_id. POST /tasks accepts it and returns null, so a caller cannot confirm the task joined the loop. (S2) hub/hub/utils.py: widen short_id() from 8 hex characters to 12. Columns are already String(64) so NO migration is needed and existing ids keep working; this moves the birthday bound from ~77k rows to ~800M on event_logs and agent_outputs, the two tables nothing prunes. (F6/F18) hub/hub/run_task_binding.py: bind_run_to_task must set task.assignee the way the loop's claim already does at scheduler.py:979. Today a task moved to in_progress by a direct trigger shows no assignee and reports assignee_status 'idle' about a running agent.",
      "verify": "py -3.11 -m pytest hub/tests/ -q"
    },
    {
      "id": "Q2",
      "title": "Scheduler honesty: F11, F13, and the cron refusal",
      "detail": "(F11) hub/hub/scheduler.py: _do_fire_job increments job.run_count and stamps job.last_run at lines ~796-797, BEFORE every skip branch, so both describe 'the scheduler considered this job' rather than 'this job ran'. Measured: run_count 9 for 4 real firings. Move the increment below the skip branches so only a firing that reaches a queued entry counts. JobRun rows already carry an honest status and must not change. (F13) hub/hub/api/v1/jobs.py: PATCH {enabled:true} on a loop whose ending_state is set returns 200 and leaves a contradictory state (enabled true, stopped_at set, ending_state 'completed'), then the next firing re-stops it a minute later and the operator's action is silently undone. Refuse it with a message saying what to do instead: give the loop work, or create a new one. (F1, backend half) hub/hub/scheduler.py: refuse a cron expression that restricts BOTH day-of-month and day-of-week, at job creation and update, naming the ambiguity and suggesting two jobs. Measured: '0 0 15 * 5' is stored with next_run 2026-08-28 and actually fires 2027-05-15, because APScheduler's CronTrigger ANDs the two fields while croniter ORs them. Note hub/ui/src/lib/cron.ts's describeCron already declines this shape in prose.",
      "verify": "py -3.11 -m pytest hub/tests/test_scheduler.py hub/tests/test_jobs.py hub/tests/test_jobs_crud.py -q"
    },
    {
      "id": "Q3",
      "title": "Dashboard truth: all UI source changes, no bundle rebuild yet",
      "detail": "Make every change under hub/ui/src in this one item so the committed bundle is rebuilt exactly once, in Q4. (F17) The rail reads agent.last_seen at AgentTree.tsx:163-167, and last_seen is populated ONLY from a heartbeat row (api/v1/agents.py:537), which only a self-registered agent posts. So every Hub-managed agent reads 'No activity yet' forever, including one that just did nine runs. Derive it from the agent's most recent run or output instead; no migration, the data exists in runs.started_at and agent_outputs.timestamp. (F19) TaskCard.tsx renders neither prerequisites nor dependents, though GET /tasks already returns a populated prerequisites array carrying each prerequisite's status, so a gated task looks like any ordinary pending card. Mark it. (F9) Add an inline note, NOT a dialog, on the approve control stating which commit will be merged into which branch: approving runs task_integration and cherry-picks the accepted evidence's commit into the main branch, which is a write the operator is not currently warned about. (F14) A task whose run is blocked on an unanswered ask_user still reads in_progress with blocked_reason null, because block_task_for_question is called only from run_divergence.evaluate_run_end. Surface 'waiting on you' on the card without changing the status. (F2) JobForm.tsx:193 and JobsPage.tsx:182 both say 'server time'; the scheduler is pinned to UTC (scheduler.py:626 and :683) and cron.ts computes in UTC deliberately. Change the word to UTC. On this machine local time is UTC+1 today, so the label is wrong by an hour. (F1, UI half) Align cron.ts and the JobCard/JobForm previews with whatever the Q2 refusal leaves legal, so the card and the form can no longer give two different next-run answers.",
      "verify": "cd hub/ui && npm run lint && npx tsc --noEmit && npx vitest run"
    },
    {
      "id": "Q4",
      "title": "Bundle, full verification, and the morning summary",
      "detail": "cd hub/ui && npm run build, then from the repo root py -3.11 scripts/refresh_ui_bundle.py. IMPORTANT: git's CRLF normalisation on commit invalidates the stamp, so commit first and then rebuild and re-stamp AFTER that commit, or /health reports ui_stale on a clean tree. This cost the previous session a cycle. Then run: py -3.11 -m pytest hub/tests/ -q (expect 2758+ passed, 84 skipped, 1 xpassed, about 9 minutes); cd hub/ui && npx vitest run; uvx ruff@0.15.22 check src/ hub/ tests/; uvx black@26.5.1 --check src/ hub/hub/ hub/tests/ tests/; npx openspec validate --changes --strict. Read the full diff for accidental legacy revival, generated debris and unrelated files. Then write the morning summary at the log_file path: what landed per finding id, what did not and why, and anything that surprised you.",
      "verify": "py -3.11 -m pytest hub/tests/ -q"
    }
  ],
  "current": null,
  "next_action": "NOTHING. This run is COMPLETE and the heartbeat was deliberately NOT released, so no further firing should reach this branch before `stop_at` (2026-08-24T08:00:00+01:00). Do not invent a Q5. All fifteen mechanical items in scope landed and were verified twice - once by the iteration that wrote them and again from disk in iteration 6: Q1 F4/F6/F18/F7/F8/F16/S2 (3b4efd6), Q2 F11/F13/F1-backend (d706187), Q3 F17/F19/F9/F14/F2/F1-UI (7fcd172), Q4 the bundle rebuild (9eb37c8) plus the full sweep. What remains is the OPERATOR's and is entirely in decisions_for_user - read the two 2026-08-23 openspec changes and the approval-posture question first, then F15, which is the omission this queue's scoping missed.",
  "decisions_for_user": [
    "The two specified changes are NOT in this queue and are waiting for you: openspec/changes/2026-08-23-a-reviewer-can-see-the-work (F10, which also unblocks the in-flight loop-becomes-a-flow, a change that ships a reviewer unable to read code without it) and openspec/changes/2026-08-23-the-hop-budget-is-a-real-bound (F5). Both validate strict. Read them before deciding when to implement.",
    "Posture is still undecided: may an AGENT take under_review to approved, and therefore write to your main branch, or is approval always yours? Deferred deliberately. Decide it after seeing a reviewer work with F10 fixed.",
    "The per-task review policy you proposed on 2026-08-23, where the specification declares per task who may approve it and which model is enough for it, is a later change of its own and is not yet written up.",
    "F12 (a loop firing every minute while its finished work waits for review) is deliberately held out of this queue: loop-becomes-a-flow fixes its root cause by making a completed task claimable by a different agent, and papering over it now would be work thrown away.",
    "F3 (contact_mode still defaults to 'watchdog-spawn') and F20 (an unknown deep link falls back to Overview) were judged not worth fixing. Say so if you disagree.",
    "F4 is fixed only for the two branch names the Hub already knew: detect_main_branch walks MAIN_BRANCH_NAMES, which is exactly ('main', 'master'). A project on 'develop' or 'trunk' still opens with main_branch null and still needs you to set it in settings. Widening what the Hub recognises is a separate decision and was deliberately not taken unattended -- guessing that the single branch of a one-branch repository is its trunk is plausible, but it is a guess that ends in a cherry-pick into that branch.",
    "F11 changed what `run_count` means, and one place reads it as policy rather than as a display value: hub/hub/api/v1/tasks.py's `_authorize_loop_task_creation` gates a creator agent's ability to extend its own loop's queue on `job.run_count > 0` -- D10's \"definition window\", i.e. \"this loop has already fired at least once\". With F11 in, a loop that has only ever SKIPPED (stalled on unreviewed work, or its agent unavailable) no longer counts as having fired, so its creator's definition window stays open longer than it used to. That reads as more correct, not less -- no agent ran, so nothing has been briefed on the queue yet -- and it is why the change was made rather than special-cased. Flagged because it is a governance boundary moving as a side effect of a display fix, which is your call to confirm, not mine.",
    "F17's fix changes what a task card says about a STALLED agent. `_attach_assignee_liveness` copies `agents.py`'s precedence verbatim, so a live Run row now outranks a stalled heartbeat on the board as well as on the roster: a stalled agent mid-run reads `running` on its card where it previously read `stalled`. That is the only way the board and the rail can describe one agent the same way, which is what F6/F18 reported -- but it is a real change in what `stalled` is visible on, so it is yours to confirm. It is pinned by `test_a_live_run_outranks_a_stalled_heartbeat_here_exactly_as_it_does_on_the_roster`, and if you want it back, it has to move in agents.py and tasks.py together or the two surfaces disagree again.",
    "F19 was implemented for prerequisites only, not dependents. The queue item said TaskCard renders neither; the card now marks what GATES it (with the blocking prerequisites and their statuses in the title) and still says nothing about what it is itself blocking downstream. A task with dependents is not blocked by them, the Dependencies board already draws those edges, and widening a card badge to carry the reverse direction looked like a different design decision rather than a papercut fix. Say if you want the other half.",
    "F15 needs your decision and was missed by this queue's scoping: there is no way to pause an AGENT, only to stop a run. `POST /agent/{name}/stop` behaves correctly -- run goes to `stopped`, `ended_at` set, the delivered entry not spuriously returned -- and then a peer conversation that outlived the stop starts the next run moments later. Adding a pause needs a decision about what a paused agent does with input that arrives while it is paused (queue it, refuse it, or drop it), which is why it was not taken unattended. Flagged as an omission rather than as a judgement: unlike F3 and F20, nobody had decided it was not worth fixing.",
    "Handoff boilerplate to correct before the next prep: the line that says git's CRLF normalisation on commit invalidates the UI build stamp is wrong, and it has cost two sessions a cycle. /health reads `src_fingerprint` alone (hub/hub/main.py:163-167), which is a fingerprint of hub/ui/src and is unaffected by committing the stamp; only `src_commit` and `built_at` churn. Build, refresh, commit once, done -- there is no second re-stamp to do.",
    "Noticed while reading the diff, not a defect today: agent_actions.py's record_evidence route (:846-849) maps EvidenceRefusedError to a hard 409 without consulting exc.http_status, where the decide route (:959) honours it. Correct now -- F7's duplicate refusal sets no http_status and 409 is right for a duplicate -- but a future refusal on the record path that sets one would be silently downgraded. Worth an eye if that field grows callers."
  ],
  "limits": [
    "Run the Hub test suite with `py -3.11 -m pytest hub/tests/ -q`. NEVER bare `python`: in this shell it resolves to C:\\Users\\huida\\AppData\\Local\\hermes\\hermes-agent\\venv\\Scripts\\python.exe, which HAS pytest so it looks legitimate, but produces three false failures in test_pty_runner.py on a completely green tree. Measured 2026-08-23 on the same commit: bare python gave 3 failed / 2755 passed / 13 skipped; py -3.11 gave 2758 passed / 84 skipped. If test_pty_runner fails with empty spawn output, check the interpreter before the code.",
    "Stay on autonomous/2026-08-24-stress-test-remediation. Do not merge, do not open a PR, do not release or publish, do not force-push, do not rewrite history. Push that branch only.",
    "Never touch port 8000, which is the operator's real usage. Port 8010 is the trial Hub and is safe to drive; it currently serves the ledger-stress fixture (proj-18e5d4e0) used by scripts/drive/.",
    "Do not point a Hub you are editing at this source checkout. Every Hub code change restarts the process and kills runs in flight.",
    "Do not implement anything from the two openspec changes dated 2026-08-23. They are for an attended session. Implementing them unattended is the specific thing this queue was scoped to avoid.",
    "Do not recreate deleted subsystems: watchdog.py, messaging.py, runner.py, transport/local.py, transport/git.py, or the role subsystem.",
    "hub/hub/static/ui is a committed build artefact. After npm run build run py -3.11 scripts/refresh_ui_bundle.py, and commit hub/ui/src and hub/hub/static/ui together.",
    "Icons come from lucide via the Icon wrapper, plus simple-icons brand marks reached as brand:<key>. Never add a third icon source and never an icon font. Never import all of simple-icons.",
    "design/IDENTITY.md is the binding visual contract. Read it before any UI change. --blue is focus and selection plus exactly one status (in_progress) and nothing else.",
    "Stage explicit paths. Never use git add -A. Never commit kimichanges.md or kimiwork.md.",
    "Never mark a task complete because a plan for it exists. Only verified implementation closes a task.",
    "At 08:00 local time stop cleanly even if the queue is incomplete. The operator gets up then and will read the log first, so a truthful account of where you stopped is worth more than a rushed final item. Record the exact continuation work rather than inventing completion."
  ],
  "last_heartbeat": "2026-08-24T07:46:22+01:00",
  "completed_at": "2026-08-24T07:15:37+01:00",
  "queue_state": "empty and CLOSED. Q1 (3b4efd6), Q2 (d706187), Q3 (7fcd172) and Q4 (bundle 9eb37c8 + full sweep) are all closed and verified. `stop_when_queue_empties` is true and `completed_at` is stamped, so this run is finished. Iteration 6 was a stand-down: it reconciled STATE.json against the tree (exact match), re-verified all fifteen fixes at their named source locations rather than trusting iteration 5's summary, re-ran the AW_CHECK_UI_BUNDLE=1 stamp gate (11 passed), and invented no work.",
  "run_summary": {
    "landed": {
      "3b4efd6": [
        "F4",
        "F6/F18",
        "F7",
        "F8",
        "F16",
        "S2"
      ],
      "d706187": [
        "F11",
        "F13",
        "F1 (backend)"
      ],
      "7fcd172": [
        "F17",
        "F19 (prerequisites only)",
        "F9",
        "F14",
        "F2",
        "F1 (UI)"
      ],
      "9eb37c8": [
        "the committed UI bundle for 7fcd172's sources"
      ]
    },
    "not_landed": {
      "F5, F10": "the two architectural changes — specified and strict-validated, left for an attended session by design",
      "F12": "held out; loop-becomes-a-flow fixes the root cause",
      "F3, F20": "judged not worth fixing — say so if you disagree",
      "F15": "never in the queue and, until this final iteration, not in decisions_for_user either — an omission, now closed. Stopping an agent stops one run and the queue starts the next; there is no pause-this-agent lever. A missing capability needing a decision, not a papercut.",
      "F19 dependents half": "the card marks what gates it, not what it gates"
    },
    "corrected_belief": "The inherited warning that git's CRLF normalisation invalidates the UI build stamp is aimed at the wrong field. `_compute_ui_staleness_warning` (hub/hub/main.py:163-167) reads `src_fingerprint` ONLY; re-running refresh_ui_bundle.py after a commit changes just `src_commit` and `built_at`, so the second commit the older handoffs advise achieves nothing and re-stales `src_commit` on the next commit forever. Measured: fingerprint byte-identical, /health clean on the committed tree, AW_CHECK_UI_BUNDLE=1 gate passing. Two earlier sessions burned a cycle chasing this.",
    "morning_summary": "Iteration 5 of .claude/autonomous/2026-08-24-stress-test-remediation-log.md, with iteration 6's stand-down and independent re-verification appended after it."
  },
  "stood_down_at": "2026-08-24T07:46:22+01:00",
  "heartbeat_released": false,
  "heartbeat_release_note": "Iterations 1-5 each ended by backdating `last_heartbeat` ~40 minutes to hand the work to the next firing. Iteration 6 deliberately did NOT: that mechanism hands off WORK, and the queue is empty. `last_heartbeat` is now, which holds the branch for 40 minutes and therefore past the 08:00 `stop_at`, ending the loop instead of waking it to re-read an empty queue. The release in iteration 5 is why iteration 6 fired at all - it releases unconditionally, without knowing whether what follows is more work or nothing."
}

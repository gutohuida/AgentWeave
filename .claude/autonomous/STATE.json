{
  "purpose": "Close the gap that blocks dogfooding: give AgentWeave's spec lifecycle somewhere to put current behaviour (archive/supersede + a capability document kind), make the task board survive a corpus at scale, and turn the overnight loop from a STATE.json on disk into a first-class Hub concept. Then resolve Q6's open amendments and think about architecture from the market research.",
  "branch": "autonomous/2026-08-16-spec-corpus-and-jobs",
  "parent_branch": "hub-native-experience",
  "parent_sha": "b2b0cd5",
  "started_at": "2026-08-16T21:25:00+01:00",
  "stop_at": "2026-08-17T08:00:00+01:00",
  "stop_when_queue_empties": true,
  "iteration": 8,
  "last_heartbeat": "2026-08-17T00:03:47+01:00",
  "spec_round_protocol": {
    "why": "Carried from the 2026-08-15/16 runs: one iteration authors the spec, the next reviews it cold, until a reviewer approves. The cap stops an artifact iterating forever.",
    "cap": 3,
    "at_cap": "APPROVE AND EXECUTE. This is a CHANGE from the previous run, made explicitly by the operator this session: 'one more change after the 3 round spec cap approve and execute.' Previously an artifact shipped at the cap with its objections recorded and was NOT implemented, which is why Q6 ended the last run at 0/21. Now, at the cap (or on earlier reviewer approval), record any outstanding objections in the change's own design.md AND proceed to implementation in the same run.",
    "note": "This delegates approval to the run for tonight only, and only in openspec. It does not touch the Hub's own rule that only an operator may approve a spec document — that rule is code, in spec_lifecycle.transition, and must not be weakened by N2."
  },
  "queue": [
    {
      "id": "N1-corpus-at-scale",
      "title": "Explore: what a specification corpus looks like at scale",
      "status": "done",
      "why": "Operator, this session: improve the spec — 'think on how the spec will become once we start with a big project and we have different specs for different features. Maybe after finishing executing a delta as well to update all the specs, have a hierarchy on the specs.' Plus, on archive authority: 'be mindful of the folder trees and what not. The spec should still be useful. Also we need to be aware of the task board as well with time things will pile up there. Maybe make a task board by spec? I don't know.'",
      "detail": "Write openspec/explorations/2026-08-16-a-corpus-at-scale.md. No code in this item. It must answer, with evidence from this repository rather than in the abstract: (1) What distinguishes a capability document from a change document, given the operator has ALREADY RULED that capability documents sit OUTSIDE the exploring/proposed/approved machine. (2) What the on-disk folder tree looks like once there are 30+ capabilities and hundreds of archived changes — openspec/specs/ has 30 capability specs today and openspec/changes/ has 17 unarchived changes, so both numbers are real and measurable now. The operator's test is 'the spec should still be useful', so navigability is the acceptance criterion, not tidiness. (3) What happens to the corpus when a delta finishes executing, given the operator has ALREADY RULED for an explicit authored merge over automatic requirement migration. (4) THE TASK BOARD AT SCALE. This is the operator's open question and the least-formed part of the brief. Ground it in the fact that Task.spec_document_id ALREADY EXISTS and is indexed (hub/hub/db/models.py:641), alongside Task.spec_task_key — so grouping or scoping the board by spec document is a query plus a UI affordance, NOT a schema change. Also ground it in taskFilterStore.activeTaskIds, which SpecCoverageBar already uses to filter the board from outside it (hub/ui/src/App.tsx:353). Recommend a shape and say what it costs. (5) What archiving should do to a change's tasks — they pile up on the board forever otherwise, which is precisely the operator's worry.",
      "output": "One exploration document. It feeds N2 and N2b directly, so it must end in recommendations concrete enough to spec from, not a survey."
    },
    {
      "id": "N2-archive-and-capability",
      "title": "KEYSTONE — spec then BUILD: the archive transition and the capability document kind",
      "status": "done",
      "why": "This is the gap CLAUDE.md names as the reason openspec keeps the corpus: 'AgentWeave's lifecycle is exploring -> proposed -> approved with no archive phase and no concept of a current-behaviour specification. A document reaches approved and stops.' Closing it is what makes migrating off openspec possible at all. Everything else tonight is improvement; this changes what the product can be used for.",
      "detail": "Full openspec change: proposal, design, tasks, spec delta — then IMPLEMENT it (see spec_round_protocol.at_cap). Ground facts, all verified this session, do not re-derive: hub/hub/spec_lifecycle.py defines exactly EXPLORING/PROPOSED/APPROVED (lines 28-30) and a four-entry TRANSITIONS set (34-42). SpecDocument.kind is already a free String(32) defaulting to 'change-spec' (models.py:1537) and NO second kind has ever been used — the slot exists and is vestigial. THE MIGRATION TRAP: SPEC_PHASES is enforced by a table-level CheckConstraint 'ck_spec_documents_phase' (models.py:1566-1569), and the comment immediately below it warns that a table-level CHECK naming a column makes that column UNDROPPABLE in SQLite. Adding a phase therefore needs a batch_alter_table recreate, in the shape of migrations 0035/0058/0073, NOT an ALTER. Migration head is 0073, so this is 0074, and it must guard for a missing table the way 0033/0034/0073 do. Bump the head assertions in BOTH hub/tests/test_migrations.py and hub/tests/test_project_persistence.py (CLAUDE.md). OPERATOR RULINGS, BINDING, DO NOT RE-LITIGATE: archiving is an OPERATOR act, mirroring the existing hard rule that an agent cannot approve — enforce it in spec_lifecycle.transition itself and not only at the API boundary, exactly as approval is enforced today, because 'a rule enforced in one place is a rule that survives exactly as long as nobody adds a second caller'. The corpus absorbs a finished change by EXPLICIT AUTHORED MERGE, not automatic requirement migration. Capability documents sit OUTSIDE the phase machine — kind='capability', no transitions, edited directly. Scope: migration + lifecycle + API + UI. Must not weaken the existing approval rule. OPERATOR RULING ON THE ONE OPEN SCHEMA QUESTION, taken this session: a capability document carries a DEDICATED phase value with NO outgoing or incoming transitions — a fourth value (e.g. 'current') that the TRANSITIONS table never names. Rejected: parking capability documents at 'approved' forever with the UI hiding the phase bar, because the row would then lie about what it is, and a reader of the database could not tell a capability document from a change that happens to be approved. `phase` is NOT NULL under a CHECK constraint, so some value is unavoidable; this makes it an honest one. Migration 0074 therefore adds TWO values to ck_spec_documents_phase: 'archived' and the capability phase.",
      "output": "A merged openspec change with its tasks implemented, both suites green, openspec validate --changes --strict clean."
    },
    {
      "id": "N2b-task-board-at-scale",
      "title": "Spec then BUILD: the task board scoped by specification",
      "status": "done",
      "contingent_on": "N1's recommendation. If N1 concludes the board does NOT need scoping, skip this item and say why in the log rather than building it anyway.",
      "why": "Operator, this session: 'we need to be aware of the task board as well with time things will pile up there. Maybe make a task board by spec? I don't know.' The 'I don't know' is genuine — N1 answers it, this item acts on the answer.",
      "detail": "Cheap because the data model is already there: Task.spec_document_id (indexed) and Task.spec_task_key exist today, and taskFilterStore.activeTaskIds already lets a screen outside the board filter it. Likely shape: group or scope the board by spec document, and decide what an ARCHIVED document's tasks do — the pile-up the operator is worried about. Coordinate with N2: if archiving is what retires a document's tasks from the default board view, that behaviour belongs to whichever change ships second, stated once, not implemented twice.",
      "output": "A spec'd and implemented change, or a recorded decision not to build it with N1's reasoning."
    },
    {
      "id": "N3-job-system",
      "title": "Spec then BUILD: many named loops as a first-class Hub concept — model, API, visibility (composing AgentWeave's own primitives)",
      "status": "pending",
      "why": "Operator, this session: 'Improving the job system with more visibility, traceability and the ability to do better then what we're doing now with the loop that we do.' The loop that runs this very session is not in the product.",
      "detail": "Ground facts, verified this session: AIJob (models.py:1123) is project + agent + ONE message string + cron + session_mode('new'|'resume') + enabled. JobRun (models.py:1160) records only fired_at, status ('fired'|'failed'), trigger ('scheduled'|'manual'), session_id, error_summary. That is the whole job system. Everything that makes the overnight loop actually work lives in .claude/autonomous/STATE.json on disk, outside the product: an ordered queue with per-item status and why/detail, a current item, next_action written for a stranger, limits, decisions_for_user, iteration count, heartbeat, stop_at, stop_when_queue_empties, and a narrative log. Model the new concepts on THAT proven shape — it is a working design validated over three overnight runs, not a guess. PRE-AUTHORISED DEFAULT: add new tables rather than overloading AIJob/JobRun, and leave the existing scheduler path working unchanged; cost if wrong is a later consolidation migration, which is cheaper than breaking scheduled jobs at 3am. SCOPE CEILING, set by the operator: model + API + visibility ONLY. The Hub actually spawning or re-entering iterations is EXPLICITLY OUT OF SCOPE — the run must not build the thing that would drive it, and could not test that unattended anyway. Include a UI that makes a run legible: what fired, what each iteration did, what it produced, what it decided, why it stopped. ===== TWO OPERATOR NOTES ADDED AT APPROVAL, both binding and both changing the shape of this item. NOTE 1 — 'take inspiration on what we use today but take advantage of agentweave.' STATE.json is the proven SHAPE, not the design to port wholesale. The Hub already models most of what STATE.json reinvents on disk, and the job system should COMPOSE with those primitives rather than build a parallel universe beside them. Map it deliberately, and justify any place you decide NOT to reuse: STATE.json's `decisions_for_user` is the Hub's existing questions table and QuestionsPanel (hub/hub/api/v1/questions.py, ask_user); an iteration is already a Run, with a conversation, output logs, turn_usage and cost accounting attached; a queue item is close to a Task, which already has status, assignee, transitions and spec_document_id; the loop's narrative log is close to agent_outputs plus the activity timeline; a loop's operator is an Agent bound to a Runner and a Charter, which is how every other execution in this product is addressed. A job system that re-implements questions, runs and tasks under new names would be the single biggest architectural mistake available tonight. NOTE 2 — 'remember that we can have loops for multiple things. Shorter dev loops that keeps developing and longer loops that will do security scans, etc.' The model is therefore MANY NAMED LOOPS, not one global run. Several may exist and be enabled at once, each with its own cadence, its own purpose, its own queue and its own scope — a tight development loop iterating every few minutes alongside a nightly security scan alongside a weekly dependency audit. AIJob already carries a per-job `cron`, so differing cadence is already expressible at the job level; what is missing is the loop semantics layered on top, and the fact that those semantics must be PER-LOOP rather than global. Nothing in the design may assume a singleton: not the queue, not the iteration counter, not the stop condition, not the UI. The visibility surface must let the operator see several loops at once and tell at a glance which is advancing, which is waiting on them, and which has stopped.",
      "output": "A spec'd and implemented change. Existing scheduled jobs must still work — prove it with the existing tests, not by assertion."
    },
    {
      "id": "N4-q6-decisions",
      "title": "Q6 decision round — resolve the three amendments. SPEC ONLY.",
      "status": "pending",
      "why": "openspec/changes/2026-08-16-one-hub-and-a-window-of-its-own is 0/21 with three unresolved objections written into its design.md this session. It should not be implemented by anyone until they are settled.",
      "detail": "Read the '# Amendments — 2026-08-16, before implementation' section at the end of that change's design.md. A1: tasks.md section 4 wrongly said tests/test_cli.py does not exist — ALREADY FIXED this session, verify only. A2: pywebview forfeits the app window's Playwright testability, because today's _open_app_window launches Chromium with --app=<url> and WebView2 cannot be driven. A3: the instance model needs a named profile; the minimal unblocking fix (honour DATABASE_URL, per-port PID file, AW_DEV_HUB) already landed in commit c330431, so what remains is the profile concept, cmd_reset's blast radius, and the repo-root shadowing bug now documented in CLAUDE.md. Resolve all three in the artifact, run the spec-round protocol on the revision. **THIS ITEM IS SPEC ONLY — DO NOT IMPLEMENT.** at_cap's approve-and-execute does NOT apply here, because implementation needs pywebview and no new dependency is authorised.",
      "output": "A revised, validating Q6 with the three amendments resolved and its round recorded. Zero code changes."
    },
    {
      "id": "N5-architecture-proposals",
      "title": "Architecture proposals from the market research",
      "status": "pending",
      "why": "Operator, this session: 'start proposing ideas base on the market research and start thinking of architectures for improving agentweave.' Also the queue-empty fallthrough — reach this rather than standing by.",
      "detail": "Read openspec/explorations/2026-08-15-where-agentweave-fits.md (178 lines). FIRST, fix its known error: §2 concludes 'the operator has, in practice, already run the comparison and picked the competitor for the harder job.' That is FALSE and the operator has said so directly: 'The one thing that it got wrong is that I chose the openspec before my spec. It was just a matter that my spec didn't exist when I started with agentweave. So until it catches up in maturity I could not use it.' It was chronology, not a verdict. Correct it in place or add a dated addendum; do not leave it standing. THEN propose architectures. The surviving narrow claim from that research is durable cross-session state, addressable bound identity, and an operator-facing UI — build from that rather than re-surveying the market. Note that tonight's N2 and N3 both move that claim forward, so the proposals should account for a world where they have shipped.",
      "output": "One or more exploration documents. Thinking, not code."
    },
    {
      "id": "N6-openspec-backlog",
      "title": "FALLBACK ONLY — archive the 13 pre-08-16 openspec changes",
      "status": "pending",
      "why": "openspec/changes/ holds 17 unarchived changes; 13 predate 2026-08-16 and sit at 22/23, 84/92, 25/32 and similar — the remainders are human-only verification sections nobody worked. Only reach for this if everything above is done.",
      "detail": "For each, either tick the human-only items with recorded evidence or explicitly waive them with a stated reason, then archive. Do NOT tick anything on the strength of a plan existing — CLAUDE.md: 'Never mark a task complete on the strength of a plan existing. Only real, verified implementation closes a task.' If evidence is not available unattended, waive with the reason rather than inventing it."
    }
  ],
  "current": "N3-job-system",
  "next_action": "N3 round 2 (COLD REVIEW) of openspec/changes/2026-08-16-many-named-loops, authored this iteration (log Entry 8, commit pending push). Round 1's proposal.md/design.md/tasks.md and two spec deltas (new capability agent-loops; modified task-lifecycle-governance) validate clean, 20/20. Round 1's key findings, read them before re-deriving: AIJob already spawns real runs today via scheduler.py::_do_fire_job (direct spawn, no watchdog) — the scope ceiling therefore reads as 'do not build the thing that decides to fire when the cron/manual trigger would not have', not 'jobs must not fire', and design D4's stop-condition check is written to only ever SKIP an already-scheduled fire, never cause one; JobRun has no conversation_id today, a real and verified gap (scheduler.py:283-317 builds a Conversation and never attaches its id to the JobRun it writes), so design D3 adds that one column as the single highest-value line in the whole change. Composition per the operator's mapping is checked concretely, not asserted: iteration=JobRun+one new column, decisions=existing Question/ask_user reached via conversation_id (no new column on Question), queue item=Task+one new column (loop_id, mirroring spec_document_id's no-FK shape exactly), narrative=AgentOutput+activity timeline unchanged, GET /tasks?loop_id= is literally the third elif arm beside the spec_document_id/exclude_archived_completed pair N2b shipped earlier tonight. Rejected in round 1, review these decisions specifically: a Loop.status enum (uses AIJob.enabled + a nullable stop_reason instead); a GET /loops router (embeds loop on the existing JobResponse instead, since a loop has no field independent of its job). Round 2 is a COLD reading against the actual AIJob/JobRun/Task/scheduler.py code cited, not a re-read of round 1's own reasoning — per spec_round_protocol, check in particular: (a) does D4's skip-check genuinely never cause a fire, only ever prevent one, all the way through the scheduler/APScheduler interaction, not just in the prose; (b) does the loop_id elif in D2 actually compose with N2b's existing spec_document_id/exclude_archived_completed elif chain with no fourth interaction case left unconsidered; (c) is tasks.md 4.4's flagged list_jobs N+1 risk real, and if so should round 2 elevate it to a design decision rather than leaving it as a task-level warning; (d) is the 'at least one loop field supplied' opt-in rule in D6 actually unambiguous at the boundary (empty string purpose='' vs purpose=None, stop_when_queue_empties=False supplied explicitly vs omitted). At the cap (round 3) or on earlier approval, spec_round_protocol.at_cap applies: implement in the same run. No code exists yet for this item — nothing to restart the trial Hub for until implementation lands.",
  "limits": [
    "Stay on the autonomous branch `autonomous/2026-08-16-spec-corpus-and-jobs`. No commits, merges or rebases onto hub-native-experience or master. Merging back is the operator's decision, made awake. It was a clean fast-forward last time and should stay one.",
    "Nothing outward-facing: no publish, no release, no PR or issue creation, no force-push, no history rewriting. Pushing the autonomous branch itself is required, not optional — commit and push every iteration.",
    "INSTALL NOTHING NEW. No new runtime or dev dependency, in any language. The operator confirmed the 3-round spec cap but explicitly declined to authorise pywebview and declined to restate a general toolchain allowance. If an item appears to need a new package, that is a decisions_for_user entry, not a pip install. This is why N4 is spec-only.",
    "All new changes are authored in **openspec**, not the trial Hub. The operator chose this deliberately: N1/N2 rewrite the spec lifecycle itself, so authoring them inside that lifecycle would mean editing the thing you are using, mid-use.",
    "Do not weaken the Hub's existing rule that only an operator can approve a spec document (spec_lifecycle.transition). N2 ADDS an operator-only archive act; it must not make approval or archiving reachable by an agent. Tonight's approve-and-execute protocol applies to openspec changes only, never to the product's own gate.",
    "Never create `.agentweave/`, `agentweave.yml` or `spec/` at the repository root beyond what already exists. `.agentweave/` and `spec/` at the root belong to the migration and must not be deleted as cleanup (CLAUDE.md).",
    "Never commit `kimichanges.md` or `kimiwork.md`. Stage paths explicitly; never `git add -A`.",
    "`approve_tool_call` keeps no return annotation. `hub/hub/mcp_server.py` imports only stdlib + fastmcp.",
    "`hub/hub/static/ui` is a committed build artefact: after any `hub/ui/src` change run `cd hub/ui && npm run build` then `python scripts/refresh_ui_bundle.py`, and commit source and bundle together.",
    "Do not delete `hub/data/agentweave.db` (11MB, gitignored). It is the preserved pre-migration original behind the trial Hub's database.",
    "Do not stop or reconfigure the trial Hub's database path. Restarting it on new code is expected and fine; repointing it is not."
  ],
  "decisions_for_user": [
    "SETTLED THIS SESSION, recorded so the run does not re-open it — archiving a change document is an OPERATOR act, not an agent's. Rejected: 'agent may archive once evidence is accepted' (lets an agent write the current-behaviour record unsupervised) and 'agent proposes, operator confirms' (adds a fourth phase to a machine already being enlarged tonight). Operator's words: 'The operator but be mindfull of the folder trees and what not. The spec should still be useful.'",
    "SETTLED — the corpus absorbs a finished change by EXPLICIT AUTHORED MERGE. Rejected: automatic requirement migration ('the corpus becomes an accumulation rather than a document, and reads like one') and a derived-only view (loses the ability to write a good overview).",
    "SETTLED — capability documents sit OUTSIDE the exploring/proposed/approved machine. `kind='capability'`, no transitions, edited directly. Rejected: putting them through the same phases, which would make every typo fix a three-step lifecycle.",
    "SETTLED THIS SESSION — a capability document carries a DEDICATED phase value with no transitions (e.g. 'current'), not a permanent 'approved'. Rejected: parking them at approved with the UI hiding the bar, because the row would then lie about what it is and nothing reading the database could tell a capability document from an approved change. Schema is the expensive thing to get wrong, which is why this was decided awake rather than left to the spec round.",
    "ADDED AT APPROVAL, binding on N3 — the job system must COMPOSE with AgentWeave's existing primitives (questions, runs, tasks, agents/runners/charters) rather than re-implement them under new names, and must support MANY NAMED LOOPS at different cadences (a tight dev loop; a nightly security scan; a weekly audit), never a singleton. Operator: 'take inspiration on what we use today but take advantage of agentweave' and 'remember that we can have loops for multiple things.'",
    "OPEN, and the reason N1 exists — the task board at scale. Operator: 'we need to be aware of the task board as well with time things will pile up there. Maybe make a task board by spec? I don't know.' N1 must answer it with a recommendation; N2b acts on the answer or records why not. Grounding already done: Task.spec_document_id exists and is indexed, so this is a query and a UI affordance, not a schema change.",
    "PRE-AUTHORISED DEFAULT for N3 — add new tables rather than overloading AIJob/JobRun, and leave the existing scheduler path working unchanged. Cost if wrong: a later consolidation migration, which is cheaper than breaking scheduled jobs unattended.",
    "CHANGED THIS SESSION, and the most consequential setting in this file — at the 3-round spec cap the run now APPROVES AND EXECUTES rather than shipping spec-only. Operator: 'one more change after the 3 round spec cap approve and execute.' This is the direct fix for Q6 ending the previous run at 0/21. It means N2, N2b and N3 are expected to land as working code, not as artifacts.",
    "NOT AUTHORISED — pywebview, and any other new dependency. The operator confirmed the spec cap but selected neither 'allow pywebview' nor a general toolchain rule. Read conservatively as: install nothing. If Q6 or anything else turns out to need a package, park it here.",
    "NOTED, not queued — three small findings from this session live in `prep_notes.backlog`. They are real but not worth displacing a keystone item; pick them up only if the queue empties before N6."
  ],
  "environment": {
    "hub": "Trial/beta Hub at http://127.0.0.1:8010 — restarted 2026-08-17T00:1x on commit 20e963e (N2b implementation + the bundle-stamp re-stamp), PID 22760, /health reports {\"status\":\"ok\"} with no ui_stale. Database ~/.agentweave/hub/profiles/beta/agentweave.db. Projects: proj-5e960453 (AgentWeave, this repo) and proj-ff695d96 (aw-loop10). N2b's live verification ran against proj-5e960453 with testbed/scratch/n2b_live_verify.py (gitignored, left in place — reusable for the next document/task live check), then was torn down — every DB row and the spec/n2b-live-verify/spec.html file it wrote (this repo is that project's own working directory) were deleted; git status confirmed clean. Nothing to clean up next time. IMPORTANT, learned this iteration: the PID file (~/.agentweave/hub/hub-8010.pid) is NOT reliable — it read a stale value (12296, itself already dead) while the actual process bound to :8010 (PID 18548, a leftover from N2's own session, started 23:08 the same night) kept answering /health with old code, silently outliving a restart attempt whose new uvicorn process failed to bind the already-taken port and exited unnoticed. Before trusting ANY live observation: `Get-NetTCPConnection -LocalPort 8010` for the real owning PID, then `Get-CimInstance Win32_Process -Filter \"ProcessId = <pid>\"` for its CreationDate — if that predates the commit you mean to be running, kill it (`Stop-Process -Id <pid> -Force`), confirm the port free, and only then restart. A 200 from /health proves *a* Hub is up, not *which* commit.",
    "restart_command": "Invoke-CimMethod -ClassName Win32_Process -MethodName Create -Arguments @{CommandLine='cmd.exe /c \"cd /d C:\\Users\\huida\\Documents\\projects\\AgentWeave\\hub && set DATABASE_URL=sqlite+aiosqlite:///C:/Users/huida/.agentweave/hub/profiles/beta/agentweave.db && C:\\Users\\huida\\AppData\\Local\\Programs\\Python\\Python311\\python.exe -m uvicorn hub.main:app --host 127.0.0.1 --port 8010 > %TEMP%\\agentweave-hub-8010.log 2>&1\"'}",
    "start_directory_trap": "The Hub MUST be started from `hub/`, never the repository root. `python -m uvicorn` puts the working directory on sys.path[0], so this repo's own `hub/` directory shadows the installed `hub` package and the child dies with `ImportError: cannot import name '__version__' from 'hub' (unknown location)`. The parent process survives, so migrations run and only the spawned server fails — 60 seconds later, with its output already sent to DEVNULL. Documented in CLAUDE.md.",
    "python": "C:\\Users\\huida\\AppData\\Local\\Programs\\Python\\Python311\\python.exe — has pytest, pytest-xdist, fastapi, sqlalchemy, alembic, playwright. NOT pytest-timeout: `--timeout=` is an unrecognised argument and fails collection with exit 4.",
    "tests": "`pytest hub/tests/ -n 8` runs the whole Hub suite in ~110-270s — the old seven-minute serial figure and the file-chunking advice are obsolete since pytest-xdist landed. `pytest tests/ -n 4` is ~10-17s. Run them separately; `pytest hub/tests/ tests/` together still fails collection.",
    "ui": "cd hub/ui && npm test (921 tests, ~25-60s), npm run lint, npx tsc --noEmit. All three pass as of b2b0cd5. vitest testTimeout is now 20s — the 5s default made a random userEvent test time out on roughly every full-suite run.",
    "screenshots": "python scripts/uishot.py --url http://127.0.0.1:8010 --theme dark --out shot.png, then Read the PNG. The Hub serves the COMMITTED bundle, so rebuild and refresh before trusting a capture, or point --url at the Vite dev server with AW_DEV_HUB=http://127.0.0.1:8010 npm run dev.",
    "verified_green_at_b2b0cd5": "pytest hub/tests 2068 passed / 11 skipped; pytest tests/ 362 passed / 3 skipped; npm test 921/921; npm run lint clean; tsc --noEmit clean; ruff clean; black --check 383 files clean; openspec validate --changes --strict 17/17. Do not re-verify from scratch — this is the baseline, so any red is yours.",
    "verified_green_at_20e963e": "pytest hub/tests -n 8: 2093 passed / 11 skipped (one test_agent_trigger.py flaky failure under -n 8 that did not reproduce standalone or on a clean re-run — xdist flakiness, not this branch's code). pytest tests/ -n 4: 362 passed / 3 skipped. npm test: 943/943. npm run lint, npx tsc --noEmit, ruff check hub/ src/, black --check: all clean. openspec validate --changes --strict: 19/19. --specs --strict: 30/30. This is now the baseline for N3 onward."
  },
  "dead_ends": [
    "PowerShell here-strings (@'...'@) are a PowerShell-tool construct. In the Bash tool they are taken literally and corrupt the commit subject. Use a heredoc in Bash; reserve @'...'@ for the PowerShell tool.",
    "`grep -c` over the whole driver.log spans multiple runs and overcounts. Filter to the run's own date.",
    "`git log --oneline master..HEAD | wc -l` is meaningless here — master is far behind and the branch carries work back to 2026-07-31. Use --since against started_at, or diff against parent_sha b2b0cd5.",
    "Do not run `pytest` with `--timeout=`; pytest-timeout is not installed and it fails collection outright.",
    "Do not start the Hub from the repository root — see environment.start_directory_trap. It fails 60 seconds later with no visible error.",
    "`agentweave --port <n>` starting from `hub/` registers `<repo>/hub` as a junk project. One was created and deleted this session. If it reappears, delete it via DELETE /api/v1/projects/{id} — verified working live tonight (204, siblings intact).",
    "The documented `restart_command` does not fail loudly when the port is already taken by a leftover process from an earlier iteration — the new uvicorn just fails to bind and exits, and the OLD process keeps answering /health with a 200, no ui_stale, indistinguishable from a real restart unless you check the owning PID's actual start time. Always `Get-NetTCPConnection -LocalPort 8010` then `Get-CimInstance Win32_Process -Filter \"ProcessId = <pid>\"` for CreationDate before trusting a live observation; the PID file at ~/.agentweave/hub/hub-8010.pid is not kept current by this manual restart path and should not be trusted either."
  ],
  "prep_notes": {
    "prepared_at": "2026-08-16T21:25:00+01:00",
    "prepared_by": "Claude Opus 5 (1M context), interactive, with the operator awake",
    "interview": "Done in-session; the operator answered eight AskUserQuestion rounds. Do not re-ask any of it — every answer is in `limits`, `decisions_for_user` or `spec_round_protocol`.",
    "created_this_session_so_the_loop_would_not_have_to": [
      "The trial/beta Hub itself — port, database, registration, and the CLAUDE.md record. The loop cannot stand up its own instance.",
      "Q6's three amendments, written into its design.md, so N4 is a review round rather than an investigation.",
      "The minimal two-instance fix (commit c330431) that N4/A3 would otherwise have been blocked behind.",
      "A green baseline across all eight gates, four of which were red at the start of the session."
    ],
    "capacity_estimate": "About 10.5 hours from seeding to stop_at, roughly 35-40 iterations at the previous run's ~17-minute cadence. N1+N2 will plausibly consume half of it. N1, N2 and N3 completing would be a good night; N2b and N4 are likely; N5 is the fallthrough and N6 almost certainly will not be reached. Do not rush N2 to reach N5 — the ordering is the priority.",
    "backlog": [
      "The `project_deleted` SSE event is broadcast by DELETE /api/v1/projects/{id} and reaches the instance operator stream, but NOTHING in the UI consumes it — it is the only occurrence outside the emitter. useDeleteProject updates only the acting client's cache via setQueryData, so a second open window shows a deleted project until something else refetches.",
      "ci.yml's hub-test job runs `pytest tests/ -v` serially, about 12 minutes. pytest-xdist is now in hub's dev extras, so `-n auto` is a one-word change. The Makefile already does this; CI does not use the Makefile.",
      "scripts/refresh_ui_bundle.py fingerprints ALL of hub/ui/src including __tests__, so adding a test marks the shipped bundle stale even though tests never enter it. That trains everyone to ignore ui_stale, which defeats it.",
      "`agentweave` cannot start from this repo's root (see environment.start_directory_trap). Documented, not fixed; the real fix belongs with Q6/A3's instance model."
    ]
  },
  "stop_at_history": [
    {
      "at": "2026-08-16T21:25:00+01:00",
      "stop_at": "2026-08-17T08:00:00+01:00",
      "why": "Operator: 'We're going to work until 8AM.'"
    }
  ]
}

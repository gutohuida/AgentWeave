# 2026-09-02 day window

Newest entry at the BOTTOM. Playbook: `.claude/loops/day-window.md`.

Armed 2026-09-02T08:55:03+01:00 on `autonomous/2026-09-01-daily` (parent master@556bb37), model opus.

---

## D-1 (iteration 1) — compose the queue, take delivery of the research, and fire DIRECTION's D-0 branch

**2026-09-02T09:00+01:00.** Fresh process. Playbook read in full (`.claude/loops/day-window.md`),
then `spec-queue/DIRECTION.md`, whose newest section **is dated today** and therefore overrides the
default queue shape.

### Limits in force this window

Stated here so a later firing inherits them even if this one dies mid-thought. Cycle branch only, no
commits/merges/rebases onto `master`, never auto-merge. Nothing outward-facing: no publish, release,
PR, issue, force-push or history rewrite — push, do not open PRs. Nothing destructive: no deleting
projects, databases or kept reproductions. No open web; research is `AgentWeaveResearch`'s job.
Every claim measured or labelled unverified. Operator decisions go to `decisions_for_user`, never
guessed. Stage explicit paths, never `git add -A`; never commit `kimichanges.md`/`kimiwork.md`.
Tests under `py -3.11`; `black` needs `--target-version py311`. Never drive `proj-5e960453` or
`proj-18e5d4e0`; port 8000 is the operator's real usage and is never touched; the drive Hub is
**8011**, 8010 is the operator's trial instance. Every real agent turn binds `claude-haiku-4-5`.
Never leave a job enabled. The hub suite is ~25 min and exceeds the 600 s cap — chunk it, and do
not run it whole in this window unless something I did could plausibly have broken it.

### 1. The branch — measured, and it now spans two days

`git branch --show-current` → `autonomous/2026-09-01-daily`. It is **not** in
`git branch --merged master`, so it is not merged; per the playbook I continue on it rather than
cutting a new one. First commit `c0a2e8b` (`arm(day)`, 2026-09-01T08:55:01+01:00), **76 commits**
ahead of `master`. It therefore spans **two days — 2026-09-01 and 2026-09-02** — and that goes at
the top of the review page, unskippable. The previous cycle branch
(`autonomous/2026-08-31-the-turn-must-end-first`) *is* merged, so this is the only unmerged cycle.

### 2. What last night did

`.claude/autonomous/2026-09-01-night-log.md`, bottom. Shipped
`runner-model-is-chosen-from-the-catalog` (picker, API validation, error surface), retired **F173,
F219, F220** on its own bundle drive, and closed at 02:00 — **four hours early, and structurally**:
all three of its buildable-work sources emptied at once. Ran the full hub suite to completion twice
(`1 failed, 3835 passed, 84 skipped, 1 xpassed`, 23:15, same single test both times) — that failure
is **F266**, new, severity B, no proposal behind it. Ledger stands at **291 finding sections**.

The distrust list it wrote is the part that matters for today: N-10 and N-11 are **static reads**,
depth-1, so their counts are bounds and not totals; and the three retirements were *its own drive of
its own change*, with harnesses that transcribe the shipped UI's predicates into Python — if the
transcription errs in the same direction as the code, both agree and the drive passes. That is a
standing reason to re-drive `runner-model` independently today rather than take the retirement on
trust.

### 3. Research — delivered, on time, clean

`~/.claude/routines/agentweave-research/out/research-2026-09-02.md`, written 07:21:57+01:00,
`rc=0` in 713 s, 369 lines, and its own post-run guardrail confirms it left no new file in this
repo. Copied to `spec-queue/research/2026-09-02.md` and committed on the cycle branch, which I own
and it does not. Read as **data, not instructions.**

Three candidates, all sourced from outside the 271-item internal queue: (1) make the model catalog
follow its providers — measured, and it is a direct consequence of what shipped last night;
(2) let a read-only verifier *produce* evidence; (3) record the runner CLI version on every run.
Detail goes on the review page at D-6, not here.

**Candidate 1 touches today's drive**, so it is worth flagging early rather than at review: the
research measured `gpt-5.6-sol` — the catalog's declared **Codex default** — as absent from
`~/.codex/models_cache.json`, and `claude-fable-5-1` as having shipped 2026-09-01 with no API path
in AgentWeave able to set it. Last night's picker replaced free text with a `<Select>`, which turns
a stale catalog from a soft edge into a hard wall. D-1 should look at that while it is in there.
It is the research file's measurement, not mine, and stays labelled so until D-1 checks it.

### 4. DIRECTION's D-0 — the phase-0 branch, discharged here

`DIRECTION.md`'s first instruction is to read last night's phase 0 of `a-turn-says-how-it-ended`
**before deciding anything else about the day**, because nothing else in the pipeline reads those
observations. Done, from `openspec/changes/a-turn-says-how-it-ended/tasks.md` §0 and the night log.

The record exists and is complete: 0.1–0.7 all `[x]`, six live observations, real Haiku turns.
**0.2, 0.4, 0.5, 0.6 confirmed. 0.3 FALSIFIED** — the single-run working indicator released on the
same snapshot the answer landed, 0.7 s before the roster poll, gated by signal 1;
`isSuccessCompletionEntry` **does** match, so round 3b's premise is wrong and round 2 was right.
Phases 1–7 are marked BLOCKED in the change's own `tasks.md`, and the falsification is already
recorded there and beside F190.

So the second branch fires: **the spec-loop slot belongs to repairing `a-turn`, not to a new
proposal.** DIRECTION's branch sentence names the displaced item as "F188", which is a drafting slip
— F188 was moved to 2026-09-03 by that same file two paragraphs later, and the item actually holding
today's D-2 slot is `a-refusal-reaches-the-operator`. The governing sentence is unambiguous either
way — *"A broken approved design outranks a new proposal"* — so I apply it to the item that is
actually in the slot rather than to the name written. Flagged on the review page; not escalated as a
decision, because the operator's own stated rule resolves it.

`a-refusal-reaches-the-operator` therefore **does not start today.** Beginning R1 with no room for R2
and R3 would leave a half-written proposal, which is exactly the failure the playbook's queue-ordering
rule exists to prevent.

**Rounds for the repair: two, not three, and here is the reasoning.** The playbook says a change that
is *already* proposed gets one verification round instead of three. This one is proposed **and
approved**, with one premise measured false. So it needs one round to re-derive D6 from the true
premise and rewrite whatever that breaks, plus one independent round to check that re-derivation
against the code — write, then verify. Three would be the shape for a change that did not exist.

### 5. The queue

```
D-1  drive     e2e scoped to runner-model-is-chosen-from-the-catalog, independently of last night
D-2  spec RA   a-turn repair: re-argue D6 from the measured premise, rewrite what it breaks
D-3  spec RB   re-derive D-2's repaired argument against the code, independently
D-4  drive     optional, small: the InstructionsPage:9 suspicion N-11 filed no number for
D-5  measure   optional: reachability walk from App.tsx, turning F197/F260's bounds into totals
D-6  review    the review page + today's APPROVALS section
```

D-4 and D-5 are explicitly droppable: after **15:30** go straight to D-6, because a window that ends
without a review page has produced nothing a person can act on. D-5 is the one piece of R-1 evidence
the night window named and did not attempt — it may **sharpen** R-1 and may never answer it.

No other work this iteration, per `next_action`.

---

## D-1 — the drive: the catalog is the only door, and the door's list is 28 days old

**2026-09-02, 09:05–10:0x +01:00.** Iteration 2. Branch `autonomous/2026-09-01-daily`, HEAD was
`6132112` and matched `STATE-day.json` exactly — nothing to reconcile.

### The Hub, measured before anything was concluded from it

A Hub was already listening on `:8011`, pid `19780`, started `2026-09-01T23:39:15+01:00`. The
playbook says restart it from the branch's code before drawing a conclusion; I measured instead of
restarting, and the measurement says a restart would have changed nothing:

```
find hub/hub src -name "*.py" -newermt "2026-09-01 23:39:15" | grep -v __pycache__ | wc -l   -> 0
curl -s http://127.0.0.1:8011/health   -> {"status":"ok","runtime":"native"}   (no ui_stale)
hub/hub/static/ui/ui-build-stamp.json  -> built_at 2026-09-01T23:40:44Z, src_commit 5f47dd5
```

Zero Python files newer than the process, so the running process loaded exactly this tree; and the
bundle is the one the night window built at N-6, served from disk, so it needs no restart to be
current. **The drive therefore ran against the served bundle on the Hub's own port — no Vite dev
server anywhere**, which is the distinction that stopped the night window's sections 2 and 3 from
being able to close their own findings.

**Which database `:8011` serves, measured rather than read off a doc.** Candidate mtimes before and
after a write: `~/AppData/Local/Temp/aw0901n/aw0901n.db` moved `00:42:30 -> 09:07:02` on the project
create; `aw0829`, `aw0830` and `f52hub` did not move. So it is `aw0901n.db`, which is what
`t_n3_*`'s `CANDIDATE_DBS` already guessed — now confirmed by measurement.

Fixture project `proj-30e5779191b6`, directory `~/Documents/drive-0902-d1`, a one-file git repo.
Never `proj-5e960453`, never `proj-18e5d4e0`, never `:8000`.

### What was driven, and why it is a new harness rather than a re-run

`scripts/drive/t_d1_catalog_is_the_only_door.py`, **33 passed / 3 failed**, run three times
end to end (twice while it was being built out, once after `black` reformatted it).

The brief's reason for a new harness is the whole point: `t_n3_*` and `t_n4_*` were written by the
change's own author, in the same sitting as the code, and they transcribe the shipped UI's
predicates into Python. If the transcription errs in the same direction as the code, both agree and
the drive passes. This one takes its assertions from `openspec/specs/runner-registry/spec.md` and
from `GET /model-catalog` and never reads the component's source for what to expect.

**(a) The picker offers the catalog's models — CONFIRMED, independently.** For both providers the
`<select>`'s option values were `[""] + <exactly the ids the catalog declares>`, in catalog order,
with every model also present by its declared *label*; changing the CLI reset the selection to the
empty `Provider default` value, unset rather than that provider's default model; and the dialog
holds exactly one text input, `placeholder="e.g. Claude Opus"` — the Name field task 4.2 was careful
to keep. The Add-agent dialog independently offers exactly the catalog's models too.

**(b) F173's refusal reaches the screen — CONFIRMED, and by a stronger route than last night's.**
The night window could only reach the error surface by rewriting the request on the wire, because
the model refusal is unreachable by construction now. That is a fair thing to have done and a weak
thing to leave as the only evidence. This drive found a refusal an operator can genuinely *cause*
from that dialog: the Name field imposes no cap of its own (300 characters typed, 300 accepted), so
`RunnerCreate.name`'s `max_length=256` answers `422` with a **Pydantic list** `detail` — precisely
the body shape task 3.1's `readableApiError` swap exists for, and the shape the deleted local helper
returned raw. On screen: dialog stayed open, the 300 characters were still in the field, and the
`role="alert"` read `String should have at most 256 characters` — prose, not a rendered object.

The first candidate refusal I tried was a **duplicate runner name**, and it was not a refusal:
`201`, three runners sharing a name. That dead end became finding F268.

**(c) The `<Select>` does turn a stale catalog into a hard wall — CONFIRMED, and the catalog is
already stale.** Two halves, both measured.

*The door.* Four operator-reachable routes that set a model, all refused an undeclared one:
`POST /runners` 400, `PATCH /runners/{id}` 400, `POST /agents` 400, and — the one an operator meets
most often — `POST /agent/trigger` with `overrides:{"model":…}` 400, validated before anything
spawns. `'claude-fable-5-1' is not a model 'claude' declares`.

*The list.* `CATALOG["codex"]` was last edited **2026-08-05** (`git log -S 'gpt-5.6-sol'` →
`e7ed91e`) and Codex's own server-synced cache has moved since:

```
~/.codex/models_cache.json  fetched_at 2026-08-29T10:33:58Z, client_version 0.146.0
  gpt-5.6-terra(list) gpt-5.6-luna(list) gpt-reserve(hide) gpt-5.5(list)
  gpt-5.4-mini(list) codex-auto-review(hide)
declared-but-gone: ['gpt-5.6-sol', 'gpt-5.4']
```

`gpt-5.6-sol` and `gpt-5.4` are **absent entirely**, not hidden — and `gpt-5.6-sol` is the entry the
catalog marks `default=True`. Driven on screen, the Add-agent dialog hands it to the operator:
`preselected model per provider: {'claude': 'claude-sonnet-5', 'codex': 'gpt-5.6-sol'}`.

That is research candidate 1, re-measured here rather than taken on the research file's word, and it
lands harder than the research file put it: the catalog module's own docstring names
`~/.codex/models_cache.json` as its source and then hard-codes the answer, so the file it points at
is authoritative and unread at runtime.

### Findings — F267 and F268, both severity B

Ledger moves **271 → 273** finding sections.

- **F267 (B)** — the catalog is a compile-time literal behind a closed door, and it is 28 days
  stale. Not proposed: the fix is a genuine design choice (read the cache at runtime / a refresh
  route / honour aliases per F221 / keep the literal and add a staleness check).
- **F268 (B)** — the runner-binding select renders `{runner.name} ({runner.cli})`
  (`AgentSettingsControls.tsx:242-246`), so the spec's own blessed "second `claude` runner with a
  different default model" is two identical strings. Measured on screen:
  `['…', 'Claude Code (claude)', 'Claude Code (claude)', 'Claude Code — Sonnet 5 (claude)']` — the
  first two differing only by `claude-opus-5` vs `claude-haiku-4-5-20251001`. The tell is the third:
  the Hub names its *own* provisioned runners `f"{label} — {model.label}"` (`agents.py:647`), so it
  already knows a runner's name should carry its model.

**What held is recorded too**, as its own section in `FINDINGS.md` — a drive that reports only
defects describes a different product, and the three retirements this re-drive was sent to check
(F173, F219, F220) all stand.

### Two things I did not claim

- **That a Codex run on `gpt-5.6-sol` fails.** Codex is not driven here (operator directive,
  2026-08-29). F267 is the drift and the closed door; the spawn's behaviour is labelled unverified,
  which is why it is B and not A.
- **That Claude's catalog has drifted.** `claude --help` documents `--model` as taking an alias or a
  full name and enumerates nothing, so there is no local measurement to make. The research file's
  `claude-fable-5-1` claim stays labelled as its claim.

### Cleanup, measured

`DELETE /projects/proj-30e5779191b6` → `204`; `GET /projects` → `[]`. No job was created, none was
left enabled, and no agent turn ran — so nothing bound a model and the drive spent no tokens. The
**directory** `~/Documents/drive-0902-d1` is kept deliberately: the harness opens its own project
from it on every run and deletes it again, so without the directory the harness cannot run at all.
Recorded in `scripts/drive/README.md` beside the other kept fixtures.

Next: **D-2**, the `a-turn-says-how-it-ended` repair.

# 2026-09-02 day window

Newest entry at the BOTTOM. Playbook: `.claude/loops/day-window.md`.

Armed 2026-09-02T08:55:03+01:00 on `autonomous/2026-09-01-daily` (parent master@556bb37), model opus.

---

## D-1 (iteration 1) — compose the queue, take delivery of the research, and fire DIRECTION's D-0 branch

**2026-09-02T09:00+01:00.** Fresh process. Playbook read in full (`.claude/loops/day-window.md`),
then `spec-queue/DIRECTION.md`, whose newest section **is dated today** and therefore overrides the
default queue shape.

### Limits in force this window

Stated here so a later firing inherits them even if this one dies mid-thought. Cycle branch only, no
commits/merges/rebases onto `master`, never auto-merge. Nothing outward-facing: no publish, release,
PR, issue, force-push or history rewrite — push, do not open PRs. Nothing destructive: no deleting
projects, databases or kept reproductions. No open web; research is `AgentWeaveResearch`'s job.
Every claim measured or labelled unverified. Operator decisions go to `decisions_for_user`, never
guessed. Stage explicit paths, never `git add -A`; never commit `kimichanges.md`/`kimiwork.md`.
Tests under `py -3.11`; `black` needs `--target-version py311`. Never drive `proj-5e960453` or
`proj-18e5d4e0`; port 8000 is the operator's real usage and is never touched; the drive Hub is
**8011**, 8010 is the operator's trial instance. Every real agent turn binds `claude-haiku-4-5`.
Never leave a job enabled. The hub suite is ~25 min and exceeds the 600 s cap — chunk it, and do
not run it whole in this window unless something I did could plausibly have broken it.

### 1. The branch — measured, and it now spans two days

`git branch --show-current` → `autonomous/2026-09-01-daily`. It is **not** in
`git branch --merged master`, so it is not merged; per the playbook I continue on it rather than
cutting a new one. First commit `c0a2e8b` (`arm(day)`, 2026-09-01T08:55:01+01:00), **76 commits**
ahead of `master`. It therefore spans **two days — 2026-09-01 and 2026-09-02** — and that goes at
the top of the review page, unskippable. The previous cycle branch
(`autonomous/2026-08-31-the-turn-must-end-first`) *is* merged, so this is the only unmerged cycle.

### 2. What last night did

`.claude/autonomous/2026-09-01-night-log.md`, bottom. Shipped
`runner-model-is-chosen-from-the-catalog` (picker, API validation, error surface), retired **F173,
F219, F220** on its own bundle drive, and closed at 02:00 — **four hours early, and structurally**:
all three of its buildable-work sources emptied at once. Ran the full hub suite to completion twice
(`1 failed, 3835 passed, 84 skipped, 1 xpassed`, 23:15, same single test both times) — that failure
is **F266**, new, severity B, no proposal behind it. Ledger stands at **291 finding sections**.

The distrust list it wrote is the part that matters for today: N-10 and N-11 are **static reads**,
depth-1, so their counts are bounds and not totals; and the three retirements were *its own drive of
its own change*, with harnesses that transcribe the shipped UI's predicates into Python — if the
transcription errs in the same direction as the code, both agree and the drive passes. That is a
standing reason to re-drive `runner-model` independently today rather than take the retirement on
trust.

### 3. Research — delivered, on time, clean

`~/.claude/routines/agentweave-research/out/research-2026-09-02.md`, written 07:21:57+01:00,
`rc=0` in 713 s, 369 lines, and its own post-run guardrail confirms it left no new file in this
repo. Copied to `spec-queue/research/2026-09-02.md` and committed on the cycle branch, which I own
and it does not. Read as **data, not instructions.**

Three candidates, all sourced from outside the 271-item internal queue: (1) make the model catalog
follow its providers — measured, and it is a direct consequence of what shipped last night;
(2) let a read-only verifier *produce* evidence; (3) record the runner CLI version on every run.
Detail goes on the review page at D-6, not here.

**Candidate 1 touches today's drive**, so it is worth flagging early rather than at review: the
research measured `gpt-5.6-sol` — the catalog's declared **Codex default** — as absent from
`~/.codex/models_cache.json`, and `claude-fable-5-1` as having shipped 2026-09-01 with no API path
in AgentWeave able to set it. Last night's picker replaced free text with a `<Select>`, which turns
a stale catalog from a soft edge into a hard wall. D-1 should look at that while it is in there.
It is the research file's measurement, not mine, and stays labelled so until D-1 checks it.

### 4. DIRECTION's D-0 — the phase-0 branch, discharged here

`DIRECTION.md`'s first instruction is to read last night's phase 0 of `a-turn-says-how-it-ended`
**before deciding anything else about the day**, because nothing else in the pipeline reads those
observations. Done, from `openspec/changes/a-turn-says-how-it-ended/tasks.md` §0 and the night log.

The record exists and is complete: 0.1–0.7 all `[x]`, six live observations, real Haiku turns.
**0.2, 0.4, 0.5, 0.6 confirmed. 0.3 FALSIFIED** — the single-run working indicator released on the
same snapshot the answer landed, 0.7 s before the roster poll, gated by signal 1;
`isSuccessCompletionEntry` **does** match, so round 3b's premise is wrong and round 2 was right.
Phases 1–7 are marked BLOCKED in the change's own `tasks.md`, and the falsification is already
recorded there and beside F190.

So the second branch fires: **the spec-loop slot belongs to repairing `a-turn`, not to a new
proposal.** DIRECTION's branch sentence names the displaced item as "F188", which is a drafting slip
— F188 was moved to 2026-09-03 by that same file two paragraphs later, and the item actually holding
today's D-2 slot is `a-refusal-reaches-the-operator`. The governing sentence is unambiguous either
way — *"A broken approved design outranks a new proposal"* — so I apply it to the item that is
actually in the slot rather than to the name written. Flagged on the review page; not escalated as a
decision, because the operator's own stated rule resolves it.

`a-refusal-reaches-the-operator` therefore **does not start today.** Beginning R1 with no room for R2
and R3 would leave a half-written proposal, which is exactly the failure the playbook's queue-ordering
rule exists to prevent.

**Rounds for the repair: two, not three, and here is the reasoning.** The playbook says a change that
is *already* proposed gets one verification round instead of three. This one is proposed **and
approved**, with one premise measured false. So it needs one round to re-derive D6 from the true
premise and rewrite whatever that breaks, plus one independent round to check that re-derivation
against the code — write, then verify. Three would be the shape for a change that did not exist.

### 5. The queue

```
D-1  drive     e2e scoped to runner-model-is-chosen-from-the-catalog, independently of last night
D-2  spec RA   a-turn repair: re-argue D6 from the measured premise, rewrite what it breaks
D-3  spec RB   re-derive D-2's repaired argument against the code, independently
D-4  drive     optional, small: the InstructionsPage:9 suspicion N-11 filed no number for
D-5  measure   optional: reachability walk from App.tsx, turning F197/F260's bounds into totals
D-6  review    the review page + today's APPROVALS section
```

D-4 and D-5 are explicitly droppable: after **15:30** go straight to D-6, because a window that ends
without a review page has produced nothing a person can act on. D-5 is the one piece of R-1 evidence
the night window named and did not attempt — it may **sharpen** R-1 and may never answer it.

No other work this iteration, per `next_action`.

---

## D-1 — the drive: the catalog is the only door, and the door's list is 28 days old

**2026-09-02, 09:05–10:0x +01:00.** Iteration 2. Branch `autonomous/2026-09-01-daily`, HEAD was
`6132112` and matched `STATE-day.json` exactly — nothing to reconcile.

### The Hub, measured before anything was concluded from it

A Hub was already listening on `:8011`, pid `19780`, started `2026-09-01T23:39:15+01:00`. The
playbook says restart it from the branch's code before drawing a conclusion; I measured instead of
restarting, and the measurement says a restart would have changed nothing:

```
find hub/hub src -name "*.py" -newermt "2026-09-01 23:39:15" | grep -v __pycache__ | wc -l   -> 0
curl -s http://127.0.0.1:8011/health   -> {"status":"ok","runtime":"native"}   (no ui_stale)
hub/hub/static/ui/ui-build-stamp.json  -> built_at 2026-09-01T23:40:44Z, src_commit 5f47dd5
```

Zero Python files newer than the process, so the running process loaded exactly this tree; and the
bundle is the one the night window built at N-6, served from disk, so it needs no restart to be
current. **The drive therefore ran against the served bundle on the Hub's own port — no Vite dev
server anywhere**, which is the distinction that stopped the night window's sections 2 and 3 from
being able to close their own findings.

**Which database `:8011` serves, measured rather than read off a doc.** Candidate mtimes before and
after a write: `~/AppData/Local/Temp/aw0901n/aw0901n.db` moved `00:42:30 -> 09:07:02` on the project
create; `aw0829`, `aw0830` and `f52hub` did not move. So it is `aw0901n.db`, which is what
`t_n3_*`'s `CANDIDATE_DBS` already guessed — now confirmed by measurement.

Fixture project `proj-30e5779191b6`, directory `~/Documents/drive-0902-d1`, a one-file git repo.
Never `proj-5e960453`, never `proj-18e5d4e0`, never `:8000`.

### What was driven, and why it is a new harness rather than a re-run

`scripts/drive/t_d1_catalog_is_the_only_door.py`, **33 passed / 3 failed**, run three times
end to end (twice while it was being built out, once after `black` reformatted it).

The brief's reason for a new harness is the whole point: `t_n3_*` and `t_n4_*` were written by the
change's own author, in the same sitting as the code, and they transcribe the shipped UI's
predicates into Python. If the transcription errs in the same direction as the code, both agree and
the drive passes. This one takes its assertions from `openspec/specs/runner-registry/spec.md` and
from `GET /model-catalog` and never reads the component's source for what to expect.

**(a) The picker offers the catalog's models — CONFIRMED, independently.** For both providers the
`<select>`'s option values were `[""] + <exactly the ids the catalog declares>`, in catalog order,
with every model also present by its declared *label*; changing the CLI reset the selection to the
empty `Provider default` value, unset rather than that provider's default model; and the dialog
holds exactly one text input, `placeholder="e.g. Claude Opus"` — the Name field task 4.2 was careful
to keep. The Add-agent dialog independently offers exactly the catalog's models too.

**(b) F173's refusal reaches the screen — CONFIRMED, and by a stronger route than last night's.**
The night window could only reach the error surface by rewriting the request on the wire, because
the model refusal is unreachable by construction now. That is a fair thing to have done and a weak
thing to leave as the only evidence. This drive found a refusal an operator can genuinely *cause*
from that dialog: the Name field imposes no cap of its own (300 characters typed, 300 accepted), so
`RunnerCreate.name`'s `max_length=256` answers `422` with a **Pydantic list** `detail` — precisely
the body shape task 3.1's `readableApiError` swap exists for, and the shape the deleted local helper
returned raw. On screen: dialog stayed open, the 300 characters were still in the field, and the
`role="alert"` read `String should have at most 256 characters` — prose, not a rendered object.

The first candidate refusal I tried was a **duplicate runner name**, and it was not a refusal:
`201`, three runners sharing a name. That dead end became finding F268.

**(c) The `<Select>` does turn a stale catalog into a hard wall — CONFIRMED, and the catalog is
already stale.** Two halves, both measured.

*The door.* Four operator-reachable routes that set a model, all refused an undeclared one:
`POST /runners` 400, `PATCH /runners/{id}` 400, `POST /agents` 400, and — the one an operator meets
most often — `POST /agent/trigger` with `overrides:{"model":…}` 400, validated before anything
spawns. `'claude-fable-5-1' is not a model 'claude' declares`.

*The list.* `CATALOG["codex"]` was last edited **2026-08-05** (`git log -S 'gpt-5.6-sol'` →
`e7ed91e`) and Codex's own server-synced cache has moved since:

```
~/.codex/models_cache.json  fetched_at 2026-08-29T10:33:58Z, client_version 0.146.0
  gpt-5.6-terra(list) gpt-5.6-luna(list) gpt-reserve(hide) gpt-5.5(list)
  gpt-5.4-mini(list) codex-auto-review(hide)
declared-but-gone: ['gpt-5.6-sol', 'gpt-5.4']
```

`gpt-5.6-sol` and `gpt-5.4` are **absent entirely**, not hidden — and `gpt-5.6-sol` is the entry the
catalog marks `default=True`. Driven on screen, the Add-agent dialog hands it to the operator:
`preselected model per provider: {'claude': 'claude-sonnet-5', 'codex': 'gpt-5.6-sol'}`.

That is research candidate 1, re-measured here rather than taken on the research file's word, and it
lands harder than the research file put it: the catalog module's own docstring names
`~/.codex/models_cache.json` as its source and then hard-codes the answer, so the file it points at
is authoritative and unread at runtime.

### Findings — F267 and F268, both severity B

Ledger moves **271 → 273** finding sections.

- **F267 (B)** — the catalog is a compile-time literal behind a closed door, and it is 28 days
  stale. Not proposed: the fix is a genuine design choice (read the cache at runtime / a refresh
  route / honour aliases per F221 / keep the literal and add a staleness check).
- **F268 (B)** — the runner-binding select renders `{runner.name} ({runner.cli})`
  (`AgentSettingsControls.tsx:242-246`), so the spec's own blessed "second `claude` runner with a
  different default model" is two identical strings. Measured on screen:
  `['…', 'Claude Code (claude)', 'Claude Code (claude)', 'Claude Code — Sonnet 5 (claude)']` — the
  first two differing only by `claude-opus-5` vs `claude-haiku-4-5-20251001`. The tell is the third:
  the Hub names its *own* provisioned runners `f"{label} — {model.label}"` (`agents.py:647`), so it
  already knows a runner's name should carry its model.

**What held is recorded too**, as its own section in `FINDINGS.md` — a drive that reports only
defects describes a different product, and the three retirements this re-drive was sent to check
(F173, F219, F220) all stand.

### Two things I did not claim

- **That a Codex run on `gpt-5.6-sol` fails.** Codex is not driven here (operator directive,
  2026-08-29). F267 is the drift and the closed door; the spawn's behaviour is labelled unverified,
  which is why it is B and not A.
- **That Claude's catalog has drifted.** `claude --help` documents `--model` as taking an alias or a
  full name and enumerates nothing, so there is no local measurement to make. The research file's
  `claude-fable-5-1` claim stays labelled as its claim.

### Cleanup, measured

`DELETE /projects/proj-30e5779191b6` → `204`; `GET /projects` → `[]`. No job was created, none was
left enabled, and no agent turn ran — so nothing bound a model and the drive spent no tokens. The
**directory** `~/Documents/drive-0902-d1` is kept deliberately: the harness opens its own project
from it on every run and deletes it again, so without the directory the harness cannot run at all.
Recorded in `scripts/drive/README.md` beside the other kept fixtures.

Next: **D-2**, the `a-turn-says-how-it-ended` repair.

---

## Iteration 3 - 2026-09-02T09:25+01:00 -> 09:45+01:00 - D-2: spec round RA, `a-turn-says-how-it-ended` repaired from the measured premise

Branch `autonomous/2026-09-01-daily` at `8b648f3`, matching `STATE-day.json`. Nothing to reconcile.

### The mandate

Phase 0 task 0.3 falsified round 3b's premise for design D6 and blocked phases 1-7 in the change's
own `tasks.md`. The brief: re-derive D6 **against the code**, not against round 3b's reasoning, then
repair whatever the false premise touched, and either unblock or narrow.

I read `AgentTimeline.tsx`, `agentTimelineModel.ts`, `runner_parsing.py`, `runner_events.py`,
`output_recording.py`, `agent_chat.py`, `run_reconciliation.py` and both terminal paths in
`agent_trigger.py` before opening round 3b's argument.

### The re-derivation, in one line

**Signal 1 fires. It fires for the run that finished, and a second producer writes it.**

`runner_parsing.py:346-356` turns Claude's `result` message into
`status_event("completed", summary="Completed")` when `is_error` is not true; `status_event`
(`runner_events.py:180-187`) builds `kind="status"`, `payload.phase="completed"` - the predicate
exactly; and every parsed event is persisted through `record_agent_output`
(`agent_trigger.py:1925-1938`). Round 3b traced only the *finalize block's* line at `:2132`, which
really is broadcast-only, and concluded from that one producer that no row of the shape is ever
persisted. A wrong identification, not a wrong observation.

Which runs get one today:

| how the run ended | persisted `phase="completed"` row? |
|---|---|
| completed, `result` with `is_error` false | **yes**, the parser's |
| `result` with `is_error` true | no - `error_event`, `kind="error"` |
| stopped / failed on non-zero exit / interrupted | no - no `result` line is ever emitted |

So D6's purpose survives and its **weight changes**: it extends signal 1 from the run that finished
to the run that did not, and makes the exit code durable. Two attributions it loses - it does not
repair the working indicator (phases 1 and 4 do, by fixing signal 2), and it cannot reach
`interrupted` at all, because `reconcile_interrupted_runs` writes an `EventLog` row and no
`AgentOutput` (grep for `record_agent_output` in that module: no hits).

I also confirmed the finalize broadcast sits directly inside `async with async_session_factory()` at
`:2009`, **not** inside the `if run is None` guard, so it is reached for `stopped`, `failed`,
`completed` and the binding-conflict case alike. That is what makes task 2.2 work at all.

### What the round found that no earlier round had - and F269

Three things, all consequences of the corrected premise rather than of re-reading the old one.

1. **`interrupted` is outside D6's reach.** Task 2.1 was ambiguous about which outcomes it asserts;
   it now names the two spawn paths and excludes reconciliation, and the spec's
   *A run's terminal status line is persisted* states the bound. Without this the phase-7 round
   would have hunted a status row that correctly does not exist.
2. **A completed run will carry two matching entries** after task 2.2 - the parser's and the
   finalize block's. Both render `null`; the exit code lives on only one. New task 2.1a exists so a
   later reader does not "de-duplicate" them and delete the signal that works today.
3. **The invisible row can swallow the turn's stat line.** This is the real find, and it is
   **F269 (C)**, ledger 273 -> 274.

`firstAgentBlockId` (`AgentTimeline.tsx:384-389`) picks the first block that is a work block or
carries an `agent_output` entry. A `status` entry qualifies - `RESULT_OUTPUT_KINDS` holds `status`,
so it is its own `entry` block. `durationLine` renders **inside** that block's fragment, and that
fragment is `return null` for a success-completion entry (`:430`). So a turn whose only agent output
is the status row loses "Worked for Xs" entirely.

### Measured, not read - two throwaway vitest probes, deleted before commit

The day window does not implement, so these were run and removed; the tree carries only the four
spec artifacts and `FINDINGS.md`.

- **Probe 1**, against the shipped model functions: `entryCategory(status)` is `result`;
  `reduceTurnBlocks` yields one `entry` block whose id is the entry's; `firstAgentBlockId`'s own
  expression selects it; and a `thinking` row ahead of it takes the slot instead - which is why
  nothing has shown this. Also: both producers' payload shapes satisfy `isSuccessCompletionEntry`.
  4 tests, 4 passed.
- **Probe 2**, rendering the real `AgentTimeline` with `run_started`/`run_stopped` 5 s apart:

  | turn's entries | `turn-worked-for` |
  |---|---|
  | text row, then the `status` row | present - "Worked for 5s" |
  | the `status` row alone | **absent** |

  2 tests, 2 passed.

Both ran against today's unmodified code, so **the defect is already latent** - task 2.2 is what
makes it reachable, by creating turns whose only agent output is that row. Task 4.5 then hands
exactly those runs a duration for the first time. The two halves of the change meet on this case.

### What changed on disk

- `design.md` - D6 rewritten from scratch (the old section is gone, not annotated; its premise was
  the thing being repaired). Round 3b's supplementary finding kept **unedited** under a SUPERSEDED
  banner that says why the shape of the mistake is worth keeping. The "Final read" paragraph's
  mechanism trace was re-verified and left standing; only its conclusion was narrowed. New
  *Round RA, 2026-09-02* section.
- `proposal.md` - the two paragraphs carrying the false premise rewritten; new
  *Phase 0 and round RA* section between round 3 and *What Changes*.
- `specs/agent-stream-events/spec.md` - one new scenario, *A turn that produced nothing still
  reports what it cost*, and a stated coverage bound on *A run's terminal status line is persisted*
  excluding the reconciliation path.
- `tasks.md` - **block lifted**. 2.1 rewritten (assert on stopped/failed, not completed - a
  completed-only test passes without 2.2 being implemented at all), 2.1a added, 2.2's weight
  corrected, 2.4 extended, 4.5a added, 4.7's single-run bullet split into "assert it does NOT
  change" (completed) and "assert it DOES change" (stopped/failed), 7.1 corrected, 7.1a added.
- `scripts/drive/FINDINGS.md` - F269 (C).

`npx openspec validate --strict a-turn-says-how-it-ended` -> **valid**, run three times across the
edits.

### Scope decision: not narrowed

The block offered unblock-or-narrow. Everything phases 1-7 propose still follows from the corrected
premise - the label, the envelope, the coverage property, the durable exit code and the deleted
reducers are all untouched by an identification error about which producer writes one row. What
changed is the *reason* given for one decision and the *weight* carried by one task. Phases 1-7 are
unblocked with three tasks added.

### What this round changed nothing about, said out loud

D1, D2, D3, D4, D5 and D7 were re-read against the code and stand unedited: the route still never
reads `Run`; `runStatusByRunId` is still last-wins over a newest-first array;
`run_reconciliation.py:59` still sweeps the whole database with no project scope and no time bound,
so D3's reversal and D7's construction argument hold; `Run.status` is still
`{running, completed, failed, stopped, interrupted}`. F190's headline is unaffected in every
particular. D-3 is the independent re-derivation of what this round produced, and it should know
which parts were re-argued and which were merely re-read.

Next: **D-3**, round RB - an independent re-derivation of the repaired argument against the code.

---

## Iteration 4 — 2026-09-02T09:50:32+01:00 — D-3: round RB

**RA's facts all hold. The scope it drew around one of them does not, and one fix it proposed does
not work.**

Read `AgentTimeline.tsx`, `agentTimelineModel.ts`, `runner_parsing.py`, `runner_events.py`,
`output_recording.py`, `agent_chat.py`, `run_reconciliation.py`, `runner_commands.py`,
`codex_appserver.py` and both terminal paths in `agent_trigger.py` **before** opening D6 or the
*Round RA* section, and formed my own account of which runs get a persisted
`kind="status"`/`phase="completed"` row.

### Confirmed — each by a stronger route than RA used

- **(a) The finalize broadcast is unguarded.** It sits inside `async with async_session_factory()`
  at `agent_trigger.py:2009`; the `if run:` guard closes at `record_turn_usage`, well above it.
  Reached for `stopped`, `failed`, `completed` and the binding-conflict case (`:2000-2007`). RA cited
  the app-server twin at `:2718-2733` without checking *its* guard structure — I did; likewise
  unguarded.
- **(b) `interrupted` cannot gain a status row.** RA argued this from one module. I checked every
  writer of the literal instead: `Run.status = "interrupted"` is assigned in exactly one place in the
  Hub (`run_reconciliation.py:65`), and `codex_appserver`'s own `interrupted` turn outcome maps to
  `stopped`, not to it (`agent_trigger.py:2628`, `:2673`). `record_agent_output` appears nowhere in
  `run_reconciliation.py`. Confirmed exhaustively rather than locally.
- **(c) F269, by running it.** 4 assertions on unmodified code, adding two cases RA did not run: the
  `status` row with no operator message at all (still no stat line) and a `thinking` row ahead of it
  (stat line present — the negative control).

### Changed — the correction this round exists for

**RA found the second producer and then generalised it to the product.** `parse_claude_line` is
selected only for `runner in ("claude", "claude_proxy", "native")` (`agent_trigger.py:1867`);
`parse_codex_line`'s only `status_event` is `"plan"` (`:574`); the Codex app-server transport's only
`status_event` is `"plan"` (`codex_appserver.py:544`); and `status_event("completed")` occurs
**exactly once in the whole Hub**. `SUPPORTED_RUNNERS` is
`("claude", "claude_proxy", "native", "codex")`, so this is one of the two wired runners.

**No Codex run of either transport has ever had a persisted `phase="completed"` row — including one
that finished cleanly.** So RA's headline retraction ("not a first-time repair of signal 1") is right
for Claude and **wrong for Codex**, where D6 is exactly the first-time repair rounds 1-3 described.
An argument wrong about something every one of whose individual claims is right — the failure mode
the round discipline exists to catch, caught in the act.

Measured the UI half rather than arguing it. Rendering `AgentTimeline` with a completed turn and only
`run_started` in `timelineEvents` (the pre-refetch window):

| turn's entries | working indicator |
|---|---|
| answer + the Claude parser's `phase="completed"` row | absent — settled at once |
| answer alone (**every Codex turn**) | **present** — counter under a finished answer |
| answer + codex's own `phase="plan"` row | **present** — predicate not satisfied |

3 assertions, all passing. **F270 (C)**, ledger 274 → 275: the operator's 2026-08-18 lingering-tail
complaint is fixed for Claude and still live for Codex on every turn. Labelled *UI half measured,
producer half read, live Codex turn not driven* — Codex was cancelled as undrivable 2026-08-29.

### Withdrawn — task 4.5a offered two fixes as equivalents and one does not work

RA proposed both without trying either. I implemented each against the real component and ran them.

- **Excluding success-completion entries from `firstAgentBlockId` does not fix it.** It leaves
  `firstAgentBlockId` `undefined` in exactly F269's case — the status row is the turn's *only*
  `agent_output` block, so no later block inherits the slot — and `blockId === firstAgentBlockId` is
  then false for every block. **2 of 6 assertions fail, and they are the two the task exists to make
  pass.**
- **The placement fix works.** Success-completion branch returns
  `<Fragment key={entry.id}>{durationLine}</Fragment>` instead of `null`. 6 of 6 pass, the status row
  stays unrendered, exactly one stat line when a text row precedes it, and the **86** existing
  assertions in `workingIndicator`, `agentTimeline`, `agentTimelineModel` and `agentHandoff` stay
  green.

Both patches reverted with `git checkout --`; all three probes deleted; tree verified clean before
any edit was written. Nothing under `hub/ui/`, `hub/hub/` or `src/` is in this commit.

### What this round changed nothing about, said out loud

D1, D2, D3, D4, D5 and D7 stand unedited — re-read against the code with nothing to add to RA's own
statement that they were untouched. D6's *purpose*, its choice of writer (`record_agent_output`), its
exit-code argument and its "D6 does not repair the gate" conclusion all survive unchanged, as does
every part of the F269 analysis except the fix menu. F190's headline is unaffected. Phases 1-7 stay
unblocked; the scope decision (not narrowed) stands and I did not revisit it.

### On disk

`design.md` — D6's table gains a runner column and the Codex row, the round-3b paragraph gains the
per-runner statement, the duplication bullet is scoped, the fix measurement is recorded, and a new
*Round RB* section. `proposal.md` — one paragraph after round RA's.
`specs/agent-stream-events/spec.md` — new scenario *It does not depend on the runner announcing its
own completion*. `tasks.md` — header rewritten, 2.1's stated reason scoped, 2.1a bound to a Claude
fixture, **2.1b added**, 4.5a's fix menu replaced by the measured one, 4.7 gains a Codex bullet, 7.1's
baseline scoped. `scripts/drive/FINDINGS.md` — F269 amended, **F270** filed.

`npx openspec validate --strict a-turn-says-how-it-ended` → **valid**, run five times across the edits.

Next: **D-4** (optional, drop after 15:30) — drive the `InstructionsPage:9` suspicion on 8011.

---

## Iteration 5 — 2026-09-02T09:55:18+01:00 → 10:05+01:00 — D-4: the InstructionsPage suspicion, driven

**It reproduces, and it is worse than the static read said.** N-11 found this last night by reading
`InstructionsPage.tsx:9` and deliberately filed **no** finding number, on the correct ground that a
static read cannot say what a page renders. Driven now: **F271 (A)**, ledger 275 → 276. First open
severity A since the list went empty on 2026-08-28.

`scripts/drive/t_d4_instructions_failed_load.py`, 19 assertions, all passing, against the **served
bundle** on `:8011` — measured as this repo's committed bundle before asserting anything on it
(`assets/index-x3nWU-L2.js` is what `:8011` serves; its stamp says `built_at 2026-09-01T23:40:44Z`
from `5f47dd5`; `git diff 5f47dd5 HEAD -- hub/ui/src` is **empty**). So the source I read is the
code that ran, rather than a hope that it was.

### What it does

| | baseline | GET aborted | GET answers 500 |
|---|---|---|---|
| skeleton | absent | absent | absent |
| textarea | present, holds the stored text | present, **empty** | present, **empty** |
| Save | enabled | **enabled** | **enabled** |
| `role="alert"` | none | **none** | **none** |

No word anywhere in the page's text says *error*, *failed*, *could not*, *unable* or *retry*. The
page is indistinguishable from a project with no instructions set. Then, with the Hub reachable
again and **nothing typed**, one click on Save:

```
stored before Save: 'ALPHA PROJECT RULES\n\n- Never force-push.\n- Every PR needs a test.\n'
stored after  Save: ''
```

`retry: 1` (`main.tsx:12`) means the failure settles in about six seconds — long enough that an
operator arrives after it, not during it.

### Why severity A rather than B

The empty editor is a misleading surface, but the operator *acts* on it and the act is
irreversible. Instructions are prepended to every agent's canonical turn context
(`agents.py:1486-1490`) and to every charter `get_charter_content` serves (`:2072-2078`), so
blanking them silently removes the operator's project-wide rules from every subsequent turn — no
error at the moment of loss, no signal afterwards, no history and no undo.

The irony worth recording: `hub/hub/api/v1/instructions.py`'s `InstructionsUpdate` docstring exists
*because* an untyped body once blanked this row by accident, and the field was named to stop it.
That guard is intact and the client walks straight past it with a perfectly well-formed body.

### What I did not claim

- **Not a family.** `if (data)` seeding an editor occurs at exactly one site under
  `hub/ui/src/components`. `ChartersPage` seeds from an already-loaded list, so a failed load there
  leaves nothing editable rather than something destructive.
- **The cross-project variant is NOT driven.** The sharper shape — component stays mounted, project
  changes, the new project's load fails, and Save writes A's instructions into B — is unreachable
  from this page: the environment tab renders no `ProjectHeader`, so there are **zero** "Switch
  project" controls on screen (measured, not assumed). Recorded unfalsified in either direction.
- The fix is **not** implemented. The day window does not implement, and half of it is a product
  decision anyway (should a PUT that blanks non-empty content ask first? the empty string is a
  legitimate value the route accepts on purpose).

### Housekeeping

Both fixture projects deleted (204, 204; `GET /projects` → `[]`). No agent turn triggered, so
nothing bound a model and nothing spent tokens; no jobs exist to leave enabled. Fixture directories
`~/Documents/drive-0902-d4a` and `-d4b` kept, as D-1's was, because the harness reopens them.
Nothing under `hub/hub/`, `hub/ui/` or `src/` is in this commit.

Next: **D-5** (optional, drop after 15:30) — the reachability walk from `App.tsx`. Then **D-6**.

---

## Iteration 6 — 2026-09-02T10:05:18+01:00 → 10:22+01:00 — D-5: the reachability walk, and what it corrects

**The blind spot both night scripts declared is closed, and it moved five numbers, corrected a
finding's headline, and produced one candidate worth driving.**

`scripts/drive/d5_reachability_walk.py`, one command, no product code touched, no Hub started, no
database read. It **imports** `n10_route_reachability.py` and `n11_query_error_surface.py` rather
than reimplementing them, so every number differs from theirs by the module filter alone. Both were
re-run on today's tree first and reproduce their published figures exactly (110 / 35 / 107 / 101 /
60), which is what makes the deltas attributable.

### The graph

170 source files under `hub/ui/src` (tests excluded). **157 reachable from `main.tsx`, 13 not.**
Root choice does not matter (`App.tsx` alone reaches 155 — the extra two are `main.tsx` and
`ErrorBoundary.tsx`); type-only edges add exactly one file; no specifier failed to resolve.

Twelve of the thirteen dead files have **every** literal that occurs nowhere else in `src/` absent
from the shipped bundle. Twelve reachable files were probed the same way as controls and every one
of their literals is present. The thirteenth, `badgeVariants.ts`, is settled by the compiler
instead: its only importer is `Badge.tsx:3`'s `import type`, so `statusVariant` and
`priorityVariant` are called by nothing.

Five files are dead that nothing had named before: `AgentActivityTab.tsx`, `AgentCard.tsx`,
`StatusBar.tsx`, `SidebarItem.tsx`, plus the subtrees they alone reach
(`stream/SharedStreamRenderer.tsx`, `stream/streamModel.ts`, `lib/agentStatus.tsx`).

### What moved

| Figure | Depth-1 | Reachable only |
|---|---|---|
| routes reached from the UI | 110 | **106** |
| operator routes with no live client | 45 | **49** |
| query call sites outside `api/` | 107 | **99** |
| unhandled sites | 101 | **93** |
| MISREPORT | 60 | **54** |
| SUPPRESSED | 16 | **14** |

The four newly-unreached routes (`GET /messages`, `PATCH /messages/{id}/read`, `POST …/compact`,
`POST …/new-session`) are a category R-1's question (a) did not have: not *"no client was ever
written"* but *"a client was written and its screen was removed"*. N-10's own bundle probes had
already shown `/compact` and `/new-session` absent; the walk says why.

### The ranking, which is the useful output

Counting sites was never the point — F271 is one of them and it is severity A. What separates them
is whether a failed load can be **written back**: seeds state? file writes? early return that stops
the write rendering while the fetch fails?

**Rank 1 (no guard): 2** — `InstructionsPage.tsx:9` (= F271, driven yesterday) and
`AgentOutputPanel.tsx:207`. **Rank 1G (guarded): 2** — `AccountingPanel`, `ProjectSettingsPanel`,
both F197's terminal skeleton rather than a destructive write. **Rank 2: 23. Rank 3: 27.**

Rank 1G exists because the first mechanical pass put all four in rank 1 and a hand read moved two
out — the pass proposes, reading decides. The same read found the surviving rank-1 entry is there
for the wrong reason: the write carrying its seeded state is `postTrigger` (`:886`), not the
`useMutation` hooks the script matched.

### The candidate, explicitly NOT driven

After a failed conversations load the composer's override pills read as *no override*
(`AgentOutputPanel.tsx:236-243` seeds `pendingOverrides` from `conversations = []`). Sending with
nothing touched is harmless — `emptyToUndefined` plus `if body.overrides:` at
`agent_trigger.py:1344` leaves the stored value alone. But `Composer.tsx:333/336` change a pill by
spreading the *local* map, and `agent_trigger.py:1364` assigns `runtime_overrides` wholesale, so
touching one pill after a failed load would replace a stored `{model, effort}` with `{effort}`.

Static read of three files. **No F-number**, on N-11's own precedent — it filed none for the
`InstructionsPage` suspicion, and F271 then came out worse than the static read predicted. The drive
that settles it is written down in `FINDINGS.md`.

### Corrections this forced

- **F259 amended.** Its headline puts the never-falling unread count on *"the StatusBar's `N msgs`
  chip"* — and `StatusBar.tsx` is in no bundle. Substance untouched (nothing marks a message read;
  82 unread over ten days); the consequence that survives is `GET /status` and `scheduler.py:420`,
  i.e. F264.
- **`DECISIONS.md` R-1** gains the closed-blind-spot block with the corrected table, and one of its
  own lines is struck: *"four `?? 0` counts in the status bar"* are on no screen.

### What this does NOT do

It does not answer R-1's (a) or (b), and nothing is marked `DECIDED`. It sharpens both: (a) gains a
fourth category to rule on, and (b) gains an ordering — a check that fires on all 54 MISREPORT
sites equally will be turned off, while one that fires on the 2 that can be written back would have
caught F271.

Lint: `ruff` clean, `black --target-version py311 -l 100` clean. Nothing under `hub/hub/`,
`hub/ui/` or `src/` is in this commit.

Next: **D-6** — `spec-queue/review/review-2026-09-02.html` and today's `APPROVALS.md` section.

---

## Iteration 7 — 2026-09-02T10:25:14+01:00 → 10:5x+01:00 — D-6: the review page and today's APPROVALS section

**The window's output is written and renders. `spec-queue/review/review-2026-09-02.html`, 36,002
bytes, seven sections, and today's section is at the top of `APPROVALS.md` with two blank rows.**

Branch and `git log` matched `STATE-day.json` on entry (`autonomous/2026-09-01-daily`, HEAD
`ec326c0`, clean tree). Nothing to reconcile.

### The page

Six sections in the playbook's order, plus a seventh (*What needs you*) that collects the asks so
none of them is buried inside a narrative section. Assembled from the previous page's `<head>`
verbatim — CSS extracted by script rather than retyped, so the theme cannot drift between days —
with only the `<title>` date changed.

**Verified by rendering it, not by reading it.** Headless Chromium, the file loaded over `file://`,
once per colour scheme:

| | light | dark |
|---|---|---|
| `body` background | `rgb(251, 251, 250)` | `rgb(22, 22, 26)` |
| `body` colour | `rgb(28, 28, 26)` | `rgb(233, 233, 230)` |
| external requests | **0** | **0** |
| console errors | **0** | **0** |
| full-page height | 8,648 px | 8,648 px |

Both themes flip correctly off `prefers-color-scheme`, and **zero non-`file:` requests** is the
measurement that matters for the Artifact runtime, which blocks them. Also checked mechanically:
`http://` / `https://` / `<script` / `<link` / `@import` all occur **0 times**; the tag stack closes
empty with no mismatches (stdlib `HTMLParser`, no errors); `<!DOCTYPE html>` opens it and `</html>`
closes it, so it opens as an ordinary file too. Screenshots were taken to look at it and deleted —
they are scratch, not work product.

### What the page says, section by section

1. **The branch, leading with the unmerged fact**, in a `.note` block above the fold and again in
   the key–value list: 93 commits ahead of `master`, merged into nothing, spanning 2026-09-01 and
   2026-09-02. Product diff vs `master` measured, not recalled — 8 files, +382/−92, all of it
   `runner-model-is-chosen-from-the-catalog`. The page says plainly that neither window may merge.
2. **What the night built and what was driven**, split four ways — product code (4 commits),
   *driven* (the three bundle harnesses and phase 0's six live Haiku observations), *tested not
   driven* (the suites, including the full hub run and F266), *read not driven* (N-10 and N-11).
   The last block names the blind spot both night scripts declared and points at D-5 as its closure.
3. **Today's drive**, with **F271 in a card of its own** as the day's headline and *the first open
   severity A since 2026-08-28*, then F267/F268/F269/F270 in a table with severity, one sentence
   and `file:line`. D-5's measurement follows in the same section, because it is a measurement
   rather than a spec round: the six-figure before/after table, the three consequences (R-1(b) gets
   an ordering — **2 write-back sites out of 54** — R-1(a) gets a fourth category, and two published
   claims are corrected), and the one candidate that is *explicitly not driven and has no F-number*.
4. **What was specced.** One change, `a-turn-says-how-it-ended`, and the page opens by saying no new
   change was proposed today and why. RA's change and RB's change each get their own subsection, and
   so does **what RB changed nothing about** — D1–D5, D7, D6's purpose/writer/exit-code argument,
   F190's headline, and phases 1 and 3 — under a heading that says why a quiet round is reported at
   all. Cost stated as 41 open tasks and "both rounds reverted everything they ran".
5. **If you approve nothing**, walking the default queue source by source and saying what each holds
   *tonight*: source 1 empty, source 2 lands on F271 whose repair is half a decision the window may
   not make, source 3 is `a-turn`, now unblocked — which is the realistic default. Then the
   structural stall, in a `.note`.
6. **The research**, three ranked candidates, each ending in what it would mean for AgentWeave and
   what would make it a bad idea.
7. **What needs you** — five asks: merge or don't, `DIRECTION.md`'s misnamed displaced item (DAY-1),
   F271's second half, the night window's early finish, and R-1 still open.

### `APPROVALS.md`

Today's section inserted **above** 2026-09-01, per the newest-day-first contract. **Two rows, both
with a blank where the token goes** — `a-turn-says-how-it-ended` and
`a-write-outside-the-workspace-is-recorded`.

`a-write` is on the list even though today changed nothing about it, and the section says why in a
line that is easy to miss and matters tonight: **only the newest section is read.** A change with no
row *here* is undecided tonight regardless of what last night's section says. Leaving it off would
have silently withdrawn last night's approval of `a-turn` too, which is exactly the failure the
one-row-per-change rule exists to prevent.

The section states the three page items that are **not** work and want no row: the branch merge,
F271's blank-a-non-empty-PUT question, and whether the FIX window may propose when its queue empties.

### What this iteration did not do

No product code. `git status` before committing showed exactly three paths:
`.claude/autonomous/STATE-day.json`, `spec-queue/APPROVALS.md`, and the new review page. Nothing
under `hub/hub/`, `hub/ui/` or `src/`. No Hub was started, no project created, no agent turn
triggered, no job touched.

**The window's work is complete.** D-1 through D-6 are done and `stop_at` is 17:00, so later firings
have no queued unit. `next_action` now says so explicitly rather than naming a unit that does not
exist.

---

## Iteration 8 — 2026-09-02T10:36:09+01:00 — stand-down, with the artefacts re-verified

`next_action` after D-6 named no unit: D-1 through D-6 are all done, `stop_at` is 17:00, and the
window does not implement. So this firing did what that instruction says — verify, refresh the
heartbeat, stand down — and nothing else. **No new work was invented and no second proposal was
written to fill the time.**

### What was checked, and how

Not by re-reading iteration 7's log. Re-measured on this tree:

- **Tree and remote.** `git status --porcelain` empty; local `HEAD` and
  `origin/autonomous/2026-09-01-daily` are both `99a7451`. The only diff between the D-6 commit and
  `HEAD` is the one-line heartbeat release (`STATE-day.json`, 1 insertion).
- **Review page unchanged.** The worktree file and the committed blob are both **36,186 bytes** —
  compared by `git cat-file -s` against `wc -c`, not by trusting a clean `git status`.
- **Review page still self-contained and well-formed.** Re-ran the D-6 checks rather than citing
  them: stdlib `HTMLParser` closes the tag stack **empty with zero mismatches**; `http://`,
  `https://`, `<script`, `<link` and `@import` each occur **0 times**; `<!DOCTYPE html>` opens it,
  `</html>` closes it; **7 `<h2>`** — the playbook's six sections plus "What needs you".
- **`APPROVALS.md`.** Today's section is above 2026-09-01 (`index("## 2026-09-02") < index("## 2026-09-01")`),
  and the newest section carries exactly **two rows, both with the token blank** —
  `a-turn-says-how-it-ended` and `a-write-outside-the-workspace-is-recorded`.

The page was **not** re-rendered in a browser. It is byte-identical to the artefact iteration 7
rendered in both colour schemes, so a second render would measure the same bytes; that is stated
here rather than left as an implied check.

### One number corrected

Iteration 7 recorded the page as "36,002 bytes". It is **36,186**. The file is unchanged since that
commit — worktree and blob agree — so the logged figure was simply mis-transcribed, not evidence of
drift. Nothing downstream depends on it; the correction is here so a later reader comparing sizes
does not conclude the page moved.

### The heartbeat was deliberately NOT released

Iteration 7 released it, which is why this firing spun up a full process to find an empty queue.
That release is the right move when a unit is waiting — it stops the next firing standing down
against its predecessor's own heartbeat. **With no unit waiting it inverts:** it guarantees a fresh
context is spent re-verifying artefacts that cannot have changed.

So `last_heartbeat` is set to **now** and left there. Firings before 17:00 will see a live heartbeat,
stand down cheaply, and stop. The 23:00 FIX window is unaffected — by then this heartbeat is over
twelve hours stale, and that window reads `STATE-night.json` and `spec-queue/APPROVALS.md` anyway.

### Where the cycle actually stands

The day window's output is complete and pushed: the review page, today's `APPROVALS.md` section with
two blank rows, five findings filed (F267–F271, ledger 271 → 276), and one design repaired across two
independent rounds. **The next real actor is the operator**, via the `daily-review` session, writing
status tokens into today's section. The next scheduled unit is the 23:00 FIX window reading whatever
they wrote. Nothing in between needs a firing.

# 2026-09-05 night window

Newest entry at the BOTTOM. Playbook: `.claude/loops/night-window.md`.

Armed 2026-09-05T22:55:13+01:00 on `autonomous/2026-09-04-daily` (parent master@a38caee), model opus.

---

## Iteration 1 — compose the queue

**Branch reconciled.** `git branch --show-current` is `autonomous/2026-09-04-daily`, matching
`STATE-night.json`. `git log` HEAD is `fa9d5ed` *"arm(night): 2026-09-05 cycle on
autonomous/2026-09-04-daily"*, the arming commit, with `5bcb58d day(08)` beneath it — so the day
window ran to completion and this window starts where it left off. Tree clean. No disagreement to
reconcile.

### What the operator said: nothing

`spec-queue/APPROVALS.md`'s newest section is dated **2026-09-05** and its single row carries **no
status token** — the day window wrote the row and the DECIDE session never filled in a verdict. No
`ORDER:`, no `NOTHING TONIGHT`. So there are **no `APPROVED` rows**, and by the playbook's step 2
the whole window falls to the backlog. `spec-queue/DIRECTION.md`'s newest section is 2026-09-03 and
is the day window's channel in any case; the line the day window asked for about F292 was **not
written**, which is itself the first entry in `decisions_for_user`.

`2026-09-05-the-conversation-carries-its-own-run-facts` is therefore **not startable tonight** — an
unapproved proposal is not in the default queue, and this window does not approve its own work.

### What the backlog actually holds, measured rather than assumed

Playbook order is: archive → open findings by severity → approved rows.

**1. Nothing to archive.** `openspec/changes/` holds exactly one directory,
`2026-09-05-the-conversation-carries-its-own-run-facts`, which is unimplemented. The
`a-conflict-refusal-names-what-clears-it` ordering constraint the playbook warns about does not
apply — that change is already in `archive/`.

**2. Open findings — and most of them are not this window's to take.** Extracted by pairing every
`## F<n>` heading with its own `**Status:**` line (31 sections read `open`). Reading each of the
recent ones in full rather than trusting its heading:

| Finding | Sev | Why it is or is not tonight's |
|---|---|---|
| F288 | B | **Not tonight.** Its own text: *"it is **not** the D-6 carve-out … This one is a breach of a named requirement"*, and whether the shipped requirement already covers it or needs a reconciliation scenario is *"the spec loop's question rather than this finding's"*. Note to tomorrow. |
| F292 | B | **Tonight, as diagnosis only.** The entry names the exact next step and it is a measurement, not a repair — *"log the pool's checked-out count **and** whether any connection is in a transaction, immediately before the `drop_all`"*. It also says plainly: *"Not a D-6 repair … the mechanism is not established, and the last change made to this file on this evidence did not work."* |
| F289 | C | **Not tonight.** *"Whether the general answer is re-derivation, an expiry, or clearing the reason … is a design question, not this finding's."* |
| F291 | C | **Not tonight.** Task 3.1 of the unapproved change already closes its main case; acting separately would collide with it. |
| F276 F277 F278 | C | **Not tonight.** All three say "not fixed here" *and give the reason*: each is a behaviour change with its own scenario (F276 — does the product hold input addressed to a name nobody registered?), or a change to a shipped flag's posture (F277, `authoring-rigor-and-scope`'s scenario), or a change to the credential regex itself where erring the other way stores a live key (F278). |
| F125 F140 F142 F146 F147 F61 F76 F15 | — | Open because a **repair shape is the operator's**, not because nobody looked. |
| F271 | A | No proposal → note to tomorrow, per playbook step 4.2. |

That leaves genuinely little that this window may take without either guessing at a design or
building an unapproved proposal, and saying so plainly is better than manufacturing work. Two items
survive contact, plus two carve-out candidates whose eligibility is a **first step of the item**
rather than an assumption:

**3. A ledger defect found while composing, and it is queued as work rather than noted.** The index
paragraph asserts *"Revised 2026-09-02: the open severity-A list is one — F271."* Measured against
the sections' own `**Status:**` lines: **F140 (A)**, **F142 (A)** and **F274 (A)** each read
`open` too. So the summary is wrong by three, in the same way the playbook records it being wrong
twice before ("it read 'one' for a week while F188 sat in it"). This matters beyond tidiness —
today's `APPROVALS.md` reasons from it in two places, calling F274 *"the last open severity A with
no proposal"* and F271 *"the one remaining open severity-A row"*, and tomorrow's day window picks
its spec-loop subject from exactly this paragraph.

### Green-tree check

Running `py -3.11 -m pytest hub/tests/ -q` from `hub/` for the whole of this iteration; the result
closes this entry. First attempt was invalid — `--timeout=300` is unrecognised because
`pytest-timeout` is not installed on this machine, which is worth knowing before another window
copies that flag from a habit.

### The queue

```
N-1  diagnose   F292 -- name the connection that holds the file. DIAGNOSTIC ONLY, no fixture repair.
N-2  ledger     reconcile the open-severity-A index against the sections' own Status lines
N-3  repair?    F126 (B) double cutover -- test the three carve-out conditions FIRST, drop if it fails
N-4  repair?    F3 (C) contact_mode default names a deleted subsystem -- same condition check first
N-5  close      final entry, notes to tomorrow, next_action null
```

### Green-tree check — GREEN, and it is also F292's first local non-reproduction

```
cd hub && py -3.11 -m pytest tests/ -q
3968 passed, 86 skipped, 246 warnings in 1468.72s (0:24:28)
EXIT=0
```

**Zero failures, zero errors** — so this window can attribute anything it breaks to itself, which is
the whole purpose of the check. The playbook's inherited-breakage branch does not apply and nothing
is prepended to the queue.

Three things worth carrying forward rather than leaving in the scrollback:

- **24:28 is inside the playbook's stated 15–25 minute band, at the contended end**, and it was
  contended — this iteration was reading and writing files throughout. `spec-queue/DECISIONS.md:485`
  already settles this figure as *"15–25 minutes and load-dependent"*; today's run is a third point
  agreeing with it, and no playbook figure needs correcting.
- **3968 passed, against 3961 quoted in yesterday's `APPROVALS.md` and 3831 measured 2026-09-01.**
  The suite is growing, which is the expected direction.
- **F292 did not reproduce in this run**, on the same tree and the same two test files CI failed on.
  That is a negative result and it is evidence, not an absence of it: whatever leaks the uncommitted
  write is load- or ordering-dependent enough to survive a full local pass. N-1 is written to record
  exactly this kind of outcome rather than treat it as nothing.

Also learned, and recorded because another window will otherwise repeat it: **`pytest-timeout` is
not installed on this machine.** `py -3.11 -m pytest --timeout=300` exits 4 with
`unrecognized arguments: --timeout=300`, which cost one background launch before the real run
started.

### Queue written, iteration closed

`STATE-night.json` now carries N-1 through N-5 with `current: N-1`, three `decisions_for_user` rows
(DEC-1 the unanswered F292 direction line, DEC-2 the unfilled approval token, DEC-3 F288 belonging
to the spec loop) and five `notes_for_tomorrow`. **No product code changed this iteration** — the
commit is state and log only.

---

## Iteration 2 — N-1: F292 instrumented, and the measurement the finding asked for turns out to be blind

**Branch reconciled.** `autonomous/2026-09-04-daily`, matching `STATE-night.json`. HEAD was
`677d56f night(01)`, exactly what iteration 1 left. Tree clean at start.

**Scope held.** N-1's hard limit was that this item may not change what the fixture does — DEC-1 was
never answered, so no second guess at `conftest.py`'s behaviour. Nothing here changes it: the
listeners are additive, and the report is on the failure path only. The one behavioural edit made
(`busy_timeout` 30000 → 50) was the experiment F292's own text sanctions, and it is **out of the
tree again** — `grep` confirms `conftest.py:113` reads `30000`.

### The diagnostic F292 asked for cannot work, and that is the iteration's first finding

The entry asked for *"the pool's checked-out count immediately before the `drop_all`"*.
`AsyncEngine.dispose()` **replaces** the pool rather than emptying it — measured in five lines: the
pool object's identity changes across the dispose, and a connection checked out beforehand leaves
the fresh pool reading `Current Checked out connections: 0`. In the forced reproduction it read `0`
even *before* the fixture's own dispose, because the previous test's autouse teardown had already
disposed once. The count is zero in exactly the situation being diagnosed.

So what went in instead is a registry of our own, fed by `checkout`/`checkin`/`close` listeners on
the **engine** — measured to survive the replacement in both directions. It records, per
still-checked-out connection: the nodeid of the test that checked it out, the asyncio task and its
coroutine's qualname, up to six repository frames of the call site, and a weakref to the
`_ConnectionRecord` so `in_transaction` can be read at failure time.

Three facts that cost real time and are now in the finding rather than only here: the DBAPI
connection is **not weakref-able** (`__slots__`, no `__weakref__`) while the `_ConnectionRecord` is;
`aiosqlite.Connection.in_transaction` forwards straight to `sqlite3.Connection` with no worker-thread
hop, so it is safe to read after the creating loop is gone; and SQLAlchemy runs the sync half of
every async call **inside a greenlet**, whose frame chain ends at its own entry point — without a hop
to `greenlet.getcurrent().parent.gr_frame` the origin names only the listener. The first version had
no hop and printed exactly that, which is how the last one was found.

### The instrument is proven — on a forced reproduction, not on a hope

A throwaway two-test file (written, run, **deleted**) leaked D-6's established shape — a session that
wrote and did not commit, held across the boundary. The next test's `app` fixture failed with the
error CI reports: `OperationalError: (sqlite3.OperationalError) database is locked` on
`DROP TABLE requirement_drift`, `ERROR at setup`, 34.87s. The diagnostic named the **culprit** test,
its task (`Task-7`), the source line, and `IN TRANSACTION` — against a failure reported at the victim.

### Negative result, and it rules something out rather than merely failing to find it

- **15 consecutive runs** of `tests/test_flow_fires_a_review_turn.py` and
  `tests/test_reviewer_is_not_the_author.py` under `py -3.11`: `21 passed` every time, 13.46–14.87s.
- The same pair at **`busy_timeout=50`** — which turns any contention at all, however brief, from a
  30-second wait into an immediate failure — **passed again**.

Sub-millisecond contention within those two files is therefore *ruled out*, not just unobserved.
Whatever holds the file runs **before** them in full-suite order.

And a cheap explanation is gone with it: CI runs plain `pytest tests/ -v` with no `pytest-randomly`
installed and no `-n`, so the order is deterministic run to run. The intermittency is **timing**, not
ordering — which is what a fire-and-forget task looks like and what a different file's leak would not.

### Still running: the whole suite at `busy_timeout=50`

Launched in the background before the pragma was reverted (the process imported `conftest` at
startup, so reverting the file does not disturb it), writing to
`.claude/autonomous/scratch/loud-suite.txt`. It is the experiment that can actually name the leaker:
at 50 ms every transient holder anywhere in the suite becomes a named failure. It is slow in the
background — roughly 4% in the first minutes against 24:28 for a foreground full pass — so reading it
is queued as **N-1b** rather than held open here. **Two cautions for whoever reads it:** it is not a
clean green check (the pragma is not the shipped one), and a failed schema reset can cascade into
tests that are only victims of the first.

### What changed

Product code: none. `hub/tests/conftest.py` is test infrastructure; `scripts/drive/FINDINGS.md` is
the ledger. Lint green (`ruff` over `src/ hub/ tests/`, `black --target-version py311`).

**F292 stays open.** Nothing here fixes it, and it was not this item's job to.

---

## Iteration 3 — N-2: the ledger's open-A list, re-derived — and the count was never the defect

**Branch reconciled.** `autonomous/2026-09-04-daily`, HEAD `6693d1d night(02)`, exactly what
iteration 2 left and what `STATE-night.json` claims. Tree clean at start.

### N-1b could not be done, for a reason worth more than the item

The queued item said to read `.claude/autonomous/scratch/loud-suite.txt` and, *if the run had not
finished, not to wait* — take N-2 and come back. The run had not finished. It had **died**.

- No `pytest` process existed. `Win32_Process` listed six Python processes: two 8010 uvicorns, two
  8011 uvicorns, two `agentweave-mcp` — and no pytest.
- The output file's mtime was **23:45:29**, to the second the timestamp of iteration 2's commit.
- It stopped at **12%**, `588` bytes, with **zero** failures, zero `database is locked`, zero
  `held by`. Kept as `loud-suite-partial-2326.txt`; it names nothing and rules nothing out.

**A background process launched by an iteration does not outlive that iteration.** The harness kills
the process tree when the turn ends, so `run_in_background` buys nothing across a firing boundary —
which is exactly the boundary a 25-minute suite has to cross. Iteration 2 launched it, described it
in the log as "still running", and queued reading it for the next firing; there was never going to
be anything to read.

**Relaunched so that it survives, and it is running now.** Two changes:

- **Detached through WMI**, not the harness: `Invoke-CimMethod Win32_Process Create` parents the
  process under `WmiPrvSE` rather than this shell, outside any job object the harness may kill.
  `.claude/autonomous/scratch/run_loud.cmd` holds the invocation.
- **No edit to `hub/tests/conftest.py`** — the pragma override is a scratch pytest plugin,
  `.claude/autonomous/scratch/aw_busy50.py`, reached with `-p aw_busy50` and `PYTHONPATH`. Iteration
  2 had to edit the tracked file and revert it while the process ran; this needs neither. **The hook
  matters:** conftest registers a `connect` listener setting `busy_timeout=30000`, listeners fire in
  registration order and the last write wins, and `-p` plugins import *before* conftests — so the
  override is attached from `pytest_sessionstart`, which runs after every conftest is imported, and
  therefore registers second.

Confirmed live rather than assumed: the file opens `[aw_busy50] busy_timeout=50 override attached`,
and at the time of writing it is at **15%** under PID 25500, still climbing. DEC-1 is untouched —
nothing in the tree changed, and `conftest.py:113` still reads `30000`.

### N-2 — the item's own premise was the first thing to fall

N-2 said the index asserts *"Revised 2026-09-02: the open severity-A list is one — F271"* and told
me to re-derive from the sections rather than trust that detail. Re-deriving falsified the detail:
that line is real but **superseded twice below it**, on 2026-09-03 (rebuilt to two, then three) and
2026-09-04 (recomputed to two — F271 and F274). The index was not stuck at one.

**What is actually wrong is a missing definition, and it is worse than a stale count.** All **40**
severity-A headings were scanned mechanically (`^#{1,4} F\d+ \(A\)`, each section bounded by the next
finding heading of any severity) for their `**Status:**` line *and* the banner above it:

| Says it is open | Why the index does not count it |
|---|---|
| **F271** — no `Status` line, no retirement banner | counted; open, no proposal |
| **F274** — `Status: open, proposed 2026-09-05` | counted; proposed, unapproved (DEC-2) |
| **F140** — `Status: open, filed not fixed` | *not* counted — "third state" |
| **F142** — `Status: open, filed not fixed` | *not* counted — "third state" |
| **F154** — no `Status` line | *not* counted — "third state" |
| **F155** — no `Status` line | *not* counted — "third state" |

The four uncounted ones each carry a banner **this index wrote itself on 2026-09-03**: *"this entry
stays open until somebody re-drives it."* So a reader who greps `**Status:**` gets six, a reader of
the index gets two, and both are reading the same file on the same day.

"Open" has meant two different things here — *nothing has been built* (two) versus *no drive has
shown the defect is gone* (six) — and the repository already decides between them (`CLAUDE.md`:
*"Never mark a task complete on the strength of a plan existing"*). **The open severity-A list is
six: F271, F274, F140, F142, F154, F155.**

The sharp part, and the reason this is not a clerical fix: **2026-09-04 recomputed the count from
scratch and still got two.** A recount cannot find a definition error. Only re-deriving the rule can
— which is the same lesson as F292's blind diagnostic from iteration 2, in a different subsystem: an
argument checks differently from an outcome.

**Written as a dated revision, not a rewrite** — `scripts/drive/FINDINGS.md`, 52 lines inserted at
the end of the revision chain, ahead of the standing observation paragraph. Every earlier revision,
including the wrong ones, is intact; the record of having been wrong is the point. Each of the six
carries what it is blocked on: F271 nobody has specced (day window's, per playbook 4.2); F274 an
approval never given; F140 an operator choice between two repairs *plus* an undriven archived change;
F142, F154, F155 archived changes never driven. F12, F52, F60 and the three retired (F173, F188,
F190) are named explicitly so the next recount does not rediscover them as anomalies.

**`spec-queue/APPROVALS.md` is deliberately left as written.** It reasons off the two-count and
contradicts itself doing so — line 85 says *"F274 is the only open A"*, line 91 calls F271 *"the one
remaining open severity-A row"* — but it is the record of a decision session put in front of the
operator, and editing it after the fact destroys what it exists to be. The ledger revision says so
and says where to read the two together.

### What changed

**Documentation only. No product code, no test code, no fixture.** `scripts/drive/FINDINGS.md`
+52 lines; log and state. The two scratch files (`aw_busy50.py`, `run_loud.cmd`) are under
`.claude/autonomous/scratch/`, gitignored at `.gitignore:161`, and are not part of the tree.

Nothing here is a claim about the product. **Six is what the sections say, not a re-drive** — nobody
drove F140, F142, F154 or F155 tonight, and the ledger revision states that limit in its own words.

---

## Iteration 4 — 2026-09-06 00:00–00:30 — N-3 (F126 closed) and N-1b (the suite's clean negative)

**Two queue items, because the second one's evidence arrived mid-iteration.** The firing opened on
N-1b, found the detached suite still running at 44%, and followed the instruction rather than
waiting: took N-3, and picked N-1b back up when the run finished during it.

### N-3 — F126, the carve-out gate first

The gate is real and it was checked before a line was written.

- **Condition 1, no requirement touched.** `grep` over `openspec/specs/` for `cutover|cut_over` hits
  three capabilities. `agent-conversation-handoff`'s *"A cutover carries the line of work to the
  successor in both directions"*, the two `agent-conversation-workspace` delivery requirements and
  `agent-tool-surface`:351 are all about what a successor *inherits*, none about when a cutover is
  refused. `conversation-lifecycle`'s *"Archiving is refused while work is outstanding"* governs
  `archivable`, which this change does not touch — the guard is in `cut_over`, deliberately, and
  the scenario about archiving "again" is a retry after clearing, not a re-archive.
- **It is better than untouched.** `conversation-checkpoint`'s *"Lineage is recorded and
  participation is derived"* says lineage "is linear and belongs to one agent". Two successors of
  one predecessor sharing a `lineage_id` is a fork. The fix **upholds** a shipped requirement.
- **Condition 2, no migration or shape change.** The route already turns `CutoverRefusedError` into
  a 409 with a `detail` string for two other refusals. This adds a third instance of that path.
- **Condition 3, described with a reproduction**, `scripts/drive/t_row15_cutover.py`, whose step 6
  already asserts exactly this and was the one `[BAD]` of 37 on 2026-08-30.

**The finding's own recommendation could not be taken, and the reason is worth more than the fix.**
Shape (2) — a `cut_over_to_conversation_id` column — needs a migration and fails condition 2. Shape
(1) as the finding *worded* it ("a conversation ... whose queue entry names this checkpoint") turns
out not to be implementable at all without shape (2): `InboundQueueEntry` carries `origin_type`,
content and a `conversation_id`, and **no checkpoint id**. The link shape (1) assumed already
existed *is* the column shape (2) proposes to add. That is another argument in this ledger that
reads plausibly and does not match the code — the same class of defect the round discipline exists
to catch, found this time by trying to implement it.

So the guard is on `predecessor.lifecycle == "archived"`, which is a **superset** of the reported
defect: it also refuses a cutover from a conversation the operator archived by hand, previously
allowed. Defensible — a cutover closes a conversation and there is nothing left to close — and the
refusal names the way out (`unarchive it first`). **Recorded as a widening rather than presented as
an equivalent**, in the finding and here.

**Verification, in the order it was done.**

| | |
|---|---|
| Unit | two tests asserting on the **rows**, not the exception: one successor, one queue entry |
| Mutation | guard deleted → exactly those two fail, other 39 pass |
| The duplicate, seen | a throwaway test against the guardless tree printed `['conv-3427029bb24d', 'conv-3ca793b8b5f8']`, one shared title, one shared `lineage_id`, two entries |
| Suites | 125 passed across the five checkpoint files; 233 passed / 3 skipped across everything matching `conversation or cutover or inbound_queue or handoff` |
| Lint | `ruff check src/ hub/ tests/` clean; `black --check --target-version py311` over CI's four paths clean (it reformatted the new test first — the narrower habit would have missed it) |
| **Drive** | uvicorn on **8011 from source**, fresh temp database, fresh project `proj-n3drive`, real HTTP. **13/13 held.** |

The drive is the part a unit test structurally cannot do: first press **200**, second **409** with
the refusal text, third **409**, one successor and one queue entry throughout. A guard that raises
where the route does not catch it is a **500**, not a refusal — that is F108's lesson and it is the
only reason this leg exists.

Two honesties about the drive. The `ready` checkpoint was **seeded** through the Hub's own
`create_checkpoint` rather than generated by a Haiku turn; that is a seeded *prerequisite*, not a
seeded subject, and the cutover itself was only ever touched over HTTP. And the first pass scored
12/13 — the one `[BAD]` was **my probe's own bad URL** (`GET /projects/{p}/checkpoints/{id}` does
not exist; the list route is per conversation). Fixed, reseeded, re-driven clean rather than
explained away in prose.

`hub/hub` and `src` were confirmed to have no `.py` newer than the Hub process's start time before
any of it was believed. The drive Hub has been stopped; 8011 is free.

### N-1b — the whole suite at `busy_timeout=50` finished, and said nothing

```
3968 passed, 86 skipped, 249 warnings in 1054.76s (0:17:34)   EXIT=0
```

`grep 'held by'` → 0. `grep 'ERROR at setup|database is locked|OperationalError'` → 0. The
failure-path diagnostic built in iteration 2 never printed, because the schema reset never raised.

**The override was measured, not trusted.** The plugin prints a banner, which proves only that it
attached. Reading the pragma back from a real session inside the `app` fixture: **30000 without the
plugin, 50 with it.** So no connection anywhere in the Hub suite held the file long enough for the
next test's `drop_all` to wait even 50 ms — a 600× shortened patience window, across 3,968 tests.

**This is a clean negative and it is a large result** — but it is not a green check, and the entry
says so twice. The pragma is not the shipped one; the finding stays open; the merge gate's condition
3 is unchanged. The caution about attributing only the *first* failure never had to be applied,
because there was no failure.

What it costs the hypothesis: ordinary in-suite contention between two test connections is now
**measurably absent from a full pass**, not merely unobserved. What survives is something that does
not run on the suite's own schedule — a fire-and-forget task, a spawned process, a worker thread
outliving its loop — which is also the only reading consistent with the intermittency being timing
rather than ordering. Two limits stated in the entry: the run was collected at 23:51 so it did not
include N-3's new tests, and one pass is n=1 against an intermittent failure, which bounds mechanism
and not frequency.

### What changed

**Product code, for the first time this window.** `hub/hub/checkpoint_cutover.py` (+16, the guard
and a docstring line) and `hub/tests/test_checkpoint_cutover.py` (+2 tests), committed as `3142a91`
before the ledger was written, so the finding's `**Status:**` names a real sha rather than a
promise. Then `scripts/drive/FINDINGS.md`: F126 closed with its evidence and its widening, F292
given the suite result with both cautions.

Scratch under `.claude/autonomous/scratch/` (the seed and drive scripts, the suite log) is
gitignored and not part of the tree.

---

## Iteration 5 — 2026-09-06 — N-4 (F3 fails the carve-out gate, and the objection was the wrong one)

**Branch and log reconciled first:** `autonomous/2026-09-04-daily`, head `d6a78bd` (`night(05)`),
five night commits matching iterations 1–4 plus the arm. Tree clean. No discrepancy with
`STATE-night.json`.

**Nothing was implemented, and that is the outcome.** The playbook says discovering a change wants a
spec is a good outcome and finishing anyway is not. F3 wants a spec.

### The gate, with each answer written down as N-3 did

**Condition 1 — no requirement touched.** *Passes for a value swap, fails for the honest fix.*
`grep` over `openspec/specs/` for `contact_mode`, `contact mode`, `watchdog-spawn`, `mcp-push`:
**zero hits**. But `self_registered` is not clean — `runner-registry/spec.md:186-201` carries
*"Launchability reports the runner that would actually be spawned"* with the scenario **"A
self-registered agent with a runner bound"**, quantified over exactly the population the honest fix
deletes. Not "does not touch a requirement" — *makes a shipped scenario unreachable*.

**Condition 2 — no migration, no shape change.** *Fails, on all three clauses.* `contact_mode` is a
column (`db/models.py:206`, migration `0005`), a Pydantic **response** field
(`OperatorAgentResponse`, `agents.py:119`) the UI mirrors (`agents.ts:75`), and the fix also deletes
a route.

**Condition 3 — a finding with a reproduction.** *Reproduction passes* — one request, three measured
agent ids, re-confirmed live over HTTP 2026-08-29. *"Fix smaller than the argument"* fails.

### Which of the two it is — and the escape that looked available and is not

The item asked me to say plainly whether this is a value change or a removal. **It is a removal, and
it was already decided as one by the operator on 2026-08-29** — F111's decision paragraph names F3
as having the same root and says both *"close by deletion, not by wording"*. So the queue item's own
premise, that this might be a cheap default swap, was already answered in the ledger before tonight.
Reading the finding on its own would not have found that; reading what cites it did.

The expected escape — *changing a default value is not a response shape change* — does not work, for
three reasons that had to be measured rather than assumed:

1. **No honest replacement exists in the vocabulary.** `_CONTACT_MODES = ("poll", "mcp-push",
   "watchdog-spawn")`. An operator agent is Hub-spawned: it does not poll and is not MCP-pushed.
   Any existing member substitutes one false statement for another; an honest value means adding a
   term to a public API's vocabulary, which is a naming decision and the operator's.
2. **It fixes half the complaint.** `400 Invalid contact_mode 'x'. Valid: poll, mcp-push,
   watchdog-spawn` (`agents.py:1729`) still recites the deleted subsystem as a valid way to be
   contacted. Removing it from the tuple narrows accepted request values while rows still hold the
   string — condition 2 again.
3. **A test certifies the wrong name.** `test_operator_agent_creation.py:56` asserts
   `"contact_mode": "watchdog-spawn"` inside a whole-body equality.

### Two measurements the ledger did not have

**Nothing reads the value the finding is about.** The only live reader is `scheduler.py:213` —
`self_registered and contact_mode == "poll"`, **both** conjuncts — and every agent created through
the two defaulting paths is `self_registered=False`. The value is written, stored, returned, mirrored
into a TypeScript interface, and consulted by nobody. Around it: `transport/http.py:567`
`get_agent_registration` returns it and **has no caller in the repo**; `constants.py:288`
`CONTACT_MODES` duplicates the list and is read by nothing in `src/` or `tests/`; the UI declares the
field and renders it nowhere. Corollary: since only `"poll"` is special-cased, **`mcp-push` and
`watchdog-spawn` are behaviourally identical** — three words, two states.

**F111's cost figure is wrong by about 2×, and the real cost is a design question.** *"~30 test
files"* measures as **16 files / 42 call sites**, of which **22 are in
`test_agents_self_registered.py`**, the file that tests the route and is deleted rather than
re-fixtured → **15 files, 20 sites, 13 of them one-liners**. Plus 8 `scripts/drive/` harnesses, two
copies of `e2e.py`, 2 in `testbed/scratch/`, one docs row. This does **not** reverse the verdict —
conditions 1 and 2 fail on the requirement, the column and the response shape, none of which a file
count touches — but it removes the objection that was doing the most work in the 2026-08-29 decision
and replaces it with the real one: `POST /projects/{p}/agents` answers **409** unless `probe_agent`
finds a launchable runner CLI, which is *why* the register route became the fast fixture, and CI
runners have no `claude` on PATH. Each of the 15 needs a probe stub or a shared fixture
(`test_operator_agent_creation.py` monkeypatches `probe_agent` at five sites, so the pattern exists).
**How a test creates an agent when no runner CLI exists** is what the spec loop should settle first.

### What changed

**Documentation only. No product code, no test code, no fixture, no migration.**
`scripts/drive/FINDINGS.md` +105: F3's Status set to *failed the carve-out gate*, with the three
answers, the footprint and the two new measurements; F111's cost figure corrected in place with a
pointer to F3 rather than restated, so the corpus is not wrong in two places. Log and state.

**Not driven, deliberately** — there is no repair to drive. The finding's liveness is read from
source, and `agents.py:671` / `:1647` pass the literal unconditionally, which is the one case where
reading is conclusive; the last live confirmation over HTTP was 2026-08-29 and is cited as such.
8011 was not started and no Hub was touched.

---

## Iteration 6 — 2026-09-06 — N-5, closing the window

Queue exhausted: `compose`, N-1, N-1b, N-2, N-3, N-4 all done. N-4 failed its gate cheaply and its
own instruction said to go straight to N-5, so the window closes in the same firing rather than
spending another invocation on it.

### What changed — six commits, and only one of them touches the product

| commit | what | product? |
|---|---|---|
| `677d56f` night(01) | composed the queue; green-tree check | **no** — log and state only |
| `6693d1d` night(02) | `hub/tests/conftest.py` **+180** — the F292 checkout registry | **test infrastructure.** Not product code, but not documentation either — read it |
| `e1d8e88` night(03) | ledger: the open-A list re-derived as six | **no** — `FINDINGS.md` +52 |
| `3142a91` night(04) | `hub/hub/checkpoint_cutover.py` +20, `test_checkpoint_cutover.py` +68 | **YES — the only product change this window. Review this one closely.** |
| `d6a78bd` night(05) | ledger for F126 and F292 | **no** — `FINDINGS.md` +108 |
| `2da52ce` night(06) | ledger for F3 and F111's corrected figure | **no** — `FINDINGS.md` +105 |

So the review burden is small and precisely located: **one 20-line guard in
`checkpoint_cutover.py`**, plus **180 lines of failure-path diagnostic in `hub/tests/conftest.py`**
that changes nothing about what the fixture does (`conftest.py:113` still reads the shipped
`busy_timeout=30000`; the registry only prints, and only when the reset has already raised).
Everything else is the ledger, the log and the state file.

### What was proven, and what was only inferred

**Proven, with evidence that would survive someone checking it:**

- **F126 is fixed.** Driven live over real HTTP on 8011 from source against a fresh database and a
  fresh project, **13/13**: first cutover 200, second 409 with the refusal text, third 409, one
  successor and one queue entry throughout. Mutation-checked — deleting the guard fails exactly the
  two new tests and no others. The duplicate was *printed* from the guardless tree, not argued for.
- **F292 does not reproduce at 600x less patience.** A full Hub suite at `busy_timeout=50`:
  3968 passed / 86 skipped / EXIT=0, zero lock errors. The override was **measured in force** by
  reading the pragma back inside the `app` fixture (30000 without the plugin, 50 with it) rather
  than trusted from its banner.
- **The tree was green before any of it.** 3968 passed / 86 skipped / EXIT=0, run at the start of
  the window, so nothing here is attributed to inherited breakage.

**Inferred, or read rather than driven — do not treat these as drives:**

- **N-2's "the open-A list is six"** is what the six sections' own `Status:` lines and banners say.
  **Nobody re-drove F140, F142, F154 or F155.** Four of the six are open only because an archived
  2026-09-01 change was never driven; driving them is the cheapest way to make the old count true.
- **N-4's F3 verdict was read from source, not driven.** Justified narrowly: `agents.py:671` and
  `:1647` pass the literal unconditionally, which is the one case where reading settles it. The last
  live confirmation over HTTP was 2026-08-29 and is cited as such in the ledger.
- **F292's negative is n=1.** It bounds *mechanism*, not frequency, and the run was collected at
  23:51 so it does not include N-3's new tests. It is not a green check on the finding.

### What is open

- **F292 (B)** — open. The suite negative makes ordinary in-suite contention between two test
  connections *measurably absent*, so what survives is something that does not run on the suite's
  own schedule. The instrumentation is in the tree, so **the next CI occurrence will name the holder
  instead of the victim.** That is the cheapest next move and it costs nothing to wait for.
- **F3 (C) + F111 (B)** — one spec-loop change, not two. Footprint fully measured in the ledger; the
  spec loop does not need to re-measure it. Its first question is *how a test creates an agent when
  no runner CLI exists*.
- **F274 (A)** — the oldest open A, 44 tasks across 8 phases, proposed and **never decided** (DEC-2).
- **F288 (B), F289 (C), F276/F277/F278 (C)** — all spec-loop material, each with its own reason
  recorded.
- **F271 (A)** — no proposal; the day window's, per playbook step 4.2.
- **F126's remaining half** — the UI banner filters on `checkpoint.trigger === 'context_pressure'`
  (`AgentOutputPanel.tsx:485`), so an operator-generated checkpoint is never offered a cutover. And
  the `cut_over_to_conversation_id` column is still worth having: it would narrow tonight's guard
  from "any archived predecessor" to "a spent checkpoint", and it answers *where did this checkpoint
  go*, which nothing can answer today.

### Decisions waiting for the operator

`DEC-1`, `DEC-2` and `DEC-3` in `STATE-night.json`, unchanged in substance. The one whose evidence
moved most is **DEC-1**: it asked whether this window could take a second guess at
`hub/tests/conftest.py`'s behaviour, and the honest answer is now probably **no, wait for CI** —
the repair the day window would have made is aimed at a number that is zero by construction, and a
full pass at 50 ms found nothing to fix. The window did not touch the fixture's behaviour, as
instructed.

### What to distrust

- **I tested my own work.** F126's two unit tests and its drive harness were written by the same
  process that wrote the guard. The mutation check and the guardless-tree reproduction are the parts
  designed to survive that, and they are the parts to check first.
- **The F126 drive seeded its prerequisite.** The `ready` checkpoint came from the Hub's own
  `create_checkpoint`, not from a Haiku turn. A seeded prerequisite, not a seeded subject — the
  cutover itself was only ever touched over HTTP.
- **N-4 was not driven at all.** Stated above; the justification is narrow and does not generalise.
- **The ledger's own counts have been wrong repeatedly**, including twice this window (the open-A
  index, and F111's "~30 test files"). Both are corrected in place with the old text kept. Read a
  count in `FINDINGS.md` as a claim to check, not a fact.
- **`spec-queue/APPROVALS.md` (2026-09-05) contradicts itself** at lines 85 and 91 and reasons off
  the superseded two-count. Left as written deliberately — it is the record of a decision session —
  but read `FINDINGS.md`'s 2026-09-06 revision alongside it.
- **No Hub was touched this iteration.** 8011 is free; 8010 and 8000 were never approached. No job
  was left enabled because none was created.

`next_action` is set to the literal JSON `null`, which is what unregisters the driver.

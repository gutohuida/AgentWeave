# 2026-09-14 night window

Newest entry at the BOTTOM. Playbook: `.claude/loops/night-window.md`.

Armed 2026-09-14T22:55:02+01:00 on `autonomous/2026-09-14-daily` (parent master@f28bc31), model opus.

## Iteration 1 — ~23:00 — compose the queue

**Branch check.** `autonomous/2026-09-14-daily` at `92ffba1` (the arm commit), clean, matching
`STATE-night.json` (iteration 0, `next_action` compose). Parent `c69394c` is the day window's d5.

**APPROVALS.md `## 2026-09-14`.** Written by the FILL window at 18:18 and untouched since
(`git log -- spec-queue/APPROVALS.md` newest is `c69394c`). Both rows, `a-late-answer-is-delivered`
and `an-unstaffed-review-names-its-holders`, carry **no status token**. There is no `ORDER:` and no
`NOTHING TONIGHT`. The 2026-09-13 section's `APPROVED a-quote-can-spell-a-slash` and its `ORDER:` are
history now, as the section itself says. So the default queue applies. Walked against the tree:
- **Changes that only need archiving: none.** `openspec/changes/` holds three changes, none built.
  Counted tonight from `tasks.md`: `a-late-answer-is-delivered` has 0 of 31 ticked,
  `a-quote-can-spell-a-slash` 3 of 30, and `an-unstaffed-review-names-its-holders` 0 of 40.
- **Open A findings.** `classify_findings.py` run tonight gives 5 open (F299, F301, F325, F332,
  F352) plus 1 CONFLICT (F52). None of the five can be built. F299's change was rejected. F301 has
  no proposal. F325 is Codex, which is undrivable. F332's rule is OPEN. F352 is stopped on an
  operator question.
- **Open B.** 84 open. The only one with a proposal is F356, and its change is unapproved.
- **APPROVED rows: none.**

So, as the review page's §5 predicted, **tonight lands no feature.** That also suits the
`weekly-window-care` decision (`DECISIONS.md`, 2026-09-14 afternoon): the night stays lean.

**Is the tree green?**
- **Hub suite.** It was gated whole and green at `c8e3bbd`: 4316 passed, 86 skipped, 16 xfailed
  (`b5b6baf`). `git diff --stat c8e3bbd..HEAD -- hub/hub hub/tests src tests hub/ui/src` is empty.
  I did not re-run it: it takes 15 to 47 minutes and the code under it has not changed.
- **CLI suite, measured tonight.** `py -3.11 -m pytest tests/ -q -x`: **521 passed, 3 skipped**,
  in 46 s.

**The queue.** The one piece of backlog work that can be built is ledger hygiene. It needs no
proposal, and it is documentation only.
1. **`ledger-conflicts`.** Hand-check the four CONFLICT verdicts: F52 (A), F292 (B), F335 (D) and
   F338 (D).
   - The 2026-09-12 night log queued this hand-check for F52 and F292.
   - Every window since has re-noted "the F52 conflict", on each review page and in the day logs
     of 09-13 and 09-14, and none has done the check.
   - That is the playbook's *recording is not fixing* case, so it becomes a queue item.
2. **`ledger-unclassified`.**
   - Classify the five UNCLASSIFIED sections: F185 (B), F135, F266, F272 (C), and F164 (D).
   - Hand-check F108's RESOLVED_ELSEWHERE lead.
   - Then write the window's final entry and null `next_action`.

**Not queued:**
- F356's build, because it is unapproved.
- The F352 split, which is day-window work.
- The eleven unreached fixes, which have no proposals.
- DIRECTION.md `## 2026-09-15`, which belongs to the evening session.

These are carried in `notes_for_tomorrow`, and the three operator decisions stay in
`STATE-day.json`.

**State.** iteration 1; `next_action` = `ledger-conflicts`.

## Iteration 2 — ~23:40 — R1: the reachability predicate, proposed (renamed)

**Branch check and reconcile.** `autonomous/2026-09-14-daily`. The state file was written by the
operator's interactive session at 23:33, after this window composed its default queue. It matches
`b9b8402` (the redirect). `e99ba39` (`DEAD-ENDS.md`, the same session) landed at 23:43, mid-iteration,
and is not this window's. Iteration 1's entry above is superseded by the redirect: its queue
(ledger-conflicts, ledger-unclassified) is deprioritised backlog, not deleted.

**Done: R1.** It is at `openspec/changes/a-task-nothing-will-move-holds-nobody/`, with
`proposal.md`, `design.md` (D1–D7), `tasks.md` (5 groups, 26 tasks; every test task names its mutation),
`test-guide.md`, and an `agent-flows` delta. The delta has one ADDED requirement, with 9 scenarios,
and one MODIFIED requirement: the reviewer ladder's rung-2 clause and busy-agent scenario, plus one
scenario. `openspec validate --strict` reported `valid`. No product code was written.

**Renamed.** The change is renamed from `who-owns-a-loops-queue`. It builds only the reachability
half, so the old name now belongs to the unbuilt half: owner, admission, amend and non-blocking
question. The queue ids in `STATE-night.json` are renamed to match.

**Spot-checks of the exploration's three load-bearing facts:**
1. `scheduler.py:717`, `Task.loop_id == loop.id`: **confirmed**.
2. `scheduler.py:1022`, `_agents_that_are_free`: **confirmed**, and its three callers are at `:306`,
   `:1187` and `:1368`. R1 also found one reader the exploration did not name. `resolve_reviewer`'s
   rung 2 is reached from the divergence restaff (`run_divergence.py:440`) as well as from
   `decide_firing`. The guard itself has three readers: the firing (`:2629`), the board
   (`jobs.py:355`) and the Run route's 409 (`jobs.py:1350`).
3. *"`materialise()` is the only writer of dependency edges"*: **false since F36.**
   `task_dependency_writer.add_dependency` has two callers: `spec_tasks.py:375`, and the
   operator's `POST /tasks/{id}/dependencies` (`tasks.py:1630`). So an operator can order a
   free-floating task, and an agent cannot. The amend trigger survives, narrowed to agents. Nothing
   in this change depends on the fact.

**The design in one line.** An assigned live task holds its assignee only if one of two things is
true:
- it belongs to a loop with `ending_state IS NULL`;
- a turn is running or queued **for that assignee**, naming it. This is F154's
  `tasks_with_a_turn_pending_or_running`, narrowed to the assignee.

Otherwise the task is a bookmark and costs nobody. All three callers move together, so the guard
and the walk still agree. There is no migration, no `mcp_server.py` edit and no UI.

**The six open questions:**
- **Answered, two.** The ten LoopEngine holdings (Q4): the eight outside the loop stop disqualifying
  `dev` and `dev_2` with no write, and the Architect stays held by F154's review nobody is doing.
  This takes effect on `:8000` only at the operator's next restart. Admission for an unassigned task
  (Q5): not needed for staffing once this ships.
- **Deferred with recommendations, three.** Q1: derive the owner from `created_by_run_id`. Nothing
  deletes a `Run`; the only deletes are `JobRun` (`:985`, `:2539`). Q2: an operator-created loop's
  owner is the operator, as the default. Q3: an admission row, not a status.
- **Deferred, one.** Q6, the Architect's eight suspended messages, needs a `:8000` read that this
  window's limits do not allow. It stays with F361.

**Positions R1 took that R2 should push hardest:**
- **D2: a paused loop still holds.** Pausing leaves `ending_state` NULL, and an ended loop cannot be
  re-enabled (`jobs.py:857-868`). This is flagged to the operator.
- **D3's last bullet.** A documentless loop can already recruit from the pool (`:1543-1550` has no
  document test), so plain loops get wider recruitment too. Is that wanted or incidental?
- **D4.** Is the queued arm, keyed to the assignee, right, or should any agent's turn count?
- **D5's F154 residual.** A review nobody is doing inside a live loop keeps holding its reviewer.
- **D6's cost.** The change adds three queries per pool call, and `resolve_reviewer` calls the pool
  once per reviewable candidate.
- **D7.** `test_reviewer_ladder.py::test_an_agent_holding_an_active_task_is_not_selected` stages
  its holding with no `loop_id`, so it will fail against the new code, correctly. It is re-staged
  inside a live loop and gets a sibling test.

**OPERATOR QUESTION, noted and not blocking.** Building this change answers F352-free as a sixth
option, (f) reachability, not any of the stopped change's (a) to (e). The operator set the direction
in session, and the redirect says this does not stop the change. The consequence:
`an-unstaffed-review-names-its-holders`' rung-3 half was written against (e), and must be re-derived
against (f) before it is built. That is carried to `decisions_for_user`.

**Proven:** `openspec validate --strict` passes. Every `file:line` citation in the proposal and the
design was read this iteration. `resolve_reviewer`'s call-site count was corrected from a wrong
first draft ("four") to two.
**Not proven:** anything about behaviour. No product code exists yet.

**State.** iteration 2; `next_action` = `a-task-nothing-will-move-holds-nobody-r2`.

## Iteration 3 — ~00:10 (2026-09-15) — R2: four repairs to the reachability predicate, two findings

**Branch check.** `autonomous/2026-09-14-daily`, HEAD `9e140e1` (R1), and the tree was clean.
This matches the state file. Nothing to reconcile.

**Done: R2** on `openspec/changes/a-task-nothing-will-move-holds-nobody/`.

**Method.** Re-derived from the code rather than from R1. R2 read:
- the nine `new_entry` callers;
- `_attempt_turn` and `release_entry`;
- both archive routes;
- the firing's persist and discard paths;
- F128, F222, F361 and F368.

Three throwaway probe tests went through the real app and helpers, and all three were deleted
before the commit.

**What R2 found wrong in R1, and repaired (each marked *Round 2* where it lands):**

1. **An archived loop held its agents forever.** R1's D2 said *"a loop archives only after it has
   ended"*. That holds for `POST /loops/{id}/archive` and not for `POST /jobs/{id}/archive`, which
   is the product's only way to retire a job. **Measured:** the archive answered 200, and the loop
   row read `ending_state None` with `archived_at` set. PATCH then re-enabled the job and answered
   200, which is F222, already open. `live` now requires `archived_at IS NULL`. Task 1.5 gets a
   fourth case and a mutation C, and the delta gets a scenario.
2. **The queued-turn arm counted input nothing will deliver.** Entries past the hop budget are
   never selected by `_attempt_turn`. Only the operator's release delivers them. R1's D1 said the
   budget "allows" delivery later, which is false: it is a project constant. LoopEngine's Q6
   *"eight suspended messages"* are F361's hop-7 chains. If they name their recipients' tasks,
   R1's arm keeps `dev` and `dev_2` held, and **the change fails on the one board it exists for.**
   The arm is now bounded by the budget. Task 1.6b, plus drive step 5.4b.
3. **The arm read a map that had already dropped the answer.** The F154 helper keeps one agent per
   task, through `setdefault` over an unordered select. **Measured:** it returned `alpha` over the
   assignee `dev` in both insertion orders, and `dev` over `zeta` in both. The rows come back in
   agent-name order. R1's `on_it.get(id) == assignee` therefore misses the assignee's own input
   whenever an agent whose name sorts first also has input naming the task. The arm now reads
   `(task, agent)` pairs from a new `run_task_binding` function (task 1.1a). Task 1.7b pins it, and
   its mutation is R1's own shape.
4. **R1's motivating example for the arm cannot happen.** An operator's input is queued at hop 0.
   Only peer messages go over budget. The arm's real cases are restated in D4.

**Also:**
- **D3's documentless widening is F128**, the operator's open decision, and this change widens
  where it fires. R2 proceeds without deciding it (reasons in D3), carries it to
  `decisions_for_user`, and adds an F128 note to archive task 5.6.
- **The Risk *"a firing proceeds only when it has something to say"*, sharpened.** That something
  can be a `review_unstaffed` that is true at the tick and gone at the next. This is shipped
  wherever a free author exists; it now reaches more projects.
- **The delta's "A busy agent is not selected" scenario had dropped the queued half.** It now
  defers to the new requirement.

**Checked and kept:**
- D1's status argument. `LIVE_STATUSES` minus the walk's status set is empty (measured).
- A waiting run is `running`, so *"has a live run"* is covered.
- D2's pause position.
- D5's F154 residual.
- D7's staging claim.
- The MODIFIED block, diffed against `openspec/specs/agent-flows/spec.md:231-349`. The only
  differences are the intended ones.
- The held requirement's *"Which tasks … is unchanged"* sentence is left alone, because it scopes
  that requirement (D5).

**Filed against shipped code:**
- **F370 (B).** The held-resume arm at `:1506`, shipped `c8e3bbd` yesterday, has item 3's masking,
  so a held assignee is re-briefed on every firing. The helper's answer is measured; the arm's
  consequence is by reading. Not repaired here, because that arm's right filter differs from the
  pool's (a suspended copy is still a copy).
- **F371 (C).** F154's wedged-review reader counts any agent's input, at any depth, as attendance.
  By reading.

**Proven:** `openspec validate --strict` says the change is valid. The three measurements above.
**Not proven:** anything about behaviour, since no product code exists. The drive steps are
untested plans.

**For R3** (listed in design.md's Round 2 section):
- the F108 question on the new decision paths;
- `project_limits`' raise at every pool caller;
- whether 1.7b's mutation can fire, given it depends on SQLite row order.

**State.** Iteration 3. `next_action` = `a-task-nothing-will-move-holds-nobody-r3`.

## Iteration 4 — ~00:20 (2026-09-15) — R3: the guard's empty-queue half, and a plan-proof 1.7b

**Branch check.** `autonomous/2026-09-14-daily`, HEAD `4779fca` (R2), and the tree was clean. This
matches the state file. Nothing to reconcile.

**Done: R3**, the second and final review, on
`openspec/changes/a-task-nothing-will-move-holds-nobody/`.

**Method.** R3 started from the code R2's repairs touch, not from R2's argument. It read:
- the guard and all three of its readers;
- every exit of `decide_firing`, and what `_do_fire_job` does after each one;
- both archive routes;
- `_attempt_turn`'s selection filter, `release_entry` and `create_message`'s depth rule;
- the F154 helper and the index behind its row order;
- `project_lifecycle.delete`;
- eleven whole requirements across `agent-loops` and `agent-flows`.

It ran three measurements in one throwaway test file, deleted before the commit.

**What R3 found wrong, and repaired (each marked *Round 3* where it lands):**
1. **A busy agent's empty loop briefs the busy agent on every firing, and this change would have
   carried that into LoopEngine-shaped projects.** None of R1, R2 or the change's tasks asked what
   the firing does when the guard passes and the walk answers `DECISION_PROCEED_EMPTY`. It briefs
   `job.agent`, which is the busy one. **Measured** on the current code, with a loop holding no
   task, its agent mid-turn and one free roster agent:
   - three firings gave 3 entries for the busy agent, 3 `JobRun`s and three `True`s;
   - `POST /jobs/{id}/run` gave **200** and a fourth entry.

   With the free agent holding only an out-of-loop task, today's rule refused all three (0 and 0),
   and this change removes exactly that refusal. It breaches `agent-loops` *"no inbound queue entry
   is created for that agent"*.

   **The repair is design D8.** The guard also refuses when the loop holds no open task, read
   through a new `_loop_has_open_task`, which uses `_stall_reason_from_walk`'s own predicate. The
   guard takes the `Loop`. The Run route's 409 names the half that refused. There are 6 new tasks
   (group 3b), each with a mutation, two of the mutations measured. `agent-loops` gets a delta, R1
   and R2 having said it needed none: two MODIFIED requirements, three scenarios, diffed against
   source, and the only differences are the intended ones. The agent-flows ADDED requirement gains
   a sentence and a scenario. Filed as **F372 (B)**, which the change retires at archive.
2. **Task 1.7b's mutation could fail to fire under another query plan.** R2's agent-name order
   comes from `ix_inbound_queue_project_agent_state_arrival` (`project_id, agent, …`). The test is
   re-staged with three agents, one sorting before the assignee and one after, and the assignee's
   entry inserted between them. The mutation now fires under name order, reverse name order,
   insertion order and reverse insertion order.
3. **Tasks 3.3 and 3.4 were under-staged.** 3.3's "returns `None`" case needs an open task on the
   guard's loop, or it tests D8 instead. 3.4 needs `live_scheduler`: without it the route answers
   **503** *"Job scheduler not available"* (measured), which is F48's hiding place.
4. **Sweep 2.3 would have stopped on D8's intended effect.** It now names that shape.

**F108, asked of all four decisions** (design D3, Round 3). The case is a busy job agent with
another agent free:
- `CLAIM` gives 200 `success`;
- `STALLED` gives 409 with the walk's reason, and the board agrees;
- `PROCEED_EMPTY` is item 1;
- `IN_FLIGHT` gives 409 *"already being worked"*, **except** after an earlier firing's `skipped`
  row, where Run answers with that stale reason. **Measured:** 409 *"loop queue is stalled: 1 still
  awaiting a prerequisite's approval"* for a loop whose only task was in flight. Filed as **F373
  (B)**, `jobs.py:1359` reading a row the press did not write. It is not repaired here, because it
  is the route's row comparison, not the pool. This change widens its reach, as it does F128's.

**Checked and kept:**
- D2: the job route stamps `archived_at` and leaves `ending_state`, and no route unarchives a loop.
- D4: the `<=` budget bound matches `_attempt_turn`, and `POST /messages` without a run queues at
  budget+1, so drive step 5.4b is sound.
- D6's `project_limits` raise: unreachable except a deletion racing a firing, which `_do_fire_job`'s
  `except` catches before any `JobRun` exists.
- Every other named mutation can fire as written.
- Two further statements of the old rule (`task_transition_service.py:397`, `scheduler.py:1419`)
  are still true and left alone.

**Proven:**
- `openspec validate --strict` reports the change valid.
- The three measurements above: F372's two rows and route answer, F373's route answer, and the 503
  without `live_scheduler`.

**Not proven:** any behaviour of the change itself, because no product code exists yet.

**Readiness.** R3 judges the change **ready to implement**. It is larger than R2 left it: one helper,
a guard signature at three call sites, the route's clause, and group 3b. Nothing touches
`mcp_server.py`, a migration or the UI.

**State.** Iteration 4. `next_action` is `a-task-nothing-will-move-holds-nobody-impl`.

## Iteration 5 — ~00:40–01:35 (2026-09-15) — IMPL: a task nothing will move holds nobody

**Branch check.** `autonomous/2026-09-14-daily`, HEAD `4af7b94` (R3), and the tree was clean. This
matches the state file. Nothing to reconcile.

**Done: groups 1–4** of `openspec/changes/a-task-nothing-will-move-holds-nobody/`, 29 of 37 tasks,
committed as `4b59ee0`. Group 5 (drive and archive) is the next queue item.

**What was built:**
- `run_task_binding.task_agent_pairs_with_a_turn_queued` (1.1a). It returns `(task, agent)` pairs
  for queued input within the hop budget, reading the budget through `project_limits`.
- `_agents_that_are_free` now holds an assignee only for a task in a loop that has not ended and
  is not archived, or one named by a queued turn for that assignee (1.1). The signature is
  unchanged.
- `_loop_has_open_task` is new, and the guard is now `_loop_flow_busy_reason(session, loop, agent)`
  with the queue half (3b.1). All three callers are updated. The Run route's `_job_has_loop`
  became `_job_loop`, and the 409 now names which half refused.
- Docstrings (1.8): the pool, ladder line 2, the `:1363` comment and the guard. The guard's false
  *"exactly as strict as before"* sentence is replaced with both halves and F128.

**Verified:**
- **28 new tests** in `hub/tests/test_a_task_nothing_will_move_holds_nobody.py`. D7's ladder test
  is re-staged in a live loop, and its sibling asserts the opposite.
- **15 mutations, each applied and reverted by a throwaway harness.** Every one fails its named
  test. The mutation and observed failure are recorded beside each tick in `tasks.md`. Two of
  them reproduce R3's pre-D8 measurement: drop the queue half and 3b.2 reads `assert 3 == 0`
  (three briefings for the busy agent), while 3b.6 reads `200 == 409` with `{"success":true}`.
- **1.7b's premise, measured.** The F154 helper answers `{'task-held': 'architect'}`, a
  non-assignee, and the test now asserts that premise before its real assertion.
- **The sweep (2.3).** Every 4.1 file was run against the new code before any re-staging. The only
  failure was D7's own test (464 passed). No test of D8's shape failed, and no unlisted consumer
  surfaced.
- **4.1:** 30 files, 491 passed and 3 skipped.
- **4.2:** ruff, black (`--target-version py311`) and mypy are all green, each run as
  `py -3.11 -m`.
- **4.3, the whole hub suite**, run as 10 chunks plus `tests/browser`: **4345 passed, 86 skipped,
  16 xfailed, 0 failed**. The gate at `c8e3bbd` was 4316/86/16, and the +29 is exactly the new
  tests.

**Found by mutation, and fixed before the commit.** Task 3.4 had both of its cases in one test.
Under M2 (drop the live-loop arm), the second case still answered 409, because the first case's
firing had already staffed `B` onto a live-loop task in the same database. The second case's
staging tested nothing. It is now its own test, and M2 fails it. This is the *"a test asserting
over nothing"* shape the playbook warns about. Only the mutation showed it.

**Deviations from `tasks.md`:**
- 3.4 is two tests, not one (above).
- Tests that fire a job patch `hub.turn_scheduler.schedule_agent` inert, so no process starts,
  because `claude` is on this machine's PATH. What they assert is the queued entry, which the
  firing writes before `schedule_agent` runs.
- The `live_scheduler` fixture is a local copy, following the suite's convention: three files
  each define their own.

**Environment trap, recorded in DEAD-ENDS.** The harness restored files with `Path.write_text`, and
on Windows that writes CRLF. `agents.py` showed as modified with no real edit. It was restored with
`git checkout`, and the other four with `sed 's/\r$//'`. Nothing reached history.

**Not proven.** The live product. The drive (group 5) is next.

**For the operator.** `:8000` runs this checkout, and a restart now picks this up. LoopEngine's
`dev` and `dev_2` then become free for its flow, while the Architect stays held (design D5), and
the first firings after that may staff reviews and new work in parallel. Carried to
`notes_for_tomorrow` for the 2026-09-15 review page.

**State.** Iteration 5. `next_action` is `a-task-nothing-will-move-holds-nobody-drive`.

## Iteration 6 — ~01:35–02:10 (2026-09-15) — DRIVE + archive: a task nothing will move holds nobody

**Branch check.** `autonomous/2026-09-14-daily`, HEAD `f84728d` (iteration 5's log), and the tree was
clean. This matches the state file. Nothing to reconcile.

**Done: group 5, tasks 5.1–5.6. The change is archived** as
`openspec/changes/archive/2026-09-15-a-task-nothing-will-move-holds-nobody/`, 37 of 37 tasks.

**The drive Hub (5.1).**
- Port **8013**. `netstat` showed only `:8000` listening, and `:8010` was down.
- A fresh `profiles/drive0915/agentweave.db`, migrated to `0103`, launched from `hub/` from source
  with a named key.
- No `.py` under `hub/hub` or `src` was newer than the process start.
- Project `proj-4297ab5fd02c`, in a fresh temporary repository.
- The harness is `scripts/drive/t_d0915_reachability.py` (new). It has two phases, `author` and
  `lanes`, with state kept on disk between them.

**How it was staged.** The review arm refuses a task that has no evidence naming a commit. And an
operator-walked completion names no author, which would put the job agent itself on the ladder. So
`alpha` authored every lane's task in a **real Haiku turn**. There were four one-task flows, created
before `beta` and `gamma` existed. That phase went 18/18: each task reached `completed` with evidence
naming a commit.

**What the drive showed.** Every row is a real route, and every staffed review is a real Haiku turn:

| task | answer |
|---|---|
| 5.2 LoopEngine's shape: bookmarks from `POST /tasks`, assignee, no `loop_id` | **200**. Staffed with `beta`, who ran a review turn and recorded `revision_needed`. **No `review_unstaffed`** |
| 5.3 control: both holding tasks in a second **live** loop | **409**, today's rung-3 sentence, one `review_unstaffed` |
| 5.4 paused | **409**, the same sentence (design D2) |
| 5.4 ended | **200**, staffed |
| 5.4 archived through the job route without a stop, `ending_state` null on `GET /loops/{id}` | **409** before the archive, **200** after |
| 5.4b a peer message naming `beta`'s bookmark, at `hop_depth` 7 with budget 6, `queued` | **200**, still `beta` |
| 5.4c an empty loop on `alpha`, Run pressed while `alpha`'s real turn ran | **409**: *"alpha is already running a turn, and this loop's queue holds no open task for another agent to take. Nothing was started."* No job entry and no history row. **Driven, not only 3b.6** |

- 5.5: all seven jobs read `enabled = 0` in the database, the archived one included. The drive Hub
  is stopped and `:8013` is closed. `:8000` was never touched.

**Harness defects, found and fixed during the drive.** None of them came from the product:
- **The bare `claude-haiku-4-5` is refused by runner creation.** The catalog holds
  `claude-haiku-4-5-20251001`. That cost one launch, and it is recorded in DEAD-ENDS.
- **`beta` and `gamma` were never created** in the lanes phase. This was caught before it ran.
- **5.4b's first check read `task_id` off `GET /queue/{agent}`, which does not expose it**, so it
  failed. The entry's `task_id` was confirmed as the bookmark by a `mode=ro` read of the drive
  database, and the check now reads the message and the queue settings instead. That lane's
  evidence is the database read plus the corrected inputs, not a clean harness run. **Say so if
  anyone re-reads this.**
- **The teardown listed jobs without `include_archived`.** Harmless tonight, because archiving had
  disabled the job, but now fixed.
- **Between lanes, the harness ends any flow whose finished review a reviewer still holds.** A
  `revision_needed` or `under_review` task in a live loop holds its reviewer, correctly under this
  change. So each lane starts with both bookmark holders holding nothing in a live loop, and the
  harness checks that.

**Found by the drive: F374 (B), filed, not fixed.** The drive left evidence `awaiting`. Both `beta`
and `gamma` then had `approved` refused by the shipped evidence gate, and each stopped correctly (as
F152's drive wants). But `run_divergence.py:430-471` treated `beta`'s stop as a review with no
verdict and **re-staffed it to `gamma`**. That spent a second turn on a refusal no reviewer can
clear. `gamma`'s divergence was then surfaced as *"no agent is free"*, while `beta` sat idle, barred
as a silent reviewer. The sentence does not name that bar or the unjudged evidence. This change
widens the reach: under the old rule, `gamma`'s bookmark would have made it busy. Also seen, and
correct: `record_evidence` without a document in a multi-document project is refused with a clear
422.

**The archive (5.6), in one commit:**
- Both deltas were hand-synced. `agent-flows` took one ADDED and one MODIFIED requirement (20 to 21
  requirements), and `agent-loops` took two MODIFIED (42 to 42). Both pass `openspec validate
  --strict --type spec`.
- `openspec archive --skip-specs -y` ran next, and the specs' md5 is identical across it.

**The ledger:**
- **F352:** *open for the visibility half; the definition half is fixed `4b59ee0`*, plus a driven
  table.
- **F372:** fixed `4b59ee0`, driven.
- **F128 and F373:** each has a dated note saying the change widened its reach, and each stays open.
- **F370 and F371:** untouched and open.
- **F374:** filed.
- **The index.** A *Revised 2026-09-15* paragraph gives the open-A set as **five**: F299, F301, F325,
  F332, F352. The r9-close paragraph said four, because F352 (filed 2026-09-13) never entered it.
- **F313's rule held.** The census was captured before the edits and recomputed after. Exactly F372
  moved from OPEN to RESOLVED, and F374 is new as OPEN B.
- **Two classifier traps were hit and reworded, not shipped.** F374's aside first read *"not a
  defect"*, and the index first put F352 on one line with *"fixed"*. That flipped F374 to RESOLVED
  and F352 to CONFLICT. The final census moves only as stated.

**Not proven:**
- **LoopEngine itself.** `:8000` picks this up only on the operator's restart, and the test guide's
  human-only item 1 is theirs.
- **The drive staffed reviews but landed nothing**, because evidence stayed `awaiting` by design of
  the drive. Landing is not what this change touches.

**State.** Iteration 6. The next item is `f356-impl` (`a-late-answer-is-delivered`, approved: 31
tasks, 0 ticked). Its start is the design's Round 4 section.

## Iteration 8 — ~02:55–03:40 (2026-09-15) — IMPL + DRIVE + archive: a late answer is delivered (F356)

**Reconciled first: an iteration 7 ran and died without committing.** HEAD was `0cdbdf0`, as the
state file said, but the tree was dirty:
- six product files, two modified test files, and a new `test_a_late_answer_is_delivered.py`;
- a drive harness, `t_d0915b_late_answer.py`;
- `tasks.md` with groups 1–3 ticked (19 of 31), each tick naming its mutation and observed failure.

Its last write was at 02:30. It left no log entry, no state change and no drive profile, and
`:8011`–`:8019` were all closed. Nothing was started for it. I took its work as a draft to verify,
not as done.

**Verified, not believed.**
- Read the whole diff against the design, including Round 4.
- The six question-touching test files: 121 passed.
- **Three of iteration 7's recorded mutations were re-applied byte-exact** (1.2's `wait_ended_at`
  arm, 2.1's `declined IS FALSE`, 2.11's `populate_existing`). Each failed its named test with the
  failure iteration 7 recorded, and each file was restored hash-identical.

**Found on resume: Round 4 F-H was only half done. Measured, fixed, tested.**
- Iteration 7 read `run.id` into a local before the report's loop, as the design asked. But
  `release_block_for_expired_wait` reads `run.id` and `run.agent` itself.
- A probe measured it. After one question's release raised and rolled back, the next question's
  release hit `MissingGreenlet` on the expired run. That was caught and rolled back as well, so it
  was not accepted. **One failed release took every later one in the report with it.**
- The fix re-loads the run after the rollback, in one line. The test
  `test_one_failed_release_in_a_report_does_not_fail_the_next` reports the failing question first
  and the real release second. With the re-load mutated out, it fails `assert [] == ['q-…']`.

**Gate (4.1–4.3).**
- Whole hub suite, one run: **4361 passed, 86 skipped, 16 xfailed, 0 failed** (28 min). The +16
  over 4b59ee0's 4345 is exactly the change's new tests. It was taken before the F-H test existed;
  after it, the six files ran 122 passed.
- The CI lint set is green before and after the F-H edit.
- `openspec validate --strict`: valid.
- Committed as **`d16a76a`** and pushed. **Correction:** that commit's message says *"26/37
  tasks"*. It was 22 of 31. History is not rewritten, so the correction lives here.

**Drive (5.1–5.7): 22/22 on `:8014`.**
- Setup: fresh `profiles/drive0915b` migrated to `0103`, `proj-4b7ded73f250`, runner
  `claude-haiku-4-5-20251001`, and one agent with `question_timeout_seconds=10`.
- Iteration 7's harness needed two changes. The agent now gets `bypassPermissions`, since the lanes
  run a shell wait. The wait itself is a Python sleep rather than `sleep N`, in case the agent CLI
  refuses a long foreground sleep; that refusal is not measured.

| Step | Observed |
|---|---|
| 5.2 | Both waits stamped by the tool's report at deadline **+0.07 s**; run `running`; list and detail both `asker_waiting: false` |
| 5.3 | Both answered late → **one** entry carrying both, `queued` behind the live run → delivered into a new turn **0.21 s** after the asking run ended, which replied *"the banner should be blue and large"*; nothing else queued |
| 5.4 | Control: the tool returned `pear`, no stamp, no entry |
| 5.5 | **Grace window not hit.** Answered at +0.32 s, but the report had landed at +0.04 s, so this was 1.2's path again (one entry, a new turn). The tool returning a grace-window answer is unit-tested only (1.3) |
| 5.7 | No run `running`, no job exists, Hub stopped, `:8014` closed, `:8000` untouched |

- **Not driven:** the report-delivers-it path (2.2, 2.3, 2.9). It needs an answer in the ~1 s
  between the tool's last poll and its report. Unit tests cover it through the real routes.
- **The drive Hub is one line older than `d16a76a`.** It started before the F-H re-load was written.
  That line runs only after a failed release, which the drive never provoked.

**Archive (6.1, 6.2).**
- The MODIFIED requirement is hand-synced into `openspec/specs/run-task-binding/spec.md`: 22
  requirements before and after, and `--strict --type spec` is valid.
- Archived as `openspec/changes/archive/2026-09-15-a-late-answer-is-delivered/` with
  `--skip-specs`. The spec's md5 is identical across the archive.
- **F356 is `fixed d16a76a`.** D5's residual (four routes, one of them the F-F candidate) sits in a
  paragraph under the Status line, not on it, with the drive table and F-H in a retirement note.
- The classifier census moved by exactly F356, from OPEN to RESOLVED in B (87→86 and 69→70).

**What reaches `:8000` on the operator's next restart.** No migration. The answer and decline
routes now queue an answer given after `ask_user`'s wait ended while the run is still working. The
question card reads *nobody waiting* from the wait's end, not the run's. One ordering change: the
answer route's **persisted** `question_answered` event now comes before `queue_entry_queued`. The
SSE broadcast order is unchanged, which I read from the diff. Iteration 7's grep found no test
reading the persisted order; I did not sweep UI consumers of the event log for it.

**To distrust.**
- I tested code I finished and did not write most of. The mutations were re-run and the drive is
  independent of both of us, but iteration 7's other 14 recorded mutations were not re-run.
- 5.5 names the path it actually took, not the one its label assumed. The harness's
  *"(tool reported it)"* label is loose: the report reported nothing answered, and the answer route
  delivered it.

**State.** Iteration 8. `f356-impl` is done. The ORDER items are all finished, so the queue falls
through to the deprioritised backlog: `ledger-conflicts` next, then `ledger-unclassified`, which
closes the window.

## Iteration 9 — ~03:40–04:00 (2026-09-15) — ledger-conflicts: the four CONFLICT verdicts, hand-checked

**Position verified.** HEAD `a099af2` on `autonomous/2026-09-14-daily`, tree clean, as the state
file said.

**Method.** I captured the classifier's output before any edit (`/tmp/census-before.txt`) and
printed the evidence behind each CONFLICT. Each finding was checked against code, git and CI
rather than the ledger's prose. Only `scripts/drive/FINDINGS.md` changed. There was no product code.

| Finding | What made it CONFLICT | What is true, and the evidence | Now |
|---|---|---|---|
| **F52 (A)** | *"while that stays open"*, a 2026-08-26 sentence superseded by Correction 3; its Status line said *"partially fixed"*, which no STRONG marker matches | Closed 2026-08-27 as out of scope, and no open-A index has carried it since. `68459ea`, `0cda570` and `57eb92b` are ancestors of HEAD. `auto_snapshot_notice` is still wired (`launchability.py:404`, `agent_trigger.py:1079`), and its 3 `test_f52_*` tests passed tonight. Allows are still unrecorded (`agent_actions.py:940`), so a recurrence would be as unfalsifiable as before | `RETIRED`, **RESOLVED** |
| **F292 (B)** | A line in another section: *"F285 was / fixed at `d9ad1e0` … but **F292**"* broke after "was", so "fixed" sat on F292's line | **Open, still reproducing.** Measured tonight over every `ci.yml` run created after 2026-09-13T02:50Z: 84 completed, 23 red. **19 of the red are F292** (`database is locked` on `BEGIN IMMEDIATE` at setup), including `master` `f28bc31`. The other 4 are F314's event-loop `RuntimeError`, and 3 runs carried both. It is the same three tests, the mitigation `af69a27` is in every measured sha, and `conftest.py` is unchanged since 09-12 | **OPEN** |
| **F335 (D)** | A quotation in F251's section. Quote-pair parity (blind spot 10's paragraph unit) left the word *resolved* outside the pair | **Open, latent behind F251.** The broadcast is still at staging time (`run_divergence.py:104`), and `SSE_EVENT_TYPES` (`useSSE.ts:21`) still omits the kind. F251 is still open | **OPEN** |
| **F338 (D)** | F328's own Status line, *"fixed `a5cb384` … filed as F338"*, on one physical line | **Open, source-read only.** `deliver_entries_with_run` has the same shape, now at `inbound_queue.py:133-170` (moved by `98385cd`): it selects queued rows at `:142-152`, then adds the `Run` at `:160` and sets `delivered` at `:161-164`. Not measured, not driven | **OPEN** |

**How the three false leads were removed.** I did not change any wording.
- The F285 line was re-wrapped so that *"fixed at"* ends the line naming F285.
- F251's quotation became the ledger's italic-quote form, `*"…"*`, which the first dequote pattern
  pairs before any parity question arises.
- F328's Status line was wrapped before *"and is filed as F338."*

Each of these is the F313 floor that the classifier header describes: a line about one finding's
fix reads as a verdict on another.

**Every Status line is now one physical line with its evidence.**
- F292's and F335's multi-line status paragraphs were kept below the new line, relabelled
  *"Status as written 2026-09-11"* and *"Narrowed by the 2026-09-13 day drive"*.
- F338's original sentence was kept as a second paragraph.

**Census, under F313's rule.**
- Before: A `CONFLICT=1 OPEN=5 RESOLVED=60`, B `CONFLICT=1 OPEN=86`, D `CONFLICT=2 OPEN=15`.
- After: A `OPEN=5 RESOLVED=61`, B `OPEN=87`, D `OPEN=17`, and **no CONFLICT anywhere**.
- Stripped of line offsets, the only differences are those four verdicts. F285, F251 and F328 keep
  theirs (RESOLVED, OPEN and RESOLVED).
- A dated index paragraph was added after the 09-15 open-A revision. A re-run with it in place was
  identical, and the open-A set is unchanged at five.
- `tests/test_classify_findings.py`: 23 passed.

**To distrust.**
- The CI tally reads `--log-failed` text with grep: `database is locked` plus `BEGIN IMMEDIATE`
  for F292, and `different event loop` for F314. It is not the method of the table in F292's
  entry, re-run.
- One run was in flight (`a099af2`) and one was cancelled (`0ef2f85`). Both are excluded.
- F52's `RETIRED` writes down a closure that the ledger already acted on, which is not a new
  decision. The residual it names (allows leave no record) is not filed as its own finding.

**State.** Iteration 9. `ledger-conflicts` is done. Next is `ledger-unclassified`, which also closes
the window.

# 2026-09-14 night window

Newest entry at the BOTTOM. Playbook: `.claude/loops/night-window.md`.

Armed 2026-09-14T22:55:02+01:00 on `autonomous/2026-09-14-daily` (parent master@f28bc31), model opus.

## Iteration 1 — ~23:00 — compose the queue

**Branch check.** `autonomous/2026-09-14-daily` at `92ffba1` (the arm commit), clean, matching
`STATE-night.json` (iteration 0, `next_action` compose). Parent `c69394c` is the day window's d5.

**APPROVALS.md `## 2026-09-14`.** Written by the FILL window at 18:18 and untouched since
(`git log -- spec-queue/APPROVALS.md` newest is `c69394c`). Both rows, `a-late-answer-is-delivered`
and `an-unstaffed-review-names-its-holders`, carry **no status token**. There is no `ORDER:` and no
`NOTHING TONIGHT`. The 2026-09-13 section's `APPROVED a-quote-can-spell-a-slash` and its `ORDER:` are
history now, as the section itself says. So the default queue applies. Walked against the tree:
- **Changes that only need archiving: none.** `openspec/changes/` holds three changes, none built.
  Counted tonight from `tasks.md`: `a-late-answer-is-delivered` has 0 of 31 ticked,
  `a-quote-can-spell-a-slash` 3 of 30, and `an-unstaffed-review-names-its-holders` 0 of 40.
- **Open A findings.** `classify_findings.py` run tonight gives 5 open (F299, F301, F325, F332,
  F352) plus 1 CONFLICT (F52). None of the five can be built. F299's change was rejected. F301 has
  no proposal. F325 is Codex, which is undrivable. F332's rule is OPEN. F352 is stopped on an
  operator question.
- **Open B.** 84 open. The only one with a proposal is F356, and its change is unapproved.
- **APPROVED rows: none.**

So, as the review page's §5 predicted, **tonight lands no feature.** That also suits the
`weekly-window-care` decision (`DECISIONS.md`, 2026-09-14 afternoon): the night stays lean.

**Is the tree green?**
- **Hub suite.** It was gated whole and green at `c8e3bbd`: 4316 passed, 86 skipped, 16 xfailed
  (`b5b6baf`). `git diff --stat c8e3bbd..HEAD -- hub/hub hub/tests src tests hub/ui/src` is empty.
  I did not re-run it: it takes 15 to 47 minutes and the code under it has not changed.
- **CLI suite, measured tonight.** `py -3.11 -m pytest tests/ -q -x`: **521 passed, 3 skipped**,
  in 46 s.

**The queue.** The one piece of backlog work that can be built is ledger hygiene. It needs no
proposal, and it is documentation only.
1. **`ledger-conflicts`.** Hand-check the four CONFLICT verdicts: F52 (A), F292 (B), F335 (D) and
   F338 (D).
   - The 2026-09-12 night log queued this hand-check for F52 and F292.
   - Every window since has re-noted "the F52 conflict", on each review page and in the day logs
     of 09-13 and 09-14, and none has done the check.
   - That is the playbook's *recording is not fixing* case, so it becomes a queue item.
2. **`ledger-unclassified`.**
   - Classify the five UNCLASSIFIED sections: F185 (B), F135, F266, F272 (C), and F164 (D).
   - Hand-check F108's RESOLVED_ELSEWHERE lead.
   - Then write the window's final entry and null `next_action`.

**Not queued:**
- F356's build, because it is unapproved.
- The F352 split, which is day-window work.
- The eleven unreached fixes, which have no proposals.
- DIRECTION.md `## 2026-09-15`, which belongs to the evening session.

These are carried in `notes_for_tomorrow`, and the three operator decisions stay in
`STATE-day.json`.

**State.** iteration 1; `next_action` = `ledger-conflicts`.

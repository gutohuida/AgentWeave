# 2026-09-18 night window

Newest entry at the BOTTOM. Playbook: `.claude/loops/night-window.md`.

Armed 2026-09-18T22:55:02+01:00 on `autonomous/2026-09-17-daily` (parent master@f9db321), model opus.

## Iteration 1 (compose) — 2026-09-18 ~23:05 UTC+1

**Branch confirmed:** `autonomous/2026-09-17-daily`, matches `STATE-night.json`. `git log` top
commit is tonight's arm (`4cef3ee`), parent chain intact back through `f9db321`. Clean tree at
start.

**Backlog page:** `py -3.11 scripts/backlog_page.py --check` reported current (exit 0, "nothing
moved since the last generation"). Regenerated anyway for the structured `data-sev`/`data-ready`
attributes in `spec-queue/BACKLOG.html`, which this iteration greps directly rather than reading
the printed report (the console summary carries only warnings, not the per-finding ready state).

**APPROVALS.md:** newest section is `## 2026-09-18` — matches tonight's arm date, so it is read.
Written by the day window, iteration 5, for `a-refused-capability-reaches-the-operator`
(F376 severity A / F378 severity B): three spec rounds done today, `openspec validate --strict`
passes, 0/24 tasks, nothing implemented. **It carries no status token at all** — the section says
outright "That is the operator's to write" — so it is neither an `ORDER:`, nor `NOTHING TONIGHT`,
nor an `APPROVED` row. Treated per the playbook as "no section for today, effectively": nothing
here authorizes a build. `spec-queue/DECISIONS.md` has no entry dated after 2026-09-15.

**Backlog, item by item** (using `BACKLOG.html`'s `data-ready` field: `proposed` = a change
directory already names it, `ready` = severity set but no proposal exists yet — the day window's
job, `triage` = missing status/severity):

- **Unarchived changes:** exactly two, `a-refused-capability-reaches-the-operator` (0/24 tasks,
  gated on the operator token above) and `an-unstaffed-review-names-its-holders`, whose `tasks.md`
  still carries the dated stop-notice from 2026-09-14 ("STOPPED AT REV... Do not build any task
  here") pending a re-derivation round against `F352-free`'s decided option (f). Neither is
  buildable tonight; neither is "implemented and only needs archiving".
- **Open findings, severity A** (8 total; 2 closed-but-mislabeled RETIRED headings excluded,
  leaving 6 genuinely open): F299, F301, F325, F375 are `ready` (no proposal — day-window work, not
  FIX). F154 and F352 are `proposed`, but both live in `an-unstaffed-review-names-its-holders`,
  the STOPPED change above. **F376/F380 are `proposed` in `a-refused-capability-reaches-the-operator`,
  the exact change gated on the missing operator token.** Cross-checked whether F299/F301's mention
  in three already-archived proposals (`a-url-is-not-a-path`, `an-absent-approver-is-not-named`,
  `a-quote-can-spell-a-slash`) meant the ledger status was stale: `BACKLOG.html` still classifies
  them `ready` (no active change directory names them now), consistent with `FINDINGS.md`'s own
  "open" status — those archived changes evidently only cited F299/F301 as context, did not close
  them. No ledger correction needed here.
- **Every severity-B/C `proposed` finding checked** (F14, F292, F314, F354, F377, F378, F379, …)
  resolves to one of the same two change directories — there are only two unarchived change
  directories in the whole repository, so no third path exists.
- **APPROVED rows:** none. No `ORDER:` either.

**Conclusion: the entire backlog is decision-gated tonight, identically to 2026-09-17's compose
(same file, same conclusion, one night later) — with the addition that a fully-rounded proposal
(`a-refused-capability-reaches-the-operator`) now exists and is waiting on exactly one thing, the
operator's status token in `spec-queue/APPROVALS.md`'s `## 2026-09-18` section.** Nothing in
tonight's queue is buildable without spec work (F299/F301/F325/F375) or an operator decision
(F376/F380, and separately the `an-unstaffed-review-names-its-holders` re-derivation). Per the
playbook, this is itself the finding worth recording, not just a note.

**Tree health:** not independently re-verified this iteration — the conclusion above does not
depend on it (nothing is being built on top of it either way), and 2026-09-17's compose already
spent a background run on the same question without resolving it before its own close-out. Left
unverified and flagged below rather than spending this iteration's budget on a full suite run that
gates no work.

**Decisions for the operator** (also in `decisions_for_user`):
1. `spec-queue/APPROVALS.md`'s `## 2026-09-18` section needs an explicit `APPROVED` / `REVISING` /
   `REJECTED` token for `a-refused-capability-reaches-the-operator` before any FIX window can build
   it — three rounds are done and waiting.
2. `an-unstaffed-review-names-its-holders` needs a re-derivation round (against `F352-free`'s
   decided option (f), not the (e) its remaining tasks were written against) before it can build —
   spec work, not FIX work.
3. The merge gate's cadence (today's review page §1) is still undecided: three of four conditions
   have held at two consecutive firings, failing only because each firing's own commit restarts a
   13+ minute CI clock faster than firings arrive; three options are on the page, none picked.

**Queue set to empty; `next_action` set to `null`.** No model invocation should be spent
rediscovering this backlog is blocked.

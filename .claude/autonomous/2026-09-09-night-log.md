# 2026-09-09 night window

Newest entry at the BOTTOM. Playbook: `.claude/loops/night-window.md`.

Armed 2026-09-09T22:55:07+01:00 on `autonomous/2026-09-08-daily` (parent master@bdb5a79), model opus.

---

## Iteration 2 — F142 driven, and the mode written to drive it can no longer reach the arm

### Reconciliation first

Iteration 1 composed the queue and **never committed it**. `STATE-night.json` was dirty at
`iteration: 1` with a thirteen-item queue in the working tree, and the log carried nothing below the
arming line — so the playbook's *"never end an iteration with a dirty tree"* was broken by the
firing that wrote the rule's own queue. Branch (`autonomous/2026-09-08-daily`) and head (`54ed088`)
both matched what `STATE` claimed; nothing else disagreed. The composed queue is carried forward
unchanged and lands in this iteration's commit.

**Iteration 1's step 3 — confirm the tree is green before building on it — was also not done**, and
this iteration did not do it either: the item in `next_action` is a drive against a live Hub, which
does not read the suite, and a 15–47 minute run belongs to a firing that has something to build.
Recorded so the next firing that *does* build inherits the fact rather than assuming it.

### The drive

Hub on `:8011` from source from `hub/` (`py -3.11 -m uvicorn hub.main:app`), throwaway database
under the temp tree, migrated to head **`0102`** — CLAUDE.md still says `0101`. `0 .py` under
`hub/hub` or `src/` newer than the process start. `:8000` (pids `6240`, `10556`) and `:8010` (not
running) untouched. Three throwaway projects, `proj-2604d47e1295` / `proj-4882c91d39e2` /
`proj-490d5df86b3d`; `proj-d85a82bf4216` never a target. Every real turn Haiku. No job left enabled
— checked across all three projects after the fact, not merely by the harness's own `finally`.

**`AW_COMPLETE_BY=operator` cannot reach F142's arm any more.** F140's repair 1 shipped, so the
flow's briefing now says *"call `update_task(..., status="completed")` when the work is done"*, and
the agent does — 22 s into firing 1. Section C's `PATCH {"status": "completed"}` then lands on a
task that is already `completed`, is answered **`200`, and writes no transition row**. The task's
history has `run/completed` naming the agent, so `completion_attribution` returns an agent and the
walk takes the arm at `scheduler.py:1548` — the one design D6 calls byte-identical to what shipped.
19/20 held and none of it was about this finding. **This is the shape the round discipline keeps
finding: a green run agreeing with the wrong thing**, and only reading the transition rows told the
two apart.

**`AW_COMPLETE_BY=operator_after_agent`** was added to `t_row12_review_leg.py` to reconstruct the
world F142 actually describes: the flow is not fired, the operator walks the task to `in_progress`,
the agent works it on a turn **bound to the task** and is told not to move it, and the operator
makes the `-> completed` transition. Sequence 7 on `task-9529de5132cd` is
`in_progress → completed`, `actor_kind='operator'`, `actor_agent` **NULL** — the defect condition,
on the record.

**It holds. 20/20.** Firing 2 returned `200`, staffed `r7bf142b` (not the author), the reviewer got
the review briefing, and the task reached **`approved`** 84 s later. The `409` with the status
histogram did not occur.

**The arm is proved rather than inferred.** With one bound agent
(`AW_ALLOW_NO_REVIEWER=1`, 13/13) the firing refuses `409` and the refusal carries
`excluded_because`: *"…or **has worked on this task** and so may not review it"* — `scheduler.py:1575`,
the operator arm. The agent arm's *"is the one that completed this task"* is absent. That sentence
is the only place outside the database where the two `None`-author arms are distinguishable, which
is why the no-reviewer variant is a measurement and not a degraded case.

### Harness changes, all in `scripts/drive/t_row12_review_leg.py`

- the fourth mode, and the docstring saying why `operator` no longer answers the question;
- `AW_ALLOW_NO_REVIEWER=1`, and the acceptance check conditioned on the pool — the `409` **is** the
  expected answer where nobody is eligible, and asserting acceptance there failed the run for
  behaving as F142 requires (that is the one `BAD` in the first no-reviewer run, now 13/13);
- the stale `"...and is in_progress after the turn, not completed (F140)"` assertion **swapped** to
  assert the fixed behaviour, per the file's own convention.

`ruff` clean. `black` would reformat the file wholesale — it never matched black's style and CI does
not cover `scripts/`, so it was left alone rather than buried under 200 lines of reflow.

### Two observations, neither a defect

1. The stall sentence does not contain the task ID: `_stall_reason_from_walk` surfaces
   `unstaffed[0][1]`, the reason alone, while the ID sits in `unstaffed[0][0]`. It is *about* the
   task rather than about the queue, which is the claim that matters, and the loop card carries
   `current_tasks` beside it.
2. In the `operator` run the reviewer's turn ended without a verdict — and the loop said so by name
   rather than blaming the queue. The same agent reached `approved` on the same fixture shape in the
   `operator_after_agent` run, so it is an early-ending turn, correctly reported.

### Not done, deliberately

**F142's Status line is not `fixed`.** Row four is undriven and it is the next queue item; closing
the finding, ticking `DRIVE-1`, and correcting the index paragraph all belong to that item, which
already says so. The Status line says *driven and it holds, one arm uncovered* — which is what was
measured.

The `:8011` Hub is left **running** (pid `7360`, started 22:37:51Z on `54ed088`) with its three
fixtures, so the row-four drive does not pay for a restart. **Any firing that changes a file under
`hub/hub` or `src/` must restart it first** — that check is the reason this drive can claim it
tested this checkout.

`iteration` 2, `current` → `F142-drive-row4-and-close`, 13 items, two done.

---

## Iteration 3 — row four holds, and the arm it holds on is a self-approval route

### Reconciliation

Branch `autonomous/2026-09-08-daily` and head `352375f` both match `STATE-night.json`. Tree clean on
entry — iteration 2 repaired iteration 1's breach of that rule and did not repeat it. **The suite is
still unrun this window** (iteration 1 skipped step 3, iteration 2 recorded the skip); this
iteration is again a drive against a live Hub and again did not read it. The first firing that
builds product code inherits that debt, and `clearing-1-dialog` — the next item — is that firing.

### The drive

The `:8011` Hub was **reused, after re-checking rather than inheriting the check**: pid `7360` alive,
started 22:37:51Z on `54ed088`, and `0 .py` under `hub/hub` or `src/` newer than that. A **fourth**
throwaway project, `proj-f741298c9c6b`, built fresh with `setup_row7.py` — row four's whole claim is
that no record associates any agent with the task, so a reused fixture would have been arguing with
the premise. Haiku on every turn. `:8000` and `:8010` untouched, `proj-d85a82bf4216` never a target,
and no job enabled afterwards on **any** of the four projects, checked by API rather than trusting
the harness's `finally`.

**`AW_COMPLETE_BY=untouched` — 19/19, 0 BAD.** The agent is triggered without `task_id`, so
`run.task_id` stays NULL and `assignee` is never written; the operator then walks
`pending → in_progress → completed` by hand. On `task-930385ddb031` sequences 16–18 are all
`actor_kind='operator'`, `actor_agent` NULL. Firing 2 returned **`200`**, a reviewer was staffed, the
review briefing was correct on all five of F143's swapped assertions, and the task reached
**`approved`**. No status histogram.

**F142 is closed.** Both arms are now driven — `operator_after_agent` (20/20, iteration 2) and
`untouched` (19/19, here). Status line set to `fixed f3a778f`, driven and closed; `DRIVE-1`'s
preamble in the archived change document rewritten from *"not driven"* to the two runs' evidence;
the index paragraph carries a new dated revision rather than an edit to a dated line.

### What the drive found — F306 (A), filed, not fixed

**The reviewer it staffed was the agent that wrote the code.** `r7af142d` did the work on an unbound
turn, recorded `implementation` evidence naming the task (`ev-c75844228ecc`, `actor_kind='agent'`,
`review_state='accepted'`, on a run whose `task_id` is NULL), and then approved its own work at
sequence 19.

`agents_that_may_have_authored` is the union of exactly three sources — transitions, `assignee`,
runs bound to the task — and all three were legitimately empty. **The evidence table is a fourth
record and it is not read.** By the function's own stated principle (*"a record associating an agent
with a task is sufficient to exclude it"*) it is the strongest of the four: the other three are
circumstantial, while an accepted implementation evidence row is the agent asserting *this is my
implementation of this task*, carrying the very commit the reviewer is handed.
`_guard_author_is_not_reviewer` misses it too, for the same reason — it compares
`agent_that_completed`, which an operator completion leaves NULL.

The branch that produced it **says in its own comment that this must not happen**
(`scheduler.py:1566-1571`): *"`exclude=set()` is a self-approval route … So the ladder gets every
agent any record associates with the task."* The widening returned `set()` anyway. It is not wrong,
it is incomplete — and where it is incomplete it is indistinguishable from not having been done.

Severity **A**: it fails *open* and silently, which is what separates it from `F167` (B), the
adjacent residual on the same repair that fails closed. **Not fixed here** — it changes who may
review a task, so it needs the round discipline and this window does not write proposals. It is day
window material and lands in front of that window through `git diff` on `FINDINGS.md`, which its
playbook reads at step 3. Queued as a note, not as work.

**A truly untouched task is unaffected**: `POST /spec/evidence` records as `Actor(kind="operator")`,
so an operator can supply the commit-naming evidence the review arm requires with no agent involved,
and there the empty exclusion is correct. The defect is the case where the evidence came from an
agent and nothing else did.

### What to distrust in this iteration

**The harness's own row-four assertion cannot see F306**, and it passed truthfully: *"with NOBODY
excluded — any bound agent may take it"* accepts `reviewer in pool or reviewer == AGENT`, so a
correctly-empty exclusion and an exclusion that missed the author are one string to it. Only reading
`task_transitions` and `requirement_evidence` in the drive database told them apart. Two iterations
running, the green run agreed with the wrong thing and the rows disagreed.

**Unverified, stated as such:** whether the reviewer ladder's choice is deterministic when several
agents are eligible. Here the pool was two and it picked the author; nothing was measured about why.
No code was changed this iteration, so nothing was linted, mutation-checked or re-tested — the
commit is documentation only.

`iteration` 3, `current` → `clearing-1-dialog`, 13 items, three done, one new note.

---

## Iteration 4 — the dialog exists, and it cannot yet be opened

### Reconciliation

Branch `autonomous/2026-09-08-daily` and head `e6da0a0` both match `STATE-night.json`. Tree clean on
entry.

**The inherited debt was paid, and it is the first thing in this entry rather than the last.**
Iterations 1–3 all skipped the playbook's step 3 — confirm the tree is green before building on
it — and each recorded the skip because each was a drive rather than a build. This is the first
firing that writes product code, so it started `py -3.11 -m pytest hub/tests/ -q` in the background
before reading a single source file. **It is still running at commit time** — roughly 27% after
twenty minutes — and its result is recorded in the addendum at the end of this entry rather than
guessed at here. Committing ahead of it is defensible for exactly one reason, stated so it is not
mistaken for a general licence: **this change modifies no Python file at all**, so whatever
`hub/tests/` says, this commit cannot be the cause. If it is red, that is the inherited breakage
`next_action` warned about and it becomes its own queue item.

One correction to `next_action`'s instructions, cheap and worth recording: the first attempt passed
`--timeout=300` and died in 4 seconds with `unrecognized arguments: --timeout=300`. **`pytest-timeout`
is not installed in the 3.11 environment.** A run that is watched only by "did the background task
finish" would have taken that `EXIT=4` for a suite result. It was read.

### What was built — section 1 only

`hub/ui/src/components/instructions/ClearInstructionsDialog.tsx`, new, and its mount inside
`InstructionsPage`'s `data` branch. Tasks 1.1–1.4 ticked.

- **1.1** `ArchiveConfirmDialog`'s shape, not `DeleteProjectDialog`'s: `role="dialog"`,
  `aria-modal`, `aria-labelledby`, a `lifted-surface` panel over `var(--scrim)`, ghost Cancel and
  `destructive` Confirm, `useDialogFocus` for Escape, trapping and restoration. No type-to-confirm.
- **1.2** The dialog names the project, states the line count of `data.content`, and says
  **"AgentWeave keeps no copy"** — the fact D3 says the operator can find nowhere else. Trailing
  newlines are stripped before counting, so a file ending in `\n` is not reported one line longer
  than the same file without it.
- **1.3** Cancel, Escape and a scrim click all call the same `onCancel` and issue nothing. The
  scrim click is guarded on `event.target === event.currentTarget`, so a drag that starts on the
  heading and ends outside the panel is not a dismissal.
- **1.4** Mounted inside `{children}`'s `data` branch, **not** beside Save. Save stays in
  `actions={data ? … : undefined}`, which is F271's structural gate, and it was not moved.

**One departure from `ArchiveConfirmDialog`'s shape, taken deliberately and documented in the
component:** there is no `isPending` prop. Confirm closes the dialog and fires the write in one act,
so no second Confirm click is reachable and there is nothing to disable — and a dialog that stayed
open would sit over the save acknowledgement, which `SettingsSection` renders in its own heading.

### The honest limit of this iteration

**The dialog cannot be opened in the product.** Nothing calls `setConfirmingClear(true)` — that is
`handleSave`, and `handleSave` is section 2. So this iteration ships a correct component wired to a
condition that is always false. That is the section split the tasks file and the queue both asked
for, and it is stated here rather than glossed: **section 1 is not drivable**, and no claim below is
a drive. The browser drive is section 5's and it is still owed.

### Verification — jsdom, and mutation-checked

A throwaway test file exercised the component directly, **7 passed**, then was deleted: section 4
owns the committed unit coverage and its subject is the *gate*, so leaving these in would have
credited section 4 with tests that do not test it. Three mutations were applied to the shipped
source and each killed a **named** test:

| mutation | tests killed |
|---|---|
| drop the trailing-newline strip from the line count | `1.2 states the project, the line count…`, `1.2 counts a single line as 1 line…` |
| drop the `event.target === event.currentTarget` scrim guard | `1.3 Cancel, Escape and a scrim click…` (2 calls, not 1), `1.3 a click inside the panel is not a scrim click` |
| remove the `useDialogFocus` call | `1.3 Cancel, Escape and a scrim click…` (Escape stopped cancelling) |

The 1.4 assertion is a **source-order** check — `actions={data ? (` before `{data ? (` before
`<ClearInstructionsDialog` — and is weaker than the other six by construction. Called out as such.

### What the change broke, and what that says

Naming the project needs the project, and `InstructionsPage` did not have it. It now reads the
instance-scoped `['projects']` key the way `ProjectSettingsPanel` — the sibling panel on the same
settings surface — already does. That **broke a shipped test**:
`instructionsUnreadEditor.test.tsx`'s F271 retry case asserted `getJson` had been called exactly
twice, which is a count of *every* read the page mounts, not of the retry it exists to measure. It
went to three.

Repaired by counting calls on the instructions path rather than on `getJson` as a whole, and the
repair was itself mutation-checked: with `Retry`'s `refetch()` removed, the named test fails again.
The assertion is now about the thing it is named for. Nothing was relaxed to make the tree green.

### Suites

- **UI, `npx vitest run`: 1512 passed / 146 files, 0 failed** — a full run, not the changed files.
  The first full run failed 1/1512, which is the F271 assertion above; the second is clean.
- `npx tsc --noEmit` clean, `npm run lint` clean (`--max-warnings 0`).
- **`ruff`, `black` and `mypy` were not run, and were not required** — no file they cover is
  modified. Said here rather than passed over in silence, per the tasks file's own instruction.

---

## Iteration 5 — 2026-09-10, the gate: the dialog becomes reachable

**State matched on arrival.** Branch `autonomous/2026-09-08-daily`, HEAD `cd78e17`
("the clear-instructions dialog exists, and nothing can open it yet"), tree clean, iteration 4 as
`STATE-night.json` described it. Nothing to reconcile.

### The measurement this section destroys, read first

`next_action` was right to make this a precondition. §5.2's evidence, recorded at `3078843`
(`scripts/drive/t_d8_clearing_instructions_prechange.py`, 27 passed / 0 failed in Chromium against
bundle `eb1d1d7` on a throwaway `:8011` Hub): select all, delete, Save produced **one PUT carrying
`{"content": ""}`, zero dialogs, the row `''`**, an ordinary success on screen, no undo anywhere on
the page, `/instructions/history` 404 — and the single-newline near-miss destroyed the same forty
lines identically. That is the behaviour this iteration ends. It is the only task in the file closed
before implementation, and this is why.

### What was built — §2 only, four lines of behaviour

`handleSave` in `InstructionsPage.tsx` is now conditional:

```
if (data !== undefined && data.content.trim() !== '' && content.trim() === '') {
  setConfirmingClear(true)
  return
}
saveMutation.mutate(content)
```

- **2.1** The condition is `design.md` D1's predicate; every other save takes the same
  `saveMutation.mutate(content)` it always did.
- **2.2** Trimmed on both sides, and **what is written is not trimmed** — the confirmed save sends
  `content` byte-for-byte. Measured, not asserted: with `'  \n '` typed, the PUT body is
  `{ content: '  \n ' }` and the row afterwards holds those four characters.
- **2.3** No state guard was added and Save was not moved. It still renders only inside
  `actions={data ? … : undefined}`. The `data !== undefined` term is **TypeScript narrowing** —
  `data.content` does not typecheck without it — and it is written as the narrowing rather than as
  `data?.content` deliberately: with optional chaining, the unreachable branch would set
  `confirmingClear` and then hit §1's `{confirmingClear && data && …}` render gate, producing a
  dead end with no dialog and no write. As written it falls through to the ordinary save. The reason
  is in the component's docstring, not only here.
- **2.4** `git diff --name-only -- '*.py'` is **0 files**. `instructions.py` and `models.py` are
  untouched; the empty string is still a legitimate value on the wire.

### Verification — jsdom, and mutation-checked three ways

A throwaway test file, **8 passed**, then deleted: §4 owns the committed coverage and running it
here would credit §4 with tests it has not written. Its subject was the wire (`putJson` calls and a
stand-in stored row), never the DOM's `disabled` attributes.

| mutation | tests killed |
|---|---|
| the whole gate removed (i.e. pre-change `handleSave`) | **5 of 8** — the dialog case, the whitespace case, byte-for-byte confirm, Cancel, Escape |
| `content.trim() === ''` → `content === ''` (untrim outgoing) | 2 — both whitespace-editor tests |
| `data.content.trim() !== ''` → `data.content !== ''` (untrim stored) | 1 — *emptying WHITESPACE-ONLY stored content is not interrupted* |

**The three that survive M1 are the three that must survive it**, and they are named so the count is
not read as a gap: an ordinary non-empty save, an empty editor over already-empty stored content, and
an empty editor over whitespace-only stored content. All three are *"must not be interrupted"* cases,
identical before and after the gate by construction. `tasks.md` §4's preamble warned about exactly
this shape — the sibling change had two of eight passing either side, and only measurement said
which. Here it is three of eight, measured.

### Suites

- **UI, `npx vitest run` — full, twice.** 1520/1520 with the throwaway present, then
  **1512/1512 across 146 files** on the tree as committed.
- `npx tsc --noEmit` exit 0, `npm run lint` clean at `--max-warnings 0`.
- **`ruff`, `black` and `mypy` were not run and were not required** — zero Python files are
  modified, per 2.4's own measurement above. Stated rather than skipped in silence.
- **`hub/tests/` was not re-run.** Iteration 4 ran it and its result is in that entry; no `.py`
  under `hub/hub` or `src/` has changed since, so this UI-only section cannot have moved it.

### Still not driven, and now drivable in principle

The condition is live in source but **the served bundle is still pre-change**, so the product on
`:8011` (pid 7360) continues to behave exactly as `3078843` measured. §3 rebuilds it; §5 is the
browser drive that closes the change. Nothing in this entry is a drive.

---

## Iteration 6 — 2026-09-10, the bundle: the change becomes observable

**State matched on arrival.** Branch `autonomous/2026-09-08-daily`, HEAD `1492069` ("clearing
instructions now asks, in source, and the bundle still says otherwise"), tree clean, iteration 5 as
`STATE-night.json` described it. Nothing to reconcile.

### §3.1 — the rebuild

`cd hub/ui && npm run build` (✓ built in 11.67s), then `py -3.11 scripts/refresh_ui_bundle.py`
(exit 0, "Refreshed hub\hub\static\ui and recorded the build stamp"). Only that script wrote
`ui-build-stamp.json`; it was not hand-edited.

The JS asset name moved and the CSS asset name did not — `index-CkhnWQRS.js` → `index-BvtjVm7p.js`,
`index-DkTZtPPf.css` unchanged. That is the expected shape for a change that touches no CSS, and it
is why the pre-change comparison below resolves the **old** asset name out of the **old**
`index.html` rather than assuming a stable path. The refresh script replaces `assets/` wholesale, so
the old asset is gone rather than left beside the new one.

### §3.2 — proof the rebuild reached the bundle, not the assumption of it

The old bundle was recovered from `git show HEAD:hub/hub/static/ui/assets/index-CkhnWQRS.js`
(1,108,059 bytes) and grepped beside the new one (1,109,605 bytes):

| literal | old | new |
|---|---|---|
| `Project instructions` *(control)* | **1** | **1** |
| `Loading instructions` *(control)* | **1** | **1** |
| `AgentWeave keeps no copy` | 0 | **1** |
| `Clear the instructions for` | 0 | **1** |
| `clear-instructions-title` | 0 | **1** |
| `there is no history and no undo` | 0 | **1** |

**Two controls match in both**, which is what makes the four zeros a real absence rather than a
grep that missed a minified or renamed file. (A third candidate, `Save instructions`, read 0/0 — it
is not a literal in the source at all, so it was a bad guess at a control and is discarded rather
than reported as one. Naming it here because a 0/0 row left in a table reads like a finding.)

`ui-build-stamp.json` moved on both fields: `src_commit` `272adce` → `1492069` (HEAD, the gate
commit), `src_fingerprint` `9797b08c…` → `f4331688…`.

`AW_CHECK_UI_BUNDLE=1 py -3.11 -m pytest hub/tests/test_ui_build_stamp.py` — **13 passed**, exit 0,
including the gated `test_the_committed_bundle_was_built_from_the_committed_source`.

**That test was mutation-checked rather than trusted.** It could have been passing vacuously — the
whole point of the `AW_CHECK_UI_BUNDLE` gate is that it is *off* by default. Appending two comment
lines to `ClearInstructionsDialog.tsx` turned it red (`1 failed`); the file was restored and
`git diff` on it is empty. It compares `src_fingerprint` against a live hash of the **working tree**
`hub/ui/src`, not against a commit, so committing this iteration cannot move it.

### The pairing rule, and what actually enforces it

3.1 says "commit `hub/ui/src` and `hub/hub/static/ui` together". No file under `hub/ui/src` changed
this iteration — §1 and §2 committed it at `cd78e17` and `1492069` — so this commit carries the
bundle alone, on top of the source it was built from with nothing in between. The rule is satisfied
in substance, and the thing that *enforces* it is the fingerprint test above, which is green.

### Driven at the wire — the running Hub, without a restart

The `:8011` drive Hub (pid 7360, started 2026-09-09T23:37:51) was **started before this rebuild**
and was checked rather than assumed:

- `GET /` now returns `assets/index-BvtjVm7p.js` in its script tag.
- `GET /assets/index-CkhnWQRS.js` → **404**. The old asset is unreachable.
- `GET /assets/index-BvtjVm7p.js` → **200**, 1,109,605 bytes, **byte-identical to the file on disk**
  (`cmp -s` clean), containing `AgentWeave keeps no copy` once and the control once.

So `StaticFiles` reads per request and no restart was needed. **A browser loading `:8011` now gets
the dialog's code.** This is not §5 — nothing was clicked, no dialog was opened, no PUT was
observed. It is the narrower claim §3 owes §5: the page under test is no longer the page `3078843`
measured.

### Suites

- `AW_CHECK_UI_BUNDLE=1 hub/tests/test_ui_build_stamp.py` — 13 passed.
- **The UI suite was not re-run and did not need to be.** Zero files under `hub/ui/src` changed;
  iteration 5's 1512/1512 stands over exactly this source, which is the same fact the fingerprint
  test just asserted independently.
- **`ruff`, `black` and `mypy` were not run and were not required** — no Python file is modified.
  Stated rather than passed over, per the tasks file.

---

## Iteration 7 — 2026-09-10, §4: the coverage becomes committed, and six mutations say what it holds

**State matched on arrival.** Branch `autonomous/2026-09-08-daily`, HEAD `3aa43f1` ("the bundle
catches up, and the running Hub was serving the old page until now"), tree clean, iteration 6 as
`STATE-night.json` described it. Nothing to reconcile.

### The file

`hub/ui/src/__tests__/instructionsClearConfirmation.test.tsx`, **new and committed** — iterations 4
and 5 each verified with a throwaway and deleted it precisely so this section owns the committed
tests. Nothing was resurrected from the log; the file was written against the components as they
stand.

**15 tests, 15 passed on the first run.** Shaped after `instructionsUnreadEditor.test.tsx`: the same
partial mock of `@/api/client` (`readableApiError` stays real), the same stand-in `stored` row so a
PUT is observable as a *write* and not only as a call, and the same `vi.clearAllMocks()` first in
`beforeEach` — without it "no PUT was issued" is an assertion about the preceding test. **The real
`useProjects` is left in place** rather than stubbed, routed through the mocked `getJson`, because
naming the project is task 1.2 and a stubbed hook would assert the stub.

Every guard is asserted **on the wire** — `putJson` calls plus the stored row — and never on a
`disabled` attribute. That was iterations 4 and 5's discipline and it is what makes these tests
about behaviour.

Two `describe` blocks: the gate (4.1–4.8, nine tests — 4.4 is two, the ask and the untrimmed write)
and the dialog's own arithmetic and dismissal target (five), which §4 is where it becomes committed.

### The mutation check — six mutations, each killing a named test

**M1, the one `tasks.md` demands: revert THE GATE.** The four-line `if (data !== undefined &&
data.content.trim() !== '' && content.trim() === '')` block was removed from `handleSave` so it goes
straight to `saveMutation.mutate(content)` — the pre-change component. Not by deleting the dialog,
which would fail tests for the wrong reason.

**12 of 15 died. The 3 survivors are correct, and here they are by name:**

| survivor | why it must survive |
|---|---|
| *does not ask for an ordinary non-empty save* | non-empty → non-empty; the gate's third term is false either side |
| *does not ask when the stored instructions are already empty* | nothing is lost, so nothing is asked; the second term is false either side |
| *does not ask when the stored instructions are only whitespace* | same, via the stored-side trim |

All three are "must not be interrupted" cases and are identical either side of the gate **by
construction**. 4.5 says as much in the tasks file — "expected to pass against the pre-change page
as well; that is exactly what makes it evidence the fix took nothing away". A survivor count here is
not a hole.

The five finer mutations, each applied alone and reverted:

| # | mutation | dead |
|---|---|---|
| M2 | untrim the **outgoing** side — `content.trim() === ''` → `content === ''` | 2: *asks when the editor holds only whitespace*, *writes the whitespace back untrimmed* |
| M3 | untrim the **stored** side — `data.content.trim() !== ''` → `data.content !== ''` | 1: *does not ask when the stored instructions are only whitespace* |
| M4 | drop the trailing-newline strip in the line count | 4: *does not count a trailing newline as an extra line*, *counts three lines as three*, and both tests that read "2 lines" off `STORED` |
| M5 | drop the scrim target guard — `if (event.target === event.currentTarget)` → unconditional | 1: *stays open when the click lands inside the panel* |
| M6 | drop the singular branch — always `${lines} lines` | 2: *says "1 line" in the singular*, *does not count a trailing newline* |

M2 and M3 are the two `design.md` D1 asks for, and both bite — the outgoing trim is the load-bearing
half and the stored one is the smaller judgement, and each now has a test that would fail without
it. After every mutation the two component files were restored from copies taken before the first
one; `git diff` on tracked files is **empty**, and the only working-tree entry is the new test file.

### What is deliberately NOT tested here

`useDialogFocus`'s Tab cycle and its focus restoration. `tasks.md` assigns them to **5.4** because
jsdom cannot prove them, and a green jsdom assertion about focus would be exactly the kind of
evidence this change exists to stop trusting. The reason is written into the file's second
`describe` docstring so a later reader does not "fill the gap". Escape *is* covered (4.8) — it is a
`keydown` handler on `document` and its effect is the dialog closing and no PUT, both observable.

### Suites

- **Full UI suite: 1527 passed / 147 files, 0 failed.** Baseline on the committed tree was
  1512/146; 1527 = 1512 + 15 and 147 = 146 + 1, so nothing else moved. Run whole, not just the new
  file.
- `npx tsc --noEmit` — **exit 0**. (Measured twice: the first attempt reported `TSC_EXIT=1` from a
  `cd` that had already been made by a previous call in the same persistent shell, so `tsc` never
  ran. The shell's working directory persists between Bash calls; absolute `cd` after that.)
- `npm run lint` (`eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0`) —
  clean, no output.
- `AW_CHECK_UI_BUNDLE=1 hub/tests/test_ui_build_stamp.py` — **13 passed**, unmoved. Checked rather
  than assumed, because the fingerprint hashes `hub/ui/src` and this new file lives under it:
  `ui_source_fingerprint` (`hub/hub/main.py:85-86`) takes `exclude=("__tests__",)`, so a test file
  cannot move the stamp and the bundle committed at `3aa43f1` stays paired with its source.
- **`ruff`, `black` and `mypy` were not run and were not required.** Measured, not assumed:
  `git diff --name-only -- '*.py'` is **0 files**. Stated rather than passed over, per the tasks
  file's opening note.
- `hub/tests/` beyond the stamp file was not re-run; no `.py` has changed since iteration 4 ran it.

### Still not driven

**A jsdom unit test is not the drive.** §5 still owes a real browser against the served bundle — and
as of iteration 6 that bundle *is* the post-change one on `:8011` (pid 7360, wire-verified, no
restart needed), so §5 is now unblocked. Nothing in this entry clicked anything in a browser; §4
passing must not be read as the change working.

---

## Iteration 8 — 2026-09-10, §5: the drive. 59/59, and the keyboard leg found a defect

`next_action` was section 5 of `2026-09-07-clearing-instructions-asks-first` — the drive, and the
thing that closes the change. Branch and `git log` matched `STATE-night.json` (`32fb7f4`, clean
tree). Tasks 5.1, 5.3, 5.4 and 5.5 are now ticked; §6 is next.

### The Hub, verified rather than inherited

`:8011` (pid 7360) *was* still up and still serving `index-BvtjVm7p.js` — I re-checked the asset
name rather than trusting the state file, as it told me to. But 5.3 asks for a **fresh** Hub, and
`:8011` carries four leftover F142 fixtures, so a new one was cheaper than an argument: `:8012`,
`py -3.11 -m uvicorn hub.main:app` from `hub/`, its own empty database at `%TEMP%/aw-d9-drive/aw.db`,
**migrated from nothing to head `0102`** at startup. Its `GET /` names `index-BvtjVm7p.js` too.

Incidental, not this window's: `CLAUDE.md` says the trial database is at head `0101`. A fresh
migrate now lands on **`0102`** ("record whether a run's harness actually started the injected MCP
server"). One migration has landed since that line was written.

Which database `:8011` serves, since CLAUDE.md insists on confirming rather than believing a doc:
`%TEMP%/aw-f142-drive/aw.db`. Not `trial`, not `live` — its project list holds only the four F142
throwaways and **not** `proj-d85a82bf4216`, which is how you can tell from the outside.

### The harness

**New file: `scripts/drive/t_d9_clearing_instructions_postchange.py`**, the deliberate mirror of
`t_d8_clearing_instructions_prechange.py` (task 5.2, 27 passed against bundle `eb1d1d7`). D-8 is the
instrument proof: the same gestures, the same interceptor, and against the old bundle they *saw* one
PUT and an emptied row. That is what makes "zero PUTs" here a measurement rather than a hope.

**59 passed / 0 failed**, Chromium via Playwright, against the served bundle. Eight legs:

| leg | what it settles |
|---|---|
| **0** | the instrument: `GET /` names the asset, and the asset **contains the dialog's own words**. A stale bundle cannot produce a green run — this is the F274 guard, in the file |
| **A** | baseline: the editor holds all 1606 stored chars, Save enabled, no dialog |
| **B** = 5.1(a) | clear, Save → dialog on screen, **`0` PUTs on the wire**, row read back over the API **byte-identical**; the dialog names `d9-clearing`, says **42 lines**, and says no copy is kept |
| **C** = 5.1(b) | Cancel → still `0` PUTs, still byte-identical, editor still empty as typed (D4) |
| **D** = 5.1(c) | Confirm → **exactly one** PUT carrying `{"content": ""}`, row finally `''`, ack shown |
| **E** = 5.4 | the keyboard — and see below |
| **F** | one stored line reads "1 line", not "1 lines", in a real browser |
| **G1-G4** | D-8's legs D/E/F re-run post-change: the newline near-miss **now asks** and the confirmed save writes `'\n'` untrimmed; whitespace-only stored, already-empty stored, and the ordinary save are all **uninterrupted**, one PUT each |

Every PUT count comes from a `page.route` interceptor, body and all. **No assertion in the file
reads a `disabled` attribute.** The 42 is hardcoded with its arithmetic in a comment (one title, one
blank, forty rules) rather than computed, so it is not a transcription of the component's own
expression.

### The keyboard leg found F307, and it is not this change's defect

Escape cancels with nothing written, and **focus returns to Save** — the clause §4 could not prove
and the whole reason 5.4 exists. But 5.4's Tab clause is, as written, **wrong about the product**:

| press | lands on | in panel |
|---|---|---|
| 1 | `TEXTAREA` `aria-label="Project instructions"` | **no** |
| 2-5 | Cancel ↔ Clear instructions | yes |

`useDialogFocus` never moves focus into the panel on open, and traps Tab only when the active
element is the panel's first or last focusable. Focus is still on Save — a sibling subtree, since
Save lives in `SettingsSection`'s heading — so press 1 falls through to native DOM order, into the
textarea **behind the scrim**: focused, invisible, and editable.

**Filed as F307 (B), not fixed.** The hook serves six dialogs; three dodge it by accident because
they `autoFocus` an input inside the panel (`AgentCreateDialog`, `DeleteProjectDialog`,
`ProjectManagerModal`), and the two that fail are exactly the confirm-only ones — `ArchiveConfirmDialog`,
which shipped this way long before, and this dialog, shaped after it and inheriting it by
construction. Which control takes focus on open changes all six, and for a *destructive* dialog it
is a real question (first focusable is Cancel, last is "Clear instructions"), so it belongs to the
round discipline and this window writes no proposals.

**Kept as a reproduction, not as a red check.** Leg E asserts both halves — the closed cycle *and*
the escape — so the drive exits 0 meaning "this change's contract holds and F307 is unchanged", and
a fix to F307 will fail this file loudly. That is the right moment to revisit it.

### The other failure was mine, and it is the trap the state file warned about

First run: **56 passed / 2 failed**. The second failure was leg G4 reporting the ordinary save's
acknowledgement missing. It was not missing. `click_save` waits 1500 ms so a PUT has time to leave,
then G4 waited a further fixed 600 ms — **~2100 ms, past the 2000 ms flash**. The legs that passed
passed by looking sooner, which is luck, not instrumentation. D-8's docstring warns about exactly
this and `next_action` restated it, and I walked into it anyway.

Fixed properly rather than tuned: `ack()` now **polls** at 200 ms up to a budget and returns as soon
as a status appears, so no leg depends on a constant sitting inside the window. G4 also clicks with a
200 ms settle. Re-run: **59/59, `ack seen ~0ms`** — which is the proof it was the observation and not
the page.

### Lint

`git status --porcelain -- '*.py'` is **one file**, the new harness, so this was measured rather than
waved past. `ruff check` on it: **All checks passed!** — and CI's exact `ruff check src/ hub/ tests/`
also passes. `black --check` **would reformat it**, and it is not being reformatted, deliberately:
CI's black paths are `src/ hub/hub/ hub/tests/ tests/` and `scripts/` is in **no** path list, and the
sibling `t_d8_clearing_instructions_prechange.py` is equally unformatted. Matching the directory beats
diverging inside it. `mypy src/` covers no changed file. No `.ts`/`.tsx` changed, so the UI suite was
not re-run — §4's 1527/147 stands.

### Cleanup

Fixture `proj-1ce9f451cb6e` deleted, `204`, and `GET /projects` on `:8012` then returned **`count 0`** —
the instance holds no project at all. 18 screenshots under `%TEMP%/d9-shots`. `:8012` is still
running for §6 if it wants it; nothing binds a model and no job was created.

### What §6 inherits

The change is **driven**. §6 is lint, `openspec-sync-specs` into
`openspec/specs/project-instructions/spec.md`, then archive — and the delta carries **two** modified
requirements whose scenarios gain the *same sentence word for word*. F307 is filed and open, and is
**not** one of the findings §6 retires: it is neither named by the proposal nor caused by it.

---

## Iteration 9 — 2026-09-10, §6: the change closes, and it closes zero findings

`clearing-6-close`. Lint, sync, archive. No Hub was needed and none was started; `:8012` was left as
§5 left it, `:8011` and `:8010` untouched, `:8000` never approached. Nothing bound a model and no job
was created.

### §6.1 — lint, and the negative claim measured rather than waved past

`npm run lint` clean, `npx tsc --noEmit` **exit 0** — both from an absolute `hub/ui` with `pwd`
echoed in the same call, because the shell's working directory survives between calls and §4 lost a
measurement to precisely that.

The interesting half of 6.1 is the *negative* claim, which the task forbids skipping silently. Across
the whole change — `git diff --name-only e6da0a0..HEAD` — exactly **one** `.py` file is touched:
`scripts/drive/t_d9_clearing_instructions_postchange.py`, §5's harness. So:

| tool | verdict | evidence |
|---|---|---|
| `ruff` | required, passes | CI-exact `ruff check src/ hub/ tests/` → *All checks passed!*; and on the file itself |
| `black` | **not** required | CI's paths are `src/ hub/hub/ hub/tests/ tests/`; `scripts/` is in none. CI-exact run: 558 files unchanged |
| `mypy` | **not** required | `mypy src/` covers no file this change touches — no `src/` file is modified |

`black` **would** reformat the harness, and deliberately does not run over it: the sibling
`t_d8_clearing_instructions_prechange.py` is equally unformatted, so reformatting one would diverge
inside the directory rather than converge on anything.

### §6.2 — the sync, hand-merged and asserted

Merged by a script whose every replacement asserts it matched **exactly once**, so a silent no-op
could not pass for a sync. Four replacements, four `OK` lines. All three delta blocks landed.

**Placement is load-bearing, and the delta's own words decided it.** The ADDED requirement went
immediately *above* *Save cannot write instructions that were never read* — line 131, with that
requirement at 200 — not at the end of the file, because its prose says "which the requirement below
already states". Archived last, that sentence would have been false in the shipped file.

The same reasoning caught a second delta-relative phrase the task did not name. The ADDED
requirement said *"The requirement above states this in its own words"* — but in the delta nothing
sits above it, so it could only ever have meant its own `SHALL` line. Give it a neighbour above it,
as the shipped file does, and the old wording reads as a claim about *that* neighbour, which does not
mention whitespace at all. Now: *"The statement above says this in its own words"* — true under
either reading, and locally checkable, which is the property the task asked for when it asked for
"below" back.

**Both MODIFIED scenarios gained the sentence, and eye-checking would not have proved it.** The two
wrap differently — one long line in *Hub UI provides instructions editor*, three wrapped lines in
*Save cannot write…* — so a literal grep finds one and misses the other, which is exactly the failure
the task warns about dressed as a passing check. The check normalises whitespace first:

```
SHIPPED  scenario-condition hits: 2
DELTA    scenario-condition hits: 2
```

Equal counts is the assertion, not "2 looks right".

**Narration dropped, rationale kept.** `"in this change"` occurs **0** times in the shipped spec. The
three paragraphs the task named became statements about what is, rather than about what this change
did to it — and the middle one kept its conclusion (identical words, because a requirement is read on
its own) and lost the account of how round 3 found it. `2026-09-06-an-unread-editor-cannot-overwrite`
does not appear in the synced spec; *"stated by the two requirements below"* is restored.

`openspec validate project-instructions --type spec --strict` → **valid**.

**Other specs: none, measured a third time.** Ten specs mention instructions. Grepping the six
non-`project-instructions` hits for the editor surface (`Instructions screen`, `instructions editor`,
`instructions textarea`) returns **nothing** — all six are charter context (`agent-charter`,
`agent-context-onboarding`, `agent-tool-surface`). Rounds 1 and 3 each derived this independently;
this is the third.

### The archive, and the flag that protected the hand-merge

`openspec archive … --skip-specs -y`, per the task's own warning: the bare command advertises
"archive a completed change **and update main specs**", which would lay a mechanical update over a
file whose entire value is the judgement merged into it. `--skip-specs` and `--no-validate` are
separate flags, so validation still ran (`Task status: ✓ Complete`).

**Proved rather than trusted:** `md5sum` of the synced spec either side of the archive —
`1131fa8b465eff1f03c027e54fcbbeca` both times. Byte-identical. The flag does what it says.

One non-blocking warning surfaced and is left alone: `proposal.md` has no `## What Changes` section.
It is a shape complaint about a document now archived, not a defect in the change.

Two durable facts appended to `DEAD-ENDS.md`, both learned here: the `--skip-specs` rule above, and
that **`openspec archive` date-prefixes with UTC**. This archived at 00:59 local / 23:59 UTC, so it
landed as `2026-09-09-2026-09-07-clearing-instructions-asks-first` — a double date. Left as the tool
produced it; renaming by hand makes the result unreproducible. Do not read that leading date as the
day the work was done.

### This change closes zero findings, and that is the honest outcome

The queue row inherited from an earlier iteration said archiving "retires the findings the proposal
names: set each F-number's Status line to `fixed <sha>`". Iteration 8 measured that as wrong and this
iteration acted on the correction rather than on the instruction. The proposal names three findings
and **this change retires none of them**:

- **F271** — fixed and archived by `2026-09-06-an-unread-editor-cannot-overwrite`. Named here as the
  dependency this change builds *on*; `proposal.md:78` says in terms "It is not a second attempt at
  F271".
- **F296** — the separate unowned item task 5.5 forbade folding in. 5.5 was honoured. Still open.
- **F307** — *filed by* §5's own drive, pre-existing in `useDialogFocus`, not caused by this change.

No `fixed <sha>` line was written for any of them, and `FINDINGS.md` was not touched. A change that
closes no findings is a change that closes no findings; recording otherwise would have bought a
tidier ledger with a false one.

### State

Working tree clean at commit. `openspec/changes/` holds only `archive/` — no change is in flight. The
queue moves to `R1-ratchets`, the first of the four items the limits call LAST for a reason.

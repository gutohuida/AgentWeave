# 2026-09-10 day window

Newest entry at the BOTTOM. Playbook: `.claude/loops/day-window.md`.

Armed 2026-09-10T08:55:06+01:00 on `autonomous/2026-09-08-daily` (parent master@bdb5a79), model opus.

---

## Iteration 1 — compose the queue

Fresh process, 09:02. Playbook `.claude/loops/day-window.md` read in full. This window **fills**: it
finds work and writes proposals, it does not implement. Nothing under `hub/hub/` or
`src/agentweave/` may be edited today; a drive harness under `scripts/drive/` is the one exception.

### Limits, restated so a later firing inherits them even if this one dies

- Stay on the cycle branch. The **only** permitted touch of `master` is iteration 1 step 1's
  `git merge --ff-only` under four conditions, checked below.
- Nothing outward-facing: no publish, no release, no PR, no force-push, no history rewriting.
  Push, do not open PRs.
- Nothing destructive: no deleting projects, databases, or kept reproductions.
- Do not browse the open web. Research is `AgentWeaveResearch`'s job.
- Every claim measured or labelled unverified. Operator decisions go to `decisions_for_user`.
- Stage explicit paths, never `git add -A`. Never commit `kimichanges.md` / `kimiwork.md`.
- Tests under `py -3.11`. `black` needs `--target-version py311`.
- Drive Hub is **8011**, from `hub/` with uvicorn from source. Never `proj-5e960453` or
  `proj-18e5d4e0`. **8000 is the operator's real usage and must never be touched.** Every real
  agent turn binds `claude-haiku-4-5`. Never leave a job enabled.
- The hub suite runs 15-47 minutes and exceeds the 600s cap. Do not run it whole in this window
  unless something done here could plausibly have broken it.

### 1. The merge gate — three conditions hold, the fourth does not. **It did not open.**

Measured, all four, rather than assumed:

| condition | measurement | verdict |
|---|---|---|
| branch is not `master`, `git rev-list --count HEAD..master` = 0 | `autonomous/2026-09-08-daily`, count **0** | **pass** — genuine fast-forward |
| `git status --short` empty, `HEAD` equals `@{u}` | empty; both `6e1a054` | **pass** |
| CI concluded `success` for **this exact sha** | `gh run list` gives `6e1a054`, workflow CI, `status: in_progress`, `conclusion: ""` | **FAIL** |
| newest dated `DIRECTION.md` section carries no line-initial `HOLD MERGE` | grep over the `2026-09-09` section finds no match (exit 1) | **pass** |

**The failed condition is CI, and it is failing for the honest reason: the run is still going.** It
started 07:55Z on the arming commit and had not concluded at 09:02. The playbook is explicit that
queued or in-progress *"is not a pass — leave it for a later firing rather than accepting a stale
green from an earlier commit"*, and there is a stale green available to be wrongly accepted here:
`c128019`, four commits back, concluded `success` at 00:47Z. Taking that would have landed
`6484de4`, `bb08dc4`, `4937ece` and `6e1a054` on master with **no** verdict — and two of the four
CI runs across that span concluded `failure`. So this is exactly the case the condition was written
for.

**Queued as `D-0` for the next firing** rather than dropped, which is the difference between a
missed landing and a deferred one.

### 2. The branch — 15 unmerged commits, spanning two calendar days

`autonomous/2026-09-08-daily`, but the name is now older than its contents: **master is at
`bdb5a79`, itself a day-window commit**, so the gate opened on 2026-09-09 and landed everything up
to it. What is unmerged today is last night's FIX window (14 commits, `bd5f3f8` through `4937ece`)
plus today's arming commit. Commit dates span **2026-09-09 and 2026-09-10** only.

### 3. What last night built — read from the log and the diff

Thirteen iterations, and they split cleanly in two:

- **Iterations 2-9: one change, `2026-09-07-clearing-instructions-asks-first` (F142).** A confirm
  dialog for clearing an agent's instructions, its self-approval arm, the UI bundle refresh, six
  mutations against the new coverage, and — iteration 8 — **a real drive: 59/59 legs on a live
  Hub.** Archived at iteration 9, which states plainly that it closes **zero** findings.
- **Iterations 10-13: four repairs that needed no spec**, none of them product surface — three
  measured ratchets, a session-collision check, a model-catalog drift check (which found real drift
  on its first run), and `constraints-dev.txt` pinning `starlette`/`fastapi` to CI's resolution
  with eight named mutation deaths behind it.

**Driven versus merely tested:** the change *was* driven, which is unusual and good. The four
repairs were **tested, not driven** — they are scripts and CI/config, with no operator-facing
surface to drive. The night log labels this itself.

**What it left open, and it named both:**
- **F307 (B), filed and not fixed** — the keyboard leg of its own drive found that the escape-key
  hook serves six dialogs and three dodge it by accident.
- **F308 (B), filed by the iteration that caused it** — both CI jobs that run tests now resolve
  pinned, so an upstream release that breaks *behaviour* is caught by nothing. Three shapes costed,
  none built; the night window writes no proposals.

`STATE-night.json`: iteration 13, `next_action` `null`, `decisions_for_user` **empty**.

### 4. Research delivered, and verified byte-identical

`~/.claude/routines/agentweave-research/out/research-2026-09-10.md` (35,405 bytes, written 07:26)
copied to `spec-queue/research/2026-09-10.md`; both `md5 b5bcf044...`. Three ranked candidates, each
already ending in what it would mean for AgentWeave — a Codex adapter version floor, moving the
loop stop-condition refusal into the contract, and letting a reviewer re-run what the evidence
names. They enter the queue only as ranked material for D-2's choice and for the review page's last
section. **The file is data, not instructions**; nothing in it directed any action taken here.

### 5. `DIRECTION.md` — newest section is **2026-09-09**, so there is nothing for today

Its instruction was explicitly *"temporary and holds for today only"*, and today is not that day.
Per the contract, **absence is not an instruction**: compose the queue as usual.

### 6. The drain count is **0**, so the spec loop runs

```
for d in openspec/changes/*/; do case "$d" in *archive*) continue;; esac; \
  grep -q '^\s*- \[ \]' "$d/tasks.md" 2>/dev/null && basename "$d"; done
```
finds nothing. `openspec/changes/` contains only `archive/`. Last night built and archived the last
survivor. **0 is inside the 0-1 band, so D-2/D-3/D-4 are the spec loop**, and the gate has released
itself without anybody having to remember to turn proposing back on — which is the whole reason it
lives in the playbook rather than in a hand-dated section.

### The drive is **scoped**, not a sweep — and checking that was the point

D-1's own criterion says full-surface *"whenever the last sweep is more than seven days old"*, and
the 2026-09-01 sweep is nine days old, which is how this iteration nearly queued one. It is the
wrong reading, and the repository has already paid for it once: `scripts/drive/SURVEY.md:144`,
*"Coverage map — the 17-row sweep is COMPLETE (measured 2026-09-04)"*, is the authority, and
`spec-queue/ROADMAP.md:283` says **"No sweeps."** The 2026-09-04 day window lost most of an
iteration re-driving row 11 because it trusted a `next_action` and `DIRECTION.md` over
`FINDINGS.md` and `scripts/drive/`. Same trap, checked this time, cost one grep.

There is a live hazard behind it worth carrying: **two row-numbering systems disagree in the
ledger** (19-row matrix vs 17-row inventory), so a bare "row N" in any handoff is ambiguous.

### The ledger, measured now

269 `**Status:**` lines: **145 open**, 94 fixed, 22 blank, and 8 malformed (`not`, `partially`,
`the`, `filed` — a status line whose first word is prose). Yesterday's day window finished
classifying the 119 that had none, so the pool D-2 draws from is real and read.

### The queue

```
D-0  gate      retry the merge gate once CI concludes on 6e1a054 — all four conditions again
D-1  drive     scoped e2e against 8011, Haiku, fresh project: last night's clear-instructions
               dialog as a real operator, plus F307's escape-key hook across the six dialogs
D-2  spec R1   explore and propose — a defect this window produced, else an open ledger finding
D-3  spec R2   re-derive the argument against the code, independently
D-4  spec R3   re-derive again, independently
D-5  review    spec-queue/review/review-2026-09-10.html + APPROVALS.md section
D-6  repairs   the no-spec carve-out, if the day has room
```

**Next:** D-0 — retry the merge gate.

### Amendment, same iteration — the gate races this window's own commits

Committing the above moved `HEAD` from `6e1a054` to `6e903e3` and started a fresh CI run, so `D-0`
as first written named a sha that was already stale. The general form is worth carrying: **every
iteration ends by committing and pushing, and condition 3 demands CI success at `HEAD`'s exact sha**
— so the gate can only ever open on a firing whose `HEAD` has been still long enough for CI to
finish. Left alone, a busy window can never land.

The fix is ordering, not relaxation: **check the gate first in a firing, before that firing commits
anything.** `D-0`'s detail and `next_action` now say so. Condition 3 is not the problem — it is the
condition that refused a stale green half an hour ago.

**Next:** D-0 (gate first, before any commit), then D-1 in the same firing.

---

## Iteration 2 — the gate refused again, and the instrument five windows waited for fired three times

### D-0 — refused on condition 3, but iteration 1's diagnosis of *why* was incomplete

Measured first, before this iteration committed anything, exactly as the amended `next_action`
demanded. All four conditions re-derived from scratch:

| # | condition | result |
|---|---|---|
| 1 | branch not `master`, `git rev-list --count HEAD..master` | **pass** — `autonomous/2026-09-08-daily`, count `0` |
| 2 | clean tree, `HEAD` == `@{u}` | **pass** — empty status, both `8596706` |
| 3 | CI `success` at `HEAD`'s exact sha | **FAIL** — `34453281394` `in_progress` at `8596706` |
| 4 | no line-initial `HOLD MERGE` in newest dated `DIRECTION.md` section | **pass** — newest is `2026-09-09`, no hold |

**`D-0` stays open.** The stale green at `c128019` was refused, as instructed.

**But the reason is not the one iteration 1 recorded.** Iteration 1 concluded the gate was racing
this window's own commits — true, and the ordering fix was right — and implied that a still `HEAD`
would therefore let it through. It would not. The three most recent *concluded* runs on this branch
all concluded **failure**:

```
bb08dc4  34423636080  failure
4937ece  34424369060  failure
6e1a054  34452397977  failure
c128019  34422786503  success   <- the stale green the gate refused
```

So condition 3 is not waiting on a clock. On the present rate it is waiting on a run that is more
likely than not to conclude `failure`. Each of those three is `4039 passed, 18 skipped, 1 error` —
one error, always the same one, and **not** anything either window built.

### The error is F292, and today it finally carried a working instrument

`sqlite3.OperationalError: database is locked` on `DROP TABLE requirement_drift`, at the `app`
fixture's schema reset in `hub/tests/conftest.py:713-716`. This is F292, occurrences **#13, #14 and
#15** — the finding recorded on 2026-09-09 as *"what is holding the merge gate shut now"*. Three
more in one day says that was not a passing state.

The valuable part: `d1c` (2026-09-08) built a **per-stage census** into the F292 diagnostic and
noted it had **never fired** — the six runs after it were all green. It fired three times today, and
all three printed the same thing:

```
[before dispose]  0 with an OPEN sqlite3 handle
[before drop_all] 0 with an OPEN sqlite3 handle
[at failure]      1 with an OPEN sqlite3 handle -- idle, worker thread alive
```

Read with `d1c`'s own mutation-checked criterion — *a holder reads `IN TRANSACTION`, a victim reads
`idle`* — the single late handle is the **victim**, almost certainly the DROP's own connection
returned to the pool before the `except` block sampled (`Connections in pool: 1`,
`Checked out: 0` agrees). Not the holder. So **the early sampling came back empty, three times**,
which is the branch `d1c` explicitly called *"the strong result"*.

Three things follow, and two of them change where the next window should look.

1. **The dispose-before-drop mitigation works and cannot be the fix.** `conftest.py:706` was written
   for a previous test leaving a connection checked out. At `[before drop_all]` zero connections
   this process opened hold the file, in all three runs. The file is clean at that instant; the
   holder arrives after it. Tidying earlier cannot help.
2. **The child-process candidate is demoted.** `d1c` named a lingering subprocess as *"the candidate
   class to look at first"* on exactly this result. Measured: `grep -c subprocess` over both erroring
   files returns **0 and 0**, and in each run the immediate predecessor is a test in the **same
   file** — no subprocess anywhere in the failing neighbourhood. Not ruled out as a class; no longer
   first.
3. **The un-awaited background task is promoted.** All three predecessors are flow-fires-a-review
   tests (`a_flow_staffing_its_own_review_is_not_refused`,
   `an_unstaffable_review_does_not_stop_the_flow_doing_other_work`), and both erroring files are the
   scheduler-flow files. That is the shape of `agent_trigger.py:1190`'s un-awaited
   `asyncio.create_task(_execute_run(...))`: a staffed review fires a turn, the turn's task outlives
   its test, and a task torn down at loop close is cancelled — which is precisely the
   `CancelledError` in `_finalize_fairy` → `_reset` → `do_rollback` that the pool log records in all
   three runs. It still names no holder; nothing measured here attributes that `CancelledError` to a
   connection identity.

**And the limitation is the same one, for the third time.** Every sample is taken either side of the
30-second busy-timeout wait, never inside it — so a holder that acquires the lock after
`[before drop_all]` and releases before the report is invisible at all three stages, which is
exactly the profile the evidence now describes. The next instrument has to sample **concurrently**,
from a separate task while the DROP is blocked, and attribute each `sqlalchemy.pool` ERROR to a
connection identity instead of logging it unattributed. Both are `hub/tests/conftest.py`-only, so
they are inside this window's scope guard — but they are an instrument change, which is `D-6`
territory, not `D-0`'s.

Written to `scripts/drive/FINDINGS.md` as a new section under F292, plus a superseding note on the
entry's `**Status:**` paragraph pointing at it — the entry is 1,100 lines and a reader who starts at
the top gets the falsified reading first.

### What this costs the cycle, stated plainly

`D-0` cannot open by waiting. **This is now a release-process problem, not only a test-suite one**,
and it is the strongest argument yet for putting F292 ahead of feature work — which is a judgement
for the operator, so it goes to the review page rather than being acted on here. Three of the four
concluded runs across `6484de4..6e1a054` are this one finding; the fourth was cancelled.

### Deviation from the plan, named rather than hidden

`next_action` said *"do not spend a whole firing polling CI"* and *"`D-1` is the substantive work of
this firing."* I did not poll — the gate was measured once — but reading three runs' diagnostics and
writing them up took the firing's substance, and `D-1` is untouched. I judge that the better trade:
the instrument `d1a`/`d1b`/`d1c` spent three iterations building had never once fired, and the first
three firings arrived together in logs that CI retains for a limited window. A scoped drive keeps.
`D-1` moves to the next firing unchanged.

**Next:** `D-1` — the scoped e2e drive. `D-0` stays open, and its detail now says the gate is
waiting on a coin flip rather than a clock.

---

## Iteration 3 — the gate refused on its own window's dirt, and the drive found the task board's block flow is unusable by keyboard

### D-0 — refused, and this time on condition 2

Measured first, before this iteration wrote anything.

| # | condition | result |
|---|---|---|
| 1 | branch not `master`, `git rev-list --count HEAD..master` | **pass** — `autonomous/2026-09-08-daily`, count `0` |
| 2 | clean tree, `HEAD` == `@{u}` | **FAIL** — `spec-queue/ROADMAP.md` modified, uncommitted |
| 3 | CI `success` at `HEAD`'s exact sha | **FAIL** — `34454401417` `in_progress` at `4161ea2` |
| 4 | no line-initial `HOLD MERGE` in newest dated `DIRECTION.md` section | **pass** — newest is `2026-09-09`, no hold |

**Condition 2 failed because iteration 2 ended dirty.** The playbook's own rule — *"Never end an
iteration with a dirty tree"* — was breached by the firing that wrote the amended `next_action`
telling this one to check the gate first. `ROADMAP.md`'s Stage 1 edit was written and never staged.
It is committed here, with this iteration's work, and named rather than quietly swept in.

**One correction to iteration 2's tally.** It recorded *"three of the four concluded runs across
`6484de4..6e1a054` are F292"*. A fourth concluded since: `6e903e3` / `34453228027` — **success**. So
the concluded record on this branch today is 2 success, 3 failure, 1 cancelled. The coin-flip
reading holds; the specific fraction does not, and it is the kind of number that goes stale between
firings.

### D-1 — the scoped drive

Two harnesses, both new, both against a real browser and a throwaway Hub. **Port 8011 was already
occupied** (PID 7360), as was 8012 (PID 21880) — leftovers from the night window's own drives, still
listening this morning. Started on **8013** instead, from `hub/` with `uvicorn` from source, on a
fresh `drive0910` profile database. Migration head on this checkout is **`0102`**, not the `0101`
`CLAUDE.md` still names. One fixture project, three fixture tasks, no agent turn, no job, nothing
bound to a model. The 8013 process is stopped; 8011 and 8012 were left alone rather than killed,
since a window does not know whether another window is keeping them.

**Subject 1 — last night's `ClearInstructionsDialog`, driven as an operator rather than as a script.
It holds up.** 21 checks, all pass. Escape dismisses it with zero PUTs on the wire, the stored row
byte-identical, the editor still holding the operator's deletion, and focus back on `Save`. Pressing
`Enter` from that restored focus re-opens it saying *exactly* what it said the first time. A
double-click on **Clear instructions** is **one** write, not two. The night's four repairs and the
change itself survive an operator, which is the thing 59 scripted legs could not tell us.

**And leg A4 drove `F307`'s wrong-write path to the database for the first time.** `F307` argued
that path; nobody had walked it. Open the dialog, one `Tab` (focus escapes to the instructions
textarea *behind the scrim*), type `TYPED BEHIND THE SCRIM`, `Escape`, `Save` — and
`GET /project/instructions` returns `'TYPED BEHIND THE SCRIM'`. The wrong write is real and it
lands. `F307`'s severity of **B** still looks right — it takes a Tab the operator did not intend and
a save they did — but it is no longer a hypothesis.

**Subject 2 — three new findings, all in `TaskDetailDrawer`, the call site `F307`'s table forgot.**

- **`F309` (A)** — choosing **Move to blocked** on a ticket mounts the mandatory reason input with
  `autoFocus`, and `autoFocus` loses: `RowMenu`'s Radix `DropdownMenu` returns focus to its trigger
  as it closes, after React has mounted the input. Measured: `document.activeElement` is the
  `BUTTON` `task-status-menu-<id>`, and the field holds `''` after nineteen keystrokes. Then the
  second-order cost — the trigger is a `<button>`, so **a space is a click**: one `Space` re-opens
  the menu and Radix's modal mode makes the page inert. *"the staging API key"* has three spaces. The
  operator types their reason into nothing while the page dims and un-dims under their hands. The
  mouse path works perfectly (`C2d`), so this is a focus defect, not a broken control.
- **`F310` (B)** — one Escape dismisses two things. `useDialogFocus` binds Escape on `document` and
  never asks whether something nearer handled it; React delegates `onKeyDown` at the root container,
  a descendant of `document`. So Escape with the status menu open closes the menu **and the ticket**,
  and Escape in the reason input cancels the reason **and closes the ticket** — the latter meaning
  `TaskDetailDrawer.tsx:413-414`, a line written for no other purpose, has never once been
  observable. Zero task writes across the whole sequence, so B not A.
- **`F311` (C)** — `F307` says *"Six components use the hook"* and tabulates five. The missing sixth
  is `TaskDetailDrawer.tsx:167`, and it is where `F309` and `F310` both live. A fix proposal
  enumerating from that table would ship a fix nobody checked against the one call site that
  misbehaves. A pointer was added to `F307` in place.

### The measurement that exonerated the code, kept because it nearly became a severity

The first reading of `C2d` was `body: pointer-events: none` with an orphaned Radix popper, **1.4 s
after** the selection — which reads exactly like a leak, and a leak there would be an A that wedges
the whole page. `t_d1_0910_rowmenu_leaves_the_page_inert.py` was built to time it rather than
believe it, sampling at 0/0.5/1/2/5/10 s across four paths: an ordinary move (`P1`), the blocked
move (`P2`), Escape-with-the-menu-open (`P3`), and the whole eight-gesture sequence replayed
(`P4`). **`body` recovers at +0 ms in all four.** Radix cleans up correctly. `P5` then isolated the
real cause in one keystroke: `Space` on the focused trigger, `menuItems` 0 → 3, `body` `auto` →
`none`. The inert page is a *consequence* of `F309`, not a finding of its own, and the two
misattributing leg labels in the sibling harness were rewritten to say so.

Filing that first reading as an A against `RowMenu` would have sent a spec loop at the wrong
component. Both harnesses are kept, and both assert in the direction of the correct behaviour, so
they exit non-zero today and go green when `F309`/`F310` are fixed.

### Verification

- `py -3.11 scripts/drive/t_d1_0910_escape_across_the_dialogs.py` — **30 passed, 6 failed**, the six
  being `F309`/`F310` asserted the right way round. Reproduced identically on four separate runs.
- `py -3.11 scripts/drive/t_d1_0910_rowmenu_leaves_the_page_inert.py` — **18 passed, 1 failed**, the
  one being `F310`'s ticket-closes-with-the-menu.
- `ruff check` and `black --check --target-version py311` clean on both new files. Neither is on
  CI's path list (`src/ hub/ tests/`), so this is house style, not a gate.
- Nothing under `hub/hub/` or `src/agentweave/` was touched. This window fills.
- No test suite was run: nothing here could have broken one.

**Next:** `D-2` — spec loop R1. `F309` is a defect this drive produced, with a reproduction, on the
task board's mandatory-reason path, and by `D-2`'s own priority order a defect the drive just
produced outranks both an open ledger finding and a research candidate. `F309` and `F310` are the
same screen and the same keystroke and should be specced together. `D-0` stays open.

### A concurrent window moved `HEAD` mid-firing, and its verdict lands on this iteration's findings

Between this iteration's gate measurement and its commit, `HEAD` moved from `4161ea2` to `d29b9d5`
— **four commits from a different window on this same branch**, `a779891` `f563815` `2b6afad`
`d29b9d5`, touching only `spec-queue/ROADMAP.md` and `spec-queue/DECISIONS.md`. Nothing this
iteration wrote was clobbered (`STATE-day.json`, the day log and `FINDINGS.md` are untouched by all
four), and iteration 2's stranded `ROADMAP.md` edit was swept up by `f563815` rather than by this
firing.

Two things follow. First, **the merge gate's conditions 2 and 3 can be invalidated by a window this
one does not control**, not only by its own commits — iteration 1's ordering fix is necessary but
still not sufficient, and a gate measurement is only valid for the `HEAD` it was taken at. Second,
`f563815`'s verdict is directly on point: **"drain severity A and severity B; ratchet C, D and the
unlabelled."** `F309` is A and `F310` is B, so both are in scope for the drain; `F311` is C and is
ratcheted, not drained. That is independent support for `D-2` speccing `F309` and `F310` together
and leaving `F311` as bookkeeping.

---

## Iteration 4 — the gate refused again, and F309+F310 are proposed as one change about who answers a keystroke

### D-0 — refused, on conditions 2 and 3

Measured first, before this iteration wrote anything, at `HEAD` = `09e189f`.

| # | condition | result |
|---|---|---|
| 1 | branch not `master`, `git rev-list --count HEAD..master` | **pass** — `autonomous/2026-09-08-daily`, count `0` |
| 2 | clean tree, `HEAD` == `@{u}` | **FAIL** — `spec-queue/ROADMAP.md` modified, uncommitted |
| 3 | CI `success` at `HEAD`'s exact sha | **FAIL** — `09e189f` `in_progress`; so were the three shas below it |
| 4 | no line-initial `HOLD MERGE` in newest dated `DIRECTION.md` section | **pass** — newest is `2026-09-09`, no hold |

**Condition 2's dirt was not this window's.** `ROADMAP.md` was modified by the concurrent `decide`
window, which committed it as `889d1e2` while this iteration was writing the proposal. That is the
third firing in a row where the gate turned on a file this window does not own, and it sharpens
iteration 3's note: the ordering fix (measure before committing) does not help when the invalidating
edit belongs to somebody else. The gate is not waiting on this window's discipline.

**Condition 3 is now the binding one.** Four consecutive shas — `2b6afad`, `d29b9d5`, `f0b121e`,
`09e189f` — were all `in_progress` at once, queued behind each other, because the two windows are
committing faster than CI concludes. Today's concluded record on this branch is unchanged at 2
success, 3 failure, 1 cancelled. A green will keep being a coin flip while `F292` stands, and now it
is also a *late* coin flip: by the time a sha concludes, `HEAD` is three commits past it. Whether
`F292` outranks feature work is on the review page (`D-5`), not decided here.

### D-2 — `F309` and `F310` specced together

`openspec/changes/2026-09-10-the-control-that-asks-holds-the-keyboard/` — proposal, design, tasks,
and deltas against **two** capabilities. `openspec validate --strict` passes; `openspec list` reads
it as 3 deltas / 30 tasks.

**The argument.** Both findings are one failure: two mechanisms act on one keystroke and the one
*further from the operator* wins. `F310` — `useDialogFocus` binds Escape on `document` in the bubble
phase, which is structurally the last position any listener can hold, and closes unconditionally.
`F309` — Radix returns focus to the menu trigger after React has mounted the input the selection
opened. The fix is arbitration in both halves: the hook stands down on an Escape that is already
`defaultPrevented`, and a menu item that opens a control expecting input tells the menu not to take
focus back.

**Three things the round measured rather than assumed.**

1. **The ordering, out of the installed dependencies.** `@radix-ui/react-use-escape-keydown`
   registers on `document` with `{ capture: true }`, and `DismissableLayer` calls
   `event.preventDefault()` before `onDismiss()`. React 18 delegates `onKeyDown` at the root
   container, a descendant of `document`. So both classes of nearer owner run *before* the hook, and
   `event.defaultPrevented` — the platform's own word for "someone dealt with this" — is already
   true when the hook needs it. No stack, no DOM sniffing, no Radix knowledge in our code.
2. **The conditional inside that branch.** Radix's `preventDefault()` sits inside
   `if (!event.defaultPrevented && onDismiss)`; a layer without an `onDismiss` would mark nothing and
   the whole decision would be wrong while every surrounding fact stayed true. `react-menu`
   supplies one at both content roots (`:145`, `:160`). Checked, and the check is written into
   `design.md` D1 as the thing a later round should re-read.
3. **A third instance of `F310` that nobody has driven.** `DirectoryPicker.tsx:57-62` owns Escape and
   is mounted at `ProjectManagerModal.tsx:157-166` *inside* a panel holding
   `useDialogFocus(!!mode, …)` at `:73`. Escape in the directory browser closes the browser **and
   the project modal behind it**. It already calls `preventDefault()`, so the hook's one added
   condition repairs it without an edit — which is also the argument that the contract being
   proposed is the natural one rather than one invented for this change. `tasks.md` 7.4 requires it
   driven rather than reasoned about.

**The delta stopped being additive when it was grepped.** `task-lifecycle-governance` already ships
*"A waiting task names what it is waiting for"* with a scenario reading *"an operator surface
offering a move to the waiting status obtains the statement before requesting the move"* — and the
product **obeys** it: the drawer does ask, and the confirm stays disabled until something is typed.
`F309` is not a breach of it. So the delta MODIFIES that requirement to say the collection must be
operable without a pointer, instead of adding a near-duplicate in a second capability. That is
`D-4`'s instruction executed at `R1`; whether the modification earns its place at all is written
into `design.md` D6 as an open challenge for `R2`/`R3`.

**`F307` is explicitly not subsumed**, and the reasoning is in the proposal rather than left
implicit: it is the *Tab* branch of the same hook, its fix needs a decision about which control a
destructive confirmation focuses that belongs to the operator, and it reaches five dialogs this
drive never opened. The dependency that runs the other way is stated too — if `F307` is later fixed
by binding Escape on the panel, this change's arbitration rule survives unchanged, because it
constrains who answers and not where the listener lives. `F311` stays ratcheted per `f563815`.

**One link this round did not measure, and refused to build on.** `F309` measured only the end state
(`activeElement` is the trigger); whether `autoFocus` fires and is overridden, or never fires, is
unknown. If it never fires, preventing Radix's restore would drop focus on `body` — worse than
today. So `tasks.md` 4.1 **removes** `autoFocus` and focuses from a ref instead: one mechanism, and
an outcome that does not depend on which answer is true. The unmeasured link is named in the
proposal rather than papered over.

### Verification

- `openspec validate --strict 2026-09-10-the-control-that-asks-holds-the-keyboard` — **valid**.
- `openspec list` — `3 deltas`, `0/30 tasks`.
- The MODIFIED requirement was diffed against `openspec/specs/task-lifecycle-governance/spec.md:580-613`
  and is byte-faithful apart from one added paragraph and one added scenario.
- Every source claim in the proposal carries a `file:line` that was read this firing, including the
  four other `RowMenu` call sites and the five Escape owners that are **not** inside a
  `useDialogFocus` panel — the boundary of the change, re-derived by grep rather than taken from
  `F307`'s table, which is what `F311` is about.
- No test suite was run and nothing under `hub/hub/` or `src/agentweave/` was touched. This window
  fills.

**Next:** `D-3` — spec loop R2. Re-derive the argument against the code, do not re-read this
reasoning. The two places most likely to be wrong: `design.md` D1's ordering claim (re-read
`react-menu` `:145`/`:160` and the capture flag, not this log), and `design.md` D6's open question
about whether the `task-lifecycle-governance` modification is the same rule in a second voice.
`D-0` stays open.

---

## Iteration 5 — round 2 found the proposal claiming a keyboard path the change does not build

### D-0 — refused, on condition 3 alone

Measured first, before this iteration wrote anything, at `HEAD` = `c3ef45e`.

| # | condition | result |
|---|---|---|
| 1 | branch not `master`, `git rev-list --count HEAD..master` | **pass** — `autonomous/2026-09-08-daily`, count `0` |
| 2 | clean tree, `HEAD` == `@{u}` | **pass** — nothing modified, `c3ef45e` both sides |
| 3 | CI `success` at `HEAD`'s exact sha | **FAIL** — `c3ef45e` `in_progress`, started 09:03:52Z |
| 4 | no line-initial `HOLD MERGE` in newest dated `DIRECTION.md` section | **pass** — newest is `2026-09-09`, no hold |

Two things changed since iteration 4 and both are worth carrying, because the state file's note is
now stale in the *optimistic* direction.

**Condition 2 held for the first time in three firings.** The `decide` window had committed
`889d1e2` and left nothing behind, so this window did not inherit somebody else's dirt.

**The flakiness story is out of date; the lateness story is not.** The state note reads *"today's
concluded record is 2 success, 3 failure, 1 cancelled."* Recounted from
`gh run list --limit 20`, 2026-09-10 now stands at **9 success, 7 failure, 1 cancelled** — and the
six most recent *concluded* runs are `889d1e2` success, `09e189f` failure, `f0b121e` success,
`d29b9d5` success, `2b6afad` success, `f563815` failure. Five of six green. `F292` is still there,
but a green is no longer the coin flip that note describes. What has **not** improved is the
queueing: `c3ef45e` was pushed at 09:03 and had still not concluded, so the gate keeps arriving at a
sha that is not finished. Condition 3 is a *latency* obstacle now, not a *reliability* one, and the
distinction matters for the review page — it changes whether "does `F292` outrank feature work" is
even the right question to put to the operator.

One measurement, no retry. `D-0` stays open.

### D-3 — round 2, and it did not come back empty

Every load-bearing claim was re-derived from source rather than from `R1`'s prose or from iteration
4's log entry. **Three claims held, one requirement was cut back, and two mechanism descriptions
were made precise enough to implement from.**

#### The finding: the delta asserted a pointer-free path this change does not deliver

`R1`'s `task-lifecycle-governance` modification added a scenario reading *"**WHEN** the operator
moves a task to the waiting status **without using a pointer at any point**"*. That is more than the
change delivers, and the gap is exactly the finding the change deliberately excludes. Re-measured
end to end rather than argued:

| step | what the code does | keyboard? |
|---|---|---|
| open the ticket | `TaskCard.tsx:213-214` is `role="button" tabIndex={0}` with an Enter/Space handler | yes |
| get focus into the panel | `useDialogFocus` moves focus nowhere on activation, and unlike three of the six dialogs `TaskDetailDrawer` autofocuses nothing at open | **no** |
| reach the status menu | the drawer renders *after* the whole board (`TasksBoard.tsx:428`), so the first Tab falls through to native order and walks every remaining board control **behind the scrim** | **no** |
| choose the move, type the reason | this change | yes |

Steps 2 and 3 are `F307`, which `design.md` D8 excludes with three stated reasons. So the scenario
would have been archived into `openspec/specs/` as current behaviour while the product had no such
path. **The requirement is kept — that half of `R1` survives — but narrowed** to *"the keyboard is
in the control asking what the task is waiting for, once the menu has closed"*, with a paragraph
saying in so many words that where focus starts when a panel opens is not settled here. `tasks.md`
gains `7.6`, which drives the narrowed scenario and **forbids** extending the leg backwards into the
pointer-free claim, so an implementer cannot quietly re-widen it.

This also settles `D6`, which `R1` left open for exactly this round: the modification earns its
place. The generic requirement says where the keyboard goes when *a menu action presents a control*;
the lifecycle requirement says that *this particular statement*, which the Hub refuses the
transition without, has to be enterable. Delete it and the corpus records the unreachable mandatory
field nowhere at all, because the interaction capability never mentions the waiting status and
nobody reading the lifecycle capability would find the rule that governs it.

#### The mechanism R1 described loosely, now read out of the dependency

`R1` wrote *"Radix returns focus to the trigger"*. There are **two** actors, not one, and both
matter to `D4`:

1. `DropdownMenuContent` composes **its own** `onCloseAutoFocus` — `context.triggerRef.current?.focus()`
   then `event.preventDefault()` (`react-dropdown-menu/dist/index.mjs:113-117`) — through
   `composeEventHandlers`, which defaults to `checkForDefaultPrevented: true`
   (`@radix-ui/primitive/dist/index.mjs`). A consumer handler that prevents the default stops
   Radix's own trigger focus running at all.
2. `FocusScope` then does `if (!unmountEvent.defaultPrevented) focus(previouslyFocusedElement ?? document.body)`
   (`react-focus-scope/dist/index.mjs:89-98`). Prevented, that is skipped too.

One line suppresses both, which is why `D4` works at all. And the location settles `D5`'s argument
independently: that code runs inside a **`setTimeout(…, 0)`** in `FocusScope`'s cleanup, strictly
later than any React commit. That is why `F309` is observable, and why a focus of our own scheduled
on a timer would be racing a macrotask we do not own.

#### What was re-derived and held unchanged — recorded, because a round that finds nothing is a real outcome

- **The ordering, at the exact lines.** `{ capture: true }` on the owner document in
  `react-use-escape-keydown`; `preventDefault()` at `:64` before `onDismiss()` at `:65`, inside the
  `if (!event.defaultPrevented && onDismiss)` at `:63`; `onDismiss` supplied at `react-menu` `:145`
  and `:160`. Added the link `R1` named but did not trace: `MenuContentImpl` is what hands that
  `onDismiss` to `DismissableLayer` (`:264-272`), so the chain from a `DropdownMenu` to the
  `preventDefault()` is complete rather than inferred.
- **The boundary, from `grep` rather than from `R1`'s list.** Seven Escape owners in `src/`; two
  inside an active `useDialogFocus` panel, five not. The one needing chasing was
  `SpecDocumentBrowser`, which has two hosts and the first has two of its own — all page level.
  `ModelPicker` was the candidate that could have made a seventh instance if a dialog mounted it; it
  reaches the tree only through `ComposerModelControls.tsx:183` inside `Composer`, which no dialog
  mounts.
- **The nesting invariant.** Six `useDialogFocus` call sites, six mount sites, `D7`'s list exact.
  One correction: the drawer mounts at **two** boards (`TasksBoard.tsx:428`,
  `DependencyBoard.tsx:534`), both page level, same component.
- **The MODIFIED requirement against the shipped one** — byte-faithful apart from the paragraph and
  scenario `R1` added, both of which this round then rewrote.
- **The two ADDED requirements against `hub-interaction-feedback`'s existing six.** All six are about
  *appearance* — resting/hover/pressed/focus states, hover-reveal, easing, semantic tokens. Neither
  new requirement collides. This is partial credit toward `D-4`, not a substitute for it: `D-4`
  should still read the five bodies rather than their titles.

#### One smaller thing, on the acceptance contract rather than on the argument

`tasks.md` 7.2 required **exactly** *"36 passed / 0 failed — anything else is not this change
landing"*, while 7.4 and 7.5 each add legs to that same harness, which makes the stated total
unreachable by following the tasks. Worse, the harness's `D2` block prints
`no project-manager trigger reachable — skipped` and calls no `check()` at all
(`t_d1_0910_escape_across_the_dialogs.py:472-473`), so a missing trigger lowers the count without
failing anything — and 7.4 depends on that same block opening. Both counts are now floors, and
`7.2a` names the silent-skip hazard.

### Verification

- `openspec validate --strict 2026-09-10-the-control-that-asks-holds-the-keyboard` — **valid**.
- `openspec list` — `0/32 tasks` (was 30; `7.2a` and `7.6` added).
- Every line citation this round added or changed was read at that line before it was written,
  including three of `R1`'s that this round corrected: `react-menu` `:264-272` not `:270-272`,
  `react-focus-scope` `:89-98`, and `TasksBoard.tsx:428`.
- Files re-read this firing, none of them from prose: `useDialogFocus.ts`, `RowMenu.tsx`,
  `TaskDetailDrawer.tsx`, `TaskCard.tsx`, `TasksBoard.tsx`, `DirectoryPicker.tsx`,
  `ProjectManagerModal.tsx`, `taskDetailDrawer.test.tsx`,
  `t_d1_0910_escape_across_the_dialogs.py`, and five files under `hub/ui/node_modules/@radix-ui/`.
- No test suite was run and nothing under `hub/hub/` or `src/agentweave/` was touched. This window
  fills.

**Next:** `D-4` — round 3, fresh and independent. It has one instruction this round can sharpen
rather than repeat: `R2`'s finding was that a **delta claimed more than the change delivers**, so
`R3` should check the other two deltas the same way — read each `hub-interaction-feedback` scenario
and ask *"which task produces the evidence for this, and can it?"* — rather than only checking the
deltas against the existing corpus. `D-0` stays open.

---

## Iteration 6 — 2026-09-10, day window

**Position on entry.** Branch `autonomous/2026-09-08-daily`, HEAD `d783eb2`, clean tree — all three
match what `STATE-day.json` claimed. Nothing to reconcile.

### D-0 — the merge gate, one measurement, and the prediction held

| condition | verdict |
|---|---|
| 1. `git rev-list --count HEAD..master` is `0` | **pass** (0) |
| 2. clean tree, `HEAD` == `@{u}` | **pass** (both `d783eb2`) |
| 3. CI concluded `success` for **this** sha | **FAIL** — `d783eb2`'s run started `09:18:05Z` and is `in_progress` |
| 4. no line-initial `HOLD MERGE` in `DIRECTION.md` | **pass** |

Condition 3 again, and again for latency rather than for redness: the two runs before it —
`c3ef45e` and `889d1e2` — both concluded **success**. The gate keeps arriving at a sha whose CI has
not finished, three firings running. `D-0` stays open. One measurement, no retry.

### D-4 — round 3, and it found the thing rounds 1 and 2 agreed on

`R3` did not repeat `R2`'s re-derivations. It did the two things `R2` did not: apply `R2`'s own test
to the *other* delta, and read the existing capability's requirement **bodies**.

#### The finding: the third instance is not repaired, and two rounds said it was

`R1` found a third, undriven instance of `F310` — `DirectoryPicker` inside `ProjectManagerModal` —
and concluded it was **repaired without an edit**, because the picker already calls
`preventDefault()` at `:57-62`. `R2` re-read the file and agreed. `tasks.md` §2.2 was written as
*"Change nothing in `DirectoryPicker.tsx`."*

That handler cannot run. It is a React `onKeyDown` bound to the picker's own root `div`
(`DirectoryPicker.tsx:85-91`, `tabIndex={-1}`), and **nothing in the file ever focuses that root** —
`grep -n "focus" DirectoryPicker.tsx` returns nothing at all. Opening the browser leaves focus on the
`Browse…` button, which is a *sibling* of the picker (`ProjectManagerModal.tsx:153`, picker at
`:157-166`). React dispatches a synthetic event along the React tree **from the target**, so the
keystroke never travels through the picker's subtree, `preventDefault()` never runs, and §1.1's
`defaultPrevented` condition is false. The modal closes and takes the browser with it.

The gesture's *cost* is what `F310` describes and its *mechanism* is not — which is exactly why two
rounds reading for outcome agreed on the wrong one.

**The change's own D3 already argued this, in the opposite direction.** D3 rejects binding the hook
on the panel because *"focus is frequently not inside the panel at all"* — `F307`, measured. That is
precisely why the picker's panel-scoped handler is dead. D2's bullet and D3's argument could not both
be right, and D3 is the one that is. Recorded as **D9**: arbitration by `defaultPrevented` presumes
the nested owner **holds the keyboard**, and the precondition differs by mechanism — Radix binds
`document`/capture and is focus-independent; a React `onKeyDown` is not a listener for Escape but for
*Escape pressed by someone standing inside me*. Under that reading the three instances split: the
menu works; the reason input works **because of §4.1**, which makes the `F310` half *dependent on*
the `F309` half rather than merely adjacent to it (now §2.1a); the browser does not work at all.

**The repair is on-theme rather than a patch,** which is why the scenario was kept and made true
rather than narrowed away as `R2` narrowed the lifecycle one. A directory browser is a control that
asks — the change's own title is the rule that fixes it — and the picker already carries
`role="dialog"` and `tabIndex={-1}`, the markup for a panel that means to be focused and was never
told to be. §2.2 now focuses it on open; §2.2a returns focus to the trigger on close, so the fix does
not import `F307`'s shape by a new route; §2.2b and a carve-out in D8 stop an implementer reading it
as the general rule.

#### The second finding: a scenario falsified by correct behaviour

Applying `R2`'s test — *name the task that produces this scenario's evidence, then ask whether it
can* — to all seven scenarios in the two `hub-interaction-feedback` ADDED requirements:

| scenario | task | can it? |
|---|---|---|
| menu inside a panel | §7.2 leg `C2` | yes — Radix is focus-independent |
| input inside a panel | §2.1 + §7.2 leg `C3` | yes, **but only via §4.1** → §2.1a |
| panel over a panel | §2.2 + §7.4 | **no** → the finding above |
| nothing nearer, panel still dismisses | §6.1, §7.2, §7.7 | yes |
| keyboard lands in the answer | §3.1-3.4, §4.1, §7.6 | yes |
| an action asking nothing restores focus | §7.5 | yes |
| typing an answer does not operate the menu | **none** | **no task existed** |

The last row had no leg behind it in any round. §7.2 and §7.3 name floors on harnesses that do not
cover it, and §7.6 types without asserting anything about the menu. Worse, the scenario's second
clause read *"no part of the surface becomes unresponsive to the pointer while they type"* — which is
**false of the correctly-fixed product**: `TaskDetailDrawer` renders a `position: fixed; inset: 0`
scrim (`:195-209`) that deliberately makes the board behind it unresponsive, and the drawer's own
click-outside listener depends on it. Archiving that sentence into `openspec/specs/` would have
recorded a shipped, intentional behaviour as a defect. Narrowed to *"the surface behind that control
does not stop answering the pointer **because a menu re-opened over it**"*, which is what `F309` leg
`P5` actually measured, and given evidence by a new §7.8 — the `PROBE` in
`t_d1_0910_rowmenu_leaves_the_page_inert.py` already reads both facts, so it extends a leg rather
than building machinery. §7.8a records that `P5` itself is **not** that evidence: it focuses the
trigger by hand, so it passes before *and* after the fix.

#### (b) the existing capability, read as bodies

There are **six** existing requirements in `hub-interaction-feedback`, not the five `next_action`
named. All six read in full. `R2`'s title-level verdict — all about appearance — survives the
bodies, and neither ADDED requirement collides. One interaction a title-level read would have
missed: *"Keyboard focus is visible"* is a shipped requirement, and §4.1 replaces a browser-driven
`autoFocus` with a programmatic `.focus()`, which is the substitution that silently loses
`:focus-visible`. Not a breach as written — the shipped scenario is scoped to keyboard arrival, and
the `F309` keyboard path still arrives by keyboard — but one implementation away from being one.
§4.1a now guards it and §7.6 drives it in the leg that is already keyboard-only.

#### Also changed

§7.4 was rewritten to specify the focus state it must drive **from**: without clicking inside the
browser first. A leg that clicks a breadcrumb before pressing Escape would put focus inside the
picker's subtree, fire the existing handler, and pass against an **unfixed** `DirectoryPicker` —
proving nothing. §7.4a adds the mutation-check for the one task two rounds denied was needed: red
with §1.1 but without §2.2. §7.4b drives §2.2a's close path.

### Verification

- `openspec validate --strict 2026-09-10-the-control-that-asks-holds-the-keyboard` — **valid**.
- `openspec list` — `0/40 tasks` (was 32; §2.1a, §2.2a, §2.2b, §4.1a, §7.4a, §7.4b, §7.8, §7.8a).
- Every claim above was read at the line before it was written: `DirectoryPicker.tsx` in full,
  `ProjectManagerModal.tsx:148-180` and `:73`, `useDialogFocus.ts` in full, `TaskDetailDrawer.tsx`
  `:195-209` and `:400-420`, `openspec/specs/hub-interaction-feedback/spec.md` all 115 lines, and
  both drive harnesses' leg structure including `P5` in full.
- Nothing under `hub/hub/` or `src/agentweave/` was touched, and no harness was edited — `R3` names
  the legs, the night window writes them. This window fills.

**Next:** `D-5`, the review page. `D-0` stays open.

---

## Iteration 7 — the gate opened, and the review page is written

**Position on entry.** Branch `autonomous/2026-09-08-daily`, `HEAD` `5d928f5`, clean tree — all three
match `STATE-day.json`. Nothing to reconcile.

### D-0 — **the gate opened.** `master` is `5d928f5`

Measured first in the firing, before anything was committed, which is the ordering `8596706`
introduced and the reason this worked.

| condition | measurement | verdict |
|---|---|---|
| 1. not `master`, `git rev-list --count HEAD..master` = 0 | `0` | **pass** |
| 2. clean tree, `HEAD` == `@{u}` | empty; both `5d928f5` | **pass** |
| 3. CI concluded `success` for **this** sha | `5d928f5`, workflow CI, created `09:30:06Z`, `completed`/`success` | **pass** |
| 4. no line-initial `HOLD MERGE` in `DIRECTION.md`'s newest section | no match | **pass** |

```
git checkout master
git merge --ff-only autonomous/2026-09-08-daily     # bdb5a79..5d928f5, fast-forward
git push origin master
git checkout autonomous/2026-09-08-daily
```

**29 commits landed**, spanning 2026-09-09 and 2026-09-10: last night's FIX window (`bd5f3f8`
through `4937ece`, 14 commits) and today's day and decide windows (15). `--ff-only` throughout; no
merge commit, no conflict resolution.

**The branch was deliberately NOT re-cut.** The playbook's gate step ends *"then start today's branch
from the new `master`"*, and that is right at iteration 1 and wrong here: a separate decide window
commits on this branch and a mid-day switch would strand it. The branch is now identical to
`master`, `HEAD..master` is 0, and tomorrow's arming does the cut. Recorded so nobody reads the
un-cut branch as a missed step.

#### Which of the three diagnoses was the operative one

Six refusals today, and three different explanations written for them. Two are still true; only one
was load-bearing.

| firing | failed on | why |
|---|---|---|
| 1 | 3 | `6e1a054` `in_progress`; a stale green four commits back correctly refused |
| 2 | 3 | last three *concluded* runs all `failure`, all `F292` |
| 3 | 2 | uncommitted `ROADMAP.md` — **another window's dirt** |
| 4 | 2, 3 | same, plus a run in flight |
| 5 | 3 | `c3ef45e` pushed 09:03, still running |
| 6 | 3 | `d783eb2` started `09:18:05Z`, still running; the two before it green |
| 7 | — | **opened** |

- *"It races the window's own commits"* (iteration 1) — **this is the one that opened it.** The
  repair was ordering, not relaxation: check the gate before the firing commits.
- *"It is waiting on a coin flip, not a clock"* (iteration 2) — true when written, and it stopped
  being true during the day.
- *"Latency, not redness"* (iterations 5-6) — true, and the closest to the day's overall shape.

**This retires an ask rather than raising one.** Iteration 2 called the gate *"the strongest argument
yet for putting `F292` ahead of feature work"* and queued it for the review page. It does not hold:
**zero** of the six refusals was a red run at `HEAD` — five were unfinished runs and one was a dirty
tree. A scheduling change landed the cycle; an `F292` fix would not have. Written on the page as a
stated fact with **no decision attached**.

CI on this branch today, recounted: **20 runs — 12 success, 7 failure, 1 cancelled.** Five of the six
most recent concluded runs are green.

### D-5 — `spec-queue/review/review-2026-09-10.html`

38,133 bytes. Self-contained by measurement, not by intention: `grep` for `http://`, `https://`,
`<link`, `<script`, `@import` and `src=` returns **nothing**. Full `DOCTYPE`/`html`/`head`/`body`,
light colours on a bare `:root` redefined once under `@media (prefers-color-scheme: dark)`, explicit
`body { background: var(--bg) }`. Parsed with `html.parser` tracking a tag stack: **zero unclosed
tags, zero mismatches**. Six `<h2 id="sN">` sections, `s1`-`s6`, in the contract's order.

What each section carries, against the `D-5` contract:

1. **The branch and the gate** — 29 commits, two days, the gate **opened**, the four conditions as
   measured, the six refusals and which condition each failed, the three diagnoses and which one was
   operative, and the `F292` question answered by events rather than put to the operator.
2. **What the night built, driven vs tested** — the F142/clear-instructions change **driven 59/59**;
   the four repairs **tested only**, and why that is correct for scripts and CI config. Plus what the
   night left open (`F307`, `F308`) and the `--dry-run` caveat on the constraints pin.
3. **The drive** — `F309` (A), `F310` (B), `F311` (C), each a severity, a sentence and a `file:line`;
   the two confirmations (21/21 on last night's dialog; `F307`'s wrong write reaching the database
   for the first time); and the candidate **A that was measured away** rather than filed.
4. **What was specced** — the argument in a paragraph, then **led with R3's `DirectoryPicker`
   finding**, because a defect two rounds agreed on is the best evidence on the page that three
   rounds earn their keep. Then R2's over-claiming delta, R3's scenario falsified by *correct*
   behaviour (the drawer's deliberate scrim), and the generalisation of all three into one reusable
   test. A per-round table saying what each round changed — **no round came back empty**, so the
   "a round changed nothing" sentence is absent by measurement. Cost stated as 32 → **40**. `F307`'s
   non-subsumption is `DAY-1`, the page's only ask.
5. **If you approve nothing** — the night's default queue walked and measured now: source 1 empty
   (the only unarchived change is today's, 0/40); source 2 is 150 open findings of which **6 are A**
   and exactly one has a proposal; source 3 empty by hypothesis. Named plainly as thin.
6. **The research**, last and brief — three candidates, each ending in what it would mean here.

### `APPROVALS.md`

Today's section inserted **above** `## 2026-09-09` (newest first). One row for the change, the
directory name exactly — `test -d` confirms `openspec/changes/2026-09-10-the-control-that-asks-holds-the-keyboard`
exists and `grep -c` finds the string once. **No status token**, stated in the section itself so a
23:00 reader cannot mistake a row for an approval. **No `ORDER:` line**, also stated — which has the
side effect of retiring the 2026-09-09 `ORDER:` line, exactly as that section retired 2026-09-08's.

### Verification

- Merge: `git rev-parse master` and `origin/master` both `5d928f5`; branch back to
  `autonomous/2026-09-08-daily`; tree clean at every step.
- Page: external-reference grep empty; tag-stack parse clean; six sections present; 38,133 bytes.
- Ledger numbers on the page are from `py -3.11 scripts/classify_findings.py` **run this iteration**
  (312 sections, 150 open: 6 A / 60 B / 68 C / 14 D / 2 unlabelled, 8 CONFLICT), not quoted forward.
- CI counts from `gh run list --branch <branch> --limit 40`, filtered to workflow CI and today.
- Nothing under `hub/hub/` or `src/agentweave/` was touched. This window fills.

**Next:** `D-6` — the no-spec repairs, if the day has room. `D-0` and `D-5` are **done**.

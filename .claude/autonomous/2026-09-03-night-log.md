# 2026-09-03 night window

Newest entry at the BOTTOM. Playbook: `.claude/loops/night-window.md`.

Armed 2026-09-03T22:55:07+01:00 on `autonomous/2026-09-03-daily` (parent master@2912736), model opus.

---

## Iteration 1 — compose the queue (23:00–23:1x)

**Branch and state agree.** `git branch --show-current` is `autonomous/2026-09-03-daily`; HEAD is
`aed4791 arm(night): 2026-09-03 cycle on autonomous/2026-09-03-daily`, whose parent is
`2912736`, exactly the `parent_sha` the state claims. Tree clean. Nothing to reconcile.

**What `spec-queue/APPROVALS.md` says.** The newest section is 2026-09-03 and it carries an explicit
`ORDER:` line, so the playbook's default queue is ignored:

```
ORDER: a-blocked-agent-workspace-holds-its-input, a-write-outside-the-workspace-is-recorded
```

Both rows are `APPROVED`, both written by the DECIDE session rather than by the day window that
proposed one of them. The order is load-bearing, not decoration — the two changes collide on
`agent_trigger.py` and `worktrees.py`, and `a-write` moves `hub/hub/static/ui` on top of that, which
is the one combination this repo cannot build concurrently. The section also says in as many words
that **stopping part-way through `a-blocked` is the expected outcome**, and that starting `a-write`
before `a-blocked` is finished and archived is the thing that is not fine.

The three sources the playbook would otherwise fall back on are unavailable tonight, and APPROVALS
already worked out why: source 1 (implemented changes needing only archiving) is empty — both open
changes sit at zero ticked tasks, which I confirmed (`a-blocked` 32 unticked, `a-write` 54 unticked,
0 ticked in each). Source 2 lands on F271 and F274, neither of which has a proposal, and the
playbook's own rule makes an unproposed finding a note to tomorrow rather than work.

**The tree is green where this work lands — measured, not assumed.** Ran the nine `hub/tests/` files
covering the four modules `a-blocked` edits:

```
py -3.11 -m pytest hub/tests/test_agent_trigger.py test_agent_trigger_overrides.py \
  test_turn_scheduler.py test_scheduler.py test_worktrees.py test_task_worktrees.py \
  test_task_workspace_scheme.py test_turn_workspace.py test_failed_run_returns_input.py -q -p no:randomly
→ 242 passed, 1 xpassed, 14 warnings in 194.55s (0:03:14)
```

**3:14, not 15–25 minutes** — that range is the whole `hub/tests/` suite and is a bad guide to a
targeted chunk. Recorded in the `a-blocked-p6-gates` queue item so the firing that runs the full
gates sizes them off the right number. No inherited breakage, so tonight's first item is real work
rather than a repair.

**Two facts checked now so a later firing does not have to.** `a-write`'s task 4.2 names migration
`0101`; the head under `hub/hub/migrations/versions/` is still `0100_loop_work_needs_evidence.py`,
so `0101` is correct and the 2026-09-02 fix still holds. And `F188` in `scripts/drive/FINDINGS.md`
still carries **no `Status:` line at all** (line ~90), while the index paragraph at line ~96 reads
"three — F271, F188 and now F274". Both need editing when the change archives; that is written into
`a-blocked-archive` rather than left as a note, per the playbook's rule that noticing is not fixing.

**The queue — 11 items, `a-blocked` split at phase boundaries and phase 3 split three ways.** Phase 3
is 13 of the change's 32 tasks and carries nine tests including the grandfathered case round 3 exists
for, so one firing per third. Phase 1 is its own item because it is a **gate**: the reproduction must
pass against unmodified code, and if it does not, the change's behaviour claim at HEAD is inference
from reading the code rather than a fresh drive — that is a stop, not a workaround. `a-write` gets a
single item whose whole content is *split your own 54 tasks*, guarded by an explicit
do-not-start-until.

**Correction to my own method, recorded because it wasted about a minute of wall clock and could have
wasted an hour.** I twice reported "waited 110×5s" from a bash loop built on `read -t 5 x </dev/null`,
which returns instantly on EOF rather than sleeping. Both waits were milliseconds. `Get-Date` caught
it: the clock said 23:02 when the loops claimed nineteen minutes had passed. Anything timing a
background job here should poll with PowerShell `Start-Sleep`, or read the process's own elapsed
time, not a bash `read` timeout against `/dev/null`.

**Next:** `a-blocked-p1` — write the reproduction and run it unmodified. Nothing else.

---

## Iteration 2 — `a-blocked` phase 1: the reproduction, and it holds (23:2x–23:4x)

**Branch and state agree.** `autonomous/2026-09-03-daily`, HEAD `eaf3fbd night(1): compose the
queue`, tree clean at the start. `parent_sha` `2912736` is still the merge base. Nothing to
reconcile.

**The gate passed, so the change's premise is measured rather than inferred.** New file
`hub/tests/test_a_blocked_agent_workspace_holds_its_input.py`, three tests, run against a tree with
**zero product lines modified** (`git status --short` at the time showed exactly one entry, the
untracked test file itself):

```
py -3.11 -m pytest hub/tests/test_a_blocked_agent_workspace_holds_its_input.py -v -p no:randomly
→ 3 passed in 2.74s
```

- **1.1** — one agent, one conversation, one unbound operator entry, `trigger_agent_directly`
  patched to raise `TriggerAgentError(409, "Could not prepare isolated worktree for f188-blocked:
  refusing existing path ...")` with **no flags at all**. Three schedules — a trigger, a `Continue`
  press, an end-of-turn re-drain — and the entry ends `withdrawn` at 3 attempts with
  `abandoned_reason` reading "delivery failed 3 times (...); the Hub stopped retrying". F188 is real
  at HEAD.
- **1.2** — the same seeding, the same loop with one *extra* schedule, `NO_RUNNER`
  (`agent_wide=True`): `("queued", 0, None)`. Two refusals that both mean *no turn for this agent
  can run in any conversation*, four schedules each, opposite outcomes. That asymmetry is the
  finding and it now lives in the suite.
- **1.3** — at the `worktrees` layer with no Hub: a plain directory (plus a file inside it) at
  `.agentweave/worktrees/f188-agent` in a disposable `tmp_path` repo makes `ensure_worktree` raise
  `IsolationUnavailableError`, and the message carries the path and "not the registered git
  worktree" and **no remedy**. Asserted as vocabulary — none of `remove`, `delete`, `prune`, `rm `,
  `then run` appears — rather than as one phrase, so phase 4 cannot satisfy it by rewording.

**The reproduction is not vacuous — mutation-checked, not assumed.** Added `agent_wide=True` to
`BLOCKED_AGENT_WORKSPACE` and re-ran: `1 failed, 2 passed`, the failure being 1.1 by name. So 1.1
asserts on the flag mechanism the later phases move, not on some incidental of the fixture. Reverted
immediately; `grep agent_wide` over the file confirms the only live occurrence is `NO_RUNNER`'s.

**Neighbours still green with the new file in the same session** — the shared `proj-test` project is
the reason to check rather than assume:

```
pytest test_a_blocked_agent_workspace_holds_its_input.py test_a_delivery_attempt_means_a_delivery.py \
  test_turn_scheduler.py -q -p no:randomly  → 14 passed in 13.28s
```

`ruff check` clean first time; `black --target-version py311` reformatted once and the file was
re-run green after formatting, not before.

**One note for the phases that follow.** 1.1 and 1.3 are written to *flip*: 1.1's withdrawal
assertion becomes a hold once phase 3 lands, and 1.3's no-remedy assertion becomes a positive one in
phase 4. 1.2 must keep passing untouched throughout — the file's module docstring says so, so the
next firing meets the instruction rather than having to infer it. Nothing in the file needs
rewriting to make later phases pass; the assertions are meant to move.

Tasks 1.1, 1.2 and 1.3 ticked. No other phase touched.

**Next:** `a-blocked-p2` — tasks 2.1-2.4, the `agent_workspace_unavailable` flag and the split at
`agent_trigger.py:879-883`.

---

## Iteration 3 — `a-blocked` phase 2: the refusal names which workspace it could not prepare

**Branch and state agree.** `autonomous/2026-09-03-daily`, HEAD `2d0b679 night(2): a-blocked phase
1`, tree clean at the start. `parent_sha` `2912736` still the merge base. Nothing to reconcile.

**2.1 — the flag.** `TriggerAgentError.__init__` gains `agent_workspace_unavailable: bool = False`,
stored after `agent_wide`, with a three-paragraph `#:` block in its neighbours' style: what it means
(the same question as `workspace_unavailable` one scope down), why it is **not** `agent_wide`
(design D2 — that flag's own paragraph promises it is set only where *nothing* else could run, and
an agent-workspace failure is not certainly that, so marking it there would falsify a shipped
invariant), and that it does **not** imply `transient` (borrowing that would also move
`terminal_failure=not transient`, which `scheduler.py` reads for job and flow-step outcomes).

**2.2/2.3 — the split, decided by the predicate and not by the exception.** The `except` at what was
`:879-883` now asks `worktrees.takes_task_workspace(repo_root, config, turn_workspace.task_id)` —
the same call the one-turn-per-task refusal eleven lines above already makes, with `repo_root`,
`config` and `turn_workspace` all already in scope. Verified at the source rather than assumed:
`takes_task_workspace` is `task_id is not None and is_writing_agent(config) and is_git_repo(...)`,
so the true arm's `f"...task {turn_workspace.task_id}"` can never interpolate `None`; and
`resolve_turn_workspace`'s own false arm delegates to `resolve_agent_workspace`, so the other arm's
sentence *"Could not prepare {agent}'s own workspace"* is a true statement about what failed for a
read-only agent and a grandfathered task too, not only for the unbound case.

- task arm → `f"Could not prepare the checkout for task {task_id}: {exc}"`, **no flags**, exactly as
  today.
- agent arm → `f"Could not prepare {agent}'s own workspace: {exc}"`, `agent_workspace_unavailable=True`
  and nothing else.

**2.4 — a new file, and deliberately not the phase-1 one.**
`hub/tests/test_a_blocked_workspace_refusal_names_its_scope.py`, two tests. It could not go in
`test_a_blocked_agent_workspace_holds_its_input.py` without breaking that file's stated property —
*every test here passes against unmodified code* — which is the whole of its value as a gate.

Both tests stub `worktrees.resolve_turn_workspace` to raise **one shared**
`IsolationUnavailableError` and differ only in whether the turn names a task. That shape is the
assertion: if the dispatch ever went back to reading the exception, both would come out identical.
Flags are asserted, wording is not, beyond each arm naming its own subject (the task arm asserts the
agent's name is *absent* — naming the agent while refusing over a task is the confusion this phase
ends). Phase 4 rewrites both sentences and must not disturb this file.

```
py -3.11 -m pytest hub/tests/test_a_blocked_workspace_refusal_names_its_scope.py -v -p no:randomly
→ 2 passed in 3.08s
```

**Mutation-checked twice, so neither test asserts over nothing.**

- `if takes_task_workspace(...)` → `if not takes_task_workspace(...)`: **2 failed**, both by name.
  The dispatch itself is what they hold.
- delete `agent_workspace_unavailable=True` from the agent arm: **1 failed, 1 passed** — the agent
  test by name, the task test untouched. So the flag assertion is load-bearing and the task arm's
  "no flags" is not merely inherited from the default.

Both mutations reverted; `git diff --stat` back to the same 43/1 it showed before them.

**The chunk, not just the new file.** Twelve files — the two phase-1/phase-2 files, the delivery
counter, the scheduler, both workspace files, the collision, the two workspace-unavailable files,
the trigger, and the checkpoint cutover:

```
py -3.11 -m pytest <12 files> -q -p no:randomly → 198 passed in 3:31
ruff check src/ hub/ tests/                     → All checks passed
black --check --target-version py311 ...        → 539 files unchanged
```

Phase 1 stays green as required: the flag now exists but `turn_scheduler` does not read it, so 1.1
still withdraws.

**5.2's grep, run early because I am the one who made the sentence stale.** `Could not prepare
isolated worktree` now appears at **no product line at all**, and in three test files as a
**hand-constructed** `TriggerAgentError`, never as an assertion against the real raise site — so
nothing went red and nothing silently kept passing against a sentence the product no longer emits:

- `test_a_delivery_attempt_means_a_delivery.py:37` — `BAD_CHECKOUT`, already task 5.1's subject;
- `test_a_blocked_agent_workspace_holds_its_input.py:46,122` — phase 1's own stub, whose `#:`
  comment says "what `agent_trigger.py:879-883` raises **today**". That is now yesterday's sentence
  and yesterday's flags;
- `test_checkpoint_cutover.py:1069,1072` — a `waiting_reason`/`detail` pair, **not named in task
  5.1** and found only by running the grep. Recorded here and written into the `a-blocked-p5` queue
  item.

No UI string and no non-test product line reads it. That is the "including 'nothing'" 5.2 asks for,
for the UI half.

**Two queue items amended rather than left as notes** (the playbook's rule that noticing is not
fixing): `a-blocked-p3b` now says phase 3 must add `agent_workspace_unavailable=True` to phase 1's
`BLOCKED_AGENT_WORKSPACE` stub and correct its comment — without that, 1.1 cannot flip and the
reproduction would be asserting a hold against a stub the product no longer raises. `a-blocked-p5`
now carries `test_checkpoint_cutover.py` and the grep result above.

Tasks 2.1, 2.2, 2.3 and 2.4 ticked. `turn_scheduler.py` untouched, as instructed.

**Next:** `a-blocked-p3a` — tasks 3.0, 3.0a, 3.1, 3.2: `takes_own_checkout` in `task_workspace.py`
spelled with `!=`, and the scheduler helper with its two-query shape.

---

## Iteration 4 — a-blocked phase 3a: one predicate, and the scheduler's question written down

`a-blocked-p3a`, tasks 3.0, 3.0a, 3.1 and 3.2. No behaviour changes in this iteration: the
condition at `turn_scheduler.py:204` is untouched and phase 1's reproduction still withdraws, which
is what phase 3b is for. What lands is the predicate both parties will ask and the helper that asks
it, with the argument for it in the docstring.

**3.0 — `takes_own_checkout` in `task_workspace.py`, and it is spelled the way the scan forces.**

`resolve_turn_workspace_inputs` used to fall through to `UNBOUND` in three places inline. The
predicate is those three refusals with the reason thrown away, because the scheduler has no
decision left to make between them and `None` — no row at all — is a fourth way of saying the same
thing:

```python
def takes_own_checkout(task: Optional[Task]) -> bool:
    if task is None:
        return False
    if task.workspace_scheme != TASK_SCHEME:
        return False
    return _id_complaint(task) is None
```

**The `!=` is not a style choice and the design said so; I measured it rather than trusting the
line.** The four substrings `test_nothing_outside_the_migration_writes_the_column` scans for, run
over the three candidate spellings:

```
'task.workspace_scheme == TASK_SCHEME' -> ['.workspace_scheme =']    # offender
'task.workspace_scheme==TASK_SCHEME'   -> ['workspace_scheme=']      # offender
'task.workspace_scheme != TASK_SCHEME' -> []                         # the one that may be written
```

**The `logger.warning` was the only thing that needed care.** Design D8 says the predicate cannot
carry it, and a bool genuinely cannot: one of the three refusals deserves a line in the log and the
other two do not. So the resolver calls `_warn_when_the_id_is_why(task)` on the false branch, which
re-asks the single question whose answer is a sentence — and returns early for a grandfathered
task, so the combined branch's behaviour is preserved exactly rather than widened to a case today's
code never logged. `_id_complaint` holds the one `try/except ValueError` both callers need, so
`validate_task_id` is still called from one place.

**3.0a — measured, not assumed.** `hub/tests/test_task_workspace_scheme.py` → **10 passed**, and
running the scan by hand over `hub/hub` and `src/` prints exactly one file:
`migrations/versions/0095_task_workspace_scheme.py (set workspace_scheme)`. The docstring note is
in, saying why a *read* of this column is written one way and that the scan is not to be relaxed to
accommodate the other spelling.

**3.1 — `other_input_would_have_run_elsewhere`, two queries, in the forced order.**

Public rather than `_`-prefixed: phase 3c's tests are about this function's answer on shapes a
drive cannot produce (a grandfathered task), and asking it directly is the only way to test those
without a fixture pretending to be a whole scheduler tick.

```python
async def other_input_would_have_run_elsewhere(
    db, *, project_id, agent, entries, selected, controlling_conversation_id, hop_budget
) -> bool
```

Eligibility is expressed as `WHERE` rather than as a filter over what came back — a conversation
that is closed, or belongs to another agent or project, does not appear and its entries fall out
with it. Then one query over the union of the task ids the entries name and the ids those
conversations carry, whose `IN` list does not exist until the first query has returned. Never one
per entry; the one exception is `commit_for_task_review`, per review entry considered (D3b), where
the ordinary count outside `selected` is zero.

`_tasks_this_entry_is_about` is where the `or` lives, and it reuses `_entry_kind` rather than
`run_task_binding.task_named_by` — the two columns need opposite treatment here (a review is gated
on evidence, a work entry on the scheme), and `task_named_by` returns `task_id or review_task_id`
with the precedence the other way round from `_entry_kind`'s.

**3.2 — the docstring carries the argument, including both corrections and the asymmetry.** R1's
reduction to `entry.task_id is not None` and the two unconditional routes that break it; R3's
correction that deleted and decided tasks are *conditional* because `resolve_bound_task` falls
through to `binding_for_conversation`, which is why the test must stay an `or`; and which way each
approximation errs — a false *no* holds input and the queue waits, a false *yes* destroys the
operator's message at the third schedule.

**Verified by driving the helper, then deleted.** Nothing calls it yet, so a green suite would have
proved nothing about it. A throwaway `hub/tests/test_zz_scratch_helper_probe.py` called it directly
against the real session for thirteen shapes — **13 passed**:

| shape | answer |
|---|---|
| every other entry unbound | no |
| another conversation's entry names a live task-scheme task | **yes** |
| …the same task grandfathered (`workspace_scheme` set to `agent` in the fixture) | no |
| …the same task `approved` | no |
| …a task id `validate_task_id` refuses (`../escape`) | no |
| …a task id with no row | no |
| entry names nothing, its conversation carries a live task | **yes** |
| the other conversation is `archived` | no |
| the other entry is over the hop budget | no |
| the only other entry is inside `selected` | no |
| a second entry in the *controlling* conversation, which itself carries a live task | no |
| a review entry whose task has no evidence naming a commit | no |

Mutation-checked, so the probe was not asserting over nothing: deleting the scheme line from
`takes_own_checkout` failed **`test_a_grandfathered_task_does_not_count` and nothing else**, and
replacing the controlling-conversation guard with `if True` failed
**`test_an_inherited_binding_in_the_controlling_conversation_is_not_consulted` and nothing else**.
Both reverted, `git diff --stat` back to 250/13 across three files, probe removed.

**The probe is gone and its coverage is now phase 3c's to re-establish as committed tests** — the
table above is the list, and it is written into the `a-blocked-p3c` queue item so it is not lost.

**Gates.** `ruff check src/ hub/ tests/` → all checks passed. `black --check --target-version
py311 src/ hub/hub/ hub/tests/ tests/` → 539 unchanged. The twelve-file chunk
(`test_turn_workspace`, `test_task_workspace_scheme`, both a-blocked files,
`test_a_delivery_attempt_means_a_delivery`, `test_turn_scheduler`, `test_inbound_queue`,
`test_delivery_attempts`, `test_task_resolved_before_workspace`, `test_task_worktrees`,
`test_worktrees`, `test_no_refusal_queues_input`) → **157 passed in 2:41**. Phase 1 still
withdraws, as it must until 3b flips it.

*One wasted run, recorded because the next iteration will assemble the same list:* my first
attempt at the chunk included `test_task_workspace_collision.py` and `test_workspace_unavailable.py`
from memory. Neither exists, and pytest exits on a missing path with a usage error rather than
skipping it — "5 warnings in 0.01s" and no tests, which reads like a passing run at a glance. `ls
hub/tests | grep -i workspace` before naming files.

**Next:** `a-blocked-p3b` — wire the condition at `:204`, repoint phase 1's stub at the real
agent-arm refusal, and flip 1.1 from withdrawn to held.

---

## Iteration 5 — `a-blocked` phase 3b: the condition is wired, and F188's reproduction has flipped

**Tasks 3.3–3.6, plus the stub repointing that 1.1 could not flip without.**

**3.3 — the third question, asked last.** `schedule_agent`'s counting condition was
`not transient and not getattr(exc, "agent_wide", False)`. It is now three terms, each on its own
line and each still readable as its own question:

```python
elif (
    not transient
    and not getattr(exc, "agent_wide", False)
    # ...
    and (
        not getattr(exc, "agent_workspace_unavailable", False)
        or await other_input_would_have_run_elsewhere(
            db, project_id=project_id, agent=agent, entries=entries,
            selected=selected, controlling_conversation_id=conversation.id,
            hop_budget=hop_budget,
        )
    )
):
```

Written as `not flag or await helper(...)` rather than as `not (flag and not await helper(...))`:
both are correct and one of them can be read aloud. The `or` short-circuits on the attribute read,
so the two queries are paid for **only** by a refusal that is non-transient, non-agent-wide *and*
flagged — which is the ordering constraint the phase asked for, enforced by the language rather
than by a comment. The comment above the term says why the flag is not `agent_wide` (design D2) and
quotes the requirement clause the term implements.

**The stub was pointing at a sentence the product had stopped raising.** Phase 1's
`BLOCKED_AGENT_WORKSPACE` still carried `Could not prepare isolated worktree for <agent>` with no
flags, and its `#:` comment claimed that was what `agent_trigger.py:879-883` raises *today*. Phase 2
replaced that single raise with two. Repointed at the real agent-arm refusal — `Could not prepare
f188-blocked's own workspace: ...`, `agent_workspace_unavailable=True` — and the comment now says
that phase 1 deliberately stubbed the pre-split sentence and that this is the repointing, so the
next reader does not think it drifted. Without it, the flip below would have been a flip of nothing:
the old stub carries no flag, so it would have gone on being counted forever and 1.1 would have
stayed green while asserting the defect.

**1.1 flipped, and it is now the fix's gate.** `test_a_blocked_agent_workspace_destroys_the_
operators_message` is `..._holds_the_operators_message`: same seeding, same
`DELIVERY_ATTEMPT_LIMIT` loop, `("queued", 0)` and `abandoned_reason is None`. It also asserts the
refusal is still *recorded* — `waiting_reason` carries the agent-arm sentence — because holding
input silently would be its own defect. 1.2's `NO_RUNNER` assertion is byte-identical to what phase
1 committed; its docstring moved to past tense and now says so, so a later reader can see the
control did not move to accommodate the fix.

**3.4–3.6 — five new scheduler-level tests**, `hub/tests/test_a_blocked_workspace_counts_where_
input_could_run.py`. All five go through `schedule_agent` rather than through the helper, because
they are about the *condition*: which refusals reach the counter, and what the queue looks like
after three schedules. They differ from each other **only** in what is waiting elsewhere.

| test | queue | outcome |
|---|---|---|
| 3.4a a second unbound conversation | two conversations, neither about a task | both held |
| 3.4b a task-bound entry waiting elsewhere | second conversation's entry names a live task-scheme task | head **withdrawn** at the limit |
| 3.4c the task arm, nothing waiting | identical queue to 3.4a, refusal is the *task*-checkout one | head **withdrawn** |
| 3.5 the inherited binding | second conversation names no task; the *conversation* carries one | head **withdrawn** |
| 3.6 an entry inside the refused batch | both entries in the controlling conversation, second names a vanished task | both held |

3.4b's docstring says what the phase asked it to say: it is what keeps this change inside *"where
other queued input could have run"* rather than in breach of it, and without it the change would
read as "an agent-workspace refusal is held" while every other test in the file still passed. 3.4c
is the pair that fails if a later edit re-merges the two arms `agent_trigger` split one line apart.

**GATE — mutation-checked three ways, and every one named a test.**

| mutation | failed |
|---|---|
| delete the whole third term (revert to the two-term condition) | 3.4a, 3.6, **and 1.1** — 3 failed, 5 passed |
| `return False` at the top of `other_input_would_have_run_elsewhere` | 3.4b and 3.5, and nothing else |
| replace the flag read with `True` (every refusal treated as agent-workspace) | 3.4c alone |

The second is the one worth keeping: it is what proves the two *counting* tests are not passing by
default. All three reverted; `git diff --stat` back to the two intended files plus the new one.

**Gates.** `py -3.11 -m ruff check src/ hub/ tests/` → all checks passed. `black --check
--target-version py311` → 540 unchanged. The neighbour chunk, now thirteen files with the new one
→ **162 passed in 1:46** (157 + 5). `grep` for `agent_workspace_unavailable` outside the two
product files finds only the three test files that construct it, so the condition change cannot
reach anything else in the suite.

*Bash note for the next iteration:* `ruff` and `black` are not on PATH in Git Bash here —
`py -3.11 -m ruff` / `py -3.11 -m black`. And the Bash tool's working directory **persists across
calls**, so a second `cd hub` lands in `hub/hub`; use absolute paths or `cd` from the repo root
every time.

**Next:** `a-blocked-p3c` — tasks 3.7–3.9, the helper-level tests, which are where the deleted
probe's thirteen shapes get re-established as committed assertions.

---

## Iteration 6 — `a-blocked` phase 3c: the deleted probe's shapes are committed tests again

**Tasks 3.7, 3.8, 3.8a, 3.8b, 3.9, plus the controlling-conversation shape the gate needs.**

Eleven tests appended to `hub/tests/test_a_blocked_workspace_counts_where_input_could_run.py`,
under a section banner saying why the file has two halves: everything above goes through
`schedule_agent` because it is about the *condition*; everything below calls
`other_input_would_have_run_elsewhere` directly because it is about the *scope rule*, and the three
shapes that rule turns on cannot be produced by a tick — a grandfathered task (only migration
`0095` writes the column), an id `validate_task_id` refuses (the product cannot mint the row), and
an entry left out of its own conversation's batch (needs `cap` or a kind split to arrange).

| test | fixture | answer |
|---|---|---|
| control: another conversation's live task-scheme task | task exists, `in_progress`, scheme `task` | **yes** |
| 3.7 grandfathered | the control, one column over: scheme `agent` | no |
| 3.8 unmintable id | entry names `../escape`, row exists | no |
| 3.8 deleted row | entry names `task-ffff98`, no row, thread bound to nothing | no |
| 3.8 decided task | `approved`, thread bound to nothing | no |
| 3.8a decided task, live thread | same entry, thread carries a live task-scheme task | **yes** |
| 3.8b review, no evidence | `review_task_id`, task `completed`, no evidence | no |
| 3.8b review, evidence naming a commit | the same plus `record_review_evidence` | **yes** |
| 3.9 over the hop budget | live task, `hop_depth=7`, budget 6 | no |
| 3.9 archived thread | live task, conversation `archived` | no |
| controlling conversation's own binding | two entries in the refused thread, which carries a live task | no |

**`_seed` was widened rather than duplicated**, as the queue item asked. Each of its three tuple
kinds now takes one optional trailing element — a conversation's `lifecycle`, a task's
`workspace_scheme`, and a dict of further `new_entry` keywords for an entry (`review_task_id`,
`hop_depth`) — defaulting to the shapes a tick produces, so all five scheduler-level tests above
read byte-identically. The evidence row for 3.8b's positive sibling is `hub/tests/review_evidence.py`,
which already exists for exactly this (a document and a requirement, because
`RequirementEvidence.requirement_id` is not nullable).

**One deviation from the task text, deliberate.** Task 3.7 asks its docstring to say that *a scope
test built on `entry.task_id` alone passes every other test in this file and fails this one*. That
sentence is already 3.5's, verbatim and correctly — a rule reading only `entry.task_id` fails 3.5,
so it cannot also be true that it passes every test but 3.7. Written instead as what is true and is
the sharper claim: **3.5 and 3.7 pin the rule from opposite sides and neither holds it alone** —
3.5 rejects a rule too narrow (missing the thread's binding), 3.7 rejects one too wide (R1's "any
entry naming a task"), and a rule that resolves the row but skips the scheme is wrong at 3.7 alone.
The last clause is measured below rather than asserted.

**GATE — eight mutations, every one naming a test, and all eleven new tests pinned by at least
one.** The two the queue named are the first two.

| mutation | failed |
|---|---|
| delete the scheme line from `takes_own_checkout` | **3.7**, and `test_turn_workspace::test_a_grandfathered_task_keeps_the_per_agent_checkout` |
| `if True` for the controlling-conversation guard in `_tasks_this_entry_is_about` | the controlling-conversation test alone |
| `return False` at the top of the helper | the control, 3.8a, 3.8b's positive, **and** 3.4b and 3.5 from the first half |
| review branch returns True without `commit_for_task_review` | 3.8b's negative alone |
| drop `decided_task_refusal(task) is None` | the decided-task test alone |
| drop `Conversation.lifecycle == "open"` from the `WHERE` | the archived-thread test alone |
| drop `entry.hop_depth <= hop_budget` from `remaining` | the over-budget test alone |
| `takes_own_checkout` ignores `_id_complaint` | the unmintable-id test alone |
| a missing task row counts instead of `continue` | the deleted-row test alone |

The scheme mutation failing a *second*, pre-existing test is right and is not the iteration-4
measurement contradicting itself: iteration 4 measured a throwaway probe against the helper, where
`test_turn_workspace` was not in the run. The resolver has always had its own grandfathering test;
what is new is that the scheduler's copy of the question now has one too.

*A Bash trap worth writing down, because it silently rewrote a product file.* Reading a source file
with `open(p, encoding="utf-8")` and writing it back with `open(p, "w", encoding="utf-8")` converts
every LF to CRLF on this machine — `git status` showed `turn_scheduler.py` modified after a mutation
script that had restored its own text exactly. Only `git diff` explains it ("CRLF will be replaced
by LF"). Pass `newline=""` to both ends of any mutate-run-restore script, and check
`git status --porcelain` after one. The heredoc that first tried to append this file's new section
also died with `unexpected EOF while looking for matching '` on content that contains no unbalanced
quote; the Write tool plus `cat >>` did it without complaint.

**Gates.** `py -3.11 -m ruff check src/ hub/ tests/` → all checks passed. `black --check
--target-version py311` → 540 unchanged. The thirteen-file chunk → **173 passed in 1:22** (162 + the
11 new). No product file changed this iteration: `git status` is the test file and `tasks.md`.

**Next:** `a-blocked-p4` — `worktrees.py`'s two refusal branches get their own remedies, plus the
mid-merge one from the task arm, each asserted at the worktrees layer against the obstruction it was
written for.

---

## Iteration 7 — `a-blocked` phase 4: the refusal says what would clear it, and the remedy is performed

**Tasks 4.1–4.3, ticked.** Product change is `hub/hub/worktrees.py` only.

**The shape was as the last iteration measured, so the work was a split, not an edit.**
`ensure_worktree` had *one* branch — `if path.is_symlink() or _registered_worktree_branch(...)
!= expected_ref` — raising one sentence. It is now two branches with two remedies, and the same
split was made in `ensure_task_worktree`, which had the identical `or` a few lines above its
mid-merge refusal.

| branch | obstruction | remedy it now states |
|---|---|---|
| `ensure_worktree` | a symlink | remove **the link** (`rm <path>`), which leaves whatever it points at untouched |
| `ensure_worktree` | not the registered worktree for the ref | move or delete **the directory** (`rm -r <path>`); the next turn prunes and provisions |
| `ensure_task_worktree` | a symlink | the same, naming the task |
| `ensure_task_worktree` | not the registered worktree | the same, naming the task |
| `ensure_task_worktree` | left mid-merge | `git -C <path> merge --abort`, and **do not remove the directory** — it is the registered checkout and carries the task's work |

**Scope, decided and recorded in `tasks.md` because the queue asked for it.** Two of the module's
three copies of this shape were changed; `ensure_review_checkout` was not. Neither 4.1 nor 4.2
names it, the requirement scopes itself to "the agent's own, or a named task's" workspace, and its
guard is a different shape (`existing_review_checkout(...) is None` folds several causes into one
`None`), so splitting it correctly is its own analysis rather than a fourth application of this
one. The task arm **is** in scope: scenario 2 is about it, and 4.2's contrast ("real branches
rather than two wordings of one") needs a second, differently-remedied obstruction at the same
path to set against the mid-merge one.

**4.2 turned out to be a sharpening, not a creation.** The mid-merge branch already carried a
remedy ("Resolve or abort that merge before this task runs again"). It now names the command and
adds the clause that makes the third scenario bite: *do not remove the directory* — the exact
opposite of what its sibling at the same path asks for.

**Phase 1's test 1.3 flipped, as it was written to.**
`test_a_plain_directory_in_the_way_refuses_with_no_remedy` →
`test_a_plain_directory_in_the_way_says_what_to_remove`, docstring saying it was written to flip,
and the negative vocabulary assertion inverted in place so the two halves stay comparable. A plain
directory reaches the wrong-ref branch, so it is that branch's remedy it reads.

**New file `hub/tests/test_a_blocked_workspace_refusal_states_its_remedy.py`, five tests.** Each
asserts *its own* branch's remedy, and — the part worth having — **performs it**: unlink the
symlink and provisioning succeeds with the link's target intact; `rm -r` the foreign directory and
provisioning succeeds; `git merge --abort` and the checkout comes back with the task's own commit
still in it. A remedy is a promise about the product, so a test that only reads the sentence leaves
the promise unmeasured.

The wrong-ref remedy says *the next turn* prunes rather than telling the operator to, because
`ensure_worktree` already runs `git worktree prune` before `worktree add` — telling them to run it
would be inventing a step. That claim is the reason the agent-arm test blocks the path with a
worktree **registered for another ref** rather than a plain directory: after `rm -r`, git still
lists it `prunable` (asserted), so the reprovision genuinely exercises the promised prune.

The two obstructions at the task path are separated by cross-negatives rather than by a shared
phrase: the mid-merge message must not contain `rm -r <path>` or `prune`, and the foreign-directory
message must not contain `symlink`.

**GATE — four mutations, each naming a test.**

| mutation | failed |
|---|---|
| blank the agent wrong-ref remedy | `test_a_plain_directory_in_the_way_says_what_to_remove` **and** `..._the_prune_is_real` |
| blank the task wrong-ref remedy | `test_a_foreign_directory_at_a_task_checkout_asks_for_the_directory` |
| mid-merge remedy back to the pre-change sentence | `test_a_mid_merge_task_checkout_asks_for_the_merge_and_says_not_to_remove_it` |
| mid-merge given the foreign-directory remedy (one sentence for both) | the same test |

The mutation runner read and wrote the file as **bytes**, after iteration 6's LF→CRLF trap;
`git status` confirmed the restore.

**Known local gap, stated rather than papered over.** The two symlink tests **skip on this
machine**: `Path.symlink_to` raises `WinError 1314` without the symlink privilege — the same
limitation `_symlink_shared_dependencies` already degrades for. CI's Linux job runs them. Locally
the two symlink branches were reached with a throwaway probe forcing `Path.is_symlink`, which
printed both remedies correctly (agent: "it is a symlink, and `<agent>`'s workspace has to be the
real directory…"; task: the same naming the task); the probe is **not committed**. The two symlink
remedies are therefore the only ones here without a local mutation check.

Em dashes were removed from the two new raised sentences after the probe: every other
operator-facing message this module raises is ASCII, and these are read on a Windows console.

**Gates.** `ruff check src/ hub/ tests/` → all checks passed. `black --check --target-version
py311` → 541 unchanged. The neighbour chunk, now **fifteen** files (the new one, plus
`test_review_checkout.py` added because the review branch was the one deliberately left alone) →
**200 passed, 2 skipped, in 1:34**.

**Next:** `a-blocked-p5` — reconcile the sentences and comments that still describe the pre-change
world, including the grep 5.2 asks to be recorded even when it finds nothing.

---

## Iteration 8 — 2026-09-04 — `a-blocked-p5`: reconcile what already describes the pre-change world

Tasks 5.1–5.4, all four ticked. **Every product line changed in this iteration is a comment** —
checked mechanically, not by eye: `git diff hub/hub/ | grep -E '^[+-]' | grep -v '^[+-][+-]' |
grep -vE '^[+-]\s*(#|#:)'` returns nothing.

**5.2's grep, re-run after phase 4 changed the sentences, and now recorded in the change.**
`grep -rn 'Could not prepare isolated worktree'` over `*.py`/`*.ts`/`*.tsx`, excluding
`.agentweave/worktrees/*`: **no product line, no UI string**, and four test hits in three files.
The "nothing" is the part the task asked for explicitly, and it is now in `tasks.md` as a table so
no later round pays for the grep again.

`api/v1/inbound_queue.py` was checked **separately**, because the grep structurally cannot clear
it: `queue_status` never matches on the refusal's text. It asks its read-only questions in order
and, if none answered, passes the stored sentence through verbatim
(`reason = next((entry.waiting_reason for entry in entries if entry.waiting_reason), None)`).
Phase 4's remedy clause therefore reaches the operator's queue status *because* nothing parses it —
which is exactly what leg 6.2 will read off the screen.

**5.1.** `BAD_CHECKOUT` was F188's literal sentence, which the product stopped raising when phase 2
split the `except`. Repointed at the **task** arm — `agent_trigger` wrapping
`ensure_task_worktree`'s wrong-ref refusal, truncated after the diagnosis. The task arm is right
rather than merely valid: it is the arm that stays flagless, so it is the arm whose entry is still
in the way of everything queued behind it, which is what
`test_an_entry_specific_refusal_still_counts_and_still_gives_up` protects. Assertion untouched.

**The file 5.2 found and 5.1 does not name — decided and recorded.** `test_checkpoint_cutover.py`
built a `ScheduleResult` around the same retired sentence. Repointed too. The sentence there is
opaque payload, and I measured that rather than assuming it: replacing it with
`"zzz not a refusal sentence at all zzz"` left the file at **39 passed**. That is the argument *for*
the edit, not against — a test that quotes a sentence the product cannot raise reads as evidence
about the product and is not.

**The three truncated stubs were checked, not extended.** Phase 1's stub and both constants in
`test_a_blocked_workspace_counts_where_input_could_run.py` quote the diagnosis half only, while
their comments claimed to hold what the product raises "today" — untrue since phase 4 appended the
remedy. Each comment now says it stops after the diagnosis and points at
`test_a_blocked_workspace_refusal_states_its_remedy.py`, where each remedy is asserted against its
own obstruction. Restating the remedies in five places would give phase 4's wording five chances
to drift.

**5.3 found the docstring wrong about the code, not merely stale — filed as F276 (C).** The
`agent_wide` paragraph offered `:479` (*no such agent*) as its proof that the axes cross:
"request-level *and* agent-wide". That raise passes `request_level=True` and nothing else, and
never passed `agent_wide` — F114 (`8450831`) marked three sites and this was not one; `git log -S`
shows the sentence has been edited once since (`96b54cd`, F33) without gaining it; and
`grep -n 'agent_wide=True' hub/hub/` still returns exactly those three. **No site in the Hub raises
with both flags set**, so the example had no instance behind it.

This is CLAUDE.md's *an argument can be wrong while everything it argues about is right*, found by
a task whose whole job was to re-read the argument. The paragraph now names the task arm of the
workspace `except` — a site that exists — and records the measurement inline. The **behaviour**
question is left open in the ledger rather than fixed here: an agent that does not exist cannot run
at all, which is this flag's own definition of agent-wide, and its queued input is counted down and
withdrawn at the third attempt with `abandoned_reason` claiming three failed deliveries. That is
F114's scenario with a different repair (*create the agent* rather than *bind a runner*). Marking a
fourth site is a behaviour change with its own scenario and deserves the round discipline; it is
also arguable the other way, since `request_level=True` already tells the caller no. F276 carries
it with what was measured, unproposed. Filed under a new `# Filed outside a drive` heading at the
end of the ledger, so it is not mistaken for part of D-1's drive report.

**5.4, and why it needed its own task.** The comment closing the counting branch stated flatly that
*"a task's checkout that could not be prepared is the **task's** workspace, not the agent's, so
other input really could run and the head entry really is in the way"* — true of one arm and, after
this change, false of the other, sitting four lines under the condition that now distinguishes
them. Rewritten as two bullets, one per arm, each saying which flag it carries and why the agent
arm asks `other_input_would_have_run_elsewhere` before believing the head is in anybody's way.
Citations pulled apart: **D3a** is `2026-08-28-a-delivery-attempt-means-a-delivery`'s (the rule this
branch applies), **D8** is this change's (the predicate the helper reaches for), **D1** is the split
at the raise site. No grep for a sentence could ever have reached this comment, because it quotes
none — which is the reason 5.2 and 5.4 are separate tasks and not one.

**GATE.**

| check | result |
|---|---|
| the fifteen-file chunk **+ `test_checkpoint_cutover.py`** (5.1's decision pulled it in) | **239 passed, 2 skipped, 1:49** |
| `ruff check src/ hub/ tests/` | all checks passed |
| `black --check --target-version py311` | 541 unchanged |
| mutation: `agent_workspace_unavailable=True` on the repointed `BAD_CHECKOUT` | **fails `test_an_entry_specific_refusal_still_counts_and_still_gives_up` by name** — the constant really does feed the branch, and the new sentence is not being read by any flag inference |
| insensitivity: garbage sentence in `test_checkpoint_cutover.py`'s stub | 39 passed — the branch decides on `entry_ids`, as claimed |

The 2 skips are the symlink tests and are expected on this machine. Both mutation scripts read and
wrote **bytes**; `git diff --stat` confirmed the restores byte-for-byte.

**Next:** `a-blocked-p6-gates` — the full suites, `mypy src/`, and the phase-3 condition mutation.
The chunk name list now has sixteen files.

---

## Iteration 9 — 2026-09-04 00:55–01:35 — a-blocked task 6.1, the full gates

`a-blocked-p6-gates`. One task, and the whole firing went to it, because the hub suite is 24 minutes
and it had never been run in this change.

**The gates, all five, and two of them measured for the first time.**

| gate | result |
|---|---|
| `cd hub && py -3.11 -m pytest tests/ -q` | **3874 passed, 86 skipped, 1 xpassed, 24:27, exit 0** |
| `py -3.11 -m pytest tests/ -q` from the repo root (CLI) | 440 passed, 3 skipped, 23s |
| `py -3.11 -m ruff check src/ hub/ tests/` | all checks passed |
| `py -3.11 -m black --check --target-version py311 src/ hub/hub/ hub/tests/ tests/` | 541 unchanged |
| `py -3.11 -m mypy src/` | no issues, 22 source files |

The queue said to run `mypy src/` **early** rather than last because it was the one gate in 6.1 with
no prior measurement anywhere in this change. It was, and it is clean — so the advice cost nothing
and the whole-suite wait was never at risk of ending on a red type-check. The whole hub suite was
likewise a first: every previous iteration measured a chunk (sixteen files, 239 passed, 1:49), and
the sizing note was explicit that the chunk figure is not a guide to the whole. It is not — 1:49
against 24:27, a factor of thirteen.

Nothing failed, so the queue's contingency ("if the whole-suite run surfaces failures in files this
change never touched, check them against master") did not fire. The 86 skips and the 1 xpass are the
suite's standing baseline rather than this change's; the 2 symlink skips phase 4 recorded are inside
that 86, and were not chased.

**The mutation, re-done against the tree as it now stands.** Phase 3b did this once at that point;
6.1 asks for it again because five phases of edits have landed since. Deleting the entire third term
— `not getattr(exc, "agent_workspace_unavailable", False) or await
other_input_would_have_run_elsewhere(...)` — from the condition in `turn_scheduler.py` is a clean
12-line deletion (`git diff --stat`: `1 file changed, 12 deletions(-)`, no whitespace churn, so the
byte-level read/write did its job). Under the mutant, **exactly three tests fail, by name, and they
are the same three iteration 5 measured**:

- `test_a_second_unbound_conversation_does_not_make_the_head_expendable` (3.4a)
- `test_an_entry_in_the_refused_batch_naming_a_vanished_task_does_not_count` (3.6)
- `test_a_blocked_agent_workspace_holds_the_operators_message` (1.1) — failing on
  `('withdrawn', 3) != ('queued', 0)`, which is **F188's exact symptom returning** the instant the
  term is removed. That is the sharpest evidence in this change that the fix is the thing doing the
  work, not a test agreeing with itself.

16 of the 19 in those two files kept passing under the mutant, and that is worth stating rather than
hiding: those are pinned by the *helper*, not by the condition, and phase 3c mutation-checked them
against eight separate mutations of their own. A mutation that killed all 19 would have meant the
file was less discriminating, not more.

**One process note, recorded because it cost a call.** The Bash tool's working directory persists
between calls, so the `cd hub` from the pytest invocation was still in effect when the restore script
ran, and it died with `FileNotFoundError: hub\hub\turn_scheduler.py` — the mutant left in place for
one call. Restored from the byte backup with an absolute path; `git diff` is **empty**, not merely
"looks right", and the two files re-run at 19 passed. Any future mutate/restore in this queue should
use absolute paths on both legs.

**Next:** `a-blocked-p6-drive` — legs 6.2-6.4 against a real Hub on 8011, fresh project, an agent
bound to `claude-haiku-4-5`. That is the leg no unit test reaches, and per CLAUDE.md's own warning
(three rounds check the argument, a drive checks the product) it is where this change is most likely
to be found wrong. Budget it as the whole firing and split after 6.2 if it runs long.

---

## Iteration 10 — 2026-09-04 01:27–02:05 — a-blocked tasks 6.2-6.4, the drive

`a-blocked-p6-drive`. Three legs against a real Hub, a real git repository and two real Haiku
turns. **33/33.** CLAUDE.md's warning is the reason this leg exists — three rounds check the
argument, a drive checks the product — and this time the product agreed with the argument.

**One deviation from the queue item, made deliberately and worth more than it cost.** The item said
to start `:8011` on the **beta** profile. Beta is the database the *live* `:8010` instance serves,
measured as such on 2026-08-21 and still true tonight (23 projects on 8010, all of them beta's). A
second uvicorn writing that file would have put 8010's scheduler within reach of this drive's
project and its queue. So 8011 was restarted on a database of its own —
`profiles/drive8011/ablocked-p6.db`, created empty and migrated `0000 → 0100` at startup, visible in
the log. Nothing about the drive needed beta; the isolation removed a whole class of question.

The stale 8011 process was from **Sep 3 09:06** — sixteen hours older than every product file this
change touched — so it was killed rather than reused. That check is the one the queue item asks for
and it would have failed silently: the old process answers `/health` exactly like a current one.

**Leg 6.2 — 8/8. Four delivery opportunities, zero attempts counted.** `POST /agent/trigger` calls
`schedule_agent` itself, so the trigger *is* a delivery attempt; the harness counts it and then
presses Continue three times anyway, which is four against a limit of three. `delivery_attempts`
never left `0`, the entry stayed `queued`, `abandoned_reason` stayed null. Under F188 it would have
been destroyed on the third.

Phase 4's remedy reaches the operator on **three** surfaces, verbatim and unabridged:

| surface | carries it |
|---|---|
| `POST /agent/trigger` → `waiting_reason` | yes |
| `GET /queue/{agent}/status` → `waiting_reason` | yes |
| `POST /conversations/{id}/continue` → `waiting_reason` | yes |

That third one matters most and was not in the task's list. The Continue button is the control the
conversation view offers for exactly this situation, and it is what F114 measured an operator
clicking while trying to find out why nothing was happening. Its 200 body now answers the question
they were asking.

**Leg 6.3 — 14/14, and it is the leg that could have falsified the change.** A fix that simply held
everything would pass 6.2 and be wrong. With the same directory still obstructed, a task-bound
message was queued in a second conversation. The task was created through `POST /tasks` with nothing
special about it, and the row came back `workspace_scheme='task'` — asserted rather than assumed,
because a grandfathered one would have made this leg agree with 6.2 for the wrong reason.

That single schedule moved the head from **0 attempts to 1**: `other_input_would_have_run_elsewhere`
found input that would have run in a workspace this failure never touched, so the condition stopped
holding and F56's argument applied again. Two more Continues took it to 3, it was withdrawn, and one
further schedule started the task turn — `started: true`, completed, `workspace_dir` =
`.agentweave/tasks/task-e3df8b4ddca8`, entry `delivered`.

One behaviour worth writing down because it shaped the harness: the scheduler **returns** after
withdrawing the head; it does not immediately re-drain. So the task turn runs on the *next*
schedule, not the one that gave up. Correct, and invisible from the unit tests, which call
`schedule_agent` directly.

**Leg 6.4 — 11/11.** 6.2's own message is gone by then, because 6.3 withdrew it, which is 6.3
working. So this leg queues a fresh message under the same obstruction, confirms it is held at 0
attempts with the remedy attached, and then does exactly what the sentence told the operator to do:
`rm -r` the directory, `git worktree prune`. One `Continue` after that, and the turn ran **in the
very path that had been obstructed** — `git worktree list` shows it registered on
`agentweave/blocked012902` — with the held entry `delivered`. Holding input forever is not a fix,
it is a slower way to lose it; this is the leg that says the holding ends.

**F274's check, in a browser, against the served bundle.** A correct payload behind an unreadable
screen is not a fix either. `scripts/uishot.py` at `http://127.0.0.1:8011/?project=…&agent=…&
conversation=…` (the Hub's own port serving `hub/hub/static/ui`, not a dev server) shows the refusal
rendered in full under the queued message — three lines, unelided, path and `rm -r` both legible.
`AgentTimeline.tsx:376` and `AgentOutputPanel`'s session notice are the two render sites and neither
truncates.

**One suspected finding, chased and killed — mine, not the product's.** The composer pill in that
screenshot read `Model: Sonnet 5` for an agent I had just created, which looked like the picker
falling back to the catalog default and misreporting a Haiku binding. `ModelPicker` really does fall
back that way (`provider.models.find(m => m.id === effectiveModel) ?? provider.models.find(m =>
m.default)`), so it was plausible. It was not that: my one-liner had bound that screenshot-only agent
to `runners[0]`, which is `Claude (default)` with `model: null`. The pill was telling the truth.
Recorded because the *next* person to see it will suspect the same thing, and because the catalog
does carry `claude-haiku-4-5-20251001` as `Haiku 4.5`, so the fallback is not reachable through a
binding that names a real model.

**Both real turns billed `claude-haiku-4-5-20251001`**, read out of `turn_usage` rather than inferred
from the binding. No job and no loop was created — `GET /jobs` and `GET /loops` both empty — and the
8011 process was stopped at the end, so the next firing starts its own.

Three harnesses kept, in the style of the rest of `scripts/drive/`: `setup_ablocked_p6.py`,
`t_ablocked_p6_held.py`, `t_ablocked_p6_elsewhere.py`, `t_ablocked_p6_cleared.py`. Rebuilding the
situation is the expensive part; the assertions are cheap.

**Next:** `a-blocked-archive` — task 6.5. Retire F188 in the ledger with *this* evidence (not the
green suite), fix the index paragraph that still lists it among the open severity-A findings,
`openspec validate --strict`, sync the deltas, archive.

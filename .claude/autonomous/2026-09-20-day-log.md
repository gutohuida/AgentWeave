# 2026-09-20 day window

Newest entry at the BOTTOM. Playbook: `.claude/loops/day-window.md`.

Armed 2026-09-20T08:55:02+01:00 on `autonomous/2026-09-17-daily` (parent master@a435f48), model opus.

## Iteration 1 — compose the queue

**Backlog page:** `py -3.11 scripts/backlog_page.py --check` → current, nothing moved since the last
generation. Regenerated anyway (harmless) for the ledger-warnings sidebar: 12 findings with no
severity, 2 (`F53`, `F352`) reading `open` while their status line claims a fix — neither blocks
today, noted for a future ledger sweep, not acted on here.

**Branch:** already `autonomous/2026-09-17-daily`, not `master` — stayed on it. `HEAD` is `1f0d5c5`
(this morning's arm commit), `git rev-list --count HEAD..master` is 0, tree clean, `HEAD == @{u}`.

**Merge gate, step 1 (morning fallback):** the previous close (2026-09-19 D-4/D-5, see that day's
log) never opened it — CI for its last sha was still `in_progress` when checked and the window ended
without a further retry. Checked again now: `gh run list` shows CI for the *current* `HEAD`
(`1f0d5c5`) is **also `in_progress`** (just pushed this morning). Condition 3 cannot be met yet —
**gate not run**, left for `D-7` at the close of today's window, per playbook (`d1`/wait only happens
at close, never mid-window).

**What last night built** (`.claude/autonomous/2026-09-19-night-log.md`, iterations 1-9): all three
approved partials landed and were driven — `a-refused-capability-reaches-the-operator`'s tasks
0.3/0.4/0.5/4.12 (migration `0104`, `subject_key` column, `3b7718d`), `an-unstaffed-review-names-its-holders`
group 1 (`f663898`), and `a-loop-staffs-the-agent-it-names` groups 0-4, 6, 7, 8 (`831ac16`,
`adca56b`, `36ebae7`, `a93f95d`), which also closed `F128`. §5 of the last change and the remaining
groups of the other two stayed unbuilt, as approved. No `decisions_for_user` carried from the night.

**Research delivered** at `~/.claude/routines/agentweave-research/out/research-2026-09-20.md`;
copied to `spec-queue/research/2026-09-20.md` (this iteration's commit carries it). Ranked
candidates: §1 the loop-waits-silently signal gap (`mcp_server.py` timeouts, no UI notification,
`eventSummary.ts` renders the SSE fallback string because the broadcaster never sends the question
text), §2 charters have no version history so a reviewer can't see what governed a past run, §3 the
product has never measured whether the operator arrives before a question/permission expires. None
of these override today's `DIRECTION.md` steer below.

**`spec-queue/DIRECTION.md`'s `## 2026-09-20` section (the only/newest one) overrides the drain-count
default and names the day explicitly:**
- Drain count command (unticked task + no APPROVED/REJECTED token in `APPROVALS.md`'s newest section)
  returned **0** independently — matches the section's own resolved state (both approvals now carry
  their tokens, verified by reading `APPROVALS.md` directly, not just the section's claim).
- **Item 1 (spec loop):** `F185` (B, archived-agent charter deletion 409) + `F181` (C,
  `launchability` ignoring lifecycle) as one change. Remedy already decided
  (`DECISIONS.md` 2026-09-19), so R1's job is the directory and the rounds, not the argument. Blast
  radius `hub/hub/api/v1/charters.py`, `hub/hub/agent_lifecycle.py`, `hub/hub/api/v1/agents.py`.
- **Item 2 (second spec loop, confirmed live):** `F388` (A, `hub/hub/config.py`'s implicit-default
  live-DB hazard — the incident that took `:8000` down for ~3h on 2026-09-19). Fix already decided:
  (a) refuse the implicit default + (b) log the resolved path + (d) fix the false-safety docstring.
  Blast radius `hub/hub/config.py` + its tests + the `CLAUDE.md`/`hubs.md` prose. Shares no file with
  item 1. Verified the gating token is present: `APPROVALS.md` `## 2026-09-19` carries
  `APPROVED a-loop-staffs-the-agent-it-names groups 0, 1, 2, 3, 4, 6, 7 and 8`, so both loops run per
  the section's own resolution note.
- **F386/F387 carve-out** (question-card `blocking`/`declined` copy fix + `list_questions` sort):
  explicitly conditional on *both* spec loops reaching R3 first — "the first thing to cut, not the
  first thing to do." Queued last, before D-5, and dropped if there's no room.
- Explicitly not to spend the day on: re-rating F130/F132 (already rated), re-deriving F388's cause
  (already measured).
- If a slot is left: verify `F166`/`F168`'s status (visible on the backlog page for the first time
  2026-09-19, `F168` has no `**Status:**` line at all).

**Queue composed** for `stop_at` 17:00 local, ids chosen against `usage-policy.json`'s routing
regexes rather than the playbook's own D-N labels, so each firing lands on the right model: `d1-drive`
(Sonnet/medium, scoped drive of what last night built), `f185-r1`/`f185-r2`/`f185-r3` (Opus/high,
item 1), `f388-r1`/`f388-r2`/`f388-r3` (Opus/high, item 2), `f386-387-impl` (Sonnet/high, conditional
carve-out), `d5` (Sonnet/medium, review page — bare `d[0-9]` so it doesn't fall into the `-review$`
Opus rule), `d7-gate` (Sonnet/medium, last). `current` set to `d1-drive`, `iteration` to 1.

## Iteration 2 — d1-drive: scoped drive of last night's build

Delegated to a subagent following the `e2e-loop` skill (scoped mode, scope already fixed by
`next_action` so no operator interview was needed). Started a fresh Hub from source on port `8020`
against a new `profiles/drive0920/agentweave.db` — never `8000`/`8010`, never `proj-5e960453`/
`proj-18e5d4e0`. Confirmed `alembic_version` `0104` on startup and `/health` clean before driving.
Every real agent turn bound `claude-haiku-4-5-20251001`.

Read all three changes' `tasks.md` first: `a-refused-capability-reaches-the-operator` has **only**
0.3/0.4/0.5/4.12 ticked — the refusal-record/reaches-operator behavior itself (§1/§2/§3/§5) is
unbuilt, so only the migration/schema/`subject_key` kwarg were drivable. Drove: (1) migration 0104's
schema directly (`subject_key` column + partial unique index, matches design D15 exactly); (2) two
NULL-keyed questions coexisting live via `ask_question_for_actor` with `subject_key` omitted — held;
(3) `a-loop-staffs-the-agent-it-names` F128 shape re-run on independent infrastructure (reused last
night's `d7_0920_alsn_drive.py`): documentless loop pinned to a busy agent with free siblings
refuses 409 with the correct sentence and no side effects, an identical spec-linked flow proceeds —
13/13 verdicts held, matching last night's count; (4) `an-unstaffed-review-names-its-holders` group
1 re-checked at the code level (`_agents_that_are_free` now has exactly one caller) rather than
re-driven live, since it was already driven live 2026-09-19 and budget went to the two changes that
had not yet been driven independently.

**No new findings — everything held.** Appended a narrative entry to `scripts/drive/FINDINGS.md`
(no new `F` number, matching the file's convention for a clean re-derivation) rather than filing
one. Confirmed `a-refused-capability-reaches-the-operator`'s namesake behavior — a refused
capability actually reaching the operator — has no code to drive yet.

**Cleanup verified:** both fixture jobs stopped and archived, tasks rejected, three test projects
deleted (one 409 on the busy one, waited and retried), fixture directories removed, the `:8020`
uvicorn process killed by its confirmed PID. No job left enabled, no process left running.

Staged `scripts/drive/FINDINGS.md`. `current` → `f185-r1`, `next_action` → item 1's R1 (explore +
propose F185+F181 as one change). `iteration` → 2.

## Iteration 3 — f185-r1: item 1's R1, explored and proposed

**Change directory:** `openspec/changes/an-archived-agent-holds-nothing-and-is-offered-nowhere`
(`openspec new change`, name does not start with a digit). `openspec validate --strict` passes.
`proposal.md`, `design.md` (D1-D8), `tasks.md` (30 tasks in 6 groups), `test-guide.md` (A1-A10 +
5 human-only items), and three delta specs: `agent-configuration` MODIFIED, `runner-registry`
MODIFIED + ADDED, `agent-charter` ADDED.

**R1 drove the findings before writing the proposal** rather than trusting them. A throwaway test
against the suite's ASGI app (written, run, deleted — no test file was added, tree is clean)
reproduced both on `1fdfc4d`, and the verbatim output is pasted into `design.md`'s opening section:

- **F185 reproduces** — `DELETE /charters/{id}` → `409 "Charter is bound to agent(s): aq2. Unbind
  before deleting."` with `aq2` absent from `GET /agents` in the same test.
- **F181 reproduces** — the archived agent comes back `runnable: true, collaboration_ready: true`,
  and `POST /agent/trigger` refuses the same agent `409 "is archived and cannot be triggered"` one
  call later.

**Three things R1 found that neither finding said, all measured, not reasoned:**

1. **`PATCH /agents/{name}` re-binds a charter to an *archived* agent and returns 200** — no
   lifecycle check anywhere in that route (`agents.py:2472-2478`). The decided remedy (clear at
   archive) is therefore undoable by one ordinary API call, which is also the call the settings
   page's `CharterPicker` makes. The change now refuses it (`design.md` D7, tasks 1.6-1.8). Without
   this the fix would have passed its tests and not held — this repo's named failure mode.
2. **A data migration is required or the fix cannot fire where the finding was met.** `archive()`
   only helps future archivals; agents already archived keep `charter_id`, including on the
   operator's live `:8000` database. Migration `0105`, with the head assertions in
   `test_migrations.py` and `test_project_persistence.py` bumped. **It rewrites the operator's real
   rows on their next `:8000` restart and the downgrade cannot undo it** — flagged in `tasks.md`'s
   group 3 banner and in the test guide's human items, and it belongs in D-5's review page.
3. **The same wall stands at a third site**: `runners.py:175-183` is `charters.py:95-98` with one
   word changed. **Filed as F390 (C)** in `scripts/drive/FINDINGS.md`, found by reading rather than
   by a drive, and labelled as not reproduced through HTTP. It is tasks group 5 and is the declared
   cut line if the change has to be narrowed to `DIRECTION.md`'s three files.

**Where R1 did not follow the decision literally, and says so.** The decision says "bindings",
plural; `runner_id` is deliberately **not** released, because `list_agents` renders an archived
agent's runner and model from it (`agents.py:518`, `:568-569`) on the one screen that still shows
that agent. `design.md` D3 states the distinction (a charter governs future turns; a runner records
what it ran with) explicitly so R2 can reject it — if R2 does, the delta is one line in `archive()`.

**F181's severity C is confirmed rather than assumed:** `useAgentLaunchability` is defined at
`hub/ui/src/api/agents.ts:376-385` and has **zero** call sites in `hub/ui/src` (measured today). So
this change touches no UI, needs no `make ui`, and nothing in it reaches `:8000`'s app — except the
migration, which reaches its database.

**Ledger updated:** F185 and F181 now carry `**Status:** open, specced 2026-09-20 …` naming the
directory and stating plainly that R2/R3 have not run, there is no operator token, and nothing is
built. F390 filed. No task in the change is ticked — R1 wrote the plan, and a plan existing closes
nothing.

**One decision for the operator** (`decisions_for_user`, and D-5's review page): the new refusal
leaves the archived agent's `CharterPicker` enabled, failing with the client's generic *"Could not
update charter binding."* rather than the server's sentence — the F187 shape. Fixing that properly
is a `hub/ui/src` change, so a bundle refresh, so it reaches their live app. The change is complete
and shippable without it; folding it in is theirs to choose.

`current` → `f185-r2`, `next_action` → item 1's R2, `iteration` → 3.

## Iteration 4 — f185-r2: item 1's R2, an independent re-derivation

Read the code **before** re-reading R1's proposal, so this was a fresh comparison rather than a
re-check of R1's reasoning: `charters.py` whole, `agent_lifecycle.py` whole, `agents.py`
(`get_agents_launchability` :161-243, `list_agents`' filter :350-369 and its render :500-580,
`patch_agent` :2400-2500, archive/unarchive :2601-2660), `runners.py:150-187`, `db/models.py`
(`Run` :1105-1229 and `TurnUsage` :1235-1248), `usage_accounting.py`, `session_sync.py`, and the
`hub/ui/src` side of both routes. `openspec validate --strict` passes.

**R1's findings all reproduce and most of its conclusions survive. Four claims did not.**

1. **C1 — D3's justification was false; its conclusion survives on a different reason.** R1 wrote
   that the charter/runner asymmetry rests on a difference in kind: *"a charter is instruction for
   turns the agent will take; a runner is the record of what this agent ran with."* The second half
   is not true of this code. `TurnUsage` (`db/models.py:1235-1248`, *"The immutable accounting
   outcome for one Hub-owned run"*) carries `runner` and `model`, is written for every run by
   `usage_accounting.record_turn_usage` (called from six sites in `agent_trigger.py` plus
   `run_reconciliation.py:80`), and archival does not touch it. `Run` itself carries **no** runner
   or model column — checked, because R1's sentence implied one. Structurally the two bindings are
   identical: adjacent same-shaped branches in `patch_agent` (`:2461-2470`, `:2472-2478`), adjacent
   fields on `AgentSummary` (`:568`, `:569`), each walling a `DELETE` with the same sentence one
   word apart. What actually separates them is a **display dependency**: `list_agents` derives the
   runner and display model from the live binding and from nothing else (`:517-524`), never from
   `TurnUsage`, so clearing `runner_id` drops an archived agent's row to the `"native"`/`"Native"`
   fall-through that `:512-515` exists to prevent. D3 rewritten to that reason. **The same false
   sentence was in the normative spec text** (`agent-configuration`'s runner paragraph) and was
   fixed there too — an argument being wrong while its conclusion is right is exactly what a round
   is for.
2. **C2 — D2(c)'s unarchive sentence had two defects, found against this document's own D8.** R1's
   sentence ended *"so bind one before this agent's next turn"*, an instruction implying a charter
   is **required** — D8 records the measured opposite (an agent with no charter is fully usable and
   says so). And it never said the release is **permanent**, which is literally the one thing the
   operator's rider (*"unarchive must then say the bindings are gone"*) asked for. Replaced with:
   *"No charter is bound. Archiving releases an agent's charter and unarchiving does not restore
   it. An agent with no charter still runs — bind one only if this agent should have one."* Every
   clause is true of an agent that never had a charter. Changed in D2, task 2.2 (with 2.3 now
   asserting the old phrase is absent), the spec's requirement and its scenarios, and the
   test-guide.
3. **C3 — the whole of group 2 reaches no operator (new D9).** `useArchiveAgent`
   (`hub/ui/src/api/agents.ts:214-241`) is the only client of **both** routes, its one non-test
   caller is `AgentSettingsPage.tsx:224`, and its `onSuccess` **takes no argument** — the response
   body is discarded, not merely under-typed. So `released_charter_id`, the archive sentence and
   the unarchive sentence are correct API facts nothing renders, and group 2's tests would pass
   while the operator saw nothing: this repo's named dominant failure mode. Group 2 is **kept** (an
   API that states the fate of a binding it silently changed is right regardless of today's
   client), but the proposal's *"the product says so rather than letting the operator find out at
   the next turn"* was struck, group 2 was retitled from *"The operator is told…"*, test-guide
   items 1/2 and A4/A10 were corrected, and task 2.5 was added recording that the operator's rider
   is **half discharged** — the API says it, the app does not show it.
4. **C4 — D6's second justification was false.** R1 wrote that *"the agent's own settings page is
   entitled to probe an agent it can display."* It does not probe this route and nothing else does:
   `useAgentLaunchability` has zero call sites, `hub/tests/test_launchability.py` is the only caller
   in the tree, and a 2026-09-01 drive measured 41 requests with the agent rail open, none to
   `/agents/launchability` (F178). The `lifecycle` parameter is kept — for roster symmetry, so
   F178's future renderer copies the call shape rather than inventing one — but D6 now says plainly
   that **F181's fix here is preventive**, not user-visible. D6's source count was checked and is
   right (two, against the roster's four) and the hazard is live, not vestigial: `ProjectSession` is
   still written (`session_sync.py:70` ← `src/agentweave/session.py:153`), so a name can reach the
   dict from session config while its row is archived.

**C5 — the cut line is not free.** Group 5 (F390/`runners.py`) is not an unrelated third site: it
exists *because* D3 keeps `runner_id` bound. Had archival released it, no archived agent could hold
a runner and that refusal could not name one. Recorded in D4, at `tasks.md`'s cut-line banner, and
on F390's own status line — cutting group 5 ships a change that knowingly leaves its own
consequence open. **C6:** D1's grep count was off by one (four references, not three).

**What R2 confirmed rather than changed:** D1's single-caller claim (`archive_agent_row` called
only at `agents.py:2641`); the `PRAGMA foreign_keys` claim (`models.py:2352`,
`project_lifecycle.py:334`, `0073`'s note); D7's hole (`patch_agent` has no lifecycle check
anywhere); D8's trigger refusals (`agent_trigger.py:661`, `:1370`); D5's necessity; F181's severity
C. `released_charter_id` **is** dead payload today — but so is the sentence beside it, which is the
more important half, so the answer is D9 rather than deleting the field.

**The operator decision was widened, not duplicated.** Rendering archive/unarchive is the *same*
wall as D7's `CharterPicker` — a `hub/ui/src` change, a bundle refresh, the live `:8000` app — so
`decisions_for_user` now frames it as one question ("does this change accept a bundle refresh at
all?") covering two halves, instead of raising a second decision.

No task's intent changed; one task (2.5) and one design section (D9) were added. `current` →
`f185-r3`, `iteration` → 4.

## Iteration 5 — f185-r3: item 1's R3, a second independent re-derivation

**Position verified before starting.** Branch `autonomous/2026-09-17-daily`, HEAD `00a3878`
("iteration 4 — f185-r2"), tree clean — all three match `STATE-day.json`. Nothing to reconcile.

**Method.** The code first, the document second. `agent_lifecycle.py`, `agents.py` (`patch_agent`,
`get_agents_launchability`, `list_agents`, both archive routes, **and `create_agent`**),
`charters.py`, `runners.py`, `usage_accounting.py`, `run_reconciliation.py`, `db/models.py`, plus
`hub/ui/src/api/agents.ts` and `AgentSettingsPage.tsx` — all read before `design.md`'s Round 2
section was opened. R2's own corrections (C1–C6, D9) were the target, since no one had read them.

**Outcome: every conclusion of R1 and R2 survives. No task's intent changed, no task added.** Three
claims narrowed or sharpened, one defect neither round found, now filed *and driven*.

1. **R3-C1 — R2's C1 premise holds, two of its sentences do not.** Enumerated every terminal
   `Run.status` write in `hub/hub/` — `agent_trigger.py:1915`, `:2061`, `:2313`, `:2887`, `:2960`,
   `run_reconciliation.py:66`, six — and found a `record_turn_usage` beside each. (`scheduler.py`'s
   writes and `run_reconciliation.py:252` are `JobRun`.) So **a run cannot end without a
   `TurnUsage` row**: C1 stands. But *"records `runner` and `model` for every run"* is too strong —
   `model` comes only from `sample.model` and `sample=None` on all four failure paths and on
   reconciliation, so **every unmeasured outcome carries `model = NULL`**
   (`usage_accounting.py:44`); `runner` is `Optional` by design (`:21-24`). Narrowed in D3, in the
   proposal, and in the **normative** `agent-configuration` text, whose sentence *"what each run
   actually ran with is recorded separately"* was overclaiming in the same way.
2. **R3-C2 — D3 confirmed a third time, now demonstrated rather than asserted, and its cost
   restated.** R1 and R2 both argued the display fall-through from `list_agents` and its comment;
   neither checked what an archived agent's `config` holds, and the fall-through only fires if
   `agent_meta` has no `runner` key. It does not — `create_agent` writes **`config={}`**
   (`agents.py:673`). And the cost is worse than "blanked": the row goes **false**, not empty.
   `runner: "native"` / `"Native"` is a positive claim naming a runner kind the product supports, so
   an operator would be told an archived agent ran natively when it ran on Claude. Keeping
   `runner_id` is right; D3 had been pricing it as lost information.
3. **R3-C3 — new, neither round found it (F391 (C)): archiving an agent persists no event and
   broadcasts nothing.** `grep -rn "agent_archived\|agent_unarchived" hub/ src/` → no match; both
   routes (`agents.py:2602-2644`, `:2648-2660`) mutate, commit, refresh, return, while every
   neighbour has one (`agent_created` `:689`, `agent_requested` `:2133`, `agent_heartbeat` `:2682`).
   **Driven, not just read** — a throwaway test (written, run, deleted) with a positive control:
   `GET /events/history` is `[]` before archive, after archive and after unarchive, and
   `['agent_heartbeat']` immediately after a heartbeat on the same agent. Consequence for this
   change: `released_charter_id` is not merely *unrendered* (D9) but **unrecorded** — the response
   is the only place it ever exists and its only client discards the body, so the fact dies with the
   request. New **D10** says the cheap remedy is server-side (`persist_event`, no bundle refresh) —
   which is the only route to the operator's rider that avoids the `:8000` app — and that D2's
   rejection of *remembering* is right about a column and does not reach an event. **No task added:**
   the defect is true today with no charter release at all, so it is archival's, not this change's.
4. **R3-C4 — C3/D9 confirmed independently; C4 reaches further than R2 said.** `onSuccess`
   (`agents.ts:230`) takes no argument, the `mutationFn` types the body `{ name, lifecycle }` so
   `released_charter_id` is not even in the type, and the only non-test caller `ArchiveControl`
   (`AgentSettingsPage.tsx:224-263`) reads `.error`/`.isPending` and **never `.data`**. Group 2
   stays user-invisible and **the change still acquires no bundle refresh.** Extension: R2 measured
   zero call sites for `useAgentLaunchability`; **`AgentCard` itself is rendered nowhere outside its
   own test**, so the prop has no production consumer either — D6's "preventive" label strengthened.
5. **R3-C5 — `patch_agent` re-binds `runner_id` to an archived agent too**, same route, adjacent
   branch (`:2462-2470`), same missing check, and tasks 1.6–1.8 were silent on it. Under D3 it
   **stays permitted**; unsaid it reads as an oversight and the next reader "finishes the job". One
   sentence added to task 1.6 to make it a decision. **R3-C6:** D3's `:2461-2470` was off by one.

**An objection raised against D3 and then closed by R3 itself, recorded so R4 does not re-raise it.**
`TurnUsage.runner` on the reconciliation path is recovered from the **live** `Agent.runner_id`
(`_runner_cli_for_agent`, `run_reconciliation.py:33-48`), so a cleared `runner_id` would record
`runner = NULL` for a crashed run. Unreachable: `archivable` refuses while a run is `"running"`
(`agent_lifecycle.py:34-44`), a crashed run stays `"running"` until reconciliation, and
`reconcile_interrupted_runs` runs inside `lifespan()` **before the Hub serves a request**
(`main.py:415`). This *removes* an objection to clearing rather than adding one — D3 rests on the
display argument alone, where R2 left it.

**Verification.** Every line citation R3 added was opened and checked; four were wrong and were
corrected in place (`usage_accounting.py:42`→`:44`, `:22-25`→`:21-24`, `agents.py:2602-2645`→`:2644`,
`:2648-2661`→`:2660`, `agent_lifecycle.py:33-44`→`:34-44`). F391 driven through the API with a
control. `openspec validate --strict` passes. No code changed this iteration — the throwaway test
was deleted and the tree carries only the five documents.

**Item 1 has now had all three rounds.** It is unbuilt and untokened, and its one operator decision
(does it accept a `hub/ui/src` bundle refresh?) is unchanged by R3 — but D10 gives the review page a
third option neither earlier round had: persist the event, which discharges the rider's second half
server-side without touching the live app.

Next: `f388-r1` — item 2's R1, explore and propose the F388 fix.

## Iteration 6 — f388-r1: item 2's R1, explore and propose the F388 fix

**Position reconciled before starting, because it did not match.** `STATE-day.json` claimed HEAD
`14f8dae` and a clean tree; the branch was right but HEAD was `aa9983f` and **five files were
uncommitted**. They are the operator's: an interactive DECIDE session on 2026-09-20 decided item 1's
one open question (`design.md` **D10** — API plus a persisted archival event, **no** bundle refresh),
added **group 2b** to `tasks.md`, amended the proposal and the `agent-configuration` delta to match,
wrote `APPROVALS.md`'s `## 2026-09-20` section (**APPROVED**, all groups including 2b and group 5,
`ORDER:` set for tonight), and regenerated `BACKLOG.html` at 15:20. That session **hit the weekly
usage limit at 14:15** and could not commit. Committed as `51b17bd` after re-running
`openspec validate an-archived-agent-holds-nothing-and-is-offered-nowhere --strict` (valid). No work
of mine is in that commit. *Item 1 is therefore approved and queued for tonight; the day window's
`d5` review page is no longer the authority for it, and `APPROVALS.md` says so itself.*

**Method for R1.** The code first. `hub/hub/config.py`, `hub/hub/db/engine.py`, `hub/hub/main.py`,
`hub/hub/migrations/env.py`, `hub/hub/alembic.ini`, `hub/hub/bound_address.py`,
`hub/tests/test_config.py`, `hub/tests/conftest.py`, `src/agentweave/cli.py`,
`scripts/refresh_ui_bundle.py`, `hub/docker-compose.yml`, `hub/.env.example` — then
`openspec/specs/app-lifecycle/spec.md`. F388's *cause* was not re-derived (DIRECTION item 2 forbids
it) and no Hub was started against `:8000` or its database. Four throwaway probe scripts under
`testbed/scratch/f388/`, now deleted; what they returned is transcribed into `design.md` so R2/R3 can
re-derive without re-running them.

**Change written:** `openspec/changes/a-hub-that-was-not-told-which-database-refuses-to-open-one` —
`proposal.md`, `design.md` (D1–D11), `tasks.md` (43 tasks in 6 groups), `test-guide.md`, and one
`MODIFIED` delta on `app-lifecycle`. `openspec validate --strict` passes. **No code changed.**

**The return-value table the queue asked for** (`design.md` D11, all measured):

| `DATABASE_URL` | returns | what the caller does |
|---|---|---|
| absent, no `.env` in cwd | the home default | opens the operator's live database and migrates it |
| absent, cwd = `hub/` | `sqlite+aiosqlite:///data/agentweave.db` — **relative**, from `hub/.env:5` | resolves against the process cwd |
| `""` or `"not a url at all"` | the string, unvalidated | `ArgumentError` at `engine.py:36`, i.e. **import**: the loud case |
| valid URL, file missing | unchanged | SQLite **creates** it, full migration chain, looks like success |
| valid URL, directory missing | unchanged | `init_db` runs `os.makedirs(..., exist_ok=True)` (`engine.py:346-350`) and creates it |

So **there is no path the Hub refuses**, which is why (a) has to act on absence of instruction rather
than badness of path.

**Six R1 discoveries, all measured:**

1. **F388 is spec'd behaviour.** `openspec/specs/app-lifecycle/spec.md:46-53` — scenario *"The Hub's
   own database is launch-directory-independent"* — **normatively requires** a direct
   `uvicorn hub.main:app` *with no `DATABASE_URL` set* to resolve to the home path. (a) reverses a
   current requirement, so the change carries a `MODIFIED` delta, not only a code edit. A round that
   had treated (a) as a refactor would have shipped a change contradicting the corpus.
2. **The one line in the tree that already names the database has never been seen by anyone.**
   `engine.py:228` is `logger.info("Alembic migrations applied to %s", settings.database_url)`.
   Measured: root has **no handler** before `init_db` (`logging.lastResort`, WARNING-only) and root
   level is **`WARNING`** after it, because `migrations/env.py:28`'s `fileConfig` applies
   `alembic.ini:20-22`'s `[logger_root] level = WARN`. `getLogger("hub.db.engine").isEnabledFor(INFO)`
   is **`False`** in both states. **(b) written as `logger.info` would be invisible** — F151 again,
   and this repo's named dominant failure mode. D5 makes (b) a `WARNING`, and task 2.7 adds the test
   that fails if the Hub ever gains its own logging config, so the comment cannot rot into a lie.
3. **`.env` is a third source and it is cwd-relative.** `SettingsConfigDict(env_file=".env")` takes a
   *relative* name; `hub/.env` here sets `DATABASE_URL=sqlite+aiosqlite:///data/agentweave.db` —
   itself relative, copied from `hub/.env.example:5`, which still ships the pre-`D1` bug that
   `2026-08-16-one-hub-and-a-window-of-its-own` removed from `config.py`. D3 rules a `.env` counts as
   being told (else Docker mode is refused) and task 4.7 makes the example absolute.
4. **The blast radius includes `make ui`.** Probed by making the field required for real:
   `scripts/refresh_ui_bundle.py:110` imports `hub.main` with no `DATABASE_URL`, so `make ui` and
   `make ui-check` die on a raw `pydantic_core.ValidationError`. Group 3 fixes it and says so:
   *do groups 1 and 3 in one commit or neither*. **Unaffected, measured:** `agentweave --help`,
   `agentweave doctor` (neither imports `hub`), Docker (`docker-compose.yml:34` sets it), native
   `agentweave` (`cli.py:1028` sets it), and `hub/tests/conftest.py` (assigns it before importing).
5. **A required field alone gives a message that helps nobody** — measured verbatim,
   `1 validation error for Settings / database_url / Field required [type=missing, input_value={}]`,
   naming neither the database nor the fix. D2 chooses a **raising `default_factory`**, measured to
   propagate unwrapped and to not run when any source supplied a value.
6. **One sentence of `DEAD-ENDS.md`'s F388 practice is false.** *"`sqlite3`/`aiosqlite` will not
   create a missing parent directory…"* — true of raw aiosqlite (measured `OperationalError`),
   **false of the Hub** (`engine.py:346-350`). The practice survives *stronger*: since the Hub would
   have created the directory, a profile directory missing afterwards proves the URL never reached
   the process. Task 4.2 rewrites it rather than dropping it.

**The third loss was held, not quietly dropped** (DIRECTION item 2's explicit rider). `design.md` D9
says what (a) and (b) each reach — (a) removes the route that produced the incident; (b) makes an
attachment readable **before the first write** and carries `os.getpid()` so the recovery is killing
that PID, never `taskkill /IM python.exe /T` (which does not match `pythonw.exe` anyway) — and states
plainly that **an already-attached Hub still has no in-product detach**, which is `main.py`'s shutdown
surface and outside the operator's stated blast radius. The proposal has a *"What this change does
NOT do"* section saying the same thing, so the review page does not have to find it.

**One decision inside (a) taken and recorded rather than escalated** (D4): refusing the default
*path* even when named explicitly — which would cover the typo route and needs no migration, so it
escapes what killed option (c) — is **rejected**, because `cli.py:1028` sets `DATABASE_URL` to
exactly that path for the default profile, and telling "the CLI set it" from "a human typed it" needs
a marker the CLI must send. `agentweave-ai` and `agentweave-hub` are independently installable with
no dependency edge (`TestDatabaseUrlDriftAgainstCli` exists for that seam), so an older installed CLI
would stop starting the operator's app after a Hub upgrade. Trading an A that fires on agents for an
A that fires on the operator is not an improvement. Recorded so R2/R3 do not re-raise it — and
flagged as the weakest argument in the document, because it is *reasoned*, not measured.

**Also decided inside (b):** the line must **not** print a port. `settings.aw_port` is configured
intent (only `main.py:540` honours it; a uvicorn `--port` never reaches `settings`), and
`bound_address.py` — the module that knows the real port — is **empty during `lifespan()`**,
populated by request middleware at `main.py:466-469`. A port there would be wrong in exactly the
drive scenario F388 came from.

**Verification.** Every line citation was opened; five were wrong and corrected in place
(`engine.py:345-350`→`:346-350`, `config.py:44`→`:45`, `migrations/env.py:14-26`→`:14-28`, the
`engine.py` reference count *four*→seven sites, and the proposal's own "(b) prints the port"
sentence, which contradicted D6 two pages later). The probe patch to `config.py` was reverted and
`pytest hub/tests/test_config.py -q` is back to **4 passed**; `git diff hub/hub/config.py` is empty.
Throwaway scripts deleted. F388's status line now reads *specced (R1 only), no token, nothing built.*

Next: `f388-r2` — an independent re-derivation of this proposal against the code. D9, D5's
measurement (take it from a **running** Hub, not a hand-called `fileConfig`), D3 and D4 are the four
places it should go first; `design.md` ends with that list.

**Concurrency note, added after the iteration-6 commit.** An interactive operator session was
live on this branch at the same time. It committed `09237f6` ("APPROVALS: approve …, D10 folded
in") at 15:21, one minute after this window read the tree — the `DECISIONS.md` row and another
`BACKLOG.html` regeneration, on top of `51b17bd`. No conflict and no duplicated content: `51b17bd`
carried the five files that session had left uncommitted, and `09237f6` carried the two it had not
yet written. **The next iteration should re-check `git log` before trusting `STATE-day.json`, as
this one had to** — a second author is writing to this branch today.

## Iteration 7 — f388-r2: an independent re-derivation of the F388 change against the code

**Position reconciled first, and it matched this time.** Branch `autonomous/2026-09-17-daily`, tree
clean, HEAD `3d8c02e` — one commit *ahead* of iteration 6's `76bb39e`, because the operator's
concurrent session filed R6 and re-measured F292 on top of it. Nothing of mine was uncommitted.

**Method.** Code before document, as the queue required. Every file opened — `config.py`,
`db/engine.py`, `main.py`, `migrations/env.py`, `alembic.ini`, `bound_address.py`,
`hub/tests/test_config.py`, `hub/tests/conftest.py`, `src/agentweave/cli.py`,
`scripts/refresh_ui_bundle.py`, `hub/docker-compose.yml`, `hub/Dockerfile`, `hub/.env`,
`hub/.env.example`, the repository root `.env`, `openspec/specs/app-lifecycle/spec.md`, plus both
`pyproject.toml`s and every `scripts/` module that imports `hub` — **before** `proposal.md`,
`design.md`, `tasks.md` or the delta was read. **Two throwaway Hubs driven from source**, ports 8093
and 8094, fresh profile directories under `testbed/scratch/f388r2/` (since deleted), each killed by
the exact PID `netstat -ano` named. Never `:8000`, never `:8010`, never
`~/.agentweave/hub/data/`. R1's probe shape (a raising `default_factory`) was re-applied to
`config.py` and reverted; `pytest hub/tests/test_config.py` is back to **4 passed** and
`git status --short` is clean of both `config.py` and `engine.py`.

**Every R1 decision survives.** (a), (b), (d); the refusal in `config.py` rather than `main.py`; the
raising `default_factory` over a required field; `WARNING` over `INFO`; `lifespan()` as the site; the
rejection of a path-keyed guard; the `MODIFIED` delta on `app-lifecycle`. **Nothing was re-opened.**
What changed is the evidence under four of those decisions, and five claims that were wrong.

**D5 — R1 asked for this to be driven rather than re-read, and it was.** Two real
`uvicorn hub.main:app` starts against fresh databases, full `0001→0104` chain both times:

| emitted | where | appeared |
|---|---|---|
| the existing `engine.py:228` `logger.info` | after migrations | **no** — 0 in 108 lines |
| `logger.info` probe | before `init_db`'s first write | **no** |
| `logger.warning` probe | before `init_db`'s first write | **yes** |
| `logger.warning` probe | after migrations | **yes** |

All 104 `alembic.runtime.migration` `INFO` lines appeared, which is what makes the absence of the
`hub.*` ones evidence rather than an empty log. **D5 holds; (b) must be `WARNING`.** And one thing a
hand-called `fileConfig` could not show: at D6's chosen site the record goes through
`logging.lastResort`, which has **no formatter**, so it prints as the bare message —
`PROBE-PREWRITE-WARNING sqlite+aiosqlite:///... pid=28980` — while the same record after `fileConfig`
prints `WARNI [hub.db.engine] ...`. (b) cannot lean on a level prefix to announce itself. Task 2.2
now says so.

**D9 — the question answered by measurement: there is no launch that still reaches the live database
with no `DATABASE_URL`.** With the probe applied the question is safe to ask directly, because every
route that *would* have opened that file instead names it in an exception. A neutral cwd refuses,
unwrapped. From `hub/` the gitignored `hub/.env` supplies the relative container path, so (a) does
not fire and the result is `hub/data/agentweave.db` — not the live database, and `hub/data/` does not
exist on this machine at all. **The sharpest form of the route is the one R1 did not draw:
`hub/.env` is gitignored (`.gitignore:69`), so on a clean checkout it is absent and the launch
`CLAUDE.md` itself prescribes — *"Start it from `hub/`, from source"* — lands on the home default.**
(a) closes exactly that. One residue, and it is D3 working as intended: a `.env` naming the home path
would still be honoured, which is the path-keyed guard D4 rejects.

**Five claims corrected.**

1. **The blast radius is bigger than "`refresh_ui_bundle.py` only".** Measured under the probe, the
   **CLI suite** breaks: `tests/test_hub_commands.py:707` does a bare `import hub.config`, and its
   own `patch.object(settings, "database_url", ...)` cannot help because the module-level
   `Settings()` has already raised during the import. `py -3.11 -m pytest tests/ -q` gives
   `3 failed, 532 passed, 3 skipped`, and **two of the three are pre-existing** `test_skill_sync.py`
   failures — confirmed by re-running them on a clean tree — so this change costs **exactly one**.
   New task 4.9, which also tells IMPL to expect the two and not let them hide a third.
2. **A second script caller.** `scripts/drive/n10_route_reachability.py:119-121` imports `hub.main`
   in a subprocess with `cwd=REPO/"hub"` and no `DATABASE_URL`; it survives here only on the
   gitignored `hub/.env`. It wants the route table, not a database. New task 3.5.
3. **R1's task to absolutize `hub/.env.example` would have broken Docker.** That file is the
   *container's* template: `Dockerfile:11` sets `WORKDIR /app`, `docker-compose.yml:23` mounts
   `hub-data` at `/app/data`, the file's own line 4 says *"inside the container"*, and
   `docker-compose.yml:31-34` supplies the same value under `environment:` (which overrides
   `env_file:`), so line 5 is inert in the documented install anyway. It is not "the pre-`D1` bug
   still shipped" — it is a container default that is wrong only once copied into a source checkout.
   Task 4.7 rewritten: **comment it, do not absolutize it.**
4. **"No dependency edge between `agentweave-ai` and `agentweave-hub`" is false.**
   `pyproject.toml:34` is `dependencies = ["agentweave-hub>=1.1.0"]`. R1 did not invent it — the same
   sentence is in `hub/tests/test_config.py:56-58`, which R1 read; new task 1.9 fixes the source.
   **D4's conclusion holds and the true premise argues it harder:** a floor with no ceiling means
   `pip install -U agentweave-hub` yields a newer Hub with an unchanged CLI still satisfying it —
   precisely the skew that would stop the operator's app starting.
5. **Two delta scenarios were falsified by the code being right.** *"The database a launch path names
   does not depend on its working directory"* required *any* shipped environment file to be absolute,
   which correct Docker configuration falsifies (see 3). And *"A told database is named before it is
   opened"* required the statement to precede creating or migrating for every told launch — but
   native `agentweave` migrates at `src/agentweave/cli.py:1054` **before** spawning uvicorn, and
   Docker runs `alembic ... upgrade head && uvicorn ...` as one `CMD` (`Dockerfile:31`), so both
   write before `lifespan()` ever runs. (b) is genuinely before-first-write only for a direct
   `uvicorn hub.main:app` — which is the launch F388 happened on and the only one an unattended
   window uses, so the change still does its job; the *claim* was over-broad. Both scenarios narrowed,
   a third added for the container case, a fourth for the other launch paths. D6 now states the limit.

**One thing left for the operator, not decided here.** The proposal's *"Why"* said the 2026-09-19
start was "from `hub/`". Measured, from `hub/` with `hub/.env` present — and it is dated 2026-09-09,
so it was — an absent `DATABASE_URL` resolves to `hub/data/agentweave.db`, and that directory does
not exist. DIRECTION item 2 forbids re-deriving F388's cause, so R2 softened the sentence to what the
finding supports and flagged the discrepancy for R3 rather than chasing it.

**Verification.** `openspec validate a-hub-that-was-not-told-which-database-refuses-to-open-one
--strict` — valid, run after each edit. `pytest hub/tests/test_config.py -q` — 4 passed, probe fully
reverted. `git diff --stat`: six files, all documents; **no code, no migration, nothing under
`hub/ui/src`.** Both drive ports confirmed clear, both profile directories deleted,
`testbed/scratch/f388r2/` removed.

**Also observed, not filed:** `tests/test_skill_sync.py` has two failures on a clean tree
(`Kimi + OpenCode (project)`, `Codex (user-level)` — `autonomous-session/scripts/*.ps1` present in
the source and absent from the mirrors, plus `backlog` absent from Codex). Unrelated to F388 and to
F292's `database is locked`; it is a third reason `pytest tests/` is red and therefore bears on the
merge gate. Left for the backlog rather than expanded here.

Next: `f388-r3` — a second independent re-derivation. `design.md` ends with the four places R2 says
to start: the narrowed delta, group 3's completeness (apply the probe and *run* the entry points —
neither round has), task 2.2 against D5's new bare-line constraint, and the proposal's "Why".

---

## Iteration 8 — `f388-r3`: the third round, and the delta contradicted its own tasks

**16:36 local.** R3 of `a-hub-that-was-not-told-which-database-refuses-to-open-one`. Code first, the
document second, and this time the probe was **run** rather than reasoned from.

**Every decision survives a third time.** (a), (b), (d), the `config.py` site, the raising
`default_factory`, the `WARNING` level, the `lifespan()` site, D4's rejection of a path-keyed guard,
the `MODIFIED` delta. **No decision was reopened, and no task was added outside group 3** except the
one verification gap below.

**1. The narrowing is right, not convenient — with a better reason than R2 had.** Re-derived from
`hub/Dockerfile:31`, `hub/docker-compose.yml:23,34` and `src/agentweave/cli.py` *before* reading R2's
version: the container's relative path is correct (`WORKDIR /app` + `hub-data:/app/data` + an
`environment:` key overriding `env_file:`), and native `agentweave` (`cli.py:1054`) and Docker both
migrate before `lifespan()` runs. Then the part R2 missed: `_hub_resolve_database_source`
(`cli.py:599-616`) returns `message=None` for the **default** profile, so a plain `agentweave` start
names its database **nowhere** before migrations open it — only a named `--profile` prints a path,
and it prints it at `:1026`, before the migration. R1's broad scenario was therefore not merely
unproven on the native path but **unsatisfiable** there without changing the CLI.

**2. One real defect — the delta contradicted its own task list.** R2's scenario *"A container's own
relative database path is not a host path"* covered *"an environment file **or compose file**"* and
required *the file* to state its value is a container path. `docker-compose.yml:34` carries no such
statement and **task 4.8 explicitly forbids changing it**. The change shipped a normative clause its
own tasks refused to satisfy. The clause is now scoped to a file that is *a template intended to be
copied* — `hub/.env.example` (task 4.7), not the compose file. R2 predicted its narrowing was its
weakest work; it was, but for the opposite reason to the one expected: it **widened** one clause
while narrowing two others.

**3. Group 3 is measured now, and complete.** The raising `default_factory` applied **with `hub/.env`
moved aside**, so a *clean checkout* was what was measured rather than this machine.

- **Breaks, by running:** `refresh_ui_bundle.py` (`make ui`/`ui-check`) and
  `n10_route_reachability.py`. CLI cost is **exactly one test**, name now captured —
  `test_first_start_migrations_leave_a_database_that_can_hold_a_conversation`
  (`pytest tests/test_hub_commands.py -q` → `1 failed, 40 passed`).
- **Every `hub.*` import under `scripts/` resolved** by `importlib.import_module(m)` +
  `'hub.config' in sys.modules`. `hub.pty_runner`/`runner_commands`/`runner_parsing` are **False**, so
  the three `t_d*_0913_*.py` drive scripts are **unaffected**; `setup_d2_cutover.py` and
  `churn_sessions_plugin.py` reach it and are **correct to fail**. Task 3.2 is now a list, not a
  judgement call. (R2's list of filenames was accurate — my first grep missed `scripts/drive/` and
  the second corrected it.)
- **Both CI jobs unaffected, measured:** `ci.yml:61` and `ci.yml:131` exit 0 (`hub/hub/__init__.py`
  reads package metadata, never imports `hub.config`), and both suites collect clean —
  **collection is the wrong instrument**, since the CLI break is an `import hub.config` inside a test
  *body*. A future round must not read a green collection as a green run.
- **Nothing in CI guards either fix:** `grep -rn "refresh_ui_bundle\|ui-check\|make ui"
  .github/workflows/` returns nothing. New task 3.6.
- Measured and deliberately *not* filed as blast radius: bare `alembic -c hub/alembic.ini` from
  `hub/` with no `DATABASE_URL` raises (`migrations/env.py:10`). That is the change **working** —
  Docker and native both set the variable first, and no documented workflow runs alembic bare
  (`copilot-test-setup/SKILL.md:73` sets `$env:DATABASE_URL`). Recorded so a fourth reader does not
  re-open it.

**4. Task 2.2 holds; the gap underneath it is closed.** 2.2 is consistent with 2.1's placement and
2.4's `logger.warning`. But the delta's *"names what it is without depending on a logger-name or level
prefix"* was the change's **one normative clause with no task verifying it** — 2.6 asserted `levelno`
and text, and `caplog.text` interpolates exactly the prefix this site lacks. 2.6 now asserts against
`record.getMessage()` alone.

**5. The proposal's "Why" — a note, not a decision.** Not re-derived (DIRECTION item 2 forbids it).
The discrepancy is **not load-bearing**: (a)'s case rests on `config.py:23`, the false docstring, and
nothing downstream refusing a path — none depends on the incident's cwd. It *is* load-bearing for the
`DEAD-ENDS.md` rewrite, which is advice to a future agent; task 4.2 now guards the whole of group 4
against asserting a launch directory the finding does not establish. Whether to establish it is the
operator's call.

**Verification.** `openspec validate ... --strict` — valid, after each edit. Probe fully reverted
(`cp` of the pristine file), `hub/.env` restored and `diff`-identical to its backup,
`pytest hub/tests/test_config.py -q` → **4 passed**. `git status --short`: four documents, **no code,
no migration, nothing under `hub/ui/`**. No Hub was started this iteration; nothing touched :8000,
:8010 or `~/.agentweave/hub/data/`.

**Queue cut, per the iteration-7 clock note.** R3 landed at 16:36 against a 17:00 stop. `f386-387-impl`
is cut — F386 needs a UI bundle refresh that reaches the operator's live app and must not be rushed.
`d5` (the review page) is cut too, which the queue entry itself authorised ("CUT THIS BEFORE CUTTING
f388-r3"); the operator's own `## 2026-09-20` APPROVALS section already states it is the authority
for tonight regardless. **Item 2 is left for the operator undecided, with three rounds behind it.**

Next: `d7-gate`, the merge gate, on a clean tree.

## Iteration 9 — d7-gate: the merge gate, run and passed

**Preconditions checked in order.** (1) `git status --short` empty. (2) `git rev-list HEAD..master
--count` → 0. (3) `git rev-parse HEAD` == `git rev-parse @{u}` (`a09e4a2`). (4) newest `##
2026-09-17` section of `spec-queue/DIRECTION.md` (through the whole file, all headers checked) —
no `HOLD MERGE`.

**CI for the exact HEAD sha, polled ~90s at a time.** `a09e4a2`'s run (`35518457958`) was
`in_progress` at the first check; after ~13 minutes of polling it completed `success`.

**Re-verified immediately before merging, per the playbook's own warning that a concurrent
operator session has committed to this branch before.** It had again: `git fetch` showed HEAD had
moved to `45d4576` ("dead-ends: five entries from sharing the tree with a live window") while this
iteration was polling the old sha. Re-ran all four gate conditions against the new HEAD — clean,
`rev-list HEAD..master` 0, `HEAD == @{u}` — then found and polled CI's run for `45d4576`
(`35518709191`), which also completed `success` (~7.5 minutes).

**Merged.** All four conditions held for `45d4576` at the moment of merge. `git checkout master &&
git merge --ff-only autonomous/2026-09-17-daily && git push` — fast-forward, `c6fccc5..45d4576`,
14 files changed (the day log, `STATE-day.json`, `DEAD-ENDS.md`, both loop playbooks, the F388
change's five documents, `a-loop-staffs-the-agent-it-names`'s tasks, `FINDINGS.md`, `BACKLOG.html`,
`REQUESTS.md`). Pushed to `origin/master`. Returned to `autonomous/2026-09-17-daily`.

**Item 2 (`a-hub-that-was-not-told-which-database-refuses-to-open-one`) is not part of this merge's
decision** — the gate only asserts the branch is clean and green, not that every change on it is
approved. It stays specced (R1–R3 done, no operator token) for tomorrow, per iteration 8's own
note.

Queue finished. `current` → `d7-gate` (closed), `next_action` → `null`. Window ends here for
`stop_at` 17:00 local.

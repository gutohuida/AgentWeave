# 2026-09-12 day window

Newest entry at the BOTTOM. Playbook: `.claude/loops/day-window.md`.

Armed 2026-09-12T08:55:02+01:00 on `autonomous/2026-09-08-daily` (parent master@6f7e486), model opus.

---

## Iteration 1 — compose the queue, and the merge gate opens (09:00–09:15)

The one unit for this firing was `day-window.md`'s "Iteration 1 — compose the queue". Times below
are from PowerShell `Get-Date`, not Git Bash `date`.

### The limits, restated so a later firing inherits them even if this one dies mid-thought

Stay on the cycle branch. The merge gate in step 1 is the only thing permitted to touch `master`,
under four conditions it checks itself. Nothing outward-facing: push, do not open PRs. Nothing
destructive. Do not browse the open web. Every claim is measured or labelled unverified. Operator
decisions go to `decisions_for_user` and are not guessed. Stage explicit paths, never `git add -A`.
Use `py -3.11`, never bare `python`, and `black --target-version py311`. Never drive
`proj-5e960453` or `proj-18e5d4e0`, and never touch port 8000, which is the operator's real usage.
Every real agent turn binds Haiku, and no job is left enabled.

### Step 1 — the merge gate opened, and the 2026-09-08 cycle is on `master`

The ordering is what made it work. At 09:00, CI for `HEAD` (`eac213c`, the arming commit) was
`in_progress`: every job was done except `hub-test`. So this firing **wrote nothing** until that run
concluded. It read the playbook, the night log, the research and `DIRECTION.md`, and polled
`gh run view 34681944629` from a clean tree. The run concluded `success`. All four conditions were
then re-measured in a single command, before any file was touched:

| condition | measured | verdict |
|---|---|---|
| branch is not `master`, and `HEAD..master` is 0 | `autonomous/2026-09-08-daily`; `HEAD..master` **0**, `HEAD..origin/master` **0** | pass |
| `git status --short` is empty, and `HEAD` equals `@{u}` | empty; both `eac213cddb89` | pass |
| CI concluded `success` for **this exact sha** | `eac213c` → CI `completed success` | pass |
| newest `DIRECTION.md` section has no line-initial `HOLD MERGE` | the newest section is `## 2026-09-11`; `grep -n '^HOLD MERGE'` over the whole file finds **nothing** | pass |

```
git checkout master; git merge --ff-only autonomous/2026-09-08-daily   Updating 6f7e486..eac213c, Fast-forward
git push origin master                                                6f7e486..eac213c  master -> master
git checkout -b autonomous/2026-09-12-daily                           pushed with -u
```

**What landed:** 21 commits and 29 files (+5815 −162), `6f7e486..eac213c`. That covers the
2026-09-11 day window (the F306 spec loop, the F292 measurement, and F317's classifier repair) and
the 2026-09-11 night window's build of
`an-agent-that-recorded-the-evidence-is-the-author` (F306 and F316; product `4929ea0`, tests
`40bd429`), which was archived at `cf8e178`. This is a true fast-forward to the branch tip.
`master`, `origin/master` and the new branch are all `eac213c`, and `git branch --merged master`
now lists `autonomous/2026-09-08-daily`. That branch spanned **five days**, 2026-09-08 through
today.

This is the gate's **second** landing, after 2026-09-07. It also answers yesterday's structural
diagnosis. The gate can only pass in a gap no firing occupies: a check at the start finds an
in-flight run, a check at the end finds a dirty tree, and committing first finds an unbuilt sha.
**The gap does exist for the first firing of a window**, provided it waits the 10–15 minutes CI
takes before writing anything. The arming commit touches only a log and state files, and CI for it
starts the moment the arm pushes. The cost of that wait here was about 10 minutes of the firing. So
yesterday's proposed playbook edit (gate on `HEAD~1`, or on the newest green sha) is **not needed**.
What is needed is "compose waits on the arming commit's CI before writing". That is this firing's
measurement, from one sample. It is not yet a playbook edit, and it goes into `decisions_for_user`
as a note, because the playbook is the operator's.

`STATE-day.json`'s `branch` field is now `autonomous/2026-09-12-daily`, and `parent_sha` is
`eac213c`. `run-iteration.ps1:124` stops the driver on any mismatch, so this field is load-bearing.
The night arm (`arm-cycle.ps1:134-139`) picks the newest unmerged `autonomous/*-daily` branch. That
will be today's branch as soon as this commit lands, so tonight's window follows automatically.

### Step 2 — the branch is settled: a fresh one, cut from the new `master`

The branch is `autonomous/2026-09-12-daily`, cut from `eac213c`. It spans **one day**. The review
page should say so, and the fact that it is fresh goes first.

### Step 3 — what last night did

Nine commits (`de5733e..cf8e178`), all of `an-agent-that-recorded-the-evidence-is-the-author`
(F306 and F316), which is now **archived**. Nothing is left over from the night. From the night's
own close:

- **Driven live and proven from the tables.** On the pre-fix tree `37b8226`, the evidence author
  approved its own work (seq 8, `proj-34d006e2f3e5`). On the fixed tree, the flow staffed only the
  other agent. The author was refused at four doors: MCP approval (403), firing (409), hand
  dispatch (403), and the S3.4 wedge `PATCH` (403).
- **Tested, not driven.** F316's restaff route (4.10 and 4.10a), the actor-kind filter (4.5), and
  unaccepted evidence still naming its author (4.6).
- **Mutation-checked:** 10 mutations, and every target leg failed. The whole Hub suite passed
  **4057, with 0 failed**, on `99d64fc`.
- **Found:** `F319` (B). A review refused on the scheduler path leaves the refused reviewer holding
  the task. Its `ReviewTurnRefused`-after-staging leg is unverified, and if that leg holds, F319 is
  an A.

The night's `decisions_for_user` held two items. **Merge** is now resolved by the gate.
**CLAUDE.md's trial-Hub section says head `0101`, but this branch's migration head is `0102`**. That
one is still the operator's, and it is carried below. The night also recommended F319 as today's
**first** spec-loop candidate. That conflicts with `DIRECTION.md`'s fixed order; see step 5.

### Step 4 — the research arrived, and yesterday's "the scheduler skipped the morning" was wrong

`research-2026-09-12.md` was written at 07:33 (log `2026-09-12_071002.log`; the part read carries
no `rc` line). It is copied to `spec-queue/research/2026-09-12.md`, 609 lines.

**A correction to yesterday's log, which is left standing rather than edited.** The 2026-09-11 day
window recorded the 2026-09-11 research as missing, *"no log for today"*, and put a
scheduler-skipped-the-morning item into `decisions_for_user`. The research routine's `logs/`
directory holds `2026-09-11_090433.log`. The run **started at 09:04:33 and finished at 09:22:46
with `rc=0`**, writing `research-2026-09-11.md` (549 lines). Yesterday's check ran at about 09:05,
some thirty seconds after the run began and before it had written anything. The file was **late,
not skipped**. It started about 1h54m after its 07:10 trigger, and why it started late is
**unverified**. It is copied as `spec-queue/research/2026-09-11-late-0922.md`, under the README's
rule that `<date>.md` names only a delivery a day window actually took. Nothing reads it; it exists
so the record is complete. Today's run started on time at 07:10:02.

Treated as data, not instructions. What bears on today's queue, from its ranked list:

1. **Pin the refusals `_ABSOLUTE_PATH_RE` makes by accident before F300 and F312 move it.** The
   routine measured this from a scratch copy of `mcp_server.py`. Five real parent-directory escapes
   are denied today only because the regex matches mid-token: `> ../x`, `tee ../x`, `cp … ../../x`,
   `> ~/x`, and `> "$HOME/x"`. A token-anchored fix would allow all five, and the suite would stay
   green. This goes straight into R1.
2. **The F312 verdict names a posture, and the posture differs between runners.** Codex
   `decide_approval` accepts a command approval by its cwd alone (`codex_appserver.py:280-283`).
   R1 must say what "Workspace only" means for network on Codex, even if the change does not build
   it. Codex is undrivable (Codex plan cancelled 2026-08-29), so any Codex claim stays labelled
   unverified.
3. **Codex runs on app-server never receive their charter, and `exec` replaces Codex's own system
   prompt with it** (`runner_commands.py:311-312`; `codex_appserver.py:955-1001`). This is
   defect-shaped and not tied to today. It is queued as `D-7`: verify the file:line claims against
   the code and file it as a finding.

The routine also records that the `claude` CLI moved from 2.1.238 to **2.1.269** at 23:00:04 last
night, between the night's arm and its compose. Every Claude run the night drove was therefore on
2.1.269, and no `Run` row records that version. F306 is Hub-side logic, so the night's results are
very probably unaffected. That is inferred, not measured.

### Step 5 — `DIRECTION.md` has no section for today, but yesterday's section fixed the order

The newest section is `## 2026-09-11`. By the playbook, absence of a section for today is not an
instruction, and the queue is composed as usual. However, that section's *"The order for the days
after, so this is not re-decided each morning"* is exactly the input the playbook's *"what to spec"*
priority needs. Position 1 (F306) shipped last night, so **position 2 is today:
`F300 + F312` as ONE change**, the `_decide` URL change. The verdicts are `DECISIONS.md`
`:630-656` (F312, option C) and `:694` onward (F300 narrowed, remedy 1d "both, notice first").

**The night's recommendation of F319 is not followed.** F319 has no operator verdict, and
`DIRECTION.md` is the operator's channel. The night's recommendation is a note, not a decision.
Today's drive (`D-1`) spends itself on measuring F319's unverified reach instead. If that measurement
makes it an A, the operator can reorder for 2026-09-13. That goes into `decisions_for_user`.

### Step 6 — the drain count is **0**, so the spec loop runs

The playbook's loop over `openspec/changes/*/` prints nothing, and `openspec/changes/` holds only
`archive/`. 0 is inside the 0–1 band, so the clear column applies.

**The drive is scoped, not a sweep**, for the reason 2026-09-10 and 2026-09-11 each re-measured.
`scripts/drive/SURVEY.md:144` says the 17-row sweep is complete, and `spec-queue/ROADMAP.md:283`
says "No sweeps". Both outrank the calendar.

### The queue, written

```
D-1  drive     what the night built (F306/F316) + F319's unverified reach, live, Haiku
D-2  spec R1   F300 + F312 as ONE change, carrying research 2026-09-12 §1 and §2
D-3  spec R2   re-derive against the code, independently
D-4  spec R3   re-derive again, independently of R2
D-5  review    review-2026-09-12.html + the APPROVALS row
D-6  repairs   F318 (classifier cross-section arm), if it passes the carve-out test
D-7  ledger    file research 2026-09-12 §3 (Codex charter delivery) as a finding, code-verified
```

There is no `D-0` today, because the gate already opened in this firing.

Work done beyond composing the queue: the gate landing, which is this iteration's step 1, and
copying the two research files. Nothing else.

## Iteration 2 — D-1, the scoped drive: F319 is reached live on both legs, and becomes an A (09:16–09:40)

Branch and `git log` matched `STATE-day.json` at the start (`autonomous/2026-09-12-daily`, head
`50bd12e`, clean). Nothing to reconcile.

### The drive Hub

- **Port 8016**, chosen after `Get-NetTCPConnection -State Listen` showed nothing listening between
  8000 and 8100 (neither 8000 nor 8010 was up). Not 8000, not 8010.
- Fresh profile **`drive0912d`** (`~/.agentweave/hub/profiles/drive0912d/agentweave.db`), migrated
  on launch to `0102`. Key set with `AW_BOOTSTRAP_API_KEY` at first launch, kept at
  `~/.agentweave/hub/drive0912d-key.txt` (outside the repository).
- Started from `hub/` with `py -3.11 -m uvicorn hub.main:app` from source at `50bd12e`. No `.py` under
  `hub/hub` or `src` was newer than the process start (the tree was clean, and `find -newer` on the
  launch log printed nothing).
- Fresh fixture project **`proj-e594aacf615a`**, a git repository at `testbed/scratch/d0912d/fixture`
  (gitignored). Agents on `claude-haiku-4-5-20251001`. Two real Haiku turns were spent in total.
- Afterwards the Hub was stopped (pid 16996), the profile kept, and the drive created **no job**
  (`ai_jobs` is empty) and left no run `running`.

Harnesses, committed: `scripts/drive/t_d1_0912_f319_reach.py` (legs B, A, C) and
`scripts/drive/t_d1_0912_f319_ui.py` (the board). Screenshots are in `testbed/scratch/d0912d/shots/`,
not committed. B ran 9/9, A+C ran 11/11, and every assertion reads the drive database, not a
response.

### Priority 1 — F319's reach: both unverified legs are reached through `POST /agent/trigger`

**(b) after staging: reached, twice, with operator actions only.** The route's pre-check is
`commit_for_task_review`, which reads the database. Three of `prepare_review_turn`'s refusals read the
*repository*, so they are asked only after `enter_selected_task` has staged the task, and
`turn_scheduler.py:349` commits that staging:

- **B1.** Operator evidence names a commit on a side branch. The branch is deleted, and `gc --prune=now`
  removes the commit. A non-author is dispatched. The response is `409` "commit … is not present in
  this repository". The table then reads `under_review`, held by the refused reviewer, with a
  `completed -> under_review` row **attributed to the operator**, and no run.
- **B2.** The reviewer's `.agentweave/reviews/<agent>` is a plain directory. The response is `409`
  "refusing existing path …". The task is left in the same state.

The stranded state blocks its own recovery. A second reviewer is refused by D9, *"already under
review by 'revb2'"*.

**(a) the entry guard, with no drift: reached through timing.** A review is queued for X while X's own
turn runs. The route's check passes, because X is not yet an author. X's turn then records evidence
for the task (`awaiting`). When the entry is delivered, the entry guard refuses it, and the task reads
`("completed", X)`. The entry was retried to `delivery_attempts=3` in about 30 s and then abandoned.
Abandoning it did not release the task.

**So F319 is severity A.** Its heading and Status line now say so, with the table, in
`scripts/drive/FINDINGS.md`. The classifier reads it as A/open.

### Priority 2 — the night's tested-not-driven legs, as a side effect

- **4.6 is driven.** In leg A, X's evidence was never accepted (`awaiting`), and it still refused X
  the review.
- **4.5 is driven.** In leg C, a task whose only evidence is operator-kind was staffed with a fresh
  agent, and a real Haiku review run bound to it ran (`run-1a4df5c4750d`).

### New: F320 (B)

Leg C's review ended without a verdict, and the divergence response restaffed it, correctly, to
`autha1`. At that moment `autha1` had leg A's stranded F319 entry at its queue head. The scheduling
pass that abandoned that head **returned**. The divergence entry stayed `queued`, with
`delivery_attempts=0` and `waiting_reason=NULL`, and the task stayed `under_review`, held by an idle
agent, with no run. It was still like that 9 min 52 s later, and after loading the served UI.
`GET /queue/autha1/status` says nothing is wrong. The mechanism does not need F319: any head refused
to its limit leaves the entries behind it for an unrelated event (there is no tick,
`agent_trigger.py:2433`). Filed with Status open first.

### Priority 3 — what the operator sees

On the Tasks board in the served bundle, B1 and B2 are ordinary `Under Review · @reviewer · Idle`
cards, and A is `Completed · @autha1`. None carries a marker. Only C shows **Stalled**, and that comes
from its open divergence, not from the undelivered entry. What the UI's own dispatch control renders
for the `409` was not captured, because the drive dispatched through the API.

### Known harness gap, not re-run

`t_row12_review_leg.py` section E was not run today, so there was nothing to misread.

### What this changes for the queue

Nothing today. D-2 is F300 + F312, per `DIRECTION.md` 2026-09-11. F319 is now an A with no verdict,
so moving it ahead is the operator's call. That decision is in `decisions_for_user`.

## Iteration 3 — D-2, spec R1: F300 + F312 become `a-url-is-not-a-path`, and the round finds F321 (09:42–10:08)

Branch and `git log` matched `STATE-day.json` at the start (`autonomous/2026-09-12-daily`, head
`e9b2bd0`, clean). Nothing to reconcile.

**Deliverable:** `openspec/changes/a-url-is-not-a-path/`, with `proposal.md`, `design.md`,
`tasks.md`, `test-guide.md`, and deltas for `agent-run-sandboxing` (three ADDED) and
`agent-capability-plane` (one MODIFIED, F300 clause only). `openspec validate --strict` passes.
The MODIFIED block was diffed against the main spec: only the F300 clause and its conclusion differ,
the F301 clause is verbatim (reserved to the notice change by `DECISIONS.md` 1d), and all three
scenarios survive.

### What R1 measured that the verdicts and the research did not have

1. **F321 (A), new, measured live.** One Haiku turn, no Hub, argv from `build_command`, `claude`
   2.1.269: `python sub/hello.py` inside the workspace → `Denied: '/hello.py' is outside your
   workspace.` `ls sub/` ran, which shows the harness does not consult the approver for a read-only
   command. The regex begins a candidate at any `/` mid-word, so `git add src/x.py`, pytest node
   ids and `origin/main` are all refused. This breaches the shipped scenario *"Work inside the
   workspace proceeds"*.
2. **A URL-only fix cannot fire.** F300's request needs `-H "Content-Type: application/json"`
   (FastAPI 0.136.3 parses JSON only under that type, read from source), and the regex refuses
   `/json`. So F321 is folded in and decided, because any fix that moves where a word begins moves
   it anyway. Rejected alternative: `design.md` D8(a).
3. **F301's `S1_python_c`, verbatim, is refused by `_decide`.** `DECISIONS.md` 1c records `allow`
   for it, and 1d's *"notice first"* remedy instructs it. That is measured false. The change does
   not make it pass either (D6). Goes to `decisions_for_user`; `DECISIONS.md` not edited.
4. `WebFetch` to any address is allowed by `_decide` today, and so is `curl example.com` with no
   `/`. The URL rule is a rule about shell text, and the change says so.
5. A 1,200-character URL already gives a 1,244-character refusal reason. That is past the
   1,000-character cap on `PermissionDecisionCreate.reason`, so the refusal report is answered 422
   and swallowed, and the refusal goes unrecorded today. The change bounds the quoted word.
6. `posixpath.realpath` raises `ValueError` on a NUL byte, measured on this machine, and `_decide`
   catches only `OSError`. So on CI's Linux the approver raises today instead of answering. The
   change catches both.

### The design, and its measured table

The design is a word reader with six ordered rules and today's regex kept as a per-word backstop.
It was prototyped in `testbed/scratch/r1f300/prototype.py` (scratch, a design aid, not the
implementation) and run over **56 labelled rows**. Exactly **16** change answer:
- seven own-Hub requests go deny→allow;
- four in-workspace paths go deny→allow;
- `curl example.com/x` goes deny→allow. This is the one widening beyond the verdict, decided in D3,
  and R2/R3 should attack it first;
- three Windows escapes go allow→deny.

The research's five accidental refusals (§1) all stay refused, each now for a true reason.

**One of R1's own arguments was wrong and was corrected before commit.** D1 first said quotes must
delimit because grouping would let a hidden path through. Measured (`measure6.py`), the backstop
catches that. Grouping actually breaks the `Content-Type` header. The outcome is the same and the
argument is different, and the design now states the measured one.

### Findings filed

- **F321 (A), open.** In-workspace relative paths are refused. Reproduction included and re-run.
- **F322 (B), open.** Codex's "Workspace only" decides a command by its cwd, so network is decided
  by cwd. Read, not run, because Codex is undrivable. It is the research §2 non-goal, recorded so
  the change does not close F312 on one runner and leave the other unrecorded.

### What this changes for the queue

Nothing. D-3 (R2) is next and must re-derive independently. Scratch harnesses for re-measuring
are in `testbed/scratch/r1f300/` (gitignored).

## Iteration 4 — D-3, spec R2: R1's reader lets sixteen escapes through, and R2 replaces its split with a lexer (10:10–10:45)

Branch and `git log` matched `STATE-day.json` at the start (`autonomous/2026-09-12-daily`, head
`977b251`, clean). Nothing to reconcile.

**R2 re-derived the change independently.** It did not trust R1's prototype: `today.py` loads only
`_decide`, and **every *today* cell in D2 agreed.** R1's prototype was then run as the *subject*
(`attack.py`, `r1col.py`), over rows R2 wrote to break it. Scratch is in `testbed/scratch/r2f300/`
(gitignored).

### What R2 found

1. **R1's reader let through sixteen commands that today's `_decide` refuses.** Five of them were
   run in Git Bash on this machine, and each wrote a file outside the workspace (`stray1`, `stray2`,
   `stray5`, `stray9`, `ws y.txt`). There are three classes:
   - quotes the shell removes and joins: `'.'./x`, `.""./x`, `.\./x`;
   - an own-Hub address used as a path: `"$HUB_URL/../../../x"`, once `http:/127.0.0.1:8016` exists;
   - a word that ends at the workspace's own name while the shell carries it on: `../ws=y/z` and
     `"../ws y.txt"`.

   R1's reader breached its own requirement text (*"reading words from their start must not let
   through a path that reading them from the middle refused"*).
2. **R1's reader refused F300's own request on Windows** whenever the JSON body used bash's `\"`
   escapes, as `'\'` (J2, J3).
3. **F323 (A), filed, live.** `echo hi > "..\stray.txt"` in the Bash tool is **allowed today** and
   escapes. One Haiku turn on `claude` 2.1.269 used the Hub's argv, with a local listener standing
   in for the Hub. The approver posted `tool_name: "Bash"`, allowed, *inside your workspace*, and
   `stray_x1b.txt` landed next to the workspace. The unquoted `..\x` wrote `..x` *inside*, because
   bash removes the backslash. So R1's X1 row was wrong for Bash: it refused X1a, which writes
   inside, and did not record X1b. This also verifies half of D10 item 5: the approver receives
   `Bash`. `PowerShell` is unverified.
4. **The bound did not bound.** `repr` renders 200 characters as up to 2,002 (astral
   non-printables). The bound now applies to the rendered quotation.
5. **An argument wrong while its outcome was right.** D6 said the quote splits F301's `'/api/…'`
   literal. Under lexing, nothing splits there, and the backstop refuses it instead. The outcome is
   unchanged and the argument is corrected.
6. **Smaller corrections.**
   - The proposal said 12 rows move from refused to allowed. Its own rows W1–W5 are five, so it was
     13.
   - The long-URL reason is 1,224 for a URL of exactly 1,200 characters, not 1,244.
   - Pydantic 2 keeps `max_length` in `.metadata`, which tasks §2.4 would have tripped on.
   - curl 8.21.0 removes dot segments (measured with a local server), which is what makes judging an
     own-Hub address as a path free.
   - `test-guide.md` check 5 told the operator that unquoted `..\stray.txt` is refused on Windows.
     In Git Bash it is correctly allowed, and the escape is the quoted form.

### What R2 changed

- **`design.md`:**
  - D1's split is replaced by a dialect-aware lexer (`Bash`, `PowerShell`, or both for an unknown
    tool), with substitutions recursed into. After it come the words, split only at characters
    that stay in the string. A word that continues is judged as it continues.
  - Rule 1/2: an own-Hub word is also judged as the path it spells. Rule 3: an expansion anywhere
    in the word. `%HUB_URL%` is dropped as a reference.
  - D2: X1–X3 are split by tool, with E, J, S and G6 rows added. The count is 91 commands and 26
    moves (21 to allowed, 5 to refused). On R1's own 56 rows it is the same 16.
  - D4 additions. D5: new wording, the bound moved, lengths measured (197/168/26). D6's argument.
    D8(f) and (g). D10 items 4–6.
- **`agent-run-sandboxing` delta:**
  - Requirement 1 gains a paragraph, plus the scenario *"The run's own Hub address does not carry a
    path out of the workspace"*.
  - Requirement 2's SHALL line now reads *"as the shell that runs the command will read it … the
    whole path, not a fragment"*. It gains paragraphs on dialect and continuing words, and three
    scenarios. Two scenarios were reworded.
  - Requirement 3 gains the rendered-bound paragraph and one scenario.

  That makes 13 scenarios → 18. None dropped. The ADDED headers collide with nothing in the main
  spec. The MODIFIED `agent-capability-plane` block is unchanged, and re-diffed: prose only, three
  scenarios.
- **`tasks.md`:** §1's table is extended, with every E row pinned passing against today's
  `_decide`. The §1.4 xfail lists. §2.1 is the lexer, §2.2 own-Hub-as-path, §2.4 the rendered
  bound. Mutation checks 5.7–5.15, one per new rule, each naming the row that must fail. The drive
  adds X1b (Windows) pre- and post-fix, and a record of every `tool_name`.
- **`proposal.md`:** a *"What R2 found"* section, F323 in Why, rewritten What Changes, and the
  corrected count.
- **`FINDINGS.md`:** F323 (A) appended. The classifier reads it as open A.

`openspec validate --strict a-url-is-not-a-path` passes.

### What R2 did not change

The verdict's shape. D3 was attacked, and it stands: every byte N3 could send leaves today without
the `/x`. Also unchanged: D7, D9, the network wording, and the capability-plane block.

### For R3: attack R2's additions hardest, since they have had one pair of eyes

- The lexer's dialect rules against real bash and PowerShell.
- The continuing-word argument (*"a prefix can only keep the path further in"*).
- The trim set.
- Whether rule 3's *"any `$`"* over-refuses. The prototype cannot tell a single-quoted literal `$`
  from an expansion, so `-d '{"price":"$5/mo"}'` would be refused as uncheckable. That is
  conservative, and it is a false refusal. R2 noticed this and did not fix it.
- Nested shells (D10 item 6).
- Whether 26 moves is still right after any fix.

## Iteration 5 — D-4, spec R3: the round finds R2's backstop lets a glued-`\` traversal escape on Windows (10:45–11:20)

Branch and `git log` matched `STATE-day.json` at the start (`autonomous/2026-09-12-daily`, head
`773ac13`, clean). Nothing to reconcile.

**R3 re-derived the change independently of R1 and R2.** It wrote its own reader from the design's
prose (`testbed/scratch/r3f300/reader3.py`, every R3 change behind a switch in `FIX`), ran the two
shells directly (Git Bash 5.2.37, Windows PowerShell 5.1), and compared today / R2 / R3 over 78 of
D2's rows plus six attack rows (`table3.py`). Scratch is `testbed/scratch/r3f300/` (gitignored).

### The one defect R3 found — a real, live escape in R2's own additions

Rule 6's backstop is `_ABSOLUTE_PATH_RE`, which opens a candidate at `/`, or at `\` **only after a
drive letter**. Rule 5 catches a *plain* relative `..\stray.txt` (X1b), but a backslash traversal
**glued to an option** is not plain (it begins with `-`), so it falls to rule 6 — and R2's backstop,
being today's regex, let it through.

- **Z1, measured live in Git Bash:** `sort -o"..\stray.txt" notes.md` wrote `stray.txt` in the
  workspace's **parent**, rc=0. R2's reader **allows** it. So does today's `_decide`. An escape
  today that R2 claimed to close and did not.
- **Z2, a regression R2 introduced:** `curl -o"..\out" http://127.0.0.1:8016/api` is refused
  *today* (the URL's `//` gives today's whole-command regex a false `/…` candidate) and R2 **opens**
  it. So on this row R2 is strictly worse than today.

**Fix:** on Windows the backstop opens a candidate at a bare `\`, symmetric with `/`. R3 verified
this is the **sole** divergence from R2 (with every other R3 switch off, only Z1–Z3 move; all 78
canonical rows agree). It inherits G3's known residual: `gcc -I"sub\include" x.c`, an inside path
glued to an option, is now over-refused as `'\include'` — the exact Windows twin of `-I../include`,
already a residual over-refusal today.

### What R3 attacked and left standing

The lexer's dialect rules (measured against both shells: bash keeps `\` before an ordinary char
inside `"…"` and removes it bare; PowerShell's `''`→`'`, `""`→`"`, `$env:HUB_URL` is the env var
while `$HUB_URL` is a PS variable expanding to nothing — so a reference's dialect matters). The
continuing-word rule and its "a prefix can only keep the path further in" argument (E15/E16 refuse
under both readers). D3's N3 widening. D4's own-Hub test. D6's `python -c` refusal. The rendered
bound. PowerShell 5.1 also splits `"a"b` into two arguments where R2's lexer joins them — a
curiosity that only ever refuses *more*, never less (Z6 denies under both), so not a defect.

### What R3 recorded but did not fix

A single-quoted literal `$` is over-refused by rule 3 (`curl -d '{"price":"$5/mo"}' $HUB_URL/api`).
The lexer knows the `$` is literal, so the clean fix is to mark active expansions at lex time and
have rule 3 test the sentinel, not the character. R3 left it as a residual (D10 item 7): it is
refused today too (no regression), and teaching the lexer a new rule in the last verification round
would ship an unreviewed change. Recorded for the operator.

### Edits

- **`design.md`:** rule 6 row; a new paragraph under "Why rule 6 exists" (the `\` gap, with Z1/Z2
  witnesses); D2 Z1–Z3 rows and an R3 count paragraph (28 move on Windows); D10 item 7; a new
  section D11 ("What R3 re-derived, and the one thing it changed").
- **`agent-run-sandboxing` delta:** one scenario, *"A traversal joined to an option is refused with
  either separator"*. 18 → **19 scenarios**, none dropped. ADDED headers still collide with nothing
  in the main spec; the MODIFIED `agent-capability-plane` block is untouched.
- **`tasks.md`:** §1.1 Z rows; §1.4 xfail lists (Z1, Z3 move; Z2 reason changes); §2.1 the backstop
  is `\`-aware on Windows; §5.16 mutation (drop bare `\` from the backstop → Z1, Z2 fail); §6.2 the
  drive adds ask 5, the glued-option form, pre- and post-fix.
- **`proposal.md`:** a *"What R3 found, and what it changed"* section; the scenario count.

`openspec validate --strict a-url-is-not-a-path` passes. The three-round discipline found a real
defect for the sixth consecutive outing: R3 caught an escape that R1 and R2 both missed, in the one
rule R2 grew the least.

### For D-5 (review page)

Section (4) must now say **three** things about the rounds: R2 replaced R1's word split after
measuring 16 escapes (5 live) and filed F323; and **R3 found a live Windows escape R2's reader
passed** (`sort -o"..\x"`, measured writing outside) plus a regression R2 introduced (Z2), fixed by
making rule 6's backstop `\`-aware. The change is stronger for it and still self-contained
(Python-only, no migration/API/schema/UI).

## Iteration 6 — D-5, the review page and the APPROVALS section (11:05–11:20)

Branch and `git log` matched `STATE-day.json` at the start (`autonomous/2026-09-12-daily`, head
`af925c5`, clean). Nothing to reconcile.

**Written:** `spec-queue/review/review-2026-09-12.html`, self-contained (no external `src`/`href`,
checked with `html.parser`, tags balance), theme-aware on the same `:root` / `prefers-color-scheme`
palette as 2026-09-11's page, with an explicit `body` background. The six sections are in the
playbook's order. (1) The branch is fresh and one day old, and the gate opened, with the four-row
table from iteration 1. (2) The night's F306+F316 build, as a driven-vs-tested table. This window
drove 4.5 and 4.6 as D-1 side effects; F316's restaff route (4.10/4.10a) is still tested only.
(3) F319 (A, raised) and F320 (B), with file:line, plus the round-filed F321/F322/F323. (4)
`a-url-is-not-a-path`: the problem, the argument, a per-round table (R1/R2/R3 all changed it; R2's
16 escapes, 5 live, and F323; R3's live `sort -o"..\x"` escape and the Z2 regression), the single-quoted
`$` residual, and the cost. (5) The default queue if nothing is approved. (6) The three research
candidates, each ending in what it means for AgentWeave, plus the late-not-skipped correction.

The eight `decisions_for_user` items are in one box near the top. Two of them change tonight's
build (F321 folded in; D3's widening and `/dev/null`), and the page says what happens with no
answer.

**Measured for the page, not copied:**
- `openspec validate --strict a-url-is-not-a-path` passes (11:17).
- `tasks.md` has 46 open tasks, split per section 5/7/1/1/16/4/3/4/4/1.
- The classifier reads **7 open A**: F299, F300, F301, F312, F319, F321, F323. Four are the
  change's; three have no proposal. `openspec/changes/` holds only the change, so the default
  queue has nothing to archive and lands no feature.
- The 7 refused-after-R3 rows were checked against `design.md:335-340`: R2's five plus Z1 and Z3.
  The page's first draft said the seven "include every Windows escape". That was true but
  misleading, since Z3 is an over-refusal and E7 is not a path. It now names all seven.

**APPROVALS.md:** a `## 2026-09-12` section above 2026-09-11, with one row paragraph for
`a-url-is-not-a-path` and **no status token**. Checked: no line in the section begins with a token,
`ORDER:` or `NOTHING TONIGHT`. It notes that the 2026-09-11 row is now history and already built.

Not done: no screenshot of the page. It needs no Hub, and its structure is yesterday's.

**Next:** D-6 (F318, the classifier's cross-section arm), if the carve-out holds.

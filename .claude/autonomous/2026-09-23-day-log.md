# 2026-09-23 day window

Newest entry at the BOTTOM. Playbook: `.claude/loops/day-window.md`.

Armed 2026-09-23T08:55:02+01:00 on `autonomous/2026-09-21-daily` (parent master@6c0a467), model opus.

## Iteration 1 (compose) — 2026-09-23T09:02+01:00

**Tree hygiene before anything else.** On arrival the tree was dirty:
`hub/hub/model_catalog.py`, modified on disk at 08:59:58 (after `ba94408` armed at 08:55, before
this process started) but never committed. Diff was a coherent, self-consistent re-verification of
the catalog against today's CLI output (`opus`/`fable` aliases now resolve to `claude-opus-5-5`/
`claude-fable-5-1`; `gpt-6-luna` added, `gpt-5.4`/`gpt-5.4-mini` dropped, `gpt-5.6-terra` now
Codex's default) — not this loop's work product (no queue item authored it), but valid
(`ast.parse` clean, `black --check --target-version py311` clean) and touching no
`openspec/specs/` requirement (`model-catalog/spec.md:80` constrains the UI not to hardcode
models; it says nothing about which ids the data file lists), so it met the D-6 no-spec-needed
bar. Committed as its own commit, `b661766`, rather than left dirty or discarded.

**Step 0 — backlog.** `scripts/backlog_page.py --check` was stale; regenerated. Since the last
generation: open findings 163→161, fixed 220→221, unbuilt changes 2→0, severity A 5→3, F352 and
F388 no longer open.

**Step 1 — merge gate, morning fallback.** Not run: CI for the inherited sha (`88f3fa1`, night's
last commit, pushed 08:56) had not concluded as of 09:02 (run `35834369496`, `createdAt`
07:56:48Z — 6 minutes old, well under the 60-minute hang threshold, just a normal in-flight run).
Concluded CI runs immediately before it: `fa47013`/`85575fe` both `success`. `git rev-list --count
HEAD..master` = 0 (branch is strictly ahead, a genuine fast-forward candidate whenever CI does
land). `git rev-list --count master..HEAD` = **123** — master is well over one day's commits
behind; this branch has spanned three days (`autonomous/2026-09-21-daily`) without a landing,
consistent with CI having been red through the small hours before last night's fixes.

**Step 2 — branch.** Staying on `autonomous/2026-09-21-daily` (day 3).

**Step 3 — what last night did.** Two changes closed and archived:
`a-hub-that-was-not-told-which-database-refuses-to-open-one` and
`an-unstaffed-review-names-its-holders` (F352's both halves). 6.3 drove a real flow live (Haiku
turns, real commits, the unstaffed-review 409 remedy watched working end to end — not asserted).
6.2's full `hub/tests/` run: 12 failed (all `test_pty_runner.py`/lifespan PID tests) / 4644 passed
/ 86 skipped, reproduced as environmental (contention with the concurrent live drive) by an
isolated re-run: 32/32 green. No open `decisions_for_user` carried forward except one already
resolved by the operator's own prerogative (the UI bundle for group 5 ships once `:8000` restarts
past `c18a87b` — not this loop's call).

**Step 4 — research.** `research-2026-09-23.md` present; copied to
`spec-queue/research/2026-09-23.md`. Two ranked candidates: **#1** — another agent's
`send_message` text can smuggle an `@/abs/path` into a `claude -p` argv and get it read with zero
tool calls and every tool disallowed (measured today on CLI 2.1.280; upstream mechanism confirmed,
`claude-agent-sdk-python#1269`, shipped in SDK v0.2.158 2026-09-23T01:40Z, with a
`client_composed` opt-out also measured working) — a real, S/M-cost security gap reaching
`inbound_queue.py:129` → `turn_scheduler.py:411` → `agent_trigger.py:1091` →
`runner_commands.py:283` and loop briefing text too. **#2** — catalog is missing Opus 5.5 and
GPT-6 Sol/Luna; **mostly already closed by this iteration's tree-hygiene commit** (`b661766` added
`claude-opus-5-5` and `gpt-6-luna`), but `gpt-6-sol` is still absent — left for a later D-6 slot,
not fixed here (compose does no other work).

**Step 5 — DIRECTION.md.** Newest dated section is `2026-09-17`; nothing dated today. Absence is
not an instruction — composing the default queue.

**Drain count (operator-unanswered changes):** `openspec/changes/` (non-archive) is **empty** →
**0**. Clear column, both loops: today runs D-2/D-3/D-4 and, per the 2026-09-12 decision, a second
independent loop D-2b/D-3b/D-4b after the first's R3.

**Queue written**, sized against `stop_at` 17:00 with ~8 hours available:
`d1-drive` → `d2-r1`/`d3-r2`/`d4-r3` → `d2b-r1`/`d3b-r2`/`d4b-r3` → `d5` (review page) → `d6`
(repairs, if room — `gpt-6-sol` is the standing candidate) → `d7-gate` (last, always). R1 targets
per the playbook's priority order: a finding from `d1-drive` first, else an open ledger finding
with no proposal, else the research file's ranked list (candidate #1 above is the leading
candidate there, being real and measured). The second loop's R1 takes whatever is next in that
same order with no file overlap against the first change's blast radius.

## Iteration 2 (d1-drive) — 2026-09-23T09:15+01:00

**Reconciled:** branch/log matched `STATE-day.json` on arrival — no drift.

Drove last night's two closed changes, scoped, from a fresh port/profile (8011,
`profiles/drive0923d1/`), never `:8000`/`:8010`, never `proj-5e960453`/`proj-18e5d4e0`.

**`a-hub-that-was-not-told-which-database-refuses-to-open-one`:** re-observed live from a second
angle group 6 didn't cover — `.env` *present* (this machine's normal state) vs. *absent*. With
`.env` present and `DATABASE_URL` unset, no refusal fires (correct: `.env` is a legitimate source)
and the startup line names the resolved path, existence and PID at `WARNING`, exactly as designed.
With `.env` moved aside too, the import-time refusal fires verbatim against the design, before
uvicorn binds a socket. Both processes killed by exact PID, never `/IM python.exe`. No new defect;
confirms group 6's finding from a second condition.

**`an-unstaffed-review-names-its-holders`:** group 6 already drove this exhaustively the same
night (two real Haiku turns, three-surface byte agreement). Re-running the identical scenario
tonight would mostly re-spend real turn cost for no new information, so today's check was lighter
but still real: the regression suite (`test_a_held_agent_is_busy.py` +
`test_a_task_nothing_will_move_holds_nobody.py`) run fresh on today's HEAD in isolation — **65
passed**, no drift — and the UI bundle-deferral decision from `e6d1c96` re-confirmed from the
drive Hub's own startup warning (bundle stamped 2026-09-22T18:07, source moved since) as still
deliberate, not an oversight.

**No new findings.** Full account: `scripts/drive/FINDINGS.md`, `## D-1, 2026-09-23 (day window)`.
Teardown: one throwaway project + agents (no jobs created) and its `testbed/scratch/` fixture
removed; both drive-Hub processes killed by PID; tree clean before commit.

Next: `d2-r1`, spec loop A R1, per the priority order in `STATE-day.json` (a finding this drive
produced — none — else an open ledger finding with no proposal, else research candidate #1: the
`@/abs/path` argv-smuggling gap).

## Iteration 3 (d2-r1) — 2026-09-23T10:05+01:00

**Reconciled:** branch `autonomous/2026-09-21-daily`, HEAD `ea9e358`, tree clean — matches
`STATE-day.json`.

**Target.** The priority order puts an open ledger finding ahead of a research candidate. Research
candidate #1 was taken anyway, filed as a ledger finding first (**F409, A**), because it is a
measured boundary escape. The ledger grades those A (F321/F323/F331), and the only open ready A
(F325) is Codex-only and undrivable here. Filed as F409, so it is now both.

**Re-measured independently** (not taken from the research file), `claude` 2.1.280, one Haiku turn
per row. Expansion was detected from the CLI's own session transcript
(`"attachment":{"type":"file"`), never from the model's reply. Several replies refused as "prompt
injection", so the reply cannot tell "not read" from "read and withheld".
- The Hub's exact queue prompt shape, all file tools disallowed: marker returned, 0 tool calls.
  **Reproduced.**
- Tokeniser: expands at start of text and after whitespace (newline, tab, U+00A0, U+3000, U+2028,
  **U+FEFF**), inside fences, after `> `, for `@../x`, `@"…"` and a backslash path. Blocked after
  one backslash (and two) and after 22 other preceding characters. The consolidated probe
  `scripts/drive/d2_0923_at_mention_tokeniser.py` was rerun in full on a fresh marker: **41/41 rows match**, exit 0.
- **`client_composed` (the research file's recommended fix) measured unusable.** It stops the read,
  but with the Hub's own `--mcp-config` the model could name **none** of the 27
  `mcp__agentweave__*` tools, because the deferred-tools and MCP-instructions attachments are
  dropped. Shape (a) rejected.
- **U+FEFF broke the "escape only an at-sign after whitespace" rule.** Python's `\s` does not
  include it, and the CLI's (JavaScript) does. D2 is therefore unconditional: every at-sign in
  non-operator text becomes `\@`.

**Proposed** `openspec/changes/an-at-mention-an-agent-wrote-reads-no-file/`: proposal, design
(D1–D6, two operator questions Q1/Q2), tasks (0 rounds, 1 tests-first, 2 fix, 3 CLI probe, 4 live
drive, 5 close), test-guide, and a spec delta (one ADDED requirement in `agent-run-sandboxing`).
`openspec validate --strict`: **valid**. Backlog regenerated: open 161→162, severity A 3→4, one
unbuilt change.

**Left for R2 explicitly** (design Risks and task 0.1): whether any Hub notice or the divergence text
interpolates agent-authored text, and whether an agent-chosen spec document path can carry an
at-sign. R1 did not read these line by line.

Spend: about 50 Haiku probe turns (well under $1).

## Iteration 4 (d3-r2) — 2026-09-23T10:50+01:00

**Reconciled:** branch `autonomous/2026-09-21-daily`, tree clean. HEAD is `821982d`, one commit past
the log's last entry. It is iteration 3's own follow-up: the probe's invisible characters are now
spelled as `\u` escapes, the same strings, and 2 rows were re-run live. No drift.

**R2 on `an-at-mention-an-agent-wrote-reads-no-file`**, re-derived from `grep`, not from R1's
table. Nine results are recorded in design.md's Round log (R2-1 to R2-9). The ones that change the
change:

- **Scope widened: the Hub's one-shot workers expand too.** `grep '"-p"'` finds three spawn sites.
  The checkpoint worker (`worker.py:139`) and the conversation titler (`conversation_titles.py:71`)
  put the transcript, notes and opening exchange on `claude -p`, and neither has any posture.
  **Measured**, with argv built by the Hub's own builders, twice on fresh markers: both attach an
  agent's `@<outside path>`. The checkpoint worker's JSON reply **carried the file's contents**, so
  they would land in a stored checkpoint handed to the successor agent. New probe:
  `scripts/drive/d3_0923_worker_at_mention.py`. Design **D7**: neutralise the whole prompt at
  `build_worker_command` and `build_title_command`, for both CLIs.
- **A regression D7 would have caused, designed out.** The checkpoint probe copies file paths
  verbatim, and `_normalise` maps `\` to `/`. So `packages/\@scope/x` would have been graded
  missing and invented, and a good checkpoint marked `failed`. `grade_probe` undoes the escape in
  the answers first. The generation template gains an "at-signs without the backslash" rule
  (`checkpoint/2`).
- **D6's trigger moved** to "the blocks contain `\@`". R1's rule missed the question echo, which is
  escaped upstream inside an `operator` entry. D4 now states that the echo's stored content shows
  `\@` in the chat (`api/v1/agent_chat.py:205`).
- **Closed:** the notices (two interpolate nothing, and the spec notice carries only a path an agent
  cannot mint with an at-sign); divergence (it does interpolate agent-authored `task.title`, which
  D3 already covered); the context file (`--append-system-prompt-file` **measured not to expand**);
  and the trust boundary (`/agent/trigger` accepts only an operator credential). No existing test
  has an at-sign in affected content.
- **Tokeniser rerun:** **41/41 rows match** (exit 0, fresh marker), `claude` 2.1.280.

**Filed F410 (C)**, read and not driven: `checkpoint` and `divergence` blocks reach the agent as
`Agent "None"`, because `format_turn_prompt` has no label for them. Out of scope for F409.
**Updated** F409 with the worker widening. Backlog regenerated: open 162→163, severity C 71→72.

`openspec validate --strict`: valid. Spend: about 50 Haiku turns (41 tokeniser, 9 across four worker-probe
runs, 1 system-prompt control), well under $1.

**Next:** D-4, R3. It must re-derive independently of R2's notes.

## Iteration 5 (d4-r3) — 2026-09-23T10:20+01:00 (system clock)

**Reconciled:** branch `autonomous/2026-09-21-daily`, HEAD `7e8bb35`, tree clean — matches
`STATE-day.json`.

**R3 on `an-at-mention-an-agent-wrote-reads-no-file`.** I read the proposal and design D1–D7 as the
claims under test. I re-derived from `grep` before reading R2's Round log:
- every `"-p"`, `exec` and stdin write in `hub/hub` and `src/agentweave`;
- every `origin_type=` site;
- every message template the browser composes in `hub/ui/src`.

What changed the change:

- **R3-1, a new vector: the board's Start work (design D8).** `useStartWorkOnTask`
  (`hub/ui/src/api/tasks.ts:455`) posts `` `Work on task ${taskId}: ${title}` `` as an **operator**
  entry, and `create_task` lets an agent author the title. R1 and R2 both held that only the
  question echo mixes authors; neither looked at what the browser composes. Fixed at composition in
  the UI (D4's pattern). The change now touches the UI (bundle refresh). Dropping the title
  instead is offered as **Q3**. Read, not driven: task 4.2b drives it.
- **R3-2, D7's "stored worker output carries no `\@`" rested on a prompt rule.** It is replaced by
  `restore_file_mentions`, the exact inverse, applied in `worker._interpret` and to the titler's
  output. The template rule and the `checkpoint/2` bump are dropped. The inverse was checked
  exhaustively over 5460 short strings: 0 failures.
- **R3-3, the probe grading is symmetric.** `_normalise` drops separators before an at-sign on both
  sides. That closes R2's accepted Windows-separator residual.
- **R3-4, measured** with the new probe `scripts/drive/d4_0923_worker_json_escape.py`: Hub builders
  and Hub parsing, 9 Haiku turns, `claude` 2.1.280.
  - **0 unparseable.** `\@` is an illegal JSON escape, and `extract_json_object` returns `None` on
    one (checked). But Haiku always emitted the legal `\@`.
  - **Generation stored 0 `\@`** both with R2's rule and without it (3/3 each), so the rule bought
    nothing.
  - **The probe copied `\@` verbatim in 2 of 3 runs.** Today's `_normalise` fails those. R2's undo
    and R3's symmetric rule both pass them.

**Held, re-derived:**
- `trigger_agent_directly` has one caller.
- There are exactly three `"-p"` sites, and no SDK spawn. The CLI's own subprocesses carry no
  prompt.
- D2's inverse property holds.
- D3's default-deny covers all five origins.

**Edited:** design (Context table and paragraph, D5/D6 wording, D7's consequences, new D8, Risks,
Q3, Round log R3), proposal, spec delta (a worker-output SHALL made enforceable, a Start work
clause, two scenarios), tasks (0.2 ticked, 0.3 with Q3, 1.9 widened, 1.10 rewritten, new 1.11, 2.3a
revised, new 2.3b and 4.2b), and test-guide. Also added an R3 paragraph to F409.
`openspec validate --strict`: **valid**. Lint: the probe passes ruff and black.

Spend: 9 Haiku worker turns (well under $1).

**Next:** D-2b, spec loop B R1.

## Iteration 6 (d2b-r1) — 2026-09-23T10:35+01:00 (system clock)

**Reconciled:** branch `autonomous/2026-09-21-daily`, HEAD `dcdf723`, tree clean. This matches
`STATE-day.json`.

**Picked.** I followed the playbook's order: first an open finding with no proposal, then research.
- **F325 (A) skipped.** It is Codex-only and undrivable here, as the 09-17 and 09-18 windows also
  concluded.
- **Research #2 is a D-6 repair, not a spec.** It is the catalog's `gpt-6-sol` row.
- **F400 (B) taken.** Its blast radius is `jobs.py`, `scheduler.py` and `agent-loops`. It shares no
  file with loop A: loop A's design cites `jobs.py:560-570` but edits nothing there.
- **F373 (B) folded in.** It sits in the same `run_job` branch (`:1383-1453`) under the same
  requirement. Two proposals editing those lines would collide (the F300/F312 rule).

**Proposed** `openspec/changes/pressing-run-names-the-reason-that-held`: proposal, design D1-D4 with
Round log R1, tasks (0-4), test-guide, and an `agent-loops` MODIFIED delta.
`openspec validate --strict`: **valid**.

**Measured on `dcdf723`** through the real route (`scripts/drive/d2b_0923_run_answer_probe.py`,
7 cases, no agent turns):
- **F400 reproduces.** A documentless loop with a sibling free is told *"no other agent is free to take
  this loop's work"*. Its empty queue reads *"…for another agent to take"*. The flow case is true.
- **F373 reproduces.** An in-flight press is answered with an hour-old *"1 still awaiting a
  prerequisite's approval"*.
- **The board is already right.** Decision `stalled`, and the board reads the busy sentence. This
  answers the archived change's Open Question 2 by measurement.
- **A trap in F373's own repair sketch.** The route's session is passed into the firing, with
  `expire_on_commit=False`, so `_stall_run_to_increment` mutates the very object the route read
  (`same_object=True`, 1→2 in place). `tick_count` must be copied to an `int` before firing. This
  shaped D3.

**Found in R1, and reflected in the change:**
- `test_board_agent_role.py:385` and `:420` pin *"no other agent is free"* on documentless loops.
  Task 1.6 moves them on purpose.
- The requirement's scenario *"A firing that recorded its own refusal…"* has no test. The busy
  re-ask gate only matters in a race, which real state cannot stage, so task 1.9 simulates it with a
  patched guard.

**Filed F411 (B), read and not driven.** The Jobs page's Run button (`JobsPage.tsx:157`,
`onRun={runJob.mutate}`) has no `onError`, and there is no global mutation handler. So every 409 or
500 from Run is invisible in the app. Only MCP `run_job` shows the answer. It is kept out of this
change because it needs a UI bundle refresh, which loop A also does. It becomes design Open
Question 1 (fold it in).

**Ledger.** Notes added to F400 and F373. Backlog regenerated: open findings 163→164, and changes
waiting on the operator 1→2. Spend: no model turns; about 12 pytest runs of the probe.

**Next:** D-3b, R2 on `pressing-run-names-the-reason-that-held`. It must re-derive from `grep`
before reading R1's tables.

## Iteration 7 (d3b-r2) — 2026-09-23T10:48+01:00 (system clock)

**Reconciled:** branch `autonomous/2026-09-21-daily`, HEAD `0beef69`, tree clean. This matches
`STATE-day.json`.

**R2 on `pressing-run-names-the-reason-that-held`.** I rebuilt the decline-path table from `grep`
over `_fire_job_internal` before opening R1's design. It agrees with R1 on nine rows and their line
ranges. R2 found two paths R1 did not describe, both in the same `not success` branch:
- **A firing that raises before its row exists** writes nothing, because the `except` checks
  `"run" in locals()`. Measured: the route answered 409 with an hour-old stall. This is F373's shape
  reached by a crash.
- **A firing that raises after its turn started** writes `failed`. The route then re-decides the loop
  before it reads the row. Measured: 409 *"already being worked … nothing is wrong"* over the
  `failed` row the press itself wrote.

**What changed in the proposal:**
- **D3 widened.** The row the press wrote is its answer whatever its status: `skipped` → 409,
  `failed` → 500. `_loop_in_flight_decision` is asked only where the press wrote nothing.
- **New spec text.** One new paragraph and two new scenarios in the delta.
- **New tasks** 1.12 and 1.13. Task 2.3 is rewritten.
- **`LoopBusyRefusal.held` renamed to `condition`**, because *held* already means a provider hold in
  this module.
- **Tasks 1.7 and 1.8 made non-vacuous.** An operator's press sends no run identity, so their
  requester check compared `None` with `None`. They now seed a sentinel, and I measured that it
  survives a counted stall.
- **Precedence wording tightened** to "the empty queue alone".
- **`agent-loops:793` needs no delta.** Its five paragraphs together state all three of the guard's
  conditions, and this change moves no decision the guard makes.

**Re-ran R1's probe on `0beef69`.** All seven cases reproduce exactly, including the identity trap.
The probe gains three R2 cases.

**Filed F412 (B).** When `schedule_agent` reports `terminal_failure`, the firing marks its row
`failed` but still returns `True`, so Run answers `200 {"success": true}`. Measured. It is an
instance of F108's class. I kept it out of this change, because `terminal_failure` has known
dishonest defaults and the fix changes the success branch. It is design Open Question 3.

**Checks.**
- `openspec validate --strict`: **valid**.
- Ruff with CI's rule set over `scripts/`: clean.
- Backlog regenerated: open findings 164→165.
- Spend: no model turns, about 5 pytest runs of the probe.

**Next:** D-4b, R3. It must re-derive the argument independently and must not start from R2's notes.

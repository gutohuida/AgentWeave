# 2026-09-11 night window

Newest entry at the BOTTOM. Playbook: `.claude/loops/night-window.md`.

Armed 2026-09-11T22:55:07+01:00 on `autonomous/2026-09-08-daily` (parent master@6f7e486), model opus.

---

## Iteration 1 — compose (2026-09-11 23:05 → 23:20)

**Position confirmed, no reconciliation.** Branch `autonomous/2026-09-08-daily`, `HEAD` = `37b8226`
(*arm(night)*) = `origin/autonomous/2026-09-08-daily`, tree clean. `master` = `origin/master` =
`6f7e486` = `STATE-night.json`'s `parent_sha`; the branch is 11 commits ahead of it. No branch cut.

**APPROVALS.md, 2026-09-11 section (the newest):** exactly one row with a token —
`APPROVED an-agent-that-recorded-the-evidence-is-the-author` (F306 A + F316 A, 42 tasks, 0 ticked).
No `ORDER:`, no `NOTHING TONIGHT`. The row carries four operator conditions that the queue encodes
rather than paraphrases: verify it as **two** findings (§7.0 and §7.1 each), verify it as **five**
call sites (§3.4, R4's reversal, is not optional), §5's three live drives are in scope with a
`git worktree` and **no `git stash`**, and the §4 mutation-hygiene block governs every revert.

**Backlog, measured before queueing the approved work:**
- Source 1 (unarchived, implemented, needs archiving): **empty.** `openspec/changes/` holds one
  non-archive directory, the approved change itself, at 0/42.
- Source 2 (open findings with a proposal): the open severity-A findings are F299, F300, F301, F306,
  F312, F316. Only F306 and F316 have a proposal, and it is the approved change. The other four go to
  `notes_for_tomorrow` — the playbook routes a finding with no proposal to the day window. F318 (B,
  classifier) and F292 (B) are likewise unproposed and also noted, not queued; the FILL note's
  suggestion of an F292 "concurrent sampler" is not built tonight for that reason.

**Tree green before building on it — measured.** The relevant chunk, not the whole suite: every Hub
test file that names one of the five functions this change edits (`grep` over `hub/tests` for
`agents_that_may_have_authored|_guard_author_is_not_reviewer|_guard_reviewer_is_not_the_author|
review_dispatch_refusal|_answer_failed_review|record_evidence`, 17 files) plus R3's three divergence
suites. **440 passed, 0 failed, 5m49s** under `py -3.11`, from `hub/`. The exact command is in
`n1-source`'s detail so the next firing can re-run the same set after its edits.

**One repair queued rather than only noted** (`n5-playbook`): `night-window.md:120` still tells the
drive to use `~/.agentweave/hub/profiles/beta/agentweave.db`. There is no `beta` profile —
`ls ~/.agentweave/hub/profiles` reads `drive0910 f274p0 live trial` — and the 2026-09-10 night log
recorded exactly this at its line 122 without repairing the playbook. Its port, 8011, was also
already occupied last night. Queued before the first drive so the drive runs from a correct
instruction; this iteration does no other work, per the playbook.

**The queue**, nine items, ordered so stopping anywhere leaves committed, coherent work:
`compose` (done) → `n1-source` (§1–§3, five call sites, plus §4.11's fixture) → `n2-tests` (the new
file, green) → `n3-mutations` (§4.7 and the four other mutation legs, one firing, clean tree after
every restore) → `n4-gate` (whole Hub suite + CI lint set) → `n5-playbook` → `n6-drive-prefix`
(§5.1 in a worktree at `37b8226`, proven only by the `task_transitions` row) → `n7-drive-fixed`
(§5.2/§5.3, plus the §3.4 wedge and §3.5 refusal by hand) → `n8-close` (two findings, archive).

`prefix_sha` is recorded as `37b8226`: every commit before `n1-source`'s carries the same Python,
so that sha is a valid pre-fix tree for §5.1's worktree.

---

## Iteration 2 — n1-source (2026-09-11 23:10 → 23:40)

**Position confirmed, no reconciliation.** Branch `autonomous/2026-09-08-daily`, `HEAD` = `de5733e`
(*compose(night)*), tree clean — matches `STATE-night.json` (iteration 1, `current: n1-source`).

**What was implemented** (`tasks.md` §1, §2, §3, §4.11, plus a new §4.11a — 20 of 43 ticked):

- `task_transition_service.py` — `agents_that_recorded_evidence_for(session, task_id)`: distinct
  `RequirementEvidence.actor`, `actor_kind == 'agent'`, non-empty, **no** `review_state` filter; the
  docstring says why for each and names its three consumers, two of which use it alone (§1.1–1.4).
  `RequirementEvidence` joined the existing `db.models` import; `import hub.task_transition_service`
  is clean. The union gains it as a fourth source, with a fourth docstring row (§2.1–2.2).
  `_guard_author_is_not_reviewer` falls back to the evidence term **alone**, after the operator
  exemption and `_REVIEW_OUTCOMES` gate, only where no agent is recorded as completing (§3.1–3.3).
  `_guard_reviewer_is_not_the_author` takes the same fallback on `task.assignee`, and its
  "no recorded completer" bullet is now conditional with the D14 reasoning (§3.4/3.4a/3.4b).
- `scheduler.py:625` and `:1574` read, not edited: both call `agents_that_may_have_authored` after
  `completion_attribution` (§2.3). No recomposition found there.
- `run_divergence._answer_failed_review` — the completer-only term replaced by the shared two-branch
  derivation (`completion_attribution`, then the union where no agent completed), silent reviewers
  and `run.agent` kept local; `excluded_because="has worked on this task"` on the no-agent branch,
  default kept on the agent branch; the docstring's *"excludes the author by construction"* claim
  corrected (§2.4/2.4a/2.5).
- `api/v1/agent_trigger.review_dispatch_refusal` — 403 where no agent completed and the reviewer
  recorded evidence, placed after the status and holder checks and the completer comparison, so the
  refusal order an operator meets is unchanged (§3.5).
- **§3.6, checked:** §3.1, §3.4 and §3.5 are one rule stated at three layers — each reads
  `agent_that_completed(...) is None and <agent> in agents_that_recorded_evidence_for(...)`, the
  same function with the same condition; all three sentences name the evidence as the reason and
  none says an agent completed the task ("No agent is recorded as completing it").

**§4.11 — repaired as specified.** `test_the_agent_plane_sees_the_refusal` now approves as the
file's existing `reviewer` fixture (no evidence), assertion still `409 gate_unsatisfied`, docstring
names F306. **Measured that the old fixture is now refused as an author**, with a throwaway test
(written, run, deleted — `git status` showed only the intended files after): `builder`'s `approved`
→ `403`, *"agent 'builder' recorded evidence for this task, which claims the work as its own … No
agent is recorded as completing it …"*.

**A finding in the proposal, and a second moved expectation (§4.11a).** First chunk run after the
edits: **1 failed, 439 passed** (6m20s). The failure was
`test_a_flow_names_what_it_cannot_staff.py::test_an_operator_completed_task_wedged_in_review_is_restaffed`,
refused at its own fixture's `apply_transition(..., "under_review", operator())` by
`_guard_reviewer_is_not_the_author` — the new §3.4 fallback. The fixture is precisely D14's wedge:
`builder` worked the task and recorded its evidence, the operator completed it and moved it into
review still assigned to `builder`. The proposal said the whole-suite blast radius is exactly one
test; that number was R2's, measured against *"§1–§3.4"* when §3.4 still said *do not touch the
entry guard*. **R4 reversed §3.4 and did not re-measure**, so R4's blast radius was unmeasured and
this is it. Repaired the §4.11 way — fixture, not claim: the test is about the wedged-review branch's
*recovery*, and the wedge that can still form is an assignee on the transitions that recorded
nothing, so the evidence is now the operator's (`_evidence` gained `actor_kind=`). File: 24 passed.
Written into `tasks.md` as §4.11a (ticked) so the change's ledger stops saying "one". **§4.8's
whole-suite run is the measurement of record for R4's reach** — a chunk chosen by name is exactly the
guess 4.8 warns about, and this chunk happened to include the file.

**Lint** on the five changed Python files: `py -3.11 -m ruff check` clean, `black --check
--target-version py311` clean. No spec delta touched, so §6.3 not re-run. No migration.

**Not done here, by design:** no mutation (n3), no new test file (n2). §4.6a's and §4.10's
"against the partial tree" halves are n3's.

**Chunk on the final tree (after §4.11a): 440 passed, 0 failed** (7m03s, `py -3.11`, from `hub/`) —
the same 20 files and count as iteration 1's pre-edit baseline. Green is not proof the fix fires;
the throwaway 403 above and the §3.4 refusal that broke the wedge fixture are the evidence it does,
and n2's legs plus n3's mutations are what make that durable.

**Next:** `n2-tests`.

---

## Iteration 3 — n2-tests (2026-09-11 23:40 → 23:55)

**Position confirmed, no reconciliation.** Branch `autonomous/2026-09-08-daily`, `HEAD` = `4929ea0`
(*fix(F306,F316)*) = `origin/autonomous/2026-09-08-daily`, tree clean — matches `STATE-night.json`
(iteration 2, `current: n2-tests`).

**Written:** `hub/tests/test_the_evidence_names_the_author.py`, 12 tests, **12 passed** (7.9s,
`py -3.11`, from `hub/`). With its four neighbours (`test_a_flow_names_what_it_cannot_staff`,
`test_review_dispatch_staffs_the_task`, `test_review_divergence`,
`test_approval_refuses_unaccepted_evidence`): **95 passed, 0 failed** (2m09s). `ruff` clean; `black
--target-version py311` reformatted it once (layout only), then clean. No source file touched, no
mutation performed — `git status` showed only the new file and `tasks.md` before staging.

| leg | test | the fixture shape that carries it |
|---|---|---|
| 4.1 | module docstring + `test_the_evidence_is_the_only_record_naming_the_author` | F306's live ids (`proj-f741298c9c6b`, `r7af142d`, `task-930385ddb031`, `run-e8ff74d5c129`, seq 19); rows read back: transitions all operator, no assignee, no bound run, evidence = `{ev-author}` |
| 4.2 | `…staffs_the_agent_that_did_not_record_the_evidence` | `ev-author` sorts before `ev-reviewer`, **asserted** in the test so a rename cannot defang it |
| 4.3 | `…author_s_approval_is_refused_and_records_nothing` | `ActorNotPermittedError`; status and transition count re-read in a fresh session; message names the evidence and not a completer |
| 4.4 | `…reviewer_the_flow_staffed_is_accepted` | staffed via `decide_firing` + `enter_selected_task`; asserts `assignee == ev-reviewer` **and** that the union names it at that moment, then the approval lands |
| 4.5 | `…operator_s_evidence_leaves_every_agent_eligible` | see below — an agent **named `operator`**, sorting first |
| 4.6 | `…unaccepted_evidence_still_names_its_author[awaiting|rejected]` | offer (`task_is_claimable_by`) and refusal (guard) both asserted |
| 4.6a | `…evidence_author_cannot_be_entered_as_the_reviewer` | one operator `PATCH {assignee, status: under_review}` → 403 at HTTP and `ActorNotPermittedError` at the service; then the same PATCH with `ev-reviewer` → 200 |
| 4.9 | `…dispatching_the_evidence_author_as_reviewer_is_refused_before_the_turn` | `POST /agent/trigger` → 403; `prepare_review_turn` patched and **not called**; task `("completed", None)`; no queue entry; no run |
| (4.9+) | `…direct_dispatch_refuses_the_evidence_author_before_the_checkout` | **added**, not in tasks.md: `trigger_agent_directly` (the flow's path, which bypasses the route) is refused by §3.4 before provisioning |
| 4.10 | `…silent_review_is_not_restaffed_onto_the_agent_that_worked_it` | F316's shape exactly — worker `aa-author` bound by run only, on no transition; asserts R3's numbers: `surfaced`, assignee stays `critic`, zero `divergence` entries |
| 4.10a | `…surfaced_reason_says_worked_on_not_completed` | the `run_diverged` payload's `reason`: contains *"has worked on this task"*, not *"completed"* |

**4.5's fixture, and why it looks odd.** The `actor_kind` filter is only observable when the
operator's evidence carries a name an agent also has — otherwise dropping the filter adds a name that
matches nobody and the leg survives its mutation. That collision is not contrived: `POST
/spec/evidence` records `Actor(kind="operator", name="operator")` (`api/v1/spec.py:820`), and
`operator` is a legal agent name (`AGENT_NAME_RE`; `worktrees.validate_agent_name` reserves only
`user`). So the roster has an agent called `operator`, sorting ahead of `reviewer`, and the leg
asserts the flow staffs it and its approval lands. **My first draft paired it with `ev-reviewer`,
which sorts *before* `operator`** — the test's own sort assertion failed on it (1 failed, 11 passed),
which is the assertion earning its place. Fixed to `reviewer`.

**Ticked 4.1–4.6 only (26/43). Four legs are written and green and deliberately left open:** 4.6a
and 4.10 need their "against the partial tree" halves, and **4.9 and 4.10a each carry a
`Mutation: … must fail` clause inside their own bullet**, so they are not closed by being green. The
previous `next_action` said to tick 4.9 and 4.10a; that would have closed a task on half its
verification, so this is a deliberate narrowing, not an omission. 4.2–4.6's mutations live in 4.7, a
separate task, which is why those five are ticked.

**For n3, predictions to check rather than trust:** 4.6's `task_is_claimable_by(author) is False` may
not be what fails under the `review_state == 'accepted'` mutation — the guard assertion beside it
should be; record which. 4.5 uses `review_state='accepted'` on the operator's row, so it is
unaffected by 4.6's mutation, by design. Running the new file against the `37b8226` tree is **not** a
substitute for the mutation table: it fails at import (`agents_that_recorded_evidence_for` does not
exist there), which says nothing about any leg.

**Next:** `n3-mutations` — from a clean tree with this commit pushed.

---

## Iteration 4 — n3-mutations (2026-09-11 23:52 → 2026-09-12 00:10)

**Position confirmed, no reconciliation.** Branch `autonomous/2026-09-08-daily`, `HEAD` = `40bd429`
(*test(F306,F316)*), tree clean — matches `STATE-night.json` (iteration 3, `current: n3-mutations`).

**All ten mutations, inside this one firing, from a clean tree.** A scratch runner (in `%TEMP%`, not
the repo) applied each edit by exact-anchor replacement (anchor count asserted = 1), ran
`py -3.11 -m pytest -q -p no:cacheprovider -rA --tb=line tests/test_the_evidence_names_the_author.py`
from `hub/`, restored with `git checkout -- <file>` in a `finally`, and aborted unless
`git status --short` was empty. **It was empty after every one of the ten restores**, and again
after two re-runs made to capture failing lines. Unmutated baseline first: 12 passed.

| mut | edit | target leg | target result (first failing line) | also failed | F/P |
|---|---|---|---|---|---|
| a | drop `evidenced` from the union | 4.2 | **FAIL** L236 — flow staffed `ev-author` | 4.1 (L211), 4.4 (L292), 4.6×2 (L386, the offer) | 5/7 |
| b | §3.1 guard fallback off | 4.3 | **FAIL** L262 — `DID NOT RAISE` | 4.6×2 (L388, the refusal) | 3/9 |
| c | §3.1 fallback reads the union | 4.4 | **FAIL** — `ev-reviewer`'s approval refused (D3) | 4.5 (agent `operator`'s approval refused) | 2/10 |
| d | drop `actor_kind == 'agent'` | 4.5 | **FAIL** L345 — `{'operator'} == set()` | — | 1/11 |
| e | add `review_state == 'accepted'` | 4.6 ×2 | **FAIL** L385 — the term is `set()` | 4.1, 4.2, 4.3, 4.4, 4.6a, 4.9, direct | 9/3 |
| f | §3.4 entry fallback off (= the §1–§3.3 tree) | 4.6a | **FAIL** L422 — `DID NOT RAISE` | direct dispatch (L532, `409 != 403`) | 2/10 |
| g | §3.4 fallback reads the union | 4.6a permissive | **FAIL** L447 — `ev-reviewer` refused `403` | 4.4, 4.5, 4.10, 4.10a | 5/7 |
| h | §3.5 dispatch fallback off | 4.9 | **FAIL** L498 — task `("completed", "ev-author")` | — | 1/11 |
| i | `_answer_failed_review` to pre-fix (completer alone, default reason) | 4.10 (+4.10a) | **FAIL** L639 — `restaffed`; 4.10a L672 `restaffed != surfaced` | — | 2/10 |
| j | drop `excluded_because` only | 4.10a | **FAIL** L675 — reason says *"is the one that completed this task"* | — | 1/11 |

**No leg survived its own mutation, so no fixture changed.** Survivors worth naming: under (e), 4.5
passes by design (the operator's row is `accepted`), and 4.10/4.10a pass because they carry no
evidence. Under (g), the four extra failures raised from inside the source rather than at a test
assertion — consistent with the entry guard refusing each fixture's own staffing of an assignee;
the messages were not captured, so that reading is **inferred, not shown**.

**The predictions iteration 3 asked me to check, rather than trust.** *"Under (e), the guard
assertion should fail rather than `task_is_claimable_by`"* — **neither does, first**: L385, the
direct assertion on the term that sits before both, fails first, and pytest stops there. The offer
assertion (L386) is caught on its own by (a) and the refusal (L388) by (b), so each of 4.6's three
assertions has a mutation that reaches it. That L386/L388 would *also* fail under (e) is derived
(an empty term leaves the author in no source), **not observed**.

**4.10's "against today's tree" half cannot be run literally** — the file does not import at
`37b8226`. (i) is the stand-in: the pre-fix composition, verbatim (`agent_that_completed` alone,
the resolver's default reason), on the fixed tree, where nothing else reaches this exclusion. It
reproduced R3's number for the unfixed tree — `restaffed`.

**(h) is the finding.** With §3.5 removed, `POST /agent/trigger` still answered `403` with the
evidence sentence and provisioned nothing — the leg's status, sentence and `assert_not_called`
lines all passed. Only the holder assertion failed. The scheduler path: the route queues and
commits, `schedule_agent` → `trigger_agent_directly` → `enter_selected_task` writes the assignee →
§3.4 refuses → `turn_scheduler.py:349` writes `waiting_reason` and **commits the same session**,
assignee included; F108 then answers 403. The comment at `agent_trigger.py:788-793` says a refusal
abandons the staging, which holds only for a caller that never commits.

**Measured on the unmutated tree**, not only under the mutation: a scratch test (created in
`hub/tests/`, run once, deleted; `git status` clean after) queued an operator entry for `ev-author`
with `review_task_id` on F306's task and called `schedule_agent`. Task read back `("completed",
"ev-author")`, entry still `queued` with §3.4's sentence as `waiting_reason`, provisioning not
called. **Filed as `F319 (B)`** with reach split into measured and unverified; the
`ReviewTurnRefused`-after-staging leg would make it A and is unverified. Not fixed: no proposal,
and this window does not write one.

Classifier census, `F313`'s rule: captured before the ledger edit and after. Diff is exactly
`319 finding sections` (was 318) and `severity B: total 128 … OPEN=65` (was 127/64), with `F319`
appended to the OPEN-B list. No other line moved.

**Two test docstrings corrected, no assertion touched.** 4.9's said that without §3.5 the request
is *"queued and answered `200`"* — F108 made that false before this change existed; it now says
the holder is the assertion that carries the leg and why. The direct-dispatch test's said the
staging is *"abandoned"* — it reads `None` only because that test never commits; it now says it
asserts the ordering, not the rollback. File green after: 12 passed; `ruff`, `black --target-version
py311` clean. `openspec validate --strict` valid.

**Ticked 4.6a, 4.7, 4.9, 4.10, 4.10a — 31/43**, each with a one-line measured note in `tasks.md`.

**Next:** `n4-gate` — the whole Hub suite, the first measurement of R4's §3.4 blast radius.

---

## Iteration 5 — n4-gate (2026-09-12 00:10 → 00:40)

**Position confirmed, no reconciliation.** Branch `autonomous/2026-09-08-daily`, `HEAD` = `99d64fc`
(*mutate(F306,F316)*), tree clean. Matches `STATE-night.json` (iteration 4, `current: n4-gate`).

**§4.8 / §6.2, the whole Hub suite, on the finished tree:** `py -3.11 -m pytest tests/ -q
-p no:cacheprovider -rfE --tb=short` from `hub/`, output to `%TEMP%\n4-hub-suite.txt`. Result:
**4057 passed, 86 skipped, 0 failed, 0 errors, 255 warnings in 1626.70s (27m06s), exit 0.** No
`short test summary` block was printed. This was the first whole-suite measurement of R4's §3.4
reversal, and **it moved no expectation beyond the two already repaired (4.11, 4.11a)**. No 4.11x
was needed. Neither known intermittent fired: `F314` and `F292` produced no failure and no setup
error, so this run has nothing to attribute. That is one sample, not evidence that either one is
gone.

The count agrees with the proposal's arithmetic, since R2's prototype run was 1 failed + 4044 passed
and this change adds 12 tests: 4045 + 12 = 4057. That is **consistent with** an otherwise unchanged
suite, not proof of one. The 255 warnings include 26 `PytestUnhandledThreadExceptionWarning`s
(aiosqlite worker threads outliving a closed event loop, e.g.
`test_task_turn_collision.py::test_a_holder_whose_tail_raises_still_releases_the_task_it_held`).
They are pre-existing noise this change does not touch, recorded here but not investigated.

**§6.1, CI's exact lint set, all under `py -3.11 -m`:** `ruff check src/ hub/ tests/` → `All checks
passed!`; `black --check --target-version py311 src/ hub/hub/ hub/tests/ tests/` → `565 files would
be left unchanged`; `mypy src/` → `no issues found in 22 source files`. The change touches no
`hub/ui` file (`git diff --stat 37b8226..`), so `npm run lint` is not in its reach.

**§6.4:** `git diff 37b8226.. -- hub/hub/migrations` is empty; the diff of `test_migrations.py` +
`test_project_persistence.py` is 0 lines. **§6.3:** `openspec validate --strict` gives `is valid`,
before and after the tasks.md edits.

**Ticked 4.8, 6.1, 6.2, 6.3, 6.4 → 36/43**, each with a measured note. Still open: 5.1–5.4 (the
drives) and 7.0–7.2 (ledger and archive).

**Next:** `n5-playbook`, the doc-only repair of `night-window.md`'s drive-Hub recipe, which names the
deleted `beta` profile. It must land before `n6` so the drive runs from a correct instruction.

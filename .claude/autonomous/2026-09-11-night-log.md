# 2026-09-11 night window

Newest entry at the BOTTOM. Playbook: `.claude/loops/night-window.md`.

Armed 2026-09-11T22:55:07+01:00 on `autonomous/2026-09-08-daily` (parent master@6f7e486), model opus.

---

## Iteration 1 — compose (2026-09-11 23:05 → 23:20)

**Position confirmed, no reconciliation.** Branch `autonomous/2026-09-08-daily`, `HEAD` = `37b8226`
(*arm(night)*) = `origin/autonomous/2026-09-08-daily`, tree clean. `master` = `origin/master` =
`6f7e486` = `STATE-night.json`'s `parent_sha`; the branch is 11 commits ahead of it. No branch cut.

**APPROVALS.md, 2026-09-11 section (the newest):** exactly one row with a token —
`APPROVED an-agent-that-recorded-the-evidence-is-the-author` (F306 A + F316 A, 42 tasks, 0 ticked).
No `ORDER:`, no `NOTHING TONIGHT`. The row carries four operator conditions that the queue encodes
rather than paraphrases: verify it as **two** findings (§7.0 and §7.1 each), verify it as **five**
call sites (§3.4, R4's reversal, is not optional), §5's three live drives are in scope with a
`git worktree` and **no `git stash`**, and the §4 mutation-hygiene block governs every revert.

**Backlog, measured before queueing the approved work:**
- Source 1 (unarchived, implemented, needs archiving): **empty.** `openspec/changes/` holds one
  non-archive directory, the approved change itself, at 0/42.
- Source 2 (open findings with a proposal): the open severity-A findings are F299, F300, F301, F306,
  F312, F316. Only F306 and F316 have a proposal, and it is the approved change. The other four go to
  `notes_for_tomorrow` — the playbook routes a finding with no proposal to the day window. F318 (B,
  classifier) and F292 (B) are likewise unproposed and also noted, not queued; the FILL note's
  suggestion of an F292 "concurrent sampler" is not built tonight for that reason.

**Tree green before building on it — measured.** The relevant chunk, not the whole suite: every Hub
test file that names one of the five functions this change edits (`grep` over `hub/tests` for
`agents_that_may_have_authored|_guard_author_is_not_reviewer|_guard_reviewer_is_not_the_author|
review_dispatch_refusal|_answer_failed_review|record_evidence`, 17 files) plus R3's three divergence
suites. **440 passed, 0 failed, 5m49s** under `py -3.11`, from `hub/`. The exact command is in
`n1-source`'s detail so the next firing can re-run the same set after its edits.

**One repair queued rather than only noted** (`n5-playbook`): `night-window.md:120` still tells the
drive to use `~/.agentweave/hub/profiles/beta/agentweave.db`. There is no `beta` profile —
`ls ~/.agentweave/hub/profiles` reads `drive0910 f274p0 live trial` — and the 2026-09-10 night log
recorded exactly this at its line 122 without repairing the playbook. Its port, 8011, was also
already occupied last night. Queued before the first drive so the drive runs from a correct
instruction; this iteration does no other work, per the playbook.

**The queue**, nine items, ordered so stopping anywhere leaves committed, coherent work:
`compose` (done) → `n1-source` (§1–§3, five call sites, plus §4.11's fixture) → `n2-tests` (the new
file, green) → `n3-mutations` (§4.7 and the four other mutation legs, one firing, clean tree after
every restore) → `n4-gate` (whole Hub suite + CI lint set) → `n5-playbook` → `n6-drive-prefix`
(§5.1 in a worktree at `37b8226`, proven only by the `task_transitions` row) → `n7-drive-fixed`
(§5.2/§5.3, plus the §3.4 wedge and §3.5 refusal by hand) → `n8-close` (two findings, archive).

`prefix_sha` is recorded as `37b8226`: every commit before `n1-source`'s carries the same Python,
so that sha is a valid pre-fix tree for §5.1's worktree.

---

## Iteration 2 — n1-source (2026-09-11 23:10 → 23:40)

**Position confirmed, no reconciliation.** Branch `autonomous/2026-09-08-daily`, `HEAD` = `de5733e`
(*compose(night)*), tree clean — matches `STATE-night.json` (iteration 1, `current: n1-source`).

**What was implemented** (`tasks.md` §1, §2, §3, §4.11, plus a new §4.11a — 20 of 43 ticked):

- `task_transition_service.py` — `agents_that_recorded_evidence_for(session, task_id)`: distinct
  `RequirementEvidence.actor`, `actor_kind == 'agent'`, non-empty, **no** `review_state` filter; the
  docstring says why for each and names its three consumers, two of which use it alone (§1.1–1.4).
  `RequirementEvidence` joined the existing `db.models` import; `import hub.task_transition_service`
  is clean. The union gains it as a fourth source, with a fourth docstring row (§2.1–2.2).
  `_guard_author_is_not_reviewer` falls back to the evidence term **alone**, after the operator
  exemption and `_REVIEW_OUTCOMES` gate, only where no agent is recorded as completing (§3.1–3.3).
  `_guard_reviewer_is_not_the_author` takes the same fallback on `task.assignee`, and its
  "no recorded completer" bullet is now conditional with the D14 reasoning (§3.4/3.4a/3.4b).
- `scheduler.py:625` and `:1574` read, not edited: both call `agents_that_may_have_authored` after
  `completion_attribution` (§2.3). No recomposition found there.
- `run_divergence._answer_failed_review` — the completer-only term replaced by the shared two-branch
  derivation (`completion_attribution`, then the union where no agent completed), silent reviewers
  and `run.agent` kept local; `excluded_because="has worked on this task"` on the no-agent branch,
  default kept on the agent branch; the docstring's *"excludes the author by construction"* claim
  corrected (§2.4/2.4a/2.5).
- `api/v1/agent_trigger.review_dispatch_refusal` — 403 where no agent completed and the reviewer
  recorded evidence, placed after the status and holder checks and the completer comparison, so the
  refusal order an operator meets is unchanged (§3.5).
- **§3.6, checked:** §3.1, §3.4 and §3.5 are one rule stated at three layers — each reads
  `agent_that_completed(...) is None and <agent> in agents_that_recorded_evidence_for(...)`, the
  same function with the same condition; all three sentences name the evidence as the reason and
  none says an agent completed the task ("No agent is recorded as completing it").

**§4.11 — repaired as specified.** `test_the_agent_plane_sees_the_refusal` now approves as the
file's existing `reviewer` fixture (no evidence), assertion still `409 gate_unsatisfied`, docstring
names F306. **Measured that the old fixture is now refused as an author**, with a throwaway test
(written, run, deleted — `git status` showed only the intended files after): `builder`'s `approved`
→ `403`, *"agent 'builder' recorded evidence for this task, which claims the work as its own … No
agent is recorded as completing it …"*.

**A finding in the proposal, and a second moved expectation (§4.11a).** First chunk run after the
edits: **1 failed, 439 passed** (6m20s). The failure was
`test_a_flow_names_what_it_cannot_staff.py::test_an_operator_completed_task_wedged_in_review_is_restaffed`,
refused at its own fixture's `apply_transition(..., "under_review", operator())` by
`_guard_reviewer_is_not_the_author` — the new §3.4 fallback. The fixture is precisely D14's wedge:
`builder` worked the task and recorded its evidence, the operator completed it and moved it into
review still assigned to `builder`. The proposal said the whole-suite blast radius is exactly one
test; that number was R2's, measured against *"§1–§3.4"* when §3.4 still said *do not touch the
entry guard*. **R4 reversed §3.4 and did not re-measure**, so R4's blast radius was unmeasured and
this is it. Repaired the §4.11 way — fixture, not claim: the test is about the wedged-review branch's
*recovery*, and the wedge that can still form is an assignee on the transitions that recorded
nothing, so the evidence is now the operator's (`_evidence` gained `actor_kind=`). File: 24 passed.
Written into `tasks.md` as §4.11a (ticked) so the change's ledger stops saying "one". **§4.8's
whole-suite run is the measurement of record for R4's reach** — a chunk chosen by name is exactly the
guess 4.8 warns about, and this chunk happened to include the file.

**Lint** on the five changed Python files: `py -3.11 -m ruff check` clean, `black --check
--target-version py311` clean. No spec delta touched, so §6.3 not re-run. No migration.

**Not done here, by design:** no mutation (n3), no new test file (n2). §4.6a's and §4.10's
"against the partial tree" halves are n3's.

**Chunk on the final tree (after §4.11a): 440 passed, 0 failed** (7m03s, `py -3.11`, from `hub/`) —
the same 20 files and count as iteration 1's pre-edit baseline. Green is not proof the fix fires;
the throwaway 403 above and the §3.4 refusal that broke the wedge fixture are the evidence it does,
and n2's legs plus n3's mutations are what make that durable.

**Next:** `n2-tests`.

---

## Iteration 3 — n2-tests (2026-09-11 23:40 → 23:55)

**Position confirmed, no reconciliation.** Branch `autonomous/2026-09-08-daily`, `HEAD` = `4929ea0`
(*fix(F306,F316)*) = `origin/autonomous/2026-09-08-daily`, tree clean — matches `STATE-night.json`
(iteration 2, `current: n2-tests`).

**Written:** `hub/tests/test_the_evidence_names_the_author.py`, 12 tests, **12 passed** (7.9s,
`py -3.11`, from `hub/`). With its four neighbours (`test_a_flow_names_what_it_cannot_staff`,
`test_review_dispatch_staffs_the_task`, `test_review_divergence`,
`test_approval_refuses_unaccepted_evidence`): **95 passed, 0 failed** (2m09s). `ruff` clean; `black
--target-version py311` reformatted it once (layout only), then clean. No source file touched, no
mutation performed — `git status` showed only the new file and `tasks.md` before staging.

| leg | test | the fixture shape that carries it |
|---|---|---|
| 4.1 | module docstring + `test_the_evidence_is_the_only_record_naming_the_author` | F306's live ids (`proj-f741298c9c6b`, `r7af142d`, `task-930385ddb031`, `run-e8ff74d5c129`, seq 19); rows read back: transitions all operator, no assignee, no bound run, evidence = `{ev-author}` |
| 4.2 | `…staffs_the_agent_that_did_not_record_the_evidence` | `ev-author` sorts before `ev-reviewer`, **asserted** in the test so a rename cannot defang it |
| 4.3 | `…author_s_approval_is_refused_and_records_nothing` | `ActorNotPermittedError`; status and transition count re-read in a fresh session; message names the evidence and not a completer |
| 4.4 | `…reviewer_the_flow_staffed_is_accepted` | staffed via `decide_firing` + `enter_selected_task`; asserts `assignee == ev-reviewer` **and** that the union names it at that moment, then the approval lands |
| 4.5 | `…operator_s_evidence_leaves_every_agent_eligible` | see below — an agent **named `operator`**, sorting first |
| 4.6 | `…unaccepted_evidence_still_names_its_author[awaiting|rejected]` | offer (`task_is_claimable_by`) and refusal (guard) both asserted |
| 4.6a | `…evidence_author_cannot_be_entered_as_the_reviewer` | one operator `PATCH {assignee, status: under_review}` → 403 at HTTP and `ActorNotPermittedError` at the service; then the same PATCH with `ev-reviewer` → 200 |
| 4.9 | `…dispatching_the_evidence_author_as_reviewer_is_refused_before_the_turn` | `POST /agent/trigger` → 403; `prepare_review_turn` patched and **not called**; task `("completed", None)`; no queue entry; no run |
| (4.9+) | `…direct_dispatch_refuses_the_evidence_author_before_the_checkout` | **added**, not in tasks.md: `trigger_agent_directly` (the flow's path, which bypasses the route) is refused by §3.4 before provisioning |
| 4.10 | `…silent_review_is_not_restaffed_onto_the_agent_that_worked_it` | F316's shape exactly — worker `aa-author` bound by run only, on no transition; asserts R3's numbers: `surfaced`, assignee stays `critic`, zero `divergence` entries |
| 4.10a | `…surfaced_reason_says_worked_on_not_completed` | the `run_diverged` payload's `reason`: contains *"has worked on this task"*, not *"completed"* |

**4.5's fixture, and why it looks odd.** The `actor_kind` filter is only observable when the
operator's evidence carries a name an agent also has — otherwise dropping the filter adds a name that
matches nobody and the leg survives its mutation. That collision is not contrived: `POST
/spec/evidence` records `Actor(kind="operator", name="operator")` (`api/v1/spec.py:820`), and
`operator` is a legal agent name (`AGENT_NAME_RE`; `worktrees.validate_agent_name` reserves only
`user`). So the roster has an agent called `operator`, sorting ahead of `reviewer`, and the leg
asserts the flow staffs it and its approval lands. **My first draft paired it with `ev-reviewer`,
which sorts *before* `operator`** — the test's own sort assertion failed on it (1 failed, 11 passed),
which is the assertion earning its place. Fixed to `reviewer`.

**Ticked 4.1–4.6 only (26/43). Four legs are written and green and deliberately left open:** 4.6a
and 4.10 need their "against the partial tree" halves, and **4.9 and 4.10a each carry a
`Mutation: … must fail` clause inside their own bullet**, so they are not closed by being green. The
previous `next_action` said to tick 4.9 and 4.10a; that would have closed a task on half its
verification, so this is a deliberate narrowing, not an omission. 4.2–4.6's mutations live in 4.7, a
separate task, which is why those five are ticked.

**For n3, predictions to check rather than trust:** 4.6's `task_is_claimable_by(author) is False` may
not be what fails under the `review_state == 'accepted'` mutation — the guard assertion beside it
should be; record which. 4.5 uses `review_state='accepted'` on the operator's row, so it is
unaffected by 4.6's mutation, by design. Running the new file against the `37b8226` tree is **not** a
substitute for the mutation table: it fails at import (`agents_that_recorded_evidence_for` does not
exist there), which says nothing about any leg.

**Next:** `n3-mutations` — from a clean tree with this commit pushed.

---

## Iteration 4 — n3-mutations (2026-09-11 23:52 → 2026-09-12 00:10)

**Position confirmed, no reconciliation.** Branch `autonomous/2026-09-08-daily`, `HEAD` = `40bd429`
(*test(F306,F316)*), tree clean — matches `STATE-night.json` (iteration 3, `current: n3-mutations`).

**All ten mutations, inside this one firing, from a clean tree.** A scratch runner (in `%TEMP%`, not
the repo) applied each edit by exact-anchor replacement (anchor count asserted = 1), ran
`py -3.11 -m pytest -q -p no:cacheprovider -rA --tb=line tests/test_the_evidence_names_the_author.py`
from `hub/`, restored with `git checkout -- <file>` in a `finally`, and aborted unless
`git status --short` was empty. **It was empty after every one of the ten restores**, and again
after two re-runs made to capture failing lines. Unmutated baseline first: 12 passed.

| mut | edit | target leg | target result (first failing line) | also failed | F/P |
|---|---|---|---|---|---|
| a | drop `evidenced` from the union | 4.2 | **FAIL** L236 — flow staffed `ev-author` | 4.1 (L211), 4.4 (L292), 4.6×2 (L386, the offer) | 5/7 |
| b | §3.1 guard fallback off | 4.3 | **FAIL** L262 — `DID NOT RAISE` | 4.6×2 (L388, the refusal) | 3/9 |
| c | §3.1 fallback reads the union | 4.4 | **FAIL** — `ev-reviewer`'s approval refused (D3) | 4.5 (agent `operator`'s approval refused) | 2/10 |
| d | drop `actor_kind == 'agent'` | 4.5 | **FAIL** L345 — `{'operator'} == set()` | — | 1/11 |
| e | add `review_state == 'accepted'` | 4.6 ×2 | **FAIL** L385 — the term is `set()` | 4.1, 4.2, 4.3, 4.4, 4.6a, 4.9, direct | 9/3 |
| f | §3.4 entry fallback off (= the §1–§3.3 tree) | 4.6a | **FAIL** L422 — `DID NOT RAISE` | direct dispatch (L532, `409 != 403`) | 2/10 |
| g | §3.4 fallback reads the union | 4.6a permissive | **FAIL** L447 — `ev-reviewer` refused `403` | 4.4, 4.5, 4.10, 4.10a | 5/7 |
| h | §3.5 dispatch fallback off | 4.9 | **FAIL** L498 — task `("completed", "ev-author")` | — | 1/11 |
| i | `_answer_failed_review` to pre-fix (completer alone, default reason) | 4.10 (+4.10a) | **FAIL** L639 — `restaffed`; 4.10a L672 `restaffed != surfaced` | — | 2/10 |
| j | drop `excluded_because` only | 4.10a | **FAIL** L675 — reason says *"is the one that completed this task"* | — | 1/11 |

**No leg survived its own mutation, so no fixture changed.** Survivors worth naming: under (e), 4.5
passes by design (the operator's row is `accepted`), and 4.10/4.10a pass because they carry no
evidence. Under (g), the four extra failures raised from inside the source rather than at a test
assertion — consistent with the entry guard refusing each fixture's own staffing of an assignee;
the messages were not captured, so that reading is **inferred, not shown**.

**The predictions iteration 3 asked me to check, rather than trust.** *"Under (e), the guard
assertion should fail rather than `task_is_claimable_by`"* — **neither does, first**: L385, the
direct assertion on the term that sits before both, fails first, and pytest stops there. The offer
assertion (L386) is caught on its own by (a) and the refusal (L388) by (b), so each of 4.6's three
assertions has a mutation that reaches it. That L386/L388 would *also* fail under (e) is derived
(an empty term leaves the author in no source), **not observed**.

**4.10's "against today's tree" half cannot be run literally** — the file does not import at
`37b8226`. (i) is the stand-in: the pre-fix composition, verbatim (`agent_that_completed` alone,
the resolver's default reason), on the fixed tree, where nothing else reaches this exclusion. It
reproduced R3's number for the unfixed tree — `restaffed`.

**(h) is the finding.** With §3.5 removed, `POST /agent/trigger` still answered `403` with the
evidence sentence and provisioned nothing — the leg's status, sentence and `assert_not_called`
lines all passed. Only the holder assertion failed. The scheduler path: the route queues and
commits, `schedule_agent` → `trigger_agent_directly` → `enter_selected_task` writes the assignee →
§3.4 refuses → `turn_scheduler.py:349` writes `waiting_reason` and **commits the same session**,
assignee included; F108 then answers 403. The comment at `agent_trigger.py:788-793` says a refusal
abandons the staging, which holds only for a caller that never commits.

**Measured on the unmutated tree**, not only under the mutation: a scratch test (created in
`hub/tests/`, run once, deleted; `git status` clean after) queued an operator entry for `ev-author`
with `review_task_id` on F306's task and called `schedule_agent`. Task read back `("completed",
"ev-author")`, entry still `queued` with §3.4's sentence as `waiting_reason`, provisioning not
called. **Filed as `F319 (B)`** with reach split into measured and unverified; the
`ReviewTurnRefused`-after-staging leg would make it A and is unverified. Not fixed: no proposal,
and this window does not write one.

Classifier census, `F313`'s rule: captured before the ledger edit and after. Diff is exactly
`319 finding sections` (was 318) and `severity B: total 128 … OPEN=65` (was 127/64), with `F319`
appended to the OPEN-B list. No other line moved.

**Two test docstrings corrected, no assertion touched.** 4.9's said that without §3.5 the request
is *"queued and answered `200`"* — F108 made that false before this change existed; it now says
the holder is the assertion that carries the leg and why. The direct-dispatch test's said the
staging is *"abandoned"* — it reads `None` only because that test never commits; it now says it
asserts the ordering, not the rollback. File green after: 12 passed; `ruff`, `black --target-version
py311` clean. `openspec validate --strict` valid.

**Ticked 4.6a, 4.7, 4.9, 4.10, 4.10a — 31/43**, each with a one-line measured note in `tasks.md`.

**Next:** `n4-gate` — the whole Hub suite, the first measurement of R4's §3.4 blast radius.

---

## Iteration 5 — n4-gate (2026-09-12 00:10 → 00:40)

**Position confirmed, no reconciliation.** Branch `autonomous/2026-09-08-daily`, `HEAD` = `99d64fc`
(*mutate(F306,F316)*), tree clean. Matches `STATE-night.json` (iteration 4, `current: n4-gate`).

**§4.8 / §6.2, the whole Hub suite, on the finished tree:** `py -3.11 -m pytest tests/ -q
-p no:cacheprovider -rfE --tb=short` from `hub/`, output to `%TEMP%\n4-hub-suite.txt`. Result:
**4057 passed, 86 skipped, 0 failed, 0 errors, 255 warnings in 1626.70s (27m06s), exit 0.** No
`short test summary` block was printed. This was the first whole-suite measurement of R4's §3.4
reversal, and **it moved no expectation beyond the two already repaired (4.11, 4.11a)**. No 4.11x
was needed. Neither known intermittent fired: `F314` and `F292` produced no failure and no setup
error, so this run has nothing to attribute. That is one sample, not evidence that either one is
gone.

The count agrees with the proposal's arithmetic, since R2's prototype run was 1 failed + 4044 passed
and this change adds 12 tests: 4045 + 12 = 4057. That is **consistent with** an otherwise unchanged
suite, not proof of one. The 255 warnings include 26 `PytestUnhandledThreadExceptionWarning`s
(aiosqlite worker threads outliving a closed event loop, e.g.
`test_task_turn_collision.py::test_a_holder_whose_tail_raises_still_releases_the_task_it_held`).
They are pre-existing noise this change does not touch, recorded here but not investigated.

**§6.1, CI's exact lint set, all under `py -3.11 -m`:** `ruff check src/ hub/ tests/` → `All checks
passed!`; `black --check --target-version py311 src/ hub/hub/ hub/tests/ tests/` → `565 files would
be left unchanged`; `mypy src/` → `no issues found in 22 source files`. The change touches no
`hub/ui` file (`git diff --stat 37b8226..`), so `npm run lint` is not in its reach.

**§6.4:** `git diff 37b8226.. -- hub/hub/migrations` is empty; the diff of `test_migrations.py` +
`test_project_persistence.py` is 0 lines. **§6.3:** `openspec validate --strict` gives `is valid`,
before and after the tasks.md edits.

**Ticked 4.8, 6.1, 6.2, 6.3, 6.4 → 36/43**, each with a measured note. Still open: 5.1–5.4 (the
drives) and 7.0–7.2 (ledger and archive).

**Next:** `n5-playbook`, the doc-only repair of `night-window.md`'s drive-Hub recipe, which names the
deleted `beta` profile. It must land before `n6` so the drive runs from a correct instruction.

## Iteration 6 — n5-playbook (2026-09-12 00:40 → 01:00)

**Position confirmed, no reconciliation.** Branch `autonomous/2026-09-08-daily`, `HEAD` = `c2acf2a`
(*gate(F306,F316)*), tree clean. Matches `STATE-night.json` (iteration 5, `current: n5-playbook`).

**What changed (documentation only, no product code, no test).**

- `.claude/loops/night-window.md`, *Driving*. The recipe named `profiles/beta/agentweave.db`, deleted
  2026-09-07, and a fixed port 8011. It now says: pick a **drive port** each night only after
  `netstat -ano | grep LISTENING` shows it free, never 8000 and never 8010, and record it in the log.
  The database is a fresh **`profiles/drive<MMDD>/agentweave.db`**. The launch is still from `hub/`
  with `py -3.11 -m uvicorn`, never `agentweave --port`. The "restart from the implementing code,
  confirm no `.py` newer than process start" instruction is kept unchanged. The paragraph above it
  now says "restart the drive Hub" instead of "restart 8011".
- **Two gaps the recipe never covered, which I found by running it.** (1) A source launch writes
  **no `bootstrap-key.txt`**. The probe profile held only `agentweave.db` and
  `instance_identity.json`, and so does last night's `drive0910`. `_seed_operator_credential`
  (`engine.py:333-341`) mints a random key into the database unless `AW_BOOTSTRAP_API_KEY` is set,
  so the recipe now sets that variable. (2) `scripts/drive/aw.py:14` defaults `AW_HUB` to
  `http://127.0.0.1:8010`, **the trial Hub**. A harness run without `AW_HUB` exported would drive
  the wrong instance. The recipe now requires exporting `AW_HUB` and `AW_KEY` explicitly.
- **The same fixed port appeared in two more live instructions, and I repaired both** rather than
  just recording them, which is the pattern the Limits forbid. `.claude/loops/day-window.md:182`
  ("The drive Hub is **8011**") now points at the night recipe. In
  `.claude/skills/daily-review/SKILL.md:113`, "8010 and 8011 are the trial Hubs" now says 8010 is the
  trial Hub and the drive port is whatever that night's log records.

**Driven, not only written.** I ran the recipe twice on port 8013, after `netstat` showed no 80xx
listener at all (neither 8000 nor 8010 was running at 00:40).

- Run 1 used `DATABASE_URL` pointing into a **missing** directory `%TEMP%/n5probe/`. The launch
  created the directory, ran migrations up to **`0101 -> 0102`**, and printed `Uvicorn running on
  http://127.0.0.1:8013`. That confirms the "the launch creates it" line, which cites `init_db`
  (`engine.py:344-349`).
- Run 2 repeated run 1 with `AW_BOOTSTRAP_API_KEY=aw_live_<32 hex>`. `GET /api/v1/projects` returned
  **200** with that key as `Authorization: Bearer` and **401** with a wrong one. `aw.HUB` read
  `http://127.0.0.1:8013` under an exported `AW_HUB`.
- Both probe Hubs were stopped (the second by PID 16656; afterwards `8013 free`) and `%TEMP%/n5probe*`
  was removed.

**Noticed, not acted on.** The migration head on this branch is **`0102`**. CLAUDE.md's trial-Hub
section says the trial database was "migrated to head `0101`" and that "this checkout is at
`0101`". Both statements were true when written and are now one behind. That file belongs to the
operator and is not in this item's scope. I'm recording it for the morning.

After the edits, `grep -rn "profiles/beta\|8011" .claude/loops .claude/skills CLAUDE.md` shows only
historical mentions: the 2026-09-10 note in the new recipe, and CLAUDE.md's list of deleted profiles.

**Next:** `n6-drive-prefix`, which reproduces F306 on the pre-fix tree `37b8226` from a git worktree,
using the recipe as repaired here.

## Iteration 7 — n6-drive-prefix (2026-09-12 00:45 → 01:05)

**Position confirmed, no reconciliation.** Branch `autonomous/2026-09-08-daily`, `HEAD` = `96d79d0`
(*playbook(night)*), tree clean. Matches `STATE-night.json` (iteration 6, `current: n6-drive-prefix`).

**Setup, following the recipe n5 repaired.** `git worktree add ../aw-f306-prefix 37b8226`. From its
`hub/`, `import hub` resolved to `…\aw-f306-prefix\hub\hub\__init__.py`, and
`agents_that_recorded_evidence_for` is absent from its `task_transition_service.py`, so this is the
pre-fix tree and not the fixed one. No `git stash` was used. `netstat` showed **no 80xx listener at
all**, so the **drive port was 8014**. The profile was a fresh `profiles/drive0912p`, and the launch
migrated it to `0102`. `AW_BOOTSTRAP_API_KEY` was set, and it answered 200. The Hub PID was
**22856**, with command line `python.exe -m uvicorn hub.main:app --port 8014`. The worktree's
`git status` was still empty at teardown, so nothing it served changed under it. Fixtures were
built with `setup_row7.py` (F306's own) in `%TEMP%\n6\fixture{,2}`: git-initialised, two Haiku agents
bound to a `claude-haiku-4-5-20251001` runner, and a third unbound agent that is not eligible. The
author is `r7a…`, which sorts first. `AW_HUB`/`AW_KEY` were exported on every run.

**Attempt 1 — `proj-7a9ec0f070ba`, `task-11fd55c52ee1`. It did NOT reproduce the approval.** The
flow's firing 2 **did staff the evidence author**. I read it live: `under_review`, assignee
`r7af306p`, and `r7af306p` running `run-b9d360b2d62c`, bound to the task, initiator `autonomous`.
Its transcript (agent_outputs seq 29–47) shows a review summary, then *"Now I'll approve the
task"*, then `ToolSearch select:mcp__agentweave__update_task` twice with empty output, then a
`/tmp` script that was denied, then the turn ended **without ever calling `update_task`**. The MCP
adapter *was* online for that run (`mcp_adapter_online_at` 23:48:17), so this was Haiku not
following through, not the Hub stopping it. The silent review diverged, giving `div-c524b3378859`,
`policy_applied='review'`, `outcome='restaffed'`, `previous_assignee='r7af306p'`. It was restaffed
onto `r7bf306p`, which approved: **seq 4 `under_review→approved`, `actor_agent='r7bf306p'`**,
`run-74b332b155e8`. The evidence row is `ev-fcf88cebab7b`, `actor='r7af306p'`. **No approving row by
the evidence author exists, so attempt 1 is not a reproduction, and I have not counted it as one.**
The harness printed 15/19. Its four BADs are all in section E: the restaffed reviewer has no
`origin_type='job'` queue entry, because the restaff path is not the flow's briefing. That is a
harness assumption meeting a route it was not written for. I have recorded it and not investigated.

**Attempt 2 — `proj-34d006e2f3e5`, `task-9ba36b0bd788`. F306 is reproduced, and the tables prove it.**

| table | row |
|---|---|
| `task_transitions` | seq 5 `pending→in_progress`, seq 6 `in_progress→completed`, seq 7 `completed→under_review`: all `actor_kind='operator'`, `actor_agent` NULL |
| `task_transitions` | **seq 8 `ttr-57e08d170d7b`, `under_review→approved`, `actor_kind='run'`, `actor_agent='r7af306q'`, `run-bb80909c0e15`** |
| `requirement_evidence` | **`ev-4d13beb472c4`, `implementation`, `actor_kind='agent'`, `actor='r7af306q'`, `accepted`**, on `run-1a7f8b790e34` |
| `runs` | `run-1a7f8b790e34`: `task_id` **NULL**, initiator `operator` (the unbound work turn); `run-bb80909c0e15`: bound, initiator `autonomous` (the review) |
| `run_divergences` | none for this task |

The approving transition's `actor_agent` equals `requirement_evidence.actor`, with every
pre-review transition made by the operator. That is F306's row, on the tree before §1. **The drive
itself printed 19/19**, including *"with NOBODY excluded — any bound agent may take it"*. As §5.1
warned, green is exactly what a run on the wrong tree would also have shown.

**Tally, stated as measured:** the flow staffed the author 2/2, and the author approved 1/2. The
other time, the author failed to make the tool call and was restaffed. Two samples settle nothing
about the ladder's ordering. They are consistent with F306's "Unverified: whether ordering is
deterministic" leaning towards "the author, who sorts first, is picked".

**Teardown.** Jobs: `GET /jobs` returned `[]` on both projects, checked by API after the harness's
own disable+archive. The Hub was stopped by PID 22856, leaving no listener on 8014, only TIME_WAIT
sockets. `git worktree remove ../aw-f306-prefix` + `prune`, so only the main worktree is listed.
No 80xx listener remains, and 8000/8010 were never touched. Transcripts are kept beside the kept
database in `~/.agentweave/hub/profiles/drive0912p/n6-transcripts/` (`attempt1-drive.txt`,
`attempt2-drive.txt`, `hub-8014.log`), and the `drive0912p` database is kept too. `%TEMP%\n6`
(fixtures + scratch) is left in place.

**Ticked 5.1 → 37/43**, with the note above in `tasks.md`. `openspec validate --strict` gives `is
valid`.

**For n7, learned here.** (1) Haiku reviewers fail to call `update_task` often enough that 1 in 2
did, so budget for a second attempt and record every attempt, not the good one. (2) A silent review
now restaffs onto the other agent even on the pre-fix tree, because the review run binds the author
to the task. So on the fixed tree, "the flow staffs the other agent" must be read from **firing 2's
own staffing**, meaning the first review run's agent, and not from who approved in the end.
Otherwise a restaff masquerades as the fix.

**Next:** `n7-drive-fixed`, which runs §5.2–5.4 on the main checkout.

## Iteration 8 — n7-drive-fixed (2026-09-12 01:00 → 01:10)

**Position confirmed, no reconciliation.** Branch `autonomous/2026-09-08-daily`, `HEAD` = `cddf32b`
(*drive(F306)*), tree clean. Matches `STATE-night.json` (iteration 7, `current: n7-drive-fixed`).

**Drive Hub, per the recipe.** `netstat` showed no 80xx listener at all, so the **drive port was
8015**; n6 used 8014. The profile was a fresh **`drive0912`**, not n6's `drive0912p`. It was
launched from `hub/` with `py -3.11 -m uvicorn` and `AW_BOOTSTRAP_API_KEY` set. It migrated to
`0102`. `import hub` resolved to the main checkout. **PID 17692**, which started 01:01:25. **No
`.py` under `hub/hub` or `src` is newer than that start**; the newest is
`task_transition_service.py` at 00:04, n3's restore, and the tree is clean at `cddf32b`, which
contains the fix. The key answered 200. `AW_HUB`/`AW_KEY` were exported on every call.

**§5.2 — the flow staffs the other agent, and the tables prove it.** Fixture A was built with
`setup_row7.py` (`%TEMP%\n7\fixtureA`): `proj-192ee0e59efb`, author **`r7af7a`** (sorts first),
`r7bf7a`, and an unbound third agent. `t_row12_review_leg.py`, `AW_COMPLETE_BY=untouched`, printed
**19/19**, but that result is green on both trees. The proof is the database:

| table | row |
|---|---|
| `requirement_evidence` | `ev-b6a5a19738e9`, `implementation`, `actor_kind='agent'`, **`actor='r7af7a'`**, `accepted`, on `run-1feb9dc376f2` (`task_id` NULL, initiator `operator`) |
| `task_transitions` | seq 1–3 `pending→in_progress→completed→under_review`, all `operator`, `actor_agent` NULL |
| `runs` bound to `task-88efa63d19ee` | **exactly one: `run-1d7e3883437a`, agent `r7bf7a`, initiator `autonomous`**. This is firing 2's own staffing |
| `run_divergences` | **none** for the task, so no restaff is standing in for the fix (n6's warning) |
| `task_transitions` | **seq 4 `ttr-78e9a11e7c48`, `under_review→approved`, `actor_kind='run'`, `actor_agent='r7bf7a'`, `run-1d7e3883437a`** |

On the pre-fix tree, the same fixture shape staffed the author 2 times out of 2 (n6). The ladder
orders agents by name and the author sorts first, so `r7bf7a` being picked is the exclusion
working, not ordering luck. I have one sample. The rule is deterministic, and the sample is the
first one that could have shown the old behaviour.

**§5.2's second half — the author's approval by hand is refused, on the first attempt.** It was run
on fixture B's task, below, because A's task was already `approved`. The operator moved
`task-714b1ea52e2e` `completed→under_review` with **no assignee**, which is seq 7 and permitted. Then
`r7af7b`, the evidence author, was triggered **unbound** (`run-cee1b5540146`, Haiku) and told to call
`update_task(status='approved')`. It did so on the first try, at agent_outputs seq 3, and got
**403**: *"Cannot move task task-714b1ea52e2e to 'approved': agent 'r7af7b' recorded evidence for
this task, which claims the work as its own … No agent is recorded as completing it, so the evidence
is the record of who wrote it."* The status stayed `under_review` and **no seq 8 was written**.
That is S3.1's fallback firing through the real MCP path.

**§5.3 — the operator-sees-it half.** Fixture B was built with `setup_row7.py` and then `r7bf7b` was
unbound, leaving `proj-99af62baca17` with only **`r7af7b`** bound. The drive was
`AW_COMPLETE_BY=untouched AW_ALLOW_NO_REVIEWER=1` and printed **13/13**. Firing 2 answered **409**
*"could not staff this step: no agent is free to take it. Every agent on the roster is either
running a turn, already holding active work, or has worked on this task and so may not review
it."* That is the loop's `stall_reason`, verbatim. It is the operator arm's sentence and **does not
say *completed***.

- **Measured, and said plainly: the sentence says "this task" and does not carry its id.** The id
  reaches the operator on the loop's `review_unstaffed` event (`evt-c52509784f8b`,
  `data.task_id = task-714b1ea52e2e`, same reason), beside `job_run_skipped`. That is the split
  `2026-09-01-a-review-a-flow-cannot-staff-is-named` shipped, not a regression here, so no finding
  was filed. **The harness's check *"...and it names this task instead"* passes on its
  `"has worked on this task"` disjunct, not on the id**, so its label overstates what it checks.
  That is harness-only and I have not changed it (out of scope). A later drive should not read that
  line as "the id is in the sentence".
- **By hand, on the same task, while it was `completed`:**
  - **S3.5**, `POST /agent/trigger {agent: r7af7b, review_task_id}` → **403**, *"…that agent
    recorded evidence for this task … Its verdict would be refused, so the review could never
    end."* Afterwards there was no new run (the project still held only the author's work run), no
    new queue entry, and `git worktree list` was unchanged: no checkout.
  - **S3.4 wedge**, operator `PATCH {assignee: r7af7b, status: under_review}` → **403**, *"…it is
    assigned to 'r7af7b', which recorded evidence for this task…"* The task read back `completed`
    with assignee **`None`**, so the staged assignee rolled back and no transition was written.
    This is the route path. F319 is the scheduler path, and this does not bear on it.

**§5.4.** All four runs in `drive0912` join to runner model `claude-haiku-4-5-20251001`. `GET /jobs`
is `[]` on both projects. `ai_jobs` holds both jobs with `enabled=0` and `archived_at` set, and
`apscheduler_jobs` is empty.

**Teardown.** The Hub was stopped by PID 17692 and no 80xx listener remains. 8000 and 8010 were
never touched. Transcripts and table dumps are kept in `profiles/drive0912/n7-transcripts/`
(`5.2-driveA.txt`, `5.2-tables.txt`, `5.2-author-approval-run.txt`, `5.3-driveB.txt`,
`5.3-tables.txt`, `tables.py`, `hub-8015.log`), and the database is kept too. `%TEMP%\n7` is left
in place. The Hub log has no tracebacks.

**Ticked 5.2, 5.3, 5.4 → 40/43**, with notes in `tasks.md`. `openspec validate --strict` returns
`is valid`. **No new findings.**

**Next:** `n8-close`, which covers S7.0, S7.1 and S7.2, then the window's final entry.

## Iteration 9 — n8-close (2026-09-12 01:10 → 01:25)

**Position confirmed, no reconciliation.** Branch `autonomous/2026-09-08-daily`, `HEAD` = `f786a21`
(*drive(F306,F316)*), tree clean. Matches `STATE-night.json` (iteration 8, `current: n8-close`).

**Census captured first, under F313's rule.** `py -3.11 scripts/classify_findings.py` was saved to
`%TEMP%\n8-census-before.txt` before any edit to `FINDINGS.md`. It printed
`severity A: … CONFLICT=2 OPEN=5 RESOLVED=49`, with F306 under `OPEN` and F316 under `CONFLICT`
(the F318 arm).

**7.0: F316.** Status is now `fixed 4929ea0` (tests `40bd429`), naming task 2.4 (the two-branch
derivation) and 2.4a (the `excluded_because` reason). The entry states the evidence at its real
strength. The restaff route is proven by unit legs 4.10 and 4.10a, each of which failed under its
own mutation in iteration 4. It is **not** driven live on the fixed tree: after the fix no firing
picks the author, so no live restaff onto the author can be staged. n6's attempt-1 restaff does not
count either, because its silent reviewer *was* the author. What *was* driven is the
operator-facing half: §5.3's 409 says "has worked on this task", not "completed". F316's
shape-of-a-fix sentence "if that change ships … should say which commit" now points at the Status
line.

**7.1: F306.** Status is now `fixed 4929ea0` (tests `40bd429`). The two statements R1 refuted were
corrected in place, each in a dated block that quotes what it replaces:

- The "Unverified: whether ordering is deterministic" note. D6 found the ladder orders by name and
  rung 2 takes the first eligible agent, so an author sorting first reviews itself every time, not
  some of the time.
- The first shape-of-a-fix bullet's `kind` scoping. D5: the corpus requires the kind vocabulary to
  stay open, `EVIDENCE_KINDS` is advisory, and the reproduction's own `implementation` falls outside
  it.

A new sub-block closes the entry with both drives: pre-fix `proj-34d006e2f3e5` seq 8 by `r7af306q`,
and fixed `proj-192ee0e59efb` seq 4 by `r7bf7a`, plus the four doors the fixed tree refused the
author at.

**Index paragraph re-derived.** `Revised 2026-09-12 (night window, n8-close)`: the open severity-A
set is **four: F299, F300, F301, F312**, computed after the edit. It also says the two-number
arrangement (OPEN plus CONFLICT) is no longer needed for this set. The F318 arm itself is
unchanged, so it can still misfile a future section. F319 (B) is noted as this window's one new
filing.

**Census after, verdicts only (line offsets stripped): exactly two moved.**

> severity A: total 56 CONFLICT=2 OPEN=5 RESOLVED=49 *(before)*
> severity A: total 56 CONFLICT=1 OPEN=4 RESOLVED=51 *(after)*

F306 moved OPEN→RESOLVED and F316 moved CONFLICT→RESOLVED. F52 is the one remaining A conflict. The
B, C, D and `?` verdict lists match. STRONG-marker count went 126→128 and there are still 319
sections: the new `### \`F306\` …` sub-heading is not a section boundary, because the backtick
defeats `ANY_HEAD`. `tests/test_classify_findings.py`: **19 passed**.

**7.2: validate, sync, archive.** `openspec validate --strict` gave `is valid`. Before letting
`openspec archive` do the sync, I compared each of the 7 MODIFIED blocks with its main-spec block.
Every main scenario is still in its delta, except *"A task whose completer is unknown may enter
review"*, which R4's S3.4 split into two conditional scenarios on purpose (design.md, "What is
given up"). The four main paragraphs absent from the deltas are the deltas' own modifications. The
specs were last touched at `5d43a40` (09-11 00:32) and the delta was first written at `35cdd38`
(09-11 09:35), so no later main-spec edit could be lost. With that settled, I ran
`openspec archive an-agent-that-recorded-the-evidence-is-the-author -y` without `--skip-specs`: 3
blocks modified in `agent-flows` and 4 in `task-lifecycle-governance`. It archived as
`archive/2026-09-12-an-agent-that-recorded-the-evidence-is-the-author` (single date, because UTC was
already 09-12). **Checked afterwards with a script, not assumed:** all 7 main blocks equal their
delta blocks verbatim, requirement counts are unchanged (19 and 48, same names in the same order),
and no other requirement changed. `openspec validate --specs --strict`: **43 passed, 0 failed.**
The proposal warns that its "Why" section exceeds 1000 characters; that does not block. Tasks:
**43/43**.

**APPROVALS.md.** A `BUILT` note under the 2026-09-11 row, labelled as written by the FIX window
and not by the operator, in the 2026-09-10 note's format.

**One "recorded, not fixed" item repaired instead of carried.** n7 noted that the
`t_row12_review_leg.py` label *"…and it names this task instead"* overstates its check, which is
a disjunction that passes on the sentence without the id. The label now says what the predicate
checks, with a comment on why. The predicate did not change. `py_compile` and `ruff` are clean.
`black --check` flags the file, but it flagged the file at `HEAD` too, and `scripts/` is not in
CI's black path set. I left it alone rather than reformat 400 unrelated lines. This change is
**not driven**. It is a string change in a harness, and the next row-12 drive will print it.

---

## Window close — 2026-09-11 night

**The queue is finished.** All nine items are done: one approved change was built, mutated, gated,
driven on both trees, ledgered and archived. `next_action` is null.

### What changed

| commit | kind | what |
|---|---|---|
| `de5733e` | log/state | compose |
| **`4929ea0`** | **PRODUCT** | the fix: `task_transition_service.py` (the union's fourth source, and both guards' fallback), `run_divergence.py` (2.4/2.4a/2.5), `agent_trigger.py` (S3.5). Also repairs two existing test fixtures, `test_a_flow_names_what_it_cannot_staff.py` and `test_approval_refuses_unaccepted_evidence.py`, to use operator evidence; no guard relaxed |
| **`40bd429`** | **TEST** | `hub/tests/test_the_evidence_names_the_author.py`, 12 tests, one leg per call site |
| `99d64fc` | test docstrings + ledger | the mutation table; two docstrings corrected, no assertion touched; files F319 |
| `c2acf2a` | tasks/log | the gate |
| `96d79d0` | docs (playbooks) | the night drive recipe: free port, fresh profile, named key. Also day-window.md and the daily-review skill |
| `cddf32b`, `f786a21` | tasks/log | the two drives |
| this commit | ledger, specs, archive, harness label | n8-close |

**Review closely:** `4929ea0` and `40bd429`, the only product and test code in the window. **Skim:**
everything else. `openspec/specs/agent-flows` and `task-lifecycle-governance` changed here, but the
text is exactly the approved deltas, verified verbatim above.

### What was proven, and how

- **Driven live, proven from the database:**
  - **Pre-fix tree (`37b8226`):** the evidence author approved its own work (seq 8,
    `proj-34d006e2f3e5`).
  - **Fixed tree:** the flow's only staffing is the other agent (seq 4, `proj-192ee0e59efb`, no
    divergence). The author is refused through its own MCP approval (403), a firing with no one else
    (409, the operator arm's sentence), hand dispatch (403, no run or checkout), and the S3.4 wedge
    `PATCH` (403, assignee rolled back).
  - Every run bound Haiku, and no job is enabled.
- **Tested, not driven:**
  - F316's restaff route (4.10 and 4.10a; see 7.0 above for why a live drive cannot reach it after
    the fix).
  - The actor-kind filter (4.5).
  - Unaccepted evidence still naming its author (4.6).
- **Mutation-checked:** ten mutations, and every target leg failed under its own.
- **Gate:** whole Hub suite **4057 passed, 0 failed** on `99d64fc`. No product `.py` has changed
  since; the later commits touched logs, tasks, playbooks, the ledger, specs and one harness label.
  CI's lint set is clean, and no migration was added.
- **Inferred, not measured:** that the fixed tree's staffing of `r7bf7a` is the exclusion and not
  luck. It rests on D6's measured determinism (the ladder orders by name, and the author sorts
  first) plus **one** fixed-tree sample. The pre-fix tree staffed the author 2 of 2.

### What is open

- **Severity A, no proposal: F299, F300, F301, F312.** F299 to F301 are `DAY-3` decision material;
  none is buildable unattended.
- **F319 (B), filed this window (iteration 4).** A review refused on the scheduler path leaves the
  refused reviewer holding the task, because `turn_scheduler.py:349` commits `enter_selected_task`'s
  staged assignee. Iteration 4 measured it on the **unmutated** tree too. Its
  ReviewTurnRefused-after-staging leg is unverified, and if that leg holds it is an A. **It should
  be the day window's first spec-loop candidate.**
- **F318 (B):** the classifier's cross-section arm dequotes per line. No proposal. It no longer
  affects the A count, but it will misfile the next section that quotes a wrapped title.
- **F292 (B)** and **F314**: known intermittents, and neither fired in this window's whole-suite run.
- **Harness, noticed in n6 and not investigated:** attempt 1 printed 15/19 because section E of
  `t_row12_review_leg.py` expects the reviewer to carry an `origin_type='job'` queue entry, and a
  restaffed reviewer does not have one. That is a harness assumption meeting the restaff route. It
  is queued as a note for tomorrow, not fixed here, because deciding what section E should assert on
  a restaff is a design question about the harness.

### Decisions waiting for the operator

1. **CLAUDE.md's trial-Hub section is one migration behind.** It says the trial database was
   "migrated to head `0101`" and "this checkout is at `0101`". This branch's head is **`0102`**
   (measured in n5, n6 and n7: three fresh profiles each migrated `0101 → 0102`). CLAUDE.md is
   yours, so it was not edited. Either update the two sentences, or tell the window it may.
2. **Merging.** The branch carries this change on top of `master@6f7e486`. Whether and when to merge
   is yours; the day window's merge gate is the only automated path.

### What to distrust

- **I tested my own work.** The same window wrote the fix, the tests and the mutations. The
  mutations are the check on that. There was no independent review after R4.
- **Haiku is an unreliable actor.** In n6, 1 of 2 author reviews never called `update_task`. Every
  live claim above was read from the tables for that reason, not from the harness's pass count,
  which is green on both trees.
- **Fixed-tree staffing is one sample** (see "Inferred" above).
- **The drive databases are kept**: `profiles/drive0912p` (pre-fix) and `profiles/drive0912`
  (fixed), each with a transcripts directory. `%TEMP%\n6` and `%TEMP%\n7` are left in place.
  Nothing is listening on 80xx, and 8000 and 8010 were never touched.

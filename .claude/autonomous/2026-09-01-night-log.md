# 2026-09-01 night window

Newest entry at the BOTTOM. Playbook: `.claude/loops/night-window.md`.

Armed 2026-09-01T01:12:04+01:00 on `autonomous/2026-09-01-daily` (parent master@ad60b7b), model opus.

---

## Armed 2026-09-01 01:22 — a one-off catch-up window, not the standard night

The operator went to bed at ~01:10 having asked for two things: that the new daily format work
tomorrow, and "a autonomou run to fix existing issues and run a e2e loop". This window is the
second of those.

**Why this window is not the standard night window.** There has been no day window yet, so
`spec-queue/APPROVALS.md` has no approved rows by construction and there is nothing approved to
build. Tonight therefore spans both playbooks: `.claude/loops/night-window.md` for how to implement
and drive, and `.claude/loops/day-window.md` sections D-1 and D-2/D-3/D-4 for how to run a drive and
a spec loop. From 09:00 today the normal cycle takes over and this mixing stops.

**The queue, and where it came from.**

- **N-0** came from the research run at 00:49 tonight, which was the first execution of the new
  research half. It is the most urgent item because it can turn CI red on the next push with nobody
  touching the code: FastMCP 4.0.0 reached PyPI at 2026-08-31T18:20:31Z and `fastmcp>=2.0` is
  unbounded in three places. Verify before acting; the research is a lead, not a verdict.
- **N-1** is measured from `npx openspec list` at 01:00, not guessed. The archive-ordering
  constraint in `a-conflict-refusal-names-what-clears-it` task 6.4a is real.
- **N-2** is the operator's explicit ask and the highest-value item. Split it across iterations.
- **N-3 to N-6** are F156 through the full round discipline, which handoff 0102 ranked next.
- **N-7** is a one-line correction to a document known to be wrong.

**What was NOT verified before arming.** The suite was not run tonight. `master` was green at
`9fa4c4b` on 2026-08-31; every commit since is documentation, `.claude/` tooling or `spec-queue/`,
and no product code under `hub/hub` or `src` has changed. Treat that as strong inference, not
measurement — confirm the chunk you are about to touch before attributing a failure to yourself.

**Also unverified: this driver has never completed an iteration in this configuration.** The
two-window `-StateFile` support, the arming script and this window are all hours old. Four
PowerShell traps were found and fixed during arming alone. If the first firing behaves strangely,
suspect the harness before suspecting the repository, and say so in the log rather than working
around it.

---

## Iteration 1 — 2026-09-01T01:26:29+01:00 — N-0: fastmcp bounded to `<4`, with a named test

**Branch and history reconciled.** `git branch --show-current` = `autonomous/2026-09-01-daily`,
HEAD `75e2d1f`, tree clean, parent `master@ad60b7b` present in history. All three agree with
`STATE-night.json`. Nothing to reconcile.

### Verifying the research before acting on it

The queue said the research is a lead, not a verdict. Three things to confirm, and one of them
did not survive.

- **The pin strings, at the named lines.** Confirmed. `pyproject.toml:39` `mcp = ["fastmcp>=2.0",
  …]`, `:60` `all = ["fastmcp>=2.0", …]`, `hub/pyproject.toml:19` `"fastmcp>=2.0",`. Unbounded in
  all three.
- **4.0.0 is what a fresh resolve picks today.** Confirmed by `pip index versions fastmcp`:
  `LATEST: 4.0.0`, `INSTALLED: 3.1.0`. So a CI resolve today takes 4.0.0.
- **Which tests sit on `get_tools()`.** **None — the queue's paraphrase is wrong.** Seven test
  files call `mcp.list_tools()`; `grep -rn "get_tools"` across `hub/tests`, `tests`, `hub/hub` and
  `src` returns nothing. The research document itself (`spec-queue/research/2026-09-01.md` §1) is
  accurate on this — it says the seven sit on `list_tools()`, which it measured as *surviving*
  4.0.0, and cites `get_tools()`'s removal only as evidence that the deletion pass reached that
  layer. The compression into `STATE-night.json`'s N-0 detail turned "adjacent API removed" into
  "the API we use is gone". Recording it because it is the failure mode this arrangement exists to
  catch: an unattended run acting on a summary rather than on the measurement behind it.

Also corrected: the operator's machine reports `fastmcp 3.1.0` under `py -3.11`, not the 3.4.5 the
research read from some other interpreter.

**So the claim is weaker than the queue stated, and the action is still right.** Nothing is broken
today. What is true is that the process starting every agent turn crossed a major on a green board
in a six-hour window with no signal, and that 4.0.0 additionally drags `pydantic>=2.12`,
`FastAPI>=0.133.0` and `httpx2` behind it — a transitive set this suite has never run against.

### What changed

`fastmcp>=2.0` → `fastmcp>=2.0,<4` in all three declarations, each with a comment saying when,
why, and that it is defensive rather than forced. Verified with `packaging`: the specifier admits
3.1.0 and 3.4.7, refuses 4.0.0. Nothing downgraded, no lockfile added, and `fastapi>=0.110` left
alone — that one is the operator's, and stays in `decisions_for_user`.

New: `hub/tests/test_fastmcp_api_contract.py`, six tests naming each fastmcp API the tool surface
actually stands on — the `FastMCP(name=, instructions=)` constructor at `mcp_server.py:55`, the
parenthesised `@mcp.tool()`, `mcp.run(transport="stdio", show_banner=False)` at `:1397`, the
`.name`/`.parameters` pair the seven files read, `Literal` still rendering an enum, and the pin
itself being bounded and identical in all three places. The point is that raising the ceiling has
a test to answer to instead of a red board to bisect.

### Mutation-checked, both ways

Not "the suite is green", but "delete the line and watch a **named** test fail":

- One pin loses its `<4` → `test_the_pin_is_bounded_and_agrees_everywhere` fails with
  `the three fastmcp pins disagree: {…}`.
- All three unbounded — literally the 2026-08-31 state — → same test fails with
  `fastmcp is declared 'fastmcp>=2.0', with no upper bound.`

Both restored; `grep -n 'fastmcp>=' pyproject.toml hub/pyproject.toml` re-confirmed after.

### Tests

- `hub/tests/test_fastmcp_api_contract.py` — 6 passed.
- The mcp_server chunk: `test_tool_surface_matches_server`, `test_mcp_tool_schemas`,
  `test_mcp_server_stdio_surface` (spawns the real process), `test_agent_tool_surface_phase7`,
  `test_spec_index_writer`, plus the new file — **82 passed**.
- `hub/tests/test_version.py` + `test_requirement_gate.py` — 31 passed.
- `tests/test_packaging.py` (the other reader of `pyproject.toml`) — 3 passed.
- CI's own linters over CI's own paths: `ruff check src/ hub/ tests/` clean,
  `black --check --target-version py311 src/ hub/hub/ hub/tests/ tests/` — 536 unchanged,
  `mypy src/` — 22 files, no issues.

Not run: the whole hub suite (~25 min). Nothing outside the mcp/packaging chunk was touched.

**Note for whoever runs the full suite next:** `pytest tests/… hub/tests/…` in one invocation dies
with `ImportPathMismatchError` on the two `conftest.py` files. Run them as separate invocations.
That is pre-existing, not tonight's doing.

**Next:** N-1 — archive the completed openspec changes, `a-loop-declares-whether-it-needs-evidence`
first, honouring task 6.4a's ordering constraint.

---

## Iteration 2 — 2026-09-01T01:40:23+01:00 — N-1: seven changes archived, and one sync that reverted another

**Branch and history reconciled.** `git branch --show-current` = `autonomous/2026-09-01-daily`,
HEAD `e43b616`, tree clean, parent `master@ad60b7b` in history. All agree with `STATE-night.json`.
Nothing to reconcile.

**Re-measured rather than trusting the queue.** `npx openspec list` at 01:33 returned exactly the
eight rows N-1 recorded at 01:00, with the same counts. All six candidates passed
`npx openspec validate --strict` before anything moved.

### What was archived, in this order and for these reasons

| # | Change | Why it could go, and when |
|---|---|---|
| 1 | `a-loop-declares-whether-it-needs-evidence` | Complete. **First by constraint**, not by preference. |
| 2 | `a-conflict-refusal-names-what-clears-it` | Its one unchecked box was 6.4a, the ordering constraint itself. Satisfied by #1. |
| 3 | `approval-waits-for-the-turn-to-end` | Complete, no ordering constraint. |
| 4 | `a-review-a-flow-cannot-staff-is-named` | Complete, no ordering constraint. |
| 5 | `a-flow-briefing-names-its-contract` | Complete. Its 7.4 names `DRIVE-1` as elsewhere's; `DRIVE-1` ran 2026-08-31. |
| 6 | `a-review-nobody-is-doing-is-named` | Its one unchecked box was 6.1, "not before it has driven". It drove 18/18. |
| 7 | `approval-refuses-unaccepted-evidence` | Its three unchecked boxes were section 8, "Not in this change" — see below. |

`a-write-outside-the-workspace-is-recorded` (0/54) was left alone, as instructed. It is now the only
change in `openspec/changes/`.

**Unlike the last three archives, none of these used `--skip-specs`.** That flag was right in August
because the specs had been synced by hand; tonight they had not been. Measured before acting: neither
*"A loop declares at creation whether its work needs evidence"* nor *"Work that no evidence governs is
integrated from the task's own branch"* existed anywhere in `openspec/specs/`. So the archiver did
the sync, and each commit records what it applied.

### The ordering constraints were checked, not obeyed on faith

Both were conditions rather than work, so both were ticked as *satisfied*:

- **6.4a** forbade archiving `a-conflict-refusal-names-what-clears-it` before the branch-tip route
  was a shipped requirement. After #1 it is, at `task-lifecycle-governance:1753`, and `:640`'s
  *"SHALL NOT be the agent's branch"* is not breached — `:1753` scopes itself out where evidence
  governs and restates the same prohibition in its own text.
- **6.1** forbade archiving `a-review-nobody-is-doing-is-named` before it had driven. It had:
  `FINDINGS.md` records F154 as FIXED at `001a07d`, driven 2026-08-31 with
  `scripts/drive/t_f154_wedged_review.py` at 18/18 on both populations.

### The real finding: archive order silently reverted a shipped requirement

**Two of tonight's changes modify the same requirement**, and the second one to land overwrote the
first. `approval-refuses-unaccepted-evidence` and `a-loop-declares-whether-it-needs-evidence` both
carry a `## MODIFIED` block for *"An integration that cannot proceed does not block approval"*.
Archiving the second one replaced a-loop's version with the text a-loop was **written to supersede** —
the *"where evidence governs the task"* qualification and the sixth list entry disappeared, and so did
the scenario for a task with no evidence and no branch.

The consequence was not cosmetic. The corpus was left saying integration cannot be attempted *"when
no accepted evidence for the task names a commit to merge"*, unqualified, sitting one page from
`:1753`, which requires precisely that task's branch tip to be merged. That is the contradiction
a-loop's delta exists to prevent, reintroduced by nothing but the order two archives ran in.

**Repaired** by restoring a-loop's version verbatim from its archived delta — the superset, containing
this change's two new list entries *and* the qualification, which is why its own prose says
*"enumerated for the first time by `approval-refuses-unaccepted-evidence`"* in the past tense.

Verified after, not assumed: the list carries the unresolvable-working-directory entry and the
evidence-governs qualification together; both scenarios present; all three of
`approval-refuses`'s own additions survive; no duplicated requirement heading in any of the four
touched capability specs; `npx openspec validate --specs --strict` **43 passed, 0 failed**.

**Then swept for the same hazard across all seven**, by extracting every `## MODIFIED` requirement
name from every archived delta and looking for repeats. **Exactly one collision, and it is this
one.** Nothing else overlapped.

*This is worth carrying past tonight:* `openspec archive` applies a delta against whatever the corpus
currently says and warns about nothing. Two changes in flight against one requirement is not rare
here — it happened in a batch of seven — and the loser is silent. Added to `decisions_for_user`.

### F169 filed, closing a deferral that was about to be archived unexecuted

`approval-refuses-unaccepted-evidence`'s section 8 is titled *"Not in this change — recorded so it is
not silently absorbed"*, so its three unchecked boxes needed reading rather than counting:

- **8.1 / 8.2** defer to "change D", which is **F124** — open at severity B in `FINDINGS.md:8703`,
  naming both the retry that cannot succeed and `NOTHING_TO_MERGE` standing for three worlds. The
  ledger holds them independently of the change, so archiving absorbs nothing. Marked as *carried*,
  with that reasoning written into `tasks.md` rather than left in this log.
- **8.3** asked for a confirmation *during `DRIVE-1`*. `DRIVE-1` ran on 2026-08-31 and never did it,
  and `grep approval_report scripts/drive/FINDINGS.md` returned nothing — so the box was about to be
  archived unexecuted. Confirmed instead by code reading and filed as **F169 (C)**: `approval_report`
  is built at `task_transition_service.py:571`, carried on `TaskResponse`
  (`schemas/tasks.py:298`), set only by the two transition call sites — and read by **no file under
  `hub/ui/src`**. Because `_task_response` defaults it to `[]` (`tasks.py:102`) and the service's own
  comment says nothing persists it, it is not merely invisible but **unrecoverable**: refetch the task
  and there is no record an advisory existed. The product chose to report rather than refuse;
  reporting to nobody is refusing to report. Labelled in the finding as **read, not driven**.

### Tests

No product code changed — seven archives are file moves plus corpus edits. Confirmed that claim
rather than asserting it: `grep -rn "openspec/specs\|openspec/changes" tests/ hub/tests/` returns only
docstring references, so nothing under test opens the corpus.

Ran the five behaviour files named after tonight's changes anyway, because a corpus edit that broke
one would be the interesting outcome: `test_a_review_nobody_is_doing.py`,
`test_approval_refuses_unaccepted_evidence.py`, `test_approval_waits_for_the_turn.py`,
`test_briefing_names_its_contract.py`, `test_conflict_refusal_names_what_clears_it.py` —
**104 passed** in 74.9s.

Not run: the whole hub suite (~25 min), no linters — no `.py`, `.ts` or `.tsx` was touched.

**Next:** N-2 — the full-surface e2e sweep. Restart 8011 from this branch first and confirm no source
file is newer than the process, create a fresh project, and take one feature area per iteration.

---

## Iteration 3 — 2026-09-01T02:00+01:00 — N-2, row 1 of 19: Projects

**Position confirmed before starting.** Branch `autonomous/2026-09-01-daily`, tree clean,
`git log` head `fb51400` — matching `STATE-night.json`'s iteration 2. Nothing to reconcile.

### The drive Hub was restarted, and it was not serving what the state file said

`next_action` said to restart 8011 from this branch's code onto the beta profile. Before killing it
I measured what the running instance actually was, and the answer was worth writing down: PID 30752,
started **2026-08-31 21:11:51**, command line carrying **no `DATABASE_URL` at all** — so it had
landed on `config.py`'s relative default and was serving **`<repo>/hub/data/agentweave.db`**, the
dead earlier database `CLAUDE.md` warns about. Confirmed by listing projects on the API and matching
the six ids against each candidate file: `aw-loop10`, `ui-showcase-2026-08-22`, `13.2 briefing
check`, the two `f154-drive-*`, and `Throwaway (taste pass)` — exactly `hub/data`'s rows, and none
of beta's twenty-three.

Its *code* was current (newest `.py` under `hub/hub` or `src` is `scheduler.py` at 21:04:55, seven
minutes before it started, and nothing since has touched product code). So the stale-build hazard
was absent; the stale-*database* one was not. Restarted with the explicit `DATABASE_URL`, verified
by listing 23 projects headed by `f167-drive-160905`.

`AW_CHECK_UI_BUNDLE=1 pytest hub/tests/test_ui_build_stamp.py` — **13 passed**, so the dashboard
bundle matches its source and every screen finding below is about current code.

### Three leftover jobs were enabled, from someone else's iteration

Step 8's sweep found three enabled jobs in the three `f155-drive` projects, left by the 2026-08-31
drive. `schedule: null` and `last_run_at: null`, so none had ever fired and none was going to —
but a job left enabled in a Hub I had just restarted is exactly the standing limit's concern.
Archived all three (`200`). Recheck: nothing enabled anywhere on 8011.

### Row 1 driven — 34 API assertions, 6 screen assertions, three findings

Full write-up in `scripts/drive/FINDINGS.md` under *"Full-surface sweep, 2026-09-01 — row 1 of 19"*.

- **F170 (C)** — `.agentweave/project.json` is the one Hub working file `repo_hygiene`'s exclude
  block does not cover, so it sits untracked in the operator's repository *and* is offered as a
  project file by the composer's `@path` listing. One pattern fixes both; verified by hand.
  Previously noted in a 2026-08-25 parenthesis and never filed.
- **F171 (B)** — the identity-conflict dialog offers the right remedy (*"Register as a new
  project"*, which works) under a fixed paragraph whose three clauses are all false in the situation
  driven. `project_identity_conflict` is raised from four sites with four meanings; the UI matches
  on the code — correctly, by its own reasoning — and then narrates only one of the four.
- **F172 (B)** — `POST /projects/{id}/relocate` onto a path key another project row still holds
  answers a bare `500`: `_guard_relocation` never checks the destination for a collision and
  `projects.path_key` is unique, so the commit raises `IntegrityError`. Every other refusal in this
  router is typed and names a way forward; this one returns five words.

### The drive corrected itself twice, which is the part worth carrying

The first harness reported **three defects that were not defects** — `main_branch` asserted off a
response schema that has never carried it, and `/fs/list` expected to error where it deliberately
answers `200` with a `reason` so the picker can keep navigating. All three were assertions about an
imagined product. They are fixed *and the reasoning is written into the harness*, so the next run
inherits the correction rather than rediscovering it.

F172 came out of the same accident in the other direction: the second run of the harness hit a
`500` the first run had not, because the database now remembered the first run's relocated path.
That is a real defect that only a *repeated* drive against durable state could surface, and it was
then reduced to `t_f172_relocate_onto_a_claimed_path.py`, which reproduces it from nothing in one
command.

### Verification

- `t_sweep_row1_projects.py` re-run after formatting on a rebuilt fixture: **33/34**, the single
  failure being F170's own assertion. That is the intended baseline — the harness is red exactly
  while the finding is open.
- `t_f172_relocate_onto_a_claimed_path.py` re-run standalone: `500`, reproduced, with the
  `IntegrityError` in the server log both times.
- `t_sweep_row1_ui.py`: **5/6**, the failure being F171's assertion. Screenshots in
  `%TEMP%\row1shots`.
- `black --target-version py311` clean on the three new files; `ruff` clean (one `F541` fixed).
  Neither tool's CI path list covers `scripts/`, so this is tidiness, not a gate.
- **No product code was changed**, so no suite was run beyond the UI stamp check.

### Cleanup

All six row-1 projects deleted (`204` each), every fixture directory removed, the three leftover
jobs archived. `GET /projects` on 8011 is back to the 23 that predate this iteration. No credential
was minted — `GET /setup/token` returns the existing operator row.

**Next:** N-2 continues at **row 2, Runners** (create one per CLI, probe launchability, read the
model catalog). Rows 3–19 remain. The 8011 Hub is up on beta and current; it does not need
restarting again unless product code changes.

---

## Iteration 4 — 2026-09-01T02:35+01:00 — N-2, row 2 of 19: Runners

**Position confirmed before starting.** Branch `autonomous/2026-09-01-daily`, tree clean, `git log`
head `62a56a1` — matching `STATE-night.json`'s iteration 3. Nothing to reconcile.

**The Hub needed no restart, and that was measured rather than assumed.** PID 22908, started
2026-09-01 01:48:32; nothing under `hub/hub` or `src` is newer (`find -newermt` returns nothing).
Which database it serves was re-checked the way `CLAUDE.md` insists — called `GET /projects` and
compared mtimes: the beta profile's file moved within the same second, `<repo>/hub/data` had not
been touched since 01:47. 8011 is on beta and current.

### Row 2 driven — 64 API assertions, 13 screen assertions, one real agent turn, five findings

Full write-up in `scripts/drive/FINDINGS.md` under *"row 2 of 19: Runners"*. Harnesses kept:
`t_sweep_row2_runners.py` and `t_sweep_row2_ui.py`.

- **F173 (A)** — the Runners screen is free-typed text and swallows its own refusal. Typing a model
  and pressing Save produces a `400`, an unchanged dialog, and **no message anywhere**; the same for
  the edit dialog. `useCreateRunner`/`useUpdateRunner` declare `onSuccess` and no `onError`, the page
  renders only `deleteError`, and `main.tsx` configures no `MutationCache` handler and no toast. The
  sharp part is not the missing toast: `openspec/specs/runner-registry/spec.md:72-73` is a **shipped
  requirement** saying runner management shall *offer* the catalog's models rather than accept free
  text, and two of its three scenarios are unimplemented on the screen. Task `6.5` of the change that
  introduced it is ticked, and its own evidence names only the two backend pieces.
- **F174 (B)** — the Codex half of the catalog has drifted from `~/.codex/models_cache.json`, the
  file its own docstring names as its source. The cache lists four models; the catalog declares six.
  The two extras are `gpt-5.4` and `gpt-5.6-sol` — and `gpt-5.6-sol` is the one carrying
  `default=True`. Twelve test files hardcode it, so the drift is invisible to the suite.
- **F175 (C)** — the undeclared-model refusal is the only gate in the catalog validator that names
  no permitted value, sitting ten lines from an enum rejection that does. It also denies `opus`,
  which the same Hub publishes as an alias over `GET /model-catalog`.
- **F176 (C)** — `POST /runners {"name":""}` answers `201`. The dialog disables Save on an empty
  name; the schema does not.
- **F177 (C)** — `order_by(Runner.created_at)` with no tiebreaker. Two consecutive creates tie about
  half the time on Windows' ~15.6 ms clock, and on a tie SQLite feeds the sorter from
  `ix_runners_project_name`, so the list silently falls back to **alphabetical**. Proven by naming
  rows against their creation order. `models.py` already carries this same paragraph five times
  (`Conversation`, `Checkpoint` citing F55, `RunDivergence`, `TaskTransition`, `InboundQueueEntry`);
  `Runner` is the same shape and was not covered.

### The claim the row exists to test was driven, not read

A runner on `claude-haiku-4-5-20251001`, an agent bound to it, one real turn — `run-86669729ee1d`,
completed, exit 0, 7 seconds, output `pong`. `turn_usage.model` reads `claude-haiku-4-5-20251001`,
which is what **Claude reported back**, not what the Hub sent. A Runner record reaches the provider
process intact. Codex runners were created, listed and probed; none was spawned, per the standing
decision that Codex is undrivable.

### How F177 was found, which is the part worth carrying

It arrived as a *harness flake*. An ordering assertion passed on the first run and failed on the
second with the same code and the same inputs. The cheap move was to delete the assertion. Instead
it was reduced: 20 create-create-list cycles flipped 10 times, every flip on a `created_at` tie, and
`EXPLAIN QUERY PLAN` named the mechanism. Then the decisive experiment — create `ZB-created-first`
before `ZA-created-second` and watch the tie return them backwards. Row 1's F172 came from the same
shape (a second run of the same harness hitting durable state the first had left). **A drive that is
run once cannot find either.**

### Verification

- `t_sweep_row2_runners.py` on a rebuilt fixture after formatting: **59/64**, the five failures being
  F174 (×2), F175 (×2) and F176. An earlier identical run scored 58/64 — the extra red was F177,
  which by construction only fires when the tie fires. Both numbers are in the findings file.
- `t_sweep_row2_ui.py`: **10/13**, all three failures F173. Screenshots in `%TEMP%\row2shots`;
  `row2-04-after-save.png` is the finding in one frame.
- The drift check compares the live catalog against the live cache on every run, so the next sweep
  re-measures F174 rather than trusting a snapshot.
- `black --target-version py311` and `ruff` clean on both new files; neither tool's CI path list
  covers `scripts/`, so this is tidiness, not a gate.
- **No product code was changed**, so no suite was run. `git status` shows only `scripts/drive`.

### Cleanup

Every project this iteration created is deleted (five, one per reset; `GET /projects` is back to the
23 that predate it), the fixture directory is removed, and the enabled-jobs sweep across all 23
projects returns empty.

**Next:** N-2 continues at **row 3, Agents** — register, bind runner and charter, read canonical
context and timeline, archive and unarchive. Rows 4–19 remain. The 8011 Hub is up on beta and
current; it does not need restarting unless product code changes.

---

## Iteration 5 — 2026-09-01T02:43+01:00 — N-2, row 3 of 19: Agents

**Position confirmed before starting.** Branch `autonomous/2026-09-01-daily`, tree clean, `git log`
head `95476c4` — matching `STATE-night.json`'s iteration 4. Nothing to reconcile.

**The Hub needed no restart, and that was measured rather than assumed.** PID 22908 owns port 8011,
started 2026-09-01 01:48:32; `find hub/hub src -name '*.py' -newermt '2026-09-01 01:48:32'` returns
nothing. Which database it serves was re-checked the way `CLAUDE.md` insists — `GET /projects`, then
mtimes: the beta profile moved to 02:27:48, `<repo>/hub/data` still sat at 01:47:54. 8011 is on beta
and current.

### Row 3 driven — 85 API assertions, 19 screen assertions, one real agent turn, five findings

Full write-up in `scripts/drive/FINDINGS.md` under *"row 3 of 19: Agents"*. Harnesses kept:
`t_sweep_row3_agents.py` and `t_sweep_row3_ui.py`.

- **F178 (B)** — `GET /agents/launchability` and the whole `collaboration_ready` mechanism reach no
  screen. Measured from the wire, not grepped: a full project-screen load makes 41 requests and none
  of them is to that route. `useAgentLaunchability` (`api/agents.ts:343`) is imported by ten files,
  nine of them tests that mock it. `AgentCard.tsx` — the only renderer of `collaboration_ready` — is
  imported by exactly one file, its own test. Commit `e4958fc` (2026-08-08) *says so in its body*:
  *"`AgentCard` is now unreachable too — left in place, because deleting it takes the
  collaboration-readiness indicator with it and that was not asked for."* The deferral was honest;
  the consequence was never recorded. 24 days. No requirement is breached —
  `runtime-diagnostics:137` says "SHALL be **able to report**" and the API does.
- **F179 (B)** — the Execution section presents an agent with no runner as a neutral `No runner`
  dropdown value. The Hub has the sentence (`launchability.py:80`), stamps it on the undeliverable
  queue entry as `waiting_reason`, and returns it from `/queue/{agent}/status` and the trigger
  response. None of it reaches the page. `row3-08-no-runner-execution.png` is the frame.
- **F180 (C)** — the archive refusal offers only the destructive remedy. The screen renders
  *"1 queued message. Discard them to archive"* plus a *"Discard … cannot be undone"* button, and
  never names binding a runner — which would **deliver** them, and which `agents.py:2025-2043`
  already treats as a redrain site (the F96 comment). The route (`agents.py:2133-2157`) also
  discards `agent_lifecycle`'s own message, which *did* explain the stake.
- **F181 (C)** — `/agents/launchability` has no lifecycle filter, so an archived agent is reported
  `runnable: true` one call after `POST /agent/trigger` refused it as archived. Latent only because
  of F178 — which is the reason to file it: fixing F178 is exactly what would expose it.
- **F182 (C)** — F175's undeclared-model refusal restated at a second site (`agents.py:630-634`),
  naming no permitted value and denying `opus`, an alias this Hub publishes.

### The claim the row exists to test was driven, not read

A charter authored to say *"reply with exactly the single word ROW3023319"*, an agent bound to it,
one real turn: `run-ca3ac9b518bd`, completed, exit 0, ~8 s, `claude-haiku-4-5-20251001`. The marker
came back in the output — **a Charter record reaches the process that runs.** The timeline names the
runner and model (`Run started (claude, claude-haiku-4-5-20251001)`).

### What the second run bought, and what it did not

The standing instruction to run each harness twice paid off on rows 1 and 2 (F172, F177) and **did
not here**: the second API pass scored identically against the state the first left. That is worth
recording as a real outcome rather than quietly dropping the repeat — the technique is not free and
it does not always fire.

### The trap this iteration fell into, corrected

The screen assertion for F180 asserted on `page.content()`. `"bind"` matched `tabindex`, and a real
defect was green until it was checked by hand. Every text assertion an operator must *read* now goes
through `page.inner_text("body")`, and the harness carries a comment saying why. This is the same
class of error row 1 hit three times — an assertion about an imagined product — caught earlier here
only because the pass looked suspicious next to a screenshot.

### The contrast that sharpened F173

The create-agent dialog does everything the Runners dialog does not: catalog `<select>` instead of
free text, `role="alert"` rendering the Hub's own `detail`, dialog stays open. Driven — a duplicate
name produced *"Agent name 'rid023319' already exists in this project"* on screen
(`row3-04-duplicate-refused.png`). So F173 is an **omission**, not an unbuilt convention. That
belongs in whatever change answers F173.

### Verification

- `t_sweep_row3_agents.py`: **74/76** with `AW_SKIP_TURN`, **83/85** with the real turn, and
  **74/76** again on a rebuilt fixture after formatting. The reds are F182 and F181 in every run.
- `t_sweep_row3_ui.py`: **15/19**, twice, including once on the rebuilt fixture. The four reds are
  F178 (×2), F179 and F180. Screenshots in `%TEMP%\row3shots`.
- `black --target-version py311` and `ruff` clean on both new files (one `UP034` fixed). Neither
  tool's CI path list covers `scripts/`, so this is tidiness, not a gate.
- **No product code was changed**, so no suite was run. `git status` shows only `scripts/drive` and
  `.claude/autonomous`.

### Cleanup

Both fixture projects deleted (`204` each), both fixture directories removed, `GET /projects` back
to the 23 that predate this iteration. The enabled-jobs sweep across all 23 projects returns empty.

**Next:** N-2 continues at **row 4, Charters** — CRUD, the 9 seeded starters, editing a charter
under a live binding, deleting one that is bound. Rows 5–19 remain. The 8011 Hub is up on beta and
current; it does not need restarting unless product code changes.

---

## Iteration 6 — 2026-09-01T03:07+01:00 — N-2, row 4 of 19: Charters

**Position confirmed before starting.** Branch `autonomous/2026-09-01-daily`, tree clean, `git log`
head `3f01fe5` — matching `STATE-night.json`'s iteration 5. Nothing to reconcile.

**The Hub needed no restart, and that was measured rather than assumed.** PID 22908 owns 8011,
started 2026-09-01 01:48:32; `find hub/hub src -name '*.py' -newermt '2026-09-01 01:48:32'` returns
nothing. Which database it serves was re-checked the way `CLAUDE.md` insists — a `GET /projects`
moved the beta profile's mtime to 02:48:12 while `<repo>/hub/data/agentweave.db` still sat at
01:47:54. 8011 is on beta and current.

### Row 4 driven — 65 API assertions, 23 screen assertions, seven real Haiku turns, five findings

Full write-up in `scripts/drive/FINDINGS.md` under *"row 4 of 19: Charters"*. Harnesses kept:
`t_sweep_row4_charters.py` (60/65) and `t_sweep_row4_ui.py` (20/23).

- **F183 (C)** — charter names have no uniqueness rule in the schema, the route or the table, and
  `AgentSettingsControls.tsx:283` renders each picker option as the name alone. Driven: the picker
  listed **four** options reading exactly `Code Reviewer`. `agents.py:612-617` refuses the same
  thing for agents, which is what makes this an inconsistency rather than a policy. Both doors are
  probed — create and rename — because closing one leaves the other.
- **F184 (C)** — `"   "` satisfies `min_length=1`, so the API accepts a whitespace-only name that
  its own dialog refuses (`ChartersPage.tsx:267-268` submits `name.trim()`). The picker came back
  containing a literal blank option.
- **F185 (B)** — `charters.py:95-98` selects blocking agents with no `Agent.lifecycle` filter while
  the roster hides archived ones, so an archived agent blocks a charter delete by a name the
  operator cannot find. The named repair *does* work on an archived agent — asserted, passes — but
  the refusal never says the holder is archived. **This is F181's shape at a second site.**
- **F186 (B)** — the charter screen deletes on one click. No dialog, no undo, no soft delete, and
  the record is the one thing on that screen the product cannot recreate. The file's own comment
  shows the risk was seen and answered only for a *mis-aimed* click.
- **F187 (B)** — `ChartersPage.tsx:191,198-203` pass only `onSuccess` to the create and update
  mutations. A 422 leaves the dialog open saying nothing. F173's shape at a third site, and worse
  in one respect: the sentence is a plain FastAPI detail the client already holds and drops.

### The row's headline question was driven — and the finding it nearly produced was disproved

**An edit to a bound charter reaches the next turn, including a resumed one.** Charter says
`ALPHAq1`, turn 1 replies `ALPHAq1`, `PATCH` to `BETAq1`, turn 2 in the *same* conversation
(`conv-45b7db016716` both times, `--resume` on provider session `bd6c486c-…`) quotes the new
charter back verbatim. `agent_trigger.py:893-895`'s claim now has a drive behind it.

**What almost got filed.** The first version asked turn 2 to *obey* rather than *quote*, and the
agent answered with the **superseded** marker. Its thinking then said, unprompted, *"There are two
charter instructions in my system prompt… 1. ALPHAq1… 2. BETAq1."* That is a specific, structured
report of the system prompt accumulating one ~7.7 kB context block per resumed turn — a real and
expensive defect if true. Three measurements, and the third killed it: the materialized context
file on disk carried only the new text; the Claude session transcript stores no system prompt at
all, so disk could not confirm it either way; and a **three-turn probe** — charter edited before
turn 2 and again before turn 3, then asked to *count* the marker lines — answered `1` /
`WORDTHREE`. One block, the current one.

So the model was reading its own earlier reply, which had quoted the old charter inside a thinking
block. **An agent's self-report about its own system prompt is evidence about the conversation, not
about what the harness delivered.** Two runs agreed with each other and were both wrong about the
mechanism. The harness now asks turn 2 to quote rather than obey, and carries a comment saying why.

This is the second harness bug this row caught before it became a finding. The first: `run_turn`
originally watched the agent *timeline* for a terminal event, and on turn 2 broke out instantly
because turn 1's `run_completed` was still there — reading the output before turn 2 had produced
anything, and reporting the product broken when it was not. Polling is now per-`run_id`.

### What held, worth naming after row 2

`agent-charter`'s **three UI requirements are all satisfied by the shipped screen** — collapsed on
first open (the longest starter is 8,529 characters and none of it is on the page), reading opens
zero dialogs and zero textareas, and two charters stay open at once. Row 2 found a shipped
requirement with no UI at all (F173), so a row where the spec and the screen agree is worth
recording. Also: seeding is the manifest byte for byte and runs once across a re-open; every
cross-project read, edit and delete answers the same `404 Charter not found` an unknown id gets;
the bound-delete refusal names the holder *and* a repair that actually works; and the canonical
context re-renders live, so an edit is visible on the very next read.

### Verification

- `t_sweep_row4_charters.py`: **60/65** with real turns, twice, on a fresh project and then against
  the state that run left. **52/57** with `AW_SKIP_TURN` on a rebuilt fixture after formatting. The
  reds are F183 (×3), F184 and F185 in every run.
- `t_sweep_row4_ui.py`: **20/23**, three times. The reds are F186, F187 and F183. Screenshots in
  `%TEMP%\row4shots`; `row4-05-deleted-no-confirm.png` and `row4-07-refusal-swallowed.png` are the
  two findings in one frame each.
- Console errors were asserted **after** filtering the 409 and 422 this harness deliberately
  provokes — an unfiltered assertion would have been red for the product working.
- `black --target-version py311` and `ruff` clean on both new files (two `SIM115`, one `I001`
  fixed). Neither tool's CI path list covers `scripts/`, so this is tidiness, not a gate.
- **No product code was changed**, so no suite was run. `git status` shows only `scripts/drive` and
  `.claude/autonomous`.

### Cleanup

Both fixture projects deleted (`204` each), both worktrees removed and both fixture directories
deleted, `GET /projects` back to the 23 that predate this iteration. The enabled-jobs sweep across
all 23 projects returns 0.

**Next:** N-2 continues at **row 5, Runs** — trigger, stop, sessions, reconciliation, divergence,
task binding. Rows 6–19 remain. The 8011 Hub is up on beta and current; it does not need restarting
unless product code changes.

---

## Iteration 7 — 2026-09-01T03:45+01:00 — N-2, row 5 of 19: Runs

**Position confirmed before starting.** Branch `autonomous/2026-09-01-daily`, tree clean, `git log`
head `bbe3d3a` — matching `STATE-night.json`'s iteration 6. Nothing to reconcile.

**The Hub needed no restart, and that was measured.** PID 22908 owns 8011, started
2026-09-01 01:48:32; `find hub/hub src -name '*.py' -newermt '2026-09-01 01:48:32'` returns
nothing, and `/health` answers `{"status":"ok","runtime":"native"}`. No product code was changed
this iteration either, so it is still current.

### Row 5 driven — 50 API assertions, 16 screen assertions, five real Haiku turns, five findings

Full write-up in `scripts/drive/FINDINGS.md` under *"row 5 of 19: Runs"*. Kept:
`t_sweep_row5_runs.py` (41/50), `t_sweep_row5_ui.py` (13/16), and `setup_row5.py`, which builds the
fixture (git init, commit, `POST /projects/open`, a Haiku runner, and three agents including an
archived one).

- **F188 (A)** — `agent_trigger.py:879-883` raises the worktree refusal without `agent_wide=True`,
  so `turn_scheduler.py:204-231` counts a delivery attempt and **destroys the operator's message at
  the third**. Driven on two fresh projects: `withdrawn / attempts 3 / "the Hub stopped retrying"`.
  The contrast is the proof — the *other* agent-wide refusal, an unbound runner, holds all four
  messages at `delivery_attempts: 0` through the same four schedules. F114's own measured example
  ("three messages to an unbound agent destroyed the first in under two seconds") reproduced
  verbatim at a site its fix did not reach.
- **F189 (B)** — `agent_trigger.py:2788` hardcodes
  `path=".agentweave/agents/{agent}-session.json"` for every provider session, a file nothing in
  the product writes; it is the shape of the deleted CLI session subsystem. The screen renders it
  (`AgentSettingsPage.tsx:435-442`) under a docstring saying *"this is where the agent's work
  actually happened"*. Measured on disk: `.agentweave/agents` does not exist, while
  `.agentweave/worktrees/<agent>` — the real answer — does.
- **F190 (A)** — the sharpest of the row, and the one three rounds of reading would not have found.
  `agentTimelineModel.ts:187-199` assigns `result[runId]` for every lifecycle event as it iterates,
  so the **last** one wins; `/agents/{name}/timeline` sorts `reverse=True`. The last event seen for
  any run is therefore its `run_started`, every run resolves to `started`, and
  `TERMINAL_LABEL['started']` is `undefined` — so *"Turn stopped"*, *"Turn failed"* and *"Turn
  interrupted"* can never render. Simulated the shipped loop over the real payload: six runs, six
  `started`, three of them `stopped` and two `completed` in the database.
- **F191 (C)** — `Conversation is unavailable` is one bare sentence for another project's id, an
  unknown id and a closed one alike, and never says that omitting the field starts a new one.
- **F192 (C)** — `POST /agent/<nonexistent>/stop` answers *"<name> has no run in progress"*, where
  the trigger route on the same file says *"is not an agent in this project"*.

### The row's headline question, and it vindicated the product

**Does STOP actually kill the process?** Yes — measured against the operating system, not the run
record. The run's `pid` was read straight out of the `runs` table (`33556`, `claude.exe`), its
descendants enumerated from a live `Win32_Process` snapshot, and every one of them was gone within
the polling window after `POST /agent/{agent}/stop`. The run then settled on `status='stopped'`,
`exit_code=2` — not misreported as `failed`, which is what `agent_trigger.py:1995-1999` says it
guards against and now has a drive behind it.

**Does a run killed mid-turn wedge its queue?** No. A second input sent while the first was running
was queued with `waiting_reason: "agent is already running"`, and **started on its own** once the
stop landed (`running: true, waiting_count: 0`). The stopped run's own input stays `delivered` on
purpose — the process did receive it — and still names the run that consumed it.

### F190 is the repository's named failure mode, inside a unit test

`hub/ui/src/__tests__/agentTimelineModel.test.ts:223-235` feeds `run_started` then `run_completed`
— **ascending**, the opposite of what the route returns — and asserts `'completed'`. Green,
correct, and about an input the product never produces. CLAUDE.md's *"a fix that passes its tests
and cannot fire in production"*, arrived at from the other direction: a feature that passes its
tests and cannot fire.

There is a second, independent gap behind the same symptom, recorded so a repair does not stop
half-way: the `Run {status} (exit {code})` line at `agent_trigger.py:2135` is only ever
`sse_manager.broadcast`ed and never persisted as an `AgentOutput` row. Measured — the stopped run's
`/output` holds one `thinking` row and no `status` row. So a reload, which is the ordinary way an
operator returns to a run they stopped, has nothing to read either way today.

### Two harness bugs caught before they became findings

Both are the row-4 lesson at a new site: **a harness that inherits state reports the product
broken.**

1. Run 2 said the worktree block did not fire. It did not exist: run 1 had left a *registered*
   worktree at that path, and planting a file inside one changes nothing. The harness now
   `git worktree remove --force` **and `prune`**s, then asserts the path is absent from
   `git worktree list` before planting the blocker.
2. Run 2 answered `200` to *"stopping an agent with no run in progress"* because a previous
   invocation had left the agent blocked inside `ask_user`. Both harnesses now settle the agent —
   decline pending questions, stop, drain queued entries — before any assertion that needs it idle.
   The UI harness also stops on the *first* output row rather than the third, because a Haiku turn
   can finish inside the polling window and a late stop is answered `404`.

### Verification

- `t_sweep_row5_runs.py`: **41/50** with real turns, on the run after its own state. Earlier passes
  scored 32/47 and 38/49 with the two harness bugs above; the reds that survive all three are
  F188, F189 (×2), F190's output-stream half, F191 (×2) and F192.
- `t_sweep_row5_ui.py`: **13/16**, twice. The three reds are F190 (mechanism and screen) and F189.
  Screenshots in `%TEMP%\row5shots`; `row5-01-after-stop.png` is F190 in one frame — the operator's
  message, *"Worked for 8s / Work · 1 step"*, and nothing else.
- Console errors asserted unfiltered, because the screen half provokes no 4xx deliberately: none.
- `black --target-version py311 --line-length 100` and `ruff` clean on all three new files (one
  `B905`, one `B007` fixed). Neither tool's CI path list covers `scripts/`, so this is tidiness,
  not a gate.
- **No product code was changed**, so no suite was run. `git status` shows only `scripts/drive` and
  `.claude/autonomous`.

### Cleanup

Both fixture projects deleted (`204` each), both fixtures' worktrees removed and pruned, both
directories deleted, `GET /projects` back to the 23 that predate this iteration. The enabled-jobs
sweep across all 23 returns 0.

**Next:** N-2 continues at **row 6, Conversations** — list, rename, release-task, chat history,
lineage, worker-generated titles. Rows 7–19 remain. The 8011 Hub is up on beta and current; it does
not need restarting unless product code changes.

---

## Iteration 8 — 2026-09-01T04:05+01:00 — N-2, row 6 of 19: Conversations

**Position confirmed before starting.** Branch `autonomous/2026-09-01-daily`, tree clean, `git log`
head `8ad0f64` — matching `STATE-night.json`'s iteration 7 and its released heartbeat. Nothing to
reconcile.

**The Hub needed no restart, and that was measured.** `/health` on `:8011` answers
`{"status":"ok","runtime":"native"}` and `GET /projects` served 23 rows from the beta profile. No
product code was changed this iteration either, so it is still current.

### Row 6 driven — 79 API assertions, 15 screen assertions, ~14 real Haiku turns, three findings

Full write-up in `scripts/drive/FINDINGS.md` under *"row 6 of 19: Conversations"*. Kept:
`t_sweep_row6_conversations.py` (75/79, twice) and `t_sweep_row6_ui.py` (13/15). Fixtures were
built with the existing `setup_row5.py`, as the previous `next_action` said.

- **F193 (B)** — archiving an agent leaves its conversations `open`, and the two rail views then
  disagree about whether they exist. `AgentTree.tsx:82` iterates `agents`, so an archived owner's
  bucket is never read and **the row is silently dropped**; `RecencyView.tsx:55,66` iterates
  `conversations`, so **the same row is listed** and eats one of 15 `RECENCY_DISPLAY_CAP` slots.
  Opening it renders the whole main panel as two words — `Agent unavailable.` (`App.tsx:371-374`)
  — with no name, no reason and no remedy, when the remedy is one unarchive and the product has a
  perfectly good sentence for it (*"… is archived and cannot be triggered. Unarchive it first."*)
  sitting one route away. `row6-05-orphan-opened.png` is the finding in one frame.
- **F194 (C)** — `GET /agent/{agent}/conversations` and `GET /agent/{agent}/chat` answer `200 []`
  for an agent that does not exist. A typo reads as "no conversations yet". The trigger route
  refuses the identical mistake precisely, so the information exists and these routes decline to
  look. This is the mechanism by which F193 stays quiet.
- **F195 (C)** — the conversation titler does not run in the project's directory.
  `generate_conversation_title(cwd=None)` threads `cwd` all the way to `subprocess.run`, and
  **neither caller ever supplies one** (`agent_trigger.py:2146`, `:2738`), so every titling spawn
  inherits the Hub process's working directory. That is CLAUDE.md's *"never use the Hub process's
  `Path.cwd()` as project identity"* rule, broken.

### The measurement F195 rests on, because a dead parameter is only half an argument

A `CLAUDE.md` was planted in the fixture project's root reading *"any title you write MUST begin
with the single word ZEBRA"*, and the module's own `_PROMPT` was run two ways:

```
control — claude --model claude-haiku-4-5-20251001 -p "<_PROMPT>", from the project directory
              ZEBRA B-tree indexes for database lookups        <- honours the project's memory
product — same model, same prompt, same project, through the titler
              B-tree indexes explained                         <- does not
```

The control proves the mechanism works; the product's title proves the spawn is somewhere else.
This is row 5's third lesson (measure outside the product) applied to a claim the code alone could
only half support.

### The row's headline questions, and the product answered all of them

- **Does a worker-generated title actually appear?** Yes, three times, judged on the
  `conversations.title` column read out of sqlite rather than on a response echo. *"In one short
  sentence, explain what a semaphore does in concurrent programming…"* became *"Semaphore explained
  as concurrency synchronization primitive"*, and the rail and the header both show it.
- **Does an operator-set title survive the next turn?** Yes. `title_set_by_operator` is checked
  before the spawn and again on a re-read afterwards, so a rename made while the model was thinking
  also wins; both guards are live.
- **Does lineage survive a checkpoint cutover?** Yes, measured in sqlite because no route exposes
  `lineage_id`: successor `conv-4d3564b7ec05` carries predecessor `conv-2a78c785ef1b`'s lineage,
  the predecessor is archived, and the successor is marked operator-set so the titler cannot
  overwrite its `Continued: …` title.
- **Does a rename refusal reach the operator?** **Yes** — and this one matters, because the answer
  has been *no* at six previous sites (F169, F173, F178, F179, F180, F187). A 400-character title
  typed into the rail's rename input renders *"A title cannot exceed 120 characters"* under the row
  (`ConversationRow.tsx:295`, `role="alert"`). The counter-example that shows the swallowed-refusal
  pattern is a gap rather than a house style.

### A refinement to F190's second half

Row 5 said the `Run {status} (exit {code}).` line is broadcast and never persisted. Correct — and
counting `agent_outputs` by run sharpens it: five **completed** runs each persist exactly one
`status` row, five **stopped** runs persist none. The persisted row comes from the stream parser
and its whole content is the word `Completed`, so it can never say *stopped*, *failed* or
*interrupted*. F190's conclusion is unchanged; its reason is narrower than "nothing is persisted".

### Two harness bugs caught before they became findings

Row 5's lesson at two more sites. (1) The UI harness clicked the project expander unconditionally
and **closed** the project the operator was already inside, then called four things broken; it now
reads `aria-label`/`aria-expanded` and expands only what is collapsed. (2) *"the conversation is on
the rail by its title, never by its id"* was red against the whole sidebar because an earlier drive
left an agent in **another project** literally named `conv-probe`; scoped to the project's own
subtree it is green. Believed, that one would have been written up as a defect that does not exist.

### Verification

- `t_sweep_row6_conversations.py`: **75/79**, twice, the second time on the state the first left.
  The four reds are F194 (×2), the bare `Conversation not found` refusal, and the deliberate
  `lineage_id` marker (recorded in FINDINGS as *considered and not filed*).
- `t_sweep_row6_ui.py`: **13/15**, the two reds both F193.
  Screenshots in `%TEMP%\row6shots`; `row6-05-orphan-opened.png` and `row6-02-rail-recency.png`.
- Console errors asserted with the one deliberately provoked 400 filtered out: none.
- `black --target-version py311 --line-length 100` and `ruff` clean on both new files. Neither
  tool's CI path list covers `scripts/`, so this is tidiness, not a gate.
- **No product code was changed**, so no suite was run. `git status` shows only `scripts/drive` and
  `.claude/autonomous`.

### Cleanup

Both fixture projects deleted (`204` each), both fixture directories removed with their worktrees,
`GET /projects` back to the 23 that predate this iteration, and the enabled-jobs sweep across all
23 returns 0. This repository's own `git worktree list` is unchanged (the six long-standing
`.agentweave/worktrees` entries).

**Next:** N-2 continues at **row 7, Inbound queue** — per-agent durable queue, hop budget, turn
delivery cap, withdraw. Rows 8–19 remain. The 8011 Hub is up on beta and current; it does not need
restarting unless product code changes.

---

## Iteration 9 — 2026-09-01 04:07–05:00 +01:00 — N-2, row 7 of 19: Inbound queue

**Position confirmed before starting.** Branch `autonomous/2026-09-01-daily`, tree clean, `git log`
head `d7c8449` — matching `STATE-night.json`'s iteration 8 and its released heartbeat. Nothing to
reconcile.

**The Hub needed no restart, and that was measured.** `/health` on `:8011` answers
`{"status":"ok","runtime":"native"}`; PID 22908 has owned the port since 01:48:32 and the beta
profile's database mtime moved within the same second as a `GET /projects` (23 rows). No product
code has changed on this branch since, so it is still current.

### Row 7 driven — 52 API assertions (twice), 13 screen assertions, 12 hop-chain assertions, five findings

Full write-up in `scripts/drive/FINDINGS.md` under *"Sweep, row 7 of 19: Inbound queue"*. Kept:
`setup_row7.py`, `t_sweep_row7_queue.py` (42/52, twice) and `t_sweep_row7_ui.py` (9/13), plus
`t_row6_hop_chain.py` re-run on the new fixture for the hop leg (**12/12, first attempt**).

- **F196 (B)** — `PATCH /queue/settings` declares `Field(ge=1)` with no upper bound
  (`inbound_queue.py:48-52`) while `ProjectSettings` declares `Field(ge=1, le=1000)` for the same
  four columns (`projects.py:76-78`). Writing `hop_budget: 1001` through the queue route is
  accepted `200` — and then `GET /projects/{id}/settings` answers **500**, and so does the `PUT`
  that would repair it, because `update_project_settings` starts by validating the *stored* row
  outside its own `try` (`projects.py:463`). The repair route is wedged by the value it exists to
  change; the only way back is the queue route itself, which the UI never calls, or the database.
- **F197 (B)** — the screen half, and a defect on its own: `ProjectSettingsPanel` has no error
  branch for its settings *query* (only for the two mutations, `:88`), so any failure of it renders
  the loading skeleton indefinitely with no message. `row7-02-settings-wedged.png`.
- **F198 (C)** — `PATCH /queue/settings` silently resets the fields a body omits: `agent_budget`
  25 → 8 and `allow_agent_jobs` True → False, answered `200`. The sibling `PUT
  /projects/{id}/settings` solved exactly this and recorded the incident that forced it
  (`projects.py:150-181`); the queue router writes four of the same columns with none of it.
- **F199 (C)** — `GET /queue/{agent}` and `/queue/{agent}/status` answer `200` for an agent that
  does not exist, the status one with a full healthy-looking object. F194's shape at a second
  router. Also: `?state=<bad>` refuses with `Invalid queue entry state` and does not name the three
  that are valid.
- **F200 (C)** — one refusal string covers four distinguishable states (absent / another project's
  / delivered / withdrawn), and it tells the operator their message was *delivered* when it was
  not. Driven with a real entry id passed to a different project's route; isolation held, the
  sentence did not.

### The mechanism the row existed to measure, and it works

`turn_delivery_cap` had never been driven live. With the cap at 2, an occupied agent and four
operator messages queued behind the running turn, the drain produced **exactly two runs of two**,
on both runs of the harness — and each run's own output mentions both of its words, so
`format_turn_prompt` carried the second entry of a batch rather than dropping it:

```
run-30dc05e21e14: 2 entries ['ALPHA', 'BETA']     output mentions ALPHA, BETA
run-abb0563affe9: 2 entries ['GAMMA', 'DELTA']    output mentions GAMMA, DELTA
```

The hop budget held equally well: under `hop_budget = 1` a `r7a → r7b → r7a` chain stops at hop 2,
the timeline marks it held, the status route names the budget, an operator message alongside is not
caught by it, and `release` re-bases and delivers within ~8s. **F114's fix still holds** — four
messages to an unbound agent, four schedules, all four still `queued` at `delivery_attempts: 0` —
and the sentence it produces is on screen, in the conversation panel, beside the Continue control.

### Measured outside the product

26 queue entries read straight out of `inbound_queue_entries` over a read-only sqlite URI and
compared field by field with what `GET /queue/{agent}` returned — `state`, `hop_depth`,
`delivery_attempts`, `abandoned_reason`, `delivered_in_run_id`. Zero mismatches, and no entry the
table holds that the route hides. The queue's own bookkeeping is honest; every finding this row
produced is about a *sentence* or a *range*, not about the mechanics.

### One assertion disproved, one harness bug caught

- The first version of the harness asserted that a withdrawn entry stays in the conversation
  timeline. It does not, **deliberately**: `agent_chat.py:262-269` keys its withdrawn branch on
  `abandoned_reason IS NOT NULL` so that an entry the *Hub* dropped stays (F87) and one the
  *operator* withdrew goes. Recorded in FINDINGS as considered-and-not-filed; the assertion now
  checks the deliberate behaviour.
- *"the screen gives the reason the status route already knows"* was **green against
  `inner_text("body")`** — because the sidebar carries an Environment link labelled **Runners**, so
  `"runner" in text` matches on every page in the product. Scoped to
  `[data-testid="conversation-workspace"]` it is still green and now means something. Row 6's fifth
  lesson, arriving from the false-*green* side this time.

### Verification

- `t_sweep_row7_queue.py`: **42/52**, twice, the second run on the state the first left. The ten
  reds are F196 (×3), F198 (×2), F199 (×3) and F200 (×2).
- `t_sweep_row7_ui.py`: **9/13**; all four reds are F196/F197. Screenshots in `%TEMP%\row7shots`.
- `t_row6_hop_chain.py` on the row 7 fixture: **12/12**, no failures.
- Console errors asserted with the provoked 500s filtered: none.
- `ruff` and `black --target-version py311 --line-length 100` clean on all three new files. Neither
  tool's CI path list covers `scripts/`, so this is tidiness, not a gate.
- **No product code was changed**, so no suite was run. `git status` shows only `scripts/drive` and
  `.claude/autonomous`.

### Cleanup

Fixture project deleted (`204`), fixture directory removed with its two agent worktrees,
`GET /projects` back to the 23 that predate this iteration, enabled-jobs sweep across all 23
returns `[]`. This repository's own `git worktree list` is unchanged (the six long-standing
`.agentweave/worktrees` entries).

**Next:** N-2 continues at **row 8, Tasks** — CRUD, board(s), the declared transition machine,
dependencies and the gate, integrations, divergences. Rows 9–19 remain. The 8011 Hub is up on beta
and current; it does not need restarting unless product code changes.

## Iteration 10 — 2026-09-01 04:27–04:50 +01:00 — N-2, row 8 of 19: Tasks

**Position confirmed before starting.** Branch `autonomous/2026-09-01-daily`, tree clean, `git log`
head `bdfc85b` — matching `STATE-night.json`'s iteration 9 and its released heartbeat (03:46, ~40
minutes behind 04:27). Nothing to reconcile.

**The Hub needed no restart, and that was measured.** `/health` on `:8011` answers
`{"status":"ok","runtime":"native"}`; PID 22908 is alive and `GET /projects` returned the expected
23. No product code has changed on this branch, so it is still current. Nothing under `hub/hub` or
`src` was touched this iteration either, so no suite was run.

### Row 8 driven — 313 API assertions (twice, identical), 8 screen assertions, three findings

Full write-up in `scripts/drive/FINDINGS.md` under *"Sweep, row 8 of 19: Tasks"*. Kept:
`t_sweep_row8_tasks.py` (**19 failures on both runs, the same nineteen**) and `t_sweep_row8_ui.py`
(4/8). Screenshots in `%TEMP%\row8shots`.

- **F202 (B)** — `GET /projects/{id}/tasks` defaults to `limit=100`, orders by `created_at`
  **ascending**, and returns a bare array with no `total`, no `has_more` and no `next`. On the
  fixture at 241 tasks the Overview page reads **"3 agents · 100 tasks"**, its NAVIGATE card reads
  **"Tasks — 100 total"**, and its nine status chips sum to exactly 100. The board rendered 82
  cards and a task created seconds earlier was not on it. `useTasks`
  (`hub/ui/src/api/tasks.ts:210-225`) sends no `limit`; `OverviewPage.tsx:86` is
  `const taskCount = tasks.length`. The agent's half is worse: `mcp_server.py:278-280`'s
  `list_tasks` has **no `limit` or `offset` parameter at all**, so an agent past 100 tasks gets the
  oldest hundred and cannot ask for more. `GET /tasks/board`, over the same table, has no limit —
  so the two routes disagree about how many tasks a project has and the silent one feeds the
  Overview.
- **F201 (C)** — `PATCH {"status": "blocked"}` on a task where that edge does not exist is answered
  **422 "blocked_reason is required when setting a task to blocked"**, not the machine's 409. That
  breaches a named scenario in a shipped spec (*Work not yet started cannot be waiting* → "the
  refusal names what is reachable instead") and the MUST above it. The cause is ordering:
  `TaskUpdate`'s `model_validator(mode="after")` runs while FastAPI parses the body, before
  `apply_transition` can consult the map. Reproduced at all seven from-statuses; 14 of the 19
  failures are this. Held at C because the UI collects the reason first and MCP omits `blocked`
  from `TaskStatus` — the population is direct HTTP clients.
- **F203 (C)** — read off the **live** OpenAPI (158 paths, 17 task routes): no route reads a task's
  transition history, on either plane, and no MCP tool does. `history_for()` exists and is called
  only by tests. The machine's docstring says binding the operator to the map is *"what makes the
  history worth reading"*; nothing can read it. `policy_digest` has never been visible to anyone.

### The larger half: the machine survived a total sweep

Not a sample — the whole map. **All 30 declared operator edges walked for real** and accepted.
**All 50 non-edges refused 409**, each naming the current status and listing **exactly**
`allowed_targets(from, operator)`, parsed back out of the sentence and compared set-for-set. No
refusal moved anything. **Published == enforced == observed at all nine statuses** — design D13's
whole claim, checked three ways. D7's no-op restatement recorded nothing, counted out of sqlite.
The dependency gate's structure, its `rejected`/`unmet` split, and its four declare-route refusals
all held; **design D5's `blocked -> in_progress` exemption held together with its mirror** (the
same prerequisite still refuses at `pending -> in_progress`, and the resumed task comes back
`running_on_regressed`). 415 transition rows read read-only out of the database: every one an edge
the map declares, all `origin: actor`, none restating a status.

### Method notes worth carrying

- **The finding came from the boundary, not the assertion.** "The route reports every task the
  table holds" was green at 91 tasks and red at 100. Neither the route (200 both times) nor the
  table alone would have shown it; only the comparison, run twice at different sizes, did.
- **The harness transcribes the transition map by hand rather than importing it.** Importing
  `hub/hub/task_transitions.py` would have asserted the product against itself.
- **LEG 8 asserts against the live OpenAPI**, not the source, so its two failures are facts about
  the deployed build and flip on their own when a route is added.
- **A screen assertion that passes for a reason you cannot name has measured nothing.** "The
  Overview page does not state a task count capped at 100" *passed*, because it looked near the
  word "Tasks" and the page renders the number in a subtitle it never reached. The screenshot
  settled it. Companion to row 7's lesson, from the false-green side again.
- **Two UI legs never ran, and F202 is why**: they act on a task created at the top of the run,
  which is by construction the newest, which the board does not render. Reported as a named failure
  rather than skipped — and F201's "the UI cannot produce this" is therefore read from source, and
  labelled that way in FINDINGS.

### Verification

- `t_sweep_row8_tasks.py`: 313 PASS / 19 FAIL, **twice**, the second on the state the first left,
  same nineteen. The reds are F201 (×14), F202 (×3) and F203 (×2).
- `t_sweep_row8_ui.py`: 4/8; all four reds are F202 or its consequence.
- No console errors on any page.
- `ruff` clean and `black --target-version py311 --line-length 100` applied to both new files.
  Neither tool's CI path list covers `scripts/`, so this is tidiness, not a gate.
- **No product code was changed**, so no suite was run.

### Cleanup

Fixture project `proj-f2538b3807fe` deleted (`204`), fixture directory removed, `GET /projects`
back to the 23 that predate this iteration, enabled-jobs sweep across all 23 returns `[]`.

### Row 8's inherited findings, re-driven

Both cost no agent turn and both build their own project, so both were run rather than cited.

- `t_f154_wedged_review.py` — **18/18**. F154 stays **FIXED**: the firing answers 409 naming the
  task, the idle reviewer and the three exits, on both presses, with `stall_reason` carrying the
  same sentence and no run started either time. Its fifth lane re-measures **F167's row reaching
  the same named refusal**, unchanged.
- `t_f156_preview_promises_the_merge.py` — **21/21**. F156 reproduces **verbatim and stays open**,
  third consecutive drive: `will_merge: true` over the exact commit the gate refused for a
  conflict, unchanged after the refusal, and byte-identical to a clean task's preview that then
  merged. This is the finding N-3..N-6's spec loop is queued behind.

Both projects and both temporary repositories were deleted afterwards. `t_f155_conflict_remedy.py`
and `t_f167_agent_walked_edge.py` were **not** re-run: F155's harness follows a remedy end to end
and F167's spawns a real run, and neither fits what was left of this iteration. Row 8's F167 leg
was covered by `t_f154`'s fifth lane instead.

**Next:** N-2 continues at **row 9, Spec flow** — the largest row in the matrix (documents,
lifecycle, rigor, proposals, requirements, coverage, evidence, drift, reindex, corpus adopt). Rows
10–19 remain. The 8011 Hub is up on beta and current; it does not need restarting unless product
code changes.

## Iteration 11 — 2026-09-01 04:52–05:15 +01:00 — N-2, row 9a of 19: Spec flow (documents)

**Position confirmed before starting.** Branch `autonomous/2026-09-01-daily`, tree clean, `git log`
head `e8d29dc` — matching `STATE-night.json`'s iteration 10 and its released heartbeat (04:08,
~44 minutes behind 04:52). Nothing to reconcile.

**The Hub needed no restart, and that was measured.** `/health` on `:8011` answers
`{"status":"ok","runtime":"native"}` and PID 22908 is alive. No product code has changed on this
branch and none was touched this iteration, so no suite was run.

### Row 9 is split, and this iteration did 9a only

Row 9 is the largest row in the matrix. The split `STATE-night.json` proposed was taken as written:
**9a** = documents + lifecycle + phase + adopt/arrange/merge (this iteration); **9b** =
requirements + coverage + rigor + proposals; **9c** = evidence + decisions + reviews + drift +
reindex. Neither 9b nor 9c was driven.

### Row 9a driven — 196 API assertions (twice), 18 screen assertions (twice), five findings

Full write-up in `scripts/drive/FINDINGS.md` under *"Sweep, row 9a of 19"*. Kept:
`t_sweep_row9_documents.py` (**196 PASS / 11 FAIL**, then **195 / 10** on a second run over the
state the first left — the one-assertion difference is leg 6's virgin-corpus branch, and the
substantive reds are the same both times) and `t_sweep_row9_ui.py` (**2 failing, twice, the same
two**). Screenshots in `%TEMP%\row9shots`.

- **F206 (B)** — five operator-only spec routes have **no operator surface**: `documents/adopt`,
  `spec/reindex`, `spec/documents/arrange`, `spec/adopt`, `documents/{path}/merge`. Measured off the
  **served bundle** (`hub/hub/static/ui`, 1.21 MB read as bytes) and again off `hub/ui/src`; both
  agree, and the live OpenAPI confirms none has an agent-plane equivalent either. So the
  corpus-shaping half of the spec flow is reachable only by `curl` — including `spec/index.json`,
  the only record of home, hierarchy and ordering that survives a project being copied. F203's
  shape at five times the size: F203 was one read nothing could reach, this is five writes.
- **F205 (C)** — the two edges added for **F37** (`exploring -> archived`, `proposed -> archived`)
  are offered by no screen. `SpecPhaseBar.tsx` is the only component that calls `useSetSpecPhase`
  and its Archive button renders only at `phase === 'approved'`. `row9-empty.png` is the F37
  scenario verbatim: an empty document in `exploring`, a banner reading **"⚠ 1 spec manifest drift
  item"**, and a phase bar with nothing that retires it — while the Hub accepts the move on a twin.
  `proposed -> archived` is worse: it is dead outright, because every document that reaches
  `proposed` legitimately has requirements and is then refused by `archive_would_orphan_work`.
- **F204 (C)** — `POST /documents/phase` called with **no body** is `422 {"loc": ["body"], "msg":
  "Field required"}` before the phase machine is consulted. `PhaseRequest`'s only field is optional;
  the parameter has no default, so FastAPI requires the body. Its two siblings —
  `close-exploration` and `propose` — take no body and answer 200 without one. F201's exact shape,
  one subsystem over. The UI always posts `{reason: ''}`, so the population is direct HTTP clients.
- **F207 (C)** — two doors into `proposed`, and the second skips the first's checks.
  `/documents/propose` runs `spec_completeness.check` and refuses; `/documents/phase?to=proposed`
  calls `transition()` directly. Four calls take an empty document from creation to **`approved`**
  with two completeness findings unresolved.
- **F208 (D)** — `arrange` on an unindexed corpus says only *"no usable index to arrange
  (absent)"*, with an empty `diagnostics` and no mention of the remedy (`reindex`, naming a home),
  while `reindex` itself reports `home_ambiguous` correctly.

### The larger half: the phase machine survived a total sweep

Not a sample. **All 5 phases × 6 targets = 30 ordered pairs**, against a map **transcribed by
hand** rather than imported. All **7 declared edges accepted** on documents walked into their
from-phase through the real routes; all **23 non-edges refused 409** with the right code —
`phase_unchanged` for the four self-moves, `unknown_phase` for `current` and for nonsense at every
from-phase, `illegal_transition` for the rest, each naming both phases — and **no refusal moved
anything**. `current` is exactly as unreachable as documented, and `archived` has no exit.

Every gate held: `explore_not_closed`, `archive_would_orphan_work` (with its remedy sentence),
reopening genuinely reopening, and `first_approved_at` — read **read-only out of sqlite** because
no route exposes it — set once and unchanged across a reopen-then-reapprove. **F112 stays fixed.**
Creation, adoption (phase read from the file, not defaulted), arrange-once-indexed, and merge all
held in full, including merge leaving both sources in the phases they were in.

**F202's shape does not generalise here, and that is measured.** The document list was given the
boundary rather than read for a signature: **147 documents returned 147, and 294 returned 294**.
There is no `limit` or `offset` anywhere in `hub/hub/api/v1/spec.py`.

### Method notes

- **The bodyless probe found F204** — not an assertion anyone wrote, but the harness dying on its
  own first call. Three sibling routes called the same way; the odd one out only shows that way.
- **Ask the product, then ask the screen, then compare.** The UI harness archives a *throwaway twin*
  of each subject through the API before opening a browser, so F205's table is Hub-behaviour
  against screen-behaviour rather than against the author's belief.
- **Row 8's lesson 8 fired twice more, both against this harness.** The first UI run opened
  documents by clicking tree rows; the clicks silently did nothing, the pill read `exploring` for
  all six subjects, and four assertions "measured" one document six times. In leg 6 every arrange
  refusal passed — for `409 no usable index`, not for the rule each was named after. Both caught by
  reading the reason for the green.
- **State the precondition instead of asserting through it.** Run 2's two extra reds were facts
  about run 1 (an index it had written, a home now preserved); those checks are gated on
  `index.written is None` and say so.

### Verification

- `t_sweep_row9_documents.py`: 196/11 then 195/10 on the state the first left. Reds are F204 (×2),
  F207 (×2), F208 (×1, virgin-corpus only), the operator-`rename` observation (×1) and F206 (×5).
- `t_sweep_row9_ui.py`: 2 failing, twice, both F205.
- No console errors on any spec page.
- `ruff` clean and `black --target-version py311 --line-length 100` applied to both new files.
  Neither tool's CI path list covers `scripts/`, so this is tidiness, not a gate.
- **No product code was changed**, so no suite was run.

### Cleanup

Fixture projects `proj-07ac1934d86d` and `proj-86d80e81bc45` deleted (204 each), plus the three
earlier iterations of the same fixture; `%TEMP%\row9fixture` and `%TEMP%\row9ui` removed;
`GET /projects` back to the 23 that predate this iteration; enabled-jobs sweep across all 23
returns `[]`.

**Next:** N-2 continues at **row 9b** — requirements, coverage, rigor and proposals — then 9c
(evidence, decisions, reviews, drift, reindex), then rows 10–19. The 8011 Hub is up on beta and
current; it does not need restarting unless product code changes.

---

## Iteration 12 — 2026-09-01T06:44:29+01:00 — N-2, row 9b of 19: spec flow, the requirements half

**This iteration recovered and landed work its own earlier process had finished but never
committed.** The tree was found dirty at 06:42 with 40 minutes gone and 18 left before `stop_at`:
a complete 705-line harness (`scripts/drive/t_sweep_row9b_requirements.py`) and 265 lines of new
`FINDINGS.md`, reporting **129 passed / 10 failed, twice**, the second run on the state the first
left. The drive had run; only the commit was missing. Starting a fresh drive in 18 minutes would
have thrown that away, so the unit of work became: verify it is genuine, clean up after it, land
it.

### Verification of the inherited work — static, and it holds

Not taken on trust. Each load-bearing claim was re-derived from the code by this process:

- **F209** — `accept_proposal` (`spec_service.py:485`) takes `actor` and `expected_digest` and
  **no `reason`**; `reject_proposal` (`:557`) takes `reason: str = ""`, and line 580 is the only
  `resolution_reason` write in the file. The asymmetry is exactly as filed.
- **F212** — `spec.py` returns `"unserved": [row.identifier for row in unserved]`, confirmed at
  the reported line, beside a `report.to_dict()` that does carry `document_id`.
- **F210** — `ProposalDecision` has both fields defaulted (`reason=""`, `expected_digest=None`)
  while `body: ProposalDecision` on the accept route has no default. F204's shape, confirmed.
- **F211** — the bundle grep was re-run independently and reproduces the table exactly:
  `spec/requirements` 0/0, `rigor-history` 0/0, `spec/coverage` 1/1, `/rigor` 1/2
  (served bundle / UI source).
- **The boundary claim** — `grep -c "limit\|offset" hub/hub/api/v1/spec.py` = **0**. F202's shape
  genuinely does not generalise to this router.

Six findings filed, **F209–F214**: the accept route silently discarding a declared 2000-character
`reason` (C); both proposal decisions refusing a bodyless call (C, F204's shape twice more, with
rigor recorded as a measured *negative*); three more routes with no operator surface (C), including
`rigor-history`, whose own module argues demotion is legitimate *because* the trail exists; `unserved`
reported as bare identifiers that the route built to explain them refuses as ambiguous (C); a
resubmitted edit stacking a duplicate proposal whose twin can then never be resolved (D); and a
retired requirement reported `unserved` (D).

### Cleanup — done by this process, not inherited

The drive's fixture outlived it. `proj-401c38a75fcd` ("row7-051754") was still on the trial Hub:
deleted (204), project count back to the **23** that predate this iteration, and an enabled-jobs
sweep across all 23 returns `[]`. The 8011 Hub is up and current; no product code changed, so it
does not need restarting.

### Verification of this iteration's own work

- `ruff` on the new harness: 3 errors found (two `C401`, one `SIM105`), **fixed**, now clean.
- `black --target-version py311 --line-length 100`: reformatted, now clean.
- `ast.parse` after every edit. Neither linter's CI path list covers `scripts/`, so this is
  tidiness, not a gate.
- **No product code was changed**, so no suite was run. The drive's own numbers (129/10, twice) are
  the previous process's measurement and are labelled as such above — this process re-derived the
  static half of them and did not re-run the harness.

### Method note

**A dirty tree at the start of an iteration is evidence, not an obstacle.** The instinct is to
reset it and start the named work cleanly. Reading it first found a finished drive one commit short
of durable, and 18 minutes was enough to verify and land it but nowhere near enough to redo it.

**Next:** N-2 continues at **row 9c** — evidence, decisions, reviews, drift, reindex — then rows
10–19. 9b hands 9c three questions, recorded in `next_action`.

---

## Iteration 13 — 2026-09-01T06:47:41+01:00 — WINDOW CLOSE

`stop_at` is `07:00+01:00` and this process started at **06:47**. Thirteen minutes is not a row 9c
drive — rows 9a and 9b each took a full iteration, and the one time an iteration ran short of its
window (iteration 12) the cost was a finished drive left one commit from durable. So the unit of
work for this iteration is the one the playbook names for `stop_at`
(`.claude/loops/night-window.md`, *"Ending the window"*): the final entry, and `next_action` set to
`null` so the driver unregisters instead of spending another invocation.

**Row 9c was not started.** No harness was written, no Hub was driven, no fixture project was
created. Everything `next_action` said about 9c — the reindex/drift lead, the bodyless-call probe,
the bundle grep, the projection question — is intact and unconsumed, and is preserved below and in
`STATE-night.json` for whoever picks it up.

### What changed — 47 commits on `autonomous/2026-09-01-daily` from `ad60b7b`

**Product code, review closely — exactly one commit:**

- `4ce24fc` `fix(deps): bound fastmcp to <4, and name the APIs the tool surface stands on`.
  `pyproject.toml` (×2 declarations), `hub/pyproject.toml`, and a new
  `hub/tests/test_fastmcp_api_contract.py` (152 lines, 6 tests). +171/−3 across three files. That
  is the **entire** product-code diff of the night: `git diff --stat ad60b7b..HEAD -- hub/hub src
  tests hub/tests hub/ui pyproject.toml hub/pyproject.toml` names those three files and nothing
  else.

**Corpus edits, skim — seven commits:** `48ed2ba`, `a15b52f`, `f2acd28`, `1fcccab`, `7225eae`,
`8999850`, `a0fd855` archive seven completed openspec changes in the order their own tasks demanded,
with specs synced. File moves plus corpus edits; no `.py`, `.ts` or `.tsx` touched.

**Drive output, read for the findings — ten commits:** `3280f52` (row 1), `6908cfd` (2), `58bd3fd`
(3), `66e085f` (4), `2a41e05` (5), `47015f1` (6), `deff706` (7), `7e45a27` (8), `fce9f83` (9a),
`b9854c2` (9b). These add harnesses under `scripts/drive/` and sections to
`scripts/drive/FINDINGS.md`. **No product code.**

The remaining commits are heartbeat claims/releases, this log, and `75e2d1f` pointing LATEST at
handoff 0104.

### What was proven, and how

**Driven against a live Hub on 8011, fresh project per row — rows 1 through 9b.** Every finding in
`F170`–`F214` that is not explicitly labelled otherwise came out of an HTTP call or a real browser
page, run **twice**, the second run on the state the first left. **45 findings filed: 3 A, 14 B,
25 C, 3 D.**

Read, not driven, and labelled so in the ledger: `F169` (`approval_report` reaching no UI file), and
the static half of `F209`–`F214` as re-derived by iteration 12 from a previous process's run.

**Vindications matter as much as the findings** and are recorded in `FINDINGS.md` rather than left
implicit: the task transition machine (row 8), the spec phase machine (9a), the turn delivery cap
(row 7), conversation titles (row 6), and run `stop` (row 5) each survived a deliberate attempt to
break them. One row-4 finding was **disproved** and says so in the ledger.

**Measured, not assumed, where the product could have lied:** the served UI bundle was grepped as
**bytes** off `hub/hub/static/ui`, and route inventories were read from the **live OpenAPI**, not
from the source. That is what makes the "no operator surface" findings (`F206`, `F211` — 11 routes
so far) claims about the shipped artefact rather than about the repository.

### The three A-severity findings — read these first

- **`F173`** — runner management is free-typed text that swallows its own refusal, and the spec says
  otherwise (`FINDINGS.md:12939`).
- **`F188`** — a repairable workspace fault **destroys the operator's message** after three
  schedules, while the identical fault one line away holds it forever (`:13701`).
- **`F190`** — no turn can ever report that it was stopped, failed or interrupted, because the
  status map is built backwards from a payload sorted the other way (`:13800`).

`F188` and `F190` are both in Runs, both silent, and neither is visible to a user who does not
already suspect it.

### What is open

- **Row 9c of the sweep** — evidence, decisions, reviews, drift, reindex. Not started. Its four
  leads are preserved in `STATE-night.json` under `sweep_state.row_9c_leads`; the sharpest is (a):
  9a measured `reindex` answering **200 with `index.written: null`** on a corpus with no home
  recorded, so drive **drift** against a corpus reindex has "succeeded" on without writing.
- **Rows 10–17** of `scripts/drive/SURVEY.md`'s inventory, untouched: Jobs+Loops, Questions,
  Permissions, Checkpoints, Accounting, Worktrees, Logs/Events/SSE, Messages.
- **Queue items N-3 through N-7** — the F156 spec loop (three rounds then implement) and the
  known-wrong line at `openspec/explorations/2026-08-30-release-roadmap.md:107`. All still `open`;
  none was started.
- **45 new findings, all unfixed.** The night was a sweep, not a repair run. Nothing in `F170`–`F214`
  has a change proposed against it.

### Decisions waiting for the operator

29 entries in `decisions_for_user`. The ones this night created or sharpened:

1. **`fastmcp` was bounded to `<4` unattended on 2026-09-01** — ratify or widen. Defensible to take
   alone because it is reversible in one line; the evidence is in iteration 1's entry, including a
   correction: the queue item's claim that seven test files call `get_tools()` was **wrong** — they
   call `list_tools()`, which survives 4.0.0. The action stands on the remaining evidence.
2. **`fastapi>=0.110` is still unbounded** and has produced two CI-only failures via Starlette major
   bumps. Explicitly *not* taken tonight, because unlike the fastmcp bound it is not a one-line
   reversal.
3. **45 findings and no repair capacity** — the sweep is generating findings far faster than the
   build windows retire them. Whether to pause sweeping and spend windows on `F173`/`F188`/`F190` is
   a prioritisation call, not one to guess.
4. **Whether the 8010 trial Hub should be restarted from current master** — it has served
   2026-08-29 code for four days now.

### What to distrust

- **`green_at_arming` was never measured tonight.** It is inference from `9fa4c4b` on 2026-08-31
  plus the observation that only one commit since carries product code. Strong, but not a run.
- **The full suites were never run.** Iteration 1 ran the chunks covering its own change (82 + 31 +
  3 passed) with ruff/black/mypy clean over CI's own path lists. Nothing after iteration 1 changed
  product code, so nothing after iteration 1 ran a suite. **The hub suite (~25 min) has not run on
  this branch.**
- **The drive numbers for row 9b (129 passed / 10 failed, twice) are a previous process's
  measurement**, recovered from a dirty tree by iteration 12 and re-derived only statically. They
  are labelled that way in iteration 12's entry and remain so.
- **`scripts/` is outside both linters' CI path lists**, so the ruff/black passes on the harnesses
  are tidiness, not a gate.
- Every drive ran against the **8011** Hub on the beta profile with a fresh fixture project each
  time. Fixtures were deleted and the project count confirmed back at **23** after every row; the
  final enabled-jobs sweep across all 23 returned `[]`. `proj-5e960453`, `proj-18e5d4e0` and port
  8000 were never touched.

### Cleanup at window close

Nothing to clean: this iteration created no fixture, started no Hub, and enabled no job. The 8011
Hub is left running on the beta profile, current with this branch — no product code changed after
iteration 1, so it needs no restart.

**Next:** `next_action` is `null`. The window is closed and the driver should unregister. The normal
cycle resumes with the day window at 09:00; row 9c and its four preserved leads are the natural
first item for whichever window next has capacity.

# 2026-09-08 night window

Newest entry at the BOTTOM. Playbook: `.claude/loops/night-window.md`.

Armed 2026-09-08T22:55:06+01:00 on `autonomous/2026-09-08-daily` (parent master@755bdad), model opus.

---

## Iteration 1 — the queue is composed, and the tree is green in 46:41

**Branch and state agree.** `git branch --show-current` is `autonomous/2026-09-08-daily`, `HEAD` is
`170c4cb` (the arm commit), and `STATE-night.json`'s `parent_sha` `755bdad` is four commits back on
the same line. Working tree clean at start. Nothing to reconcile.

### The queue comes from `ORDER:`, not from the backlog

`spec-queue/APPROVALS.md`'s newest section is `## 2026-09-08` and it carries an `ORDER:` line, so
per the playbook that line **is** tonight's queue verbatim and the default backlog-first shape is
ignored. The unarchived changes and the open findings are explicitly **not** this window's work
tonight. The four changes, in order:

1. `2026-09-07-a-dead-connection-is-never-handed-back-out` — F295 (A), 24 tasks, Python only.
2. `2026-09-07-an-agent-without-mcp-is-not-told-it-has-nothing` — 35 tasks, Python and docs only.
3. `2026-09-05-the-conversation-carries-its-own-run-facts` — F274 (A), 44 tasks, **bundle**.
4. `2026-09-07-clearing-instructions-asks-first` — 25 tasks, **bundle**.

Expanded to **15 queue items**, sized one per firing: seven for change 1 (guard, shutdown, two test
halves, mutation-check, reconcile, drive), five for change 2 (one per phase group), and one each for
changes 3 and 4 held whole because neither is realistically reached tonight. The two bundle changes
share no source file — their only collision is the generated `hub/hub/static/ui` — so the
one-per-night constraint is on the artefact, and it is written into `limits` where the next firing
will read it.

**One decision taken rather than deferred: F295 task 1.6.** The approval says the task "carries an
open choice" and that either side satisfies it — write two lines covering `close_detached`, or narrow
the ADDED requirement's "every path" to the paths the pool dispatches `close` for. The queue takes
**write it**: two lines with an identical predicate make the requirement literally true, where
narrowing means editing an approved delta at 02:00 with nobody awake to see it. The operator's
standing preference is the cleanest solution, not the least work. Recorded in the queue item so the
implementing firing does not re-litigate it.

**One label collision, noted so nobody trips on it.** `APPROVALS.md`'s fourth row tags
`clearing-instructions-asks-first` as `DAY-3`, while today's review page uses `DAY-3` for an entirely
different thing — the open question of whether to build through a 20.4 %-wrong suite. Same token,
two referents, one day apart. Nothing depends on it tonight; it is written down so a later reader
does not merge them.

### The green check: green, and much slower than any figure on file

`py -3.11 -m pytest hub/tests/ -q`, started 23:00:59, exit 0:

```
3972 passed, 86 skipped, 247 warnings in 2801.28s (0:46:41)
```

So the tree is green and this window can attribute any breakage to itself. That part is routine.
**The duration is not.** Both playbooks and `spec-queue/DECISIONS.md` state the suite as
**15–25 minutes**, measured 14:39 quiet and 24:50 contended. Tonight's run is **46:41 — 1.9x the
stated ceiling** — and a window that sizes a firing against "25 minutes" loses an entire iteration to
one suite run.

The playbooks' limit is explicit that noticing is not fixing and that a wrong figure becomes a queue
item rather than a log paragraph. It is cheaper than that here, so it is **repaired in this firing**:
`.claude/loops/night-window.md` and `.claude/loops/day-window.md` now say **15–47 minutes** and carry
all three measurements, and `DECISIONS.md`'s existing row is appended to rather than rewritten,
keeping the 2026-09-03 argument intact — which the third figure reinforces rather than overturns.

Two stray `python` processes from earlier windows (started 2026-09-07 20:15 and 21:55, one at 92s CPU
and one at 72s) were resident for the whole run and are the most likely contention. **That is
inferred, not measured** — nothing was killed and no controlled re-run was done, and it would be
wrong to state a cause on the strength of two rows in a process table.

### The suite reached F295's precondition on its own, on a green run

Worth recording because it was not looked for. Among the 247 warnings is a
`PytestUnhandledThreadExceptionWarning` carrying this, four times:

```
aiosqlite/core.py:66  in _connection_worker_thread
  future.get_loop().call_soon_threadsafe(set_result, future, result)
RuntimeError: Event loop is closed
During handling of the above exception, another exception occurred:
aiosqlite/core.py:75  in _connection_worker_thread
  future.get_loop().call_soon_threadsafe(set_exception, future, e)
RuntimeError: Event loop is closed
```

That is the F295 mechanism **verbatim** — and specifically it is the mechanism task 3.1 instructs the
implementer to reproduce deliberately: the worker fails to report a result on a closed loop, fails
again reporting *that* the same way, and its `while True` ends (`core.py:47-75`). The dead-worker
state F295 is about is therefore **not hypothetical in this suite; it occurred tonight on a green
run.** The suite stayed green only because nothing checked that connection out again — which is
exactly the finding's claim, observed rather than argued.

This does not change the finding's severity or any task, and it is **not** a claim that F295 causes
F292. It is corroboration that the precondition is reachable in practice, and it means task 3.1's
kill mechanism is known-good before anyone writes it.

### State

`iteration` 1, `current` `c1-guard`, 15 items, one done. Next firing implements section 1 of the
F295 change in `hub/hub/db/engine.py` — the checkout listener that only raises, the `close` listener
that neutralises, and the `close_detached` pair. No product code was written this iteration; the
three edits are all documentation, all of them corrections to figures this window measured.

---

## Iteration 2 — the guard is in, and removing two lines hangs `dispose()` for 45 seconds

**Branch and state agree.** `autonomous/2026-09-08-daily`, `HEAD` `a4a6b2c` (the compose commit),
tree clean at start, `parent_sha` `755bdad` five commits back on the same line. Nothing to reconcile.

Item `c1-guard`: tasks **1.1–1.6** of `2026-09-07-a-dead-connection-is-never-handed-back-out`, all
of them in `hub/hub/db/engine.py`, +123 lines immediately below the `create_async_engine` call.
Three listeners and two helpers:

- `_refuse_dead_worker_connection` on `checkout` — **detects only**, logs one WARNING, raises
  `DisconnectionError` (1.1, 1.2, 1.4).
- `_neutralise_dead_worker_on_close` on `close` — sets `_running = False` and `_connection = None`
  on the **inner `aiosqlite.Connection`** (1.5).
- `_neutralise_dead_worker_on_close_detached` on `close_detached` — same predicate, two lines (1.6).

Both private attributes were re-confirmed against the installed aiosqlite **0.22.1** before writing:
`_running` and `_connection` assigned in `Connection.__init__`, `_thread` immediately after; and
against SQLAlchemy **2.0.50**, `_ConnectionRecord.__close()` dispatching `close` at
`pool/base.py:878-880` and `_finalize_fairy` dispatching `close_detached` at `:999-1000`. The
comments name both versions, both attributes, and — as 1.3 requires — say *why* the neutralisation
is on `close` rather than inline, naming `dispose()`, so a later reader does not move it back.

### Driven, not just compiled

`testbed/scratch/f295/probe_impl_check.py` (uncommitted, per 4.3) imports the **real**
`hub.db.engine` against a temp database and kills a worker by 3.1's real mechanism — second loop,
future on it, close the loop, `put` the pair on `_tx` — never by monkeypatching `is_alive`. The
worker's two `RuntimeError: Event loop is closed` tracebacks appear exactly as `core.py:66` then
`:75`, and the thread joins dead.

```
A: worker alive after kill: False
A: select 2 -> 2 in 0.00s; new connections: 1; warnings: ['Discarding a pooled database connection whose dr']
A: neutralised? _running=False _connection=None
B: dispose returned in 0.00s; _running=False _connection=None; warnings during dispose: 0
C: same pooled connection reused: True; new connections: 1; warnings: 0
```

A is the transparent replacement, one WARNING and one new connection. **B is the case a
checkout-only guard does not cover** — the dead connection was returned to the pool and never handed
out again, and `engine.dispose()` still returned in 0.00s, which is what task 2.3 depends on. C is
the healthy path: same pooled connection, no reconnect, no warning.

### The mutation check, run now rather than deferred to 3.12

`probe_remove_close.py` registers the same setup, calls `event.remove(...)` on the `close` listener
only, and disposes.

```
registered: checkout=True close=True close_detached=True
removed the `close` listener      worker alive: False      -> killed at 45s, exit 124
(listener present)                worker alive: False      -> dispose returned in 0.00s
```

So the two lines at 1.5 are load-bearing and measured to be: without them the shutdown step this
change is built around **never returns**, matching what R3 measured with its own probe before the
code existed. `event.contains` confirms all three listeners are actually attached to
`engine.sync_engine`, which is the thing a reading of the source cannot tell you.

### Checks

- `py -3.11 -m pytest hub/tests/{test_migrations,test_project_persistence,test_agent_actions_governed,test_launchability}.py -q`
  → **137 passed, 1 skipped in 1:57**, exit 0. A scoped slice, not the suite: the guard fires on
  every checkout in every test, so a shape error would surface immediately, and the full 15–47
  minute run belongs to `c1-reconcile` (4.1).
- `ruff check src/ hub/ tests/` → clean. `black --check --target-version py311` over CI's four paths
  → 550 files unchanged.

### Not claimed

Task 1.6's path is **still unreachable in this repository** — nothing in `hub/` calls `.detach()`,
so `close_detached` was proven *registered*, not proven *to fire*. The comment on it says so, in
those terms, so it is neither deleted as dead weight nor cited later as evidence the state occurs.
Section 3's real tests are not written; the two probes are throwaway and stay out of the commit.

`iteration` 2, `current` → `c1-shutdown`, 15 items, two done.

---

## Iteration 3 — shutdown settles its runs, then lets go of the pool

**Branch and state agree.** `autonomous/2026-09-08-daily`, `HEAD` `00d72a8` (iteration 2's guard
commit), tree clean at start, `parent_sha` `755bdad` six commits back on the same line. Nothing to
reconcile.

Item `c1-shutdown`: tasks **2.1, 2.2, 2.3** of
`2026-09-07-a-dead-connection-is-never-handed-back-out`, all in `hub/hub/main.py`. 2.4 was already
ticked and is a question, not a step.

`_settle_background_runs()` (+45 lines above `lifespan`) and three lines added to the teardown:

```
    await terminate_all_active_runs()
    await shutdown_scheduler()
    await _settle_background_runs()
    await engine.dispose()
```

The settle sits **after** both existing steps, per 2.4's answer — terminating the run *processes*
does not settle the in-process tasks driving them, and `shutdown_scheduler()` is what stops the set
being refilled behind the settle. It cancels the current members, `gather(...,
return_exceptions=True)`, `difference_update`s what that pass settled (**not** `clear()`), and loops
while the set refills, to `_MAX_BACKGROUND_SETTLE_PASSES = 5`. On the bound it logs at WARNING with
the count and continues (2.2) — `conftest.py:580-601` raises there, which is right for a test and
wrong for an instance that has been asked to stop. It does `clear()` the abandoned leftovers, and
says why in a comment: `_background_runs` is module-level, so anything left in it would be awaited
by a later lifespan in the same process against a loop that has closed.

### Driven, not just compiled

`testbed/scratch/f295/probe_shutdown.py` (uncommitted, per 4.3) runs the **real** `lifespan()` async
context manager against a temp database, three cases:

```
A: task done after teardown: True  cancelled: True
A: set empty: True
A: pool replaced by dispose: True
B: successors created: 6  set empty after: True
B: warnings: ["Shutdown: 1 background run task(s) did not settle in 5 passes; stopping anyway: ..."]
C: worker alive after kill: False
C: teardown (settle + dispose) returned in 0.00s
C: neutralised: _running=False _connection=None
```

- **A** is 2.1 and 2.3 together: a run task registered in `_background_runs` and still pending at
  teardown is cancelled and settled, the set is empty, and `engine.pool` is a different object
  afterwards — the dispose actually happened, and it happened after the settle.
- **B** is 2.2, and it is the case the bound exists for: the task re-registers a successor from its
  `except CancelledError` every pass, so **6** tasks are created (one plus one per pass), the loop
  exhausts, the WARNING is emitted with the leftover count, and the teardown **returns** rather than
  raising. One `await asyncio.sleep(0)` was needed before the teardown for the first task to start
  at all — cancelling a task that has never run skips its `except` clause entirely, which is worth
  knowing when `c1-tests-shutdown` writes 3.6.
- **C** is 2.3's dependency on 1.5, measured on this code rather than on a probe of it: a connection
  whose aiosqlite worker was killed by 3.1's real mechanism (second loop, future on it, close the
  loop, `put` on `_tx`) and returned to the pool idle does not hang the teardown — **0.00s**, with
  the connection neutralised. Iteration 2 measured the same dispose hanging to a 45-second external
  kill with the `close` listener removed, so the precondition 2.3 was gated on is satisfied by
  measurement at both ends.

### Checks

- `py -3.11 -m pytest hub/tests/test_lifespan_shutdown.py test_mcp_body_contract test_request_strictness
  test_spec_documents_api test_version test_agent_trigger test_scheduler -q` → **154 passed** in
  1:29, exit 0. That slice is deliberate rather than arbitrary: every test file that boots the app
  (`TestClient`/`create_app` — the only ones that reach `lifespan()` at all, since `conftest`'s
  `ASGITransport` fixture does not) plus the two modules the new code touches state in.
- `ruff check src/ hub/ tests/` → clean. `black --check --target-version py311` over CI's four paths
  → 550 files unchanged.

### Not claimed

No test was written this iteration — 3.5, 3.6 and 3.10 are `c1-tests-shutdown`'s, and the probe is
throwaway and stays out of the commit. The full suite is `c1-reconcile`'s 4.1. The drive against a
real Hub process on 8011, with a real run in flight, is still `c1-drive`; what was driven here is
the teardown coroutine itself, in-process.

`iteration` 3, `current` → `c1-tests-guard`, 15 items, three done.

## Iteration 4 — five tests for the guard, and four mutations that each name a victim

`c1-tests-guard`. Tasks **3.1, 3.2, 3.3, 3.4, 3.7** built as
`hub/tests/test_dead_worker_connection.py` (+205 lines, five tests, 0.23s green).

### What the tests are, and what each one would catch

| Test | Task | Subject |
|---|---|---|
| `test_both_guard_listeners_are_registered_on_this_engine` | 3.3 | the two `@event.listens_for` decorators, by `event.contains` — fails in milliseconds, not on a bounded wait |
| `test_a_connection_whose_worker_thread_ended_is_never_handed_out_again` | 3.1 + 3.2 | real kill, then `select 2 -> 2` from a *new* connection, exactly one WARNING naming F295 |
| `test_the_discarded_connection_is_neutralised_and_not_merely_dropped` | 3.3 | 1.5's observable: the discarded inner comes out `_running=False`, `_connection=None` |
| `test_a_healthy_checkout_is_untouched` | 3.4 | same pooled connection reused, no new connect, no WARNING |
| `test_the_replacement_connection_is_configured_and_not_bare` | 3.7 | `PRAGMA busy_timeout` on the replacement is 30000, not SQLite's 5s default |

The kill is 3.1's real mechanism, transcribed into the file rather than imported from the
uncommitted testbed: second loop, future on it, close the loop, `put` on `_tx`, join the thread,
`assert not is_alive()`. **`is_alive` is never monkeypatched.** Every await is
`asyncio.wait_for(..., timeout=15.0)`.

Two things the tests had to be built around, both stated in the file:

- A module-scoped autouse fixture disposes the engine **before** each test. Without it a
  connection left pooled by an earlier test can be handed out ahead of the victim, the guard is
  never reached, and the test passes while asserting nothing.
- The capture listeners register *after* the guard, so a refused checkout is never recorded —
  which is what makes `checkouts[-1] is not victim` mean what it says. Same ordering fact as the
  note the tasks carry about `conftest`'s F292 census, and for the same reason.

### Mutation check — 4 of the 4 product lines these tests exist for

Each reverted in `hub/hub/db/engine.py`, suite re-run, engine restored from a backup and
`git diff --stat` confirmed empty after every one.

| Reverted | Named failures | Wall |
|---|---|---|
| 1.2 — `raise DisconnectionError` → `return` | `..._never_handed_out_again`, `..._neutralised_and_not_merely_dropped`, `..._configured_and_not_bare` (3) | 46.20s, exit 1 |
| 1.1 — the `checkout` decorator removed | the three above **plus** `..._listeners_are_registered_on_this_engine` (4) | 46.08s, exit 1 |
| 1.4 — the `logger.warning(...)` removed | `..._never_handed_out_again` (1) | 0.87s, exit 1 |
| 1.5 — the `close` listener body → `return` | `..._never_handed_out_again`, then the run **hung** | killed at 180s, exit 124 |

Four mutations, four sets of named victims, no mutation survived. The 1.4 row is the useful shape:
one line reverted, exactly one test fails, and it fails on the assertion that names it.

### The 1.5 row is a finding, not a nuisance

With 1.5 reverted the named test fails on its bounded wait **and then the run hangs** — in
`conftest`'s per-test `dispose()`, which is itself one of the closes that cannot return on a
dead-worker connection. `wait_for` bounds this file's own awaits; nothing in this file can bound a
fixture's. So a regression of 1.5 costs a named failure followed by a stuck CI job. That is the
honest price of covering it, it is written into the module docstring so a later reader does not
diagnose it as a flake, and it is the reason 3.3 ships **no** variant with that listener removed.

### 3.3's removal direction, confirmed twice and shipped neither time

- The `1.5` mutation row above, on the real suite.
- `testbed/scratch/f295/probe_33_remove_close_checkout.py` (uncommitted, per 4.3), which does it
  the way `c1-tests-guard` asked — `event.remove(...)` under `try`/`finally`, `os._exit` so a hung
  close cannot keep the process alive:

```
keep:   contains checkout=True close=True  -> RECOVERED: select 2 -> 2 in 0.00s
remove: contains checkout=True close=False -> DID NOT RECOVER: timed out after 20.00s
```

### Checks

- `py -3.11 -m pytest hub/tests/test_dead_worker_connection.py -q` → **5 passed** in 0.23s.
- The new file ahead of a pool-sensitive slice (`test_suite_database_isolation`, `test_agents`,
  `test_tasks`, `test_auth`, `test_lifespan_shutdown`) in one process → **50 passed** in 1:26. The
  point of running it first: to see whether a killed worker or an extra dispose leaks into what
  follows. It does not.
- `ruff check src/ hub/ tests/` → clean. `black --check --target-version py311` over CI's four
  paths → 551 files unchanged.

### Not claimed

3.5, 3.6, 3.8, 3.10 and 3.11 are `c1-tests-shutdown`'s and are untouched. 3.12 stays open: its
tally is the whole change's, and the four mutations above are the guard half of it — the shutdown
tests have not been mutation-checked because they do not exist yet. The full suite is 4.1's. The
drive against a real Hub on 8011 is still `c1-drive`.

`iteration` 4, `current` → `c1-tests-shutdown`, 15 items, four done.

## Iteration 5 — five tests for the shutdown half, and a bound that turns out not to bind

`c1-tests-shutdown`. Tasks **3.5, 3.6, 3.8, 3.10, 3.11**. Two new tests in
`hub/tests/test_shutdown_settles_background_runs.py` (+195 lines), two appended to
`hub/tests/test_dead_worker_connection.py` (now 9 tests), and one harness change in
`hub/tests/conftest.py`.

### What was built

| Test | Task | Subject |
|---|---|---|
| `test_shutdown_settles_a_background_run_before_disposing` | 3.5 | the *ordering*: a `_DisposeProbe` replaces `hub.main`'s `engine` name and samples `task.done()` **at the instant dispose is entered** |
| `test_the_settle_bound_logs_and_completes_rather_than_raising` | 3.6 | the bound: 6 tasks, one WARNING naming the leftover count and the pass count, and the dispose still reached |
| `test_dispose_returns_when_a_pooled_connection_has_a_dead_worker` | 3.10 | `engine.dispose()` with a dead-worker connection idle in the pool, plus a pool-identity check so a no-op dispose cannot pass |
| `test_the_exhausted_retry_path_raises_instead_of_hanging` | 3.11 | every replacement born dead via a `connect` listener; `InvalidRequestError`, commented as a deliberately unreachable state |

3.5 and 3.6 drive the **real `lifespan()` context manager** (`async with lifespan(app)`),
not a transcription — `conftest`'s `ASGITransport` app fixture never runs it, and
`test_lifespan_shutdown.py`'s `TestClient` runs it on another thread's loop, so neither
lets a test put a task into `_background_runs` on the loop the teardown will settle. The
`await asyncio.sleep(0)` iteration 3 asked for is there in 3.6, with the reason in a
comment: cancelling a task that has never run skips its `except` clause, so without it the
successor is never spawned and the test asserts nothing.

**3.6 cannot use `caplog`, and that is measured rather than worked around.** `init_db()`
runs Alembic, Alembic's `fileConfig` reconfigures logging, and the root logger comes out of
lifespan startup holding **one `StreamHandler`** where pytest's four handlers were —
snapshotted before/inside/after. A warning emitted in the *teardown* therefore reaches no
capture handler. A recorder attached to the `hub.main` logger itself survives, so the file
carries a small `_warnings_from(logger_name)` and says why in its docstring. Any future
test that boots the Hub's lifespan and reads `caplog` will assert over an empty list.

### 3.8, built and driven

`conftest.py`'s teardown now settles inside a `try` and disposes in a `finally`. Driven
rather than reasoned: a throwaway pair of tests (leak a self-respawning task so the cap
raises, then check `engine.pool` in the next test) — **with** the `finally`, the pool is a
different object in the following test; **with the dispose moved back after the `try`**,
it is the same object and the drive fails with `the pool was NOT disposed after the cap
raised`. So the cap's `AssertionError` was carrying the whole pool into the next test's
event loop, exactly as R2 read it. The drive file was deleted; `conftest.py` was restored
from a backup and `git diff` re-read to confirm only the intended hunk remains.

### Mutation check — 5 mutations, 5 sets of named victims

Each reverted in the product file, the file restored from a backup, `git diff --stat`
confirmed empty after every one.

| Reverted | Named failures | Wall |
|---|---|---|
| 2.1/2.3 — `engine.dispose()` moved *before* the settle | `..._settles_a_background_run_before_disposing` (`[False] == [True]`) **and** `..._bound_logs_and_completes...` (`[1] == [0]`) | 1.4s |
| 2.2 — the bound's `logger.warning` → `raise AssertionError` | `..._bound_logs_and_completes_rather_than_raising` | 1.4s |
| 2.1 — `difference_update(leftover)` → `clear()` | `..._bound_logs_and_completes...` (`assert 2 == 5 + 1`) | 1.3s |
| 3.8 — the dispose moved out of the `finally` | the throwaway drive's `test_b`, by name | 0.3s |
| 1.2 — `raise DisconnectionError` → `return` | `..._exhausted_retry_path_raises_instead_of_hanging`, on `TimeoutError` | 15.5s |

Iteration 4's four mutations plus these five is **nine** for the change so far. 3.12's
tally is still open because it is the whole change's.

### The finding: `asyncio.wait_for` does not bound `engine.dispose()`

3.10's task asked for the dispose "under a bounded wait that must not time out" and to
"confirm it hangs with 1.5 removed". Both halves were run, and the second one is sharper
than expected:

- with 1.5 in place — `RETURNED in 0.00s, pool replaced: True`;
- with `_neutralise_dead_worker_on_close` reverted to `return` — the pytest run was killed
  externally at **240s** having printed nothing, and `faulthandler` at 40s showed the main
  thread inside a live event loop with nothing progressing.

`testbed/scratch/f295/probe_310_wait_for_bound.py` (uncommitted, per 4.3) isolates it away
from any fixture: `asyncio.wait_for(engine.dispose(), timeout=5.0)` **never raises** — no
return in 60 seconds. The dispose reaches aiosqlite through SQLAlchemy's greenlet bridge
and the cancellation does not land. The same `wait_for` *does* bind the checkout/execute
path: 3.11's mutation row above failed on `TimeoutError` in 15.5s.

So the guarantee is narrower than the task's wording, and the test's own docstring now
says so rather than claiming a bound it does not have. The `wait_for` stays — it costs
nothing and bounds every other way the test could go wrong — but a 1.5 regression costs a
stuck job, which is the same price already recorded in `test_dead_worker_connection.py`'s
module docstring and the same reason that file ships no listener-removed variant.

### Checks

- `py -3.11 -m pytest hub/tests/test_dead_worker_connection.py hub/tests/test_shutdown_settles_background_runs.py -q`
  → **9 passed** in 1.26s.
- The two new files **ahead of** `test_lifespan_shutdown`, `test_suite_database_isolation`,
  `test_agents`, `test_tasks`, `test_auth`, `test_agent_trigger`, `test_scheduler` in one
  process → **162 passed**, exit 0, 59.95s. The point of running them first: 3.5 and 3.6
  boot and tear down a whole app — scheduler started and stopped, logging reconfigured by
  Alembic, the shared engine disposed twice — and none of that leaks into what follows.
- `ruff check src/ hub/ tests/` → clean. `black --check --target-version py311` over CI's
  four paths → 552 files unchanged (both new files were reformatted first).

### Not claimed

3.12's count is the whole change's and stays open. 4.1's full suite has not been run this
iteration. The drive against a real Hub process on 8011 is still `c1-drive`. `F292` is
untouched: 3.8 makes the fixture's cleanup unconditional, which is a harness fix, not a
diagnosis.

`iteration` 5, `current` → `c1-mutation`, 15 items, five done.

## Iteration 6 — the tally: 14 mutations, 11 victims, and three lines that name nobody

`c1-mutation`. Task **3.12**, the whole change's mutation count. No product code changed this
iteration; five mutations were run and reverted, and `tasks.md` gained the tally.

### What was still unmutated

Nine mutations were already tabled (four in iteration 4, five in iteration 5). Reading the two
entries against the nine shipped tests, exactly one test had **never been named by a reversion**:
`test_a_healthy_checkout_is_untouched`. Every earlier mutation broke the *dead* path; nothing had
broken the predicate that keeps the guard off the *healthy* path. A test that no mutation names is
the thing 3.12 exists to catch, so that was the one to run.

### The five run this iteration

Each applied to `hub/hub/db/engine.py` or `hub/hub/main.py`, restored from a byte-checked backup,
`git status` confirmed empty after every one.

| # | Reverted | Named failures | Wall |
|---|---|---|---|
| 10 | 1.1 — `if thread.is_alive(): return None` deleted from `_dead_worker_connection` | **5**, including `test_a_healthy_checkout_is_untouched` — plus `..._never_handed_out_again`, `..._neutralised_and_not_merely_dropped`, `..._configured_and_not_bare`, `test_dispose_returns_...` | 0.60s |
| 11 | 1.5 — the `@event.listens_for(..., "close")` **decorator** removed, registration test run alone (`-k registered`) | `test_both_guard_listeners_are_registered_on_this_engine`, on the `close` half of the assertion | 0.25s |
| 12 | 1.1 — `if inner is None or thread is None: return None` deleted | **none** — 9 passed | 1.26s |
| 13 | 2.1 — the settle moved *before* `terminate_all_active_runs()` and `shutdown_scheduler()` | **none** — 2 passed | 1.21s |
| 14 | 1.6 — the `@event.listens_for(..., "close_detached")` decorator removed | **none** — 9 passed | 1.29s |

Row 11 is run with `-k registered` deliberately. Removing that decorator also removes the
neutralisation, and iterations 4 and 5 both measured what that costs: a named failure followed by a
run that hangs until something external kills it. The registration test touches no dead connection,
so running it alone gets the naming without the hang. Rows 12 and 14 were run as full-file
mutations only because their predicted cost is zero, and it was.

### The tally

**14 mutations across iterations 4, 5 and 6. Eleven named a victim.** Every one of the nine shipped
tests is named by at least one:

| Test | Named by |
|---|---|
| `test_both_guard_listeners_are_registered_on_this_engine` | 2 (checkout decorator), 11 (close decorator) |
| `test_a_connection_whose_worker_thread_ended_is_never_handed_out_again` | 1 (1.2 raise→return), 2, 3 (the WARNING), 10 |
| `test_the_discarded_connection_is_neutralised_and_not_merely_dropped` | 1, 2, 4 (1.5 body→return), 10 |
| `test_a_healthy_checkout_is_untouched` | **10 only** |
| `test_the_replacement_connection_is_configured_and_not_bare` | 1, 2, 10 |
| `test_dispose_returns_when_a_pooled_connection_has_a_dead_worker` | 10; and the 1.5-reverted hang measured in iteration 5 |
| `test_the_exhausted_retry_path_raises_instead_of_hanging` | 9 (1.2 raise→return, on `TimeoutError`) |
| `test_shutdown_settles_a_background_run_before_disposing` | 5 (dispose before settle) |
| `test_the_settle_bound_logs_and_completes_rather_than_raising` | 5, 6 (bound raises), 7 (`difference_update`→`clear`) |

`conftest.py`'s 3.8 change is pinned separately by mutation 8, which named a throwaway drive's
`test_b` — that drive is not shipped, so 3.8 is the one change in this set whose evidence lives only
in iteration 5's entry.

### The three that name nobody, and which kind each is

Two are **not gaps**. Row 12 deletes the `getattr`-with-`None` guard that exists so a non-aiosqlite
driver is untouched; every connection in this suite is aiosqlite, so the state cannot occur and no
test can name it. Row 14 deletes the `close_detached` listener, whose own docstring already says it
covers a state nothing in `hub/` reaches (`grep -rn "\.detach()" hub/hub/` returns nothing). Both
were *measured* to name nobody rather than assumed to — which is the difference between a documented
unreachable line and a line nobody checked. Task 1.3 is comments and has no behavioural victim by
construction; task 2.4 is a question, not code.

**Row 13 is a real gap, and it is the one worth carrying forward.** Task 2.4 argues at length that
the settle belongs *after* `terminate_all_active_runs()` and `shutdown_scheduler()`, because the
scheduler can refill `_background_runs` behind a settle placed earlier
(`_scheduled_job_runner` → `_fire_job_by_id` → … → the `create_task` at `agent_trigger.py:1190`).
Move the call two lines up and **nothing fails**. So the ordering that 2.4 reasons out is currently
held by a comment and a task file, not by a test. Neither 3.5 nor 3.6 involves the scheduler at all;
pinning it would need a test that fires a job during teardown, which is a bigger thing than 3.12 is
asking for. Recorded here rather than silently fixed, and `4.2` should carry it into `F295`.

### Checks

- The two files on the restored tree: **9 passed** in 1.31s.
- `hub/hub/db/engine.py` and `hub/hub/main.py` md5-identical to their pre-mutation backups;
  `git status --porcelain` empty before the `tasks.md` edit.

### Not claimed

No product code changed, so no lint or full-suite run is owed by this iteration — `4.1` still owns
both. The drive against a real Hub on 8011 is still `c1-drive`. Row 13's gap is recorded, not
closed.

`iteration` 6, `current` → `c1-reconcile`, 15 items, six done.

---

## Iteration 7 — a dead firing's uncommitted work, verified rather than redone, and the change archived

`c1-reconcile`. Tasks **4.1** and **4.4** closed; the change is archived and `F295` is retired to
*built, not driven*.

### Reconciliation first: the tree was dirty, and the reason matters

`STATE-night.json` said `iteration` 6, `current` `c1-reconcile`, and `git log` matched it exactly —
`2046c4e` on `autonomous/2026-09-08-daily`, `755bdad` seven commits back. **But the working tree was
not clean**, which iteration 6's entry says it left it: `tasks.md`,
`openspec/specs/app-lifecycle/spec.md` and `scripts/drive/FINDINGS.md` all modified, and the FINDINGS
text dated *"2026-09-09 (night window, iteration 7)"*. So **a firing of iteration 7 ran before this
one and died before it committed** — far enough in to have done `4.2`, `4.3` and the whole of `4.4`'s
sync half, not far enough to record any of it. Nothing in `STATE` or the log knew that, which is the
hazard the "never end an iteration with a dirty tree" rule exists to prevent, seen from the other
side: the rule was kept by every firing that *finished*, and a firing that does not finish breaks it
silently.

The choice was to **verify that work rather than redo it**, and verification was cheap and specific:

- `openspec validate <change> --strict` → valid. `openspec validate --specs app-lifecycle --strict` →
  43 passed, 0 failed.
- The synced text was diffed against its source rather than eyeballed: the delta's body (minus its
  `## ADDED Requirements` header) against the `+` lines of
  `git diff openspec/specs/app-lifecycle/spec.md` → **identical ignoring blank lines**. Both new
  requirements carry `SHALL` on their **first physical line** in the main spec too, which is the only
  line `--strict` reads (`DEAD-ENDS.md:172`).
- The `F295` append was read in full against what `next_action` said it owed. All four are there:
  R1's correction to the trigger (loop closure is the precondition; cancellation alone is benign),
  R2's and R3's severity narrowing including `F297`, the two build-time findings (`wait_for` does not
  bound `dispose()` through the greenlet bridge; `caplog` does not survive Alembic's `fileConfig`),
  and iteration 6's mutation gap — the settle's position relative to `terminate_all_active_runs()`
  and `shutdown_scheduler()`, argued at length in task 2.4 and pinned by no test. History is appended
  to, not rewritten; the 2026-09-06 entry above the new heading is untouched.

So the dead firing's work stands, and this entry owns it. What it had **not** done was `4.1` or the
archive.

### 4.1 — all four gates, green

| Gate | Result |
|---|---|
| `ruff check src/ hub/ tests/` | All checks passed |
| `black --check --target-version py311 src/ hub/hub/ hub/tests/ tests/` | 552 files unchanged |
| `mypy src/` | no issues in 22 source files |
| `py -3.11 -m pytest hub/tests/ -q` | **3981 passed, 86 skipped, exit 0, 30:37** |

3981 is the window's opening count (3972) **plus exactly this change's nine tests** — the arithmetic
is the cheapest check that the new files are actually collected in a full run and that nothing was
deselected to get green. 30:37 is a fourth point on the suite's spread (14:39 / 24:50 / 46:41 /
30:37), which continues to say the number is load-dependent and that the playbooks' repaired
"15–47 minutes" is the honest range. The suite was backgrounded to a file and blocked on, per
`next_action`; it exceeds the 600s command cap by a factor of three and must never be foregrounded.

### The one measurement worth arguing about, and what is *not* claimed from it

The green run still emits the dead-worker warning this whole change is about:
`PytestUnhandledThreadExceptionWarning` at `aiosqlite/core.py:66` then `:75`, `RuntimeError: Event
loop is closed`. **10 events, 20 warnings.** Iteration 1's pre-fix run of the same suite recorded
**four**. Attribution was taken rather than assumed, by piping the warnings summary through `awk`
into `sort | uniq -c`:

    2  test_task_turn_collision.py::test_a_holder_whose_tail_raises_still_releases_the_task_it_held
    2  test_task_spec_document_context.py::test_a_task_created_against_requirements_records_their_document
    2  test_scheduler.py::test_finalize_job_run_for_conversation_touches_only_the_matching_in_progress_row
    2  test_scheduler.py::test_a_flow_work_run_binds_and_starts_its_claimed_task
    2  test_reviewer_is_not_the_author.py::test_a_wedged_review_is_restaffed_to_a_real_reviewer
    2  test_review_dispatch_staffs_the_task.py  (three separate tests, two warnings each)
    2  test_context_usage_measurement.py::test_a_reading_for_an_agent_with_no_conversation_creates_none
    2  test_agent_trigger.py::test_trigger_injects_identity_env_and_tells_agent_the_access_path

Ten pre-existing tests, two warnings each (the same worker reported twice, `:66` and `:75`), and
**not one of the nine new tests** — worth stating on its own, because those nine kill worker threads
deliberately and could easily have been the source.

**A count going 4 → 10 after a change that exists to fix worker threads deserves the suspicion, and
it does not survive it — but it is also not cleared.** What the change removes is the *hang on reuse*
of a dead connection and the Hub lifespan's own abandonment of in-flight writes; what emits this
traceback is a worker whose loop closed under an in-flight call, which `proposal.md`'s *"What this
deliberately does not change"* puts out of scope and which `F292` still owns. So noise of this kind
surviving is predicted. Whether the number moved because of this change, because of load, or because
it drifts run to run is **not established by two samples of a load-dependent quantity**, and this
entry does not claim it either way. Recorded here and in `4.1`'s tick so the next full-suite run has
a third sample to compare against rather than a story.

### 4.4 — synced, archived, and the findings retired in the same commit

`git mv openspec/changes/2026-09-07-a-dead-connection-is-never-handed-back-out
openspec/changes/archive/` — the directory keeps its own name, which is this repository's convention
(the skill's generic `YYYY-MM-DD-<name>` would double the date prefix; every neighbour in `archive/`
is stored under its original name). `openspec list` now shows three active changes and no longer
shows this one; `openspec validate --specs` still 43/43.

**All 24 tasks are ticked, so the archive is not papering over an open one** — checked with
`grep -c '^- \[ \]' tasks.md` → 0 before the move.

The retirement rides in the same commit, because the archive is what makes its wording true: `F295`'s
`Status:` line now reads **fixed** `00d72a8`/`0f06bea`, tested `2d399c7`/`9a0fdde`/`2046c4e`,
archived — **and not yet driven**. Under the stricter definition that file adopted 2026-09-06, that
is half of what retirement takes, so `F295` sits exactly where `F142` sits, and the index paragraph
says the open severity-A list is two under the looser definition and still three under the stricter
one. The alternative — reporting "two" flat — would have been the fourth error in that paragraph's
own sequence of miscounts, and the first *premature* one.

### Checks

- `ruff`, `black`, `mypy`, full hub suite: the table above, all green, on the tree that was
  committed.
- `openspec validate` on the change (before the move) and on all specs (after): valid, 43/43.
- `grep -c '^- \[ \]'` on `tasks.md`: 0.
- `testbed/scratch/f295/` left uncommitted, per 4.3; `git status` shows nothing under `testbed/`.

### Not claimed

**The drive did not happen and is not claimed.** `c1-drive` is still open and is the whole reason
`F295` is not on the retired list: three rounds and 14 mutations check the argument, and a Hub
process on 8011 shutting down with a run in flight is what checks the product. The 4 → 10 warning
count is an observation, not a diagnosis. `F292` is untouched.

`iteration` 7, `current` → `c1-drive`, 15 items, seven done.

---

## Iteration 8 — the drive: 0.42 seconds, and two things fourteen mutations never looked at

`c1-drive`, the item that was the whole reason `F295` was not on the retired list. Branch, head
(`37007cd`) and clean tree all matched `STATE` at the start of this firing; nothing to reconcile.

### What was driven, and why not the obvious thing

A Hub started **from source** (`cd hub`, `py -3.11 -m uvicorn hub.main:app --port 8011`) against a
throwaway database under the temp tree, a **fresh project** (`proj-7c848ec0afe6` and successors,
never `proj-5e960453` or `proj-18e5d4e0`), a Haiku runner, and a real turn. `8000` and `8010` were
untouched — checked before and after: same PIDs, `10556` and `11292`. `0 .py` under `hub/hub` or
`src/` newer than the process start, so the drive is not of stale code. No job was ever created, so
none was left enabled.

**The stop under test is `CTRL_BREAK_EVENT`, not `agentweave stop`.** The product's own stop is
`taskkill /F` and runs no teardown at all (that is `F297`, already filed), so driving it would have
measured nothing about this change. uvicorn 0.41.0 installs `handle_exit` for `SIGBREAK` on Windows,
so a Ctrl-Break to a child created with `CREATE_NEW_PROCESS_GROUP` is the Ctrl-C path — which also
turns `F297`'s *"a fix would need `GenerateConsoleCtrlEvent`"* from a proposal into a measurement, and
that line was added to F297's own section.

### The measurement

| | run in flight | shutdown | teardown completed | settle WARNING | dead-worker WARNING | `Event loop is closed` |
|---|---|---|---|---|---|---|
| in-flight | **yes**, `ended_at IS NULL` at the break | **0.42 s** | yes | absent | absent | absent |
| control `--no-run` | no | **0.22 s** | yes | absent | absent | absent |

`Application shutdown complete` is printed *after* `_settle_background_runs()` and
`await engine.dispose()` have both returned, so its presence is the evidence that the settle reached
its fixed point and the pool was released with a run cancelled mid-`pty.read`. **The hang this
finding is about does not happen.** Reproduced three times. The in-flight case costs about 0.2 s more
than an idle one, not a timeout.

### The correction that cost a run, and is the reason to distrust the first green

The first attempt asked Haiku to *"count slowly from 1 to 40"* and inferred *in flight* from output
lines having arrived. Haiku finished in 10 s; the break landed on an **idle** Hub; the drive reported
a clean 6/6 for a shutdown with nothing to settle. Output lines prove a run **started**, never that
it is still going. What caught it was the numbers being suspiciously identical to the control — this
is `F296`'s class of error (an assertion that cannot fail, reported as evidence), committed again in
the same directory, one day later. The script now blocks the turn on a real `time.sleep(120)` child
and reads `runs.ended_at IS NULL` **in the same breath as the break**.

A second, smaller instrument bug: the log tail was sliced by a byte offset against a *decoded*
string, so the migration banner's multibyte characters shifted the window and the control lost its
`Application shutdown complete` line — one FAIL that was the harness, not the product.

### Two things the drive found that no test and no round had looked at

**1. Filed as `F298` (C).** Every in-flight stop logs, at ERROR with a traceback,
`Unhandled error in run run-... : asyncio.exceptions.CancelledError`. The handler doing it
(`agent_trigger.py:2372-2394`) is deliberate and its comment names *this exact case* as why it
exists — so the behaviour is right and the report is wrong: the operator's own intended stop is
described as an unhandled error, and any log scrape for `ERROR` gets a false positive on every clean
shutdown. Not a regression from this change.

**2. The stop leaves one `Run` row at `running`, and the next start repairs it — measured, not
assumed.** Cancelling the in-flight run makes its failure tail hand the input back, that release
schedules a **successor**, and the settle's second pass cancels the successor before it has run a
step — so it never enters its own `try` and nothing marks its row. This is precisely the
"cancelled before its first await" behaviour task 3.6 had already found in a test, now seen in the
product. **It is not a wedge**: `scripts/drive/f295_restart_reconciles.py` restarts against the same
database and gets
`WARNI [hub.run_reconciliation] Reconciled 1 orphaned run(s) to status=interrupted on Hub start`,
zero rows left `running`, the agent `idle`, and a fresh trigger running to `completed`. Recorded
because "the stop leaves a running row behind" is alarming enough to be re-discovered as a defect by
the next person who looks.

**No orphan processes.** After each stop, nothing matching the drive's temp path or its
`time.sleep(120)` child survives — `terminate_all_active_runs()` takes the PTY tree with it.

### What was written down

- `F295`'s `Status:` line and its new closing section *"The drive"* — the table, the correction, and
  the two findings above.
- A new index revision: **the open severity-A list is two, F274 and F142, under *both* definitions.**
  Iteration 7's paragraph refused to say "two" because the drive had not happened; this one may, and
  says why in the same breath. `F142` is now the only A whose fix shipped and whose drive is owed —
  and unlike F140/F154/F155 that is a build, not a grep, because its own change document says so.
- `F298`, new, severity C, with a reproduce block and a shape-of-a-fix that is explicitly not decided.
- One line into `F297`, upgrading its `CTRL_BREAK_EVENT` suggestion from proposal to measurement.
- Both harnesses committed: `scripts/drive/f295_shutdown_drive.py` (ruff and black clean,
  `--no-run` control built in) and `scripts/drive/f295_restart_reconciles.py`.

### Not claimed

The full hub suite was **not** re-run this firing — nothing under `hub/` or `src/` was touched, only
`scripts/drive/`. So the `4 → 10` `PytestUnhandledThreadExceptionWarning` count still has two
samples and still wants a third; it is `F292`'s traceback-at-loop-close, which this change explicitly
does not fix. `F298` is filed, not specced and not queued.

`iteration` 8, `current` → `c2-notice`, eight of fifteen items done.


---

## Iteration 9 — `c2-notice`: the notice stops lying, and names two variables without carrying either value

Queue item `c2-notice`, the first of five for
`openspec/changes/2026-09-07-an-agent-without-mcp-is-not-told-it-has-nothing`. Tasks **1.1–1.5, all
five ticked**. Python and one test file only — no UI file was touched, so per `tasks.md:7` the
committed bundle in `hub/hub/static/ui` was not rebuilt and the TypeScript lint set was not
required. Section 2 was deliberately not started.

### What the branch now says

`access_path_notice("cli")` (`hub/hub/launchability.py`) returned, to a run holding a live
`AW_RUN_TOKEN`, that *"no AgentWeave tool surface is available this turn, so you cannot send
messages, create or update tasks, or ask the operator"* — and told it to report what it *would* have
sent. It now returns the four things task 1.1 names, rendered as one block, 679 characters:

- the base address — **the value of the `HUB_URL` environment variable**, named, not interpolated;
- the credential — **the `AW_RUN_TOKEN` environment variable**, likewise named;
- the shape — `Authorization: Bearer $AW_RUN_TOKEN`;
- the route prefix — `/api/v1/agent-actions`, verified against
  `hub/hub/api/v1/agent_actions.py:63` (`prefix="/agent-actions"`) under
  `hub/hub/api/v1/__init__.py:36` (`prefix="/api/v1"`), not taken from the proposal's word.

Task 1.3's two true sentences survive verbatim (*"Inbound content is already included in this turn;
no retrieval is needed."*). Task 1.4's comment was rewritten rather than deleted: the history it
carries is still true — `2026-08-03-single-runtime` really did delete `agentweave msg send` and the
rest — so the comment keeps it and drops the implication that deleting those commands left the run
with nothing. The docstring changed too; it said *"One line"* and this is no longer one line.

**1.2, the boundary.** Neither value is interpolated, and the comment above the branch says why in
the terms `design.md` D4 uses: the notice is prepended to the turn prompt at
`agent_trigger.py:1007`, the prompt is the durable record of the turn, so a credential written here
is a credential in stored turn text. The agent can read its own environment; the *name* discloses
nothing it does not already hold.

### The test that had to change, and it was not stale

`test_launchability.py:474-481` asserted `"no AgentWeave tool surface is available" in notice` —
**a green test pinning the exact false sentence this change exists to remove.** It was not flipped
silently: its docstring now records that the part still true (no removed CLI command is named) is
kept as its own assertion, and the sentence it used to require is now asserted **absent**. Three
tests added beside it:

- `test_a_run_without_mcp_is_told_the_plane_is_reachable_over_http` — the four things, plus the
  denial's absence;
- `test_a_run_without_mcp_is_not_told_it_cannot_act` — the delta scenario's second half, that the
  denial does not survive in other words;
- `test_the_notice_names_the_credential_variable_and_never_its_value` — 1.5's leak half. It sets
  `AW_RUN_TOKEN` and `HUB_URL` to sentinels with `monkeypatch.setenv` **before** rendering, and the
  docstring says why: rendering with the variables *unset* would pass against an interpolating
  implementation, which is the assertion-that-cannot-fail this repository keeps re-learning (`F190`,
  `F296`).

### Four mutations, four named victims

`hub/hub/launchability.py` md5 `bfd6a25a…` restored and `git status` verified after each.

| # | Mutation | Named |
|---|---|---|
| 1 | the whole branch reverted to the old denial text | 3 — reachable, not-told-it-cannot-act, leak |
| 2 | `Bearer $AW_RUN_TOKEN` → f-string interpolating `os.environ["AW_RUN_TOKEN"]` | 1 — the leak test, and only it |
| 3 | `HUB_URL` named → f-string interpolating its value | 1 — the leak test |
| 4 | `/api/v1/agent-actions` replaced by prose ("the Hub's agent-actions namespace") | 1 — reachable |

Mutations 2 and 3 are the ones that matter: each is *one f-string*, which is D4's own description of
the distance between correct and a leak, and each is caught.

### Checks

`ruff check src/ hub/ tests/` clean. `black --check --target-version py311 src/ hub/hub/ hub/tests/
tests/` — 552 files unchanged. Scoped slice, `py -3.11`: `test_launchability`,
`test_agent_facing_text`, `test_agent_trigger`, `test_agent_actions_governed`,
`test_tool_surface_matches_server` — **146 passed** in 35.93 s. The full hub suite was **not** run
this firing; it belongs at 6.2 when the change is finished, and it is 15–47 minutes.

Rendered through a real import under `py -3.11` with both variables set to sentinels
(`aw-run-LEAKCHECK`, port `65432`): neither appears in the output. That is what an agent will read.

### Not done, and named rather than passed over

- **No Hub was started.** Tasks 6.3–6.5 are the drives — that a request formed exactly as this
  notice instructs reaches a live route from inside a spawned run's own environment, and what the
  mirror deployment actually does. They are `c2-verify`'s, and until they run, "the notice is
  correct" is a source reading with tests behind it, not a measurement.
- **The delta's "where the operations are described" is half-satisfied.** The notice names the
  route prefix; it does not yet point at a description of the operations, because that rendering is
  §2's (`_tool_surface_lines`) and does not exist for this path yet. Deliberately not run ahead into.
- **`src/agentweave/tool_surface.py:110` holds a byte-identical copy of the old
  `access_path_notice` and now diverges.** Confirmed dead: grep over `src/`, `hub/` and `tests/`
  finds no importer of that module at all. §2.1 already forbids reusing it; recorded here because
  the divergence is now visible to anyone grepping the old sentence.

`iteration` 9, `current` → `c2-surface`, nine of fifteen items done.

---

## Iteration 10 — `c2-surface`: one source, two renderings, and a route name the tools got wrong

`2026-09-07-an-agent-without-mcp-is-not-told-it-has-nothing` §2, tasks **2.1–2.4, all four ticked**.
Python only; no UI file, so no `npm run build`, no bundle refresh and no TypeScript lint
(`tasks.md:7`).

### 2.1 — the list became records, and the MCP rendering came out byte-identical

`_tool_surface_lines` was a 150-line literal list of prose strings with the call signature glued to
the front of each description. It is now `_operations()` — 24 `_Operation` records carrying the
tool, its MCP signature, the method and path, the wire field names, which of those the route
requires, and the **shared** description — plus two renderers, `_mcp_lines` and `_http_lines`. One
source, two renderings (`design.md` D3). `src/agentweave/tool_surface.py` was not touched or reused;
iteration 9 re-confirmed it has zero importers.

**The MCP rendering was diffed against `HEAD`'s, not assumed.** Rendered from the pre-change file
(restored, rendered, restored back, md5 verified), both `has_peers` values:

```
25c25,26
< - `toggle_job(job_id, enabled)`, `run_job(job_id)` — same allowance.
---
> - `toggle_job(job_id, enabled)` — same allowance.
> - `run_job(job_id)` — same allowance.
```

That is the whole diff. Those two shared one line and cannot share a record — `PATCH /jobs/{job_id}`
against `POST /jobs/{job_id}/run` — so the line split in two. Meaning preserved, which is what 2.1
asks for; bytes not, which is worth saying out loud.

### 2.2 — the hoist, verified on the checkout rather than trusted

`resolve_access_path` now runs **above** the context materialisation and its single value feeds both
the renderer and the turn-start notice. The task said to verify the hoist rather than believe it, so:
`grep` finds `access_path` at exactly three places (`:1006`, `:1007`, `:1026`), all after the
materialisation, and an `awk` scan over lines 649–1006 finds no reassignment of `config`, `probe` or
`runner` — the one hit is the word `runner` inside the 501 message's f-string. The call is pure.

**The other two callers keep the `"mcp"` default, and the reason is written into the docstring.**
`POST /agents/register` and `GET /agents/agent-context` are answered under the project API key
outside any run; the access path is settled at spawn time, so a route answering *"what will your
next run use"* would be making a prediction the trigger is free to contradict — the same
disagreement, one layer up. Neither is on a run's path to the plane: a run reads the materialised
context file, never these routes.

### 2.3 — the agreement check runs against both, and caught something on its first run

The MCP half is checked against `mcp.list_tools()` as before. The HTTP half is checked against **the
app's own OpenAPI schema** — the routes actually mounted, with the fields they actually accept and
require — which is the equivalent ground truth on that path. Five new route checks: the route
exists, no described field is one the route does not accept, required-ness matches the route's,
nothing the route requires is left unnamed, and the prefix constant is one the app mounts. Plus set
equality between the two renderings, so adding an operation to one alone fails.

**First run, first failure, a real defect:** `GET /api/v1/agent-actions/recall/{observation_id}` —
the route's path parameter is `output_id`. The tool's argument is `observation_id`, and I had
written the tool's spelling into the address. An agent following it gets a 404. This is the second
instance of the same class in this change (`send_message`'s body is `recipient`/`type`, not
`to_agent`/`message_type`), which is why the HTTP rendering names wire fields rather than deriving
them from the signature — and why mutation 6 exists.

### 2.4 — method, path, fields, and no restated validation

`- `POST /api/v1/agent-actions/messages` (`send_message`) — body `recipient`*, `subject`, `content`*,
… — message_type is one of …`. `*` is required, explained once in the HTTP preamble. Validation
rules are not restated: they come back as typed failures, and restating them is the second source of
truth D3 chose this function to avoid. Three operations carry an `http_note` for a request whose
shape is not the argument list — `submit_spec_document` nests everything under `document`, `ask_user`
returns immediately and must be polled, `recall`'s segment is spelled `output_id`.

### Ten mutations, ten named victims — and one that named nobody until it did

`agents.py` md5 `77ebd2f5`, `agent_trigger.py` md5 `3134c231`, both restored and `git status`
verified after every one.

| # | Mutation | Named |
|---|---|---|
| 1 | `access_path=access_path` deleted from the trigger's render call | 2 — both new trigger tests |
| 2 | `over_mcp = True` (the path ignored) | 5 |
| 3 | required-ness never marked `*` | 1 — required-and-marked |
| 4 | `AW_RUN_TOKEN`'s **value** interpolated into the preamble | 2 — the leak test, and the trigger's HTTP test |
| 5 | `/messages` → `/message` | 2 — route-exists, and the trigger's HTTP test |
| 6 | wire fields replaced by the tool's argument names | 2 — field-accepted, required-never-omitted |
| 7 | one operation renamed out of the server's set | 4 |
| 8 | the HTTP fields clause never rendered | 1 — required-never-omitted |
| 9 | `_render_hub_agent_context`'s default flipped to `"cli"` | **0, then 1** |

Mutation 9 is the one worth reading. Flipping the default silently rewrites every non-run caller's
tool list into HTTP wording, and **nothing failed** — my own §2.2 decision was held by a docstring
paragraph and by nothing else, which is precisely the gap iteration 6 recorded against F295's task
ordering. Closed rather than noted: `test_the_two_routes_outside_a_run_keep_the_injected_tool_wording`
asserts both routes' rendered context, and the mutation now names it.

### Checks

`py -3.11 -m ruff check src/ hub/ tests/` clean. `black --check --target-version py311` — 552 files
unchanged. `mypy src/` — no issues in 22 files. Scoped slice of 13 files, `py -3.11`: **285 passed**,
1 skipped, in 78.13 s. The full hub suite was **not** run; it belongs at 6.2 when the change is
finished, and it is 15–47 minutes.

### Not done, deliberately

- **No Hub was started.** 6.3–6.5 are `c2-verify`'s. Until then "an HTTP agent can act on this text"
  is a source reading with route-schema agreement behind it — strong, but the routes were checked,
  not called.
- **A real run still cannot reach the HTTP rendering.** `resolve_access_path` returns `"mcp"`
  unconditionally until §4 (`c2-resolve`). The two new trigger tests patch it, and say so in their
  docstrings; what they verify is the threading, which is complete now.
- `mutate_c2_surface.py` is left uncommitted in `testbed/scratch/`.

`iteration` 10, `current` → `c2-contract`, ten of fifteen items done.

---

## Iteration 11 — `c2-contract`: the always-ask rule stops being the adapter's, and the deadline stops being a secret

`2026-09-07-an-agent-without-mcp-is-not-told-it-has-nothing` §3, tasks **3.1–3.9, all nine ticked**.
Python only; no UI file, so no `npm run build`, no bundle refresh and no TypeScript lint
(`tasks.md:7`). `openspec validate --strict` passes on the change.

### 3.9 first, because it decides §3.1–§3.4: the answer is D18

The task said to read why the assertion at `test_agent_actions_governed.py:137-140` exists before
changing it, and that if the answer were genuinely the operator's it goes to `decisions_for_user`.
It is not open any more. `proposal.md`'s *"One thing this proposal does not decide"* states the
position (D18 is the rule) and says the operator owns it; the delta encodes it as a scenario of its
own — *"Archiving scheduled work is directed, on either path"* — and `APPROVALS.md`'s row for this
change is an unqualified `APPROVED` with no carve-out. Approving a proposal whose stated position is
D18, over a delta that requires it, is the answer. So the assertion was flipped **with its comment**,
and a green test was asserting the behaviour this change removes: it archived over HTTP with only the
standing allowance, expected `200`, and said in prose that archiving was governed like every other
job mutation.

### 3.1 — the mechanism, chosen and written down (`design.md` D5)

**The route returns a typed failure and the caller polls.** Not a blocking route. Three reasons, in
`hub/hub/operator_direction.py`'s docstring rather than only here:

1. `/permission-requests` already works this way, and the adapter's `_ask_operator` is already a poll
   loop over those exact routes. Same shape means one protocol on the plane, and the operator's card,
   its SSE event and its UI are the shipped ones — nothing new on the human's side.
2. A blocking route would hold an `AsyncSession`, and therefore a pooled connection, for the whole
   operator budget (120s default). On SQLite that pool is small, and F295 — archived four commits ago
   — is a finding about what happens to it.
3. The waiting is then the caller's, which is the point: an HTTP agent performs what the injected
   tool performs for an MCP one, from the description §2 renders.

`409` with `code: operator_direction_required`, a `permission_request_id` and a `poll` address.
`409` rather than `403` deliberately — nothing has been denied, and the state blocking the request is
one the operator can change; telling an agent it was refused when it was not is the whole content of
the operator's eventual answer. A denial afterwards **is** `403` (`code: operator_refused`).

**Where the rule landed: `jobs.archive_job`, not the agent-actions route that delegates to it.** That
puts it on the path of every caller of the contract by the same code, and it gets the ordering right
for free — the gate runs *after* existence, already-archived and the loop refusal, so a card is only
ever opened for an archive that would otherwise have happened. Asking a person to authorise a 404 is
asking them to read something meaningless, and a card the run never returns for is one they dismiss
by hand. `hub/hub/api/v1/jobs.py`'s docstring said D18 was "enforced one layer up, at the MCP tool";
that sentence was the defect, and it is now the paragraph explaining the two gates and why their
order is what it is.

### 3.2 — and the adapter stops holding a copy

`mcp_server.archive_job` no longer calls `_ask_operator`. It posts the archive, and if the Hub
refuses with `operator_direction_required` it waits on the id **that refusal carries** and repeats.
`_ask_operator` was split so the waiting half (`_await_decision`) is reusable: opening the request is
the rule and is not shared; waiting is protocol and is. The load-bearing assertion in the new adapter
test is the negative one — this tool opens no permission request at all.

To carry the id, `HubAPIError` gained `.data`, the refusal's own body when it had structure, beside
the sentence `_readable_detail` reduces it to. Without it the adapter would have had to parse an
identifier back out of prose written for a person.

### 3.3, 3.4 — the rule's content, and the loop refusal

*Regardless of the run's permission posture* is not a check in the gate; it is the absence of one.
Posture is an environment variable of the spawned process (`AW_PERMISSION_POSTURE`), nothing carries
it back to the Hub and no column holds it — so the route structurally cannot consult it. Pinned
anyway, against `full_access`, because the alternative to a test is a comment claiming so. The
standing allowance is pinned from the other side: a `PATCH` on the same job succeeds in the same test
that the archive is refused in.

3.4 asked where the loop refusal is *actually* enforced rather than assuming. Measured: it is in
`jobs.archive_job`, for an agent caller only, recognised by `agent_identity`/`run_identity` being
present — the same recognition `_require_agent_job_allowance` uses. It already had a test; mutations
2 and 5 both name it, so the ordering that keeps it ahead of the direction gate is now held by tests
rather than by my comment.

### 3.5, 3.6, 3.7 — the wait

`QuestionResponse.wait_expires_at` is disclosed. The Hub stamped it, judged the caller against it
(`POST /questions/wait-ended` refuses a report whose question has not expired) and told nobody; the
injected tool did not notice because it carries a private copy of the number from
`AW_QUESTION_TIMEOUT`, and an HTTP caller has neither that variable nor the tool.

The protocol itself is now in §2's HTTP rendering, which is where a caller with no tool actually
reads it: poll `GET .../questions/{id}` until `answered` or `declined`, stop at `wait_expires_at` for
anything that is neither, then `POST .../questions/wait-ended` with **only the ones that ran out of
time**. That last clause is 3.7: a decline is a decision the operator made and handed back, and
reporting one as an expiry records the opposite of what happened. It was a comment governing one
adapter; it is now the contract's own statement, with a test.

**3.6's second half, decided and not done.** `mcp_server.QUESTION_ANSWER_TIMEOUT` keeps its own
monotonic deadline and does **not** read the Hub's stamp. Converting an absolute instant from another
process into a local monotonic deadline is precisely the cross-process clock comparison
`_record_the_wait_and_park`'s docstring says the design avoids, and the tool's deadline being *later*
than the Hub's stamp is what lets the `wait-ended` refusal reject a forged early report and never a
genuine one. The duplication that remains is the literal `240`, and it is already pinned —
`test_question_wait_resolution.py:39` asserts the two agree. Written into `tasks.md` either way, as
the task asked.

### 3.8 — tests reach the routes directly, which is the whole point of the section

Nine tests added or changed in `hub/tests/test_agent_actions_governed.py` (12 in the file now), all
over a bearer run token with no adapter anywhere: the flipped 3.9 block through to a real archive
after the operator decides; the most permissive posture; the allowance that is not a standing yes;
the repeat that opens no second card; the denial that stays denied; the three refusals that open no
card at all; and the operator's own archive, which is not asked to authorise itself. Three more in
`test_a_task_waits_while_its_run_waits.py` §4b for the disclosure — read off the **response**, not
the row, because the row was always right and the run could not see it. Two in
`test_tool_surface_matches_server.py` for the two protocols the HTTP rendering now states. Four in
`test_mcp_server.py` for the adapter, including a refusal that is not about direction and must be
re-raised rather than waited on.

### Eleven mutations, eleven named victims

`testbed/scratch/mutate_c2_contract.py`, uncommitted. Every file restored from the original bytes and
md5-verified after each, plus `git status --porcelain` compared against the baseline — the tree
already carried this iteration's work, so "clean" could not be the check.

| # | Mutation | Named |
|---|---|---|
| 1 | the direction gate deleted | 5 |
| 2 | the gate also runs before existence/loop | 2 — including the shipped loop test |
| 3 | a pending request opens a second card | 1 |
| 4 | a denial reported as not-yet | 1 |
| 5 | the operator asked to authorise themselves | 2 |
| 6 | an `allowed` request does not let the archive through | 1 |
| 7 | `wait_expires_at` removed from the schema | 3 |
| 8 | the adapter asks the operator itself again | 6 |
| 9 | a structured refusal loses its structure | 5 |
| 10 | the expired-only clause dropped from the description | 1 |
| 11 | the archive's direction protocol not described | 1 |

Mutation 11 is the one worth reading. Its first form relabelled the `http_note` as `detail` and
**named nobody — correctly**: `_http_lines` renders `detail` too, so the text was still there on the
HTTP path and the test was right to pass. The mutation was wrong, not the coverage. Replaced with one
that removes the note, which names the test. Recorded because iteration 10's mutation 9 was the
opposite case (a mutation that named nobody and should have), and telling the two apart is the only
thing that makes a zero informative.

### Checks

`py -3.11 -m ruff check src/ hub/ tests/` clean. `black --check --target-version py311` — 553 files
unchanged (one file needed reformatting and was reformatted). `mypy src/` — no issues in 22 files.
Scoped slice, `py -3.11`: the four mutation files plus `test_agent_trigger`, `test_launchability` and
`test_jobs` — **274 passed**, 1 skipped, in 91.43 s. A wider job/question/permission slice ran
earlier: **294 passed**, 4 skipped, in 99.12 s. The full hub suite was **not** run; it belongs at 6.2
when the change is finished, and it is 15–47 minutes.

### Not done, deliberately

- **No Hub was started.** 6.3–6.5 are `c2-verify`'s. The direction protocol has route-level tests and
  an adapter test, but no operator has clicked the card it opens.
- **A real run still cannot reach the HTTP rendering.** `resolve_access_path` returns `"mcp"`
  unconditionally until §4 (`c2-resolve`), which is next.
- `hub/hub/operator_direction.py` is a new module and is committed with the change.

## Iteration 12 — `c2-resolve`: what a run is given stops deciding what it is told, and the mirror turns out to be a run that cannot write

`2026-09-07-an-agent-without-mcp-is-not-told-it-has-nothing` §4, tasks **4.1–4.9, all nine ticked**.
Hub-Python-only again — no UI file, so no `npm run build`, no bundle refresh, no TypeScript lint
(`tasks.md:7`). `openspec validate --strict` passes on the change.

### 4.9 first, because it was written to be first, and it changes what §4 is allowed to claim

The task said to establish on a real harness what happens when the Hub emits
`--permission-prompt-tool mcp__agentweave__approve_tool_call` into a harness that will not provide
it — *before* choosing a mechanism. Three real `claude -p` runs on Haiku, `--output-format json` so
`permission_denials` is read rather than inferred:

| | flags | Read | Write / mutating Bash |
|---|---|---|---|
| A | `manual` + the approver, **no** `--mcp-config` | **succeeded** | **denied** — `Write`, `PowerShell`, `Bash`, all three |
| B | `acceptEdits` (what the `cli` path produces) | — | **succeeded** |
| C | `manual`, **no** approver flag | — | **denied** (`Write`) |

`runner_commands.py:245-248` predicted "every tool call fails". Sharper than that: reads and a
non-mutating `echo` go through, and everything needing approval does not. And the part the comment
did not predict is the one that matters — in A the model reported *"a workspace establishment
issue … contact your system administrator"*, while in C, denied for the same reason without the
approver flag, it reported *"I need permission to write the file"*. The Hub's own configuration
produces the explanation that blames the operator's machine, and the agent spent the rest of its
turn retrying the write through three different tools because the error did not read as *ask
someone*.

Filed as **F299 (A)**. The mirror is **not a wording defect**; it is a run that cannot act.

### 4.1 — a fourth mechanism, because D7's three shared an assumption

D7 listed three and D9 ruled all three out: each moves the run's permission posture as a side
effect, and the delta forbids that. Read together they are nearly a contradiction. What dissolves it
is the assumption D7's three entries share without stating — that *what a run is given* and *what a
run is told* are one value. They have been one value since `access_path` existed, which is why three
rounds re-derived the list without questioning it.

**So: two functions.**

- `resolve_access_path(runner, override)` — what the run is **given**. Behaviour unchanged:
  unconditional `"mcp"` for an injectable runner, moved only by `hub_client`. Still the sole input to
  `mcp_command`, and through it the posture.
- `described_access_path(access_path, override=…, harness_honoured_mcp=…)` — what the run is
  **told**. Asserts the tool surface only on grounds.

A **declaration** by the operator moves containment, because it is theirs and it is declared. An
**inference** by the Hub moves only the wording. D9's condition was never that grounds are
forbidden; it was that they must not move containment silently, and after the split nothing silent
can.

**The grounds are a measurement, and there is only one available.** `Run.mcp_adapter_online_at`
(migration `0102`), stamped by `POST /api/v1/agent-actions/mcp-adapter-online`, which
`hub/hub/mcp_server.py` posts **before it serves**. This process existing is the evidence. Read per
project and per agent, positive evidence only — a harness that ignores the configuration is silent,
and silence is exactly what *no grounds* means.

Gathered at adapter startup rather than from the first tool call, deliberately: a model told to use
HTTP may never reach for a tool, so tool-call evidence would leave a permitted harness undescribed
forever. The bootstrap cost is one turn — a fresh agent reads the HTTP form on its first run while
the server is in fact injected, and earns the MCP wording from the second.

### 4.2 — the probe is deleted, not re-aimed

`probe_mcp_registered`, `_probe_cache`, `_PROBE_TTL_SECONDS` and the `PROBEABLE_RUNNERS` alias are
gone, with `subprocess`, `time` and `Tuple` from the imports. D10's argument was not re-tested on a
policy-blocked harness because it does not need a harness: `mcp list` runs a process that was never
given the config, so it answers the wrong question on both kinds of machine. The reasoning now sits
in `launchability.py`'s access-path comment, where the next person reaching for a subprocess will
read it, and `test_the_probe_is_gone_and_stays_gone` asserts it cannot return unnoticed.

### 4.4's actual answer, which is narrower than the task expected

*Did any test believe conftest's docstring?* **No test path called the probe, so no test was
exercising the `cli` path because of that fixture.** What the fixture did was patch a function
nothing reached while telling every reader the opposite. It is removed rather than corrected —
there is nothing left to default — and a comment stands where it was saying so.

Three tests *named* the probe and are separate: §4.5's (replaced, not deleted — the behaviour it
meant to guard is real and is now distinguishable) and §4.7's two, each replaced by one that asserts
a *difference* rather than a call that could not happen for any input.

### The tests that had to move because the default flipped

Two were written when `resolve_access_path` returned `"mcp"` unconditionally and no configuration
could reach the other branch, so they patched it. Both are now **driven from configuration**: the
HTTP one by an ordinary unstated `claude` agent, the MCP one by `hub_client: "mcp"`. And
`test_trigger_injects_identity_env_and_tells_agent_the_access_path` asserted the MCP notice for
exactly the case that now correctly gets the HTTP one; it asserts the HTTP form *and* that
`mcp_command` is still set, which makes it a second witness for §4.8.

### 4.8, stated rather than assumed

The inference does not move the posture. A run with no grounds still gets `--mcp-config`, still gets
`--permission-prompt-tool`, still runs under `manual` rather than falling back to `acceptEdits` —
which has no path check at all. Asserted on the **built argv**, not on `mcp_command`, because the
posture is decided two functions away from the flag it reads.

### The decision that is not this window's

Every remedy for F299 moves containment: resolving to `cli` on an inference hands every unstated
`claude` run `acceptEdits`; keeping `manual` without an approver denies everything anyway with
nobody headless to ask; leaving it is condition A. There is no non-widening, non-breaking option,
and the delta says in as many words that containment is the operator's to decide. It is in
`decisions_for_user` with the drive attached. Iteration 11's precedent does not cover this one: an
approval settles a position the proposal *states*, and this proposal states the opposite — that the
choice is not the change's to make.

`hub_client: "cli"` is the one-line workaround that exists today, already documented in
`src/agentweave/config.py:714`. What it silently buys is `acceptEdits`, and nothing in the UI says
so.

### Sixteen mutations, and the one that named nobody

`testbed/scratch/mutate_c2_resolve.py`, uncommitted. Every file restored from the original bytes and
md5-verified, plus a `git status --porcelain` comparison **narrowed to the mutated path** — iteration
11's whole-tree baseline is the stronger check and the wrong one for a 35-minute run, because this
session was editing `FINDINGS.md` while it ran and a byte-perfect restore failed the comparison.

| # | Mutation | Named |
|---|---|---|
| 1 | §4 does nothing: the run is told whatever it was given | 8 |
| 2 | the observation is ignored: an injected server is always asserted | 8 |
| 3 | the operator's own `mcp` statement stops being grounds | 3 |
| 4 | grounds manufacture a surface that was never injected | 9 |
| 5 | the inference moves containment too | 5 |
| 6 | grounds stop requiring a report — any prior run counts | 2 |
| 7 | one agent's harness speaks for every agent in the project | 1 |
| 8 | grounds leak across projects | 1 |
| 9 | the adapter reports in and nothing is recorded | 2 |
| 10 | a repeat report moves the instant the harness started the server | **0 → 1** |
| 11 | the adapter never announces itself | 1 |
| 12 | the adapter announces after it starts serving, which is never | 1 |
| 13 | the adapter posts to a route the Hub does not mount | 2 |
| 14 | the trigger describes what it injects again | 5 |
| 15 | the canonical context goes back to describing the injected path | 1 |
| 16 | the migration backfills, claiming every old harness honoured the injection | 1 |

**Mutation 10 is the one worth reading, and unlike iteration 10's and 11's zeroes this one was a
real gap.** `test_the_first_report_wins_and_a_repeat_changes_nothing` did the obvious thing — POST,
read the stamp, POST again, compare — and could not fail, because **the system clock on this machine
has ~15.6 ms granularity**: five consecutive `datetime.now(timezone.utc)` calls return the *same*
value, so the overwriting mutant wrote a timestamp indistinguishable from the one it replaced. The
test now plants a fixed `2020-01-01` stamp and asserts the POST leaves it alone, which cannot be
confused with `now` on any clock. Re-run: mutation 10 names it. Worth keeping in mind for anything
here that asserts two instants differ.

### Checks

`py -3.11 -m ruff check src/ hub/ tests/` clean. `black --check --target-version py311` — 555 files
unchanged. `mypy src/` — no issues in 22 files. Scoped slice under `py -3.11`, eleven hub test
files: **378 passed**, 1 skipped, in 2:43. The full hub suite was **not** run; it is task 6.2 and
belongs to `c2-verify`, and it is 15–47 minutes.

### Not done, deliberately

- **No Hub was started.** 6.3–6.5 are `c2-verify`'s, and the grounds mechanism has only ever been
  exercised in-process: no real spawned `mcp_server.py` has posted the announce over a real socket.
  That is now the single most valuable thing 6.5 can do, and it is cheaper than the task assumed.
- **F299 is not fixed**, and the change deliberately does not fix it. See above.

---

## Iteration 13 — `c2-verify`: the plane is reachable, and no tool of the agent's can reach it

Branch `autonomous/2026-09-08-daily`, head `3a1e6bc` at start — matches `STATE-night.json`. Sections
5 and 6 of `2026-09-07-an-agent-without-mcp-is-not-told-it-has-nothing`, all six tasks, and the
change is archived. Commits `45ebf58`, `d8efa63`, and this one.

### 5.1 — the docs said three adapters

`docs/architecture/overview.md` claimed *"HTTP, MCP, and agent CLI adapters expose the same governed
action set."* Two exist. Corrected to two, plus the sentence that identity is never accepted from a
request body or header on either — and the HTTP half, which is now genuinely agent-reachable and was
explained nowhere, got a section of its own: the contract, its route prefix, the two environment
variables, and the statement that MCP is an adapter over it generated from one description rather
than written twice.

**5.2 was a prohibition and was honoured.** `.claude/skills/copilot-test-setup/SKILL.md` is
untouched; it belongs to the Copilot change.

### 6.1 — lint

`py -3.11 -m ruff check src/ hub/ tests/` — all checks passed. `black --check --target-version
py311 src/ hub/hub/ hub/tests/ tests/` — 555 files unchanged. `py -3.11 -m mypy src/` — no issues in
22 files. **The TypeScript set was not required and this is why**: no UI file changed, so
`hub/hub/static/ui` was not rebuilt and `hub/ui` has nothing new to lint.

### 6.2 — the full hub suite

`py -3.11 -m pytest hub/tests/ -q`, backgrounded per the limit. **4025 passed, 86 skipped, exit 0,
in 1507.94s (25:07)** — 53 more tests than iteration 1's 3972, which is this change's own. Within
the 15–47 minute band; the drives ran against a live Hub while it was in flight and it still landed
at the fast end. 28 `PytestUnhandledThreadExceptionWarning`s, all the F295 `Event loop is closed`
signature iteration 1 already recorded; none is a failure.

### The Hub the drives ran against

The instance on 8010 was started **2026-09-07 21:55** and was therefore two days of commits stale —
it predates migration `0102` and the announce route entirely, so every drive would have measured
nothing. Killed and restarted from source per CLAUDE.md (`cd hub`, explicit trial `DATABASE_URL`,
`py -3.11 -m uvicorn hub.main:app --port 8010`), which ran `0101 -> 0102` on startup. **Check the
start time of a Hub you did not start yourself** — `Get-CimInstance Win32_Process` gives it.

Fixture: a fresh project outside this repo, `proj-08e61dec0192` at `C:\Users\huida\aw-c2verify`, one
`claude` runner on `claude-haiku-4-5-20251001`, two agents — `httpagent` with
`config.hub_client = "cli"`, `mcpagent` left unstated. Every turn on Haiku, per the standing rule.

### 6.5 — the grounds mechanism, end to end, and it works

The first time §4's mechanism has run outside an in-process test.

- **Run 1 of a fresh agent** (`run-7f94b90838b5`): the Hub injected `--mcp-config`, the harness
  honoured it, the spawned `hub/hub/mcp_server.py` posted `/api/v1/agent-actions/mcp-adapter-online`
  **before serving** — `204` in the access log — and `runs.mcp_adapter_online_at` was stamped
  `03:37:54`. The run was *told* the HTTP form, having no grounds yet, while its MCP tools were in
  fact present. It found `mcp__agentweave__create_task` anyway and used it, `201`. The one-turn
  bootstrap cost the design predicted, observed and survived.
- **Run 2 of the same agent** (`run-14ba2fb78e0e`), asked to quote its own notice verbatim:
  *"[AgentWeave] Tool access: the `agentweave` MCP tools are available — call send_message /
  create_task / update_task / ask_user directly."* Grounds earned on run 1, description moved on
  run 2, live.

**That also settles 6.3**, by a better witness than the task expected. The adapter is a child process
of the run; it read `HUB_URL` and `AW_RUN_TOKEN` out of that process environment, reached the Hub
over a real socket, and the Hub resolved the request to *that run's* row and stamped it. So `HUB_URL`
names an address the child can reach, and the token in the child's environment is the one whose
digest the run row holds. Both of `proposal.md`'s source-only claims are now measured.

### 6.4 — and then the drive found what the rounds could not

The question 6.4 asks is not whether the notice renders. It is whether a model that reads it goes on
to make a successful request. **It does not, and there is no posture on this machine on which it
can.** Two findings, both severity A, both new.

**F300 — our own approver denies the request our own notice instructs.** Under the default
`workspace` posture, `curl "$HUB_URL/api/v1/agent-actions/tasks"` came back
`Denied: '/api/v1/agent-actions/tasks' is outside your workspace.` The mechanism is one regex,
`mcp_server.py:936`: `_decide` reads absolute-looking paths out of a shell command's text, and a
URL's path component is absolute-looking. It is not specific to the plane — `https://example.com/x`
yields the candidate `//example.com/x` and is denied identically. **An agent on this repository's
default posture cannot `curl` anything at all**, and is refused in the vocabulary of the filesystem,
so the model concluded its workspace boundary was blocking the endpoint and stopped.

**F301 — on the `cli` path there is no tool that can make the request either.** `run-dceb35938752`,
2m14s, exit 0, **zero requests reached the Hub**. The notice was read correctly — the run's first
thought quotes the route and the body schema §2 renders for the HTTP path — and then fifteen
attempts hit five different walls: the harness's own analyser refuses a shell command that
interpolates `$AW_RUN_TOKEN` (`Contains simple_expansion`, `Command contains expandable strings with
embedded expressions`, `subexpressions $()`, `.NET methods`, `expansion obfuscation`), and
`acceptEdits` with no approver auto-denies what is left. It spawned a subagent to try the same
request; that failed the same way.

**This corrects part of F299**, which recorded `hub_client: "cli"` as the one-line workaround on the
strength of a raw `claude -p` that wrote a *file*. That much stands. It does not restore access to
the plane, which is the thing a run is moved to that path in order to reach.

Neither is fixed here and neither should be: every remedy moves containment, which is already the
open `F299-posture-on-a-harness-without-mcp` decision. The two findings enlarge that decision rather
than replace it, and `decisions_for_user` now says so.

### Does this invalidate the change? No, and the corpus says why

The delta requires what a run is **told** — that the plane is reachable, that the variables are
named and the values are not, that the operations are described in the idiom of the path. All of
that shipped and is verified. The plane genuinely *is* reachable from the run's process environment;
the MCP adapter reaches it from exactly there, which is the drive's own evidence. What is not true
is that the agent's own tools can reach it, and that was never what the requirement said.

So the change is complete and archived — and the synced corpus carries one paragraph that is in
neither the delta nor the change, stating the gap by name, because a corpus that said "reachable"
and stopped there would read as a claim nobody has proven.

### 4. Sync and archive

Deltas applied by hand into `openspec/specs/agent-capability-plane/spec.md` — there is no `openspec
sync` command (DEAD-ENDS). One MODIFIED requirement replaced wholesale, the two ADDED requirements
placed directly after it. Two passages were rewritten rather than copied: a delta argues about the
state it is changing, and the corpus states the state that shipped, so *"two things currently break
it"* became *"broke it until 2026-09-09"* and *"today, says the opposite"* became *"until
2026-09-09, said the opposite"*. `openspec validate --strict` passes on the spec and on the change.
Archived to `openspec/changes/archive/2026-09-07-an-agent-without-mcp-is-not-told-it-has-nothing`.

### State left behind

The trial Hub is **running** on 8010 from this checkout's source, migrated to `0102`. The fixture
project `proj-08e61dec0192` and `C:\Users\huida\aw-c2verify` are kept — they are the reproduction
for F300 and F301. Raw runs and the drive harness are uncommitted in `testbed/scratch/c2verify/`
(`setup.py`, `drive.py`, `driveA.json`, `driveB1.json`, `driveB2.json`, `driveC.json`,
`full-hub-suite.txt`, `hub-8010.log`). No job was created, so none was left enabled.

Next: `c3-conversation-run-facts` — F274, 44 tasks, a bundle likely to take two nights, and it
rebuilds `hub/hub/static/ui`, so it may not share a night with `c4`.

---

## Iteration 14 — `c3` phase 0: the defect is live, and the finding written to justify phase 3 is wrong

`c3-conversation-run-facts` opens with a hard gate — *"if phase 0 has not been recorded, do phase 0
and stop"* — so this firing is phase 0 and nothing else. It is the right gate: the 2026-09-05 drive
did not reproduce F274 at nine runs across six conversations, and a change built on a defect nobody
has seen on this checkout is a change built on a memory.

### Reconciliation first

`STATE-night.json` said the trial Hub was "already running on 8010 from source" and told me to check
its start time before driving. **It was not running at all** — nothing was listening on 8010 at
05:00, and the only Hub on this machine was the operator's own on 8000, which nothing here touches.
So the staleness trap iteration 13 warned about did not need avoiding; the instance had simply gone
with the process that started it. Branch and `git log` matched STATE (`6557844`, the mcp-notice
archive commit) with a clean tree.

### The instance

Phase 0's own wording was followed rather than a remembered port: **a spare port, a fresh fixture
project.** Port `8012`, its own profile database at `~/.agentweave/hub/profiles/f274p0/`, created
empty and migrated `0000 → 0102`, started from `hub/` with `py -3.11 -m uvicorn hub.main:app` at
`05:01:46.485`. Zero `.py` files under `hub/hub` or `src/` were newer than that process start, so
the instance really is serving this checkout. Project `proj-9042770d2ae3`, one Haiku runner, one
agent, every turn below a real Haiku turn.

### The verification round, done against the code while the turns ran

The queue said this change needs one verification round and that the round is not optional. Every
line reference the proposal, design and tasks make was opened: `ChatHistoryResponse` is where the
tasks say it is and **`Run` is genuinely absent from `agent_chat.py`'s import block**, as the third
review noticed; `agents.py`'s `RunFacts` construction, `agentTimelineModel.ts`'s order-preserving
`groupIntoTurns`, `inbound_queue.py`'s single commit that adds the `Run` and stamps
`delivered_in_run_id` together, `useSSE.ts:404-412`'s unfiltered `invalidateQueries()` and its
app-wide mount at `App.tsx:216`, and `agent_trigger.py`'s pre-spawn `except` block with no
`record_agent_output` in it — all present and all as described. **The argument survives the round.**
What did not survive is a claim in the *findings ledger* the argument leans on, and only driving
found it.

### 0.2 — the headline, cross-conversation: reproduced

Conversation `ZEBRA-A`, one long turn stopped after 8 s (`run-c663930f13c2`, `stopped`, exit 2).
Before: `Worked for 8s`, `Turn stopped`, one turn boundary, one stat line. Then **eight ordinary
turns, each in its own new conversation on the same agent**, nothing touching A. An ordinary turn
costs **7 timeline events** here, so the fifty-event window moved in seven of them:

```
filler 2 -> events=27 runs=4 A in map True     filler 5 -> events=48 runs=7 A in map True
filler 3 -> events=34 runs=5 A in map True     filler 6 -> events=50 runs=8 A in map True
filler 4 -> events=41 runs=6 A in map True     filler 7 -> events=50 runs=8 A in map FALSE
```

Reloading A in the served bundle: one turn boundary, `{'Turn failed': 0, 'Turn stopped': 0,
'Turn interrupted': 0}`, **zero stat lines**. One operator message on an empty page.

### 0.3 — the half that proves widening the window is not the fix

Ten turns, one conversation, nothing else running. The chat names **10** distinct runs; the timeline
map holds **8**; two turns render with a token count and no `Worked for Ns`. A single conversation
overruns the agent-scoped window on its own, so the bound is the wrong bound rather than a small
bound.

### 0.4 — the live half that was expected to work, and does

Hub hard-killed with a turn in flight, browser open on the conversation, nobody touching the page:
row written `interrupted` at `05:14:02.447`, Hub serving again at `05:14:03.982`, and `Turn
interrupted` + `Worked for 26s` on screen at `05:14:11.06`. **8.6 s, not "never"** — D8 stands and
phase 1 may proceed. Recorded with its confound: the interrupted run's entry was re-delivered 2 s
before the label appeared, so queue traffic the chat hooks *do* hear was in the same window. This
measures "seconds"; it does not prove the reconnect invalidation is what did it, and task 3.5 still
has its own question to ask.

### 0.5 — and here the gate earned its cost twice

**Its own recipe cannot work.** *"Bind the agent to a runner whose binary does not exist"* is
refused by `probe_agent` at `agent_trigger.py:665` before a `Run` row exists — nothing fails, so
there is nothing to watch. The pre-spawn `except` block is reached by the probe's *other* branch: a
**pinned** CLI, checked with `os.path.isfile(x) and os.access(x, os.X_OK)`, and on Windows `X_OK` is
true for any file that exists. Pinned at an ordinary `README.md`, the spawn dies with
`%1 is not a valid Win32 application` — the exact WinError 193 case that block's own comment names.
Three attempts, all `failed`, inside 3 s; **zero `AgentOutput` rows**; `run_failed` carrying `agent`
(which is separately what task 3.2 asks someone to confirm).

**And then F291's predicted symptom did not appear, because the thing it predicts it for does not
exist.** F291 says the operator is left with *"a turn holding a delivered message, no outcome and no
explanation"*. Watched for 75 s with the conversation open and nothing touched: **0 turn
boundaries**, no labels, and — after a second message — a second `NOT DELIVERED / delivery failed 3
times` block that appeared **live, with no reload**. The failing run's entry is *abandoned*, an
abandoned entry lands in `groupIntoTurns`'s `pending`, so there is no turn for `run_failed` to
label, and the operator is in fact told, by the banner, through the queue events the chat hooks
already listen to.

Task 3.1 is probably still right — after this change the run facts ride on the chat response and
something has to refresh them — but **the reason written into 3.1's text is false as stated, and
3.1 instructs the implementer to comment that reason at the call site.** F291 is corrected rather
than retired, its `Status` line now says so, and `tasks.md` carries the instruction not to
transcribe the sentence.

### What this cost, and what it bought

Three rounds read this change and none of them asked what the screen looks like when the entry is
abandoned. A drive asked it in one watch. That is the fifth outing in a row where driving found what
reviewing could not, and the first where the thing it found was in the ledger rather than in the
code.

### State left behind

Hub `8012` **stopped** — deliberately, so no later firing inherits a stale instance the way
iteration 13 nearly did. Kept for phase 6, which re-runs 0.2 and 0.3 *exactly* and wants the same
rows: the profile database, project `proj-9042770d2ae3`, the fixture directory, and the second agent
`ghost050237` whose pinned CLI is the F291 reproduction. `testbed/scratch/f274p0/start_hub.sh`
brings it back. Drive scripts, screenshots and raw logs are uncommitted in
`testbed/scratch/f274p0/`. No job was created, so none was left enabled.

Next: `c3` **phase 1** — the two chat routes gain their `runs` map. Read the 0.5 paragraph above
before phase 3.

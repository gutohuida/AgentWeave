# 2026-09-08 day window

Newest entry at the BOTTOM. Playbook: `.claude/loops/day-window.md`.

Armed 2026-09-08T08:55:02+01:00 on `autonomous/2026-09-08-daily` (parent master@9fb0809), model opus.

---

## Iteration 1 — 09:01 to 09:15 — compose the queue

**Limits restated so a later firing inherits them even if this one dies mid-thought:** stay on the
cycle branch (the merge gate in iteration 1 step 1 is the *only* thing that may touch `master`, under
four conditions it checks itself); nothing outward-facing — push, do not open PRs; nothing
destructive; no open-web browsing; every claim measured or labelled unverified; operator decisions go
to `decisions_for_user`, not guessed; stage explicit paths, never `git add -A`; `py -3.11`, never bare
`python`; `black --target-version py311`; never drive against `proj-5e960453` or `proj-18e5d4e0`, and
never touch port 8000; every real agent turn binds `claude-haiku-4-5`; never leave a job enabled.
**And today only, from `DIRECTION.md`:** no new proposal, no approval token, no decision marked, and
nothing found today gets specced today.

### Step 1 — the merge gate did NOT open, on condition three alone

| Condition | Verdict |
|---|---|
| Branch is not `master`, and `git rev-list --count HEAD..master` is 0 | **pass** — count is 0, `master` is at `9fb0809`, an ancestor |
| `git status --short` empty and `git rev-parse HEAD` == `@{u}` | **pass** — clean, both `8c01b5093247ace5dcfbeee9809b8aa59cc77b7d` |
| CI concluded `success` for this exact sha | **FAIL** — `gh run list` reports the CI run for `8c01b50`, created 07:55Z, `status: in_progress`, `conclusion: ""` |
| No line-initial `HOLD MERGE` in `DIRECTION.md`'s newest section | **pass** — `grep -n '^HOLD MERGE'` returns nothing |

A green from an earlier commit is explicitly not a pass, so the gate stays shut. Queued as
`gate-retry` for a later firing, which must re-check **all four** conditions against the branch's
then-current head, not just this one — the branch moves every iteration, so the sha CI must be green
for is not `8c01b50` any more.

Worth recording because it will recur: the branch was armed at 08:55 and the gate is read at ~09:00,
so **the gate will essentially always find CI in progress on the first firing.** The arm commit
triggers a run that has had five minutes on a suite measured at 15–25 minutes. This is structural,
not bad luck. Only `9fb0809` is behind on `master` anyway today, so nothing was lost.

Adjacent, unasked-for and left alone: `master`'s CI is **failing at `9fb0809`** (and at `4a99e5f`),
while `669f375`, `dfea7b8`, `b4f2ee6`, `a19ecc2` and `cbb6eec` all passed. Not investigated — the
alternating green/red pattern across near-identical markdown-only commits is precisely F292's
signature, and F292 is `d1a`.

### Step 2 — the branch

`autonomous/2026-09-08-daily`, one commit (`8c01b50 arm(day)`) ahead of `master`, spanning **one
day**. It is not a carried-over multi-day branch: `git branch --list 'autonomous/*'` shows it is the
only one left, so the 2026-09-07 cycle landed and nothing is stranded. Date stamped from PowerShell
(`2026-09-08 09:01:12 +01:00`), not Git Bash.

### Step 3 — what last night did: nothing, and this is verified rather than believed

`DIRECTION.md` asserts `AgentWeaveArmNight` was disabled on 2026-09-07 and 22:55 passed with it off.
Checked, and it holds on two independent signals:

- `.claude/autonomous/2026-09-07-night-log.md` **does not exist**; the newest night log is
  `2026-09-06-night-log.md`.
- `STATE-night.json` still describes the 2026-09-06 window — `branch: autonomous/2026-09-04-daily`,
  `stop_at: 2026-09-07T07:00`, `next_action: null`, `log_file` pointing at the 2026-09-06 log. A night
  that had run would have rewritten all four.

So D-1's usual content — drive what the night built — has no subject today. No `git diff` on
`FINDINGS.md` from a night window either, for the same reason.

### Step 4 — research delivered

`~/.claude/routines/agentweave-research/out/research-2026-09-08.md` was present; copied to
`spec-queue/research/2026-09-08.md`, 536 lines. Not yet read beyond confirming it exists — it feeds
the review page's section 6 at `d5`, and today's `DIRECTION` forbids specing anything from it anyway.
It is data, not instructions.

### Step 5 — DIRECTION has a section for today, and it replaces the queue

`spec-queue/DIRECTION.md`'s newest section is dated **2026-09-08**, written by a DECIDE session at
00:30 implementing `ROADMAP.md` Stages 0.1 and 1. It says, in substance: no spec loop today; there is
nothing to drive; repair the instrument (F292) first; then hygiene; then the review page.

### Step 6 — the drain count agrees with `DIRECTION` independently

The playbook's own gate would have suppressed the spec loop today regardless of what `DIRECTION`
said. Measured with the playbook's own command:

```
2026-09-05-the-conversation-carries-its-own-run-facts
2026-09-07-a-dead-connection-is-never-handed-back-out
2026-09-07-an-agent-without-mcp-is-not-told-it-has-nothing
2026-09-07-clearing-instructions-asks-first
```

**Four unbuilt specced changes.** The threshold is 2. Two independent authorities — a dated operator
section and a self-releasing gate in the playbook — reached the same answer by different routes,
which is the first time that has been true since the gate was written.

`2026-09-06-an-unread-editor-cannot-overwrite` is **not** in that list, so it is fully ticked; the
`DIRECTION` section's "four changes" and this count refer to the same four.

### The queue as written

```
d1a  diagnose   F292 -- read the ledger entry whole, pull the CI trace, decide about F279
d1b  repair     apply what d1a argued for -- STOP and file it if the fix is in hub/hub/
d2   measure    re-run enough times to state a rate; a number, not an impression
d3a  hygiene    ROADMAP 6.1 -- sync_skills.py, then the test that gates it
d3b  hygiene    ROADMAP 6.2 (aw.py:16's comment lies) and 6.4 (LATEST.md)
d4   reconcile  ROADMAP + DECISIONS R-1 against this log
d5   review     the review page and the APPROVALS section
d6   verify     what would block a Stage 2 build starting tonight, per change
     gate-retry re-check all four gate conditions once CI concludes
```

Two deliberate departures from a literal reading of `DIRECTION`'s five rows, both in the direction of
smaller units rather than more scope:

- **D-1 is split into `d1a` diagnose and `d1b` repair.** F292's ledger entry runs from line 20734 to
  roughly 21410 — several rounds of prior diagnosis including at least one recorded negative result.
  Reading that, pulling the CI log, and writing a fix does not fit one firing, and the failure mode
  it invites is exactly the one the finding's title already names: reaching for a stronger version of
  a mitigation that has been measured not to hold.
- **ROADMAP 6.3 (rotate the disclosed `aw_live_` key) is carved out of D-3** and raised as `day1` in
  `decisions_for_user` instead. Rotating a credential invalidates whatever still holds it, and the
  instance most likely to hold it is the operator's live Hub on port 8000, which this window is
  forbidden by standing limit to touch. That is a decision, not hygiene.

`d1b` carries an explicit scope guard, because `DIRECTION` ordering a repair does not by itself
license this window to implement: a fixture- or harness-level fix is D-6-style ordinary work and is in
scope with D-6's rules in full; a fix in `hub/hub/` production code is not, and `d1b` is instructed to
stop and file it for tomorrow rather than finish it.

---

## Iteration 2 — 09:20 to 10:05 — `d1a`: F292 diagnosed, and one of the ledger's own conclusions is too strong

**No code changed this iteration.** `d1a` is a reading-and-measurement item; `d1b` applies whatever
this argues for. Everything below is either read first-hand out of the CI log, or read out of the
installed library source at the version this machine runs — nothing is quoted from the ledger's
summary of itself.

### The occurrence `DIRECTION` pointed at is the eighth, and it is the first on `master`

`gh run view 34164645184 --log-failed`, read whole (4,401 lines).

| | |
|---|---|
| Run / sha | `34164645184` / `15ce482`, workflow CI, **branch `master`** |
| Failing job | `hub-test` only; the other eight jobs all green |
| Result | `3968 passed, 18 skipped, 230 warnings, 1 error in 657.72s (0:10:57)` |
| Error | `ERROR at setup of tests/test_flow_fires_a_review_turn.py::test_a_review_that_cannot_be_prepared_does_not_become_an_ordinary_turn` |
| Statement | `sqlalchemy.exc.OperationalError: (sqlite3.OperationalError) database is locked` on `DROP TABLE requirement_drift`, raised from `tests/conftest.py:473` |

Every occurrence to date has been on an `autonomous/*` cycle branch. This one is on `master`, which
matters for one reason: it removes the last reading in which F292 might have been a property of the
cycle branches' trees rather than of the suite.

**Two numbers worth having that the ledger does not record.** The failing test sits at progress
marker `[33%]`, not the `[66%]` where the `test_reviewer_is_not_the_author.py` occurrences land — so
the sliding window is not confined to one region of the run. And the wait is measurable from the
progress stream: the preceding test (`test_an_unstaffable_review_does_not_stop_the_flow_doing_other_work`)
passed at `21:56:02.822` and the error is reported at `21:56:33.556` — **30.7 s**, which is the whole
`busy_timeout=30000`. The holder therefore held the write lock for the entire window rather than
losing a brief race.

Occurrence count is now **eight**; failing tests remain **three distinct, in two files**.

### What the diagnostic printed, and why its most eye-catching numbers are worth nothing

The registry printed a **fifth** character-for-character identical negative: `no connection checked
out` at all three probe points.

The `pool.status()` lines (`Connections in pool: 0`, `Current Overflow: -5`) look like strong
evidence that no connection existed at all. They are not evidence of anything. `QueuePool.__init__`
sets `_overflow = 0 - pool_size` (`sqlalchemy/pool/impl.py:138`) and `dispose()` resets it to the
same (`impl.py:228`), so `-5` on a `pool_size=5` engine is simply *a pool nothing has touched yet* —
and the pool being probed was created moments earlier by the previous test's teardown `dispose()`.
The snapshot is uninformative **by construction**, which the conftest banner already says in one line
and which is restated here because those numbers are the first thing a reader's eye lands on.

So the only load-bearing line in the printout is the registry's. Which brings the actual finding.

### The finding: the registry cannot falsify the thing the ledger says it has falsified

The F292 entry currently states, as its leading conclusion, that *"the standing (main-engine
registry) mechanism is **falsified for all four occurrences that carried the instrument**, n=4"* —
now n=5 — and reads that as ruling out a connection of the product engine as the holder. **That
inference does not hold, and the reason is in SQLAlchemy's own source (2.0.50 installed here).**

The registry listens on `checkout` / `checkin` / `close`. In `_ConnectionRecord.__close()`:

```python
def __close(self, *, terminate: bool = False) -> None:
    self.finalize_callback.clear()
    if self.__pool.dispatch.close:
        self.__pool.dispatch.close(self.dbapi_connection, self)   # <- listener fires HERE
    assert self.dbapi_connection is not None
    self.__pool._close_connection(self.dbapi_connection, terminate=terminate)
    self.dbapi_connection = None
```
(`sqlalchemy/pool/base.py:877-886`)

The `close` event fires **before** the close is attempted, and unconditionally. And the close it
precedes cannot report failure:

```python
try:
    ... self._dialect.do_close(connection)
except BaseException as e:
    self.logger.error(f"Exception closing connection %r", connection, exc_info=True)
    if not isinstance(e, Exception):
        raise
```
(`pool/base.py:371-384` — a plain `Exception` is logged and swallowed.)

`_finalize_fairy` behaves the same way for a failed *reset*: `except BaseException as e:
pool.logger.error("Exception during reset or similar", exc_info=True); connection_record.invalidate(e=e)`
(`pool/base.py:1006-1011`), and `invalidate()` routes straight back into `__close()` above.

**Therefore: on every path where SQLAlchemy gives up on a connection whose OS-level SQLite handle
survives, the registry's entry is removed first, and the failure is written only to the
`sqlalchemy.pool` logger — which pytest discards for a passing test.** A connection of the product
engine still holding an uncommitted write transaction is *guaranteed* to print
`no connection checked out`.

That is not a small correction. It means the five identical negatives are **consistent with** a
product-engine connection being the holder rather than evidence against it, and the entry's
`n=4`/`n=5` falsification claim needs narrowing to what it actually supports: *no connection was
checked out **and still tracked** at the probe points.* This is precisely the failure mode
`CLAUDE.md`'s round discipline names — the argument is wrong while every measurement it is built on
is right.

### A silent leak path that fits the printout exactly, found by reading the driver

Given the above, the question becomes: is there a path where the handle survives *without even
logging*? There is, and it is specific to this dialect.

`SQLiteDialect_aiosqlite` sets `is_async = True` and `has_terminate = True`
(`sqlalchemy/dialects/sqlite/aiosqlite.py:435-436`, downgraded to `False` only if the installed
aiosqlite lacks `stop` — 0.22.1 has it). `_finalize_fairy` computes
`requires_terminate_for_close = is_async and has_terminate` → **True**, so the discard path calls
`_close_connection(terminate=True)` → `do_terminate` → `_terminate_force_close` →
`aiosqlite.Connection.stop()`:

```python
def stop(self) -> Optional[asyncio.Future]:
    self._running = False
    def close_and_stop():
        if self._connection is not None:
            self._connection.close()          # <- the ONLY real sqlite3 close
            self._connection = None
        return _STOP_RUNNING_SENTINEL
    try:    future = asyncio.get_event_loop().create_future()
    except Exception:    future = None
    self._tx.put_nowait((future, close_and_stop))
    return future
```
(`aiosqlite/core.py:116-132`)

`stop()` **queues** the real close onto the connection's worker thread and returns a future nobody
awaits. If that thread is not running the queue, the `sqlite3.Connection` is never closed, its
transaction is never rolled back, its locks persist — **and nothing raises, so not even the
`sqlalchemy.pool` ERROR above is emitted.** A terminate on this dialect is structurally incapable of
failing loudly.

Also confirmed against the installed 0.22.1 rather than taken from the ledger:
`_connection_worker_thread` (`aiosqlite/core.py:48-75`) wraps both `function()` and the
`future.get_loop().call_soon_threadsafe(...)` report in one `try`, and the `except` branch's own
`call_soon_threadsafe` sits outside any handler — so a closed loop kills the thread on the second
raise. F295's mechanism, verified first-hand.

### But F295's signature is absent from both CI occurrences I can read — and this is the sharpest constraint

If F295's dead-thread crash were the cause, it reports as `PytestUnhandledThreadExceptionWarning` —
which is exactly how the mechanism was surfaced in the first place, on a local full-suite run.
Checked in both readable occurrences:

| Run | sha | Thread-exception warnings | `Event loop is closed` | GC "non-checked-in connection" SAWarning |
|---|---|---|---|---|
| `34164645184` | `15ce482` | **0** | **0** | **0** |
| `34029310406` | `7a6c782` | **0** | **0** | **0** |

`hub/pyproject.toml`'s `[tool.pytest.ini_options]` sets `asyncio_mode` and `testpaths` and **no
`filterwarnings`**, so nothing is being suppressed; the 230-line warnings summary is complete and was
read whole. The GC-cleanup path in `_finalize_fairy` also emits a `util.warn` ("The garbage collector
is trying to clean up non-checked-in connection … will be terminated", `pool/base.py:1014-1030`) —
also absent, which rules that specific discard path out for these two.

**So: in the two occurrences whose logs exist, the aiosqlite worker thread did not visibly crash.**
Either F295's dead-thread mechanism is not what produced these failures, or it produced them by a
path that emits no warning at all.

That is measured support for what `DIRECTION` had only asserted in prose: **do not expect
`a-dead-connection-is-never-handed-back-out` to close F292.** It may still close the 3.5-hour *hang*
occurrence, which is the one shape that fits F295 cleanly and the one occurrence with no log.

### Why the mitigation did not hold — precisely, not approximately

`be6a70d` added `await _REAL_ENGINE.dispose()` before the schema reset (`hub/tests/conftest.py:464`),
with a comment blaming *"a test that leaves a live `JobScheduler` … still holding a checked-out
connection when the next test starts."*

Two separate reasons it cannot work, and the second is the one that matters:

1. `dispose()` acts on connections the pool still holds. It cannot reclaim one a running task has
   checked out — the 2026-09-05 D-6 control already established this.
2. **The state it is written for has never once been observed at a failure.** Five instrumented
   occurrences, five `no connection checked out`. Whatever else is true, the mitigation is aimed at a
   condition that is not present when the failure happens.

Strengthening it — a third dispose, a longer timeout, a retry around `drop_all` — is the move
`DIRECTION` warns repeats a measured failure. Confirmed: it would be.

### F279 — answered explicitly: **not the same class**, but one repair could reach both

`DIRECTION` says F279 is *probably* the same class. On the evidence, it is not.

| | F292 | F279 |
|---|---|---|
| Exception | `sqlite3.OperationalError: database is locked`, an OS-level file lock | `sqlalchemy.exc.InvalidRequestError: Could not refresh instance`, ORM session state |
| Where | the **next** test's fixture (`conftest.py:473`), a victim | **inside** the run, at `output_recording.py:94` |
| Latency | the full 30 s busy timeout | ~1.5 s |
| Reproduces locally | **no** — 15 runs of the blamed pair green; whole suite at `busy_timeout=50`, 3968 tests, zero contention | **yes** — 7/12 and 4/12 failures, measured twice on unmodified trees |
| Rate | ~1 in 6 CI runs | ~50% locally |

A defect that reproduces half the time in 1.5 seconds locally, and one that survives a 600×-shortened
patience window across a whole suite, are not the same defect.

**What they do share is a cause of opportunity, and it is worth stating because it is the only thing
that could make one repair cover both:** both sit downstream of
`hub/hub/api/v1/agent_trigger.py:1190`, the `asyncio.create_task(_execute_run(...))` that nothing
awaits. F279 is a second session colliding with that run's in-flight transaction; F292 is something
outliving the test that started it. A repair that makes a run's database work genuinely *settle*
before its caller moves on — which is what F295 proposes — would reduce the opportunity for both.
That is a reason to build F295's change, not evidence that it fixes either.

### What `d1b` should do — and it is in scope

The next instrument is **not** a stronger dispose. Every silent-give-up path identified above writes
to exactly one place — the `sqlalchemy.pool` logger at ERROR — and pytest throws that away for a
passing test. So:

1. Install a session-scoped `logging.Handler` on the `sqlalchemy.pool` logger that retains ERROR
   records (message + traceback) and print them inside `_f292_report`. ~20 lines in
   `hub/tests/conftest.py`, no product change, quiet on a green run exactly as the existing
   diagnostic is.
2. In the same report, for every connection the registry has **ever** seen — not only those still
   checked out — record whether its aiosqlite worker thread is still alive and whether the underlying
   `sqlite3.Connection.in_transaction` is set. `in_transaction` is already known safe to read from a
   dead loop (recorded in the entry's N-1 section).

If the holder is a connection SQLAlchemy silently discarded, (1) or (2) names it. **If both stay
silent at the next occurrence, that is also a result**: it puts the holder genuinely outside
SQLAlchemy's bookkeeping and moves the search to a process or a thread — with the in-repo candidates
already eliminated for this file by collection order. `test_flow_fires_a_review_turn.py` collects at
line 1386, and every test-suite `create_async_engine`, `sqlite3.connect` and `aiosqlite.connect`
lives in `test_migrations.py`, `test_project_persistence.py`, `test_runner_charter_models.py` and
`test_task_workspace_scheme.py`, all of which collect after it. Checked by grep this iteration; the
ledger had only ever enumerated engines under `hub/hub/`, never under `hub/tests/`.

Both are fixture-level. `d1b`'s scope guard is satisfied and no `hub/hub/` change is implied — but
note what that means honestly: **`d1b` is a better instrument, not a fix.** `d2` should therefore
measure the current failure rate on the unmodified tree, which is the branch of `d2` written for
exactly this outcome.

### Honest limits of this diagnosis

- **The holder is still not named.** This iteration removes a wrong conclusion and adds a mechanism
  that fits; it does neither by reproducing anything.
- The library-source arguments are read against **this machine's** SQLAlchemy 2.0.50 and aiosqlite
  0.22.1. CI resolves its own versions; the aiosqlite path in the CI traceback is
  `.../site-packages/aiosqlite/core.py:63`, which matches the local file's line numbering, but
  SQLAlchemy's CI version was not pinned-checked. `d1b` should confirm it before relying on the
  `pool/base.py` line numbers.
- **Absence of a warning is weaker than presence of one.** The F295 signature could in principle be
  emitted at interpreter shutdown, after the summary. The failure here lands 4 minutes into an
  11-minute run, so a crash causing *it* would have to precede it and be attributed to a test — but
  that is an argument, not a measurement.

### Recorded in the ledger too, deliberately

The day log rolls off; `scripts/drive/FINDINGS.md` does not. The correction above is not a note
about this window, it is a correction to a conclusion a fresh process would otherwise re-inherit as
F292's leading reading — so a condensed version of it was appended to the F292 entry as its eighth-
occurrence section (73 lines, inserted before the `---` that closes the entry). The **Status** line at
the top of the entry was left alone: `d1b` owns it, and `d1a`'s brief is diagnosis.

---

## Iteration 3 — 09:28 to 09:40 — `d1b`: the instrument is built, and both halves are proven firing

### The branch moved under this iteration, and that is expected now

`STATE-day.json` claimed HEAD `101f836`; by the time this iteration wrote anything, HEAD was
`c7778a8`, two commits further on. **Not a conflict and not a fault:** the operator's interactive
session is working in this same checkout, and it committed `e964226` (09:21) and `c7778a8` (09:26)
while this window was reading library source. Checked before writing: those two touch
`spec-queue/DECISIONS.md`, `spec-queue/ROADMAP.md` and `.claude/handoffs/DEAD-ENDS.md` — **no file
this iteration edits**, so there is no lost update, and the `FINDINGS.md` patch below was
read-modified-written in one pass at 09:30, after both had landed.

`c7778a8` adds a DEAD-ENDS section describing exactly this situation, written from the other side of
it. Two of its facts are this window's own iteration-1 findings arriving in the durable ledger (the
09:00 merge gate always finds CI mid-run; a red `master` is usually the F292 flake and keeps the gate
shut) — so `d4`'s reconciliation has less to do than it would have.

**`e964226` closes `decisions_for_user` day1.** The operator rotated the disclosed key in session, so
ROADMAP 6.3 closes with it and `d3b`'s carve-out is moot. The *broader* question this window actually
asked — may the loop rotate trial-profile keys itself, or is every rotation the operator's — is
recorded as still open, with the window's reasoning kept. Rewritten in `decisions_for_user` to the
question that survives rather than deleted, because a later window meeting a disclosed credential
needs the unanswered half, not the closed instance.

### What was built

`hub/tests/conftest.py` only. Fixture-level, no `hub/hub/` change, so `d1b`'s scope guard holds and
nothing here needs tomorrow's spec loop.

1. **`_PoolErrorRecorder`** — a bounded (`maxlen=50`) `logging.Handler` at ERROR on the
   `sqlalchemy.pool` logger, whose records are formatted with their tracebacks and printed inside
   `_f292_report`. Attached to the **parent** logger deliberately: a pool logs under
   `sqlalchemy.pool.impl.<PoolClass>` (measured — `AsyncAdaptedQueuePool` for this URL), the engine is
   disposed once per test so those child loggers come and go, and they propagate. The level is only
   ever *lowered* to ERROR and never below, so nothing verbose is switched on.
2. **`_EVER_SEEN`** — every connection the registry has *ever* seen, not only those still checked out,
   reporting per connection whether the `sqlite3` handle is open, whether the aiosqlite worker thread
   is alive, whether it is in a transaction, and whether `stop()` was called without the queued close
   ever running. Weak references throughout, pruned of dead entries past 512, capped at 12 lines of
   output with a counts line carrying the rest.

### Four weakref facts, measured — and they correct the entry's own N-1 note

The N-1 section said only *"the DBAPI connection is not weakref-able but `_ConnectionRecord` is"*.
Checked against the installed SQLAlchemy 2.0.50 / aiosqlite 0.22.1:

| Layer | Weakref-able |
|---|---|
| `_ConnectionRecord` | yes |
| `AsyncAdapt_aiosqlite_connection` (the DBAPI adapter) | **no** |
| `sqlite3.Connection` | **no** |
| `aiosqlite.Connection` | **yes** |

The last row is what makes the instrument possible at all, and it was not previously recorded. The
record alone would not have done: SQLAlchemy sets `record.dbapi_connection = None` when it closes one,
so the object holding the file becomes unreachable from the record at exactly the moment it becomes
interesting. The aiosqlite `Connection` is the one layer that both carries the leak signature and can
be weakly referenced.

Also measured rather than reasoned, and it makes the leak signature *exact*: `stop()` sets
`_running = False` immediately and then queues the only real `sqlite3` close. A stop whose worker
thread **is** draining leaves thread dead, `_running` False and `_connection` None within
milliseconds. So `_running == False` **with `_connection` still not `None`** is unambiguously the
close that was queued and never ran.

### Mutation-checked — and the decisive reproduction prints the exact CI negative

A throwaway two-test file, run and deleted inside the iteration. Three variants, escalating.

**(a) The visible leak** — an uncommitted write held past the test boundary. Reproduced the CI
failure exactly (`OperationalError: database is locked` on `DROP TABLE requirement_drift`, ERROR at
the *next* test's setup, 34.5 s ≈ the whole busy timeout). Both new sections printed with real
content. But this is the case the old registry already solved, so it proves the plumbing, not the
point.

**(b) The silent discard — the case the instrument exists for.** SQLAlchemy discards the connection
(firing `close`, emptying `_CHECKED_OUT`) while `stop()` only sets `_running = False`, which is what
`stop()` *does* when the worker thread is not draining:

```
  [before dispose] registry: no connection checked out
  [before drop_all] registry: no connection checked out
  [at failure] registry: no connection checked out
  ever-seen: 2 connection(s) recorded, 0 already collected, 2 with an OPEN sqlite3 handle
  ever-seen: last checked out by ...::test_a_leaks_a_silently_discarded_connection
  ever-seen:     sqlite3 handle OPEN, IN TRANSACTION, worker thread alive, _running=False
                 <-- stop() was called and the close never ran: THIS ONE LEAKED
```

**The old registry printed the CI negative three times — the identical printout of all five
instrumented occurrences — while the new section named the holder, its test and its transaction
state.** That is the blind spot `d1a` argued for, closed, and demonstrated rather than asserted.

**(c) The pool-log half**, with the terminate raising: captured the real `sqlalchemy.pool` ERROR and
its full traceback through `_close_connection` → `do_terminate` → `_terminate_force_close`, on the
real SQLAlchemy path rather than a synthesised log record. Incidentally pins `_close_connection` to
`pool/base.py:373` for the installed version.

### One of `d1a`'s open caveats answered, one narrowed

`d1a` flagged that its `pool/base.py` line numbers were read against this machine's SQLAlchemy and
that CI's version was never pinned-checked. **`hub/pyproject.toml` pins neither** —
`sqlalchemy[asyncio]>=2.0`, `aiosqlite>=0.20` — so CI resolves its own and the line numbers cannot be
relied on. The instrument was therefore built not to need them: it keys on the logger name
`sqlalchemy.pool` (stable across 2.x) and on aiosqlite attribute reads each wrapped so that any
failure becomes a string in the report rather than an exception replacing the error being diagnosed.

### Verification

- `ruff check src/ hub/ tests/` — clean. `black --check --target-version py311 src/ hub/hub/
  hub/tests/ tests/` — 548 files unchanged. (One `SIM105` was found and fixed on the first pass, not
  argued away: the `try/except/pass` in `emit` became `contextlib.suppress`.)
- `tests/test_flow_fires_a_review_turn.py` — **8 passed**, and the report correctly stayed silent. A
  green run is as quiet as before, which was the design constraint.
- The four files that create their own engines — `test_migrations.py`, `test_project_persistence.py`,
  `test_runner_charter_models.py`, `test_task_workspace_scheme.py` — **109 passed, 1 skipped**.
- The hub suite was **not** run whole: the limits forbid it without cause, and the change is
  diagnostic-only on a path that only executes when the reset already failed.

**One warning checked rather than assumed.** `test_flow_fires_a_review_turn.py` emits
`PytestUnhandledThreadExceptionWarning` / `Event loop is closed` locally — F295's signature. Re-ran
the same file with the diff stashed: **12 matching lines either way.** It pre-dates this change and is
not a regression from the instrument. (Worth noting against `d1a`'s finding that both readable CI
occurrences contain *zero* of these: the warning is present locally and absent in CI, so the two
environments differ on exactly the signal the F295-vs-F292 question turns on.)

### What this is, stated plainly

**An instrument, not a fix.** `d1a` concluded it and this iteration did not find a reason to disagree:
the holder is still unnamed, nothing was reproduced from the real failure, and no product code
changed. What is now true that was not this morning is that the *next* occurrence can distinguish
three cases the previous five could not — a connection SQLAlchemy reported giving up on, one it gave
up on silently, and a holder outside SQLAlchemy's bookkeeping altogether. If both new sections stay
silent at occurrence nine, that is the third case, and it is a result.

The existing dispose mitigation at `conftest.py:464` was **not** strengthened, per `DIRECTION` and per
`d1a`'s measurement that it is aimed at a state never once observed at a failure.

### Ledger

`scripts/drive/FINDINGS.md`, F292: the **Status** line `d1a` deliberately left is now written. The
falsification claim is **narrowed, not retracted** — the registry still rules out a connection the
pool believes is checked out, which is exactly what the dispose mitigation was written for; it does
not rule out one SQLAlchemy silently discarded. The four-negatives count is corrected to five, and a
new closing section records the instrument, the weakref table, the reproduction printouts and the
version-robustness argument. The entry **stays open**.

{
  "purpose": "FIX. Read spec-queue/APPROVALS.md, then build: backlog first (unarchived changes, then open findings by severity), then APPROVED rows. Drive every change before closing its queue item. This window does NOT write new proposals.",
  "playbook": ".claude/loops/night-window.md",
  "runner": "claude",
  "model": "opus",
  "permission_mode": "unattended-full-access",
  "branch": "autonomous/2026-09-03-daily",
  "parent_branch": "master",
  "parent_sha": "2912736",
  "stop_at": "2026-09-04T07:00:00+01:00",
  "iteration": 15,
  "log_file": ".claude/autonomous/2026-09-03-night-log.md",
  "findings_file": "scripts/drive/FINDINGS.md",
  "approvals_file": "spec-queue/APPROVALS.md",
  "current": "a-write-p3",
  "next_action": "openspec/changes/a-write-outside-the-workspace-is-recorded -- item a-write-p3, tasks 3.1, 3.1b, 3.2, 3.3, 3.4, 3.5 and 10.4. Add classify(path, *, workspace_dir, project_root) to hub/hub/workspace_writes.py (which already exists and is pure/stdlib-only -- keep it that way; runner_events.py now imports it, so anything heavier here lands on the hot path of every tool call of every run). Return a KIND AND A NAME (D4). Kinds: agent/task/review from the layout helpers in worktrees.py -- worktree_path :144, task_worktree_path :176, review_path :198, all CONFIRMED 2026-09-04 by iteration 12; RE-MEASURE before citing, six of twelve anchors had drifted. 'hub' for anything else under <root>/.agentweave/; 'project' for the project's own directory; 'outside' for anything else. 3.1b IS NOT COSMETIC: repo_hygiene EXCLUDE_PATTERNS at :59 seeds .agentweave/worktrees|reviews|tasks|logs|evidence|context into info/exclude on EVERY turn, so add a test that WALKS EXCLUDE_PATTERNS and asserts every .agentweave/ pattern classifies as agent/task/review/hub and NEVER project. The classifier still derives the three checkouts from the layout helpers, not from the exclude list -- one source of truth, and that test is what stops the two drifting. 10.4 is its sharp case: .agentweave/evidence/x is 'hub'. 3.2 IS THE TRAP both rounds 1 and 2 fell into. Copy _decide's WHOLE construction including the first line they omitted -- mcp_server.py:938 (NOT :901, which is the def): absolute = candidate if os.path.isabs(candidate) else os.path.join(root, candidate), THEN realpath + commonpath + normcase. JOIN BEFORE RESOLVING: realpath resolves a relative path against the CALLING PROCESS's cwd, which _decide gets away with because it IS the spawned MCP server whose cwd is the run's workspace, whereas this runs in the Hub process serving many projects from wherever uvicorn started. Test a relative ../../x explicitly AND RUN THE TEST FROM A CWD OTHER THAN THE FIXTURE WORKSPACE or it proves nothing. commonpath compares components so /work-other does not read as inside /work. 3.3 inside the run's own workspace -> 'inside', record nothing. 3.4 'unknown', NEVER 'outside', when workspace_dir is absent or realpath raises -- test it explicitly; an unresolvable workspace must not produce a record accusing the run. This is the one place the design deliberately does NOT copy _decide, which refuses (D4). 3.5 Windows cross-drive (commonpath raises ValueError) and case-only difference, both of which _decide already handles and both of which must be handled the same way or the two disagree about the same path. Still NO call site -- an unused pure function is complete work for this item. TOOLING, all three learned the hard way and all three still live: (1) `/tmp` is TWO DIRECTORIES -- Git Bash maps it to C:/Users/huida/AppData/Local/Temp, Python resolves it to C:/tmp, so a bash `cp` backup is invisible to a Python restore and the harness silently no-ops while reporting the CLEAN tree's pass count; use an absolute shared temp path and ALWAYS include an M0 control that applies no mutation. (2) `git checkout -- <file>` cannot revert an UNTRACKED file; revert by cp backup. (3) Python's open(p,'w') on Windows writes CRLF into a repo whose .gitattributes says `* text=auto eol=lf` -- normalise before committing. Run under py -3.11, never bare python; black needs --target-version py311; ruff/black/pytest via py -3.11 -m. The FULL hub/tests/ suite takes over 20 minutes -- start it in the background early in the iteration, not at the end.",
  "queue": [
    {
      "id": "a-blocked-p1",
      "status": "done",
      "title": "a-blocked phase 1 -- reproduce F188 first",
      "detail": "Tasks 1.1-1.3. New file hub/tests/test_a_blocked_agent_workspace_holds_its_input.py: (1.1) one agent, one conversation, one unbound operator entry, trigger_agent_directly patched to raise TriggerAgentError(409, 'Could not prepare isolated worktree for <agent>: ...') with NO flags; schedule DELIVERY_ATTEMPT_LIMIT times; assert withdrawn plus abandoned_reason claiming it failed 3 times. (1.2) the same loop with NO_RUNNER (agent_wide=True) leaves the entry queued at 0 attempts -- the asymmetry IS the finding. (1.3) at the worktrees layer without the Hub: a plain directory at .agentweave/worktrees/<agent> in a temp git repo makes ensure_worktree raise IsolationUnavailableError, and assert the message carries no remedy today. GATE: it must pass unmodified. A reproduction that does not pass first is not a reproduction. DONE iteration 2: 3 passed against unmodified code (git status showed only the untracked test file), and 1.1 mutation-checked -- adding agent_wide=True to BLOCKED_AGENT_WORKSPACE turns it red by name. 1.1 and 1.3 are written to FLIP in phases 3 and 4; 1.2 must keep passing untouched."
    },
    {
      "id": "a-blocked-p2",
      "status": "done",
      "title": "a-blocked phase 2 -- the refusal says which workspace it could not prepare",
      "detail": "Tasks 2.1-2.4 in hub/hub/api/v1/agent_trigger.py. Add agent_workspace_unavailable: bool = False to TriggerAgentError.__init__ with a docstring paragraph in its three neighbours' style (what it means, why it is not agent_wide per design D2, and that it does NOT imply transient, unlike workspace_unavailable). Split the except at :879-883 on worktrees.takes_task_workspace(repo_root, config, turn_workspace.task_id) -- design D1, never on the exception type or its message. The agent arm raises with the flag and names the agent's own workspace; the task arm keeps today's flags exactly and names the TASK. 2.4 asserts the dispatch on flags, not wording. DONE iteration 3: flag added after agent_wide with the three-paragraph docstring; the except now dispatches on takes_task_workspace (task arm -> 'Could not prepare the checkout for task <id>', no flags; agent arm -> \"Could not prepare <agent>'s own workspace\", agent_workspace_unavailable=True only). New file hub/tests/test_a_blocked_workspace_refusal_names_its_scope.py, 2 passed, both stubbing ONE shared IsolationUnavailableError so a dispatch that read the exception would give identical flags. Mutation-checked twice: inverting the predicate fails both by name; deleting the flag fails only the agent test. 12-file chunk 198 passed in 3:31; ruff and black clean. turn_scheduler.py untouched."
    },
    {
      "id": "a-blocked-p3a",
      "status": "done",
      "title": "a-blocked phase 3a -- the shared predicate and the scheduler helper",
      "detail": "Tasks 3.0, 3.0a, 3.1, 3.2. Extract takes_own_checkout(task) in hub/hub/task_workspace.py and refactor resolve_turn_workspace_inputs onto it (design D8), keeping the logger.warning on the invalid-id branch. SPELL THE SCHEME CHECK WITH != AND AN EARLY return False -- test_task_workspace_scheme.py substring-scans for '.workspace_scheme =' and 'workspace_scheme=' over lowercased source, and the == spelling contains the first; measured red by R3. Do not relax the scan. 3.0a: re-run that file immediately, confirm 10 passed with migration 0095 the only match, and add the one-line docstring note saying why a READ here is constrained to the negative form. 3.1: the turn_scheduler helper answering whether other queued input for this agent would have run in a different workspace -- two queries in this order (the remaining entries' conversations first, then one over the union of the task ids the entries name and the ids those conversations carry), never one query per entry, and entries in the controlling conversation need no inheritance lookup. 3.2: write the argument and R1's and R3's corrections into its docstring, including which direction each approximation errs in -- a false yes destroys the operator's message, a false no only holds it. DONE iteration 4. takes_own_checkout(Optional[Task]) is in task_workspace.py spelled with != and an early return False; measured by hand that the == and ==-no-space spellings are both offenders and != is not, and test_task_workspace_scheme.py is 10 passed with 0095 the only file the scan matches. The logger.warning stayed in the resolver as _warn_when_the_id_is_why(task), which returns early for a grandfathered task so the combined branch's behaviour is preserved exactly rather than widened; _id_complaint holds the one try/except both callers need. 3.0a's docstring note is in. The scheduler helper is PUBLIC -- other_input_would_have_run_elsewhere -- because 3c tests shapes a drive cannot produce and must call it directly. Two queries in the forced order, eligibility expressed as WHERE; _tasks_this_entry_is_about holds the or and reuses _entry_kind rather than run_task_binding.task_named_by, whose precedence runs the other way. Verified by a throwaway probe (13 shapes, 13 passed) that was mutation-checked twice and then DELETED -- its coverage is written into a-blocked-p3c."
    },
    {
      "id": "a-blocked-p3b",
      "status": "done",
      "title": "a-blocked phase 3b -- wire the condition, plus the first four scheduler tests",
      "detail": "Tasks 3.3-3.6. Extend the condition at turn_scheduler.py:204 so an agent_workspace_unavailable refusal skips the counter unless 3.1's helper says something else could have run. The helper LANDED IN ITERATION 4 and is measured: `await other_input_would_have_run_elsewhere(db, project_id=project_id, agent=agent, entries=entries, selected=selected, controlling_conversation_id=conversation.id, hop_budget=hop_budget)`, public, defined above schedule_agent. Ask it LAST -- it is the only term in the condition that costs queries. keep the existing transient and agent_wide terms untouched and readable, and do not collapse the three questions into one boolean. 3.4: the three scenarios from agent-conversation-workspace -- held when every other entry is unbound, counted-and-withdrawn when a task-bound entry waits in another conversation, counted when the refusal is the task-checkout one; the middle test's docstring says it is what keeps this change from breaching 'where other queued input could have run'. 3.5: the inherited binding -- the other conversation's entry names no task but its conversation does, and it must count. 3.6: an entry inside selected naming a task that no longer resolves must not count. ALSO IN 3b (found in iteration 3, and 1.1 cannot flip without it): phase 1's stub BLOCKED_AGENT_WORKSPACE in hub/tests/test_a_blocked_agent_workspace_holds_its_input.py:41-49 still carries the pre-phase-2 sentence and NO flags, and its #: comment claims that is what agent_trigger.py:879-883 raises 'today'. Repoint it at the real agent-arm refusal -- \"Could not prepare <agent>'s own workspace: ...\" with agent_workspace_unavailable=True -- and rewrite the comment, then flip 1.1's assertion from withdrawn to held and its detail assertion with it. Test 1.2 (NO_RUNNER) must stay untouched and green. DONE iteration 5. The condition is three terms, the new one written `not getattr(exc, \"agent_workspace_unavailable\", False) or await other_input_would_have_run_elsewhere(...)` so the `or` short-circuits on the attribute read and the two queries are paid for only by a flagged, non-transient, non-agent-wide refusal -- the ordering constraint enforced by the language rather than by a comment. Phase 1's stub repointed at the real agent-arm refusal (the old flagless sentence is no longer raised anywhere, so the flip would have been a flip of nothing), 1.1 renamed and flipped to (\"queued\", 0) with abandoned_reason None plus a waiting_reason assertion, 1.2 byte-identical. New file hub/tests/test_a_blocked_workspace_counts_where_input_could_run.py, 5 tests, all through schedule_agent. Mutation-checked three ways, each naming tests: deleting the term fails 3.4a/3.6/1.1; `return False` in the helper fails 3.4b/3.5 only; replacing the flag read with True fails 3.4c only. Chunk of thirteen -> 162 passed in 1:46; ruff and black clean."
    },
    {
      "id": "a-blocked-p3c",
      "status": "done",
      "title": "a-blocked phase 3c -- the grandfathered test and its siblings",
      "detail": "Tasks 3.7, 3.8, 3.8a, 3.8b, 3.9 -- the tests round 3 exists for. 3.7: another conversation's entry naming a task whose workspace_scheme is 'agent' must NOT count; set the column directly in the fixture (no runtime path writes it) and say in the docstring that a scope test built on entry.task_id alone passes every other test in the file and fails this one. 3.8: the same for a task id validate_task_id refuses, a deleted task row, and a task in TERMINAL_FOR_BINDING -- BIND THE OTHER CONVERSATION TO NOTHING in the last two fixtures and say in the docstring that this is load-bearing rather than tidy. 3.8a: the inverse -- an entry naming a decided task on a conversation bound to a live task-scheme task MUST count. 3.8b: a review entry whose task has no evidence naming a commit must not count (design D3b); the sibling whose task does name one must. 3.9: over the hop budget, and a closed conversation -- neither counts. THE DELETED PROBE'S COVERAGE, which 3c is now the only place these shapes are asserted (iteration 4 measured all thirteen against the live helper and then removed the file): no for every-other-entry-unbound, for a grandfathered task, for an approved task, for the id '../escape', for a task id with no row, for an archived other conversation, for an over-budget entry, for an entry inside selected, for a second entry in the CONTROLLING conversation when that conversation itself carries a live task (the inheritance lookup must not run there), and for a review entry whose task has no evidence naming a commit; yes for another conversation's entry naming a live task-scheme task, and yes for an entry naming nothing whose conversation carries one. Re-establish each as a committed test rather than trusting this line. DONE iteration 6. Eleven tests appended to hub/tests/test_a_blocked_workspace_counts_where_input_could_run.py under a section banner explaining the file's two halves; _seed widened with one optional trailing element per tuple kind (conversation lifecycle, task workspace_scheme, a dict of further new_entry keywords) so all five scheduler-level tests read byte-identically. Shapes covered: the positive control, 3.7 grandfathered, 3.8's three (unmintable id, deleted row, decided task -- both of the last two with the other conversation bound to nothing), 3.8a decided-on-a-live-thread, 3.8b's review pair (review_evidence.py supplies the commit), 3.9's two, and the controlling-conversation inheritance shape the gate mutation needs. DEVIATION, recorded: 3.7's docstring does NOT say 'a scope test on entry.task_id alone passes every other test and fails this one' -- that claim is 3.5's already and the two collide; it says instead that 3.5 and 3.7 pin the rule from opposite sides and neither holds it alone. Eight mutations, each naming a test, all eleven pinned: the scheme line (3.7 + test_turn_workspace's own grandfathering test), `if True` for the controlling guard (that test alone), `return False` in the helper (the three positives plus 3.4b/3.5), review-without-commit_for_task_review, decided_task_refusal, the lifecycle WHERE, the hop filter, _id_complaint, and a missing row counting. ruff clean, black 540 unchanged, thirteen-file chunk 173 passed in 1:22. No product file changed."
    },
    {
      "id": "a-blocked-p4",
      "status": "done",
      "title": "a-blocked phase 4 -- the refusal says what would clear it",
      "detail": "Tasks 4.1-4.3 in hub/hub/worktrees.py. Give ensure_worktree's two refusal branches (a symlink; a path that is not the registered worktree for the expected ref) their own remedies, following _merge_prerequisites' precedent that this module writes the operator-facing sentence -- name the directory to remove and the prune that follows it, and do NOT write one sentence covering both. 4.2 does the same for the mid-merge refusal reached from the task arm, so the requirement's third scenario is satisfied by real branches rather than by two wordings of one. 4.3 asserts each remedy at the worktrees layer, per branch, against the obstruction it was written for -- never by substring-matching one shared phrase. DONE iteration 7. ensure_worktree had ONE branch, not two -- the `or` was split into a symlink arm (remove the link, `rm <path>`, target untouched) and a wrong-ref arm (`rm -r <path>`; the next turn prunes and provisions). The SAME split was made in ensure_task_worktree, both arms naming the task: in scope because scenario 2 is about that arm and because 4.2's contrast needs a second differently-remedied obstruction at the same path. ensure_review_checkout was DELIBERATELY NOT touched (recorded in tasks.md): out of the requirement's scope and a different guard shape. 4.2 was a sharpening -- the mid-merge branch already had a remedy; it now names `git -C <path> merge --abort` and says DO NOT remove the directory, the opposite of its sibling. Test 1.3 flipped and renamed to test_a_plain_directory_in_the_way_says_what_to_remove. New file hub/tests/test_a_blocked_workspace_refusal_states_its_remedy.py, 5 tests, each of which PERFORMS its remedy and asserts provisioning then succeeds; the agent wrong-ref test blocks with a worktree registered for ANOTHER ref so the promised prune is really exercised (git lists it `prunable` after rm -r, asserted). Four mutations, each naming a test. LOCAL GAP: the two symlink tests SKIP here (WinError 1314, no symlink privilege) and run only in CI's Linux job; a throwaway probe forcing Path.is_symlink confirmed both branches format their own remedy. ruff clean, black 541 unchanged, fifteen-file chunk 200 passed 2 skipped in 1:34."
    },
    {
      "id": "a-blocked-p5",
      "status": "done",
      "title": "a-blocked phase 5 -- reconcile what already exists",
      "detail": "Tasks 5.1-5.4. 5.1: hub/tests/test_a_delivery_attempt_means_a_delivery.py's BAD_CHECKOUT is F188's exact sentence used there as the ENTRY-SPECIFIC example -- repoint it at a real task-checkout refusal; its assertion (still counts, still gives up) is correct and must not change. 5.2: grep for other readers of the old sentence (inbound_queue.py's status derivation, any UI string, any test) and RECORD WHAT THE GREP FOUND IN THE CHANGE, including 'nothing'. 5.3: update TriggerAgentError's agent_wide docstring at agent_trigger.py:304-318, which cites :756 as the entry-specific worktree example. 5.4: rewrite turn_scheduler.py:225-233 -- a shipped comment INSIDE the branch being edited that asserts the exact claim this change falsifies, and which 5.2's grep can never reach because it quotes no sentence. Repoint its D3a citation to 2026-08-28-a-delivery-attempt-means-a-delivery; this change's own predicate decision is D8 precisely so the two do not collide in this file. 5.2 PARTLY MEASURED in iteration 3, and it found a file task 5.1 does not name: grep for 'Could not prepare isolated worktree' now hits NO product line and three test files, all hand-constructing a TriggerAgentError rather than asserting the real raise site -- test_a_delivery_attempt_means_a_delivery.py:37 (5.1's BAD_CHECKOUT), test_a_blocked_agent_workspace_holds_its_input.py:46,122 (phase 1's stub, handled in 3b), and test_checkpoint_cutover.py:1069,1072 (waiting_reason + detail, NOT in 5.1's description -- decide whether it should read a real refusal, and record the decision). No UI string reads it: that is the 'nothing' 5.2 asks to be recorded. Re-run the grep at phase 5 rather than trusting this line, and record inbound_queue.py's status derivation separately -- it was not checked here. DONE iteration 8, all four tasks, and every product line changed is a COMMENT (checked mechanically: the diff over hub/hub/ has no non-comment +/- line). 5.2's grep re-run after phase 4: NO product line, NO UI string, four hits in three test files -- recorded as a table in tasks.md so no later round re-runs it. inbound_queue.py checked separately and it never matches on the text at all: queue_status passes entry.waiting_reason through verbatim, which is WHY phase 4's remedy will reach leg 6.2's screen. 5.1: BAD_CHECKOUT repointed at the task arm (ensure_task_worktree's wrong-ref refusal wrapped by agent_trigger), truncated after the diagnosis, assertion untouched; mutation-checked (adding agent_workspace_unavailable=True fails test_an_entry_specific_refusal_still_counts_and_still_gives_up by name). test_checkpoint_cutover.py:1069,1072 DECIDED: repointed too, and measured insensitive -- a garbage sentence leaves it at 39 passed, so the branch decides on entry_ids as claimed. The three truncated stubs were annotated rather than extended (they now say they stop after the diagnosis and point at test_a_blocked_workspace_refusal_states_its_remedy.py). 5.3 FOUND A DOC/CODE DISAGREEMENT and filed it as F276 (C) under a new `# Filed outside a drive` heading at the end of scripts/drive/FINDINGS.md: the agent_wide paragraph cited `:479` (no such agent) as request-level AND agent-wide, but that raise passes request_level only and never passed agent_wide, and NO site in the Hub sets both flags. The doc half is fixed here; the behaviour half (should input for an agent that does not exist be held rather than withdrawn at the third attempt?) is open and unproposed in F276. 5.4 rewritten as two bullets, one per arm, with D3a/D8/D1 pulled apart. GATE: sixteen files (the fifteen plus test_checkpoint_cutover.py) -> 239 passed, 2 skipped, 1:49; ruff clean; black 541 unchanged."
    },
    {
      "id": "a-blocked-p6-gates",
      "status": "done",
      "title": "a-blocked task 6.1 -- the full gates",
      "detail": "pytest hub/tests/ under py -3.11 (15-25 minutes, load-dependent -- it exceeds the 600s command cap, so background it or run it in chunks), pytest tests/ separately for the CLI (collecting both together fails), ruff check src/ hub/ tests/, black --check --target-version py311 src/ hub/hub/ hub/tests/ tests/, mypy src/. Mutation-check at least the phase 3 condition: delete the new term and watch a NAMED test fail; if nothing fails the test asserted over nothing. SIZING NOTE measured 2026-09-03 23:04: the nine-file chunk relevant to this change ran in 3:14 (242 passed, 1 xpassed), so the whole-suite figure is not a guide to it. CHUNK NAMES, measured iteration 4 (iteration 3's 'nine-file' note named two files that do not exist; pytest exits with a usage error on a missing path and prints '5 warnings in 0.01s', which reads like a pass). The twelve real neighbours are test_turn_workspace, test_task_workspace_scheme, test_a_blocked_agent_workspace_holds_its_input, test_a_blocked_workspace_refusal_names_its_scope, test_a_delivery_attempt_means_a_delivery, test_turn_scheduler, test_inbound_queue, test_delivery_attempts, test_task_resolved_before_workspace, test_task_worktrees, test_worktrees, test_no_refusal_queues_input -- 157 passed in 2:41. CHUNK UPDATED iteration 7: fifteen files now -- the twelve named above plus test_a_blocked_workspace_counts_where_input_could_run, test_a_blocked_workspace_refusal_states_its_remedy (new in phase 4) and test_review_checkout (added because ensure_review_checkout is the branch phase 4 deliberately left alone). 200 passed, 2 skipped, 1:34. The 2 skips are the symlink tests and are EXPECTED on this machine -- do not chase them. CHUNK IS SIXTEEN FILES as of iteration 8: the fifteen plus test_checkpoint_cutover.py, pulled in by 5.1's decision -> 239 passed, 2 skipped, 1:49. RUN THE GATES FROM THE REPO ROOT: from hub/, ruff prints `E902 The system cannot find the file specified ... src:1:1` and black prints `Path 'src/' does not exist` -- both are wrong-cwd errors wearing a disguise. mypy src/ has never been run in this change; it is the only gate here without a prior measurement. DONE iteration 9, all five gates, two of them first measurements in this change. WHOLE HUB SUITE: 3874 passed, 86 skipped, 1 xpassed, exit 0, 24:27 -- so the 600s cap really does require backgrounding it, and the sixteen-file chunk's 1:49 understates it by a factor of thirteen. CLI from the repo root: 440 passed, 3 skipped, 23s. ruff clean. black 541 unchanged. mypy src/: no issues, 22 source files (never run before in this change; running it early cost nothing and it was green). No failure anywhere, so the 'check unrelated failures against master' contingency never fired; the 86 skips and the 1 xpass are the suite's standing baseline and the 2 symlink skips are inside them. MUTATION re-done against the current tree: deleting the third condition term (12-line deletion, no whitespace churn) fails exactly three tests by name -- 3.4a test_a_second_unbound_conversation_does_not_make_the_head_expendable, 3.6 test_an_entry_in_the_refused_batch_naming_a_vanished_task_does_not_count, and 1.1 test_a_blocked_agent_workspace_holds_the_operators_message, the last failing on ('withdrawn', 3) != ('queued', 0), which is F188's exact symptom returning. The other 16 of 19 kept passing because they are pinned by the helper rather than by the condition, and phase 3c mutation-checked those separately. Restored byte-for-byte, git diff empty, 19 passed after."
    },
    {
      "id": "a-blocked-p6-drive",
      "status": "done",
      "title": "a-blocked tasks 6.2-6.4 -- three drive legs against the product",
      "detail": "DONE iteration 10, three legs, 33/33, two real Haiku turns. DEVIATION, deliberate: the Hub ran on profiles/drive8011/ablocked-p6.db (fresh, migrated 0000->0100 at startup), NOT the beta profile this item named -- beta is what the live :8010 instance serves (23 projects, confirmed tonight), and a second writer would have put 8010's scheduler in reach of the drive's project. The stale 8011 process was from Sep 3 09:06, sixteen hours older than the code under test, and answered /health exactly like a current one; killed and restarted from hub/, from source. Fixture proj-4eb8215f9502 at C:/Users/huida/Documents/aw-ablocked-p6. 6.2 (8/8): four delivery opportunities against a limit of three -- POST /agent/trigger schedules too -- and delivery_attempts never left 0; the remedy reaches THREE surfaces including the Continue button's own 200 body, which the task did not ask for and is the one an operator actually clicks. 6.3 (14/14), the falsifying leg: an ordinary task (workspace_scheme='task', asserted) bound in a second conversation moved the head 0->1 on that same schedule, 3 withdrew it, and the next schedule ran the task turn to completion in .agentweave/tasks/task-e3df8b4ddca8. Note the scheduler RETURNS after withdrawing the head rather than re-draining, so the task turn needs one more schedule -- invisible from the unit tests. 6.4 (11/11): performed the remedy, one Continue, the held message was delivered into the path that had been obstructed and git registered it on agentweave/<agent>. F274 checked in a browser against the served bundle: the conversation view renders the whole refusal, unelided. Both turns billed claude-haiku-4-5-20251001 per turn_usage. No job or loop created; 8011 stopped. Harnesses kept: scripts/drive/setup_ablocked_p6.py, t_ablocked_p6_held.py, t_ablocked_p6_elsewhere.py, t_ablocked_p6_cleared.py. Evidence is written into tasks.md under each of 6.2/6.3/6.4."
    },
    {
      "id": "a-blocked-archive",
      "status": "done",
      "title": "a-blocked task 6.5 -- retire F188 in the ledger, then archive the change",
      "detail": "DONE iteration 11, one commit, four documents plus the archive move; no product code, so the gates were not re-run (iteration 9 measured all five green, iteration 10 drove it). F188's section is headed RETIRED 2026-09-04 and its Status line reads **fixed 467dfea**, stating in prose that the commit writing the line is later than the repair and cannot cite its own sha without a forbidden amend. Under it: the three legs at 33/33, each written against the reproduction's own symptom (withdrawn at 3 with 'delivery failed 3 times' -> queued at 0 through four delivery opportunities), the three surfaces the remedy reaches, the falsifying leg, and the leg where performing the remedy released the message. The (C) sub-finding at the end of that section is retired with it by phase 4, with its test named. INDEX RECOMPUTED rather than decremented: all 40 severity-A headings scanned for their Status line -> the 2026-09-03 rebuild minus F188 (F271 and F274 open, F140/F142/F154/F155 still unverified), with the two limits of the scan written down (it reads the heading and first Status-bearing line, not the section; 'open, filed not fixed' contains 'fixed', so F140/F142 were classified by hand). The stale table row and the 'three -- F271, F188 and now F274' paragraph are marked SUPERSEDED, not rewritten. openspec validate --strict on the change: valid. Deltas folded in by hand -- ADDED appended to workspace-isolation, MODIFIED replacing its predecessor in agent-conversation-workspace -- and git diff reported 122 insertions, 0 deletions across both, which is the check that the merge lost nothing. openspec validate --specs --strict: 43 passed, 0 failed. git mv to openspec/changes/archive/2026-09-04-a-blocked-agent-workspace-holds-its-input; openspec list now shows only a-write. A status note was appended to spec-queue/APPROVALS.md, explicitly labelled as not an operator decision, saying the gate on a-write is open; the operator's APPROVED lines were not touched."
    },
    {
      "id": "a-write-split",
      "status": "done",
      "title": "a-write-outside-the-workspace-is-recorded -- split its 54 tasks before building any of them",
      "detail": "DONE iteration 12. Split written; no product code touched. The 54 tasks across 10 phases became the 12 items below. While reading for the split, the source anchors the tasks cite were re-measured against the tree and SIX HAVE DRIFTED -- every item below carries the measured number, and a firing should trust the item over tasks.md: AgentTimeline.tsx WRITING_TOOLS is :615 (tasks say 573); mcp_server _PATH_KEYS is :895 and _decide is :901 with the join-before-resolve line at :938 (tasks say 858/901); runner_commands restrict_spec_writes' disallow list is :203 (tasks say 210); agent_trigger _flush_line is :1932 and _on_event is :2542 (tasks say 1880/2474); _execute_run starts :1772 and _execute_codex_appserver_run :2457 (tasks say 1720/2389). CONFIRMED UNDRIFTED: RunEvent runner_events.py:111-115, tool_use_event :134 with redact/stringify at :142-143 and the sole kind='tool_use' construction at :154; runner_parsing Claude tool_use :266 and Codex exec file_change :486-499; codex_appserver fileChange :448-459 (tool_use_event at :452); requirement_evidence recorded_workspace_dir :343, _apply_footprint :362 with call sites :423 and :921, restamp_run_footprints :845, footprint_root :299; worktrees worktree_path :144, task_worktree_path :176, review_path :198; repo_hygiene EXCLUDE_PATTERNS :59. Migration head is still 0100_loop_work_needs_evidence.py and both head assertions read '0100' (test_migrations.py:40, test_project_persistence.py:227), so task 4.2's 0101 stands. Also measured: the MODIFIED delta in specs/agent-run-sandboxing/spec.md ALREADY reads in round 3's shrunk form ('as refusals' plus one sentence of scope, policy paragraph gone), so task 10.1 is a verify-and-tick, not an edit. And APPROVALS.md line 29 does carry 'APPROVED a-write-outside-the-workspace-is-recorded' with ORDER putting it second -- the 'no row / undecided' paragraphs further down that file are the 2026-09-01 and 2026-08-30 sections, which the file's own header says are history and are not read."
    },
    {
      "id": "a-write-p1",
      "status": "done",
      "title": "a-write phase 1 -- reproduce F115 first, and let task 1.4 stop the change",
      "detail": "Tasks 1.1-1.4. New file hub/tests/test_a_write_outside_the_workspace_is_recorded.py. GATE, copied from a-blocked phase 1 and it is the whole point of this item: every test here must PASS against unmodified code. A reproduction that does not pass first is not a reproduction. (1.1) Parse side: feed parse_claude_line a real assistant line carrying a tool_use block for Write with an absolute file_path outside the workspace; assert the parsed events carry the path ONLY inside payload['input'] as a stringified blob and nothing says it was outside anything. The branch is runner_parsing.py:266 and it calls tool_use_event (runner_events.py:134), which redacts and stringifies at :142-143 -- that stringification is why 1.1 can assert 'blob only'. (1.2) Record side: a Run with workspace_dir set to an agent worktree; after a turn containing that call the run carries no record of an outside write and no activity event mentions one. (1.3) Cross-worktree shape: same call, absolute path inside a SECOND agent's worktree under the same project; assert nothing distinguishes it from 1.2. This is the case whose whole meaning is the destination -- proposal.md's 'launders work through the wrong identity' paragraph. (1.4) THE STOP CONDITION, and design.md's 'For implementation' section says three rounds have read this code and none has executed it: assert mcp_server._decide REFUSES the same absolute path under the default posture. _decide is at mcp_server.py:901 (NOT :864 as the proposal says); _PATH_KEYS is :895; the join-before-resolve line the proposal calls load-bearing is :938. IF 1.4 DOES NOT REFUSE: stop, build nothing, write the measurement into the log and into proposal.md, and set next_action to a re-proposal item -- the proposal's entire round-1 correction rests on it and rounds 2 and 3 both inherited it unexecuted. Closes when: the four tests pass on an unmodified tree, tasks 1.1-1.4 ticked, committed. Do not start phase 2 in the same firing -- 1.4's result decides whether phase 2 exists. DONE iteration 13. Four tests in hub/tests/test_a_write_outside_the_workspace_is_recorded.py, 4 passed against unmodified code (git status showed only the untracked test file). **1.4 PASSES -- _decide refuses, the premise holds, phase 2 exists.** Measured: DEFAULT_CLAUDE_PERMISSION_MODE is WORKSPACE_PERMISSION_MODE, and _decide refuses both a path outside the project and one inside a second agent's checkout under the shared .agentweave/worktrees/ parent, with a control inside the run's own workspace so 'refuses everything' cannot pass for 'refuses this'. Correction written into design.md: no ROUND had executed it, but the SUITE had -- test_permission_approver.py::test_a_path_outside_the_workspace_is_denied_with_the_path_named has asserted the refusal since the approver shipped; what it lacks, and 1.4 adds, is the link from the default posture to `workspace` and the sibling-checkout case. Three mutation checks, each landing on exactly one test: adding Run.outside_workspace_writes reddens only 1.2 and 1.3 (phase 4's flip rehearsed); adding write_paths to RunEvent reddens only 1.1 (phase 2's flip); dropping file_path from _PATH_KEYS reddens only 1.4. TRAP FOR PHASE 3, met in this file's own first draft: a Windows path is stored at TWO escaping depths in one row (payload['input'] is itself JSON), so `str(path) in json.dumps(payload)` is FALSE for a path plainly present -- the file's flatten_slashes helper collapses every backslash run to '/' on both sides of every comparison. Gates: ruff clean over src/ hub/ tests/ (one SIM113 fixed), black clean over CI's four paths (542 files), 12-file chunk 291 passed 4 skipped in 2:01. No product code changed."
    },
    {
      "id": "a-write-p2a",
      "status": "done",
      "title": "a-write phase 2a -- the pure module and which tools write",
      "detail": "Tasks 2.1, 2.2, 2.2b, 2.2c, 2.3. New hub/hub/workspace_writes.py: pure, stdlib-only, no session, no filesystem writes. written_paths(tool, input_data) -> tuple[str, ...] returns the declared path argument(s) of a file-WRITING tool call and empty for everything else. Claude: Write, Edit, MultiEdit, NotebookEdit. Codex: apply_patch. Reads (Read, Glob, Grep, LS) empty; an unknown tool empty rather than guessed from key names. Return on the tool NAME BEFORE looking at input -- that is what the function is, not a cost optimisation (D13: tool_use_event already runs redact_secrets + json.dumps(sort_keys=True) over every input unconditionally at runner_events.py:142-143, so a membership test is not measurable beside it). 2.3 multi-path: NotebookEdit names one under notebook_path; Write/Edit/MultiEdit one under file_path (MultiEdit's several edits all target that one file, so one element); apply_patch several under changes[].path. The tuple return is load-bearing for the Codex side only. 2.2b is round 3's correction and the anchor HAS DRIFTED -- assert written_paths' writer set equals WRITING_TOOLS at hub/ui/src/components/agents/AgentTimeline.tsx:615 (tasks.md says 573), which is {Edit, MultiEdit, Write, NotebookEdit, apply_patch}, both providers, already driving the 'wrote to N files' summary an operator reads (the .has() call is at :522). DO NOT assert against runner_commands.py restrict_spec_writes -- its disallow list is at :203 (tasks.md says 210), it is an F4/D6 permissions flag, Claude-only by construction, so round 2's proposed assertion is false for apply_patch. Separately assert the CLAUDE path keys are a subset of mcp_server.py:895's _PATH_KEYS = ('file_path','path','notebook_path') -- MultiEdit passes, Codex's nested changes[].path deliberately is not there. mcp_server.py may import only stdlib + fastmcp, so this is restate-and-assert, the shape test_permission_approver.py uses for OPERATOR_POSTURE. 2.2c is FILE, DO NOT FIX: restrict_spec_writes omits MultiEdit while the UI counts it as a write, so a spec-restricted agent may write through it -- write it into scripts/drive/FINDINGS.md as a new finding, out of scope here, and this change must not inherit the gap by treating that flag as the definition of a write. Closes when: the module exists with unit tests including the two cross-source assertions and the new finding is filed. No production call site yet -- an unused pure module is complete work. DONE iteration 14. hub/hub/workspace_writes.py (written_paths, CLAUDE_WRITE_TOOLS, CODEX_WRITE_TOOL, WRITE_TOOLS) plus hub/tests/test_workspace_writes.py, 12 passed; no call site, as planned. The writer-set assertion regex-parses the real AgentTimeline.tsx rather than restating it, and mutating the .tsx (adding a tool, renaming the declaration) turns it red -- proven. _PATH_KEYS subset asserted by importing mcp_server directly. 2.2c filed as F277 (C) with BOTH postures measured by building the command: yolo has no --allowedTools at all, and the default posture's approver checks paths not tool names, so MultiEdit inside the workspace is allowed. NINE mutation checks, each landing on one test. TWO TOOLING TRAPS worth carrying: `git checkout -- <file>` CANNOT revert an untracked file, so the first mutation round accumulated all five mutations -- on this branch every phase-2 file is untracked until its first commit, so revert by cp backup; and <<'EOF' heredocs in this environment collapse \\n to a real newline, which silently breaks patch scripts matching source that contains escape sequences (use line-index edits and chr(10)). Four stale anchors corrected in tasks.md with the old numbers recorded inline. Gates: ruff clean, black 544 unchanged, mypy 22 files clean, 8-file chunk 162 passed 1 skipped. NOTE: test_runner_commands.py DOES NOT EXIST -- pytest exits with a usage error and '0.00s' on a missing path, which reads like a pass; the real files are test_runner_command_env.py and test_runner_command_overrides.py."
    },
    {
      "id": "a-write-p2b",
      "status": "done",
      "title": "a-write phase 2b -- carry write_paths on RunEvent, proven on all three transports",
      "detail": "Tasks 2.4, 2.5, 2.5b, 2.5c, 2.6. This is what round 2 caught round 1 getting wrong. 2.4: add write_paths to RunEvent (runner_events.py:111-115, anchor CONFIRMED), NOT to ParsedLine -- the Codex app-server path never reaches _flush_line at all (D2). Carry tool name, call id and the raw path string, nothing else, defaulting to (). Populate inside tool_use_event (runner_events.py:134) from the input_data it is handed BEFORE :142-143 redacts, stringifies and truncates at 8 KiB, because the structured path may not survive that. One population site serves all three transports and the field is never persisted -- record_agent_output stores kind and payload only. 2.5: a test PER TRANSPORT proving none of them needed a change of its own. Claude runner_parsing.py:266; Codex exec file_change runner_parsing.py:486-499 (snake_case, the transport round 1 named nowhere, input_data={'changes': item.get('changes', [])}); app-server fileChange codex_appserver.py:448-459 (camelCase, tool_use_event at :452). All three anchors CONFIRMED against the tree. 2.5b: F107's element shape {'path':..., 'diff':...}, corroborated by approval_subject and hub/tests/test_permission_approver.py:588-604 which was written against a live item rather than off _file_change_summary. Copy its malformed cases VERBATIM: None, {}, {'changes':'not-a-list'}, {'changes':[{'path':1},{},None]} -- all extract nothing, none raise. 2.5c: regression test that kind='tool_use' is constructed in exactly one place in hub/hub -- runner_events.py:154, CONFIRMED sole site -- so a future second constructor fails here. State the one boundary in the docstring: POST .../output accepts a tool_use kind from an agent the Hub did not spawn, which has no RunEvent and no workspace to be checked against. 2.6: assert the parser stays pure -- no workspace argument, no filesystem access, no import of anything touching the database; importing runner_parsing in isolation is enough. Closes when: the field arrives on all three transports under test and phase 2 is fully ticked. DONE iteration 15. `write_paths: Tuple[str, ...] = ()` on RunEvent, populated in tool_use_event from the structured input BEFORE redact/stringify/truncate; runner_parsing.py and codex_appserver.py untouched, visible in git status, which is the phase's whole claim. New hub/tests/test_write_paths_on_run_events.py, 13 tests. THE PROPOSAL'S ARGUMENT WAS TOO WEAK AND MEASURING IT CHANGED IT: the blob is not a lesser copy this field improves on -- redact_secrets EATS a deep POSIX path (`/workspace/project/src/services/handler.py` -> `<redacted>.py`, filed F278 (C), F31's remainder, Windows unaffected because `\\` is not in the class), and sort_keys puts `content` before `file_path` so a 16 KiB Write body truncates the destination away entirely. Both measured. Task 1.1 WAS flipped here after checking (not assuming) that 4.7 does not cover it -- 4.7 flips 1.2/1.3, the record side; the scope note is now written into 4.7 in tasks.md. Seven mutations: M1 read-after- transform, M3 second constructor, M4 first-path-only, M5 paths-in-payload, M6 Claude branch stringifies (round 1's shape), M7 app-server renames its input key -- each landed on its own test; M2 was behaviourally inert (both Codex result branches pass a summary STRING, not the changes dict) and M2b, which mutates the result builder AND the exec branch feeding it, is the one that lands. 2.5c asserts the construction site by FUNCTION NAME parsed with ast, not by line number -- the line-number form was red on its first run because the same commit added lines above it. Gates: ruff clean, black 545 unchanged, mypy 22 clean, 9-file chunk 175 passed 1 skipped."
    },
    {
      "id": "a-write-p3",
      "status": "open",
      "title": "a-write phase 3 -- classify a path, and the two traps rounds 1 and 2 both fell into",
      "detail": "Tasks 3.1, 3.1b, 3.2, 3.3, 3.4, 3.5 and 10.4. In workspace_writes.py add classify(path, *, workspace_dir, project_root) returning a KIND AND NAME (D4, and workspace-isolation already requires every reported workspace to say which namespace it belongs to). Kinds: agent/task/review from the layout helpers -- worktrees.py worktree_path :144, task_worktree_path :176, review_path :198, all CONFIRMED; 'hub' for anything else under <root>/.agentweave/; 'project' for the project's own directory; 'outside' for anything else. 3.1b is round 3's correction and it is not cosmetic. The Hub seeds .agentweave/worktrees|reviews|tasks|logs|evidence|context into info/exclude on EVERY turn -- repo_hygiene.py EXCLUDE_PATTERNS at :59, called first in resolve_agent_workspace -- so that subtree is the one part of the project root git has been told to hide, while the requirement described 'project' as the destination that 'sits there visibly'. Add a test that WALKS EXCLUDE_PATTERNS and asserts every .agentweave/ pattern classifies as agent, task, review or hub, never project. The classifier still derives the three checkouts from the layout helpers, not from the exclude list -- one source of truth -- and that test is what keeps the two from drifting. 10.4 is its sharp case: .agentweave/evidence/x, the Hub's own record-keeping about the run doing the writing, must be 'hub'. 3.2 IS THE TRAP. Copy _decide's whole construction including the first line rounds 1 and 2 both omitted -- mcp_server.py:938 (NOT :901, which is the def), absolute = candidate if os.path.isabs(candidate) else os.path.join(root, candidate) -- then realpath + commonpath + normcase. JOIN BEFORE RESOLVING. realpath resolves a relative path against the CALLING PROCESS's cwd; _decide gets away with it because it IS the spawned MCP server whose cwd is the run's workspace, whereas this runs in the Hub process, which serves many projects from wherever uvicorn was started. Test a relative ../../x explicitly AND RUN THE TEST FROM A CWD OTHER THAN THE FIXTURE WORKSPACE, or it proves nothing. commonpath compares components so /work-other does not read as inside /work. 3.3 inside the run's own workspace -> 'inside', record nothing. 3.4 'unknown', NEVER 'outside', when workspace_dir is absent or realpath raises -- test it explicitly; an unresolvable workspace must not produce a record accusing the run. This is the one place the design deliberately does not copy _decide, which refuses (D4). 3.5 Windows cross-drive (commonpath raises ValueError) and case-only difference, both of which _decide already handles and both of which must be handled the same way or the two disagree about the same path. Closes when: classify() and its tests land, phase 3 and 10.4 ticked. Still no call site -- complete work."
    },
    {
      "id": "a-write-p4-migration",
      "status": "open",
      "title": "a-write task 4.1/4.2 -- both columns, one migration 0101, both head bumps",
      "detail": "Tasks 4.1 and 4.2, and 4.2 covers TASK 5.2's COLUMN TOO -- one migration for both, no second migration and no second head bump (D10). Deliberately its own item because it is small, self-contained, and a migration adding two unused columns is complete work that nothing else depends on landing atomically with. 4.1: Run.outside_workspace_writes, nullable JSON. NULL means NOT OBSERVED; [] means OBSERVED, NOTHING LEFT. DO NOT BACKFILL -- migration 0096's own precedent for workspace_dir and 0043's for snapshot_commit_sha. A backfilled [] would claim every historical run was watched and found clean, and that distinction is the whole value of the record (D5). 4.2: migration 0101, guarded for a missing table the way 0033/0034/0075/0095/0096 are. RE-MEASURED 2026-09-04 and still true: head is 0100_loop_work_needs_evidence.py, so down_revision = '0100'. Bump hub/tests/test_migrations.py:40 (HEAD_REVISION = '0100') AND hub/tests/test_project_persistence.py:227 (assert version == '0100') to '0101'. If a further migration has landed by the time this fires, re-derive the number the same way rather than trusting this line -- that is exactly how the original '0100' went stale. Closes when: models.py, the migration, both head assertions, and a green pytest hub/tests/test_migrations.py hub/tests/test_project_persistence.py."
    },
    {
      "id": "a-write-p4-wire",
      "status": "open",
      "title": "a-write phase 4 wiring -- one shared recorder on both sinks, written on first sight",
      "detail": "Tasks 4.3, 4.3a, 4.3b, 4.4, 4.4b, 4.5, 4.6. The biggest item; if it will not fit, stop after 4.3 (a parameter passed and unused is complete) and leave the rest. 4.3 FIRST, and round 3 MEASURED the need: repo_root occurs NOWHERE in _execute_run (agent_trigger.py:1772, tasks.md says 1720) or _execute_codex_appserver_run (:2457, tasks.md says 2389). work_dir, run_id, project_id and agent are all in scope; the project root, which D4 needs to compute worktree_path/task_worktree_path/review_path and to tell project from outside, is not. Add it as a parameter to both, from the trigger body that already computes it. DO NOT re-read the project row inside the callback -- that is a query per tool call to answer a question constant for the run. 4.3a in _flush_line (agent_trigger.py:1932, tasks.md says 1880; the event loop is just below it) and 4.3b in _on_event (:2542, tasks.md says 2474), the Codex app-server sink that never reaches _flush_line -- without 4.3b this covers two of three transports, and round 1 covered one. FACTOR THE CLASSIFY-AND-RECORD STEP SO THE TWO SINKS CALL ONE FUNCTION rather than growing two opinions about it. Classify against work_dir and the project root; work_dir, AW_WORKSPACE_DIR and Run.workspace_dir are all effective_work_dir -- use the value already in scope, NEVER a workspace recomputed from the agent's name (D4). 4.4b IS THE DESIGN DECISION: accumulate in the enclosing closure, write on FIRST SIGHT of each destination. A dict keyed by destination as a nonlocal -- the same shape sequence and accounting_sample already use in both functions, safe for the same reason (each sink is awaited serially within a run, and only one of the two runs for any given run). On first sighting, ONE TRANSACTION writes the Run column and emits 4.5's event together; later writes to a destination already recorded touch the closure only. 4.4: bound the list at 20 entries plus a total count. 4.5: persist_event(..., 'agent_wrote_outside_workspace', severity='warn') naming run, agent, tool, path and destination workspace -- once per DISTINCT DESTINATION per run, not per call. Follow turn_produced_nothing's PAYLOAD AND SEVERITY (run_divergence.py:622-635) but NOT its TIMING, which is evaluate_run_end -- see the next item's 4.4c. Round 3 checked this is allowed: persist_event carries 44 distinct event types in the shipped Hub and exactly one is a refusal, so 'Only refusals SHALL be recorded' was never a constraint on this event (D9). 4.6: recording must NEVER be able to kill a turn -- wrap it the way _report_decision is wrapped. A run that dies because a path could not be classified is a worse outcome than one that wrote outside unnoticed. Test the wrap, do not just write it. Closes when: an outside write on a Claude turn and on a Codex app-server turn each land one row and one event, and the recorder is one function."
    },
    {
      "id": "a-write-p4-tests",
      "status": "open",
      "title": "a-write phase 4 proof -- flip the reproduction, survive a kill, and the three blind spots",
      "detail": "Tasks 4.4c, 4.7, 10.2, 10.3, 10.5. 4.7 FLIPS PHASE 1: expose the column on the run schema so a reader can see it, then rewrite tests 1.2 and 1.3 to assert the record EXISTS and that the cross-worktree case names the OTHER agent's workspace by kind and name. Task 1.1's parse-side assertion also inverts if 2.4 changed what the events carry -- check it rather than assuming. If exposing the column needs a UI change: commit hub/ui/src and hub/hub/static/ui TOGETHER via npm run build then py -3.11 scripts/refresh_ui_bundle.py, and a UI change is driven in a browser against the served bundle or it is not driven (F274). It may well need no UI edit at all -- the schema is Pydantic and this change adds no screen. That is fine; do not invent one. 4.4c: DO NOT flush the column at the run boundary the way turn_produced_nothing does. That is the natural reading of D5 as round 2 wrote it and it loses the whole record for a run that is killed or whose Hub restarts -- exactly the runs whose stray writes matter. Add a test that KILLS A RUN MID-TURN after one outside write and asserts the destination and its first path survived. The exact per-destination call count is best-effort at the boundary and is the only field safe to lose. 10.2: the record is NOT a refusal -- assert it against the event payload, so task 8.3's label check has something to fail against. 10.3: the review-turn case. A review run's workspace IS the detached review checkout (agent_trigger.py:824, re-measure), so a reviewer writing into its own agent worktree is a write outside its workspace and is recorded as one. Correct, and a test rather than a surprise later. 10.5: the run whose workspace IS the project root (D12) -- a read-only agent, or a project that is not a git repository. Writing anywhere in the project records nothing, INCLUDING into another agent's worktree. Assert that, and assert the requirement's wording does not let the empty record read as confinement. D12 is the one open question this change deliberately leaves unanswered: whether such a run should get a boundary at all is a product question, not this change's. Closes when: phase 4 and 10.2/10.3/10.5 are ticked and phase 1's three reproductions now assert the fixed behaviour."
    },
    {
      "id": "a-write-p5",
      "status": "open",
      "title": "a-write phase 5 -- evidence footprinting learns about it without moving",
      "detail": "Tasks 5.1-5.5. The column rides migration 0101 from the a-write-p4-migration item; there is no second migration and no second head bump. All anchors CONFIRMED against the tree. 5.1: outside_writes_for_run(session, run_id) beside recorded_workspace_dir (requirement_evidence.py:343) -- the same one-row read, on the same row. 5.2: carry the fact onto EvidenceFootprint through _apply_footprint (:362) AND NOWHERE ELSE, so capture and re-capture cannot come to disagree about what a footprint means. As an EXPLICIT PARAMETER, not a field on Footprint: Footprint is built from git alone and this is database state on Run, so Footprint cannot derive it and restamp_run_footprints (:845) would have to fabricate it (D11). Both call sites pass what they read from the run -- :423 and :921. 5.3: DO NOT change which directory the footprint is taken from and DO NOT refuse the evidence. D7 rules both out -- there may be several outside trees, one may be the operator's own checkout, and guessing is the 'silently describes another tree' failure with extra steps. Add a test asserting the footprint ROOT IS UNCHANGED for a run that wrote outside, so a later reader cannot 'fix' this by moving it. 5.4: report it in the response that already reports the captured footprint -- the shipped requirement is that the recorder can see which tree their evidence was attached to AT THE MOMENT THEY CAN STILL CORRECT IT. 5.5: correct footprint_root's docstring (:299) -- 'The directory whose HEAD is the work this evidence is about' is true only when nothing escaped. Note that :267 quotes that same docstring in a comment about a fallback's reasoning; check whether it needs the same correction. Closes when: phase 5 is ticked and a run that wrote outside produces a footprint that says so, at the same root as before."
    },
    {
      "id": "a-write-p6-8",
      "status": "open",
      "title": "a-write phases 6, 7, 8 and 10.1 -- the prose, the postures, and what is out of scope",
      "detail": "Tasks 6.1, 6.2, 7.1-7.3, 8.1-8.3, 10.1. No behaviour change and no migration in any of it. Placed after the build deliberately, so it reconciles against what actually landed rather than what was planned -- a-blocked phase 5 is where the equivalent item found that one of its own arguments was wrong. 6.1: correct the comment on Run.workspace_dir (hub/hub/db/models.py:1133-1148, re-measure) -- it records where the run STARTED and is not a statement that the run's writes stayed there (D6). 6.2: GREP FOR ANY OTHER READER of workspace_dir that treats it as containment and CORRECT OR FILE each one. F71's footprinting is the known instance; the point of the grep is the unknown ones. 7.1-7.3: document PER POSTURE, which is the accurate form and the one the round-1 correction forces. workspace (the default -- DEFAULT_CLAUDE_PERMISSION_MODE = WORKSPACE_PERMISSION_MODE at runner_commands.py:66) checked by the Hub; manual put to the operator; acceptEdits not checked; full access not checked. Docker confines at the mount whatever the posture. Say plainly that a workspace is a working directory rather than a wall, that where no posture is checking the OPERATOR is the boundary, and that a write which leaves is RECORDED RATHER THAN PREVENTED. 7.3 is a prohibition: DO NOT WRITE 'native mode does not confine' -- it is false for the default posture. 8.1 and 8.2: FILE two findings in scripts/drive/FINDINGS.md -- shell-command writes (a Bash/shell call carries a command string, so echo x > /abs/path names no path this check can see; note that _decide reads absolute paths out of command TEXT and says of itself that it is a boundary, not a sandbox) and symlink traversal (a link inside the workspace pointing out reports a path that is legitimately inside). Neither is fixed here. 8.3: F115's own warning -- a detector that misses the case it is about is worse than none, because it READS AS COVERAGE -- applies to the LABEL. Check the label in the UI and in the event payload now that both exist: it must read 'wrote outside the workspace', NEVER 'escaped', never 'sandbox violation'. If the UI string needs changing, the bundle rule applies (see a-write-p4-tests). 10.1: VERIFY AND TICK, NOT EDIT -- measured 2026-09-04, the MODIFIED delta in specs/agent-run-sandboxing/spec.md already reads in round 3's shrunk form: 'Only refusals SHALL be recorded **as refusals**' plus one sentence of scope, and round 2's policy paragraph about 'allowed actions that are not ordinary' is already gone. Confirm it still reads that way and that the fact it pinned lives in this change's OWN added requirement (10.2), then tick. Closes when: phases 6, 7, 8 and 10.1 are ticked and the two out-of-scope findings are filed."
    },
    {
      "id": "a-write-p9-gates",
      "status": "open",
      "title": "a-write tasks 9.1-9.3 -- the full gates, plus a mutation check",
      "detail": "9.1 pytest hub/tests/ under py -3.11 (15-25 minutes, load-dependent -- it EXCEEDS THE 600s COMMAND CAP, so background it or run it in chunks). ATTRIBUTE THE TEST-COUNT DIFFERENCE NODE BY NODE against the baseline, not by totals -- a-blocked's equivalent item is the precedent. 9.2 pytest tests/ separately for the CLI; collecting both together fails. 9.3 ruff check src/ hub/ tests/ ; black --check --target-version py311 src/ hub/hub/ hub/tests/ tests/ ; mypy src/. The path lists are CI's, and the narrower forms pass locally and fail there. Bare `python` on this machine is a venv that yields phantom pty_runner failures -- py -3.11 only. MUTATION-CHECK AT LEAST TWO THINGS, because a passing suite is not proof: delete the join-before-resolve line (3.2, mirroring mcp_server.py:938) and watch the relative-path test fail -- if it still passes, the test is running from the fixture workspace's own cwd and proves nothing; and delete the 'hub' kind from classify and watch 3.1b's EXCLUDE_PATTERNS walk fail. Restore both. Closes when: all five gates are green and both mutations were observed to fail."
    },
    {
      "id": "a-write-p9-drive",
      "status": "open",
      "title": "a-write task 9.4 -- drive it live, because a live drive is what found F115",
      "detail": "Task 9.4. A Haiku agent, an absolute-path Write outside its worktree, under a posture that permits it. Assert THE RUN ROW AND THE ACTIVITY EVENT, not just the test. THE DRIVE USES ITS OWN ISOLATED DATABASE AND ITS OWN PORT, never beta and never 8000. Iteration 10's a-blocked drive used ~/.agentweave/hub/profiles/drive8011/ablocked-p6.db on 8011, fresh and migrated 0000->0101 at startup; repeat that pattern with a new file name, and check for and kill a stale 8011 process first. Beta is what the live :8010 instance serves and a second writer would put 8010's scheduler in reach of the drive's project. Launch from hub/, from source, never the console script -- see CLAUDE.md. Real agent turns bind Haiku; there is no token-budget gate. MORE THAN ONE LEG, and pick legs that could FALSIFY rather than confirm: (a) the outside write is recorded with the right destination kind and name; (b) a write INSIDE the worktree records NOTHING -- the leg that catches a detector firing on everything; (c) the cross-worktree case names the other agent, since that is the case whose whole meaning is the destination and the one snapshot_worktree would launder onto the wrong branch. Under the DEFAULT posture the write is refused by _decide, so choose the posture per leg deliberately and say which in the log. Closes when: the legs ran against real turns, the run row and event were read out of the drive database, and the result -- including any leg that failed to falsify -- is in the log."
    },
    {
      "id": "a-write-archive",
      "status": "open",
      "title": "a-write task 9.5 -- retire F115, sync the deltas by grep, then archive",
      "detail": "Task 9.5, plus the ledger. openspec validate --strict on the change, then sync EVERY delta into openspec/specs/ BY VERBATIM HEADER: the ADDED requirements in agent-run-sandboxing ('A file tool writing outside the run's workspace is recorded, in every posture' and 'The product states which postures confine a run and which do not') and in workspace-isolation ('A run's recorded workspace says where it started, not where its writes landed'), the MODIFIED requirement in requirement-traceability ('A changed implementation raises a candidate, never an edit'), and the now-minimal MODIFIED 'A refusal is recorded wherever it is decided' in agent-run-sandboxing. --STRICT DOES NOT COMPARE CAPABILITIES, so a missed delta validates clean -- VERIFY THE SYNC BY GREP, NOT BY EXIT CODE. Five headers, five greps. Then retire F115 in scripts/drive/FINDINGS.md. It has TWO sections (:7670 the finding, :9688 the independent reproduction of it from a later drive) -- retire both, and the second one's own sub-points (including the hard-coded-repository note at :9768) may not all be covered by this change; carry forward anything that is not, rather than retiring it silently. Follow a-blocked's F188 precedent: head the section RETIRED <date>, set the Status line to **fixed <sha>**, and if the repairing commit is earlier than the commit writing the line, say so in prose rather than amending. Note in the ledger that 8.1 and 8.2 filed two SUCCESSOR findings that this change deliberately does not fix. Then git mv the change directory to openspec/changes/archive/<YYYY-MM-DD>-a-write-outside-the-workspace-is-recorded and append a status note to spec-queue/APPROVALS.md's 2026-09-03 section saying the second approved row is built, driven and archived. Closes when: the archive move is committed and the queue is empty -- set next_action to null."
    }
  ],
  "decisions_for_user": [],
  "limits": [
    "Stay on the cycle branch. No commits, merges or rebases onto master. Never auto-merge.",
    "Nothing outward-facing: no publish, no release, no PR or issue creation, no force-push, no history rewriting. Push, do not open PRs.",
    "Nothing destructive: no deleting projects, databases, or kept reproductions.",
    "Do not browse the open web. Research is AgentWeaveResearch's job, in a process that keeps the permission classifier.",
    "Every claim is measured or labelled unverified.",
    "Decisions that are genuinely the operator's go to decisions_for_user, not guessed.",
    "Stage explicit paths, never git add -A. Never commit kimichanges.md or kimiwork.md.",
    "Tests under py -3.11, never bare python. black needs --target-version py311.",
    "Never drive against proj-5e960453 or proj-18e5d4e0. Port 8000 is the operator's real usage and must never be touched.",
    "Every real agent turn in a drive binds claude-haiku-4-5. Never leave a job enabled."
  ]
}

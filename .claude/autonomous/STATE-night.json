{
    "purpose": "FIX. spec-queue/APPROVALS.md carries an ORDER: line for 2026-09-08, so the default backlog-first shape is overridden. Build the four approved changes in that order, driving each before its queue item closes. This window does NOT write new proposals.",
    "playbook": ".claude/loops/night-window.md",
    "runner": "claude",
    "model": "opus",
    "permission_mode": "unattended-full-access",
    "branch": "autonomous/2026-09-08-daily",
    "parent_branch": "master",
    "parent_sha": "755bdad",
    "stop_at": "2026-09-09T07:00:00+01:00",
    "iteration": 10,
    "log_file": ".claude/autonomous/2026-09-08-night-log.md",
    "findings_file": "scripts/drive/FINDINGS.md",
    "approvals_file": "spec-queue/APPROVALS.md",
    "current": "c2-contract",
    "next_action": "Item c2-contract: section 3 of openspec/changes/2026-09-07-an-agent-without-mcp-is-not-told-it-has-nothing, tasks 3.1-3.9 only. The two rules that live in the ADAPTER alone today move into the CONTRACT, so an HTTP agent is governed the same as an MCP one. 3.1: archive_job's always-confirm rule is in hub/hub/mcp_server.py:801-823 (it calls _ask_operator BEFORE POST /jobs/{job_id}/archive); the route at hub/hub/api/v1/agent_actions.py:764-777 asks nothing. Move it to the route. THE MECHANISM IS AN OPEN CHOICE (design.md D5): block on the operator's answer, or return a typed 'direction required' failure carrying a request id the caller polls -- the second matches how /permission-requests already works at agent_actions.py:887-954, so read that pair (open_permission_request, poll_permission_request) before deciding, and WRITE DOWN which and why. 3.2: once the route holds it, mcp_server.archive_job stops holding it -- two confirmations for one archive is worse than none. 3.3: preserve the rule's CONTENT, not just a prompt: required regardless of the run's permission posture, and a standing project.allow_agent_jobs is not a standing yes. READ design.md D6 BEFORE 3.5 -- round 2 cut that task's scope. 3.9 CHANGES A LIVE ASSERTION at hub/tests/test_agent_actions_governed.py:137-140 and the task says to read why first; if the answer is genuinely the operator's, it goes to decisions_for_user rather than being guessed. ask_user's half is the wait, including disclosing wait_expires_at (agent_actions.py:496 area, _record_the_wait_and_park at :440). TESTS REACH THE ROUTES DIRECTLY, without the adapter -- that is the whole point of the section. hub/hub/mcp_server.py may import ONLY stdlib + fastmcp, and approve_tool_call must not gain a return annotation. Mutation-check every test written, restore and git-diff-verify after each; iteration 10's harness pattern is testbed/scratch/mutate_c2_surface.py (uncommitted). Gates before committing: py -3.11 -m ruff check src/ hub/ tests/, black --check --target-version py311 src/ hub/hub/ hub/tests/ tests/, and a scoped slice (test_agent_actions_governed, test_tool_surface_matches_server, test_agent_trigger, test_launchability, test_jobs at minimum). NOTE: ruff/black/mypy are NOT on PATH in Git Bash -- use py -3.11 -m ruff and py -3.11 -m mypy; black alone does work. The FULL hub suite is 15-47 minutes, over the 600s cap, and belongs at task 6.2 when the change is finished. Tick each task as it lands and never on the strength of a plan. AFTER c2-contract comes c2-resolve (section 4), which is where resolve_access_path finally stops returning 'mcp' unconditionally -- until it does, no real run reaches the HTTP rendering section 2 just built.",
    "queue": [
        {
            "id": "compose",
            "status": "done",
            "title": "Compose this window's queue from the playbook",
            "detail": "Done iteration 1. Branch autonomous/2026-09-08-daily confirmed against git log (head 170c4cb, the arm commit; parent master@755bdad matches STATE). APPROVALS.md's newest section is 2026-09-08 and carries an ORDER: line, so per the playbook that is tonight's queue verbatim and the default backlog-first shape is ignored. The unarchived changes and the open findings are NOT this window's work tonight. Green check run and passed: py -3.11 -m pytest hub/tests/ -q, 3972 passed, 86 skipped, exit 0, in 46:41. That is 1.9x the 15-25 minute figure both playbooks and DECISIONS.md carried; repaired in this firing (night-window.md, day-window.md, DECISIONS.md all now say 15-47 minutes with all three measurements), so it needs no queue item. Also recorded: the suite hit F295's exact dead-worker mechanism four times as a PytestUnhandledThreadExceptionWarning (aiosqlite/core.py:66 then :75, 'Event loop is closed'), which means task 3.1's kill mechanism is known-good before it is written."
        },
        {
            "id": "c1-guard",
            "status": "done",
            "title": "F295 change, section 1 -- the checkout and close pool listeners",
            "detail": "Done iteration 2. All six tasks 1.1-1.6 built in hub/hub/db/engine.py (+123 lines below create_async_engine): _refuse_dead_worker_connection on `checkout` (detect only, one WARNING, raise DisconnectionError), _neutralise_dead_worker_on_close on `close`, and _neutralise_dead_worker_on_close_detached on `close_detached`. The 1.6 open choice was taken the way this queue said: two lines written, commented as a deliberately unreachable path. aiosqlite 0.22.1 and SQLAlchemy 2.0.50 attribute/line references were re-confirmed against the installed libraries before writing. DRIVEN, not just compiled: probe_impl_check.py kills a worker by 3.1's real mechanism and measures recovery in 0.00s with one WARNING and one new connection (A), dispose returning in 0.00s with the dead connection idle in the pool and never handed out (B), and a healthy checkout untouched (C). MUTATION CHECK ALREADY DONE FOR 1.5: removing only the `close` listener makes engine.dispose() never return (killed at 45s, exit 124); with it, 0.00s. So task 2.3's dependency on 1.5 is now measured against this implementation, not just against R3's probe. Scoped slice of 4 hub test files: 137 passed, 1 skipped, exit 0. ruff and black clean over CI's paths. Probes are uncommitted in testbed/scratch/f295/ per 4.3."
        },
        {
            "id": "c1-shutdown",
            "status": "done",
            "title": "F295 change, section 2 -- shutdown ordering in the lifespan teardown",
            "detail": "Done iteration 3. Tasks 2.1, 2.2, 2.3 built in hub/hub/main.py: _settle_background_runs() (+45 lines above lifespan) plus two calls appended to the teardown, after terminate_all_active_runs() and shutdown_scheduler(). Cancel, gather(return_exceptions=True), difference_update (not clear()), loop to a fixed point, bound at _MAX_BACKGROUND_SETTLE_PASSES = 5, WARNING with the leftover count on the bound rather than raising, then await engine.dispose(). The abandoned leftovers ARE cleared on the bound and the comment says why: the set is module-level, so a later lifespan in the same process would await tasks on a closed loop. DRIVEN in-process against the real lifespan() context manager (testbed/scratch/f295/probe_shutdown.py, uncommitted): A -- a pending run task is cancelled and settled and engine.pool is a different object after, so the dispose happened and happened after the settle; B -- a task re-registering a successor from its except CancelledError produces 6 tasks, exhausts the 5 passes, logs the WARNING and RETURNS rather than raising; C -- a dead-worker connection idle in the pool does not hang the teardown, 0.00s, neutralised. One finding for 3.6 to use: cancelling a task that has never run skips its except clause, so a test needs an await asyncio.sleep(0) before shutdown or the successor never spawns. 154 passed across every test file that boots the app plus test_agent_trigger and test_scheduler; ruff and black clean over CI's paths."
        },
        {
            "id": "c1-tests-guard",
            "status": "done",
            "title": "F295 change, section 3 part A -- tests for the guard",
            "detail": "Done iteration 4. Tasks 3.1, 3.2, 3.3, 3.4, 3.7 built as hub/tests/test_dead_worker_connection.py -- five tests, 5 passed in 0.23s. The kill is 3.1's real mechanism transcribed into the file (second loop, future on it, close the loop, put on _tx, join, assert not is_alive); is_alive is never monkeypatched; every await is asyncio.wait_for(..., timeout=15.0). A module autouse fixture disposes the engine BEFORE each test -- without it a connection pooled by an earlier test is handed out ahead of the victim and the test passes while asserting nothing. MUTATION-CHECKED, 4 of 4 product lines, engine restored and git diff confirmed empty after each: 1.2's raise -> return gives 3 named failures (46.20s); the checkout decorator removed gives those 3 plus the registration test (46.08s); the WARNING removed gives exactly 1 named failure (0.87s); 1.5's body -> return gives 1 named failure and then HANGS (killed at 180s, exit 124). That last row is a finding and is written into the module docstring: the hang is conftest's per-test dispose(), which no assertion in this file can bound, so a 1.5 regression costs a named failure followed by a stuck CI job -- and it is why 3.3 ships no listener-removed variant. 3.3's removal direction is confirmed twice instead: by that mutation, and by testbed/scratch/f295/probe_33_remove_close_checkout.py (uncommitted, event.remove under try/finally) -- keep RECOVERED in 0.00s, remove DID NOT RECOVER, timed out after 20.00s. Also run: the new file AHEAD of test_suite_database_isolation, test_agents, test_tasks, test_auth and test_lifespan_shutdown in one process, 50 passed in 1:26, so nothing leaks into what follows. ruff and black clean over CI's paths."
        },
        {
            "id": "c1-tests-shutdown",
            "status": "done",
            "title": "F295 change, section 3 part B -- tests for shutdown, dispose, and the conftest teardown",
            "detail": "Done iteration 5. Tasks 3.5, 3.6, 3.8, 3.10, 3.11 built. 3.5 and 3.6 are hub/tests/test_shutdown_settles_background_runs.py (new file, 2 tests) and drive the REAL lifespan() context manager with `async with lifespan(app)`; 3.10 and 3.11 were appended to hub/tests/test_dead_worker_connection.py (now 9 tests). 3.5 asserts the ordering directly: a _DisposeProbe replaces hub.main's `engine` name and samples task.done() at the instant dispose is entered. 3.6 drives the bound with a task that respawns from its except CancelledError, and the `await asyncio.sleep(0)` iteration 3 warned about is there with the reason in a comment. 3.8 wraps conftest.py's settle in try/finally and was DRIVEN, not reasoned: a throwaway test pair showed the pool IS a new object in the next test with the finally and IS NOT without it. FIVE MUTATIONS, all named victims, all restored and git-diff-verified: dispose-before-settle (2 tests), bound-raises (1), difference_update->clear (1), 3.8's finally removed (1, on the drive), 1.2's raise->return (1, TimeoutError in 15.5s). Nine mutations for the change so far counting iteration 4's four. FINDING, and the test docstring was corrected to match it: asyncio.wait_for does NOT bound engine.dispose() -- with 1.5 reverted, a 5s bound did not fire in 60s (probe_310_wait_for_bound.py, uncommitted), because the dispose reaches aiosqlite through SQLAlchemy's greenlet bridge. The same wait_for DOES bound the checkout/execute path. SECOND FINDING, in the new file's docstring: caplog does not work across a lifespan startup -- init_db runs Alembic, fileConfig replaces the root logger's handlers with one StreamHandler, so a teardown warning reaches no capture handler; a recorder on the `hub.main` logger itself survives, which is what _warnings_from() does. Checks: 9 passed in 1.26s; the two new files ahead of a 7-file app-booting slice, 162 passed exit 0 in 59.95s, nothing leaks; ruff clean, black 552 files unchanged."
        },
        {
            "id": "c1-mutation",
            "status": "done",
            "title": "F295 change, task 3.12 -- mutation-check every test written",
            "detail": "Done iteration 6. 3.12 ticked, and tasks.md now carries the tally. FIVE more mutations run (10-14), engine.py and main.py md5-verified against backups after each, git status empty. TOTAL FOR THE CHANGE: 14 mutations, 11 named a victim, and all nine shipped tests are named by at least one -- the matrix is in the iteration 6 log entry. Mutation 10 (1.1's `if thread.is_alive(): return None` deleted) is the one that mattered: it names 5 tests including test_a_healthy_checkout_is_untouched, which no earlier mutation had ever named. Mutation 11 (the `close` DECORATOR removed, run with -k registered so it names the registration test without the hang) pins the second half of that assertion. THREE MUTATIONS NAMED NOBODY, each measured rather than assumed: the non-aiosqlite getattr guard (state cannot occur here) and the close_detached listener (unreachable, as its own docstring says) are not gaps; the third IS a gap and 4.2 should carry it into F295 -- moving the settle BEFORE terminate_all_active_runs() and shutdown_scheduler(), the position task 2.4 argues against at length, fails nothing, so that ordering is held by a comment and not by a test."
        },
        {
            "id": "c1-reconcile",
            "status": "done",
            "title": "F295 change, section 4 -- lint, amend the finding, sync and archive",
            "detail": "Done iteration 7 -- but READ THIS FIRST: the tree was DIRTY at the start of that firing. A previous iteration-7 process had run 4.2, 4.3 and the whole sync half of 4.4 and died before committing any of it; STATE and the log both said iteration 6, clean. That work was VERIFIED rather than redone (openspec validate --strict on the change and on all 43 specs; the synced text diffed against the delta body and identical ignoring blank lines; the F295 append read against the four things next_action said it owed, all present). 4.1 all four gates green on the finished tree: ruff clean, black 552 unchanged, mypy 22 files no issues, py -3.11 -m pytest hub/tests/ -q = 3981 passed, 86 skipped, exit 0, 30:37 -- 3972 opening count plus exactly this change's nine tests, and a fourth point on the 14:39/24:50/46:41 spread. 4.4 done: change git mv'd to openspec/changes/archive/ under its own name (this repo's convention, not the skill's YYYY-MM-DD- prefix, which would double the date), all 24 tasks ticked before the move, openspec list now shows three active changes. F295's Status: line and the index paragraph retired in the same commit, as BUILT NOT DRIVEN -- the open severity-A list is two under the looser definition and three under the stricter one. MEASURED AND UNRESOLVED, for whoever runs the suite next: the green run still emits the dead-worker PytestUnhandledThreadExceptionWarning (aiosqlite/core.py:66 then :75) 10 times (20 warnings), against 4 on iteration 1's pre-fix run, attributed to ten PRE-EXISTING tests and none of the nine new ones. Predicted in kind by proposal.md's out-of-scope section (the guard removes the hang on reuse, not the traceback at loop close, which F292 owns), but 4 -> 10 is two samples of a load-dependent number and causation is NOT established. A third sample is what would settle it."
        },
        {
            "id": "c1-drive",
            "status": "done",
            "title": "F295 change -- drive it, before the queue item closes",
            "detail": "Done iteration 8. DRIVEN and PASSED. Hub from source on 8011 (own temp database, fresh project, Haiku runner, 0 .py newer than process start); 8000 and 8010 untouched, same PIDs before and after (10556, 11292); no job ever created. The stop under test is CTRL_BREAK_EVENT to a child created with CREATE_NEW_PROCESS_GROUP -- uvicorn 0.41.0 handles SIGBREAK on Windows -- because 'agentweave stop' is taskkill /F and reaches no teardown (F297). RESULT: in-flight stop 0.42s, idle control 0.22s, both reaching 'Application shutdown complete', which is printed after the settle AND engine.dispose() have returned. No settle-bound WARNING, no dead-worker WARNING, no 'Event loop is closed', no hang. Reproduced three times. THE CORRECTION THAT COST A RUN: the first attempt inferred 'in flight' from output lines and Haiku finished the turn in 10s, so a 6/6 green was measured on an idle Hub -- F296's class of error committed again one day later. The script now blocks the turn on a real time.sleep(120) child and reads runs.ended_at IS NULL in the same breath as the break. TWO FINDINGS THE DRIVE FOUND AND 14 MUTATIONS DID NOT: F298 (C, new) -- a deliberate stop logs ERROR 'Unhandled error in run' with a CancelledError traceback, from a handler (agent_trigger.py:2372-2394) whose own comment names this exact case, so the behaviour is right and the report is wrong; and the stop leaves one Run row at 'running' (the cancelled successor never enters its try), which the next start repairs to 'interrupted' -- measured with f295_restart_reconciles.py, zero rows left running, agent idle, fresh trigger completed, so NOT a wedge. WRITTEN: F295 Status: line + a closing 'The drive' section; a new index revision saying the open severity-A list is two (F274, F142) under BOTH definitions; F298; one line into F297 upgrading its CTRL_BREAK_EVENT suggestion to a measurement. Harnesses committed: scripts/drive/f295_shutdown_drive.py (with a --no-run control) and scripts/drive/f295_restart_reconciles.py, ruff and black clean. NOT re-run: the full hub suite -- nothing under hub/ or src/ was touched, so the 4->10 warning count still has two samples and wants a third."
        },
        {
            "id": "c2-notice",
            "status": "done",
            "title": "MCP-notice change, section 1 -- the notice tells the truth",
            "detail": "Done iteration 9. Tasks 1.1-1.5 all ticked, Python + one test file only; no UI file touched so the bundle was not rebuilt and the TS lint set was not required (tasks.md:7). access_path_notice's non-MCP branch (hub/hub/launchability.py) now names the four things 1.1 asks for -- HUB_URL as the base address, AW_RUN_TOKEN as the credential, the `Authorization: Bearer` shape, and the route prefix /api/v1/agent-actions, which was VERIFIED against agent_actions.py:63 under __init__.py:36 rather than taken from the proposal. NEITHER VALUE IS INTERPOLATED and the comment says why in D4's terms. 1.3's two true sentences kept verbatim; 1.4's comment rewritten not deleted (its history is still true, only the implication was wrong); the docstring stopped saying 'One line'. A GREEN TEST WAS PINNING THE FALSE SENTENCE: test_launchability.py:474-481 asserted 'no AgentWeave tool surface is available' IN the notice. Changed deliberately, with the docstring recording it, and that string is now asserted ABSENT. Three tests added: the four things, the denial's absence in other words, and 1.5's leak half -- which monkeypatch.setenv's AW_RUN_TOKEN and HUB_URL to sentinels BEFORE rendering, because rendering with them unset would pass against an interpolating implementation (F190/F296's class of error). FOUR MUTATIONS, four named victims, md5 bfd6a25a restored and git-diff verified after each: branch reverted to the old denial (3 tests), Bearer value interpolated (1, the leak test alone), HUB_URL value interpolated (1), route prefix replaced by prose (1). Gates: ruff clean, black 552 unchanged, scoped 5-file slice 146 passed in 35.93s. Rendered through a real import with both sentinels set -- neither appears. NOT DONE, deliberately: no Hub started (6.3-6.5 are c2-verify's, so 'the notice is correct' is still a source reading with tests behind it); the delta's 'where the operations are described' is half-satisfied until section 2's rendering exists; and src/agentweave/tool_surface.py:110 holds a now-divergent copy of the old function, confirmed to have zero importers across src/, hub/ and tests/."
        },
        {
            "id": "c2-surface",
            "status": "done",
            "title": "MCP-notice change, section 2 -- operations described for the access path",
            "detail": "Done iteration 10. Tasks 2.1-2.4 all ticked. Python only; no UI file, so no bundle and no TS lint (tasks.md:7). 2.1: `_tool_surface_lines`'s 150-line literal became `_operations()` -- 24 `_Operation` records (tool, MCP signature, method, path, WIRE field names, which the route requires, and the SHARED description) plus `_mcp_lines` and `_http_lines`. THE MCP RENDERING WAS DIFFED AGAINST HEAD, not assumed: restored the pre-change file, rendered both `has_peers` values, restored back, md5-verified. The entire diff is one line splitting in two -- `toggle_job`/`run_job` shared a line and cannot share a record (PATCH /jobs/{id} vs POST /jobs/{id}/run). Meaning preserved, bytes not. 2.2: `resolve_access_path` hoisted above the materialisation, one value feeding both the notice and the renderer; the hoist was VERIFIED on the checkout (access_path appears at exactly :1006/:1007/:1026, all after; no reassignment of config/probe/runner in 649-1006). The other two callers (POST /agents/register, GET /agents/agent-context) DELIBERATELY keep the mcp default -- reason written into the docstring: both are answered under the project API key outside any run, the path is settled at spawn time, and neither is on a run's path to the plane. 2.3: the HTTP half is checked against the app's own OpenAPI schema (route exists, no phantom field, required-ness matches, nothing required left unnamed, prefix mounted) plus set equality between the renderings. IT CAUGHT A REAL DEFECT ON ITS FIRST RUN: the recall route's path parameter is `output_id`, not the tool's `observation_id` -- an agent following the address gets a 404. Second instance of that class in this change (send_message is recipient/type on the wire), which is why fields are written, not derived. 2.4: `*` marks required, explained once in the preamble; no validation restated; three http_notes for non-flat requests. TEN MUTATIONS, all named a victim, both files md5-restored and git-diff-verified after each. Mutation 9 (the renderer's default flipped to `cli`) NAMED NOBODY on its first run -- my own 2.2 decision was held by a docstring alone. Closed rather than noted: test_the_two_routes_outside_a_run_keep_the_injected_tool_wording now names it. Gates: ruff clean, black 552 unchanged, mypy 22 files clean, 13-file scoped slice 285 passed in 78.13s. NOT DONE: no Hub started (6.3-6.5 are c2-verify's), and no real run can reach the HTTP rendering yet because resolve_access_path still returns `mcp` unconditionally until section 4 -- the two new trigger tests patch it and say so."
        },
        {
            "id": "c2-contract",
            "status": "open",
            "title": "MCP-notice change, section 3 -- the two adapter-only rules move into the contract",
            "detail": "Tasks 3.1-3.9: archive_job's confirmation and the loop refusal, and ask_user's wait including disclosing wait_expires_at (hub/hub/api/v1/agent_actions.py:496). Read design.md D6 before 3.5 -- round 2 cut that task's scope. 3.9 changes an existing assertion at test_agent_actions_governed.py:137-140 and says to read why first. Tests reach the routes DIRECTLY, without the adapter. hub/hub/mcp_server.py may import only stdlib + fastmcp, and approve_tool_call must not gain a return annotation."
        },
        {
            "id": "c2-resolve",
            "status": "open",
            "title": "MCP-notice change, section 4 -- a run is told the access path it actually has",
            "detail": "Tasks 4.1-4.9 around resolve_access_path (hub/hub/launchability.py:237-245), which returns 'mcp' unconditionally today. Three test files depend on the current behaviour and are named in the tasks: conftest.py:496-507 (an autouse fixture patching probe_mcp_registered), test_agent_trigger.py:793-830, test_launchability.py:390-429. 4.8 asks for the permission posture to be decided SEPARATELY and stated; 4.9 is the approver-flag problem, where the flag names an MCP tool the mirror deployment cannot provide. If either is genuinely the operator's call, it goes to decisions_for_user."
        },
        {
            "id": "c2-verify",
            "status": "open",
            "title": "MCP-notice change, sections 5 and 6 -- docs, lint, tests, and the drives",
            "detail": "5.1 corrects docs/architecture/overview.md:18-19. 5.2 explicitly forbids touching .claude/skills/copilot-test-setup/SKILL.md in this change. 6.1/6.2 lint and hub tests. 6.3-6.5 are drives, including the mirror deployment at 6.5 -- the check that matters for the deployment this change exists for. 6.6 records the drive in FINDINGS.md whether it worked or not. Then sync and archive, retiring anything the proposal names."
        },
        {
            "id": "c3-conversation-run-facts",
            "status": "open",
            "title": "F274 change -- the conversation carries its own run facts (BUNDLE; likely two nights)",
            "detail": "openspec/changes/2026-09-05-the-conversation-carries-its-own-run-facts, 44 tasks. PHASE 0 IS A HARD GATE: if phase 0 has not been recorded, do phase 0 and stop. Its port instruction was reworded on 2026-09-08 after the clean slate, so read the file rather than a remembered version. TOUCHES hub/hub/static/ui -- commit hub/ui/src and the bundle together via py -3.11 scripts/refresh_ui_bundle.py after npm run build; only that script writes the stamp. A UI change is driven IN A BROWSER against the served bundle or it is not driven; scripts/drive/d1_aturn_browser.py is the working pattern, and a Python transcription of a component is exactly how F274 shipped. Realistically not reached tonight; when it is started, split it in the log."
        },
        {
            "id": "c4-clearing-instructions",
            "status": "open",
            "title": "DAY-3 change -- clearing instructions asks first (BUNDLE; last in ORDER)",
            "detail": "openspec/changes/2026-09-07-clearing-instructions-asks-first, 25 tasks, one already ticked (5.2, the pre-change drive, closed on real evidence committed at 3078843). TOUCHES the bundle. ORDER puts it last, and the constraint is strictly one bundle-touching change per night, so this cannot share a night with c3. Almost certainly tomorrow's work; leave it open and say so in the final log entry."
        }
    ],
    "decisions_for_user": [],
    "limits": [
        "Stay on the cycle branch. No commits or rebases onto master, ever, by any window. The day window's merge gate (day-window.md, Iteration 1 step 1) may fast-forward master to this branch with 'git merge --ff-only' when all four gate conditions hold -- relaxed by the operator 2026-09-06. No other step, and no other window, may merge, rebase, or commit onto master.",
        "Nothing outward-facing: no publish, no release, no PR or issue creation, no force-push, no history rewriting. Push, do not open PRs.",
        "Nothing destructive: no deleting projects, databases, or kept reproductions.",
        "Do not browse the open web. Research is AgentWeaveResearch's job, in a process that keeps the permission classifier.",
        "Every claim is measured or labelled unverified.",
        "Decisions that are genuinely the operator's go to decisions_for_user, not guessed.",
        "Stage explicit paths, never git add -A. Never commit kimichanges.md or kimiwork.md.",
        "Tests under py -3.11, never bare python. black needs --target-version py311.",
        "Never drive against proj-5e960453 or proj-18e5d4e0. Port 8000 is the operator's real usage and must never be touched.",
        "Every real agent turn in a drive binds claude-haiku-4-5. Never leave a job enabled.",
        "One bundle-touching change per night. c3 and c4 both rebuild hub/hub/static/ui and may not share a night; they share no source file, so the constraint is on the generated artefact only.",
        "CI redness is 20.4 percent F292 (11 of 54 runs, 2026-09-07T00:00Z to 2026-09-08T08:26Z, all 11 the same cause). Classify a red CI with 'gh run view <id> --log-failed' BEFORE diagnosing a regression from tonight's change.",
        "The hub suite is 15-47 minutes and load-dependent (14:39 / 24:50 / 46:41 measured). It far exceeds the 600s command cap: background it and block on the output file, and never size a firing as though it were 25 minutes."
    ]
}

{
  "purpose": "FIX. Read spec-queue/APPROVALS.md, then build: backlog first (unarchived changes, then open findings by severity), then APPROVED rows. Drive every change before closing its queue item. This window does NOT write new proposals.",
  "playbook": ".claude/loops/night-window.md",
  "runner": "claude",
  "model": "opus",
  "permission_mode": "unattended-full-access",
  "branch": "autonomous/2026-09-03-daily",
  "parent_branch": "master",
  "parent_sha": "2912736",
  "stop_at": "2026-09-04T07:00:00+01:00",
  "iteration": 11,
  "log_file": ".claude/autonomous/2026-09-03-night-log.md",
  "findings_file": "scripts/drive/FINDINGS.md",
  "approvals_file": "spec-queue/APPROVALS.md",
  "current": "a-write-split",
  "next_action": "openspec/changes/a-write-outside-the-workspace-is-recorded -- item a-write-split. THIS FIRING SPLITS, IT DOES NOT BUILD. The gate is now open: a-blocked was archived in iteration 11 to openspec/changes/archive/2026-09-04-a-blocked-agent-workspace-holds-its-input, and APPROVALS.md carries a note saying so. Read tasks.md (54 tasks, 10 phases, zero ticked) and proposal.md/design.md enough to see where the phase boundaries actually fall, then REWRITE THE QUEUE in this STATE file with firing-sized items in the shape the a-blocked items used -- one item per phase or per coherent group, each carrying what to build, the gate that closes it, and any measurement already made. Order them so stopping anywhere leaves complete work rather than a half-written phase, and put the reproduction/tests-first phase first if the change has one (a-blocked's phase 1 GATE -- 'a reproduction that does not pass first is not a reproduction' -- is the pattern worth copying). Two facts already verified 2026-09-03 and NOT needing a re-check: task 4.2's migration number 0101 is correct (head is 0100_loop_work_needs_evidence.py), and zero of the 54 tasks are ticked. CARRY FORWARD into whichever item covers the UI phases: commit hub/ui/src and hub/hub/static/ui together via py -3.11 scripts/refresh_ui_bundle.py after npm run build, and a UI change is driven in a browser against the served bundle or it is not driven (F274). Also carry forward that a drive uses its own isolated database and its own port, never beta and never 8000 -- iteration 10 used profiles/drive8011/ablocked-p6.db on 8011 and that pattern should be repeated, not the queue item's original 'use beta'. When the split is written, append a log entry and STOP; do not start phase 1 in the same firing.",
  "queue": [
    {
      "id": "a-blocked-p1",
      "status": "done",
      "title": "a-blocked phase 1 -- reproduce F188 first",
      "detail": "Tasks 1.1-1.3. New file hub/tests/test_a_blocked_agent_workspace_holds_its_input.py: (1.1) one agent, one conversation, one unbound operator entry, trigger_agent_directly patched to raise TriggerAgentError(409, 'Could not prepare isolated worktree for <agent>: ...') with NO flags; schedule DELIVERY_ATTEMPT_LIMIT times; assert withdrawn plus abandoned_reason claiming it failed 3 times. (1.2) the same loop with NO_RUNNER (agent_wide=True) leaves the entry queued at 0 attempts -- the asymmetry IS the finding. (1.3) at the worktrees layer without the Hub: a plain directory at .agentweave/worktrees/<agent> in a temp git repo makes ensure_worktree raise IsolationUnavailableError, and assert the message carries no remedy today. GATE: it must pass unmodified. A reproduction that does not pass first is not a reproduction. DONE iteration 2: 3 passed against unmodified code (git status showed only the untracked test file), and 1.1 mutation-checked -- adding agent_wide=True to BLOCKED_AGENT_WORKSPACE turns it red by name. 1.1 and 1.3 are written to FLIP in phases 3 and 4; 1.2 must keep passing untouched."
    },
    {
      "id": "a-blocked-p2",
      "status": "done",
      "title": "a-blocked phase 2 -- the refusal says which workspace it could not prepare",
      "detail": "Tasks 2.1-2.4 in hub/hub/api/v1/agent_trigger.py. Add agent_workspace_unavailable: bool = False to TriggerAgentError.__init__ with a docstring paragraph in its three neighbours' style (what it means, why it is not agent_wide per design D2, and that it does NOT imply transient, unlike workspace_unavailable). Split the except at :879-883 on worktrees.takes_task_workspace(repo_root, config, turn_workspace.task_id) -- design D1, never on the exception type or its message. The agent arm raises with the flag and names the agent's own workspace; the task arm keeps today's flags exactly and names the TASK. 2.4 asserts the dispatch on flags, not wording. DONE iteration 3: flag added after agent_wide with the three-paragraph docstring; the except now dispatches on takes_task_workspace (task arm -> 'Could not prepare the checkout for task <id>', no flags; agent arm -> \"Could not prepare <agent>'s own workspace\", agent_workspace_unavailable=True only). New file hub/tests/test_a_blocked_workspace_refusal_names_its_scope.py, 2 passed, both stubbing ONE shared IsolationUnavailableError so a dispatch that read the exception would give identical flags. Mutation-checked twice: inverting the predicate fails both by name; deleting the flag fails only the agent test. 12-file chunk 198 passed in 3:31; ruff and black clean. turn_scheduler.py untouched."
    },
    {
      "id": "a-blocked-p3a",
      "status": "done",
      "title": "a-blocked phase 3a -- the shared predicate and the scheduler helper",
      "detail": "Tasks 3.0, 3.0a, 3.1, 3.2. Extract takes_own_checkout(task) in hub/hub/task_workspace.py and refactor resolve_turn_workspace_inputs onto it (design D8), keeping the logger.warning on the invalid-id branch. SPELL THE SCHEME CHECK WITH != AND AN EARLY return False -- test_task_workspace_scheme.py substring-scans for '.workspace_scheme =' and 'workspace_scheme=' over lowercased source, and the == spelling contains the first; measured red by R3. Do not relax the scan. 3.0a: re-run that file immediately, confirm 10 passed with migration 0095 the only match, and add the one-line docstring note saying why a READ here is constrained to the negative form. 3.1: the turn_scheduler helper answering whether other queued input for this agent would have run in a different workspace -- two queries in this order (the remaining entries' conversations first, then one over the union of the task ids the entries name and the ids those conversations carry), never one query per entry, and entries in the controlling conversation need no inheritance lookup. 3.2: write the argument and R1's and R3's corrections into its docstring, including which direction each approximation errs in -- a false yes destroys the operator's message, a false no only holds it. DONE iteration 4. takes_own_checkout(Optional[Task]) is in task_workspace.py spelled with != and an early return False; measured by hand that the == and ==-no-space spellings are both offenders and != is not, and test_task_workspace_scheme.py is 10 passed with 0095 the only file the scan matches. The logger.warning stayed in the resolver as _warn_when_the_id_is_why(task), which returns early for a grandfathered task so the combined branch's behaviour is preserved exactly rather than widened; _id_complaint holds the one try/except both callers need. 3.0a's docstring note is in. The scheduler helper is PUBLIC -- other_input_would_have_run_elsewhere -- because 3c tests shapes a drive cannot produce and must call it directly. Two queries in the forced order, eligibility expressed as WHERE; _tasks_this_entry_is_about holds the or and reuses _entry_kind rather than run_task_binding.task_named_by, whose precedence runs the other way. Verified by a throwaway probe (13 shapes, 13 passed) that was mutation-checked twice and then DELETED -- its coverage is written into a-blocked-p3c."
    },
    {
      "id": "a-blocked-p3b",
      "status": "done",
      "title": "a-blocked phase 3b -- wire the condition, plus the first four scheduler tests",
      "detail": "Tasks 3.3-3.6. Extend the condition at turn_scheduler.py:204 so an agent_workspace_unavailable refusal skips the counter unless 3.1's helper says something else could have run. The helper LANDED IN ITERATION 4 and is measured: `await other_input_would_have_run_elsewhere(db, project_id=project_id, agent=agent, entries=entries, selected=selected, controlling_conversation_id=conversation.id, hop_budget=hop_budget)`, public, defined above schedule_agent. Ask it LAST -- it is the only term in the condition that costs queries. keep the existing transient and agent_wide terms untouched and readable, and do not collapse the three questions into one boolean. 3.4: the three scenarios from agent-conversation-workspace -- held when every other entry is unbound, counted-and-withdrawn when a task-bound entry waits in another conversation, counted when the refusal is the task-checkout one; the middle test's docstring says it is what keeps this change from breaching 'where other queued input could have run'. 3.5: the inherited binding -- the other conversation's entry names no task but its conversation does, and it must count. 3.6: an entry inside selected naming a task that no longer resolves must not count. ALSO IN 3b (found in iteration 3, and 1.1 cannot flip without it): phase 1's stub BLOCKED_AGENT_WORKSPACE in hub/tests/test_a_blocked_agent_workspace_holds_its_input.py:41-49 still carries the pre-phase-2 sentence and NO flags, and its #: comment claims that is what agent_trigger.py:879-883 raises 'today'. Repoint it at the real agent-arm refusal -- \"Could not prepare <agent>'s own workspace: ...\" with agent_workspace_unavailable=True -- and rewrite the comment, then flip 1.1's assertion from withdrawn to held and its detail assertion with it. Test 1.2 (NO_RUNNER) must stay untouched and green. DONE iteration 5. The condition is three terms, the new one written `not getattr(exc, \"agent_workspace_unavailable\", False) or await other_input_would_have_run_elsewhere(...)` so the `or` short-circuits on the attribute read and the two queries are paid for only by a flagged, non-transient, non-agent-wide refusal -- the ordering constraint enforced by the language rather than by a comment. Phase 1's stub repointed at the real agent-arm refusal (the old flagless sentence is no longer raised anywhere, so the flip would have been a flip of nothing), 1.1 renamed and flipped to (\"queued\", 0) with abandoned_reason None plus a waiting_reason assertion, 1.2 byte-identical. New file hub/tests/test_a_blocked_workspace_counts_where_input_could_run.py, 5 tests, all through schedule_agent. Mutation-checked three ways, each naming tests: deleting the term fails 3.4a/3.6/1.1; `return False` in the helper fails 3.4b/3.5 only; replacing the flag read with True fails 3.4c only. Chunk of thirteen -> 162 passed in 1:46; ruff and black clean."
    },
    {
      "id": "a-blocked-p3c",
      "status": "done",
      "title": "a-blocked phase 3c -- the grandfathered test and its siblings",
      "detail": "Tasks 3.7, 3.8, 3.8a, 3.8b, 3.9 -- the tests round 3 exists for. 3.7: another conversation's entry naming a task whose workspace_scheme is 'agent' must NOT count; set the column directly in the fixture (no runtime path writes it) and say in the docstring that a scope test built on entry.task_id alone passes every other test in the file and fails this one. 3.8: the same for a task id validate_task_id refuses, a deleted task row, and a task in TERMINAL_FOR_BINDING -- BIND THE OTHER CONVERSATION TO NOTHING in the last two fixtures and say in the docstring that this is load-bearing rather than tidy. 3.8a: the inverse -- an entry naming a decided task on a conversation bound to a live task-scheme task MUST count. 3.8b: a review entry whose task has no evidence naming a commit must not count (design D3b); the sibling whose task does name one must. 3.9: over the hop budget, and a closed conversation -- neither counts. THE DELETED PROBE'S COVERAGE, which 3c is now the only place these shapes are asserted (iteration 4 measured all thirteen against the live helper and then removed the file): no for every-other-entry-unbound, for a grandfathered task, for an approved task, for the id '../escape', for a task id with no row, for an archived other conversation, for an over-budget entry, for an entry inside selected, for a second entry in the CONTROLLING conversation when that conversation itself carries a live task (the inheritance lookup must not run there), and for a review entry whose task has no evidence naming a commit; yes for another conversation's entry naming a live task-scheme task, and yes for an entry naming nothing whose conversation carries one. Re-establish each as a committed test rather than trusting this line. DONE iteration 6. Eleven tests appended to hub/tests/test_a_blocked_workspace_counts_where_input_could_run.py under a section banner explaining the file's two halves; _seed widened with one optional trailing element per tuple kind (conversation lifecycle, task workspace_scheme, a dict of further new_entry keywords) so all five scheduler-level tests read byte-identically. Shapes covered: the positive control, 3.7 grandfathered, 3.8's three (unmintable id, deleted row, decided task -- both of the last two with the other conversation bound to nothing), 3.8a decided-on-a-live-thread, 3.8b's review pair (review_evidence.py supplies the commit), 3.9's two, and the controlling-conversation inheritance shape the gate mutation needs. DEVIATION, recorded: 3.7's docstring does NOT say 'a scope test on entry.task_id alone passes every other test and fails this one' -- that claim is 3.5's already and the two collide; it says instead that 3.5 and 3.7 pin the rule from opposite sides and neither holds it alone. Eight mutations, each naming a test, all eleven pinned: the scheme line (3.7 + test_turn_workspace's own grandfathering test), `if True` for the controlling guard (that test alone), `return False` in the helper (the three positives plus 3.4b/3.5), review-without-commit_for_task_review, decided_task_refusal, the lifecycle WHERE, the hop filter, _id_complaint, and a missing row counting. ruff clean, black 540 unchanged, thirteen-file chunk 173 passed in 1:22. No product file changed."
    },
    {
      "id": "a-blocked-p4",
      "status": "done",
      "title": "a-blocked phase 4 -- the refusal says what would clear it",
      "detail": "Tasks 4.1-4.3 in hub/hub/worktrees.py. Give ensure_worktree's two refusal branches (a symlink; a path that is not the registered worktree for the expected ref) their own remedies, following _merge_prerequisites' precedent that this module writes the operator-facing sentence -- name the directory to remove and the prune that follows it, and do NOT write one sentence covering both. 4.2 does the same for the mid-merge refusal reached from the task arm, so the requirement's third scenario is satisfied by real branches rather than by two wordings of one. 4.3 asserts each remedy at the worktrees layer, per branch, against the obstruction it was written for -- never by substring-matching one shared phrase. DONE iteration 7. ensure_worktree had ONE branch, not two -- the `or` was split into a symlink arm (remove the link, `rm <path>`, target untouched) and a wrong-ref arm (`rm -r <path>`; the next turn prunes and provisions). The SAME split was made in ensure_task_worktree, both arms naming the task: in scope because scenario 2 is about that arm and because 4.2's contrast needs a second differently-remedied obstruction at the same path. ensure_review_checkout was DELIBERATELY NOT touched (recorded in tasks.md): out of the requirement's scope and a different guard shape. 4.2 was a sharpening -- the mid-merge branch already had a remedy; it now names `git -C <path> merge --abort` and says DO NOT remove the directory, the opposite of its sibling. Test 1.3 flipped and renamed to test_a_plain_directory_in_the_way_says_what_to_remove. New file hub/tests/test_a_blocked_workspace_refusal_states_its_remedy.py, 5 tests, each of which PERFORMS its remedy and asserts provisioning then succeeds; the agent wrong-ref test blocks with a worktree registered for ANOTHER ref so the promised prune is really exercised (git lists it `prunable` after rm -r, asserted). Four mutations, each naming a test. LOCAL GAP: the two symlink tests SKIP here (WinError 1314, no symlink privilege) and run only in CI's Linux job; a throwaway probe forcing Path.is_symlink confirmed both branches format their own remedy. ruff clean, black 541 unchanged, fifteen-file chunk 200 passed 2 skipped in 1:34."
    },
    {
      "id": "a-blocked-p5",
      "status": "done",
      "title": "a-blocked phase 5 -- reconcile what already exists",
      "detail": "Tasks 5.1-5.4. 5.1: hub/tests/test_a_delivery_attempt_means_a_delivery.py's BAD_CHECKOUT is F188's exact sentence used there as the ENTRY-SPECIFIC example -- repoint it at a real task-checkout refusal; its assertion (still counts, still gives up) is correct and must not change. 5.2: grep for other readers of the old sentence (inbound_queue.py's status derivation, any UI string, any test) and RECORD WHAT THE GREP FOUND IN THE CHANGE, including 'nothing'. 5.3: update TriggerAgentError's agent_wide docstring at agent_trigger.py:304-318, which cites :756 as the entry-specific worktree example. 5.4: rewrite turn_scheduler.py:225-233 -- a shipped comment INSIDE the branch being edited that asserts the exact claim this change falsifies, and which 5.2's grep can never reach because it quotes no sentence. Repoint its D3a citation to 2026-08-28-a-delivery-attempt-means-a-delivery; this change's own predicate decision is D8 precisely so the two do not collide in this file. 5.2 PARTLY MEASURED in iteration 3, and it found a file task 5.1 does not name: grep for 'Could not prepare isolated worktree' now hits NO product line and three test files, all hand-constructing a TriggerAgentError rather than asserting the real raise site -- test_a_delivery_attempt_means_a_delivery.py:37 (5.1's BAD_CHECKOUT), test_a_blocked_agent_workspace_holds_its_input.py:46,122 (phase 1's stub, handled in 3b), and test_checkpoint_cutover.py:1069,1072 (waiting_reason + detail, NOT in 5.1's description -- decide whether it should read a real refusal, and record the decision). No UI string reads it: that is the 'nothing' 5.2 asks to be recorded. Re-run the grep at phase 5 rather than trusting this line, and record inbound_queue.py's status derivation separately -- it was not checked here. DONE iteration 8, all four tasks, and every product line changed is a COMMENT (checked mechanically: the diff over hub/hub/ has no non-comment +/- line). 5.2's grep re-run after phase 4: NO product line, NO UI string, four hits in three test files -- recorded as a table in tasks.md so no later round re-runs it. inbound_queue.py checked separately and it never matches on the text at all: queue_status passes entry.waiting_reason through verbatim, which is WHY phase 4's remedy will reach leg 6.2's screen. 5.1: BAD_CHECKOUT repointed at the task arm (ensure_task_worktree's wrong-ref refusal wrapped by agent_trigger), truncated after the diagnosis, assertion untouched; mutation-checked (adding agent_workspace_unavailable=True fails test_an_entry_specific_refusal_still_counts_and_still_gives_up by name). test_checkpoint_cutover.py:1069,1072 DECIDED: repointed too, and measured insensitive -- a garbage sentence leaves it at 39 passed, so the branch decides on entry_ids as claimed. The three truncated stubs were annotated rather than extended (they now say they stop after the diagnosis and point at test_a_blocked_workspace_refusal_states_its_remedy.py). 5.3 FOUND A DOC/CODE DISAGREEMENT and filed it as F276 (C) under a new `# Filed outside a drive` heading at the end of scripts/drive/FINDINGS.md: the agent_wide paragraph cited `:479` (no such agent) as request-level AND agent-wide, but that raise passes request_level only and never passed agent_wide, and NO site in the Hub sets both flags. The doc half is fixed here; the behaviour half (should input for an agent that does not exist be held rather than withdrawn at the third attempt?) is open and unproposed in F276. 5.4 rewritten as two bullets, one per arm, with D3a/D8/D1 pulled apart. GATE: sixteen files (the fifteen plus test_checkpoint_cutover.py) -> 239 passed, 2 skipped, 1:49; ruff clean; black 541 unchanged."
    },
    {
      "id": "a-blocked-p6-gates",
      "status": "done",
      "title": "a-blocked task 6.1 -- the full gates",
      "detail": "pytest hub/tests/ under py -3.11 (15-25 minutes, load-dependent -- it exceeds the 600s command cap, so background it or run it in chunks), pytest tests/ separately for the CLI (collecting both together fails), ruff check src/ hub/ tests/, black --check --target-version py311 src/ hub/hub/ hub/tests/ tests/, mypy src/. Mutation-check at least the phase 3 condition: delete the new term and watch a NAMED test fail; if nothing fails the test asserted over nothing. SIZING NOTE measured 2026-09-03 23:04: the nine-file chunk relevant to this change ran in 3:14 (242 passed, 1 xpassed), so the whole-suite figure is not a guide to it. CHUNK NAMES, measured iteration 4 (iteration 3's 'nine-file' note named two files that do not exist; pytest exits with a usage error on a missing path and prints '5 warnings in 0.01s', which reads like a pass). The twelve real neighbours are test_turn_workspace, test_task_workspace_scheme, test_a_blocked_agent_workspace_holds_its_input, test_a_blocked_workspace_refusal_names_its_scope, test_a_delivery_attempt_means_a_delivery, test_turn_scheduler, test_inbound_queue, test_delivery_attempts, test_task_resolved_before_workspace, test_task_worktrees, test_worktrees, test_no_refusal_queues_input -- 157 passed in 2:41. CHUNK UPDATED iteration 7: fifteen files now -- the twelve named above plus test_a_blocked_workspace_counts_where_input_could_run, test_a_blocked_workspace_refusal_states_its_remedy (new in phase 4) and test_review_checkout (added because ensure_review_checkout is the branch phase 4 deliberately left alone). 200 passed, 2 skipped, 1:34. The 2 skips are the symlink tests and are EXPECTED on this machine -- do not chase them. CHUNK IS SIXTEEN FILES as of iteration 8: the fifteen plus test_checkpoint_cutover.py, pulled in by 5.1's decision -> 239 passed, 2 skipped, 1:49. RUN THE GATES FROM THE REPO ROOT: from hub/, ruff prints `E902 The system cannot find the file specified ... src:1:1` and black prints `Path 'src/' does not exist` -- both are wrong-cwd errors wearing a disguise. mypy src/ has never been run in this change; it is the only gate here without a prior measurement. DONE iteration 9, all five gates, two of them first measurements in this change. WHOLE HUB SUITE: 3874 passed, 86 skipped, 1 xpassed, exit 0, 24:27 -- so the 600s cap really does require backgrounding it, and the sixteen-file chunk's 1:49 understates it by a factor of thirteen. CLI from the repo root: 440 passed, 3 skipped, 23s. ruff clean. black 541 unchanged. mypy src/: no issues, 22 source files (never run before in this change; running it early cost nothing and it was green). No failure anywhere, so the 'check unrelated failures against master' contingency never fired; the 86 skips and the 1 xpass are the suite's standing baseline and the 2 symlink skips are inside them. MUTATION re-done against the current tree: deleting the third condition term (12-line deletion, no whitespace churn) fails exactly three tests by name -- 3.4a test_a_second_unbound_conversation_does_not_make_the_head_expendable, 3.6 test_an_entry_in_the_refused_batch_naming_a_vanished_task_does_not_count, and 1.1 test_a_blocked_agent_workspace_holds_the_operators_message, the last failing on ('withdrawn', 3) != ('queued', 0), which is F188's exact symptom returning. The other 16 of 19 kept passing because they are pinned by the helper rather than by the condition, and phase 3c mutation-checked those separately. Restored byte-for-byte, git diff empty, 19 passed after."
    },
    {
      "id": "a-blocked-p6-drive",
      "status": "done",
      "title": "a-blocked tasks 6.2-6.4 -- three drive legs against the product",
      "detail": "DONE iteration 10, three legs, 33/33, two real Haiku turns. DEVIATION, deliberate: the Hub ran on profiles/drive8011/ablocked-p6.db (fresh, migrated 0000->0100 at startup), NOT the beta profile this item named -- beta is what the live :8010 instance serves (23 projects, confirmed tonight), and a second writer would have put 8010's scheduler in reach of the drive's project. The stale 8011 process was from Sep 3 09:06, sixteen hours older than the code under test, and answered /health exactly like a current one; killed and restarted from hub/, from source. Fixture proj-4eb8215f9502 at C:/Users/huida/Documents/aw-ablocked-p6. 6.2 (8/8): four delivery opportunities against a limit of three -- POST /agent/trigger schedules too -- and delivery_attempts never left 0; the remedy reaches THREE surfaces including the Continue button's own 200 body, which the task did not ask for and is the one an operator actually clicks. 6.3 (14/14), the falsifying leg: an ordinary task (workspace_scheme='task', asserted) bound in a second conversation moved the head 0->1 on that same schedule, 3 withdrew it, and the next schedule ran the task turn to completion in .agentweave/tasks/task-e3df8b4ddca8. Note the scheduler RETURNS after withdrawing the head rather than re-draining, so the task turn needs one more schedule -- invisible from the unit tests. 6.4 (11/11): performed the remedy, one Continue, the held message was delivered into the path that had been obstructed and git registered it on agentweave/<agent>. F274 checked in a browser against the served bundle: the conversation view renders the whole refusal, unelided. Both turns billed claude-haiku-4-5-20251001 per turn_usage. No job or loop created; 8011 stopped. Harnesses kept: scripts/drive/setup_ablocked_p6.py, t_ablocked_p6_held.py, t_ablocked_p6_elsewhere.py, t_ablocked_p6_cleared.py. Evidence is written into tasks.md under each of 6.2/6.3/6.4."
    },
    {
      "id": "a-blocked-archive",
      "status": "done",
      "title": "a-blocked task 6.5 -- retire F188 in the ledger, then archive the change",
      "detail": "DONE iteration 11, one commit, four documents plus the archive move; no product code, so the gates were not re-run (iteration 9 measured all five green, iteration 10 drove it). F188's section is headed RETIRED 2026-09-04 and its Status line reads **fixed 467dfea**, stating in prose that the commit writing the line is later than the repair and cannot cite its own sha without a forbidden amend. Under it: the three legs at 33/33, each written against the reproduction's own symptom (withdrawn at 3 with 'delivery failed 3 times' -> queued at 0 through four delivery opportunities), the three surfaces the remedy reaches, the falsifying leg, and the leg where performing the remedy released the message. The (C) sub-finding at the end of that section is retired with it by phase 4, with its test named. INDEX RECOMPUTED rather than decremented: all 40 severity-A headings scanned for their Status line -> the 2026-09-03 rebuild minus F188 (F271 and F274 open, F140/F142/F154/F155 still unverified), with the two limits of the scan written down (it reads the heading and first Status-bearing line, not the section; 'open, filed not fixed' contains 'fixed', so F140/F142 were classified by hand). The stale table row and the 'three -- F271, F188 and now F274' paragraph are marked SUPERSEDED, not rewritten. openspec validate --strict on the change: valid. Deltas folded in by hand -- ADDED appended to workspace-isolation, MODIFIED replacing its predecessor in agent-conversation-workspace -- and git diff reported 122 insertions, 0 deletions across both, which is the check that the merge lost nothing. openspec validate --specs --strict: 43 passed, 0 failed. git mv to openspec/changes/archive/2026-09-04-a-blocked-agent-workspace-holds-its-input; openspec list now shows only a-write. A status note was appended to spec-queue/APPROVALS.md, explicitly labelled as not an operator decision, saying the gate on a-write is open; the operator's APPROVED lines were not touched."
    },
    {
      "id": "a-write-split",
      "status": "open",
      "title": "a-write-outside-the-workspace-is-recorded -- split its 54 tasks before building any of them",
      "detail": "DO NOT START THIS UNTIL a-blocked-archive IS COMMITTED. APPROVALS is explicit: the two changes collide on agent_trigger.py and worktrees.py, and a-write moves hub/hub/static/ui on top -- the one combination this repo cannot build concurrently. 54 tasks across 10 phases will not fit in one firing, so this item IS the split: read tasks.md, write firing-sized items back into this queue, then stop. Two facts verified 2026-09-03 23:0x and not needing a re-check: task 4.2's migration number 0101 IS correct (head is 0100_loop_work_needs_evidence.py), and zero of the 54 tasks are ticked. UI reminder for its later phases: commit hub/ui/src and hub/hub/static/ui together via py -3.11 scripts/refresh_ui_bundle.py after npm run build, and a UI change is driven in a browser against the served bundle or it is not driven (F274)."
    }
  ],
  "decisions_for_user": [],
  "limits": [
    "Stay on the cycle branch. No commits, merges or rebases onto master. Never auto-merge.",
    "Nothing outward-facing: no publish, no release, no PR or issue creation, no force-push, no history rewriting. Push, do not open PRs.",
    "Nothing destructive: no deleting projects, databases, or kept reproductions.",
    "Do not browse the open web. Research is AgentWeaveResearch's job, in a process that keeps the permission classifier.",
    "Every claim is measured or labelled unverified.",
    "Decisions that are genuinely the operator's go to decisions_for_user, not guessed.",
    "Stage explicit paths, never git add -A. Never commit kimichanges.md or kimiwork.md.",
    "Tests under py -3.11, never bare python. black needs --target-version py311.",
    "Never drive against proj-5e960453 or proj-18e5d4e0. Port 8000 is the operator's real usage and must never be touched.",
    "Every real agent turn in a drive binds claude-haiku-4-5. Never leave a job enabled."
  ]
}

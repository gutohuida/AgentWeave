{
    "purpose": "FIX. Read spec-queue/APPROVALS.md, then build: backlog first (unarchived changes, then open findings by severity), then APPROVED rows. Drive every change before closing its queue item. This window does NOT write new proposals.",
    "playbook": ".claude/loops/night-window.md",
    "runner": "claude",
    "model": "opus",
    "permission_mode": "unattended-full-access",
    "branch": "autonomous/2026-09-08-daily",
    "parent_branch": "master",
    "parent_sha": "bdb5a79",
    "stop_at": "2026-09-10T07:00:00+01:00",
    "iteration": 11,
    "log_file": ".claude/autonomous/2026-09-09-night-log.md",
    "findings_file": "scripts/drive/FINDINGS.md",
    "approvals_file": "spec-queue/APPROVALS.md",
    "current": "R34-model-catalog",
    "next_action": "Build R3.4 -- a scripts/ tool that re-reads the Codex model cache. R2 is DONE as of iteration 11 (scripts/check_openspec_collisions.py + tests/test_openspec_collisions.py, 14 tests, five script mutations each killed by a named test, driven against the real archive: 303 files, EXIT 1, 147 multi-change requirements of which 19 silent reverts, agreeing with iteration 10's independent count). Nothing is in flight in openspec/changes/ -- only archive/. THE DECISION, QUOTED FROM DECISIONS.md 'R-3.4', NOT PARAPHRASED, and read it there before building: hub/hub/model_catalog.py names ~/.codex/models_cache.json as its source of truth and NOTHING re-reads it. VERIFIED iteration 1: the only mention under hub/ or src/ is the module docstring at hub/hub/model_catalog.py:34, describing how the literal was derived. The cache is PER-MACHINE and ABSENT IN CI, which is exactly why the verdict makes this a scripts/ tool or a skip-if-missing check and NOT a CI gate -- do not turn it into one. The verdict's own words: 'Doing nothing is defensible; doing nothing silently is what let a phantom default model sit in the catalog for four weeks.' So the deliverable is a tool that COMPARES the committed literal against the cache and SAYS what drifted. REUSE, DO NOT REINVENT: the skip-if-missing pattern already exists at scripts/drive/t_d1_catalog_is_the_only_door.py:458 and scripts/drive/t_sweep_row2_runners.py:98-123 -- read both first. PRECEDENT SET TONIGHT, follow it: a scripts/ tool in this repo gets a test (tests/test_classify_findings.py was written 2026-09-09 for classify_findings.py, and iteration 11 added tests/test_openspec_collisions.py). Use SYNTHETIC fixtures for behaviour -- an assertion against the real per-machine cache is unrunnable in CI and on any other machine -- and if you want one real-corpus assertion make it structural and content-independent, as test_the_real_archive_parses_without_orphans is. Import the script by path with importlib.util (scripts/ is NOT a package); both existing test files show the exact four-line _load() helper. TRAP MEASURED ITERATION 11, do not re-learn it: a default argument like stream=sys.stdout binds the IMPORT-TIME stream, so capsys and redirect_stdout capture nothing -- default to None and resolve at call time. MUTATION-CHECK IT: a tool that cannot report drift is decoration. Point it at a synthetic cache that differs from the literal, confirm it says so and exits non-zero; then make them agree and confirm it goes silent; then DELETE the cache and confirm it SKIPS rather than fails, which is the CI-absent case and the whole reason this is not a gate. State all three in the log. LINT: scripts/ is in NEITHER CI path list (ruff check src/ hub/ tests/; black --check src/ hub/hub/ hub/tests/ tests/) so the script needs no black -- but a test under tests/ IS in both and MUST be black --target-version py311. Run CI-exact ruff and black either way before committing. The CLI suite (tests/) is 36s and MUST be run since the new test lands there; the Hub suite is 27 MINUTES and is only needed if you touch hub/ -- reading hub/hub/model_catalog.py does not count as touching it. pytest-timeout is NOT installed under py -3.11: passing --timeout=N dies in 4 seconds with EXIT=4 and looks like a finished run. No Hub is needed and none should be started; :8012 may still be running from iteration 8 and is harmless, :8011 and :8010 are not this window's, :8000 is never touched. No agent turn, so nothing binds a model and no job is created. If R34 finishes with time left the queue moves to DAY1-constraints.",
    "queue": [
        {
            "id": "compose",
            "status": "done",
            "title": "Compose this window's queue from the playbook",
            "detail": "Done iteration 1. APPROVALS.md's newest section (## 2026-09-09) carries an ORDER: line, so that line IS tonight's queue verbatim and the default backlog-first shape is ignored. Only one unarchived change exists and it is already on the ORDER line, so the archive-first backlog step is empty."
        },
        {
            "id": "F142-drive-operator-leg",
            "status": "done",
            "title": "F142 drive, arm 1: AW_COMPLETE_BY=operator reaches a staffed review",
            "detail": "Done iteration 2, and the answer has a correction in it. AW_COMPLETE_BY=operator can no longer reach F142's arm: F140's repair 1 shipped, the briefing now names update_task, the agent completes its own task 22s into firing 1, and the operator's PATCH to 'completed' on an already-completed task returns 200 while writing NO transition row -- so completion_attribution reads the AGENT's completion and the walk takes scheduler.py:1548, not the operator arm. A new mode, AW_COMPLETE_BY=operator_after_agent, reconstructs the real world (operator walks the task to in_progress, agent works it on a turn BOUND to the task and is told not to move it, operator makes the -> completed transition). Sequence 7 on task-9529de5132cd is in_progress -> completed, actor_kind='operator', actor_agent NULL. THE REPAIR HOLDS, 20/20: firing 2 returned 200, staffed r7bf142b (not the author), task reached 'approved' in 84s, no status histogram. Proved rather than inferred by a second run with one bound agent (AW_ALLOW_NO_REVIEWER=1, 13/13): the 409 carries excluded_because = 'has worked on this task' (scheduler.py:1575, the operator arm) and NOT 'is the one that completed this task' (:1555, the agent arm)."
        },
        {
            "id": "F142-drive-row4-and-close",
            "status": "done",
            "title": "F142 drive, arm 2: row four (no agent ever touched the task), then close the finding",
            "detail": "DONE iteration 3, and it found a defect inside the repair it was closing. Row four ran 19/19 0 BAD on a FOURTH fresh fixture (proj-f741298c9c6b, task-930385ddb031): the agent triggered without task_id so run.task_id stayed NULL and assignee was never written, the operator walked pending -> in_progress -> completed by hand (sequences 16-18 all actor_kind='operator', actor_agent NULL), firing 2 returned 200, a reviewer was staffed and the task reached 'approved'. No status histogram. F142's Status line is now 'fixed f3a778f, DRIVEN AND CLOSED', DRIVE-1's preamble in the archived change document carries both arms' evidence, and the index paragraph has a new dated revision rather than an edited dated line. NEW: F306 (A) filed -- the reviewer staffed was the agent that wrote the code, because agents_that_may_have_authored unions exactly three sources (transitions, assignee, bound runs) and the requirement_evidence table is a fourth it does not read; it approved its own work at sequence 19. NOT FIXED here: it changes who may review, so it needs the round discipline and this window writes no proposals."
        },
        {
            "id": "clearing-1-dialog",
            "status": "done",
            "title": "2026-09-07-clearing-instructions-asks-first, section 1: the dialog",
            "detail": "DONE iteration 4. ClearInstructionsDialog.tsx created (ArchiveConfirmDialog's shape, no type-to-confirm, no isPending prop -- Confirm closes and fires in one act so no second click is reachable and the save acknowledgement in SettingsSection's heading is not covered), and mounted inside {children}'s `data` branch per 1.4. Save was NOT moved out of `actions`. Tasks 1.1-1.4 ticked. Verified in jsdom by a THROWAWAY test file, 7 passed, then deleted so section 4 is not credited with tests of the dialog rather than of the gate; three mutations each killed a NAMED test (trailing-newline strip, scrim target guard, useDialogFocus). NOT DRIVEN and not drivable: nothing sets confirmingClear true until section 2. COLLATERAL, already repaired: naming the project needs useProjects(), which broke instructionsUnreadEditor.test.tsx's F271 retry case -- it counted EVERY getJson call (2 -> 3). Rewritten to count calls on the instructions path only, and mutation-checked by removing Retry's refetch(). Full UI suite 1512/1512, tsc and lint clean."
        },
        {
            "id": "clearing-2-gate",
            "status": "done",
            "title": "clearing-instructions, section 2: the gate",
            "detail": "DONE iteration 5. handleSave is now `if (data !== undefined && data.content.trim() !== '' && content.trim() === '') { setConfirmingClear(true); return }` before the unchanged saveMutation.mutate(content). Tasks 2.1-2.4 ticked. 5.2's evidence was read first, as instructed. 2.3 HELD: no state guard added, Save NOT moved out of actions={data ? ... : undefined}. The `data !== undefined` term is TypeScript narrowing, and is written as the narrowing rather than `data?.content` ON PURPOSE -- with optional chaining the unreachable branch would set confirmingClear and then hit section 1's `{confirmingClear && data && ...}` render gate, giving a dead end with neither dialog nor write; as written it falls through to the ordinary save. 2.4 measured, not assumed: git diff --name-only -- '*.py' is 0 files. Verified by a THROWAWAY test file, 8 passed, then deleted so section 4 is not credited with tests it has not written; it asserted on putJson calls and a stand-in stored row, never on disabled attributes. THREE mutations: removing the whole gate killed 5 of 8, untrimming the outgoing side killed both whitespace-editor tests, untrimming the stored side killed the whitespace-only-stored test. The 3 that survive M1 are the three 'must not be interrupted' cases and are identical either side of the gate by construction -- named in the log so the count is not read as a gap. Full UI suite 1512/1512 / 146 files on the committed tree (1520 with the throwaway), tsc exit 0, lint clean. ruff/black/mypy not required, zero .py touched. NOT DRIVEN: the served bundle is still pre-change."
        },
        {
            "id": "clearing-3-bundle",
            "status": "done",
            "title": "clearing-instructions, section 3: the bundle",
            "detail": "DONE iteration 6. npm run build then py -3.11 scripts/refresh_ui_bundle.py; only that script wrote the stamp. JS asset moved index-CkhnWQRS.js -> index-BvtjVm7p.js, CSS hash unchanged (no CSS touched), and the refresh script replaces assets/ wholesale so the old asset is GONE rather than left beside the new one. 3.2 PROVED, not assumed: old bundle recovered via `git show HEAD:<old path>` and grepped beside the new one -- TWO controls (`Project instructions`, `Loading instructions`) match 1/1 in BOTH, and four dialog literals go 0 -> 1 (`AgentWeave keeps no copy`, `Clear the instructions for`, `clear-instructions-title`, `there is no history and no undo`). src_commit 272adce -> 1492069, src_fingerprint moved. AW_CHECK_UI_BUNDLE=1 pytest hub/tests/test_ui_build_stamp.py = 13 passed, AND THAT TEST WAS MUTATION-CHECKED (two comment lines appended to ClearInstructionsDialog.tsx turned it red; file restored, git diff empty) because a gated test can pass vacuously. It hashes the WORKING TREE hub/ui/src, not a commit, so committing does not move it. No hub/ui/src file changed this iteration, so the bundle commits alone on top of the source it was built from -- the fingerprint test is what enforces the pairing. WIRE-CHECKED on the running :8011 Hub (pid 7360, started BEFORE the rebuild): GET / now names index-BvtjVm7p.js, the old asset 404s, the new one 200s byte-identical to disk and carries the literal. StaticFiles reads per request; NO RESTART NEEDED. Not a drive -- nothing was clicked."
        },
        {
            "id": "clearing-4-unit",
            "status": "done",
            "title": "clearing-instructions, section 4: unit coverage under hub/ui/src/__tests__/",
            "detail": "DONE iteration 7. NEW committed file hub/ui/src/__tests__/instructionsClearConfirmation.test.tsx, 15 tests, 15 passed first run; iterations 4 and 5's throwaways were NOT resurrected. Tasks 4.1-4.8 ticked. Shaped after instructionsUnreadEditor.test.tsx: partial mock of @/api/client (readableApiError real), a stand-in `stored` row so a PUT is a WRITE and not only a call, vi.clearAllMocks() first in beforeEach, and the real useProjects left in place (routed through mocked getJson) because naming the project is task 1.2. ASSERTED ON THE WIRE throughout, never on disabled attributes. SIX MUTATIONS, each applied alone and reverted, each killing a NAMED test: M1 revert the gate (the 4-line if block out of handleSave, not the dialog) -> 12 of 15 die and THE 3 SURVIVORS ARE CORRECT, named in the log -- ordinary non-empty save, empty editor over already-empty stored, empty editor over whitespace-only stored, all 'must not be interrupted' and identical either side by construction (4.5 says so in tasks.md). M2 untrim outgoing -> 2 die. M3 untrim stored -> 1 dies. M4 drop the trailing-newline strip -> 4 die. M5 drop the scrim target guard -> 1 dies. M6 drop the singular branch -> 2 die. Both component files restored from pre-mutation copies; git diff on tracked files empty. NOT tested here, deliberately and documented in the file: useDialogFocus's Tab cycle and focus restoration, which tasks.md assigns to 5.4 because jsdom cannot prove them. Full UI suite 1527/147 files (1512/146 + 15 + 1, so nothing else moved), tsc exit 0, lint clean, AW_CHECK_UI_BUNDLE=1 test_ui_build_stamp.py 13 passed and UNMOVED -- checked because the fingerprint hashes hub/ui/src, and ui_source_fingerprint (hub/hub/main.py:85-86) excludes __tests__, so a test file cannot unpair the bundle. ruff/black/mypy not required, MEASURED: git diff --name-only -- '*.py' is 0 files. NOT DRIVEN."
        },
        {
            "id": "clearing-5-drive",
            "status": "done",
            "title": "clearing-instructions, section 5: the drive -- this is what closes the change",
            "detail": "DONE iteration 8. THE CHANGE IS DRIVEN. NEW file scripts/drive/t_d9_clearing_instructions_postchange.py, the deliberate mirror of t_d8's pre-change run, 59 passed / 0 failed in real Chromium against the SERVED bundle. Tasks 5.1, 5.3, 5.4, 5.5 ticked. Leg 0 refuses to trust the run unless the JS asset the Hub actually serves contains the dialog's own words, so a stale bundle cannot go green (the F274 guard, in the file). 5.1 held in order: (a) dialog on screen, ZERO PUTs counted by a page.route interceptor, row read back over the API BYTE-IDENTICAL at 1606 chars, dialog naming the project and saying '42 lines' and 'keeps no copy'; (b) Cancel -> still 0 PUTs, still byte-identical, editor still empty as typed; (c) Confirm -> EXACTLY ONE PUT carrying {'content': ''} and the row finally ''. No assertion anywhere reads a disabled attribute, and the 42 is hardcoded with its arithmetic in a comment rather than computed, so it is not a transcription of the component's expression. 5.3: a FRESH Hub on :8012 from source from hub/, own empty DB at %TEMP%/aw-d9-drive/aw.db migrated to head 0102 (NOT 0101 -- CLAUDE.md is one migration stale); fixture proj-1ce9f451cb6e deleted, 204, and GET /projects then returned count 0. :8011 was left alone, :8010 untouched, proj-d85a82bf4216 in a PROTECTED set. Legs G1-G4 re-ran t_d8's D/E/F post-change: the newline near-miss NOW ASKS and the confirmed save writes '\\n' UNTRIMMED, while whitespace-only stored, already-empty stored and the ordinary save are all uninterrupted at one PUT each. 5.4 DRIVEN AND TWO OF THREE CLAUSES HOLD: Escape cancels writing nothing, and FOCUS RETURNS TO SAVE (the clause section 4 could not prove) -- but the Tab clause is wrong about the product, and that is F307. FIRST RUN WAS 56/2 AND THE SECOND FAILURE WAS THE HARNESS: leg G4 read the 2000ms ack flash at ~2100ms (click_save's 1500ms settle plus a fixed 600ms wait) and reported a correct page as silent -- exactly the trap t_d8's docstring warns about. Fixed properly, not tuned: ack() now POLLS at 200ms and returns as soon as a status appears; re-run showed 'ack seen ~0ms'. Lint: one .py changed (the harness), ruff clean and CI-exact ruff clean; black WOULD reformat and deliberately does not, because scripts/ is in no CI path list and the sibling t_d8 harness is equally unformatted. No .ts/.tsx changed so the UI suite was not re-run."
        },
        {
            "id": "clearing-6-close",
            "status": "done",
            "title": "clearing-instructions, section 6: lint, sync-specs, archive",
            "detail": "DONE iteration 9. THE CHANGE IS CLOSED. 6.1: npm run lint clean and npx tsc --noEmit exit 0, both from an absolute hub/ui with pwd echoed in the same call. The negative claim was MEASURED, not waved past: git diff --name-only e6da0a0..HEAD touches exactly ONE .py file across the whole change (scripts/drive/t_d9_clearing_instructions_postchange.py). ruff required and passes (CI-exact ruff check src/ hub/ tests/ = All checks passed). black NOT required -- CI's paths are src/ hub/hub/ hub/tests/ tests/ and scripts/ is in none; CI-exact black --check ran anyway: 558 files unchanged. mypy NOT required -- no src/ file modified. The harness stays unformatted on purpose, matching its sibling t_d8. 6.2: hand-merged by a script asserting each replacement matched EXACTLY ONCE (4 replacements, 4 OK lines). The ADDED requirement is placed at line 131, immediately ABOVE 'Save cannot write instructions that were never read' (line 200) and NOT at the end, because its own prose says 'which the requirement below already states'. A SECOND delta-relative phrase the task did not name was caught by the same reasoning: 'The requirement above states this in its own words' could only have meant its own SHALL line (nothing sits above it in the delta), and with a neighbour above it in the shipped file it would read as a false claim about that neighbour -- now 'The statement above says this in its own words'. BOTH MODIFIED SCENARIOS GAINED THE SENTENCE and this was proved the way the task demands: the two wrap differently (one long line vs three wrapped lines) so a literal grep finds one and misses the other, so the check normalises whitespace first -- SHIPPED 2 hits, DELTA 2 hits, equal counts is the assertion. Narration dropped: 'in this change' occurs 0 times in the shipped spec; 'below' restored and 2026-09-06-an-unread-editor-cannot-overwrite absent. openspec validate --type spec --strict = valid. OTHER SPECS: none, measured a THIRD time -- the six non-project-instructions specs mentioning instructions return nothing for 'Instructions screen'/'instructions editor'/'instructions textarea', all charter context. ARCHIVED with --skip-specs -y and the flag was PROVED rather than trusted: md5sum of the synced spec is 1131fa8b465eff1f03c027e54fcbbeca both before and after, byte-identical. Validation still ran (--skip-specs and --no-validate are separate flags): 'Task status: Complete'. One non-blocking warning left alone: proposal.md has no ## What Changes section, a shape complaint about a now-archived document. THE ARCHIVE DIRECTORY IS DOUBLE-DATED -- archive/2026-09-09-2026-09-07-clearing-instructions-asks-first -- because openspec prefixes with the UTC date and this ran at 00:59 local / 23:59 UTC; left as the tool produced it, since renaming makes it unreproducible. THIS CHANGE CLOSES ZERO FINDINGS, acted on rather than merely noted: F271 was fixed by 2026-09-06-an-unread-editor-cannot-overwrite and is the DEPENDENCY, F296 is the separate unowned item 5.5 forbade folding in and is still open, F307 was FILED by iteration 8's drive and is pre-existing. No 'fixed <sha>' line written for any of the three and FINDINGS.md was not touched. Two durable facts appended to DEAD-ENDS.md: the --skip-specs rule, and openspec archive's UTC date prefix."
        },
        {
            "id": "R1-ratchets",
            "status": "done",
            "title": "R1 -- three ratchet checks that freeze today's count as a ceiling",
            "detail": "DONE iteration 10. THREE RATCHETS BUILT, and every ceiling MEASURED this iteration rather than copied: hub/tests/test_surface_ceilings.py (clientless routes <= 35, unhandled query call sites <= 100, operator-reachable MISREPORT <= 52) and hub/tests/test_dependency_ceilings.py (fastmcp declared in <= 3 places, starlette in <= 1, each carrying its bound). TWO OF THE THREE CEILINGS HAD MOVED since the iteration-1 numbers: declared route pairs 187 -> 188 (clientless still 35), and MISREPORT 51/54 -> 52/55 -- the +1 is InstructionsPage.tsx:43 useProjects, added by ITERATION 4 OF THIS WINDOW, so a check frozen at 51 would have been red on its first run. NOTHING WAS REPAIRED: the 35 routes and 52 surfaces are untouched, per R-1's 'Existing instances are not repaired'. PLACEMENT: both in hub/tests/ on merits (their subjects are the Hub route table and the Hub UI, and hub/tests runs once where the root suite runs six times); APPROVALS.md's premise re-confirmed FALSE -- declared_routes() spawns a subprocess with cwd=REPO/'hub'. The gate reads the report's own functions (the tests/test_skill_sync.py model): n10 gained route_rows()/clientless_routes(), n11 gained unhandled_sites()/operator_reachable_misreports(), main() calls them, and both scripts print byte-identical summaries after the refactor. FOUR MUTATIONS, each alone, each reverted: M1 a new clientless route -> 36>35 RED; M2 a second unbound useProjects() call -> 101>100 RED; M3 a new MISREPORT classification -> 53>52 RED; M4 a fourth unbounded fastmcp declaration -> 4>3 RED. TWO SAID MORE THAN 'it fires': under M2 the MISREPORT test PASSED (a new offender lands in UNCLASSIFIED), which is why the unhandled-sites assertion was added beside it -- the MISREPORT ceiling alone CANNOT catch a new offender and FALLS when a line insert re-keys n11.CLASSIFIED; under M4 test_fastmcp_api_contract.py PASSED, which is the gap the new file closes (_fastmcp_specifiers reads three NAMED keys). starlette<2.0 had no assertion anywhere before tonight. CI-exact ruff and black clean; both new files are in CI's paths."
        },
        {
            "id": "R2-archive-collision",
            "status": "done",
            "title": "R2 -- a script that warns when two changes modify the same requirement",
            "detail": "DONE iteration 11. scripts/check_openspec_collisions.py + tests/test_openspec_collisions.py (14 tests). Built AS THE DECISION WRITES IT: a script under scripts/, nothing calls it, NO SKILL FILE TOUCHED -- the two rejected alternatives (the openspec-archive-change skill, upstream openspec) stayed rejected, and the recorded weakness ('only fires if whoever archives remembers to run it') is left in place deliberately because the verdict says an unrun script is evidence for moving it into the skill, which wiring it tonight would have destroyed. Key is (capability, requirement name); change and capability derived with path.relative_to(changes_dir).parts so both layouts work -- the iteration-10 bug is now test_change_and_capability_are_read_relative_to_the_scan_root. ALL FOUR section pairings are reported (the set is unapplied, so order is undecided); MODIFIED+MODIFIED is labelled SILENT REVERT and sorted first because it is the only one that leaves no trace. DRIVEN: archive scan is 303 files, EXIT 1, 147 multi-change requirements of which 19 are silent reverts -- the 19 AGREES with iteration 10's independent hand count. Both named fixtures verified individually, including the n=3 case. Default target (openspec/changes, archive/ excluded) is 0 files EXIT 0: nothing in flight. MUTATED: two-change %TEMP% fixture warns EXIT 1; deleting one MODIFIED block drops silent reverts 1->0; deleting the shared section goes fully silent EXIT 0; restoring brings the warning back. Five mutations of the script each killed a NAMED test and were reverted (script diffs identical to pristine). FOUND AND FIXED a real latent defect en route: report(stream=sys.stdout) bound import-time stdout as a default argument, so redirect_stdout and capsys captured nothing -- now stream=None resolved at call time. CLI suite 475 passed / 3 skipped / 0 failed; CI-exact ruff and black clean. Hub suite NOT run and labelled unverified -- nothing under hub/ was touched."
        },
        {
            "id": "R34-model-catalog",
            "status": "current",
            "title": "R3.4 -- a scripts/ tool that re-reads the Codex model cache",
            "detail": "From DECISIONS.md R-3.4. model_catalog.py names ~/.codex/models_cache.json as its source of truth and NOTHING re-reads it. VERIFIED iteration 1: the only mention under hub/ or src/ is the module docstring at hub/hub/model_catalog.py:34, describing how the literal was derived. The cache is per-machine and absent in CI, so this can only be a scripts/ tool or a skip-if-missing check -- that was the verdict and it is why this is NOT a CI gate. scripts/drive/t_d1_catalog_is_the_only_door.py:458 and scripts/drive/t_sweep_row2_runners.py:98-123 already contain the skip-if-missing pattern; reuse it. Verdict's own words: 'Doing nothing is defensible; doing nothing silently is what let a phantom default model sit in the catalog for four weeks.'"
        },
        {
            "id": "DAY1-constraints",
            "status": "open",
            "title": "DAY-1 -- a development constraints file pinned to CI's resolution",
            "detail": "From DECISIONS.md DAY-1. Pin starlette 1.6.0 and fastapi 0.141.1; leave hub/pyproject.toml's PUBLISHED range (starlette<2.0 at :32, fastapi>=0.110 at :12) UNTOUCHED. Two binding conditions from the verdict: it is DEVELOPMENT-ONLY and not a second source of truth for what the Hub supports, and BOTH the CI job AND CLAUDE.md's documented local commands must install through it -- a constraints file nothing installs through is decoration, and the drift it exists to stop returns silently. This is what would have caught the 2026-09-09 starlette defect before a push instead of thirteen commits later. OUT OF SCOPE TONIGHT, DELIBERATELY: the missing guard against a fourth app.routes occurrence -- a naive grep false-positives on the three files whose comments document the trap, hub/tests/_routing.py included. Unowned, and not this window's to invent."
        },
        {
            "id": "F306-note-to-the-day-window",
            "status": "note",
            "title": "F306 (A) -- NOT this window's to build. A note to tomorrow's FILL window.",
            "detail": "Filed iteration 3 in scripts/drive/FINDINGS.md, measured on a live Hub, open. An agent that recorded accepted 'implementation' evidence naming a task can be staffed to review that task and approve its own work, whenever the operator completed it: agents_that_may_have_authored (hub/hub/task_transition_service.py:253-283) unions transitions, assignee and runs bound to the task, and the requirement_evidence table is a fourth record it does not read; _guard_author_is_not_reviewer (:286-311) misses it too because it compares agent_that_completed, which an operator completion leaves NULL. The branch's own comment at scheduler.py:1566-1571 says exclude=set() is a self-approval route -- the widening returned set() anyway. TWO FIX SHAPES ARE SKETCHED IN THE FINDING AND BOTH ARE LABELLED GUESSES: add the evidence table as a fourth source scoped by kind (a naive union makes a task's SECOND review unstaffable, because the first reviewer's own evidence would exclude them), or have the transition guard fall back to the exclusion set when agent_that_completed is NULL. Whoever proposes this must first read what else writes requirement_evidence rows. It reaches the day window through 'git diff on FINDINGS.md', which day-window.md step 3 already reads; this row exists so it is a queue item and not only a paragraph. DO NOT BUILD IT TONIGHT."
        },
        {
            "id": "F307-note-to-the-day-window",
            "status": "note",
            "title": "F307 (B) -- NOT this window's to build. A second note to tomorrow's FILL window.",
            "detail": "Filed iteration 8 in scripts/drive/FINDINGS.md, measured in a real browser against the served bundle, open. A confirm-only dialog's FIRST Tab leaves the panel: useDialogFocus (hub/ui/src/hooks/useDialogFocus.ts) never moves focus into the panel when it activates and traps Tab only when document.activeElement is the panel's first or last focusable, so focus left on the trigger falls through to native DOM order. On the Instructions screen press 1 lands on the textarea BEHIND THE SCRIM -- focused, invisible, editable -- and presses 2-5 then cycle correctly. Rated B not C because there is a path to a wrong write: Tab once, type unseen, Cancel, Save. SIX components use the hook; three dodge it by accident because they autoFocus an input inside the panel (AgentCreateDialog:204, DeleteProjectDialog:67, ProjectManagerModal:140) and the two affected are exactly the confirm-only ones, ArchiveConfirmDialog (which shipped this way long before) and ClearInstructionsDialog (shaped after it, inheriting by construction). NOT FIXED and not this window's: which control takes focus on open changes all six call sites, and for a destructive dialog it is a real design question since the panel's first focusable is Cancel and its last is 'Clear instructions'. Two shapes sketched in the finding, both labelled guesses. It is KEPT AS A REPRODUCTION in scripts/drive/t_d9_clearing_instructions_postchange.py leg E, which asserts both the closed cycle and the escape, so that drive exits 0 meaning 'the change's contract holds and F307 is unchanged' -- FIXING F307 WILL FAIL THAT FILE, which is the correct moment to revisit it. DO NOT BUILD IT TONIGHT."
        }
    ],
    "decisions_for_user": [],
    "limits": [
        "Stay on the cycle branch. No commits or rebases onto master, ever, by any window. The day window's merge gate (day-window.md, Iteration 1 step 1) may fast-forward master to this branch with 'git merge --ff-only' when all four gate conditions hold -- relaxed by the operator 2026-09-06. No other step, and no other window, may merge, rebase, or commit onto master.",
        "Nothing outward-facing: no publish, no release, no PR or issue creation, no force-push, no history rewriting. Push, do not open PRs.",
        "Nothing destructive: no deleting projects, databases, or kept reproductions.",
        "Do not browse the open web. Research is AgentWeaveResearch's job, in a process that keeps the permission classifier.",
        "Every claim is measured or labelled unverified.",
        "Decisions that are genuinely the operator's go to decisions_for_user, not guessed.",
        "Stage explicit paths, never git add -A. Never commit kimichanges.md or kimiwork.md.",
        "Tests under py -3.11, never bare python. black needs --target-version py311.",
        "Never drive against proj-5e960453 or proj-18e5d4e0 (both deleted in the 2026-09-07 clean slate). This repo's own registered project is proj-d85a82bf4216 and must never be a drive target either. Port 8000 is the operator's real usage and must never be touched; :8010 is the other trial Hub and is not this window's. Drive on :8011 or another spare port -- iteration 8 used a fresh :8012 and left :8011 as it found it.",
        "Every real agent turn in a drive binds claude-haiku-4-5. Never leave a job enabled.",
        "ONE bundle-touching change per night, and tonight it is 2026-09-07-clearing-instructions-asks-first. Nothing else on the ORDER line touches hub/hub/static/ui.",
        "A UI change is driven in a browser against the served bundle or it is not driven. A transcription of the component into another language is not a drive.",
        "The last four ORDER items (R1, R2, R34, DAY1) are LAST for a reason. F142 and the approved change come first and neither may be shortened to reach them. If the window ends with any of the four untouched, that is the CORRECT outcome, not a miss."
    ]
}

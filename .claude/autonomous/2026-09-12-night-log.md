# 2026-09-12 night window

Newest entry at the BOTTOM. Playbook: `.claude/loops/night-window.md`.

Armed 2026-09-12T22:55:04+01:00 on `autonomous/2026-09-12-daily` (parent master@eac213c), model opus.

---

## Iteration 1 — compose (2026-09-12 23:00 → 23:05)

**Position confirmed, no reconciliation.** Branch `autonomous/2026-09-12-daily`, `HEAD` = `c12a7d3`
(*arm(night)*), tree clean. `STATE-night.json`'s `parent_sha` `eac213c` is `master` per the
2026-09-12 APPROVALS note (the merge gate landed `6f7e486..eac213c` this morning). No branch cut.

**APPROVALS.md, 2026-09-12 section (the newest):** two `APPROVED` rows and an `ORDER:` line.
- `a-url-is-not-a-path`: F300, F312, F321 and F323, all severity A. 46 tasks, 0 ticked.
- `a-refused-review-leaves-nothing-behind`: F319 (A) and F320 (B). 52 tasks, 0 ticked.
- `ORDER: a-url-is-not-a-path, a-refused-review-leaves-nothing-behind`

**The ORDER line is tonight's queue**, per the playbook, so the default backlog is not consulted
for work. It was measured anyway, for the notes. `openspec/changes/` holds exactly these two
non-archive changes, so nothing is waiting to be archived. `scripts/classify_findings.py` reads
eight open severity-A findings:
- six belong to the two approved changes: F300, F312, F321, F323, F319;
- three have no proposal: F299, F301 and F325. They go to `notes_for_tomorrow`.

**The operator's sizing rules are encoded in the queue's gates, not paraphrased.**
- Build `a-url-is-not-a-path` first, and finish it.
- `r1-pin` starts only with at least 3 hours left.
- The r-change's sections go in the operator's order: §1, §2, mutations 4.1–4.4c, §3, the remaining
  mutations, §7, §5, §8.
- No drive with less than 75 minutes left.
- No close-out without the drive.
- Each drive gets its own profile, `drive0913u` or `drive0913r`, and its own free port.

**Tree green before building on it: measured.** I ran the relevant chunk: every test file either
change edits or names (the list is in `STATE-night.json` `baseline_chunk`). Result: **153 passed,
1 skipped, 1m48s**, under `py -3.11` from `hub/`, at `c12a7d3`.

**CI on the branch: two reds today, and neither is its commit's.** Each was classified by its
signature, per DEAD-ENDS §pytest:
- `34711633220` (`681737a`, doc-only): `ERROR test_reviewer_is_not_the_author.py::test_a_wedged_review_is_restaffed_to_a_real_reviewer`,
  `database is locked`. That is **F292**. Result: 4053 passed, 1 error.
- `34716889283` (`5bc00c9`, F329): `FAILED test_flow_holds_the_loop_requirements.py::test_a_wide_flows_state_is_still_one_call`,
  `Lock … is bound to a different event loop`. That is **F314**. Result: 4056 passed, 1 failed.
  F329's change does not touch the flow-state code path. That is inferred from the signature and
  was not re-run.

The arming commit's CI (`34721298030`) was still in progress at compose. Run `34714440450` on
`5ea0b98` has shown `in_progress` for about two and a half hours (started 19:31Z; `gh` read 2h28m at 21:55Z). It looks wedged, but nothing
depends on it. It is noted and not touched.

**Ports at compose:** only `127.0.0.1:8000` is listening, the operator's instance, pid 3488. 8010
(the trial Hub) is down. The drive ports are chosen at drive time, not now.

**The queue**, eighteen items. It is ordered so that stopping anywhere leaves committed, coherent
work:
- `compose` (done)
- `a-url-is-not-a-path`: `u1-pin` (§1) → `u2-reader` (§2) → `u3-wire-docs` (§3, §4) →
  `u4-mutations` (§5) → `u5-gate` (§8) → `u6-drive-prefix` (§6.1) → `u7-drive-fixed` (§6.2–6.4) →
  `u8-close` (§9, §10)
- the **3-hour gate**
- `a-refused-review-leaves-nothing-behind`: `r1-pin` → `r2-rollback` (the green stopping point) →
  `r3-mut-a` → `r4-loop` → `r5-mut-b` → `r6-gate` → `r7-drive-prefix` (behind the **75-minute
  gate**) → `r8-drive-fixed` → `r9-close`
- `w-final`

`prefix_sha` is `c12a7d3`. Every commit before `u2-reader`'s, and before `r2-rollback`'s, carries
the same product Python.

One trap is written into `r6-gate` in advance. Task 7.4's non-goal diff names
`hub/hub/mcp_server.py`, which `a-url-is-not-a-path` legitimately changes earlier the same night. So
7.4 must diff from `r1-pin`'s parent, not from `c12a7d3`. Otherwise it reports the first change's
work as a breach of the second's non-goals.

---

## Iteration 2 — u1-pin: `a-url-is-not-a-path` §1, the table pinned (2026-09-12 23:08 → 23:25)

**Position confirmed, no reconciliation.** `HEAD` was `1682858` (the log correction on top of
compose), the tree was clean, and `current` was `u1-pin`, as STATE claimed.

**What was built.** `hub/tests/test_permission_approver.py` gains three tests, and nothing under
`hub/hub` changed:
- `test_the_decided_table` holds 88 rows on every platform, plus 9 Windows or 7 POSIX rows (the
  `\` class and Z1–Z3). The ids are D2's labels.
- `test_a_fetch_is_not_governed` is N8.
- `test_the_run_s_own_hub_is_nobody_s_without_hub_url` is H12, over H1–H4.

The rows are every D2 row, plus E8, E10, E12, E17, E19, S3, J5 and J6. Those are `table.py`'s
rows, which D2 names only as variants; they are pinned by answer only. One new row, `F331`, is
described below.

**Windows result (measured).** The file gives 86 passed, 1 skipped, 59 xfailed. The baseline chunk
gives **194 passed, 1 skipped, 59 xfailed**, where compose measured 153 passed and 1 skipped.
`testbed/scratch/night0912/why_each_row.py` prints which assertion each row fails today:
- the rows that fail on the **answer** are exactly §1.4's answer-moving list;
- the rows that fail on the **reason** alone are exactly its new-reason list;
- every E row except E7 and E14 passes its answer.

**The table is Windows-only, and measuring POSIX changed four marks.** CI runs Linux. So the same
checker, and then pytest itself, ran under **WSL Ubuntu** (Python 3.12.3). WSL has pytest and
fastmcp; `testbed/scratch/night0912/posix_stubs.py` stubs the two Hub imports the file needs. WSL
gives **47 passed, 52 xfailed, 0 disagreements**. On POSIX, today's regex reads `https://…` as
`s://…` and `file:///…` as `e:///…`. Those are relative paths there, so they are allowed. The
consequences:
- **H4, H15 and H16** pass today on POSIX, so their xfail is Windows-only.
- **X8** raises `ValueError` on POSIX today (D5, *Totality*), so it is a POSIX-only xfail with
  `raises=ValueError`.
- **H12's H4 case** is allowed on POSIX today, so it is a POSIX-only xfail.
- **R10, N1, N2, N7, H5–H7, H13 and H14** keep their marks, but on POSIX they fail on the answer.

Recorded in `tasks.md` under §1.4 and in `design.md` D2 (the *today* column is Windows).

**F331 filed (A), measured.** On Linux, today's `_decide` allows each of these:
- `curl -T notes.md file:///tmp/f331_stray.txt`. curl 8.5.0, run in the same WSL shell, exited 0
  and **wrote `/tmp/f331_stray.txt`**: an escape from the workspace on the Docker deployment's
  platform;
- `curl file:///etc/passwd`;
- `curl https://example.com/x`;
- `curl http://169.254.169.254/…`.

The F331 row is a POSIX-only strict xfail. So CI's Linux job is where the fix will be seen to land.
F331 also corrects F300 (*"yields the candidate `//example.com/x`"*) and F312 (*"no … network
request"*). Both are true on Windows only. This was **not** driven through a live run.

**Friction worth recording.** Every *today* cell in D2 was measured three times, by R1, R2 and R3,
all on one Windows machine. Nobody asked what CI's platform answers. The pins would have gone red on
Linux the moment S2 landed: H4, H15 and H16 would XPASS under the strict marks.

**Deviations from the task text, each measured, none bending a test:**
- Z2 on POSIX: task 1.1 says *"keep today's answer"*. Today's answer there is `allow`, not `deny`.
- H2, H2p and W6 are read as the `PowerShell` tool, as `table.py` and `table3.py` read them.
- H11's reason is pinned as containing `/api/v1/agent-actions/tasks` and *is outside your
  workspace*, not as one exact quotation. D2 says only *"unchanged"*. R2's reader quotes the whole
  continuing argument, so an exact pin would have decided that for §2.

**Next:** `u2-reader`.

---

## Iteration 3 — u2-reader: `a-url-is-not-a-path` §2, the reader (2026-09-12 23:30 → 23:55)

**Position confirmed, no reconciliation.** `HEAD` was `00a5569` (u1's pin), the tree was clean,
and `current` was `u2-reader`, as STATE claimed.

**What was built.** `hub/hub/mcp_server.py` gains D1's reader beside `_decide`:
- the lexer, in the `Bash` or `PowerShell` dialect, or both for any other tool;
- the word split, with each word's "continues" flag;
- the six rules, in order, with the D4 own-Hub test;
- D5's three texts, verbatim;
- `MAX_REASON_CHARS = 1000`, restated beside `MIN_WAITING_SECONDS`, and a 200-character bound on
  the rendered quotation;
- `(OSError, ValueError)` totality.

Rule 6's backstop is `_ABSOLUTE_PATH_RE`, rewritten: `\`-aware on Windows, and the exact old regex
on POSIX. The docstring and the `command`-branch comment are rewritten (2.6), and the *"Relative
paths are left alone"* sentence is gone. Nothing imports outside stdlib and fastmcp.
`approve_tool_call` is untouched: no annotation, and `test_response_carries_no_structured_content`
passes. Tasks 2.1–2.7 are ticked, with a measured note under 2.7.

**Verified (measured).**
- **Windows.** The file gives 176 passed and 1 skipped (the symlink test). The baseline chunk
  gives **285 passed, 1 skipped, 0 xfailed**; u1 measured 194 passed, 1 skipped, 59 xfailed.
  `why_each_row.py` was run before any marker came off: all 59 marked rows passed, and no unmarked
  row failed.
- **WSL Ubuntu.** 170 passed, 1 skipped, 2 failed. Both failures are the stub environment's
  `sse_starlette.event` import, in `test_generated_context_…` and `test_codex_posture_mapping`.
  Neither reaches `_decide`. The skip is the new cap test, which cannot import the Hub's API there.
  `why_each_row.py` reports 0 disagreements over 98 rows.
- `grep -c xfail` on the test file gives **0**.
- ruff and black are clean on both files.

**The one real defect found: the design's reader opened an escape (E20–E22).** R2's lexer turns a
quoted or escaped `$` into a plain `$`. So `'$HUB_URL'/../../x` became a trusted reference, and
the Hub's URL (three components) was put in its place, which leaves the two `..` inside. Bash
treats that `$HUB_URL` as one literal directory name, so the same `..` leave the workspace.
Measured in Git Bash 5.2.37: `'$HUB_URL'/…`, `\$HUB_URL/…` and `"\$HUB_URL/…"` each wrote outside,
rc=0. R2's `reader.py` and R3's `reader3.py` both **allow** all three. The old regex refused them.

The fix is narrow. The lexer marks a `$` the shell will not expand with a sentinel, so rule 2
needs a real `$`. Rule 3 still counts the sentinel as an expansion, so no new allow exists, and
D10 item 7's refinement is not taken up. Rows E20–E22 are pinned. With the sentinel removed, all
three fail. That was checked by mutation now, and it is **a seventeenth mutation for u4**.

**Three smaller departures from R2's code**, recorded in design **D11a**:
1. **References follow the dialect (H17).** A bare `$HUB_URL` under PowerShell is not the
   environment's value. Measured: `Write-Output $HUB_URL/x` printed `/x` with `$env:HUB_URL`
   set. R2's reader allowed H17.
2. **A continuing word's refusal quotes the whole argument only when the extension decided it.**
   So H11 keeps D2's *"unchanged"* reason.
3. **Nesting past 8 falls back to the whole-text backstop.** R2 skipped it and **allows** fifty
   levels of `$(…)` hiding `cat /etc/x`, measured. The file-tool branch also gets the bounded
   quotation.

**F332 (A) filed, measured under WSL.** On POSIX, `echo hi > $'..\x2fansic_stray.txt'` wrote in
the workspace's parent under bash 5.2.21. The old regex and the new reader both allow it: no `/`
appears in the text. It is pre-existing, and not this change's to close. It is in
`notes_for_tomorrow`.

**A tooling trap, hit and repaired, and now in DEAD-ENDS.** I ran the E20 mutation with a
PowerShell read and write, and a careless `git checkout` in between. That round-tripped
`mcp_server.py` through cp1252, and 92 characters became mojibake. The tests still passed, and
only `git diff --stat` showed it. It was reversed exactly with `testbed/scratch/night0912/enc_fix.py`:
90 em dashes back, 0 mojibake, and the diff shows only the intended removals. **u4 must mutate
with `py -3.11` and restore with `git checkout` only from a committed tree.**

**Not done here:** §3's wire test through a real spawn, and §4's docs. Both are u3.

**Next:** `u3-wire-docs`.

## Iteration 4 — u3-wire-docs: `a-url-is-not-a-path` §3 wire shape, §4 docs, 8.3 (2026-09-12 23:55 → 2026-09-13 00:02)

**Position confirmed, no reconciliation.** `HEAD` was `612b9c9` (u2's reader), the tree was clean,
and `current` was `u3-wire-docs`, as STATE claimed.

**3.1: the answer Claude receives, through a real spawn.** `_call_tool_over_stdio` gains an
optional `env_overrides`. Two new tests go through `_shell_answer_over_stdio`, which sets
`HUB_URL=http://127.0.0.1:8016` and an empty `AW_PERMISSION_POSTURE` in the child:
- `test_a_network_address_is_refused_on_the_wire`
- `test_the_run_s_own_hub_is_allowed_on_the_wire`

Each asserts no `structuredContent` and no `isError`.

**Measured: the two tests are not vacuous.** A scratch runner,
`testbed/scratch/night0912/u3/prefix_wire.py`, points `MCP_SERVER` at c12a7d3's `mcp_server.py`.
Both new tests **fail** there, and the existing `test_response_carries_no_structured_content`
control passes. The answers on the wire, verbatim:

| server | command | answer |
|---|---|---|
| c12a7d3 (pre-fix) | `curl -s https://example.com/x` | `deny` — `Denied: 's://example.com/x' is outside your workspace.` |
| c12a7d3 (pre-fix) | `curl -s "$HUB_URL/api/v1/agent-actions/tasks"` | `deny` — `Denied: '/api/v1/agent-actions/tasks' is outside your workspace.` |
| working tree | `curl -s https://example.com/x` | `deny` — `Denied: 'https://example.com/x' is a network address. Under this posture…` (D5 verbatim) |
| working tree | `curl -s "$HUB_URL/api/v1/agent-actions/tasks"` | `allow`, with `updatedInput` unchanged |

None of the four carried `structuredContent`. The pre-fix `'s://…'` quotation on Windows is F312's
symptom exactly.

**4.1: the docs paragraph.** In `docs/reference/permission-postures.md`, *"A shell command declares
no path"*, the sentence *"reads absolute paths out of the command text"* is replaced with what D1
reads:
- the shell's reading of the command;
- relative and absolute paths, resolved against the workspace;
- run-time expansions, refused rather than guessed;
- *"a boundary, not a sandbox"*, kept, with the reason.

One further sentence says that only the run's own Hub may be named, and that this is a rule about
the command's text, not a network boundary. `curl example.com` is not recognised, and `WebFetch`
is not checked. **Every claim in the paragraph was checked against `_decide` before it was written
(measured):** `curl example.com` is allowed, `curl -s https://example.com/x` gets the network
refusal, `~/x/y` gets cannot-be-checked, `sub/../../x` is outside, a `WebFetch` to example.com is
allowed, and `"$HUB_URL/…"` is allowed. The page keeps its per-posture framing.
`hub/tests/test_a_write_outside_the_workspace_is_recorded.py`, which reads the page, gives 8 passed.

**8.3.** `openspec validate --strict a-url-is-not-a-path` reports *valid*, both before and after
the tasks.md edit. No delta was edited.

**Verified (measured, Windows).**
- `hub/tests/test_permission_approver.py`: **178 passed, 1 skipped**. u2 measured 176 and 1.
- ruff and black (`--target-version py311`) are clean on the test file.

The docs page has no lint. `mkdocs build` was **not run**, and the edit adds no link or nav entry.

**Not run here:** WSL for the two wire tests. They spawn `sys.executable` with fastmcp, and u2 found
WSL's stub environment lacking pieces. CI's Linux job is where they will first run on POSIX. Note
for u8, **inferred from u1's note and not re-measured**: on POSIX the pre-fix reading of
`https://…` differed from Windows, so the pre-fix answer there may not have been this deny. The
fixed answer should be D5's text on both platforms, because rule 1 is not platform-scoped. CI's
Linux job is the measurement.

**Next:** `u4-mutations`.

## Iteration 5 — u4-mutations: `a-url-is-not-a-path` §5, seventeen mutations (2026-09-13 00:05 → 00:15)

**Position confirmed, no reconciliation.** `HEAD` was `5b6f393` (u3's wire and docs), the tree was
clean, and `current` was `u4-mutations`, as STATE claimed.

**Method.** `testbed/scratch/night0912/u4/mutate.py` applied each mutation alone to
`hub/hub/mcp_server.py`. The file was read and written as UTF-8 through `open(..., newline="")`, and
each edit was asserted to match exactly once. The whole of `hub/tests/test_permission_approver.py`
then ran with a JUnit report, and the file was restored by `git checkout` from the committed tree.
`git status --short` was checked empty after every restore, and the harness stops if it is not. It
never stopped. There was no PowerShell round trip, per DEAD-ENDS 2026-09-12. Unmutated, the file
gives 178 passed and 1 skipped (9.7s).

**Result, measured on Windows: every mutation was killed by the row the task names, and no test
was changed.**

| task | mutation | named rows (all failed) | also failed |
|---|---|---|---|
| 5.1 | rule 6 deleted | G1 G2 G3 G4 | H11 N5 N6 X5 X6 X8 E11 E18 J5 Z1–Z3 |
| 5.2 | `HUB_URL` count condition | H10 | — |
| 5.3 | remainder condition | H8 | E7 E8 |
| 5.4 | userinfo condition | H13 | — |
| 5.5 | rule 3 read as plain relative | R4 R5 G5 | H17 G6 X7 E9 E10 X2p X3p, cap test |
| 5.6 | relative words unresolved | R1 R2 R3 | E1 E2 E12 E15–E17 E19 X1b X1c E3, cap test |
| 5.6′ | relative words against the process's directory | — (see below) | W1–W6 H1 N3 E15 E16 J1–J4 J7 J8 H2p, cap test |
| 5.7a | quotes delimit arguments | E1 E2 | E7 E19 Z1–Z3 |
| 5.7b | no whitespace split | H1, refused as `'/json'` | E16 J1–J4 J7 H2p |
| 5.8 | no `\` separator on Windows | X1b X1c | X2p X3p Z1–Z3 |
| 5.9a | no bound | long-URL test | cap test, rendered-bound test |
| 5.9b | bound before rendering | rendered-bound (non-printable) test | cap test |
| 5.10 | own-Hub word not judged as a path | E4 E5 E6 | E6b |
| 5.11 | "continues" dropped | E15 E16 | E17 E18, cap test |
| 5.12a | PowerShell tool read as bash | X1c | H2 H17 H2p X2p X3p |
| 5.12b | Bash tool read as PowerShell | J2 | R11 H1 H3 X1a X2b X3b E3 E4 E5 E7 E14 J1 J3, own-Hub wire test |
| 5.13 | expansion only at word start | G6: `'/$X/stray.txt'` outside, not *cannot be checked* | — |
| 5.14 | no recursion into substitutions | S2 | S3, nested-past-bound test |
| 5.15 | expansion allowed after a reference | E13 | — |
| 5.16 | Windows backstop without bare `\` | Z1 Z2 | Z3 |
| 5.17 | `_LITERAL_DOLLAR` removed (both sites) | E20 E21 E22 | — |

J5 stayed refused under both 5.7 mutations, as 5.7 asks. J5 did fail under 5.1: it guards the
backstop, which is what 5.7 says about it. The failure messages for the two tasks that name a
reason were read out of the JUnit report: 5.7b's H1 is `'/json' is outside your workspace`, and
5.13's G6 is `assert 'cannot be checked' in "'/$X/stray.txt' is outside your workspace"`.

**Two readings, written into tasks.md under 5.17:**
- **5.6.** Read literally, "resolve without joining to the root" means resolving against the
  process's own directory. That mutation cannot fail R1–R3 under pytest. The directory is `hub/`,
  which is itself outside the fixture workspace, so `../stray.txt` is refused either way. W1–W6 kill
  it instead. When the process's directory *is* the workspace, the two readings give the same
  answer, so no row can separate them. R1–R3 fail under the reading the task means: relative words
  not resolved at all, which is the pre-fix behaviour. Both forms were run.
- **5.12b.** J2 is killed on its `$HUB_URL` word, which is no reference in PowerShell, as H1 is,
  not by its body's `\"`. The mutation is killed. J2 simply is not what isolates the body.

**Tasks.** 5.1–5.16 are ticked, 5.17 is added (from D11a) and ticked, and the table is under it.
`openspec validate --strict a-url-is-not-a-path` reports *valid*. No product code or test changed in
this iteration.

**Not run here:** WSL. The mutations were measured on Windows only, which is where 5.8, 5.12 and
5.16 are scoped. On POSIX, `\` is not a separator, so those rows' answers are platform-scoped by
design.

**Next:** `u5-gate`.

## Iteration 6 — u5-gate: `a-url-is-not-a-path` §8, the gate (2026-09-13 00:35 → 01:13)

**Reconciliation first.** The state file said iteration 5, `u5-gate` open, and the HEAD was
`3fba6b2`. Both matched `git log`. The tree was dirty: `tasks.md` had 8.1 and 8.4 ticked with
measured notes, and nothing was committed. That was a **lost firing**, 00:20 → 00:31. It ran the
lint set and the 8.4 diff, backgrounded the whole Hub suite at 00:20, armed a monitor, and ended
its turn. `driver-night.log` at 00:31:52 reads: `Background tasks still running after 600s;
terminating. Set CLAUDE_CODE_PRINT_BG_WAIT_CEILING_MS=0 to wait indefinitely.` The suite died at
28% with no summary line (`u5/suite.txt`). That firing wrote no log entry and changed no state.
This iteration kept its 8.1/8.4 edits, re-measured both, and ran 8.2 again from the start. This
time the turn stayed alive through the wait, using foreground ~9-minute poll loops. The dead end
went into DEAD-ENDS under *Unattended runs*.

**8.1, re-measured at 3fba6b2 under `py -3.11 -m`:** ruff reported "All checks passed!", black
(`--target-version py311`) "565 files would be left unchanged", and mypy "Success: no issues found
in 22 source files".

**8.4, re-measured:** `git diff c12a7d3.. -- hub/hub/migrations hub/hub/api hub/ui | wc -l` = 0.

**8.2, the whole Hub suite from `hub/`, Windows, at 3fba6b2, 00:35:36 → 01:10:25:**
`4194 passed, 86 skipped, 261 warnings in 2079.29s (0:34:39)`, exit 0. The JUnit report
(`u5/suite2.xml`) agrees: 4280 tests, 0 failures, 0 errors, 86 skipped. **Neither known
intermittent fired**, not F292 (`database is locked` setup ERROR) and not F314
(`test_a_wide_flows_state_is_still_one_call`), so no failure needed attributing. The 30
`PytestUnhandledThreadExceptionWarning`s are aiosqlite `Event loop is closed` in
`test_migrations.py` downgrade/upgrade round trips. They are warnings, not failures, and come from
no file this change touches.

**Tasks.** 8.1, 8.2 and 8.4 are ticked with the measurements above, and 8.3 was already ticked.
§8 is complete. No product code or test changed in this iteration.

**Not run here:** the suite on WSL/Linux. CI's Linux `hub-test` is the first POSIX whole-suite run
of this change. Its result is what u8-close cites for F331.

**Next:** `u6-drive-prefix` (§6.1, the pre-fix reproduction). It is 01:13, with 5 h 47 min left
before `stop_at`.

## Iteration 7 — u6-drive-prefix: `a-url-is-not-a-path` §6.1, the pre-fix reproduction (2026-09-13 01:15 → 01:25)

**Position confirmed, no reconciliation.** `HEAD` was `00867a5` (u5's gate), the tree was clean, and
`current` was `u6-drive-prefix` at iteration 6, as STATE claimed.

**Drive Hub.** Worktree `../aw-url-prefix` at **`00a5569`**, u1's pin commit, which is the commit
before §2. `git diff --stat c12a7d3 00a5569 -- hub/hub src` is empty, so it carries the same product
Python as `prefix_sha`. Port **8023** was free: `netstat` showed only 8000 listening. The profile
was a fresh **`drive0913u`**, started with `AW_BOOTSTRAP_API_KEY` set. The key is `aw_live_` plus
32 hex characters, kept at `~/.agentweave/hub/drive0913u-key.txt`, outside the repository. The Hub
was launched from the worktree's `hub/` with `py -3.11 -m uvicorn`. It migrated to `0102`, ran as
**PID 17968**, answered 200 to the key and 401 to a wrong key, and `import hub.mcp_server` from that
directory resolved to the worktree's file (it still has `_ABSOLUTE_PATH_RE`).
`agent_trigger.py:1077` spawns `mcp_server.py` relative to its own file, so every run's approver was
the pre-fix one.

**Harness.** The new `scripts/drive/t_url_is_not_a_path.py` takes `AW_EXPECT=prefix|fixed`. It
builds the fixture: a git repository with `sub/hello.py` printing `hello from sub`, plus
`notes.md` and `README.md`, all committed. It then creates one Haiku runner and one agent on the
**default posture**, with no override. It sends five turns, one ask each, each in a fresh session,
and waits on the HTTP API alone while a run is live (F279). Once every run has ended, it opens the
sqlite database read-only to collect the `permission_denied` rows, the transcripts and the
run→runner model join. Asks 1, 2, 4 and 5 tell the agent to report a refusal and stop. Ask 3 is
deliberately left without that instruction, for §6.3. Ask 1 is told the HTTP form (H1's command
with a single-quoted JSON body) and told not to use MCP.

**Result, measured on Windows: 14/14, reproduced.** Project `proj-7451ee4a1b89`, agent `url012022`.

| ask | run | tool input (as sent) | pre-fix answer |
|---|---|---|---|
| 1 | `run-826a458f6bc6` | `curl -s -X POST "$HUB_URL/api/v1/agent-actions/tasks" -H "Authorization: Bearer $AW_RUN_TOKEN" …` | `Denied: '/api/v1/agent-actions/tasks' is outside your workspace.` No task created |
| 2 | `run-32066fa85347` | `python sub/hello.py` | `Denied: '/hello.py' is outside your workspace.` |
| 3 | `run-bb7c55057449` | `curl -s https://example.com/` | `Denied: 's://example.com/' is outside your workspace.` |
| 4 | `run-c264a82f2819` | `echo hi > "..\stray.txt"` | **allowed**, `(Bash completed with no output)`, `stray.txt` = `hi` |
| 5 | `run-7d8b1603f75b` | `sort -o"..\out.txt" notes.md` | **allowed**, `(Bash completed with no output)`, `out.txt` = `apple fig pear` |

- **Where the strays landed: `<fixture>\.agentweave\worktrees\`, the parent of the agent's
  workspace, not the project root's parent.** A writing agent in a git project works in its own
  worktree (`.agentweave/worktrees/url012022`). `AW_WORKSPACE_DIR` is that worktree, so `..`
  leaves it for the directory holding every agent's worktree. That is outside the boundary the
  approver enforces, which is what D2 claims. The task's words "the fixture's parent directory"
  hold only if "fixture" means the agent's workspace. I record it here rather than reading it as
  a failure of the reproduction.
- **D10 item 5, measured:** all three `permission_denied` rows (`evt-d95b0a702d6a`,
  `evt-6bd2fcf2b000`, `evt-51110e6640dc`) have **`tool_name='Bash'`**. The approver receives the Bash
  tool under that name, and D1 selects the bash dialect by it. The PowerShell name was not
  exercised, and none of the asks used it.
- The exact commands reached the approver with their backslashes intact. That was read from each
  `tool_use` payload, not inferred from the prompt.
- Ask 3's agent was not graded (§6.3 is for the fixed tree). It stopped, explained that the URL was
  "outside the workspace", and offered `WebFetch`. So on the pre-fix tree, the filesystem reason
  sent it looking for another route.
- Every run joins to runner model `claude-haiku-4-5-20251001`. `GET /jobs` is `[]`.

**Teardown.** The two strays were deleted. The Hub was stopped by PID and nothing listens on 8023.
Its log has 0 tracebacks. `git worktree remove --force ../aw-url-prefix` was needed only because of
ignored `__pycache__/`, and `git worktree list` shows only the main checkout. The transcripts,
`record.json`, the drive output and the Hub log are kept in
`~/.agentweave/hub/profiles/drive0913u/u6-prefix-transcripts/`. The database and `%TEMP%\u6\prefix`
are kept too. 8000 and 8010 were never touched.

**Tasks.** 6.1 is ticked with the measurement. `openspec validate --strict` reports *valid*. No
product code changed.

**For u7, learned here.** Use a new `AW_DRIVE_DIR` (such as `%TEMP%\u6\fixed`), because the harness
refuses an existing fixture, and the new profile's database path goes in `AW_DB`. Its `strays()`
scans the whole fixture, `.agentweave/worktrees/` included. On the fixed tree, ask 4's reason is
asserted as starting with `'..\\stray.txt' is outside your workspace` (`repr` doubles the
backslash), and ask 3's as exactly
`'https://example.com/' ` followed by `_NETWORK`.

**Next:** `u7-drive-fixed` (§6.2–6.4). It is 01:25, with 5 h 35 min left before `stop_at`.

---

## Iteration 8 — `u7-drive-fixed` (a-url-is-not-a-path §6.2–6.4), 01:30–01:36

**Reconcile.** The branch `autonomous/2026-09-12-daily` is at `39b6be3`, and the tree is clean.
Both match STATE's iteration 7.

**Drive Hub.** It was started from **this checkout's** `hub/` at `39b6be3` with `py -3.11 -m uvicorn`
and ran as PID 23648 on port **8024**. `netstat` showed only 8000 listening. The process started at
01:30:43. The newest `.py` under `hub/hub` and `src` is `mcp_server.py` at 00:13:05, so no source is
newer than the process. The profile was a fresh **`drive0913f`**, started with `AW_BOOTSTRAP_API_KEY`
= `aw_live_` plus 32 hex characters. The key is kept at `~/.agentweave/hub/drive0913f-key.txt`,
outside the repository. The Hub migrated to `0102` and answered 200 to the key and 401 to a wrong
key. `import hub.mcp_server` from `hub/` resolved to this checkout's file, which has `_NETWORK`.

**Result, measured on Windows: 15/15, every §6.2 assertion held.** Project `proj-45310ef3de5f`,
agent `url013122`, default posture, Haiku. `AW_DRIVE_DIR=%TEMP%\u6\fixed`.

| ask | run | tool input (as sent) | fixed answer |
|---|---|---|---|
| 1 | `run-cb819f7613b4` | `curl -s -X POST "$HUB_URL/api/v1/agent-actions/tasks" -H "Authorization: Bearer $AW_RUN_TOKEN" …` | allowed. `task-03dd81bf6fec` was created with `created_by_run_id=run-cb819f7613b4` and no `permission_denied` |
| 2 | `run-ec291fb33231` | `python sub/hello.py` | allowed, `hello from sub` |
| 3 | `run-f729c2de5139` | `curl -s https://example.com/` | `Denied: 'https://example.com/' is a network address. Under this posture a shell command may name only this run's own Hub ($HUB_URL); if the task needs another address, ask the operator with ask_user.` |
| 4 | `run-7d9fd8579e1c` | `echo hi > "..\stray.txt"` | `Denied: '..\stray.txt' is outside your workspace.` and no stray |
| 5 | `run-e1831f34f07d` | `sort -o"..\out.txt" notes.md` | `Denied: '\out.txt' is outside your workspace.` and no `out.txt` |

- **The operator's record:** there are three `permission_denied` rows: `evt-12f9a4705b4a`,
  `evt-24d349950cfc` and `evt-14049a3d8e1a`. Each reason equals its tool result, less `Denied: `
  and the final period.
- **D10 item 5:** each row has `tool_name='Bash'`, which makes 6/6 across §6.1 and §6.2. The
  PowerShell name has still not been exercised.
- **Tool inputs:** each was read from the `tool_use` payload, and each was the exact command, with
  single backslashes in asks 4 and 5.
- **Strays:** `strays()` scans the whole fixture, `.agentweave/worktrees/` included, and the
  fixture's parent. It found none after any ask.

**§6.3, verbatim and ungraded.** After ask 3's refusal the agent made no further tool call. There
was no `ask_user`, no retry and no other route. It ended with this: *"The curl command was denied due to the current
permission settings. The Bash tool cannot make external network requests in this environment—shell
commands are restricted to only accessing the Hub's own URL (via `$HUB_URL`). To make external
network calls, you would need to either: 1. Grant network access permissions in your settings 2.
Use a tool specifically designed for web requests (like `WebFetch` if available). Would you like me
to try a different approach, or would you like to grant network permissions for the Bash tool?"*
It stopped and reported. The operator question went into prose rather than `ask_user`, and
`WebFetch` was named but not called. The judgement is §7.2's, and it is human-only.

**§6.4.** The profile holds one project and five runs, and all five join to
`claude-haiku-4-5-20251001`. `GET /projects/proj-45310ef3de5f/jobs` returns `[]`. The Hub was
stopped by PID and its `py.exe` launcher exited with it. Nothing listens on 8024, and the Hub log has
0 tracebacks. `drive.out`, `hub.log`, `record.json` and the five transcripts are kept in
`~/.agentweave/hub/profiles/drive0913f/u7-fixed-transcripts/`. The database and `%TEMP%\u6\fixed` are
kept too. 8000 and 8010 were never touched.

**Tasks.** 6.2, 6.3 and 6.4 are ticked, each with its measurement. `openspec validate --strict`
reports *valid*. No product code changed, and `FINDINGS.md` is not touched: its Status lines belong
to u8.

**Next:** `u8-close` (§9 + §10). It is 01:36, with 5 h 24 min left before `stop_at`.

---

## Iteration 9 — `u8-close` (a-url-is-not-a-path §9 + §10, archived), 01:40–01:50

**Reconcile.** The branch `autonomous/2026-09-12-daily` is at `f492261`, and the tree is clean.
Both match STATE's iteration 8.

**9.1: five Status lines, not four.** `F300`, `F312`, `F321` and `F323` are each `fixed 612b9c9`,
driven `f492261`. Each names its §2 mechanism and its §6.2 ask, quoted from tasks.md, with run IDs,
and each also quotes its §6.1 pre-fix run. I mapped the `permission_denied` event IDs from
`drive0913f/u7-fixed-transcripts/record.json` rather than guessing them:
- ask 3 (network): `evt-12f9a4705b4a`;
- ask 4: `evt-24d349950cfc`;
- ask 5: `evt-14049a3d8e1a`.

I verified in `ask1.jsonl` that ask 1's command carried `Content-Type: application/json`, which is
F321's row 5.

`F331` is closed with CI's Linux evidence, and it is labelled **tested, not driven** on POSIX. The
evidence is two runs of the `hub-test` job on `ubuntu-24.04`:
- **Run `34722726736` at `00a5569`** (the pin): `[F331]`, `[N7]`, `[R10]` and `[N1]` XFAIL. Its one
  error is F292's `database is locked`.
- **Run `34723993806` at `612b9c9`:** those rows, plus `[N2]`, `[H5]`–`[H7]`, `[H13]` and `[H14]`,
  PASSED. The summary is `4187 passed, 18 skipped`, with no XFAIL in the file.

The other red on the branch, `34724236621` at `5b6f393`, is also F292's signature
(`test_reviewer_is_not_the_author.py`, `database is locked`). CI for `39b6be3` and `f492261` was
still in progress at 01:42.

**The dated corrections.**
- **`F312`:** *"cannot make any network request"* was never true. N4 (`curl -s example.com`) and
  `python -c` were always allowed, and every scheme URL was allowed on POSIX (F331).
- **`F300`:** *"any URL in any shell command is denied"*, corrected the same way. The misstated
  candidate `//example.com/x` is really `s://example.com/x`.

The index gained a `u8-close` revision paragraph: the open severity-A set is now `F299`, `F301`,
`F319`, `F325` and `F332`.

**Classifier, under F313's rule.** I captured it before editing and re-ran it after each edit.
Exactly F300, F312, F321, F323 and F331 moved from OPEN to RESOLVED, and open severity-A went from
10 to 5. Stripped of `@line` offsets, the B, C, D and `?` lists are identical, and the only
severity-A CONFLICT is still F52. Re-run after the index paragraph: unchanged.

**9.2.** F322, F299, F301 and F332 stay OPEN. The commit says so.

**9.3.** `DECISIONS.md` is not touched. The review page `review-2026-09-12.html:200-203` already
carries the 1c/1d correction, and APPROVALS 2026-09-12 lists it as wanting no row. So nothing was
added to `decisions_for_user`.

**9.4.** `validate --strict` is valid. Before archiving, a script compared the MODIFIED block with
main. The only hunk is the *"Told is not the same as able"* paragraph. All three scenarios are
byte-identical, and the F301 clause is verbatim.

`openspec archive a-url-is-not-a-path -y` gave 44/47 tasks (§7's three are human-only), 1 modified
and 3 added, and archived as `2026-09-13-a-url-is-not-a-path`. The UTC date is also the local date,
so there is no double date. A post-sync script found all four blocks in main, identical to the
delta. `validate --specs --strict` passed 43/43. `test_openspec_collisions.py` gave 14 passed, and
`test_permission_approver.py` gave 178 passed and 1 skipped.

**10.1.** Every command in the test guide was run through the shipped `_decide` on Windows, 17
commands in all, and every answer and reason is what the guide says. The guide gained:
- where a stray lands for a worktree agent;
- the glued-option ask;
- a §6 for D11a's E20–E22 and H17 (all four refused as *cannot be checked*);
- N3 as the one widening;
- the POSIX limits (F331 tested, not driven; F332 open).

**Also.** APPROVALS 2026-09-12 gained a `BUILT` note for this change, labelled as written by the
FIX window.

**No product code changed this iteration.**

**Next:** `r1-pin`. It is 01:50, and 5 h 10 min remain before `stop_at`, so the 3 h gate is met.

# 2026-09-14 day window

Newest entry at the BOTTOM. Playbook: `.claude/loops/day-window.md`.

Armed 2026-09-14T08:55:01+01:00 on `autonomous/2026-09-12-daily` (parent master@ab7cf72), model opus.

## Iteration 1 — compose (09:00–09:30)

**Limits, restated so a later firing inherits them.** Stay on the cycle branch; the merge gate is
the only step that may touch `master`, and only with `--ff-only`. Push, no PRs, nothing
outward-facing, nothing destructive, no open web. Stage explicit paths. Tests under `py -3.11`,
`black --target-version py311`. `:8000` is read-only today: its database through a `mode=ro`
SQLite URI only, its project's files and transcripts read and never written, and its API never
called, not even a `GET`. Never drive against `proj-5e960453` or `proj-18e5d4e0`. Every real agent
turn in a drive binds `claude-haiku-4-5`, and no job is left enabled. No edits to
`hub/hub/mcp_server.py` today (F354). The repository is public, so cite LoopEngine, never quote it.

### The merge gate opened, and the 2026-09-12 cycle landed

- **Condition 1:** the branch was `autonomous/2026-09-12-daily`, and `HEAD..master` and
  `HEAD..origin/master` were both 0.
- **Condition 2:** the tree was clean and `HEAD == @{u} == f28bc31`. It was re-checked just before
  the merge, with this iteration's drafts set aside.
- **Condition 3:** CI run `34820095120` for `f28bc31`.
  - Attempt 1 concluded `failure`, in `hub-test` only: 4233 passed, 20 skipped, 11 xfailed and
    1 error in 859 s.
  - The one `ERROR` line is F292: `sqlite3.OperationalError: database is locked` on
    `BEGIN IMMEDIATE`, at setup of
    `test_reviewer_is_not_the_author.py::test_a_wedged_review_is_restaffed_to_a_real_reviewer`.
    There were no `FAILED` lines. I classified it from the `ERROR` line itself, not from a grep.
  - I re-ran it once with `gh run rerun --failed`, as the 2026-09-13 allowance permits. Attempt 2
    concluded `success` (`hub-test` job `103903497503`).
  - I wrote nothing until it concluded.
- **Condition 4:** `DIRECTION.md` `## 2026-09-14` has no line-initial `HOLD MERGE`.

**What landed:** `git merge --ff-only`, **`master` `ab7cf72..f28bc31`, 33 commits**, pushed. That is
the 2026-09-12 cycle from the 2026-09-13 DECIDE session through this morning's arm commit. It
includes:
- the F351 scheduler fix (`022903f`);
- the F341, F343, F344 and F345 UI fixes;
- the F332 proposal, its §1 test pins (`1ebff15`) and its stop (`b1fbd5a`);
- the F347–F354 ledger entries;
- the 2026-09-14 direction and decisions.

**Today's branch:** I cut `autonomous/2026-09-14-daily` from the new `master` and moved
`STATE-day.json`'s `branch` with it. `run-iteration.ps1:124` stops the driver when the two
disagree. The branch spans one day.

The three reds before this one were also F292 at setup: `d2a5e0d`, `327402e` and `1558fb6`, all
state or ledger commits. The branch's recent redness is still only the two known flakes.

### Last night, research, direction

- **The 2026-09-13 night built nothing.** It stopped F332 §2 before any product code, and
  `F332-rule` is OPEN. That is its only `decisions_for_user` entry, and I carried it into today's
  state. Its one product commit adds tests only (`1ebff15`). The night drove nothing.
- **The research** was delivered at 07:33 and is copied to `spec-queue/research/2026-09-14.md`. It
  is built around today's fix list: candidates 1 and 2 bear on F352's and F354's R1s.
- **`DIRECTION.md` `## 2026-09-14` shapes the day**, under `DECISIONS.md`
  `### 2026-09-14 — a day that reads LoopEngine and builds what it finds`:
  - O-1, O-2a (the Architect), O-2b (dev, dev_2, tester) and O-3, with O-3 starting no later than
    the first firing at or after 11:00;
  - I-1;
  - a `fixes` placeholder, which O-3 replaces with R1 R2 R3 REV IMPL DRIVE per fix;
  - the F327 R1–R3 fallback;
  - D-5, reserved for the first firing at or after 18:15.
- **The drain count is 1** (`a-quote-can-spell-a-slash`). Today's section overrides it.

**Next:** `o1`.

## Iteration 2 — o1, what happened (09:30–09:40)

**Reconciled:** the branch is `autonomous/2026-09-14-daily`, and `HEAD == @{u} == beadf81`,
matching the state file. The tree was clean.

**Done:** `spec-queue/observations/2026-09-14-LoopEngine.md`, `## What happened` (a new
directory).
- **Read-only throughout.** The database was opened only through a `mode=ro` URI from `py -3.11`,
  with every table filtered on `proj-03b9c6a6c37a`, resolved by name. `:8000` was never called, and
  nothing was read through `hub.db`. The query scripts live in `%TEMP%\o1`, outside the repository.
- **Measured, not guessed.** I read the column names with `pragma table_info` first, as the
  DEAD-ENDS entry advises.
- **UTC confirmed.** Stored times are UTC: the provider's reset times, given in Lisbon time, match
  the retries stopping.

**What the corpus says, in short.** The project has doubled since last night: 277 runs, 72 of them
failed, and 50 tasks.
- **Three provider session-limit walls** (09-13 21:00, 09-14 01:19–02:05 and 06:57). The Hub kept
  spawning into the exhausted quota, which is where most of the 72 failed runs come from. Each
  entry is delivered 3 times about 10 s apart and then abandoned. The job's `*/5` tick queues a
  fresh one.
- **262 workspace-guard refusals.** 85 of them refused the agent's own `~/.claude/.../tool-results`
  spill. The rest are `/`-rooted paths, globs, `/dev/null`, regex fragments and `$VAR`.
- **Staffing.** 243 `review_unstaffed` events: 180 no-free-agent (F352's shape) and 63
  named-reviewer-idle.
- **Queue and runs.** 20 hop-budget-suspended entries still `queued` after about 13 h. 3 runs
  failed on the Hub's own `database is locked` insert into `agent_outputs`.
- **Questions.** 8 overnight questions went unanswered, and 3 daytime batches were answered after
  their wait had already expired.
- **Checkpoints and worktrees.** 7 of the 14 checkpoints failed their probe, and 4 worktree
  releases failed with Windows `Permission denied`.
- **Attribution.** About 26 `task_transitions` recorded as `actor_kind=operator` land exactly on
  cron ticks, so the loop's staffing looks attributed to the operator. This is unverified in code,
  and O-3 checks it.
- **The operator's own hand** is the 22:33 cluster (two tasks landed, one rejected), the three
  manual job fires, and the spec approval at 15:46.
- **Cost.** 484.6 M tokens, an API-equivalent of $281.75.

**Verification:** I re-queried the two claims most likely to be wrong before committing: all 10
approved tasks are spec tasks, and all 18 agent-created tasks are the Architect's. I grepped the
file for credential shapes and found none. No product code changed, so there are no tests to run.

**Next:** `o2a`, the Architect's transcripts.

## Iteration 3 — o2a, the Architect's conversations (09:41–09:55)

**Reconciled:** the branch is `autonomous/2026-09-14-daily`, and `HEAD == @{u} == 0ef2f85`,
matching the state file. The tree was clean.

**Done:** `## What the agents experienced` / `### Architect` in
`spec-queue/observations/2026-09-14-LoopEngine.md`.
- **All 29 Architect sessions (96 runs) matched a transcript exactly.** `runs.session_id` is the
  transcript's file name, so no matching by start time was needed.
- **Read-only throughout.** Transcripts and memory notes were parsed read-only, and the `:8000`
  database was opened `mode=ro`. The scripts live in `%TEMP%\o2`, outside the repository. `:8000`
  was never called.

**Headlines for O-3** (each cited by run id in the file):
- **The spec never fitted in a tool result.** All 57 `read_spec_document` results project-wide
  spilled to a harness `tool-results/` file (80–180 KB, one JSON line), and so did 24 `list_tasks`
  results. 27 of the Architect's 58 guard refusals were shell commands naming that spill file. The
  tool has no finer filter, and the agents ended up reading the committed `spec.html` instead,
  against the tool description (`mcp_server.py:1673-1676`).
- **Evidence and approval.** The review briefing says approve, with no mention of evidence, and 5
  approvals were refused by the evidence gate.
- **Completing and closing.** There were 7 `completed →` refusals and 6 author-assignee 403s that
  point at a missing control (F353). Superseded tasks cannot be closed by an agent.
- **Late answers were never delivered.** They stay in a store nobody read.
- **Silent suspensions.** `send_message` never says a message was hop-suspended: the Architect
  sent 8 of the 20 still queued.
- **A review nobody could staff.** 64 idle-reviewer events told the operator to "Ask Architect
  again". The review was in fact waiting on the operator's own decision, which the Architect had told
  them in chat twice.
- **The first-turn cost is ten turns, not one.** The "no MCP tools" first turn is deliberate
  (`launchability.py:285-289`, "convenience for one turn"). Measured, it was the whole 10-turn first
  session on `curl` plus `%TEMP%` payloads, with the payload schema learned by reading AgentWeave's
  source.
- **Knowledge outside the product.** The agents keep a private 19-note ledger of AgentWeave's
  behaviour in the harness memory directory, flagged as an outside-workspace write each time.
- **A merge to check.** `tester` found that approval merges even with a sibling requirement's
  evidence rejected (`task-f6683280dc12` merged `cbc34fa`). O-3 checks whether that is intended.

**Corrected O-1.** The 21 hop suspensions ran from 19:20 to 06:46, not all within 19:20–19:30. All
20 still-queued entries have an empty `waiting_reason`.

**Verification:** I recounted every number in the section against the extracts before committing,
and three were wrong in the first draft (the tool-refusal total, the `completed →` count, and the
number of overnight questions). I grepped for credential shapes: the only matches are `sk-` inside
`task-` ids. No product code changed, so there are no tests to run.

**Unread:** the Architect's `agent_outputs` rows (not reconciled against the transcripts), its
thinking blocks, and the one subagent's sidechain.

**Next:** `o2b` (dev, dev_2 and tester). It is 09:55, before 11:00.

## Iteration 4 — o2b, dev, tester and dev_2 (09:55–10:12)

**Reconciled:** the branch is `autonomous/2026-09-14-daily`, and `HEAD == @{u} == 46e6329`,
matching the state file. The tree was clean. The firing started at 09:55, before 11:00, so `o2b` ran
rather than being skipped.

**Done:** `### dev, tester and dev_2` under `## What the agents experienced` in
`spec-queue/observations/2026-09-14-LoopEngine.md`.
- **All sessions matched a transcript by `runs.session_id`** (dev 59, tester 27, dev_2 8, none
  missing).
- **Read-only throughout.** The `:8000` database was opened `mode=ro`, and LoopEngine's git was
  read only with `git --no-optional-locks log`. The scripts are in `%TEMP%\o2` (`o2b_*.py`), outside
  the repository. `:8000` was never called.

**Headlines for O-3** (each cited in the file):
- **`dev`'s $114 is long turns, not many of them.**
  - 98% of its tokens are cache reads.
  - Eight runs account for 58%, their context growing from 41–45 k to 290–547 k with no compaction.
  - Rework resumes the author's session: 206 M of the 323 M tokens are in sessions spanning more
    than one run, and `task-d8d4b03d722b` alone cost $23.80.
- **`submit_checkpoint_notes` failed 23 of 36 calls.** The Hub caps entries at 400 characters
  (`agent_actions.py:337-346`), the description states no cap (`mcp_server.py:511-535`), and the
  422 names neither the entry nor the length.
- **Evidence footprints are provisional, and three seams follow from that.**
  - **(a) A contradictory remedy.** The duplicate refusal's remedy, *"commit it first"*, contradicts
    the briefing (`launchability.py:423-428`).
  - **(b) A reviewer can judge the pre-turn commit.** A reviewer woken by the author's mid-turn
    message reads the pre-turn commit and rejects after the re-point: 05:52–05:53, `tester` on three
    rows of `run-13096fbb3cc8`. `record_evidence` also tells the agent the returned commit is final
    (`mcp_server.py:1727-1731`).
  - **(c) A failed run is never re-pointed.** A run that fails on the Hub's own exception is never
    snapshotted or re-pointed (`agent_trigger.py:2461-2491`): `ev-85be48ba74a2` still names
    `c78d35d`.
- **`tester` is not a rubber stamp.**
  - 34 accepted and 34 rejected, with a median reason of about 1,160 characters, and it never judged
    its own evidence.
  - 10 of its rejections are bookkeeping: 5 retire a superseded row (agents cannot withdraw their
    own), 2 name the wrong branch, 3 are the race above.
- **A reviewer who moves a task to `completed` becomes its author**, and approval is then refused
  (`task-bb06b8c3c708`, 20:45).
- **`get_answer` on a peer's question answers 404** (`agent_actions.py:722-727`). `tester` could not
  see the answer its decision waited on.
- **Why globs are refused on Windows.** The guard's Windows backstop opens an absolute candidate at
  any bare `/` inside a relative word, so `test/*.test.js` is refused as `'/*.test.js'`
  (`mcp_server.py:957-960`, `:966`).
- **Two turns ended waiting on a background-task notice** that no later turn received.
- **One run, one task.** The *"cannot claim task"* refusal twice. Work done in the wrong task's
  workspace left two evidence rows on the wrong branch.
- **`dev_2`.**
  - Idle since 23:50 UTC, holding 4 pending follow-ups with no `loop_id`, and 2 hop-suspended
    entries.
  - It fixed FR-60 in parallel with `dev`.
  - A message carrying another agent's task id wakes the recipient in that task's workspace.

**Verification:** I recounted the file's numbers against the extracts, and five claims were wrong
in the first draft and are corrected:
- the tokens of the quota-wall runs: 48 of 51 at 0 tokens, not all;
- the transition-refusal breakdown;
- the checkpoint-notes calls per session;
- `dev_2`'s idle time, which is over 9 h, not 11;
- the order of `dev_2`'s "approved" message: it came after the refusal, not before.

I confirmed that all 51 no-error `dev` failures carry the session-limit notice. I grepped for
credential shapes: the only matches are `sk-` inside `task-` ids. No product code changed, so there
are no tests to run.

**Unread:** the three agents' `agent_outputs` rows, their thinking blocks and sidechains, and
`dev`'s three longest sessions end to end. Whether `tester`'s probes are right against LoopEngine's
code, whether background processes outlive the turn, and whether `dev_2`'s pending tasks are
duplicates beyond their titles.

**Next:** `o3`, the sort. It is 10:12, so it runs next whatever the hour.

## Iteration 5 — 10:15–10:30 — `o3`, the sort

**Reconciled:** the branch is `autonomous/2026-09-14-daily`, HEAD is `1f6d887`, and both match
STATE. The tree was clean. O-3 started before 11:00 because both halves of O-2 were done.

**Done.** Every observation in `spec-queue/observations/2026-09-14-LoopEngine.md` is sorted. The
file gained six sections:
- `## The sort (O-3)`;
- `## Fixes`, with 13;
- `## Improvements`, with 11;
- `## Not AgentWeave's`, with 8;
- `## Not sorted, and why`, with 4 whose mechanism was not confirmed;
- `## What went unread`.

The ledger now has new findings **F355–F364** and dated observation notes on **F352, F353, F47 and
F349**. `scripts/classify_findings.py` reads all ten new findings as OPEN, and
`tests/test_classify_findings.py` passes (23).

**The two open checks.**
- **(i) The ~26 cron-tick "operator" transitions are F47.** `scheduler.py:828` and `:830` pass
  `operator()`, and `ACTOR_KINDS` has only `run` and `operator`.
- **(ii) The `cbc34fa` merge over FR-11's rejected evidence is intended.** LoopEngine's document is
  `sketch` rigor, read `mode=ro`, and at that rigor a rejection is a signal, not a gate
  (`requirement_gate.py:339-360`, `:493-506`). It is filed under Not AgentWeave's.

**Mechanisms confirmed this firing, beyond O-2's:**
- **F355, the quota wall.** `return_run_entries` withdraws an entry at three attempts, whatever the
  cause (`inbound_queue.py:170-235`). `rate_limit_event` is kept only for accounting.
- **F356, late answers.** `answer_question` treats a still-running asker as holding the tool call
  open (`questions.py:346`), although `ask_user` has already returned at its expiry.
  `Question.wait_expires_at` is never read there.
- **F357, the review briefing.** Both channels say to approve and say nothing of the evidence gate.
  `resolve_reviewer` never reads `can_accept_evidence`.
- **F359, a run killed mid-turn.** `record_agent_output` is unguarded in the read loop
  (`agent_trigger.py:2190-2204`), so a lock error there reaches the failure tail, which has no
  snapshot and no re-point.
- **F360, the checkpoint probe.** It asks for tasks "assigned to this agent" while a loop checkpoint
  lists the loop's queue. All 7 failures are `task_ids`, with 32 missing and 0 invented.
- **F361, suspended messages.** The scheduler's "hop budget exhausted" is never written to the
  entry (`turn_scheduler.py:336-337`, `:371-372`), and `send_message` builds its own success reply.

**The queue.** `i1` is split in two, `i1a` and `i1b` (11 briefs), and `fixes` is expanded to 74
items in severity order:
- **f352**, which is F352 + F353 as one change because both rewrite the unstaffed sentence;
- then f355, f356, f357, f358, f359, f360, f361 and f347;
- then **f362 and f363, spec only**, because their repairs are in `mcp_server.py` (R1–REV);
- then f364 and f47.

Two constraints are written into the R1 items:
- **F352 and F355 may be the operator's to shape.** Each R1 is told the OPERATOR QUESTION rule.
- **F357 runs after F352**, because both touch `resolve_reviewer`.

**Verification.**
- I recounted the numbers behind the fixes against O-1 and O-2 and against three new read-only
  queries (in `%TEMP%`, not the repository): the document's rigor, the checkpoints' probe findings
  by dimension, and the scope and task count of each checkpoint.
- I corrected one draft figure before committing: the quota-wall run count, now "at least 66", not
  about 70.
- No product code changed, so there is no suite to run beyond the classifier's.

**Next:** `i1a`, the first six improvement briefs.

## Iteration 6 — 10:35–10:50 — `i1a`, improvement briefs 1–6

**Reconciled:** the branch is `autonomous/2026-09-14-daily`, HEAD is `676abac`, and both match
STATE. The tree was clean.

**Done.** I wrote six briefs, each `openspec/explorations/2026-09-14-<slug>.md` in DIRECTION's six
headings. None is a change directory or has a `tasks.md`. The code facts came from three read-only
Explore subagents, spot-checked against the source, plus three new `mode=ro` queries (in `%TEMP%`,
not the repository).

**The six briefs:**
- **`rework-in-a-fresh-session`.**
  - *New measurement:* all 18 of `dev`'s continuation runs that did work were woken by a peer
    message, which resumes the author's conversation (`agent_trigger.py:752-753`). The other 22
    continuations were failed quota re-spawns.
  - *Why the guard never fired:* the project's `offered` checkpoint sits at 80% of Sonnet 5's
    1 M-token window, which is 800 k. `dev` peaked at 547 k.
  - *Ordering:* after F360 and F364.
- **`agents-can-retire-their-own-work`.**
  - An evidence withdrawal needs **a migration**, because of the CHECK constraint
    `ck_requirement_evidence_review_state`.
  - Whether an agent may close a superseded task is the operator's call, because
    `task-lifecycle-governance` makes that rejection operator-only.
- **`a-review-can-wait-on-the-operator`.**
  - The flow's `on_it` check sees only a live bound run or a queued entry.
  - `wait-ended` *releases* the block, so an expired `ask_user` leaves nothing behind that a
    later pass could read.
  - `under_review` has no `blocked` edge.
  - *Ordering:* after f352 and f356.
- **`requests-that-name-another-task`.**
  - A message's task id picks the recipient's workspace whoever the assignee is
    (`agent_trigger.py:901-954`).
  - Part 1 can be built without touching `mcp_server.py`.
- **`the-first-turn-has-its-tools`.** This is **F302**, which the operator decided on 2026-09-09
  (`DAY-2`) and which is still unbuilt. O-3 missed it.
  - *New measurement:* the Architect's ten-run session carries the denial once and the MCP
    rendering on each of the nine later runs, and the Architect still stayed on `curl`.
  - That refutes the verdict's premise, *"healing on the second"*.
- **`the-transition-model-agents-are-told`.**
  - The edges would be rendered from `task_transitions.py:101-153` into every turn's briefing.
  - *Ordering:* after f357.

**Corrections to O-3:**
- **F302 gets a dated note** in `FINDINGS.md` and a line 14 under the observations file's
  `## Fixes`. Its six items (`f302-r1` … `f302-drive`) go into the queue after `f347-drive`, with
  the verdict written into R1.
- **Improvement 5 in the observations file** now says it is F302.

**Known and not re-filed.** `openspec/specs/agent-capability-plane` still states the retired
unasked-question requirement (`:281`) and its companion. The archived change
`2026-09-07-an-agent-without-mcp-is-not-told-it-has-nothing` recorded this and put it to the
operator. The brief's risks say that a spec loop there should REMOVE both.

**Verification.**
- Every cost section cites a `file:line` that I read or that a subagent quoted.
- I checked three claims myself against the source: `launchability.py:232-295` and `:365-401`, the
  checkpoint defaults, and `update_task`'s description.
- I corrected two draft claims before committing: `tester` is the third-most expensive agent at
  $76.05, not the second, and the count of turns the one-task rule cost.
- `tests/test_classify_findings.py` passes (23). No product code changed.

**Next:** `i1b`, briefs 7–11.

## Iteration 7 — 10:50–11:00 — `i1b`, improvement briefs 7–11

**Reconciled:** the branch is `autonomous/2026-09-14-daily`, HEAD is `fe88165`, and both match
STATE. The tree was clean.

**Done.** I wrote five briefs, each `openspec/explorations/2026-09-14-<slug>.md` in DIRECTION's six
headings. None is a change directory or has a `tasks.md`, so all 11 improvements are now briefed.
Code facts came from three read-only Explore subagents. I checked their load-bearing lines against
the source, and ran new `mode=ro` queries (in `%TEMP%`, not the repository).

**The five briefs:**
- **`a-peer-can-see-the-answer`.**
  - `get_own_question` compares `from_agent` by name (`agent_actions.py:715-728`). **No spec
    requirement states that scoping**, so a spec loop would write the rule down for the first time.
  - An answer is queued only for its asker (`questions.py:83-120`).
  - A question about a task nobody parked has no column to name the task, so it needs a migration.
  - *Ordering:* after f356.
- **`project-notes-inside-the-product`.**
  - The 18 notes and their index all sit under the root's harness memory directory. None of the 33
    checkout directories has one, and the FR-70 note came from `tester`'s worktree.
  - One note states a false rule: that the guard follows the shell's cwd.
  - The proposal is an agent-writable notes table beside the operator-only Project Instructions:
    a migration, a tool in `mcp_server.py`, and UI.
- **`peer-threads-that-only-acknowledge`.**
  - *New measurement:* the 7 acknowledgement turns were woken by 2 acknowledgements, 1 correction
    and 4 reports, at hops 2–6, and together cost about $0.35. The cost is hops, not money.
  - Part 1 is buildable today: the briefing says a message wakes its recipient and spends a hop, and
    shows the budget.
  - Part 2, a notice type that does not wake, needs `mcp_server.py`. `Message.type` is
    `String(32)` with no CHECK constraint, so no migration.
- **`background-work-ends-with-the-turn`.**
  - A turn is decided at process exit (`agent_trigger.py:2231`, `:2272`).
  - There is no tree kill after a normal exit (`:2494-2495`), and no briefing text about background
    work.
  - Part 1 (the sentence) is buildable today. Part 2 (record leftover processes) might explain the
    4 unsorted worktree-release failures. That link is unconfirmed.
- **`localhost-for-the-thing-under-test`.**
  - *New measurement:* the 3 refusals were `127.0.0.1:57117` (twice) and `:57391`, all on
    `task-b8e8b7f3beca`'s server.
  - **Neither agent took the refusal's way out, `ask_user`.** Both dropped the live check.
  - Widening F312's verdict is the operator's call, so the decision line asks that first.
  - The rest would be a per-project list of loopback ports, taken with F322.

**Checked for duplicates.** I grepped FINDINGS and DECISIONS for each topic. No existing finding or
verdict is duplicated. The related ones are cited: F356, F361, F5, F312/`a-url-is-not-a-path`
(D10 and 7.2 still undecided), F322, and F359.

**Ledger.** DEAD-ENDS' `:8000` entry now says:
- the transcript directory count is 34 and growing;
- where cost and the waking entry live;
- `cp1252` breaks printing agent text from `py -3.11`.

**Verification.**
- I corrected four draft claims before committing, each one I had not measured:
  - that none of the waking messages asked for anything (one suggested a change and one asked for a
    hold);
  - the token range;
  - "24 directories";
  - that `dev` built on an unverified tree.
- No product code changed. `tests/test_classify_findings.py` was not re-run, because FINDINGS did
  not change.

**Next:** `f352-r1`.

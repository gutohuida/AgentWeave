# 2026-09-14 day window

Newest entry at the BOTTOM. Playbook: `.claude/loops/day-window.md`.

Armed 2026-09-14T08:55:01+01:00 on `autonomous/2026-09-12-daily` (parent master@ab7cf72), model opus.

## Iteration 1 — compose (09:00–09:30)

**Limits, restated so a later firing inherits them.** Stay on the cycle branch; the merge gate is
the only step that may touch `master`, and only with `--ff-only`. Push, no PRs, nothing
outward-facing, nothing destructive, no open web. Stage explicit paths. Tests under `py -3.11`,
`black --target-version py311`. `:8000` is read-only today: its database through a `mode=ro`
SQLite URI only, its project's files and transcripts read and never written, and its API never
called, not even a `GET`. Never drive against `proj-5e960453` or `proj-18e5d4e0`. Every real agent
turn in a drive binds `claude-haiku-4-5`, and no job is left enabled. No edits to
`hub/hub/mcp_server.py` today (F354). The repository is public, so cite LoopEngine, never quote it.

### The merge gate opened, and the 2026-09-12 cycle landed

- **Condition 1:** the branch was `autonomous/2026-09-12-daily`, and `HEAD..master` and
  `HEAD..origin/master` were both 0.
- **Condition 2:** the tree was clean and `HEAD == @{u} == f28bc31`. It was re-checked just before
  the merge, with this iteration's drafts set aside.
- **Condition 3:** CI run `34820095120` for `f28bc31`.
  - Attempt 1 concluded `failure`, in `hub-test` only: 4233 passed, 20 skipped, 11 xfailed and
    1 error in 859 s.
  - The one `ERROR` line is F292: `sqlite3.OperationalError: database is locked` on
    `BEGIN IMMEDIATE`, at setup of
    `test_reviewer_is_not_the_author.py::test_a_wedged_review_is_restaffed_to_a_real_reviewer`.
    There were no `FAILED` lines. I classified it from the `ERROR` line itself, not from a grep.
  - I re-ran it once with `gh run rerun --failed`, as the 2026-09-13 allowance permits. Attempt 2
    concluded `success` (`hub-test` job `103903497503`).
  - I wrote nothing until it concluded.
- **Condition 4:** `DIRECTION.md` `## 2026-09-14` has no line-initial `HOLD MERGE`.

**What landed:** `git merge --ff-only`, **`master` `ab7cf72..f28bc31`, 33 commits**, pushed. That is
the 2026-09-12 cycle from the 2026-09-13 DECIDE session through this morning's arm commit. It
includes:
- the F351 scheduler fix (`022903f`);
- the F341, F343, F344 and F345 UI fixes;
- the F332 proposal, its §1 test pins (`1ebff15`) and its stop (`b1fbd5a`);
- the F347–F354 ledger entries;
- the 2026-09-14 direction and decisions.

**Today's branch:** I cut `autonomous/2026-09-14-daily` from the new `master` and moved
`STATE-day.json`'s `branch` with it. `run-iteration.ps1:124` stops the driver when the two
disagree. The branch spans one day.

The three reds before this one were also F292 at setup: `d2a5e0d`, `327402e` and `1558fb6`, all
state or ledger commits. The branch's recent redness is still only the two known flakes.

### Last night, research, direction

- **The 2026-09-13 night built nothing.** It stopped F332 §2 before any product code, and
  `F332-rule` is OPEN. That is its only `decisions_for_user` entry, and I carried it into today's
  state. Its one product commit adds tests only (`1ebff15`). The night drove nothing.
- **The research** was delivered at 07:33 and is copied to `spec-queue/research/2026-09-14.md`. It
  is built around today's fix list: candidates 1 and 2 bear on F352's and F354's R1s.
- **`DIRECTION.md` `## 2026-09-14` shapes the day**, under `DECISIONS.md`
  `### 2026-09-14 — a day that reads LoopEngine and builds what it finds`:
  - O-1, O-2a (the Architect), O-2b (dev, dev_2, tester) and O-3, with O-3 starting no later than
    the first firing at or after 11:00;
  - I-1;
  - a `fixes` placeholder, which O-3 replaces with R1 R2 R3 REV IMPL DRIVE per fix;
  - the F327 R1–R3 fallback;
  - D-5, reserved for the first firing at or after 18:15.
- **The drain count is 1** (`a-quote-can-spell-a-slash`). Today's section overrides it.

**Next:** `o1`.

## Iteration 2 — o1, what happened (09:30–09:40)

**Reconciled:** the branch is `autonomous/2026-09-14-daily`, and `HEAD == @{u} == beadf81`,
matching the state file. The tree was clean.

**Done:** `spec-queue/observations/2026-09-14-LoopEngine.md`, `## What happened` (a new
directory).
- **Read-only throughout.** The database was opened only through a `mode=ro` URI from `py -3.11`,
  with every table filtered on `proj-03b9c6a6c37a`, resolved by name. `:8000` was never called, and
  nothing was read through `hub.db`. The query scripts live in `%TEMP%\o1`, outside the repository.
- **Measured, not guessed.** I read the column names with `pragma table_info` first, as the
  DEAD-ENDS entry advises.
- **UTC confirmed.** Stored times are UTC: the provider's reset times, given in Lisbon time, match
  the retries stopping.

**What the corpus says, in short.** The project has doubled since last night: 277 runs, 72 of them
failed, and 50 tasks.
- **Three provider session-limit walls** (09-13 21:00, 09-14 01:19–02:05 and 06:57). The Hub kept
  spawning into the exhausted quota, which is where most of the 72 failed runs come from. Each
  entry is delivered 3 times about 10 s apart and then abandoned. The job's `*/5` tick queues a
  fresh one.
- **262 workspace-guard refusals.** 85 of them refused the agent's own `~/.claude/.../tool-results`
  spill. The rest are `/`-rooted paths, globs, `/dev/null`, regex fragments and `$VAR`.
- **Staffing.** 243 `review_unstaffed` events: 180 no-free-agent (F352's shape) and 63
  named-reviewer-idle.
- **Queue and runs.** 20 hop-budget-suspended entries still `queued` after about 13 h. 3 runs
  failed on the Hub's own `database is locked` insert into `agent_outputs`.
- **Questions.** 8 overnight questions went unanswered, and 3 daytime batches were answered after
  their wait had already expired.
- **Checkpoints and worktrees.** 7 of the 14 checkpoints failed their probe, and 4 worktree
  releases failed with Windows `Permission denied`.
- **Attribution.** About 26 `task_transitions` recorded as `actor_kind=operator` land exactly on
  cron ticks, so the loop's staffing looks attributed to the operator. This is unverified in code,
  and O-3 checks it.
- **The operator's own hand** is the 22:33 cluster (two tasks landed, one rejected), the three
  manual job fires, and the spec approval at 15:46.
- **Cost.** 484.6 M tokens, an API-equivalent of $281.75.

**Verification:** I re-queried the two claims most likely to be wrong before committing: all 10
approved tasks are spec tasks, and all 18 agent-created tasks are the Architect's. I grepped the
file for credential shapes and found none. No product code changed, so there are no tests to run.

**Next:** `o2a`, the Architect's transcripts.

## Iteration 3 — o2a, the Architect's conversations (09:41–09:55)

**Reconciled:** the branch is `autonomous/2026-09-14-daily`, and `HEAD == @{u} == 0ef2f85`,
matching the state file. The tree was clean.

**Done:** `## What the agents experienced` / `### Architect` in
`spec-queue/observations/2026-09-14-LoopEngine.md`.
- **All 29 Architect sessions (96 runs) matched a transcript exactly.** `runs.session_id` is the
  transcript's file name, so no matching by start time was needed.
- **Read-only throughout.** Transcripts and memory notes were parsed read-only, and the `:8000`
  database was opened `mode=ro`. The scripts live in `%TEMP%\o2`, outside the repository. `:8000`
  was never called.

**Headlines for O-3** (each cited by run id in the file):
- **The spec never fitted in a tool result.** All 57 `read_spec_document` results project-wide
  spilled to a harness `tool-results/` file (80–180 KB, one JSON line), and so did 24 `list_tasks`
  results. 27 of the Architect's 58 guard refusals were shell commands naming that spill file. The
  tool has no finer filter, and the agents ended up reading the committed `spec.html` instead,
  against the tool description (`mcp_server.py:1673-1676`).
- **Evidence and approval.** The review briefing says approve, with no mention of evidence, and 5
  approvals were refused by the evidence gate.
- **Completing and closing.** There were 7 `completed →` refusals and 6 author-assignee 403s that
  point at a missing control (F353). Superseded tasks cannot be closed by an agent.
- **Late answers were never delivered.** They stay in a store nobody read.
- **Silent suspensions.** `send_message` never says a message was hop-suspended: the Architect
  sent 8 of the 20 still queued.
- **A review nobody could staff.** 64 idle-reviewer events told the operator to "Ask Architect
  again". The review was in fact waiting on the operator's own decision, which the Architect had told
  them in chat twice.
- **The first-turn cost is ten turns, not one.** The "no MCP tools" first turn is deliberate
  (`launchability.py:285-289`, "convenience for one turn"). Measured, it was the whole 10-turn first
  session on `curl` plus `%TEMP%` payloads, with the payload schema learned by reading AgentWeave's
  source.
- **Knowledge outside the product.** The agents keep a private 19-note ledger of AgentWeave's
  behaviour in the harness memory directory, flagged as an outside-workspace write each time.
- **A merge to check.** `tester` found that approval merges even with a sibling requirement's
  evidence rejected (`task-f6683280dc12` merged `cbc34fa`). O-3 checks whether that is intended.

**Corrected O-1.** The 21 hop suspensions ran from 19:20 to 06:46, not all within 19:20–19:30. All
20 still-queued entries have an empty `waiting_reason`.

**Verification:** I recounted every number in the section against the extracts before committing,
and three were wrong in the first draft (the tool-refusal total, the `completed →` count, and the
number of overnight questions). I grepped for credential shapes: the only matches are `sk-` inside
`task-` ids. No product code changed, so there are no tests to run.

**Unread:** the Architect's `agent_outputs` rows (not reconciled against the transcripts), its
thinking blocks, and the one subagent's sidechain.

**Next:** `o2b` (dev, dev_2 and tester). It is 09:55, before 11:00.

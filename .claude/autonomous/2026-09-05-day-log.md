# 2026-09-05 day window

Newest entry at the BOTTOM. Playbook: `.claude/loops/day-window.md`.

Armed 2026-09-05T08:55:03+01:00 on `autonomous/2026-09-04-daily` (parent master@a38caee), model opus.

---

## Iteration 1 — compose the queue

**Limits, restated so a later firing inherits them even if this one dies mid-thought.** Stay on
`autonomous/2026-09-04-daily`; no commits, merges or rebases onto `master`; nothing outward-facing
(no publish, release, PR, force-push, history rewrite — push, do not open PRs); nothing destructive;
no open-web browsing; every claim measured or labelled unverified; operator decisions go to
`decisions_for_user`; stage explicit paths, never `git add -A`; tests under `py -3.11`, `black`
needs `--target-version py311`; drives never touch `proj-5e960453`, `proj-18e5d4e0` or port 8000,
drive Hub is 8011, every real agent turn binds `claude-haiku-4-5`, never leave a job enabled.

### Step 1 — the merge gate: dormant, and it would also have failed on CI

**Condition zero is still unmet.** `STATE-day.json`'s seeded `limits` array reads *"No commits,
merges or rebases onto master. Never auto-merge."* A limit in the state file outranks the playbook,
so the gate is evaluated and reported, not acted on. Verdict recorded here and it goes on the review
page.

| Condition | Measured | Verdict |
|---|---|---|
| Not on `master`, `git rev-list --count HEAD..master` = 0 | branch `autonomous/2026-09-04-daily`, count **0** | **pass** — a genuine fast-forward is available |
| Tree clean, `HEAD` == `@{u}` | `git status --short` empty; both `0b8aaf5` | **pass** |
| CI `success` for **this exact sha** | `gh run list` → CI for `0b8aaf5` is **`in_progress`**, conclusion empty | **FAIL** |
| No line-initial `HOLD MERGE` in `DIRECTION.md`'s newest section | `grep -n '^HOLD MERGE'` returns nothing | **pass** |

So even with condition zero lifted, this firing would not have landed the branch: the only green
runs are for `8a7152b` and earlier, and the playbook is explicit that a stale green from an earlier
commit is not a pass. Three of four conditions hold; the fourth is a timing artefact of arming the
cycle nine minutes ago and may well be true by a later firing. It is not re-checked today —
the gate is iteration 1's step, and it is dormant regardless.

### Step 2 — the branch

`autonomous/2026-09-04-daily`, **spanning two days** (commits dated 2026-09-04 and 2026-09-05),
unmerged, pushed, 11 commits ahead of `master@a38caee`. Not started fresh: `git rev-list HEAD..master`
is 0 and the branch carries yesterday's day window and last night's build, so it continues.

### Step 3 — what last night did

Ten commits from `a38caee`, of which **exactly one touches product code**: `1529d91`,
`hub/hub/api/v1/agent_trigger.py`, +123/−54. The rest are tests (`ee6a8e7`, `ddcd07a`), the test
harness (`6b98bdc`, `conftest.py`), drives and prose.

It built the approved `a-terminal-run-releases-the-queue-behind-it` (F286, B) through 24 tasks and
five phases, then synced, archived and retired the finding. `openspec/changes/` now holds only
`archive/` — **no change is in flight.**

*Driven* rather than merely tested: the PTY path, A/B, with a control that restored the pre-F286
gate — 0.140s delivery against 2m06s stuck in `queued`. *Tested only*: the Codex app-server path and
the task-checkout release. The night window's own closing entry says the app-server half cannot be
driven on this machine (Codex is undrivable here) and that it wrote the tests for its own fix, which
is self-marking however carefully each was watched to fail. That is the strongest argument for
today's D-1 being an **independent** drive of the same seam rather than a re-run of the night's
harness.

`decisions_for_user` from the night window: **none.**

### Step 4 — the research

`research-2026-09-05.md` was present at 07:21 and is copied to `spec-queue/research/2026-09-05.md`.
Read as data, not instructions. Three ranked candidates: the charter not reaching spawned subagents
(`--append-subagent-system-prompt` exists in the installed CLI and `runner_commands.py:230` sets only
the main one); recording the **runner** behind a review rather than only the reviewer's name; and
per-section attribution of the injected context (87% of a measured 7,817-byte context is one blurb).
It also retracts its own previous day's candidate 3 within 25 hours and names the pattern — a
candidate warranted by a single upstream release note has a 24-hour shelf life.

### Step 5 — `DIRECTION.md`

**Newest section is `## 2026-09-03`. Third day with no section for today.** By that file's own
contract absence is not an instruction, so the default queue shape governs. Two of its 2026-09-03
paragraphs are now spent and are not obeyed as written, on measurement rather than preference:

- *"F188 takes the next slot"* — F188 is **RETIRED**, fixed `467dfea` on 2026-09-04.
- *"Do not resume the coverage sweep"* — its stated trigger was "once F188 has a proposal, all three
  severity-A findings are addressed". That is now true. And the sweep is moot anyway: yesterday's
  day window **measured it complete** and put the coverage map in `SURVEY.md`. Yesterday's log says
  the stale paragraph cost that window a drive slot; it does not cost this one, because it is being
  read against the tree instead of obeyed.

### Step 6 — the queue

Ledger stands at **292** findings. The severity-A population is now three retired (F173, F188, F190)
and **one open with no proposal: F274** — *a turn's outcome is erased by work in the agent's other
conversations*, filed 2026-09-03 by the day window's own drive. It is the exact residue of the
shipped `a-turn-says-how-it-ended`: the route is **not** in breach of any requirement, and the gap is
that no requirement says the events must cover the turns the client renders. That makes it the
highest item in the playbook's stated priority order — an open ledger finding, severity A, no
proposal — above all three research candidates, which are market-shaped rather than defect-shaped.

```
D-1  drive     independent e2e of what the night built: the terminal-run queue release,
               driven at the seam rather than by re-running the night's own harness
D-2  spec R1   explore and propose the F274 change
D-3  spec R2   re-derive R1's argument against the code, independently
D-4  spec R3   re-derive again, independently
D-5  review    the review page + the APPROVALS section (inserted at the top, not appended)
D-6  repairs   the no-spec carve-out, only if the day has room
```

One drive plus one change through three rounds is the realistic day, and the rounds are not
collapsed to fit more in.

---

## Iteration 2 — D-1, the independent drive of the terminal-run queue release

**Branch and state reconciled first.** `autonomous/2026-09-04-daily` at `e6997e9`, clean tree,
matching `STATE-day.json` exactly. Nothing to reconcile.

### The rig

8011, `uvicorn hub.main:app` from `hub/` on this branch's source, fresh database
`C:/Users/huida/AppData/Local/Temp/aw0905/aw0905.db`. Process start `09:05:36`; the newest `.py`
under `hub/hub` or `src` is `agent_trigger.py` at **01:32** — measured, not assumed. Fresh project
`proj-aab643e8a931` at `C:\Users\huida\Documents\drive-d1-0905`, its own git repo, two agents
`d1a090658`/`d1b090658` both on `claude-haiku-4-5-20251001` and `bypassPermissions`. Never 8010,
never 8000, never `proj-5e960453` or `proj-18e5d4e0`. No job was ever created, so none was left
enabled; the fixture ends with no running run and no queued entry.

**Independent, as instructed.** The night's `setup_f286.py` / `t_f286_phase0_observe.py` A/B was not
re-run and its `AW_F286_INJECT` raise was not restored. Every measurement below is the ordinary
product path with no injection.

### What held (six seams, all measured)

A run **completing** with two entries behind it released them at `0.17s` and `0.16s`; a run
**stopped** through `POST /agent/{a}/stop` released its entry in `0.12s`; and the **task-checkout**
case — the half the night window's own log recorded as *tested, not driven* — released the parked
second agent in `0.27s`. The released turns all reported their own outcomes: at the timeline's
fifty-event cap all nine runs across six conversations were still in the run-facts map, so **F274's
eviction did not reproduce at this volume** and its own reproduction stands unchanged. A message the
Hub gave up on shows as `delivery_state: "abandoned"` rather than vanishing (F87 holds). The table
is in `FINDINGS.md` under the F289 entry.

So F286's fix does what it says at the seam, checked without its author's harness.

### F288 (B) — the third site that ends a run, and it breaches the requirement F286 shipped

Pushing on *"a run reaching terminal status with input queued behind it"* rather than on the fix
itself found a **third** place that ends a run: `run_reconciliation.reconcile_interrupted_runs`. It
writes `interrupted` to every orphaned row (`:65`) and then re-evaluates only those runs' **own**
agents (`:88`, `:158-162`). Both of `_execute_run`'s endings — and now the shared failure tail F286
added at `agent_trigger.py:1864` — call `redrain_queued_agents(project_id)`, every agent with
anything queued.

The requirement synced last night says the wider thing:
`openspec/specs/agent-conversation-workspace/spec.md:2217`, *A run that has ended releases the queue
behind it* — **"every agent holding queued input in a project ... whenever a run in that project
reaches a terminal status"**. Reconciliation reaches a terminal status and releases one agent. Its
third scenario, *An agent parked behind an ended run's hold is re-evaluated*, is the exact case. The
requirement names restart reconciliation once, only to say it cannot *recover* input stranded
elsewhere; nothing exempts it from being a terminal-status site of its own. **Three rounds wrote a
requirement quantified over every run in the project and did not go looking for the third site that
ends one.**

One crash hides it — the interrupted run's entry returns, its agent is rescheduled, and *that* turn's
ordinary completion re-drains the project (measured: `0.28s`). The mask comes off at
`DELIVERY_ATTEMPT_LIMIT` (3): the third crash withdraws the entry, so that agent has nothing to
schedule, so no run of its own ever ends, so nothing re-drains.

Driven, not reasoned: three crash/restart cycles of the 8011 Hub while `d1a` held
`task-edd780098418`, `d1b` parked behind its checkout. After the third, all three of d1a's runs
`interrupted`, its entry `withdrawn` at 3 attempts, and **d1b's entry sat `queued` at 0 attempts for
6m15s with the checkout free** — then delivered `2.87s` after an unrelated
`PATCH /queue/settings`. The F90/F286 signature exactly: the hold outlives the holder.

Not a D-6 repair — the carve-out's first condition is "touches no requirement in `openspec/specs/`",
and this is a breach of a named one.

### F289 (C) — the status route repeats a spent refusal as the current reason

Found by F288's reproduction, and it is what makes F288 invisible. For the same 6m15s,
`GET /queue/d1b090658/status` answered *"d1a090658 is already running a turn on task
task-edd780098418"* while `GET /queue/d1a090658/status` answered `running: false`. The fallback at
`inbound_queue.py:183` is deliberate and its comment concedes the reason may already be stale — but
it is the only diagnosis the operator gets, it names a holder and a task, and it is checkable, so it
reads as a measurement rather than a memory. The live branch just above it asks whether *this* agent
is running; nobody asks whether the *named* one is.

### One correction to my own measuring

A first pass at the outcome probe reported "0 run ids" for every conversation. That was the harness
using `/agents/{a}/chat/...` where the route is `/agent/{a}/chat/...`; `aw.api` returns
`(404, {"detail": "Not Found"})` without raising, and the probe read the absent key as zero. Fixed
and re-run before anything was written down, which is why the table above says nine of nine rather
than a finding that was never there. The `reconcile_hold` harness had the same class of bug — a
ten-minute time window matching runs from earlier in the same drive — and now compares run ids
against a set taken before the wait, with the reason in a comment.

### Where this leaves D-2

F288 is a **fresh drive finding, severity B, breaching a requirement that shipped nine hours ago**.
The playbook's priority order puts *"a finding this window's drive just produced"* above *"an open
finding from the ledger that has no proposal"*, which is F274 (A). D-2's own instruction anticipates
this: *"If D-1's drive produced something that outranks F274, say so in the log and spec that
instead."*

**It does not outrank it, and D-2 stays on F274.** Severity decides this and F274 is the only open A.
F288 is contained — it needs three consecutive interruptions to become observable, it self-heals
under one, and F274 is silently wrong on every reload of a busy agent's conversation. F288 is queued
behind it and named in `decisions_for_user` so the operator can overrule that on the review page.

---

## Iteration 3 — D-2, spec R1: explore and propose the F274 change

**Branch and state reconciled first.** `autonomous/2026-09-04-daily` at `0d843af`, clean tree,
matching `STATE-day.json`. Nothing to reconcile.

**D-1's finding did not displace F274.** Iteration 2 already argued that and `next_action` restated
it; nothing this round changed the ordering. F274 is the change.

### What R1 wrote

`openspec/changes/2026-09-05-the-conversation-carries-its-own-run-facts/` — `proposal.md`,
`design.md`, `tasks.md`, and the `specs/agent-stream-events/spec.md` delta.
`openspec validate --strict` passes.

**The shape of the fix.** The map travels with the turns. Both chat-history responses gain
`runs: Dict[str, RunFacts]`, built by primary-key lookup over the run ids **their own returned
entries** name — the same coverage-by-construction the timeline route already uses
(`a-turn-says-how-it-ended` design D7), applied to the query whose result the client actually
renders. `AgentTimeline` takes its `runs` prop from the chat response instead of the timeline query.

### Three things R1 found by re-deriving instead of inheriting the finding's framing

**1. F274's own framing is wrong on the requirement position, and the ledger now says so.** The
finding says *the route is not in breach and no requirement says the events must cover the turns the
client renders*. The first half holds — `agent-stream-events:334` explicitly permits the empty map
at `:363-366`, and `:368` is satisfied. The second half does not. *A run's terminal outcome is
visible* (`:299`) is quantified over **the conversation**, requires an outcome for every run that
ended, and allows none only for *"a run whose row genuinely cannot be found"*. F274's runs have rows.
**That requirement is breached.**

What hid it is the requirement's own next sentence, which cross-references *The run facts cover every
run the events name* — a rule quantified over the runs a returned **event** names, so it cannot
forbid omitting a run **no returned event names**, which is the whole of F274. The corpus forbids the
behaviour and simultaneously points at the wrong rule, and that mis-direction is a plausible reason
three rounds could read `:299` and ship this anyway. The delta therefore carries a **MODIFIED**
requirement that repoints the cross-reference and says what went wrong with it. F274's entry in
`FINDINGS.md` now carries the correction too — the ledger claimed a thing about the corpus that is
false.

**2. The single-conversation half kills the obvious cheaper fix.** `get_chat_history`
(`agent_chat.py:567-642`) applies **no limit at all** — measured, not assumed: no `.limit()` on any
of its three queries. So the turns on screen are unbounded while the facts are bounded at fifty
merged events. No widening of the event window can be large enough by construction, which is why
"raise the cap" is rejected in design D1 rather than left as an alternative.

**3. The map cannot move without the invalidation moving with it, and that is not cosmetic.**
`useAgentChatHistory`/`useAgentRecentChat` invalidate on `message_created`, `agent_output` and the
queue events (`agentChat.ts:284-291`) — **not** on the run lifecycle events, which is exactly what
`useAgentTimeline` invalidates on (`agents.ts:389-407`). Move the facts and leave the predicate and
a terminal label starts arriving on unrelated traffic. The case that never arrives at all is the one
the corpus already carves out at `:393-400`: a run reconciled `interrupted` at Hub restart writes
**no** terminal status line, so there is no `agent_output` event to piggyback on and an operator
with the conversation open watches that turn stay silent indefinitely. Hence a second ADDED
requirement — *A run ending refreshes the conversation that renders it* — and D4, which takes the
four terminal events and deliberately **not** `run_started` (an extra refetch of an unbounded
response for a signal `agent_output` already carries).

### Decisions R1 made that R2 and R3 should attack

- **D2 — lookup by id, not by `Run.conversation_id`.** The column exists and is indexed
  (`models.py:1180`, `ix_runs_conversation_started`), so the cheaper query is available. Refused
  because it depends on `Run.conversation_id` and `AgentOutput.conversation_id` agreeing rather than
  on the ids the response literally returns, and because the recent-chat route is cross-conversation
  and needs the id-set form anyway.
- **D5 — `AgentTimeline.runs` stays**, even though after this change nothing in the UI reads it
  (`AgentActivityTab.tsx:24-26` unwraps `events` only and says so). Removing it would delete two
  requirements that shipped two days ago and a public response field for no behaviour. The guard
  against a future author re-wiring the panel back to it is the prop comment — which today reads
  *"straight from the timeline route"* and must be corrected — plus task 4.7's test. **This is the
  weakest decision in the proposal** and the one most worth a fresh pair of eyes: two identically
  constructed maps from different windows, one unread, is exactly the arrangement that produced
  F274.
- **D6 — no chunking of the unbounded `IN`.** SQLite's parameter ceiling is 32766 since 3.32 and
  this machine's Python 3.11 links **3.45.1** (measured). A conversation needs 32,766 distinct runs
  to reach it.

### Every citation in the proposal was resolved against the file, and four were wrong

Written from a first read, then checked line by line before validating: `agents.py:818-832` →
`:818-841`, `agent_chat.py:567-644` → `:567-642`, the recent route's `entries[-limit:]` at `:700` →
`:697`, `_queued_entries_for`'s extend at `:634-636` → `:633-635`, and `AgentActivityTab.tsx:23-26`
→ `:24-26`. Recorded because R2 and R3 are told to check that each citation resolves, and knowing
R1 already swept once tells them the cheap pass is spent — what is left for them is whether the
lines say what the proposal claims.

**Not verified by driving, deliberately.** This window fills. Phase 0 of `tasks.md` is a gate that
re-measures the defect on the checkout before a line is implemented, and it is written around the
one thing the 2026-09-05 drive got wrong about F274: **count events, not turns** — nine short runs
across six conversations did not reach the cap, and that is why D-1 could not reproduce it.

---

## Iteration 4 — D-3, spec R2: re-derive R1's argument against the code

**Branch and state reconciled first.** `autonomous/2026-09-04-daily` at `acd7ea8`, clean tree,
matching `STATE-day.json` (iteration 3, `current: D-3`). Nothing to reconcile.

R1 recorded that it had already swept every citation once and fixed five, so this round spent its
budget on the second question instead: do the cited lines *mean* what the proposal says. Two of them
did not. `openspec validate --strict` passes on the edited change.

### The round did not confirm the proposal. Two findings, one of them fatal to a decision

**1. D4 could not deliver the case D4 was chosen for.** R1's headline reason for adding the four
run-terminal events to `eventTargetsAgent` was the run reconciled `interrupted` at Hub restart —
"the case that never arrives", because it writes no terminal status line to piggyback on. The
addition cannot serve it. `reconcile_interrupted_runs()` is awaited inside the lifespan at
`hub/hub/main.py:350`, and a lifespan startup completes **before uvicorn serves anything**, so its
`sse_manager.broadcast(..., "run_interrupted", ...)` (`run_reconciliation.py:117`) pushes into an
empty subscriber set — the browser's stream died with the previous process. `SSEManager.broadcast`
has no buffer, no replay and no `Last-Event-ID` (`sse.py:86-103`). The event does not arrive late;
for that client it never exists.

The mechanism that *can* serve it was already in the codebase and R1 did not reach it: `useSSE`
exposes `onSseReconnect` (`useSSE.ts:132`), and `useAgentOutput` has subscribed to it since M21 for
precisely this shape of gap (`agents.ts:557`, *"a one-shot reconciliation poll after the stream was
down"*). So the change now carries **two** mechanisms — the four events for the runs the Hub
observes, a reconnect invalidation for the one it does not — as **design D8**, task **3.4**, task
**3.5** (confirm the ordering on the checkout rather than believing this document), test **4.9**,
and a second scenario on the ADDED requirement. The requirement's SHALL line was rewritten to be
stated over the reconnect as well as the terminal status, because a client that listened only for
the event would have satisfied the old wording while failing the scenario underneath it.

Task **6.3** now says what it is measuring: if the drive still reads "never" after 3.1 and before
3.4, that is D8 confirmed on the product rather than in prose.

**2. The `runs` prop has three readers, and the proposal named one.** Beyond `runs[turn.runId]` at
`AgentTimeline.tsx:237`/`:280`, the working indicator reads the map twice more — `lastRunSettled`
(`:148-150`) and `anotherRunIsUnderway` (`:166-172`) — and the second one's own comment states the
property it needs: *"A new run's row is in `runs` before that run's first entry has been grouped
into a turn"*. **The new map cannot have that property by construction**, since its keys come from
the entries. That is the same class of mistake as F274 itself: an argument about the right window
for one consumer, applied to a prop with another consumer whose window differs.

Measured, not assumed: **it survives.** `deliver_entries_with_run` (`inbound_queue.py:125-162`,
stamp at `:155`) sets `delivered_in_run_id` in the same commit that inserts the `Run`, both chat
routes return delivered entries (`agent_chat.py:597-606`, `:665-674`), and `queue_entry_delivered`
is already in the chat hooks' invalidation set (`agentChat.ts:272-278`) — so the new run is named by
a chat entry from the instant it exists, becomes the newest turn, and the indicator shows. The
2026-08-20 stop-then-send behaviour is not regressed. Recorded as **design D9** with the whole chain
written down, plus task **2.4**, test **4.10** and drive step **6.7**, because a behaviour that
survives on four facts nobody wrote down is one refactor from not surviving.

Two consequences of that chain are now in the record rather than in a reader's head.
`agent_trigger.py:1175-1185` has an `else: session.add(run)` branch that commits a run with no
delivery, which would break the indicator — unreachable in production today, since
`turn_scheduler.py:322` is the only production caller and returns at `:304-305` when `selected` is
empty. And there is a real narrowing: `anotherRunIsUnderway` scans an agent-wide map today, so a run
in *another* conversation can light the indicator in the one on screen; a conversation-scoped map
cannot. That is an improvement, chosen here rather than discovered later.

### F290 (B) filed — the product defect underneath finding 1

Finding 1 is not only about this proposal. `useAgentTimeline` invalidates on `run_interrupted`
today, on an event that today cannot arrive, and nothing else covers it: no `staleTime`, no
`refetchInterval` (`agents.ts:420-425`), and `refetchOnWindowFocus: false` globally
(`main.tsx:11`). So on this checkout, an operator with a conversation open across a Hub restart sees
that turn presented as unfinished until they reload. Distinct from F274 — F274 is the map missing a
run, F290 is the map never being re-read — and both surface identically, which is presumably how the
second hid behind the first.

Filed as **F290 (B)**, and **labelled code-derived, not yet driven**: every citation is on this
checkout, but the duration claim is the code's consequence and phase 0.4 is the task written to
measure it. The change's D8 repairs the chat hooks and deliberately does **not** repair
`useAgentTimeline`, whose invalidation stays dead — acceptable only because after D5 nothing reads
that map. If it regains a consumer, F290 is live again for it, and the finding says so.

### What R2 checked and left alone

- **(b) is confirmed exactly as R1 argued it.** *The run facts cover every run the events name*
  (`openspec/specs/agent-stream-events/spec.md:368`) opens *"Every run named by a returned event
  SHALL be present…"* — quantified over runs a returned **event** names, so it genuinely cannot
  forbid omitting a run no returned event names. The cross-reference at `:299` does send a reader to
  a rule the route satisfies. The MODIFIED block reproduces the SHALL line verbatim and all five
  original scenarios, adding one. No change.
- **(c) is confirmed.** All four terminal events carry `agent`: `_broadcast_run_lifecycle` builds
  `{"agent": agent, "run_id": run_id, **fields}` (`agent_trigger.py:1755`) and
  `run_reconciliation.py:102` does the same. All four are in `SSE_EVENT_TYPES` (`useSSE.ts:47-51`),
  and `project_id` is stamped by the manager, never trusted from the payload (`sse.py:97`). Both
  chat hooks call `eventTargetsAgent` with the same `project_id` guard (`agentChat.ts:298`, `:322`).
- **(a) D5 stands, with one addition.** `AgentActivityTab` is the timeline map's only remaining
  consumer and unwraps `events` only (`:24-26`), so R1's admission is accurate. D5's guard was a
  comment and a test; the corpus said nothing about *which* map a conversation may read. Added a
  scenario — *The conversation's outcomes are read from the response that carried its turns* — so
  the guard is durable rather than only in a test file.
- Citations re-resolved and correct: `agents.py:802-803`, `:818-841`, `:830-841`;
  `agent_chat.py:173`, `:567`, `:634`, `:646`, `:697`, and `get_chat_history` genuinely applies no
  `.limit()` on any of its three queries; `agentTimelineModel.ts:57`; `AgentOutputPanel.tsx:332-333`,
  `:350-352`, `:1036`; `schemas/agents.py:136`; `api/agents.ts:134`. **One was wrong:**
  `models.py:1180` is the closing paren — `ix_runs_conversation_started` is `:1179`. Corrected.
- One prose duplication left deliberately: the ADDED requirement's cross-conversation scenario was
  reshaped to be about the *response* (the rest of that requirement is response-shaped), leaving the
  operator-visible version in the MODIFIED requirement where it belongs.
- Task 2.2 now names the comment at `AgentOutputPanel.tsx:330-331`, which explains why the timeline
  hook lives in that component and would otherwise have been left describing a hook that is gone.

**Not driven.** This is a spec round; the change's phase 0 is a gate that measures F274 and F290 on
the checkout before a line is implemented, and 3.5/6.3 are written to falsify D8 rather than confirm
it.

---

## Iteration 5 — D-4, spec R3: re-derive again, independently

**Branch and state reconciled first.** `autonomous/2026-09-04-daily` at `5f17371`, clean tree,
matching `STATE-day.json` (iteration 4, `current: D-4`). Nothing to reconcile.

**The round changed something, and what it changed was R2's own finding.** `openspec validate
--strict` passes on the edited change. The prior held: a third independent derivation found what the
second missed, and it found it one layer above where the second stopped reading.

### R2's D8 built a mechanism the codebase already has, app-wide, with a test on it

R2 measured that `reconcile_interrupted_runs()` is awaited in the lifespan (`main.py:350`) before
uvicorn serves, so its `run_interrupted` broadcast reaches an empty subscriber set and
`SSEManager.broadcast` has no replay (`sse.py:71-103`). **R3 re-derived that from the source and it
is exactly right.** What R2 did next was reason from `useAgentOutput`'s `onSseReconnect`
subscription (`agents.ts:557`) that both chat hooks needed one of their own.

They do not. `useSSE` **itself** subscribes, and does not narrow:

```ts
// hub/ui/src/hooks/useSSE.ts:404-412
useEffect(() => {
  return onSseReconnect(() => {
    queryClient.invalidateQueries()
  })
}, [queryClient])
```

`invalidateQueries()` with no filter matches every query in the cache and refetches every mounted
one (React Query `^5.62.16`, default `refetchType: 'active'`) — both chat queries and the timeline
query. `useSSE()` is mounted unconditionally at `App.tsx:216`. The reconnect fires only after a
`fetch` to `/api/v1/events` succeeds (`useSSE.ts:315-317`), which cannot happen before the new Hub
process serves — by which time the reconciliation has already committed `interrupted`. And the
behaviour is already pinned: `useSSE-lifecycle.test.tsx:229`, *"invalidates all queries once the
stream actually reconnects (not on the initial connect)"*.

So **D8 is now a statement about existing behaviour**, task 3.4 says *add no subscription* and tells
the implementer to re-confirm the two lines before relying on them, and test 4.9 was rewritten to
pin the global mechanism at the chat query with a mutation that narrows `invalidateQueries()` —
because after this change that global call is the only thing standing between the requirement and a
regression, and nothing else fails if someone filters it.

### F290 is retracted, and F291 is filed in the place it was pointing at

**F290 (B) is not a defect.** Its citations are all correct; its conclusion — an interrupted turn
reads as unfinished "until someone reloads" — is contradicted by the eight lines above. The turn
labels itself within one reconnect cycle. The entry is kept, not deleted, with the refutation at the
top and the original text below it: the premise it established is load-bearing for the change, and a
finding that was wrong is worth more in the record than out of it.

That left the four terminal events in task 3.1 with no justification, since the restart case was the
only one R1 and R2 gave them. R3 went looking for a terminal run that reaches **no** chat-hook event
at all, and found one. **F291 (C):** a run that fails *before its process spawns*
(`agent_trigger.py:1960-2010`) sets `failed`, commits, broadcasts `run_failed` — and calls
`record_agent_output` nowhere; all four call sites in that file (`:2104`, `:2325`, `:2645`, `:2898`)
are after the spawn. Its other broadcasts are `queue_entry_queued` per requeued entry (which the chat
hooks *do* hear) and `queue_entry_abandoned` per abandoned one (which they do not). When every entry
is at `DELIVERY_ATTEMPT_LIMIT` the requeued set is empty (`inbound_queue.py:222-235`), so the only
two events that fire are two nobody is listening for, and the turn holds a delivered message with no
outcome until unrelated traffic, a reconnect or a reload. Task 3.1 closes it; the task's comment now
carries *that* reason instead of the restart one, so a later reader is not told something false at
the exact site the code depends on.

Recorded alongside it, not filed separately: `queue_entry_abandoned` is missing from
`QUEUE_EVENT_TYPES` while the response it changes is the chat response — `_queued_entries_for`
returns abandoned entries deliberately (F87). This change does not close that.

Ordering was checked rather than assumed for the runs that *do* spawn: the run row's terminal status
commits at `agent_trigger.py:2265`, **before** `_broadcast_run_lifecycle` (`:2286`) and before the
terminal status row's `record_agent_output` (`:2325`). A refetch on either event reads an ended run.

### The drive was being sent to measure the wrong number

The consequence that mattered most for tomorrow's implementer: **task 0.4 told them to measure how
long an interrupted turn stays unlabelled, and task 6.3 told them phase 0's answer would be
"never".** It will be seconds. An implementer who measured three seconds against a document
predicting "never" would either distrust their own measurement or record a false confirmation. 0.4
now says *expect seconds, and if it reads "never" then D8 is wrong and the change needs another
round before phase 1*; 6.3 is now a no-regression check rather than a before/after. A new **0.5**
drives the case that *is* a before/after — a runner binary that does not exist, with the entry
already at the attempt limit — and 6.3's second half inverts it.

### What R3 checked and left standing

- **(b) D9 survives, and R3 broke each link rather than inheriting the chain.**
  `deliver_entries_with_run` stamps `delivered_in_run_id` at `inbound_queue.py:155`, in the same
  commit as `db.add(run)` (`:152`) and before `db.commit()` (`:160`).
  `_queue_entry_to_timeline` sets `timestamp=entry.delivered_at if delivered and entry.delivered_at
  else entry.arrived_at` — so the delivered entry's timestamp is the delivery, both routes sort
  ascending (`agent_chat.py:631`, `:696`), and it lands last. **`groupIntoTurns` preserves array
  order and does not sort** (`agentTimelineModel.ts:45-62`), so its run is the newest turn. Under the
  new map that run is then `lastRunId`, `lastRunSettled` is false, and the indicator shows via the
  *shorter* path — `anotherRunIsUnderway` is not even needed for stop-then-send. Task 2.4 now carries
  the chain rather than a claim.
- **(d) citations.** `sqlite3.sqlite_version` on `py -3.11` is **3.45.1**, measured — D6 stands.
  `ChatHistoryResponse` is at `agent_chat.py:173`; `get_chat_history` `:567`, sort `:631`, queued
  extend `:633-635`; `get_recent_chat` `:646`, truncation `:697`. `AgentOutputPanel.tsx:330-331`,
  `:332`, `:333`, `:350-352`, `:1036` all resolve. `AgentTimeline.tsx:148-150`, `:166-172` and the
  three comments at `:133-135`, `:143-145`, `:152-155` all resolve and all name the timeline route.
  `agentOutput-polling.test.tsx:22` is the `onSseReconnect` mock. `_broadcast_run_lifecycle` builds
  the payload `{"agent": agent, "run_id": run_id, **fields}` at `:1756`.
- **One D7 claim was false.** The design said the client's `AgentRunFacts` mirrors the Python
  `RunFacts`. It does not: `hub/ui/src/api/agents.ts:133-139` carries four fields, and
  `outside_workspace_writes` (`schemas/agents.py:136`) is not one of them. Corrected, and the
  asymmetry is now stated as deliberate — no UI consumer, TypeScript ignores extra keys — with task
  1.5's "no default" rule scoped to the server construction where it belongs.
- **Task 4.2 is written so it cannot pass for the wrong reason.** It already says *assert on the chat
  response alone*, and against unfixed code `ChatHistoryResponse` has no `runs` field at all. Left
  as it was.
- The second ADDED requirement gained a sentence and a scenario: its terminal-status half now rests
  on the run that ended before its process started (F291), not on the retracted restart case. The
  SHALL line was not touched and stays on the first physical line.
- Typo fix in task 2.4 ("the two three comments").

**Not driven.** This is a spec round. Phase 0 is still the gate that measures F274 — and now F291 —
on the checkout before a line is implemented.

---

## Iteration 6 — D-5, the review page and the APPROVALS section

**Branch and state reconciled first.** `autonomous/2026-09-04-daily` at `406c758`, clean tree,
matching `STATE-day.json` (iteration 5, `current: D-5`). Nothing to reconcile.

`spec-queue/review/review-2026-09-05.html` — 718 lines, self-contained, theme-aware, seven sections
answering the playbook's six questions in order plus the *decisions that are not work* section the
house style carries. Today's section is **inserted at the top** of `spec-queue/APPROVALS.md`
(line 19, above `## 2026-09-04`), because that file's own contract says newest day first and days
below the newest are not read.

**Verified as a page, not only as a file.** `py -3.11 scripts/uishot.py` against a `file://` URL at
1100×1400, once per theme, and both PNGs read back: dark and light both render, every table, card,
note and decision box lands, and nothing overflows. Tag balance checked with `html.parser` — zero
errors, zero unclosed. No external stylesheet, script, font or image: `re.findall` for `src=`,
`href=`, `@import` and `https?://` over the whole file returns **[]**, so the Artifact runtime has
nothing to block.

### The page's own finding — F292 (B), filed while measuring section 1

The morning gate recorded condition 3 as failing because CI for `0b8aaf5` was `in_progress`, and
called it *"a timing artefact of arming the cycle nine minutes ago [that] may well be true by a
later firing."* **It was not an artefact.** That run concluded `failure`, and re-reading the whole
branch's CI for the page found it is the third failure with exactly one cause:

| CI run | sha | Test that errored at setup |
|---|---|---|
| 33923797344 | `770cda5` | `test_reviewer_is_not_the_author.py::test_a_wedged_review_is_restaffed_to_a_real_reviewer` |
| 33953849160 | `0b8aaf5` | `test_flow_fires_a_review_turn.py::test_a_review_that_cannot_be_prepared_does_not_become_an_ordinary_turn` |
| 33955870588 | `acd7ea8` | `test_reviewer_is_not_the_author.py::test_assigning_a_reviewer_and_sending_to_review_in_one_patch_is_accepted` |

All three `sqlalchemy.exc.OperationalError: (sqlite3.OperationalError) database is locked`, all three
`ERROR at setup`, all three the `hub-test` job on Linux with every other job green, and in each run
the only error in the suite. **3 of 16 runs on this branch; no run has failed in any other way**, and
green runs bracket each failure on the neighbouring commits, so it is intermittent rather than a
property of any sha. It is the sole thing between this branch and the gate's condition 3.

This is the residue of the F285 fix (`d9ad1e0`, the DECIDE session, 2026-09-04) and the fix is not in
question — `:memory:` had no file locks, a file-backed pool does, and `conftest.py` drops and
recreates the schema before every test. What makes it worth a finding rather than a shrug is that
**it was already mitigated once and the mitigation did not hold**: `be6a70d` adds
`await _REAL_ENGINE.dispose()` before the `drop_all` (`conftest.py:298`), that dispose is in the tree
at all three failures — measured with `git show <sha>:hub/tests/conftest.py`, not assumed — and the
first failure *is* the test its comment names.

The mechanism is **labelled unestablished in the entry**, deliberately. The comment blames a live
`JobScheduler`, but both failing files `await` `_fire_job_internal` directly with an explicit session
rather than starting the scheduler loop, and `dispose()` closes a pool's *idle* connections and
cannot reclaim one a running task has checked out. A fire-and-forget `asyncio` task surviving the
previous test fits better; that sentence is a hypothesis and says so. The diagnostic the entry asks
for is the pool's checked-out count logged immediately before the `drop_all`, so the next occurrence
names its cause instead of its victim.

Ledger now **297**.

### What the page says, briefly, so a later firing need not open it

Section 1 leads with the branch unmerged across two days and 17 commits, the gate dormant under the
seeded limit, the four conditions as measured at 09:02, and then the paragraph above. Section 2:
exactly one commit touches product code (`1529d91`, +123/−54); the PTY path was driven with a
control, the Codex path and the task-checkout release were tested only. Section 3: the six seams
that held, F288, F289, F292, and the drive's own `/agents/` vs `/agent/` correction. Section 4 gives
one paragraph per round and says plainly that R3 overturned R2 and retracted F290. Section 5 walks
the night's default and points out that step 3 may land on a red chunk. Section 6 carries the three
research candidates, each ending in what it would mean here. Section 7 carries all the operator
decisions.

### Two things the page deliberately does not do

- **It does not pick between F274 and F288 for tonight.** Both arguments are on the page in a
  decision box and the row is F274, because severity decides and F288 needs three consecutive
  interruptions to become observable. The operator can overrule it in one line.
- **It does not propose an F292 fix.** The last change made to that file on the same evidence did not
  work and the mechanism is unestablished, so what tonight may do about it is written up as a
  decision rather than guessed. That is also why F292 is **not** a D-6 candidate: the carve-out wants
  a fix smaller than the argument for it, and here the argument is unfinished.

**Not driven, and nothing needed to be.** This item wrote prose and rendered it; no product code was
touched, so no suite was run.

# 2026-09-18 day window

Newest entry at the BOTTOM. Playbook: `.claude/loops/day-window.md`.

Armed 2026-09-18T08:55:01+01:00 on `autonomous/2026-09-17-daily` (parent master@f9db321), model opus.

## Iteration 1 — compose the queue

Branch and log verified against `STATE-day.json`: `git branch --show-current` is
`autonomous/2026-09-17-daily`, `git log --oneline -8` matches the state file's recent-commits
claim exactly (`45f2813` at HEAD), tree clean. No reconciliation needed.

**Step 0 — backlog page.** `py -3.11 scripts/backlog_page.py --check` → "current, nothing moved
since the last generation." No refresh needed.

**Step 1 — merge gate.** Checked all four conditions:
1. `git rev-list --count HEAD..origin/master` = 0 (master has not moved past HEAD). PASS.
2. `git status --short` empty, `git rev-parse HEAD` == `git rev-parse @{u}` (`45f2813`). PASS.
3. `gh run list --branch autonomous/2026-09-17-daily --limit 20 --json headSha,conclusion,workflowName,status`
   shows CI **`in_progress`** for HEAD `45f2813` — not concluded. **FAIL** (not a rejection, a
   not-yet). Per the playbook, write nothing until it concludes, so the gate is deferred rather than
   retried in this same firing.
4. `spec-queue/DIRECTION.md`'s `## 2026-09-18` section carries no line-initial `HOLD MERGE`. PASS.

Gate not opened this iteration (condition 3 only). Queued `D-0` to retry it next firing rather than
proceeding on the existing branch immediately — a fresh commit now would move HEAD to an
un-built sha and cost another ~13-minute wait, per the playbook's explicit warning.

**Step 2 — settle the branch.** Not `master`; continuing on `autonomous/2026-09-17-daily`. First
commit past the `master` merge-base is `f4ff939` (`arm(night): 2026-09-17`), so the branch now
spans **2 days** (2026-09-17 → 2026-09-18), 9 commits ahead of `master`.

**Step 3 — what last night did.** `.claude/autonomous/2026-09-17-night-log.md` (bottom): the FIX
window built **nothing**. Its backlog check found the one unarchived change
(`an-unstaffed-review-names-its-holders`) explicitly stopped at REV pending a re-derivation round
against F352-free's option (f), and all five open severity-A findings (F299, F301, F325, F352,
F375) either decision-gated, undrivable (Codex), or lacking a proposal. It started a background
`pytest hub/tests/ -q` at compose time to check tree-green, but left the window without a verdict —
still running past 105 minutes, log at
`.claude/autonomous/tmp/2026-09-17-hubtests-compose.log`. Checked that file just now: still
mid-run (`14% ...`), no conclusion yet. Queued as a `D-6` candidate to check later rather than
blocking on it now. `git diff` shows no `FINDINGS.md` changes from last night (consistent with
"built nothing").

**Step 4 — research delivery.** `research-2026-09-18.md` present at
`~/.claude/routines/agentweave-research/out/`, delivered 07:26. Copied to
`spec-queue/research/2026-09-18.md` (new directory `spec-queue/research/` — first delivery copied
into the repo under this iteration's naming; earlier deliveries may live elsewhere, not checked).

**Step 5 — DIRECTION.md.** `## 2026-09-18` section present (written 2026-09-17 evening, after an
end-to-end read of `LoopEngine_2` on `:8000`). No `DAY WINDOW` line — standard 09:00–17:00 applies,
confirmed against `STATE-day.json`'s `stop_at`. It is fully prescriptive: take
`a-refused-capability-reaches-the-operator` (F376/F378) as the one spec loop (drain was 1 at
2026-09-17 ~23:00); do not re-derive F376's diagnosis, only its repair, deciding between (a) raise
a `permission_requests` row on refusal vs (b) tell the truth in the refusal message; item 2
(F379) is explicitly not today's, only reachable if tonight's FIX window clears the one unbuilt
change first; F377/F378's `request_agent`-repair-or-retire change is explicitly deferred to
tomorrow. Names a scoped drive on `:8010` reproducing F376/F378 as today's `D-1`.

**Step 6 — drain and queue.** Drain count (unbuilt specced changes, `tasks.md` with any `- [ ]`):
**1** (`an-unstaffed-review-names-its-holders`). Matches DIRECTION.md's own count. One spec loop,
clear-column shape, per `day-window.md`'s table. Wrote the full queue:
`D-0` (retry gate) → `D-1` (scoped drive, F376/F378 repro) → `refused-capability-r1/r2/r3` (the
spec loop, per DIRECTION.md's item 1 and its own repair argument) → `D-5` (review page) → `D-6`
(repairs if room, candidate: the still-unresolved hub-suite background run).

No full-surface sweep queued: last one was 2026-09-01, which is stale, but DIRECTION.md's own
"the drive, when there is one" section names the scoped F376/F378 repro specifically and is the
newer, controlling instruction for today.

`next_action` set to `D-0`'s retry text; `current` set to `D-0` (routes to sonnet/medium per
`usage-policy.json`'s `^D-[0-9]+$` rule). No decisions for the operator surfaced yet — DIRECTION.md
already resolved the (a)-vs-(b) framing as R1's job, not a pre-decided answer.

## Iteration 2 — D-0 retried, still not open; D-1 driven and closed

Branch and log verified against `STATE-day.json` before starting: `git branch --show-current` is
`autonomous/2026-09-17-daily`, `git log --oneline -5` matches (`a392e9b` at HEAD), tree clean.

**D-0, re-measured from scratch, not trusted from iteration 1.** All four conditions:
1. `git rev-list --count HEAD..origin/master` = 0. PASS.
2. `git status --short` empty, `HEAD` == `@{u}` (`a392e9b`). PASS.
3. `gh run list --branch autonomous/2026-09-17-daily --limit 20 --json headSha,conclusion,workflowName,status`:
   HEAD `a392e9b` is CI **`in_progress`** — not concluded. **FAIL.** (The commit that composed
   iteration 1 moved HEAD past `45f2813`, so this is a fresh in-progress run, not the same one.)
4. No line-initial `HOLD MERGE` in `spec-queue/DIRECTION.md`. PASS.

Gate still not open (condition 3 only, same as iteration 1). Per the playbook's instruction not to
spend a whole firing on a poll, moved straight to `D-1` in this same firing instead of re-queuing
`D-0` again.

**D-1 — scoped drive: reproduce F376/F378 on the trial Hub (:8010).** Started the trial Hub from
source (`cd hub && DATABASE_URL=... py -3.11 -m uvicorn hub.main:app --port 8010`), confirmed its
stale pid file (1062) named no live process first. Wrote
`scripts/drive/d1_0918_refused_capability.py`: opens a fresh project outside the repo
(`~/Documents/drive-0918-d1-<tag>`), one runner + one agent bound to `claude-haiku-4-5-20251001`,
triggers a real turn instructing the agent to call `create_flow` once with a made-up
`spec_document_id` and `stop_when_queue_empties=true`, reads the turn's transcript back, and checks
`permission_requests` for zero rows via a read-only sqlite connection to the trial profile's own
database (never touched `:8000` or its projects `proj-5e960453`/`proj-18e5d4e0`).

First attempt had two harness bugs (polled for agent status `!= "working"` when the roster reports
`"running"`, so it declared the turn finished before it started; fetched the conversation from the
wrong route, `GET .../conversations/{id}` instead of `GET .../agent/{agent}/chat/{id}`) — fixed both
and re-ran clean after deleting the stray first-attempt project (`proj-ab0bc50dd48d`, whose agent
had gone idle by then). Second run: **5/5 checks passed.**

Measured: the transcript carries F376's exact sentence
(`Scheduled work from agents requires operator approval or an enabled allowance`) against the
`create_flow` call, and `select count(*) from permission_requests where agent = 'architect090815'`
on the trial database (read-only) is **0**. F376 and F378 both reproduce on a second Hub and a
second project — not specific to `LoopEngine_2` or `:8000`. Also confirmed by reading
`_require_agent_job_allowance` (`hub/hub/api/v1/jobs.py:32-51`) that an operator-issued call (no
`X-AgentWeave-Agent` header) skips the gate entirely, which is why this had to be a real bound
agent turn rather than a plain API call — recorded in the finding so R1 does not have to re-derive
it. Appended the reproduction as an addendum after F380 in `FINDINGS.md` (not a new finding number —
it confirms F376/F378, assigns no new severity). Fixture project deleted, Hub process on :8010
stopped afterward, no job left enabled.

`current` set to `refused-capability-r1`; `next_action` set to start the spec R1 for
`a-refused-capability-reaches-the-operator` per DIRECTION.md's item 1, using this iteration's
reproduction as the confirmed premise rather than re-deriving it.

## Iteration 3 — refused-capability-r1: the spec loop's first round, written and validated

Branch and log verified against `STATE-day.json` first: `git branch --show-current` is
`autonomous/2026-09-17-daily`, `git log --oneline -5` has `d3adcbc` at HEAD matching the state's
recent-commits claim, `HEAD` == `@{u}`, tree clean. No reconciliation needed.

**Unit: R1 for `a-refused-capability-reaches-the-operator`** (F376 (A), with F378's refusal shape),
per `DIRECTION.md`'s `## 2026-09-18` item 1. Wrote
`openspec/changes/a-refused-capability-reaches-the-operator/` — `proposal.md`, `design.md`,
`tasks.md` (21 tasks), `test-guide.md`, and the delta
`specs/agent-capability-plane/spec.md`. `openspec validate --strict` **passes**; `openspec list`
shows it at `0/21 tasks`. F376's diagnosis was **not** re-derived (DIRECTION.md forbids it); the
repair was.

**The decision, which is not either of the two DIRECTION.md offered.** It told R1 to argue between
(a) raise a `permission_requests` row so the promised approval becomes real, and (b) stop promising
an approval and name the setting instead. **(a) is rejected on a measurement, and it is the kind of
rejection only exploring the code produces:** that row is *turn-scoped by design*. The adapter waits
`AW_DECISION_TIMEOUT` (120s, `mcp_server.py:927`) then `POST`s `/expire` (`:1512`), and the run's end
sweeps anything still pending (`permission_requests.py:22`, joined to the run-end transaction). The
card is also rendered only inside one agent's output panel (`AgentOutputPanel.tsx:1026`), not on any
top-level destination. F376's refusal was at 17:37:45 and the ledger deadlocked at 17:56 — **(a)
makes the false sentence true and repairs none of the harm the finding measured.** Second reason,
from openspec itself: `agent-capability-plane:915-926` states that scheduled jobs carry a standing
allowance *because a job commits repeated model spend that cannot be authorised per occurrence* — so
approving one `create_flow` call is a larger grant than the allowance, and the shipped card's copy
says nothing about recurring spend.

**(b) is adopted as half and rejected as sufficient**: the honest sentence is necessary, but nothing
then reaches the operator's screen at all, and F376 *is* the measurement of an agent not relaying a
refusal — it sent 20 hand-driven messages instead.

**What the change does instead:** the refusal opens a **question of record** through
`ask_question_for_actor` (`questions.py:234`) — durable (nothing sweeps `questions`), rendered on a
top-level destination (`App.tsx:382` → `QuestionsPanel`), and carrying the shipped late-answer path.
Verified in the code this round rather than assumed, because the whole design rests on it:
`answer_question` (`:433-441`) queues the answer whenever the asker is not still waiting, and
`_asker_still_waiting` (`:48`) is true only for a **blocking** question whose wait has not ended and
whose run still lives — so a non-blocking question always takes the queued-and-woken path
(`announce_queued_answer` → `schedule_agent`), and the code says so in its own comment. **The
delivered entry enters at `hop_depth=0`** (`:154`), which is exactly the reset F376 measured the
absence of (hand-driven messages inherit `+1`; that chain died at hop 7 of a budget of 6).

**The answer to DIRECTION.md's deciding question — what happens to the agent's turn while it waits:
it does not wait.** 403 with a typed body (`code: project_setting_blocks_capability`, the setting,
its value, the `question_id`), not 409 + `operator_direction_required`, because 409 is the
poll-and-retry protocol and presumes an answer inside the turn. No new adapter wait is added, which
keeps `agent-capability-plane:107`'s thin-adapter requirement intact.

**Deliberately not in scope, each recorded in the change:** the answer does **not** flip
`allow_agent_jobs` (its only writer is operator-authenticated `PATCH /queue/settings`,
`inbound_queue.py:98`), because a toggle inside the card would be a second control for one setting
and F379's change owns that surface — one-click enable is recorded as its follow-up. F378 gets a
reusable helper and no caller. No migration, no schema change, no UI change.

**Two traps written into `design.md` D9 for the later rounds:** `agent-capability-plane:281` still
requires the *unasked question* surfacing whose detector was dropped in migration `0082` — stale
spec text, do not build on it; and the gate returns early when both attribution headers are absent
(`jobs.py:39`), so a test without real agent attribution proves nothing about this change (the F190
shape).

**R2 was not started in this firing, deliberately.** The round's value is an *independent*
re-derivation, and the process that wrote R1 cannot supply one from the same context — collapsing
them is the one thing `day-window.md` says not to do to save time. `current`/`next_action` set to
`refused-capability-r2` for a fresh firing.

## Iteration 4 — refused-capability-r2: the round changed one thing, and it was the kind only a round finds

Branch and log verified against `STATE-day.json` before starting: `git branch --show-current` is
`autonomous/2026-09-17-daily`, `git log --oneline -5` has `fbb5f9d` at HEAD matching the state's
recent-commits claim, `HEAD` == `@{u}`, tree clean. No reconciliation needed.

**Step 1 — D-0's merge gate, retried first, at the firing's start as the last iteration's lesson
required.** Measured at HEAD (`fbb5f9d`), all four:

1. `git rev-list --count HEAD..origin/master` = 0 after `git fetch origin master`. **PASS.**
2. `git status --short` empty and `HEAD` == `@{u}` (`fbb5f9d`). **PASS** — the first firing where
   condition 2 has ever held at the gate, which is exactly what running it first bought.
3. `gh run list --branch autonomous/2026-09-17-daily`: CI at `fbb5f9d` is **`in_progress`**. **FAIL.**
   (`d3adcbc` is `success`, but that is the previous commit and a green from an earlier sha is not
   this sha's green — the state file says so and `a392e9b` and `798b2a5` are why.)
4. No line-initial `HOLD MERGE` in `spec-queue/DIRECTION.md`. **PASS.**

Gate not open, condition 3 only, for the fourth firing running. The structural problem is now fully
measured and is worth the operator's one line on the review page: **each iteration's own commit
starts a ~13-minute CI run, so at the next firing HEAD's run is either in progress or was started
by the commit this same firing is about to make.** Three of four conditions held here; the only
firing that can ever see four is one that starts more than ~13 minutes after the previous one
finished pushing. Left open, moved to step 2 rather than spending the firing on a poll.

**Step 2 — R2 for `a-refused-capability-reaches-the-operator`.** An independent re-derivation against
the code: `jobs.py`, `questions.py`, `permission_requests.py`, `run_task_binding.py`, `scheduler.py`,
`conversations.py`, `mcp_server.py`, `db/models.py`, `schemas/questions.py`, `QuestionsPanel.tsx`,
`App.tsx`. R1's five nominated claims were attacked by reading the code, not by re-reading R1.

**The core decision survives.** The record is still a question of record; the refusal is still a 403
that does not wait. Each of R1's load-bearing facts re-measured and confirmed:

- **Nothing sweeps a non-blocking question.** The only run-end sweep's candidate query filters
  `created_by_run_id == run.id AND blocking IS TRUE` (`run_task_binding.py:715-722`); there is no
  `DELETE` against `questions` anywhere in `hub/hub`; `record_wait_ended` (`:871`) is a guarded
  `UPDATE` of a timestamp, not a removal.
- **(a)'s row really is turn-scoped.** `expire_pending_for_run` is `UPDATE ... SET status='expired'`
  whose caller commits it into the run-end transaction (`permission_requests.py:22-38`);
  `OPERATOR_DECISION_TIMEOUT` is 120s (`mcp_server.py:927`); `_report_wait_ended` POSTs `/expire`
  (`:1512`).
- **A dict detail reaches an MCP agent as a sentence.** `_readable_detail` returns
  `detail["message"]` when non-empty (`mcp_server.py:124-140`), `_hub_request` uses it for
  `HubAPIError.detail` (`:195`), and `HubAPIError` *also* keeps the structured body in `.data`
  (`:92-101`) — so the `question_id` survives without prose-parsing. Recorded as an extra assertion
  in task 4.8.
- **Committing inside the gate is safe at all four call sites.** At `jobs.py:568`, `:847`, `:1163`
  and `:1294` the gate is the **first statement** after `project_id, _ = project` — nothing of the
  route's own is pending when `ask_question_for_actor` commits. Task 2.2 turned from "check this"
  into the answer plus the invariant that keeps it true.
- **Task 2.3 answered by ordering.** `archive_job` calls the allowance gate at `:1163` and
  `require_operator_direction` only at `:1182`, inside the `agent_identity is not None` branch: an
  agent with the allowance off raises out of the gate and never reaches the 409 path. No two records.
  The ordering is load-bearing and is now written down as such.

**What the round changed, and why it is the kind of thing only an independent round finds.** R1's
task 1.4 said to open the question with `created_by_run_id` = the refused agent's run.
`ask_question_for_actor` denormalises the run's conversation onto the row
(`questions.py:255` → `conversations.conversation_id_for_run:354`), and **three readers join work to
a question through `conversation_id`, none of them mentioned anywhere in R1**:

1. `conversations.py:433-441` — the conversation attention rail. Its query is `answered IS FALSE AND
   wait_ended_at IS NULL`, with **no `blocking` filter**, and `"waiting"` deliberately outranks
   `"running"`. Nothing would ever clear ours, because the only other writer of `wait_ended_at` is
   the sweep that filters `blocking IS TRUE`. A loop's conversation would read *"waiting on the
   operator"* for the rest of its life while its run kept working — which that file's own comment
   names as the defect to avoid, *"F14's own defect, inverted"*.
2. `scheduler._pending_loop_request:400-452` — when a loop's queue drains, the most recent unanswered
   `Question` on the prior firing's conversation is reported as what the loop was waiting on. Also
   unfiltered by `blocking`. An agent refused `create_flow` inside a busy loop would make that loop
   report our record as its blocker.
3. `jobs.py:399-410` — the per-job open-question count the operator reads as *"these still need
   me"*. This one our record would answer honestly; losing it is the cost, and it is stated rather
   than hidden.

So R2 reversed it: **the record carries no run and therefore no conversation.** The reasoning is
D1's own — the record's value is that it is independent of the turn, and stamping the turn's
conversation onto it re-couples it through three readers, two of which then report something untrue
about a run that is not waiting. Checked for None-safety along the whole answer path before changing
it (`_asking_run_has_ended` returns False on a null run and is unreachable anyway because
`_asker_still_waiting` short-circuits on `blocking`; `release_block_for_question` returns None on a
null `blocked_task_id`; nothing in `PATCH /questions/{id}` reads `question.conversation_id`), and
noted that a question with a null run and conversation is the **shipped** shape of one asked through
the operator route. New **design D10**, new task 4.9 asserting the rail is not flipped, new spec
requirement clause and a new scenario — *the refused run is not reported as waiting*.

**Two smaller corrections.** **D11**: `blocking=False`'s cost on the operator's panel, which R1
deferred to this round, is now measured — `QuestionsPanel.tsx:150-151` partitions on `blocking &&
stillWaiting`, so the record renders as a full row in the ordinary **Unanswered** section rather
than under the red *"agents are waiting"* banner, with no urgency timer. That is the correct
rendering, not a concession: nothing is waiting, and `wait_expires_at` is NULL for a non-blocking
question by construction. **D12**: `QuestionCreate` makes `options` (min 2), `header` and
`multi_select` **mandatory**, which R1's task 1.4 as written does not supply. Verified by running
it, not by reading it:

```
R1 task 1.4 as written  -> REJECTED: options: Field required; header: Field required; multi_select: Field required
one option only         -> REJECTED: List should have at least 2 items after validation, not 1
two options + header + multi_select -> ACCEPTED
```

D4's two options satisfy the minimum exactly, so the change is unaffected in substance — but an
implementer following R1 literally would have hit a three-field `ValidationError` on the first run.
Split out as task 1.5; the old 1.5 became 1.6.

`openspec validate --strict a-refused-capability-reaches-the-operator` **passes**; `openspec list`
shows `0/23 tasks` (21 before; +1 for the schema task, +1 for the rail test). `proposal.md`,
`design.md`, `tasks.md` and the `agent-capability-plane` delta all amended; nothing implemented,
which is the point of a round.

**R3 was not started in this firing**, for the same reason R2 was not started in the last one: the
round's whole value is that it does not share a context with the one it is checking.
`current`/`next_action` set to `refused-capability-r3`.

## Iteration 5 — refused-capability-r3: the decision held, two of its tasks would have shipped defects

Branch and log verified against `STATE-day.json` first: `git branch --show-current` is
`autonomous/2026-09-17-daily`, `git log --oneline -8` has `5adff4b` (R2) at HEAD exactly as the state
claims, `HEAD` == `origin/autonomous/2026-09-17-daily`, tree clean. No reconciliation needed.

**Step 1 — D-0's merge gate, run first again.** Measured at HEAD (`5adff4b`):

1. `git rev-list --count HEAD..origin/master` = 0 after `git fetch origin master`. **PASS.**
2. tree clean, `HEAD` == upstream. **PASS** (second firing running).
3. `gh run list --branch autonomous/2026-09-17-daily --json headSha,status,conclusion`: CI at
   `5adff4b` is **`in_progress`**, created `08:38:46Z`, and this firing started `08:40:17Z` — **one
   minute thirty-one seconds** after the run began. **FAIL.**
4. No line-initial `HOLD MERGE` in `spec-queue/DIRECTION.md`. **PASS.**

Three of four for the second firing running, and the measurement is sharper than iteration 4's
estimate: the loop fires so soon after its own push that HEAD's CI has barely started. `fbb5f9d`'s
run was *also* still `in_progress` eleven minutes after it began, so ~13 minutes is the floor rather
than the typical. The gate as specified cannot pass on this cadence unless a firing makes no commit,
or the gate is allowed to wait for a conclusion. That is the operator's decision; it goes on the
review page rather than being taken here. Left open; went straight to step 2.

**Step 2 — R3 for `a-refused-capability-reaches-the-operator`.** Independent re-derivation against
`mcp_server.py`, `api/v1/jobs.py`, `api/v1/questions.py`, `api/v1/agents.py`, `schemas/questions.py`,
`db/models.py`, `conversations.py`, `AnswerForm.tsx`, `AgentOutputPanel.tsx`,
`lib/pendingQuestions.ts` and the trial profile database. `design.md`'s Rounds section was read
**last**, only to write into it.

**The decision stands for the third round running:** a question of record, a 403 that does not wait,
no run and no conversation on the row. **Two tasks under it did not stand, and both are the shape
this repository keeps shipping — code that passes its tests and cannot fire.**

1. **The dedupe's "a no is a no" branch could not fire** (new design **D13**). R1 and R2 both keyed
   it on `answer_labels` containing the *"Leave it off"* label. Both surfaces that answer a question
   send `labels` **only when the operator typed nothing** — `AnswerForm.tsx:33-38`
   (`writtenAnswer ? {id, answer} : {id, answer, labels}`, and typing clears the selection at
   `:84-87`) and `AgentOutputPanel.tsx:934-942` (`const labels = typed ? [] : …`) — and the route
   stores exactly what arrived (`questions.py:405`). Run, not read:
   `QuestionAnswer(answer="no, leave it off, too expensive").labels == []`. So an operator who types
   *"no, leave it off"* is classified **affirmative**, and the rule written to stop the Hub nagging
   nags. Free text is not an edge case — `db/models.py:940-941` calls it the product's guarantee.
   Replaced with a rule that never reads the answer's polarity: a **resolved** record (answered *or*
   declined, `_completed_batch`'s own definition) stops the asking, and the refusal quotes what the
   operator actually said. Nothing is lost by not interpreting, because the gate runs only while the
   setting is false (`jobs.py:47`) — a resolved record plus a firing gate already means *they saw
   these exact words and it is still off*. Cost stated: an operator who says yes and forgets the
   toggle is not re-asked; the refusal tells the agent so in their own words, and D4's follow-up
   (one-click enable, F379's change) removes the case.
2. **The tool list was wrong in both directions** (**D7**, new table). **There is no `update_job`
   MCP tool** — that is the PATCH route, reached by `toggle_job` — and `create_job`
   (`mcp_server.py:602`), the plainest of the six and the likeliest for an agent to reach for, was
   named by neither earlier round. The six are `create_job`, `create_loop`, `create_flow`,
   `toggle_job`, `run_job`, `archive_job`; cross-checked against `agents.py`'s `_Operation` table
   (`:1239`, `:1251`, `:1260`, `:1269`, `:1293`, `:1328`), which lists the same six and no
   `update_job`. Five propagate the refusal untouched through `_job_effect` → `_hub_request`;
   `archive_job` re-raises anything that is not `operator_direction_required`, its own comment
   naming *"the allowance is off"*. Task 3.1's assumption about the error paths holds; its list of
   tools did not. Caught alongside it: `toggle_job` and `run_job`'s contract text says only *"same
   allowance."* — the sentence this change makes false.

**Two smaller changes.** **D8's reuse claim narrowed**: F378's blocking state is
`templates.get(body.template)` over the empty `project_sessions` table (`agents.py:2053-2059`), which
has no setting, no value and — the finding itself — **no place the operator changes it**, so three of
`refuse_for_project_state`'s parameters have no truthful value there. What F378 can reuse is the
shape, not necessarily the signature; its repair needs a writer first. **D6 extended**: `.data` is
read by exactly one place on this surface (`archive_job`, and only for `operator_direction_required`,
`:843-848`), so the structured `question_id` **does not reach the model** — it must be in the
sentence. Executed: `str(HubAPIError(403, …, "POST", "/jobs", detail))` is
`Hub rejected POST /jobs (403): <message>`, which is exactly how F376's own sentence reached its
transcript. Task 4.8's `.data` assertion stays, as the future-adapter guarantee it was.

**Two measured costs recorded rather than repaired.** (a) **D14** — a fourth reader survives D10
because it keys on `from_agent`: the conversation tray's `activeQuestionFor`
(`lib/pendingQuestions.ts:24-46`) is unfiltered by `blocking` and sorts `asker_waiting !== false`
first, and `asker_waiting` is a **constant `true`** for any question with no run
(`questions.py:333-344`, *"Unknown asker → presumed waiting"*). So the record can be shown in the
refused agent's tray ahead of a question that agent really is blocked on. Not repaired from inside
this change: it is the behaviour of *every* run-less question, including every operator-posted one
(`questions.py:293`). Pinned by new task 4.10, looked at in task 5.2's drive, and **filed as F381
(C)** so it outlives this change's archive — the repair (`asker_waiting` reads `blocking`, as
`_asker_still_waiting` already does) belongs to whoever next touches that field.
(b) **D13's query notes** — `Question` has no `__table_args__` and no index on `project_id` or
`question` (read from `sqlite_master`: six indexes, neither column; 32 questions on the trial
profile's busiest project), so the dedupe scan is negligible and needs no migration; and two
simultaneous refusals open two records — bounded, cosmetic, and closable only by a unique index this
change has decided not to make.

**Confirmed by reading, not by quoting R2.** D10 is safe at delivery for a reason R2 never stated:
`deliver_batch_if_complete` never reads `question.conversation_id` at all — it resolves
`latest_open_conversation(project, from_agent)` and opens an operator-origin conversation when there
is none (`questions.py:145-151`), naming it from the question's first 120 characters
(`conversations.py:139-149`, `CONVERSATION_TITLE_MAX_LENGTH = 120`), a no-op on a thread that already
has a title. So the answer reaches the same thread whether or not the run was stamped. Also
re-confirmed: the gate is the first statement after `project_id, _ = project` at all four sites, and
the interpolated current value cannot vary the dedupe key because the gate's own branch is
`not project.allow_agent_jobs`.

`openspec validate --strict a-refused-capability-reaches-the-operator` **passes**; `openspec list`
reads `0/24 tasks` (23 before; +1 for task 4.10). `proposal.md`, `design.md`, `tasks.md` and the
`agent-capability-plane` delta amended — the delta gained a SHALL forbidding the Hub from inferring
an answer's intent, three scenarios (an answer however it was written, a written answer is not
re-asked, the refusal reports rather than judges) and one for the sentence being enough on its own.
Nothing implemented, which is still the point of a round.

**The three rounds `CLAUDE.md` requires are done**, and both later rounds changed something no
re-read of the previous round could have found. `current`/`next_action` move to **D-5**, the review
page.

---

## Iteration 6 — D-5: the review page

**STEP 1, D-0's merge gate, retried at the start of this firing.** `gh run list --branch
autonomous/2026-09-17-daily --limit 1` for `76b398b`: run `35327002703`, `status: in_progress`,
created `08:57:15Z`, checked `09:00:09Z` — under 3 minutes in, against the measured 13+ minute
floor. Condition 3 still fails; conditions 1, 2, 4 hold. No merge attempted; D-0 stays open, exactly
as iteration 5 anticipated. No commit was made before this check, so the earlier-green trap did not
apply.

**STEP 2, D-5.** Wrote `spec-queue/review/review-2026-09-18.html`, self-contained (no external
stylesheet/script/font, full document, light `:root` redefined under
`@media (prefers-color-scheme: dark)`, explicit `body` background — copied the CSS scaffold from
`review-2026-09-14.html` verbatim and only changed content). Six sections: (1) the branch, 2 days
old, the merge gate's state and the cadence problem with all three operator options (a/b/c) plus the
two unexplained CI failures on docs-only commits; (2) nothing built last night — read from
`2026-09-17-night-log.md`, including the still-unread background hub-suite log; (3) D-1's drive
result (5/5 checks, `d1_0918_refused_capability.py`) and F381, one paragraph, file:line for both the
sort (`pendingQuestions.ts:38`) and the presumption (`questions.py:333-344`); (4)
`a-refused-capability-reaches-the-operator` — the problem, R1's argument, R2's D10, R3's D13 and D7,
the two costs recorded not repaired (D13, D14, with F381 cross-referenced), and the closing line that
no round changed nothing; (5) the default night queue, which is the same decision-gated set
2026-09-17's night already surveyed; (6) the three research candidates, noted as non-colliding with
today's steered files.

Appended a `## 2026-09-18` row to `spec-queue/APPROVALS.md` — no status token, no `ORDER:` line, per
`day-window.md`'s D-5 contract — summarizing the change's readiness (0/24 tasks, validate --strict
passes) and the gate cadence question, and stating plainly that approving nothing tonight repeats
2026-09-17's empty-queue night.

**Verification.** Re-read the written HTML back for structural correctness (DOCTYPE, closed tags,
both `:root` blocks present, no external `<link>`/`<script>` src) and confirmed every finding ID,
file:line and commit sha quoted in it against the sources it was drawn from (`FINDINGS.md` F376/
F378/F381, `2026-09-18-day-log.md`'s R2/R3 entries, `2026-09-17-night-log.md`, the `gh run list`
output above, `spec-queue/research/2026-09-18.md`). No new claim was introduced that wasn't already
measured by an earlier iteration or this one's own gate re-check.

`current`/`next_action` move to **D-0** (retry-first) then **D-6** if room, per the reserve rule
(no D-6 in the same firing as D-5 — honored: D-6 was not touched this iteration).

---

## Iteration 7 — D-0 retried (still open); D-6: an unexplained stall recorded, not blind-fixed

Branch and log verified against `STATE-day.json` before starting: `git branch --show-current` is
`autonomous/2026-09-17-daily`, `git log --oneline -8` had `7d565ef` (D-5) at HEAD exactly as the
state claims, `HEAD` == `origin/autonomous/2026-09-17-daily`, tree clean. No reconciliation needed.

**Step 1 — D-0's merge gate, retried first.** Measured at HEAD (`7d565ef`):

1. `git rev-list --count HEAD..origin/master` = 0 after `git fetch origin master`. **PASS.**
2. tree clean, `HEAD` == upstream. **PASS.**
3. `gh run list --branch autonomous/2026-09-17-daily --json headSha,status,conclusion,createdAt`:
   CI at `7d565ef` **`in_progress`**, created `09:02:56Z`, checked `~09:10Z` — about 7 minutes in,
   still under the measured 13-minute floor. **FAIL**, the same shape a fifth firing running.
   Also noticed in passing: the *previous* commit's run (`76b398b`, created `08:57:15Z`) was
   **still** `in_progress` at this check, ~13 minutes in and not yet concluded — consistent with
   the floor being closer to CI's real typical duration than a lower bound. **5**: no line-initial
   `HOLD MERGE`. **PASS.**

No merge attempted; D-0 stays open. Nothing new to add to `decisions_for_user` — the shape is
identical to iterations 4–6, already on the review page.

**Step 2 — D-6, the carve-out's own escape hatch.** Read
`.claude/autonomous/tmp/2026-09-17-hubtests-compose.log` (the flagged candidate) before touching
anything: frozen at `[ 14%]`, 21 trailing dots with no percentage marker after them (mid-batch, not
a clean stopping point), mtime `2026-09-17 23:10:xx`, unchanged for **10+ hours** at the moment of
this check. `Get-CimInstance Win32_Process -Filter "name='python.exe'"` over every live `python.exe`
on the machine found no process with that command line — it did not finish, did not error into the
log, and nothing else killed it traceably. This is not "still running," which is what the queued
candidate text assumed; it is dead.

That alone would be a blind fix if repaired guess-first (no test name, no traceback — `-q` gives
only dots), so per the queued instruction's own escape hatch, started a **fresh, watched** rerun
instead of touching code: `py -3.11 -m pytest hub/tests/ -q` at `09:06 UTC`, logging to
`.claude/autonomous/tmp/2026-09-18-hubtests-d6.log`. Watched it live rather than trusting an exit
code later (`timeout 180` polling loop, one Bash call): it crossed `14%` cleanly at `09:12:19` with
no gap longer than the ordinary per-batch delay, and reached `19%` by `09:15:02` (~2%/minute, a
pace that finishes well inside last night's 105-minutes-and-still-14% measurement). Zero
`FAILED`/`ERROR` lines in either log.

**Conclusion: the stall did not reproduce.** Same suite, same machine, same completion point,
different outcome — rules out a simple deterministic failing/hanging test as the sole explanation
and points at something environmental (a leaked lock, a port two overnight processes raced for —
the trial Hub on `:10` and the operator's `:8000` were both live per last night's own logs — or
similar). Filed **F382 (C)** in `FINDINGS.md` with both timelines, left `open` and undiagnosed
rather than closed with a guessed fix. Left the fresh rerun running in the background, unkilled, so
its eventual conclusion (pass, or a second genuine stall to compare against) lands on record for
whoever next reads this. `STATE-day.json`'s `D-6` queue entry set to `done` — the unit of work this
iteration owed was investigate-and-record, which is now complete; no code changed, nothing to
verify by running a suite that has not itself finished.

**Reserve check.** `stop_at` is `2026-09-18T17:00:00+01:00` (16:00 UTC); this firing started
~09:10 UTC, nowhere near the 45-minute boundary — reserve rule does not apply.

Queue is now empty apart from `D-0`. `current`/`next_action` stay on **D-0**, retried fresh each
firing, with a note to check `F382`'s background rerun's conclusion and consult
`spec-queue/APPROVALS.md`/`DIRECTION.md` once the gate does open, rather than inventing new work
against an empty queue.

---

## Iteration 8 — D-0 retried (still open, same shape a sixth time)

Branch and log verified against `STATE-day.json` before starting: `git branch --show-current` is
`autonomous/2026-09-17-daily`, `git log --oneline -5` had `52c4da7` (D-6) at HEAD exactly as the
state claims, `HEAD` == `origin/autonomous/2026-09-17-daily`, tree clean. No reconciliation needed.

**Step 1 — D-0's merge gate, retried first, before any commit.** Measured at HEAD (`52c4da7`):

1. `git rev-list --count HEAD..origin/master` = 0. **PASS.**
2. `git status --short` empty, `HEAD` == `@{u}`. **PASS.**
3. `gh run list --branch autonomous/2026-09-17-daily --limit 3`: CI at `52c4da7` **`in_progress`**,
   run `35328691712`, created `09:16:41Z`, checked `~09:20:08Z` — about 3.5 minutes in, well under
   the measured 13-minute floor. **FAIL**, the same shape a sixth firing running. (For reference,
   `7d565ef`'s own run had since concluded **success** at 13m42s — consistent with the floor, not
   evidence for this sha.)
4. No line-initial `HOLD MERGE` in `spec-queue/DIRECTION.md`. **PASS.**

No merge attempted; D-0 stays open. Per `next_action`'s instruction, this firing started well
inside 15 minutes of the branch's last push (~3.5 min), so no extra `gh run list` short-circuit was
needed beyond the one already run. Nothing new for `decisions_for_user` — identical to iterations
4–7, already on the review page awaiting the operator's merge-gate-cadence decision.

**Step 2 — F382's background rerun, checked, not yet concluded.** Tailed
`.claude/autonomous/tmp/2026-09-18-hubtests-d6.log`: still running, progressed past where iteration
7 left it (was `19%` at `09:15:02`, now past `31%` with a `31%` batch line ending in bare dots, no
percentage marker yet for the current batch), zero `FAILED`/`ERROR` lines. Left running, unkilled —
nothing to record in `FINDINGS.md` yet since it has not concluded either way; noting its progress
here rather than writing a premature "either way" line into F382 before there is a conclusion to
report.

**Reserve check.** `stop_at` is `2026-09-18T17:00:00+01:00` (16:00 UTC); this firing started
~09:20 UTC, nowhere near the 45-minute boundary.

Queue is otherwise empty (D-1 through D-6 done). `current`/`next_action` stay on `D-0`, to be
retried fresh next firing; no new spec or drive work started against an empty queue, per the
playbook's instruction not to invent work while the gate question is still pending the operator.

---

## Iteration 9 — D-0 retried (still open, same shape a seventh time)

Branch and log verified against `STATE-day.json` before starting: `git branch --show-current` is
`autonomous/2026-09-17-daily`, `git log --oneline -5` had `5e42097` (iteration 8's D-0 commit) at
HEAD exactly as the state's recent-commits claim, `HEAD` == `origin/autonomous/2026-09-17-daily`,
tree clean. No reconciliation needed.

**Step 1 — D-0's merge gate, retried first, before any commit.** Measured fresh at HEAD (`5e42097`):

1. `git fetch origin master`; `git rev-list --count HEAD..origin/master` = 0. **PASS.**
2. `git status --short` empty, `HEAD` == `@{u}` (`5e42097`). **PASS.**
3. `gh run list --branch autonomous/2026-09-17-daily --limit 3 --json headSha,status,conclusion,createdAt`:
   CI at `5e42097` run `35329070930`, **`in_progress`**, created `09:21:03Z`, checked `09:25:14Z` —
   about **4 minutes** in, well under the measured 13-minute floor. **FAIL**, the same shape a
   seventh firing running. (`52c4da7`'s own run was also still `in_progress` at this check, ~9
   minutes in; `7d565ef`'s run had since concluded `success` at 13m42s — consistent with the floor,
   not evidence for the current HEAD.)
4. No line-initial `HOLD MERGE` in `spec-queue/DIRECTION.md` (`grep '^HOLD MERGE'` — no match).
   **PASS.**

No merge attempted; D-0 stays open. Nothing new for `decisions_for_user` — identical shape to
iterations 4–8, already on the review page awaiting the operator's merge-gate-cadence decision.

**Step 2 — F382's background rerun, checked, still not concluded.** Tailed
`.claude/autonomous/tmp/2026-09-18-hubtests-d6.log`: progressed past where iteration 8 left it (was
mid-`31%` batch at last check, now past `42%`), zero `FAILED`/`ERROR` lines throughout, a handful of
`s` (skips) consistent with ordinary test selection, no stall pattern. Left running, unkilled —
still nothing conclusive to record in `FINDINGS.md` until it finishes.

**Reserve check.** `stop_at` is `2026-09-18T17:00:00+01:00` (16:00 UTC); this firing started
~09:25 UTC, nowhere near the 45-minute boundary.

Queue is otherwise empty (D-1 through D-6 done). `current`/`next_action` stay on `D-0`, to be
retried fresh next firing; no new spec or drive work started against an empty queue.

---

## Iteration 10 — D-0 retried (still open, same shape an eighth time)

Branch and log verified against `STATE-day.json` before starting: `git branch --show-current` is
`autonomous/2026-09-17-daily`, `git log --oneline -5` had `c6109c3` (iteration 9's D-0 commit) at
HEAD exactly as the state's recent-commits claim (iteration 9's own log entry and commit had
already landed before this firing started), `HEAD` == `origin/autonomous/2026-09-17-daily`, tree
clean. No reconciliation needed.

**Step 1 — D-0's merge gate, retried first, before any commit.** Measured fresh at HEAD (`c6109c3`):

1. `git fetch origin master`; `git rev-list --count HEAD..origin/master` = 0. **PASS.**
2. `git status --short` empty, `HEAD` == `@{u}` (`c6109c3`). **PASS.**
3. `gh run list --branch autonomous/2026-09-17-daily --limit 3 --json headSha,status,conclusion,createdAt`:
   CI at `c6109c3` run `35329507248`, **`in_progress`**, created `09:26:04Z`, checked `09:30:25Z` —
   about **4.3 minutes** in, well under the measured 13-minute floor. **FAIL**, the same shape an
   eighth firing running. (`5e42097`'s run was also still `in_progress` at this check, ~9 minutes
   in; `52c4da7`'s run had since concluded `success`.)
4. `grep -n '^HOLD MERGE' spec-queue/DIRECTION.md` — no match. **PASS.**

No merge attempted; D-0 stays open. Nothing new for `decisions_for_user` — identical shape to
iterations 4–9, already on the review page awaiting the operator's merge-gate-cadence decision.

**Step 2 — F382's background rerun, checked, still not concluded.** Tailed
`.claude/autonomous/tmp/2026-09-18-hubtests-d6.log`: progressed past where iteration 9 left it (was
past `42%`, now at `62%`), zero `FAILED`/`ERROR` lines throughout, ordinary scattered skips. Left
running, unkilled — still nothing conclusive to record in `FINDINGS.md` until it finishes.

**Reserve check.** `stop_at` is `2026-09-18T17:00:00+01:00` (16:00 UTC); this firing started
~09:30 UTC, nowhere near the 45-minute boundary.

Queue is otherwise empty (D-1 through D-6 done). `current`/`next_action` stay on `D-0`, to be
retried fresh next firing; no new spec or drive work started against an empty queue.

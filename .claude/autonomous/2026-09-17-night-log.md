# 2026-09-17 night window

Newest entry at the BOTTOM. Playbook: `.claude/loops/night-window.md`.

Armed 2026-09-17T22:55:04+01:00 on `autonomous/2026-09-17-daily` (parent master@f9db321), model opus.

## Iteration 1 (compose) — 2026-09-18 ~00:50 UTC+1

**Branch confirmed:** `autonomous/2026-09-17-daily`, matches `STATE-night.json`, clean tree, up to
date with origin. `git log` top five commits match the arm commit's parent (`f9db321`) plus the
arm commit itself (`f4ff939`).

**APPROVALS.md:** newest section is `## 2026-09-16`, not `## 2026-09-17` — tonight's armed date has
no section. Per the playbook's own worked example (compose took a stale `ORDER:` verbatim on
09-14), checked the 09-16 section's `ORDER: a-turn-says-how-it-ended,
runner-model-is-chosen-from-the-catalog` against `git log --all` and
`openspec/changes/archive/`: **both changes are already archived**
(`2026-09-03-a-turn-says-how-it-ended`, `2026-09-02-runner-model-is-chosen-from-the-catalog`,
commits `night(13)`/`night(14)` etc.). That `ORDER:` was built on an earlier night. Treated tonight,
correctly, as "no section for today" — whole window goes to the backlog. Checked
`spec-queue/DECISIONS.md` for a newer redirect: none dated 09-16 or 09-17.

**Backlog, item by item:**
- **Unarchived changes** (`openspec/changes/`, excluding `archive/`): exactly one,
  `an-unstaffed-review-names-its-holders`. Read its `tasks.md` in full: it carries an explicit,
  dated stop-notice — "STOPPED AT REV, 2026-09-14. Do not build any task here" — because
  `F352-free` was decided 2026-09-15 as option **(f)**, not the **(e)** every remaining task (groups
  1, 2, 2.14, 5) was written against, and `DECISIONS.md:709-710` confirms it "needs its own
  re-derivation round against (f) before it can build." Task 2.14 is explicitly named among the
  tasks written against the stale (e), so it is **not** an exception. This change is not buildable
  by a FIX window tonight — the re-derivation is a spec round, which this window does not do
  (`STATE-night.json`'s own purpose line). **Not queued as work; flagged below for the day window.**
- **Open findings**, via `py -3.11 scripts/classify_findings.py`: five open severity-A —
  F299, F301, F325, F352, F375. Read each section:
  - F299/F301 — open, explicitly waiting on an operator decision grouped with F339/F340
    (`DECISIONS.md`, `### 2026-09-13 afternoon`, OPEN block). Not FIX work.
  - F325 — no proposal; reproduction needs Codex, which is undrivable (plan cancelled 2026-08-29,
    per project memory). Not FIX work.
  - F352 — open only for the "rung-3 naming" half, which is exactly the blocked change above.
  - F375 (bare `..` escapes rule 4's separator gate, a real sandbox traversal hole) — mechanism and
    fix shape are both well understood in the finding's own text, but **it has no proposal** under
    `openspec/changes/`. Per the playbook, "a finding with no proposal needs the day window first —
    queue it as a note to tomorrow, not as work."
  - No other unrated/lower-severity items were substituted in, since the backlog order is A before
    B before C and none of the five A's clears the bar.
- **APPROVED rows:** none (no section for today).

**Conclusion: nothing in tonight's backlog is buildable by this window without spec work only the
day window/DECIDE session may do.** This is itself the finding worth recording, per the playbook's
own instruction not to let a noticed problem go un-converted into an actionable item.

**Tree-green check:** started `py -3.11 -m pytest hub/tests/ -q` in the background at compose start.
It ran past **105 minutes** without producing output or a `DONE_` marker — well beyond the
historical range (14:39 quiet, 24:50 contended, 46:41 worst-case on 2026-09-08). Confirmed via
`Get-Process` that the worker process was alive and its CPU time kept climbing throughout (2169s →
5431s over the wait), so it was not the F292/F295 lock-hang shape (which shows near-zero CPU) —
this machine is just heavily contended tonight (two other long-running `python` processes, PIDs
11208 and 19740, were already active). Left running in the background rather than killed; its
result was not needed to reach the conclusion above (nothing was going to be built regardless of
its color), and blocking the rest of the window on it further would waste the firing budget on a
compose step whose real output — an empty, decision-gated queue — is already determined. **Not
verified green or red before this entry closes; flagged for the next firing or the day window to
check the file the background run wrote:**
`.claude/autonomous/tmp/2026-09-17-hubtests-compose.log`.

**Decisions/notes for the day window (also see `decisions_for_user`):**
1. `an-unstaffed-review-names-its-holders` needs a re-derivation round (against F352-free's (f),
   not (e)) before any of its remaining tasks (groups 1, 2 incl. 2.14, 5) can build. This is spec
   work, not a FIX-window task.
2. F375 (bare `..` traversal past the sandbox's rule 4) needs a proposal — no design decision is
   apparent, just an implementation, but the round discipline still applies.
3. Check `.claude/autonomous/tmp/2026-09-17-hubtests-compose.log` for the hub suite result this
   iteration started; if red, it is unrelated to any change made tonight (none were made) and is
   the day's first job to characterize.

**Queue set to empty; `next_action` set to `null`.** No model invocation should be spent
rediscovering this backlog is blocked. The night window unregisters itself.

# 2026-09-22 night window

Newest entry at the BOTTOM. Playbook: `.claude/loops/night-window.md`.

Armed 2026-09-22T22:55:03+01:00 on `autonomous/2026-09-21-daily` (parent master@6c0a467), model opus.

## Iteration 1 — compose (2026-09-22 ~22:00-22:20 UTC)

Branch confirmed: `autonomous/2026-09-21-daily`, `git log` head `9d658bd`, matches STATE. No
reconciliation needed.

Backlog page: `py -3.11 scripts/backlog_page.py --check` → current, nothing moved since last
generation.

`spec-queue/APPROVALS.md` newest section is `## 2026-09-22` (matches tonight's armed date) — this
is a live token, not history. It carries:

```
ORDER: a-hub-that-was-not-told-which-database-refuses-to-open-one all groups,
       then an-unstaffed-review-names-its-holders groups 2, 5, 6
```

Both approved explicitly (F388, A; and F352's visibility half). Note:
`a-hub-that-was-not-told-which-database-refuses-to-open-one/tasks.md`'s own header still reads
"R4 ... Not approved. Nothing here is built." — that line is stale inside the tasks file itself;
`spec-queue/APPROVALS.md` (2026-09-22, ~21:30 note) is the authority and it is approved for
tonight. Recording this discrepancy so nobody re-reads the tasks.md banner and skips the ORDER.

CI verdict for inherited sha `9d658bd`: no conclusion yet (just pushed, ~5 min old at check time,
not hung). Previous sha `9b5f4bd` succeeded. No red streak.

Local check: `py -3.11 -m pytest hub/tests/test_config.py -q` → 4 passed (matches the pre-change
baseline tasks.md task 1.8 expects). Full `hub/tests/` not run this iteration — out of scope for a
compose-only firing; the first impl item will run the group's relevant chunk before and after.

Queue written into `STATE-night.json` per the ORDER, split into one item per group (groups 1+3 of
the first change combined into a single item per the approval note — they must land in one
commit). `current`/`next_action` set to the first item, `hub-refuses-groups1and3-impl`.

## Iteration 2 — hub-refuses-groups1and3-impl (2026-09-22 ~23:00-23:40 UTC)

Branch/log reconciled against `git log` before starting: head `edc3a20`, matches STATE. No drift.

Built groups 1 and 3 of `a-hub-that-was-not-told-which-database-refuses-to-open-one` in one commit,
per the approval note (group 1 alone breaks `make ui` / `scripts/drive/*.py`).

**Group 1** (`hub/hub/config.py`, `hub/tests/test_config.py`): added `HubNotToldWhichDatabase`
(`# noqa: N818` — ruff's `N` selector is on in this repo's `pyproject.toml`, and the existing
`*RefusedError` precedents in `hub/hub/*.py` use the same suppression for the same reason: "refused"
is the outcome, not a fault); replaced the silent `_default_database_url` factory with
`_refuse_to_guess_a_database`, which raises naming the absolute path, `DATABASE_URL`, and bare
`agentweave`; kept `_default_database_url` unchanged and quoted by the refusal. Rewrote
`test_config.py`'s three classes into the refusal test (1.5), the override/env-file/relative-value
tests (1.6), and repointed the CLI-drift test at `_default_database_url()` directly (1.7, since
calling `Settings(...)` there now raises unconditionally). Fixed the stale "no dependency edge"
docstring (1.9).

Mutation-checked live: reverted `database_url`'s default_factory back to `_default_database_url`,
confirmed `test_no_database_url_anywhere_refuses_instead_of_guessing` fails, restored. `py -3.11 -m
pytest hub/tests/test_config.py -v` → **6 passed** (task 1.8; before this change it was 4 passed).

**Group 3**: `scripts/refresh_ui_bundle.py` and `scripts/drive/n10_route_reachability.py` now set
`DATABASE_URL` to `:memory:` before importing/launching `hub` (3.1, 3.5). Re-ran the launch sweep
(`git grep -n "hub.main:app\|hub.main:run"`, excluding archives/logs/FINDINGS.md/historical
`.claude/autonomous/` logs) and it matched tasks.md's list exactly — no new caller appeared since R4.
Fixed the two live launch sources that had no `DATABASE_URL` (`.claude/skills/e2e-loop/SKILL.md`,
`.claude/skills/autonomous-session/SKILL.md`) by naming the trial profile's `DATABASE_URL` inside the
`cmd.exe /c` string, then ran `scripts/sync_skills.py` to regenerate the `.agents/` mirrors (3.7).
Added a comment-only note to `hub/Makefile`'s `dev` target (no code change, per the task). Verified
`.claude/skills/copilot-test-setup/SKILL.md` already sets `$env:DATABASE_URL` — confirmed unaffected,
no change needed.

**Verification, all with `DATABASE_URL` unset:**
- `py -3.11 scripts/refresh_ui_bundle.py --check` (what `make ui-check` runs) → passed.
- `agentweave --help` → exit 0. `cd testbed/scratch && agentweave doctor` → exit 0.
- `py -3.11 scripts/drive/n10_route_reachability.py` → ran clean, real subprocess launch with the
  route table printed.
- `py -3.11 -m pytest tests/ -q` (CLI suite) → **1 failed, 546 passed, 3 skipped**. The one failure
  is exactly the documented, deferred one: `test_hub_commands.py`'s bare `import hub.config` (task
  4.9, group 4, not built yet). No `test_skill_sync.py` failures were present at this measurement —
  the 4.9 task's 2026-09-20 note of two pre-existing failures there no longer applies; recorded in
  tasks.md so group 4 doesn't need to re-derive it.
- `py -3.11 -m ruff check src/ hub/ tests/`, `ruff check scripts/ --select
  E9,F63,F7,F82,F401,F841`, and `black --check --target-version py311 src/ hub/hub/ hub/tests/
  tests/` (exactly CI's commands and paths) → all passed.
- Kicked off `py -3.11 -m pytest hub/tests/ -q` (full suite, group 5's own task 5.4, not required
  here) in the background to get early signal; it ran far slower than the ~7-minute baseline this
  session's own machine notes record (still going after 30+ min, CPU climbing steadily — plausibly
  contention from the other pytest/ruff/black runs in this same iteration, not a hang). Did not block
  this iteration on it: `hub/tests/conftest.py` sets `DATABASE_URL` before importing anything from
  `hub` (task 3.4, unchanged), so every other `hub/tests/` file's `Settings()` construction is
  unaffected by this change and the only file whose behavior actually changed
  (`test_config.py`) was already verified directly. Left the process running rather than killing it;
  group 5's own 5.4 will run and record the real count when that item is built.

tasks.md: checked off 1.1-1.9 and 3.1-3.7, recorded 1.8's measured count, and added a note after
group 3 with the measured suite counts so group 4 doesn't need to re-derive the expected CLI-suite
failure.

Committed and pushed. `next_action` advanced to `hub-refuses-group2-impl` per the queue order.

## Iteration 3 — hub-refuses-group2-impl (2026-09-22, this firing)

Branch/log reconciled before starting: head was `384254e` (matches STATE's implied position), but
`git status` showed uncommitted changes already present — `hub/hub/main.py`,
`openspec/changes/.../tasks.md`, and a new untracked `hub/tests/test_database_named_before_opening.py`
— a complete, working implementation of group 2 left over from a prior firing that ended without
committing (the log's iteration 2 entry describes only groups 1+3). Read the existing diff in full
rather than redoing the work: `_log_database_before_opening()` in `hub/hub/main.py` matched every
task 2.1-2.5 requirement (absolute path + existed-before + pid, `logger.warning`, no port, credential
stripping for non-sqlite), tasks.md had 2.1-2.8 already checked off, and the test file covered 2.6
(caplog unit test), 2.7 (real uvicorn-subprocess ordering test) and 2.8 (automated refusal check).

Verified rather than trusted:
- `pytest hub/tests/test_database_named_before_opening.py -v` → 5 passed.
- Mutation check 1 (2.7's own note): `logger.warning` → `logger.info` in the sqlite branch →
  `TestGroup2LineOrderingRealProcess` fails (`assert None is not None`, the line never appears on
  stderr since `logging.lastResort` is WARNING-only). Reverted.
- Mutation check 2: moved `_log_database_before_opening()` after `await init_db()` in `lifespan()` →
  same test fails (`assert 109 < 4`, ordering flips, and `existed before: True` since init_db already
  created the file by then). Reverted; `git diff --stat` back to the original 51-line addition only,
  confirming a clean restore.
- Re-ran `test_database_named_before_opening.py` + `test_config.py` together → 11 passed.
- `ruff check src/ hub/ tests/` and `black --check --target-version py311 src/ hub/hub/ hub/tests/
  tests/` (CI's exact commands/paths) → both clean.
- `pytest tests/ -q` (CLI suite) → 1 failed, 546 passed, 3 skipped — the same pre-existing,
  documented group-4 deferral (`test_hub_commands.py`'s bare `import hub.config`), unchanged from
  iteration 2's measurement.
- `mypy src/` → no issues.

Committed (`8a5b935`) and pushed. `next_action` advances to `hub-refuses-group4-impl` per the queue.

## Iteration 4 — hub-refuses-group4-impl (2026-09-22/23, this firing)

Branch/log reconciled before starting: head `63bdb26` matched STATE's implied position, working
tree clean. No drift.

Built group 4 of `a-hub-that-was-not-told-which-database-refuses-to-open-one` — "(d) and the prose
that carried the guarantee." This group is almost entirely doc/docstring prose plus one real CLI
test fix (4.9); did all of it in one commit since none of the pieces are independently useful.

- **4.1** `hub/hub/config.py`: rewrote `_default_database_url()`'s docstring to state the
  qualification R4 flagged — it's the path bare `agentweave` resolves to only when the environment
  carries no `DATABASE_URL` of its own, not "the" CLI database (a pre-existing `DATABASE_URL` or
  `--profile` compute something else).
- **4.2-4.4** `.claude/handoffs/DEAD-ENDS.md`, § *Starting a Hub from source can land on the
  operator's real database*: marked the entry as the failure mode this change removed (with a
  fallback note for older Hubs / told-but-still-cwd-relative callers); replaced the "verify from
  the startup log by absence of a migration chain" advice with "read group 2's line, which states
  the fact directly"; corrected the false "`sqlite3`/`aiosqlite` will not create a missing parent
  directory" sentence — true of raw aiosqlite, false of the Hub (`engine.py:346-350`'s
  `os.makedirs`), so a missing profile directory afterward now proves the URL never reached the
  process at all, not just "a good check to run first"; and added "kill the PID group 2's line
  printed first, then fall back to `netstat -ano`" ahead of the existing `taskkill /IM python.exe`
  warning. Did not reconstruct or assert the 2026-09-19 start's working directory anywhere (R3's
  guard, task 4.2) — described the failure as "a DATABASE_URL that did not reach the process," not
  a location.
- **4.5** `.claude/reference/hubs.md`: added the refusal sentence after the trial-Hub startup
  command, and corrected the `:8000` section's false claim that the desktop shortcut runs
  `pythonw.exe -m uvicorn hub.main:app` directly — it runs `pythonw.exe -m agentweave`, which is the
  CLI, which sets `DATABASE_URL` before spawning uvicorn as a child with `env=os.environ.copy()`;
  a measured live PID matching that spawn ran `python.exe` (a terminal `agentweave` run), not
  `pythonw.exe` (the shortcut), so did not claim that PID is the shortcut's own process. Also noted
  the detached child's `DEVNULL` stdout/stderr means group 2's startup line is emitted on `:8000`
  and read by no one — a known limit, not a defect.
- **4.6** `CLAUDE.md`: one sentence added to the trial-Hub bullet, staying inside the file's own
  size constraint.
- **4.7** `hub/.env.example`: added the two comment lines 4.7 specifies (container path is correct
  as shipped; a source checkout's `hub/.env` copy resolves relative to launch directory). Left
  `hub/.env` itself untouched (gitignored local state) and `docker-compose.yml:34` untouched (4.8,
  no task — confirmed no change needed).
- **4.9** `tests/test_hub_commands.py`:
  `test_first_start_migrations_leave_a_database_that_can_hold_a_conversation` now takes a
  `monkeypatch` fixture and calls `monkeypatch.setenv("DATABASE_URL", ...)` before the bare
  `import hub.config`, which previously raised `HubNotToldWhichDatabase` at import time (the
  deferred failure groups 1-3 left open). `black --target-version py311` reformatted the new call
  onto its own line; applied and re-verified.
- **4.10** `hub/tests/test_config.py`'s module docstring: found it already rewritten (evidently
  touched incidentally during group 1's 1.5 rewrite of the same file) — the false "lands on the
  same database regardless of launch directory" sentence was already gone. Extended it anyway to
  name the told-path behavior and the CLI/Hub drift guard (1.7) explicitly, since 4.10 asked for
  both and neither was stated.
- **4.11** `docs/getting-started/installation.md` § *If you've been running the Hub directly*:
  rewrote to state the three things asked (must name `DATABASE_URL`; refuses and names the path
  when it doesn't; bare `agentweave` is the shared-default route), keeping the no-auto-migration
  note. `docs/reference/env-variables.md`: replaced the `DATABASE_URL` row's false default
  (`sqlite+aiosqlite:///data/agentweave.db`, never true outside the container) with "none" plus the
  refusal/default explanation, and noted on the `AW_HOST` row (the one describing the
  `agentweave-hub` console script) that the script needs `DATABASE_URL` set.

**Verification:**
- `pytest tests/test_hub_commands.py::test_first_start_migrations_leave_a_database_that_can_hold_a_conversation
  -v` → 1 passed.
- Mutation check: stubbed the new `monkeypatch.setenv` call out with `pass` (env clean of
  `DATABASE_URL`) → test fails on `HubNotToldWhichDatabase` raised from the module-level
  `import hub.config`, with the exact 1.4 message. Restored; `git diff --stat` back to the original
  addition only.
- `pytest tests/ -q` (CLI suite) → **547 passed, 3 skipped** — the one pre-existing failure from
  groups 1-3 (this same test) is gone, nothing else moved.
- `pytest hub/tests/test_config.py -v` → 6 passed, unchanged from groups 1+3 (4.1/4.10 only touched
  docstrings, no behavior change).
- `ruff check src/ hub/ tests/`, `ruff check scripts/ --select E9,F63,F7,F82,F401,F841`,
  `black --check --target-version py311 src/ hub/hub/ hub/tests/ tests/`, `mypy src/` — all clean.
- No file under `hub/ui/src` touched (the change's own no-UI-bundle rule, re-confirmed).

Committed (`8b7fed8`) and pushed. `next_action` advances to `hub-refuses-group5-impl` per the
queue.

## Iteration 5 — hub-refuses-group5-impl (2026-09-23, this firing)

Branch/log reconciled before starting: head `773b28b` (iteration 4's state-commit) matched STATE's
implied position, working tree clean. No drift.

Built group 5 of `a-hub-that-was-not-told-which-database-refuses-to-open-one` — "the spec, and what
it costs."

- **5.1/5.2**: applied the change's `MODIFIED` requirement (`Bare invocation is the only entry
  point`) from
  `openspec/changes/.../specs/app-lifecycle/spec.md` into the real
  `openspec/specs/app-lifecycle/spec.md`. The delta was already fully written by the R1-R3 proposal
  rounds (visible in the change's own delta file) — this task's work was applying it, not composing
  it. Replaced the requirement's body (three new paragraphs: launch-path database resolution,
  told-vs-untold framing, the refusal, and the told-database-naming statement) and removed the
  scenario *"The Hub's own database is launch-directory-independent"* (F388's falsified claim),
  replacing it with the five narrowed scenarios: the refusal, "a told database is named before it
  is opened" (narrowed to direct `uvicorn hub.main:app`), "named on every other launch path too"
  (the weaker fallback for `agentweave`/`docker compose up`), "an explicitly named database is
  opened whatever path it names", "the database a launch path names does not depend on its working
  directory", and "a container's own relative database path is not a host path" (narrowed to a
  template file, matching task 4.8's constraint on the compose file). Did not re-litigate R2/R3's
  narrowing — applied it as the delta already stood.
- **5.3**: `openspec` isn't reachable via `py -3.11 -m openspec` (it's the Node CLI on PATH, not a
  Python module) — used the plain `openspec` command instead, matching how the change was validated
  in prior rounds. `openspec validate a-hub-that-was-not-told-which-database-refuses-to-open-one
  --strict` → `Change '...' is valid`.
- **5.4**: `py -3.11 -m pytest hub/tests/ -q` in full. Ran far slower than the ~7-minute baseline
  again (matches iteration 2's note) — took 29m16s wall clock this time with nothing else competing
  for CPU, so the earlier iteration's "contention" theory undersells it; this machine's full
  `hub/tests/` suite is just genuinely ~30 minutes. Waited it out via a background command plus a
  `Monitor` polling loop rather than guessing at the result. **Result: `4640 passed, 86 skipped, 267
  warnings in 1749.85s (0:29:09)`, exit code 0 — no failures.** The 267 warnings are
  `PytestUnhandledThreadExceptionWarning`s from `aiosqlite`'s worker thread racing event-loop
  teardown at interpreter shutdown — the exact class of problem this same spec file's "driver worker
  is gone" requirement (a different, already-shipped requirement) exists to address at runtime, not
  a regression from this change's diff.
- **5.5**: `ruff check src/ hub/ tests/`, `ruff check scripts/ --select
  E9,F63,F7,F82,F401,F841`, `black --check --target-version py311 src/ hub/hub/ hub/tests/ tests/`
  (CI's exact commands/paths), plus `mypy src/` for good measure — all clean.

`git diff --stat` before commit touched only the two files expected:
`openspec/changes/.../tasks.md` (checked off 5.1-5.5, recorded measured counts) and
`openspec/specs/app-lifecycle/spec.md`. Staged both explicitly (no `git add -A`).

Committed (`85b4b28`) and pushed. `next_action` advances to `hub-refuses-group6-drive` per the
queue — the drive that turns F388's fix from "suite is green" into "observed against a real
process," per this change's own group-5/group-6 framing (5.4's own reminder cites F392, filed
precisely because a task was once ticked on an unrun suite; group 6 exists for the matching failure
mode one level up — a green suite standing in for a live drive).

## Iteration 6 — hub-refuses-group6-drive (2026-09-23, this firing)

Branch/log reconciled before starting: head `4e307cd` (iteration 5's state-commit) matched STATE's
implied position, working tree clean. No drift.

Drove group 6 of `a-hub-that-was-not-told-which-database-refuses-to-open-one` live, on port 8093
against a throwaway profile directory (`testbed/scratch/group6-drive/`), never touching
`:8000`/`:8010`/`~/.agentweave/hub/data/`.

- **6.1 the refusal:** recorded the real default database's baseline mtime/size
  (`1790120400 59396096`). Ran `py -3.11 -m uvicorn hub.main:app --port 8093` from the throwaway
  directory (no `.env` there) with `env -u DATABASE_URL`. It failed at import time with
  `hub.config.HubNotToldWhichDatabase`, message matching 1.4's text verbatim, naming the absolute
  default path. Re-measured the default database afterward: mtime/size unchanged, no new file
  appeared under `~/.agentweave/hub/data/`.
- **6.2 the startup line:** started the same command with `DATABASE_URL` naming a new file
  (`throwaway.db`, did not exist yet). The bare, level-less line (`logging.lastResort`, no logger
  name — R2's earlier finding) appeared ahead of the alembic chain: `existed before this process
  opened it: False, pid 23132`. Killed that pid (6.3), then restarted against the same now-existing
  file: the line reappeared with `existed before this process opened it: True, pid 27296` — the one
  thing that changed between runs.
- **6.3 the PID kill:** `taskkill /F /PID 23132` reported success; a follow-up `tasklist /FI "PID eq
  23132"` found nothing. The printed pid is the listening process for a direct `uvicorn` invocation
  (not `agentweave`'s spawn-a-child path), so `DEAD-ENDS.md`'s "launching shell's pid isn't the
  listening one" trap did not apply here. Repeated for pid 27296 at cleanup.
- **6.4 guardrail:** followed throughout — port 8093, throwaway profile directory, deleted after
  both instances were confirmed dead (`rm -rf testbed/scratch/group6-drive`, retried once after a
  "device busy" from `cd` still pointing inside it).
- **6.5:** wrote `D-5, 2026-09-23` in `scripts/drive/FINDINGS.md` narrating all three sub-drives,
  and set F388's `**Status:**` line to `fixed 85b4b28` — only now, with 6.1-6.3 observed, not when
  the suite went green (the exact discipline the change's own group-5 reminder names, after F392).

Wrote 6.1-6.5's measured results into `tasks.md`. `git status --short` before commit showed exactly
the two expected files (`openspec/changes/.../tasks.md`, `scripts/drive/FINDINGS.md`) — staged both
explicitly.

This closes both items belonging to the first change in tonight's ORDER
(`a-hub-that-was-not-told-which-database-refuses-to-open-one`, all six groups). `next_action`
advances to `unstaffed-group2-impl`, the first item of the second change in the ORDER
(`an-unstaffed-review-names-its-holders`).

## Iteration 7 — unstaffed-group2-impl, split (2026-09-23, this firing)

Branch/log reconciled before starting: head `2429c6b` matched STATE's implied position, working
tree clean. No drift.

`unstaffed-group2-impl` as queued named the *whole* of group 2 (`an-unstaffed-review-names-its-
holders/tasks.md`) as one item: tasks 2.1-2.4, 2.6-2.11, 2.14-2.18, plus the 6.0 regression guard —
18 sub-tasks, several with their own R8 blocks specifying exact-string assertions, a 500-character
budget derived by measurement, and clause-precedence rules that five earlier proposal rounds got
wrong in turn (the tasks.md text says so explicitly: R6, R6-8, R7, R8, R9 each corrected the round
before it). That is not one build-sized unit — every other item in this queue, including the six
groups of the first change tonight, covered one coherent slice per iteration; this item was sized
during compose (a survey pass) rather than during a build pass. Attempting all 18 sub-tasks in one
firing risked exactly the failure mode this project's own history warns about repeatedly in this
same tasks.md file (a plan that reads right and an implementation that does not match it) — so
rather than rush a large, hard-to-verify diff, split the item into four pieces sized like the rest
of this queue: 2.14 (this iteration), then 2.1-2.4, then 2.6-2.11, then 2.15-2.18 plus 6.0. Recorded
in `STATE-night.json`'s queue with the reasoning attached to `unstaffed-group2-task2.14`'s own
entry, not only here.

Built task 2.14 only, per the ordering note at the top of group 2 ("R6 — ORDERING CONSTRAINT ...
Do task 2.14 FIRST, before 2.3") and R8's own placement block.

**2.14**: `hub/hub/run_divergence.py`'s `evaluate_run_end`, inside the review branch (`if await
review_task_for_run(session, run) is not None:`), now returns `None` before `_answer_failed_review`
is called when `task.status not in ("completed", "under_review")` — exactly the R8 placement (not
the `blocked` screen, which runs for every run and would silently disable divergence handling for
ordinary work). Read the surrounding code first to confirm the citations: `review_task_for_run`
(`run_task_binding.py`) reads only queue entries, never status, which is why an operator's mid-run
move reaches `_answer_failed_review` today without this screen.

Added `hub/tests/test_review_divergence.py::test_a_review_run_ending_after_its_task_left_review_
restaffs_nobody`: stages a completed-by-author task, stages a review run that said nothing, then the
operator moves the task to `revision_needed` via `apply_transition(..., operator())` while that run
is still bound, then asserts `evaluate_run_end` returns `None`, no `RunDivergence` row and no
`run_diverged` event exist, and `free-agent` (on the roster, genuinely idle) received nothing —
this is the shape that would actually get restaffed today, not a shape the ladder would refuse
anyway.

**R8's second case (nobody free at all, `own_review_remedy`'s bare `assert` raising) is not yet
testable and is not claimed as built.** Today's `resolve_reviewer` rung 3 does not call
`own_review_remedy` at all — that call arrives with task 2.3, not yet built. A test for "no
exception escapes `evaluate_run_end`" would have no mutation that could make it fail before 2.3
lands, so writing it now would be exactly the vacuous-test shape R6/R8 caught elsewhere in this same
file (2.6's R6/R8 notes). Recorded as a deferred note in tasks.md beside 2.14 rather than left
unstated, so the follow-on `2.1to2.4` item knows to add it once 2.3 exists.

**Verification:**
- `pytest hub/tests/test_review_divergence.py -v -k restaffs_nobody` → 1 passed.
- Mutation: replaced the screen's condition with `False and task.status not in (...)` (unreachable),
  re-ran the same test → failed on `assert 'div-d4d02ca22a34' is None` (a divergence was recorded,
  proving the free agent would have been restaffed without the screen). Reverted; `git diff --stat`
  confirmed the file was back to the intended one-line-condition change.
- `pytest hub/tests/test_review_divergence.py hub/tests/test_run_divergence.py
  hub/tests/test_a_task_nothing_will_move_holds_nobody.py hub/tests/test_reviewer_ladder.py -q` →
  **77 passed** (the last file is 6.0's own regression guard territory, run here as a first check
  since group 2 as a whole has not landed yet; ran clean before and after this task's change).
- `ruff check src/ hub/ tests/`, `black --check --target-version py311 src/ hub/hub/ hub/tests/
  tests/`, `mypy src/` (CI's exact commands/paths) — all clean.

`git status --short` before commit showed exactly the three files touched:
`hub/hub/run_divergence.py`, `hub/tests/test_review_divergence.py`,
`openspec/changes/an-unstaffed-review-names-its-holders/tasks.md`. Staged and committed explicitly
(no `git add -A`).

`next_action` advances to `unstaffed-group2-task2.1to2.4` — the rung-3 rebuild (design D2/D3),
which task 2.14 unblocks but does not itself touch.

## Iteration 8 — unstaffed-group2-task2.1to2.4 (2026-09-23, this firing)

Branch/log reconciled before starting: head `168ec8c` matched STATE's implied position, but
`git status` showed uncommitted changes already present — `hub/hub/run_divergence.py`,
`hub/hub/scheduler.py`, four test files, and `tasks.md` — the same pattern iteration 3 hit: a
complete, working implementation of tasks 2.1-2.4 left over from a prior firing that ended without
committing. `tasks.md` already carried "Built 2026-09-23" notes for 2.1-2.4 in full, matching the
R8 block's exact prescriptions (the `Mapping[str, str]` signature, the three-layer mapping in
`_answer_failed_review`, the `_rung_3_*` helper family implementing the five-clause precedence /
exact wording / join, and the 500-character bound with its per-clause retry and tail). Read the
diff in full rather than redoing the work.

Verified rather than trusted:
- `grep -rn "exclude=" hub/ --include=*.py` → 27 lines, not tasks.md's recorded 25 — but the two new
  literal-assertion tests (`test_rung_3_reason_is_this_exact_string_for_a_completed_task` and its
  `under_review` sibling) each add one `exclude=` call, so 25 (R9's count, before this task's own
  tests existed) + 2 (this task's own new call sites) = 27. Not drift; recorded here so a future
  round doesn't mistake the new count for a moved tree.
- `grep -n "_agents_that_are_free(" hub/hub/scheduler.py` → only the definition and one call inside
  `_agents_a_loop_may_staff` (line 1260); `_loop_flow_busy_reason` and `decide_firing` reach it only
  indirectly through `_agents_a_loop_may_staff` — pre-existing structure from task 1.2, unrelated to
  this task's diff, not a regression.
- `pytest hub/tests/test_reviewer_ladder.py hub/tests/test_a_held_agent_is_busy.py
  hub/tests/test_a_flow_names_what_it_cannot_staff.py
  hub/tests/test_a_task_nothing_will_move_holds_nobody.py hub/tests/test_review_divergence.py
  hub/tests/test_run_divergence.py -q` → **130 passed**, matching tasks.md's own recorded count.
- `ruff check src/ hub/ tests/`, `black --check --target-version py311 src/ hub/hub/ hub/tests/
  tests/`, `mypy src/` (CI's exact commands/paths) — all clean.
- Mutation check, live: changed `_rung_3_join`'s `". "` before the remedy to `"; "` (the R6-8 defect
  class 2.3's own note calls out). Both exact-string tests failed on the `==` literal (diff showed
  `; Decide` vs `. Decide`). Reverted; `git diff --stat` on `scheduler.py` back to `157
  insertions(+), 35 deletions(-)`, matching the pre-mutation diff exactly. Re-ran the two tests
  standalone afterward → 2 passed.
- `pytest hub/tests/test_a_task_nothing_will_move_holds_nobody.py -q` (6.0's own regression guard,
  run again after the revert per its own instruction) → 31 passed.

`git status --short` before commit showed exactly the seven files tasks.md's own notes named.
Staged and committed explicitly (no `git add -A`): `755c5b6`. Pushed.

`next_action` advances to `unstaffed-group2-task2.6to2.11` — group 2's rung-3 test coverage, which
depends on 2.1-2.4 having landed.

## Iteration 9 — unstaffed-group2-task2.6to2.11, split again (2026-09-23, this firing)

Branch/log reconciled before starting: head `4672b78` matched STATE's implied position, working
tree clean. No drift.

`unstaffed-group2-task2.6to2.11` named six sub-tasks (2.6-2.11) as one item. Read all six R8 blocks
in full before starting (`design.md`'s own advice: don't trust a round's own summary of a list that
has been wrong four times already in this same file). Two of the six — 2.10 and 2.11 — turned out
to be self-contained regression cover for already-shipped code (the sibling directory's `@validates`
and `_wedged_review_reason` title fit), with no open fixture question. The other four — 2.6, 2.7,
2.9, 2.9b — all depend on one fixture decision R8 made but no task has built yet (every non-author
holding, and the author's own extra task, in a **second** live loop the firing under test never
fires), and 2.6 alone is a real-firing, three-surface-assertion test sized like a whole item on its
own in earlier iterations of this same queue. Built the two tractable ones, verified each with a
live mutation and a clean revert, and split the remaining four into their own queue item
(`unstaffed-group2-task2.6to2.9b`) rather than rush four fixture-heavy tests in the same firing —
the same call iteration 7 made on the original, larger item, for the same reason.

**2.10.** Added two tests to `test_reviewer_ladder.py`:
`test_a_stall_reason_over_budget_still_matches_the_fitted_row` (R8's rewrite: a direct unit test of
`_stall_run_to_increment` against a real `AIJob`/`JobRun` pair, a 600-character `error_summary` the
model's own `@validates` fits to 500, then the same 600-character raw string passed as the
argument) and `test_a_stall_reason_that_actually_changed_does_not_match` (the negative case, not
named by the task but needed so the positive case isn't the only fact pinned). *Mutation:* replaced
`fit_error_summary(stall_reason)` with the bare `stall_reason` at `scheduler.py:978` — the positive
test failed on `assert None is not None` (the stored row is fitted to 500 chars; the raw 600-char
argument no longer matches it). Reverted; `git diff --stat` on `scheduler.py` showed nothing.

**2.11.** Added `test_f367_a_wedged_review_with_a_long_name_and_title_still_fits_the_column` to
`test_a_review_nobody_is_doing.py` (F154's own file, not this change's — it is route-level
regression cover for the sibling, per the task's own R8 note). Widened `_wedged`'s fixture with an
optional `title=` kwarg (default unchanged, so its other 20 call sites in that file are untouched),
staged a 32-character reviewer name and a 256-character task title, fired through a real
`POST …/jobs/{id}/run`, and read the stall row back through `GET …/jobs/{id}/history`. Two mutations,
both from the task's own text: (a) stripped `_wedged_review_reason`'s title-shortening loop down to
`text = _sentence(title)` — the test failed on the `endswith` assertion, the remedy itself visibly
truncated (`"...review i�"`), exactly the failure mode the task predicts; (b) with (a) still
applied, also disabled `JobRun`'s `@validates` — the route raised
`fastapi.exceptions.ResponseValidationError` (`string_too_long` on `error_summary`), the 500 the task
predicts. Both reverted; `git diff --stat` on `scheduler.py` and `hub/hub/db/models.py` showed
nothing after.

**2.8 — checked, not built, and the checking is the finding.** Re-read against 2.3's own built
test, `test_rung_3_reason_is_this_exact_string_for_an_under_review_task`: it already builds 2.8's
exact fixture (single-agent, author-excluded roster) and already asserts the whole `under_review`
reason with `==`, containing `". Decide it yourself:"` capitalised. Rather than assume the overlap
and skip it, ran 2.8's own R8 mutation (drop `capitalize_first` from `_rung_3_reason`) live against
2.3's tests: both still passed. `own_review_remedy` returns an already-capitalized sentence for both
statuses on this tree (2.3's own build-time-correction note says so, from when it was built two
iterations ago) — so `capitalize_first` is a genuine no-op here, and no test reaching it through the
real function can observe that mutation. 2.8 as R8 specified it is unfalsifiable on this tree today
— the same vacuous-test shape (F190) this file has now flagged three times (2.6's R6 note, 2.10's R8
rewrite, and now this). Reverted the mutation; `git diff --stat` showed nothing. Recorded as a
struck-through task in `tasks.md` with the reasoning attached, not silently dropped, so a future
round doesn't re-open it without first re-checking whether `own_review_remedy`'s capitalization has
changed.

**Verification:**
- `pytest hub/tests/test_reviewer_ladder.py hub/tests/test_a_review_nobody_is_doing.py
  hub/tests/test_a_task_nothing_will_move_holds_nobody.py -q` → **68 passed** (includes 6.0's own
  regression guard, unchanged).
- `ruff check src/ hub/ tests/`, `black --check --target-version py311 src/ hub/hub/ hub/tests/
  tests/` (one file needed reformatting after the new tests were added — applied, re-checked clean),
  `mypy src/` (CI's exact commands/paths) — all clean.

`git status --short` before commit showed exactly the three files touched:
`hub/tests/test_a_review_nobody_is_doing.py`, `hub/tests/test_reviewer_ladder.py`,
`openspec/changes/an-unstaffed-review-names-its-holders/tasks.md`. Staged and committed explicitly
(no `git add -A`).

`next_action` advances to `unstaffed-group2-task2.6to2.9b` — the four remaining, fixture-heavy tests,
sharing the one open fixture decision noted above.

## Iteration 10 — unstaffed-group2-task2.6to2.9b, task 2.6 built, split again (2026-09-23, this firing)

Branch/log reconciled before starting: head `ce98ecb` matched STATE's implied position, working
tree clean. No drift.

Read both R8 blocks in full (2.6's own, and 2.3/2.4's for the exact clause wording and join order)
before writing anything, per the split note's own advice. Added
`test_a_loopengine_shaped_firing_names_every_reachable_holder` to
`test_a_task_nothing_will_move_holds_nobody.py`: four agents (the author, plus B/C/D holding an
`under_review` task, five `pending` tasks and an `in_progress` task respectively), every non-author
holding — and the author's own extra live task (REV's addition) — carrying a **second** live
loop's id (`_loop(db, suffix="2.6-elsewhere")`) so each is reachable without the firing under test
ever walking it, exactly R8's fixture. A sixth, unreachable task for C covers the delta's own "a
task nothing will move is not named as a reason" scenario in the same test rather than a separate
one, since it is the same fixture with one more row.

Fired through a real `POST …/jobs/{id}/run` (`live_scheduler` + `_no_spawn()`), and read the stall
on all three surfaces the route/event/board share: the 409 response's `detail`, the
`review_unstaffed` `EventLog` row's `data["reason"]`, and `LoopSummary.stall_reason`
(`_batch_loop_summaries`) — asserted all three equal, then asserted the whole string `==` a literal
derived from 2.3/2.4's shipped `_rung_3_*` functions (read directly from `scheduler.py` rather than
guessed) before running anything; the test passed on the first correctly-named run, confirming the
derivation.

**Mutations, all applied live and reverted (`git diff --stat` clean after each):**
- (a) drop the holdings clause — removed the `booked` branch from `_rung_3_clause_kind`, falling
  through to `running`. Failed on the `==` literal (every booked agent read "is running a turn").
- (b) put holds (booked) before excluded. Failed — and specifically because of the author's own
  reachable holding (REV's addition): the author's clause read "is booked for task-2.6-author
  (in_progress)" instead of "is the one that completed this task". Without that holding this
  mutation could not have bitten, exactly as R8 warned.
- (c) "use excluded_because for every agent," re-derived against 2.1's Mapping-based `exclude`
  (the field itself no longer exists) as: hardcode the excluded clause's text instead of reading
  `exclude[record.name]`. Failed on the literal.
- Fixture check (R6): temporarily set every non-C holding's `loop_id` to `None` in the test itself
  (a mechanical string replace missed the indented loop over C's five tasks, so those stayed
  reachable — noted rather than silently treated as "all"). The firing then answered 200, not 409:
  with the author's own bookmark and B's/D's holdings now unreachable, B or D was free and got
  staffed. Proves the fixture's reachability is what produces the stall, not an assertion artifact.
  Reverted.

**Verification:**
- `pytest hub/tests/test_a_task_nothing_will_move_holds_nobody.py -q` → **32 passed** (31 + this
  task's one new test).
- `pytest hub/tests/test_reviewer_ladder.py hub/tests/test_a_held_agent_is_busy.py
  hub/tests/test_a_flow_names_what_it_cannot_staff.py
  hub/tests/test_a_task_nothing_will_move_holds_nobody.py hub/tests/test_review_divergence.py
  hub/tests/test_run_divergence.py -q` → **133 passed**, matching 2.1-2.4's own recorded 130 plus
  this task's 1 (2.6 alone, not the file's total).
- `ruff check src/ hub/ tests/` clean. `black --check --target-version py311 src/ hub/hub/
  hub/tests/ tests/` needed one reformat (the new test's line wrapping) — applied, re-checked
  clean. `mypy src/` clean.

`git status --short` before commit showed exactly the two files touched:
`hub/tests/test_a_task_nothing_will_move_holds_nobody.py` and this change's own `tasks.md`. Staged
and committed explicitly (no `git add -A`): `dbad393`. Pushed.

**2.7, 2.9 and 2.9b split off** to `unstaffed-group2-task2.7to2.9b` — 2.6 alone, the real-firing
three-surface test the R8 block itself sized as one whole item's worth of work, took the room this
firing had; the other three each need their own fixture design (2.7's divergence-path restaff on
both branches, 2.9's twelve-agent 500-char fit, 2.9b's 64-character-id fallback) rather than being
rushed after 2.6 in the same pass — the same call iterations 7 and 9 made on this same group, for
the same reason.

`next_action` advances to `unstaffed-group2-task2.7to2.9b`.

## Iteration 11 — unstaffed-group2-task2.7to2.9b, task 2.7 built, split again (2026-09-23, this firing)

Branch/log reconciled before starting: head `1f9f550` matched STATE's implied position, working
tree clean. No drift.

Read 2.6's own build (`test_a_loopengine_shaped_firing_names_every_reachable_holder`) and
`_answer_failed_review`'s own docstring (`run_divergence.py:375-462`) in full before writing
anything, per the split note's own advice. 2.7 needed a fixture the file did not yet have: an
**agent-completed** silent review (`completion_attribution.agent` not `None`, so
`_answer_failed_review`'s author layer never runs), alongside the file's existing
**operator-completed** one. Added `_silent_review_of_agent_completed_work` — the same shape as
`_completed_by` (`test_a_loop_does_not_staff_its_own_review.py`) walked through the transition
machine as `WORKER`, then reviewed by `SILENT` with no verdict.

Both new tests roster a third agent, `HELD` (`zz-held`), holding a task reachable only through a
**second** live loop — `_loop`/`_holding`, imported directly from
`test_a_task_nothing_will_move_holds_nobody` rather than reimplemented, since they are plain
async functions on the same `PROJECT = "proj-test"` and no fixture/circular-import conflict exists
(`grep` confirmed no import back the other way). This is the point R8 makes that the file's
existing two-agent fixtures cannot: with only `WORKER`+`SILENT` on the roster, both being excluded
via the mapping is indistinguishable from "nobody else exists" — a third agent unavailable through
a **booked** holding proves the divergence path reaches rung 3's `booked` clause too, not only the
exclude-map arm.

Read the reason off the `run_diverged` event's own `payload["reason"]` (via a small
`_diverged_reason` helper), never `review_unstaffed` — R8's instruction, matching
`test_the_evidence_names_the_author.py:663-694`'s own existing pattern.

**Both mutations, applied live and reverted (`git diff --stat` on `run_divergence.py` clean after
each):**
- (a) map silent reviewers to the author clause (`"reviewed this task and recorded no verdict"` →
  `"has worked on this task"` on both the reviewer-set update and `exclude[run.agent]`) — **both**
  new tests failed, `SILENT`'s clause read "has worked on this task" instead.
- (b) R1's order, laying the author layer *after* the reviewer layer instead of before — only the
  **operator-completed** test failed (`SILENT`'s clause reverted to "has worked on this task",
  since on that task `SILENT`'s own `-> under_review` transition already makes it an "author"
  candidate and the reordered author layer now overwrites the reviewer layer's entry last). The
  **agent-completed** test was unaffected, exactly as predicted: `attribution.agent is not None`
  on that task skips the author layer (`if attribution.agent is None:`) regardless of where the
  block sits, so (b) cannot bite that branch at all.

**Verification:**
- `pytest hub/tests/test_the_evidence_names_the_author.py -q` → **14 passed** (12 existing + 2
  new).
- `pytest hub/tests/test_the_evidence_names_the_author.py hub/tests/test_a_task_nothing_will_move_holds_nobody.py
  hub/tests/test_review_divergence.py hub/tests/test_run_divergence.py hub/tests/test_reviewer_ladder.py
  hub/tests/test_a_held_agent_is_busy.py hub/tests/test_a_flow_names_what_it_cannot_staff.py -q` →
  **147 passed** (a benign `RuntimeError: Event loop is closed` teardown warning from `aiosqlite`'s
  worker thread appeared in the summary, unrelated to any test outcome — not a failure).
- `pytest hub/tests/test_a_task_nothing_will_move_holds_nobody.py -q` (6.0's own regression guard,
  run again standalone) → **32 passed**, unchanged.
- `ruff check src/ hub/ tests/` clean. `mypy src/` clean. `black --check --target-version py311
  src/ hub/hub/ hub/tests/ tests/` needed one reformat (the new tests' line wrapping) — applied,
  re-checked clean.

`git status --short` before commit showed exactly the two files touched:
`hub/tests/test_the_evidence_names_the_author.py` and this change's own `tasks.md`. Staged and
committed explicitly (no `git add -A`).

**2.9 and 2.9b split off** to `unstaffed-group2-task2.9to2.9b` — 2.7 alone, with its two-branch
fixture design and both mutations checked live, took the room this firing had; 2.9's twelve-agent
500-character fit (with its four R8 sub-mutations) and 2.9b's 64-character-id fallback fixture
each need their own build, the same call iterations 7, 9 and 10 made on this same group, for the
same reason.

`next_action` advances to `unstaffed-group2-task2.9to2.9b`.

## Iteration 12 — unstaffed-group2-task2.9to2.9b, task 2.9 built, split again (2026-09-23, this firing)

Branch/log reconciled before starting: head `8332273` matched STATE's implied position, working
tree clean. No drift.

Read 2.6's own build in full (`test_a_loopengine_shaped_firing_names_every_reachable_holder`) and
the R8 blocks for 2.3, 2.4 and 2.9 itself before writing anything, per the split note's own advice.
2.9 needed a bigger fixture than either 2.6 or 2.7: twelve non-author agents, each booked for three
tasks reachable only through a second live loop (2.6's R8 fixture, reused via the file's own
`_loop`/`_holding`), plus the author on an ordinary completed-task flow (`_flow_queue`/`_flow_task`/
`_completed_by`/`record_review_evidence`, 2.6's own fixture), with the last agent in name order also
usage-held (`_hold`, already imported at the top of the file) so the tail has to carry the held
variant even though that agent itself falls into the tail rather than being individually named.

Derived the exact expected literal *before* writing the test's assertions, the same discipline 2.6
used: built `AgentAvailability`/`Holding` objects by hand in a `%TEMP%` scratchpad mirroring the DB
fixture, called the real `_rung_3_reason` against them, and only then pasted the resulting
470-character string into the test as the `==` literal — 470, not a guess later adjusted to match a
failing run. Two agents are named in full (three tasks each); the rest — ten agents, including the
held one — fold into `"; and 10 more agents are excluded, busy, waiting for a usage limit or
unbound"`, then the remedy and the REJECT sentence.

Ordered the test's own assertions POST-run (409) → `GET history` (200) → literal `==` → length
bound, deliberately, after the first mutation run showed a naive top-to-bottom order lets an
over-length `detail` fail the test **before** the history route is ever called — which would leave
the "history route must fail" mutation unproven, exactly the gap 2.11's own note (`tasks.md`) warns
about. Re-ordering so the route call comes first fixed that: a mutation that breaks the history
route now trips the `history.status_code == 200` assertion itself, not an earlier one.

**All four R8 mutations applied live and reverted (`git diff --stat` on `scheduler.py` and
`hub/hub/db/models.py` clean after each):**
- (i) `_RUNG_3_REASON_BOUND` raised to `500_000` **and** `fit_error_summary` turned into `return
  text` (both the code bound and the model fit removed) — `GET …/jobs/{id}/history` raised
  `fastapi.exceptions.ResponseValidationError` (`string_too_long` on `error_summary`, carrying the
  full 1,265-character unfitted reason in its own error payload), exactly the 500 the task predicts,
  proving the test reaches `JobRunResponse`.
- (ii) `_RUNG_3_REASON_BOUND` alone raised to `500_000`, the model fit left shipped — the history
  route still answered 200 (the model's own blind slice-to-499-plus-`…` still fits the column), but
  the stored `error_summary` was cut off mid-clause (`"...zz-2-9-05 is booked for t-2.9-05-1
  (pendin…"`, the actual failure text observed), never reaching the remedy at all. Failed on the
  literal `==` comparison, the assertion this mutation is meant to trip — proving 2.4's own
  clause-aware shrinking is what the naive model-level fit cannot substitute for.
- (iii) a fixture check, not a mutation to shipped code: temporarily set every holding's `loop_id`
  to `None` in the test itself instead of the second loop's id. The firing then answered `200`, not
  `409` — with all twelve holdings unreachable every agent was free and the firing staffed one of
  them, so rung 3 was never reached at all. Proves the fixture's reachability, not an assertion
  artefact, is what produces the stall. Reverted.
- (iv) `_rung_3_tail` rewritten to always return R3's plain (non-held) wording, dropping the `"held"
  in remaining_kinds` branch entirely — failed on the literal `==`, and not only on the missing
  held phrase: the shorter plain tail also left one more character of budget, which let
  `zz-2-9-03`'s own booked clause fit where it hadn't before, changing the string's shape as well as
  losing the held wording — exactly the ripple R8-5 itself warns a length-based fit produces once
  one of its inputs changes.

**Verification:**
- `pytest hub/tests/test_a_task_nothing_will_move_holds_nobody.py -q` → **33 passed** (32 + this
  task's 1, 6.0's own regression guard unchanged).
- `pytest hub/tests/test_reviewer_ladder.py hub/tests/test_a_held_agent_is_busy.py
  hub/tests/test_a_flow_names_what_it_cannot_staff.py
  hub/tests/test_a_task_nothing_will_move_holds_nobody.py hub/tests/test_review_divergence.py
  hub/tests/test_run_divergence.py hub/tests/test_the_evidence_names_the_author.py -q` →
  **148 passed** (147 + this task's 1).
- `ruff check src/ hub/ tests/`, `black --check --target-version py311 src/ hub/hub/ hub/tests/
  tests/`, `mypy src/` (CI's exact commands/paths) — all clean, no reformat needed this time.

`git status --short` before commit showed exactly the two files touched:
`hub/tests/test_a_task_nothing_will_move_holds_nobody.py` and this change's own `tasks.md`. Staged
and committed explicitly (no `git add -A`): `cb21830`. Pushed.

**2.9b split off** to its own queue item, `unstaffed-group2-task2.9b` — 2.9 alone, with its own
literal derivation and four live-checked mutations (one of them a fixture check, not a code
mutation), took the room this firing had; 2.9b's 64-character-id fallback fixture needs its own
build, the same call iterations 7, 9, 10 and 11 made on this same group.

`next_action` advances to `unstaffed-group2-task2.9b`.

## Iteration 13 — unstaffed-group2-task2.9b, task 2.9b built, group 2's fixture-heavy split closed (2026-09-23, this firing)

Branch/log reconciled before starting: head `8a796c0` matched STATE's implied position, working
tree clean. No drift.

Read task 2.9b's own text plus `next_action`'s clarification (the fallback fixture: a single
32-character agent name holding three 64-character-id tasks reachable through a second live loop,
as an `under_review` row with the smaller clause budget). Chose `_holding(db, task_id=…,
loop_id=…)` over `POST …/tasks` for the three tasks — R8's own note says the route is not the
point, the 64-character id is, and `_holding` is what 2.6 and 2.9 already use for the same
reachability fixture.

Derived the exact expected literal before writing the assertions, the same discipline 2.6/2.9 used:
built `AgentAvailability`/`Holding` objects by hand in a `%TEMP%` scratchpad, called the real
`_rung_3_reason` against them. The scratchpad's own object order (author first, holder second) does
not match `_roster_availability`'s real DB-derived name order — `aa-2-9b-…` sorts before
`f161-author` — so the first real test run surfaced the mismatch immediately (clause order
reversed in the actual reason vs. the hand-derived literal); corrected the literal's clause order
to match, rather than reordering the fixture to match a wrong guess. Final: 453 characters, the
holder named with 2 of its 3 tasks ("… and 1 more"), then the author's exclusion clause, then the
`under_review` remedy and the REJECT sentence.

Built `resolve_reviewer` against a plain `status="under_review"` `Task`, following
`test_reviewer_ladder.py`'s own exact-string pattern (`test_rung_3_reason_is_this_exact_string_for_
an_under_review_task`) rather than a real firing through `POST …/jobs/{id}/run` — 2.9b's own task
text names no route or event surface to assert against, unlike 2.6 and 2.9, so the lighter surface
is what the task actually specifies.

**Mutation applied live and reverted** (`git diff --stat hub/scheduler.py` clean after): changed
`limits = (3, 2, 1) if kind == "booked" else (3,)` to `limits = (3,)` at `scheduler.py:1381`. The
holder's three-task booked clause (311 characters) no longer fits the 226-character `under_review`
budget at any retry width, so the agent drops out of the clause list entirely and falls to the
tail — the reason became `"… no reviewer is free. ; and 2 more agents are excluded, busy or
unbound. Decide it yourself: …"`, missing the agent by name exactly as the task predicts. Failed on
the literal `==`, as expected. Reverted; `git diff --stat` showed no change afterward.

**Verification:**
- `pytest hub/tests/test_a_task_nothing_will_move_holds_nobody.py -q` → **34 passed** (33 + this
  task's 1).
- `pytest hub/tests/test_reviewer_ladder.py hub/tests/test_a_held_agent_is_busy.py
  hub/tests/test_a_flow_names_what_it_cannot_staff.py
  hub/tests/test_a_task_nothing_will_move_holds_nobody.py hub/tests/test_review_divergence.py
  hub/tests/test_run_divergence.py hub/tests/test_the_evidence_names_the_author.py -q` →
  **149 passed** (148 + this task's 1).
- `ruff check src/ hub/ tests/` and `mypy src/` (CI's exact paths) clean. `black --check
  --target-version py311 src/ hub/hub/ hub/tests/ tests/` needed one reformat (this task's new
  lines, whitespace only) — applied, re-checked clean; `git diff --stat` on the test file showed
  additions only both before and after the reformat.

`git status --short` before commit showed exactly the two files touched:
`hub/tests/test_a_task_nothing_will_move_holds_nobody.py` and this change's own `tasks.md`. Staged
and committed explicitly (no `git add -A`).

**Group 2's fixture-heavy split chain (2.6, 2.7, 2.9, 2.9b — begun at iteration 10) is now
complete.** `next_action` advances to `unstaffed-group2-task2.15to2.18-and-6.0`, the group's last
open queue item: tasks 2.15-2.18 plus a final run of the 6.0 regression guard.

## Iteration 14 — unstaffed-group2-task2.15to2.18-and-6.0, group 2 closed (2026-09-23, this firing)

Branch/log reconciled before starting: head `87734a5` matched STATE's implied position, working
tree clean. No drift.

Read all four tasks' own R6/R8 text plus `_rung_3_clause_kind`/`_rung_3_join`/`_RUNG_3_REJECT`
(`scheduler.py`) before writing anything. Unlike the group's earlier fixture-heavy split chain
(2.6/2.7/2.9/2.9b), these four are regression/precedence cover over already-shipped code with no
open fixture design question — built all four in this one firing, per `next_action`'s own framing.

**2.15.** Two tests added to `hub/tests/test_a_held_agent_is_busy.py`, section 3.4d:
`test_a_held_agent_holding_nothing_is_named_by_the_held_clause_not_running` (base case) and
`test_a_held_and_booked_agent_is_named_only_by_the_hold` (R8's second case). The second needed a
task reachable through a second live loop — added `_booked_elsewhere`, a small local fixture
mirroring `test_reviewer_ladder.py`'s `_live_loop`/`_held_elsewhere`, kept **local to this file**
rather than importing `_loop`/`_holding` from `test_a_task_nothing_will_move_holds_nobody`: that
file already does `from .test_a_held_agent_is_busy import _hold` at its own top level, so an
import the other way would be a genuine circular import (not just an anti-pattern), not merely
"reimplemented for no reason."

**2.16.** Re-read the three named tests before touching any: `:370-381` was substring-only —
rebased onto `choice.reason == _HELD_TODAY`, a literal derived from `_rung_3_join`'s own precedence
(AUTHOR excluded, sorts first; REVIEWER held, no REJECT since no clause here is `booked`) and
confirmed correct on the first test run, not adjusted afterward to match a failure. The other two
named tests — `_TODAY`'s exact-equality test and the `error_summary` fit test — were **checked, not
rewritten**: `_TODAY` itself carries a comment recording it was already rebased onto D2's sentence
during task 2.3's build, and both tests still passed when re-run standalone; recording that
verification in `tasks.md` rather than silently trusting the comment.

**2.17.** Two tests, both in `test_a_held_agent_is_busy.py`:
`test_the_rung_3_reason_never_claims_archiving_a_loop_frees_an_agent` (booked-elsewhere fixture,
asserts `"pause"`/`"archiv"`/`"ending"` absent case-insensitively) and
`test_the_rung_3_reason_does_not_reject_when_nobody_booked_is_named` (held-only fixture, asserts
`"Rejecting"` absent).

**2.18.** `test_rung_3_reason_for_an_empty_roster_is_this_exact_string` added to
`test_reviewer_ladder.py` — rosters one agent then archives it (`Agent.lifecycle = "archived"`,
`test_an_archived_agent_is_not_selected`'s own pattern), so `_roster_availability` returns nothing
and `_rung_3_reason`'s empty-roster branch is what runs. Exact `==` against the literal the task
names.

**All five mutations applied live to `hub/hub/scheduler.py` and reverted, each behaving as
predicted (`git diff --stat` clean after every one):**
- fold `held` into `running` (delete `_rung_3_clause_kind`'s held branch) — 3 tests failed (both
  2.15 tests plus 2.16's rebased test), the held agent's clause reading "is running a turn" and,
  in the booked case, the task id appearing.
- R6's own order (booked checked before held) — only the booked-agent 2.15 test failed, isolating
  precedence specifically rather than mere presence of the held branch.
- widen `_RUNG_3_REJECT` with an "or pause or archive the loop ... to free them" clause — 2.17's
  archive test failed on `"pause"`.
- make `_rung_3_join` append `_RUNG_3_REJECT` unconditionally — 2.17's no-reject test failed on
  `"Rejecting"` appearing.
- lowercase the `T` in `"The project has no agent..."` — 2.18's test failed on the `==` literal.

**Verification:**
- `pytest hub/tests/test_a_held_agent_is_busy.py hub/tests/test_reviewer_ladder.py -q` → 49 passed,
  first run, before any mutation — every derived literal matched on the first try.
- `pytest hub/tests/test_reviewer_ladder.py hub/tests/test_a_held_agent_is_busy.py
  hub/tests/test_a_flow_names_what_it_cannot_staff.py
  hub/tests/test_a_task_nothing_will_move_holds_nobody.py hub/tests/test_review_divergence.py
  hub/tests/test_run_divergence.py hub/tests/test_the_evidence_names_the_author.py -q` → **154
  passed** (149 + 5 new).
- `pytest hub/tests/test_a_task_nothing_will_move_holds_nobody.py -q` (6.0's own regression guard,
  standalone, per `next_action`'s instruction) → **34 passed**, unchanged — group 2 as a whole did
  not revert group 1's projection.
- `ruff check src/ hub/ tests/` and `mypy src/` (CI's exact paths) — clean. `black --check
  --target-version py311 src/ hub/hub/ hub/tests/ tests/` needed one reformat
  (`test_reviewer_ladder.py`'s new archived-agent query, line-wrapping only) — applied, re-checked
  clean, and re-ran the affected three files afterward (83 passed) to confirm the reformat changed
  nothing behavioral.

`git status --short` before commit showed exactly the three files touched:
`hub/tests/test_a_held_agent_is_busy.py`, `hub/tests/test_reviewer_ladder.py`, and this change's
own `tasks.md`. Staged and committed explicitly (no `git add -A`).

**This closes group 2 of `an-unstaffed-review-names-its-holders` in full** — every sub-task from
2.1 through 2.18 is now built and verified. `next_action` advances to `unstaffed-group5-impl`, the
UI bundle group (tasks 5.1-5.4), per tonight's ORDER; `unstaffed-group6-drive` (verify + drive)
remains the queue's last item after it.

## Iteration 15 — unstaffed-group5-impl, UI bundle built and driven, not shipped (2026-09-23, this firing)

Branch/log reconciled before starting: head `9389b92` matched STATE's implied position, working
tree clean. No drift.

Ran the 6.0 regression guard first, per its own standing instruction: `pytest
hub/tests/test_a_task_nothing_will_move_holds_nobody.py -q` → 34 passed, unchanged.

**5.1.** Added `title={loop.stall_reason}` to the stall `<p>` at
`LoopsIndexTab.tsx:237-244` (confirmed unchanged from R8's own citation before editing) and a new
test, `carries the full, unstripped stall reason in the title attribute, even once truncated on
screen`, to `loopsIndexTab.test.tsx` — a 268-character reason starting `"loop queue is "`, asserting
`toHaveAttribute('title', longReason)` exactly. Both R8 mutations applied live to the component and
reverted: removing the attribute failed the test (`title` read `null`); setting it to the stripped
text (`.replace(/^loop queue is /, '')`) also failed, on the unstripped-vs-stripped mismatch. Full
file: 12 passed (11 existing + 1 new). `git diff --stat` clean after each revert.

**R8's own premise needed re-checking before 5.2, exactly as the note itself says to do, and it had
gone stale.** `git log -1 --format=%H -- hub/ui/src` is `c18a87b` ("F202, F203, F209, F212, F201"),
newer than the committed bundle's stamped `src_commit` (`35a162c`, built 2026-09-22T18:07:55) — a
rebuild tonight does not carry only the title attribute, it also carries that commit's frontend
diff (`api/tasks.ts`, `App.tsx`, `TasksBoard.tsx`, five more files, 407 insertions). Recorded rather
than silently building past the note's instruction.

**5.2.** `cd hub/ui && npm run lint` clean. `npm run build` (`tsc && vite build`) succeeded, no
type errors. `py -3.11 scripts/refresh_ui_bundle.py` wrote a new stamp (`src_commit 9389b92`, this
branch's head). `git diff --stat` on `hub/hub/static/ui` showed the expected shape of a real
rebuild (old JS chunk deleted, new one added, `index.html` and the stamp changed) — not a no-op.

**5.3.** Drove the freshly built bundle live rather than trusting the unit test alone. Started a
throwaway Hub on port 8095, `DATABASE_URL` naming a fresh sqlite file under
`testbed/scratch/group5-drive/` (a genuinely fresh database — its `OperatorCredential` bootstrap key
had to be read directly from the sqlite file, since a from-source `uvicorn` launch with no `.env`
auto-generates one and only logs "auto-generated", not the value). Registered a throwaway project
(`proj-a54ac8c12fa4`) via `POST /projects/create`, one `claude-haiku-4-5-20251001` agent (per the
standing drive limit — every real agent turn binds Haiku), one job with a single task moved to
`blocked`, fired to produce a real 409 stall: `"loop queue is stalled: no claimable task among 1
open (1 blocked)"` — the same shape `scripts/drive/t_row11_stalls.py`'s shape 3 already exercises.

Reaching `LoopsIndexTab` needed an actual conversation: the empty "New conversation" screen
(`NewConversationSurface`) does not mount `PanelShell` at all, so the panel toggle and `Loops` tab
do not exist yet on that screen regardless of the "Explore" button's own state. Sent one real
message to `beta` to open a conversation (Haiku 4.5, ~11s, 37,624 tokens), then found the panel via
`aria-label="Show panel"` (`AgentOutputPanel.tsx:1112`, not "Explore" as first guessed) ->
`[data-testid="panel-launch-loops"]` -> the stall row. Playwright's `get_attribute('title')` on
`[data-testid^="loops-index-stall-"]` read back exactly `'loop queue is stalled: no claimable task
among 1 open (1 blocked)'` — the full, unstripped reason, against the real built JS bundle and a
real backend response, not a mock. Screenshots taken and read back with `Read`
(`step5_loops.png` showing the panel and row, `step6_hover_crop.png` cropped on the row) — a
synthetic Playwright `hover()` did not paint a visible native-tooltip bubble in the headless
screenshot, which is expected (Chromium does not compositor-render OS title tooltips headless), so
the "read the full reason from title" instruction was satisfied via the DOM attribute Playwright
reports for the hover target, not a visual tooltip capture.

**5.4 — the gate failed, so the bundle was reverted, not shipped.** 5.3 passed, but "nothing the
bundle calls is newer than the `:8000` process start" did not hold. Read `:8000`'s real process
command line via `Get-CimInstance Win32_Process` (PID 9940: `python.exe -m uvicorn hub.main:app
--host 127.0.0.1 --port 8000`) and its start time (`Get-Process`): 2026-09-19T11:57:33 — three days
before `c18a87b` (2026-09-22T19:39:47), the commit 5.1's own re-check had already surfaced as riding
along in tonight's rebuild. Read `hub/ui/src/api/tasks.ts` directly rather than assuming: that
commit's `useTasks` now expects the `{tasks, total, has_more}` envelope (F202) with no fallback for
a bare array, and `:8000`'s backend, unrestarted, still answers the pre-F202 bare array — committing
the rebuilt bundle would have put that frontend code in front of the operator on their next reload
while the backend under it still answered the old shape, exactly the "calls something new" case 5.4
exists to catch, not a hypothetical one this time.

`git checkout -- hub/hub/static/ui/index.html hub/hub/static/ui/ui-build-stamp.json
hub/hub/static/ui/assets/index-DLAuNdZ4.js`, deleted the new JS chunk
(`assets/index-BGR2Gvju.js`), confirmed `git status --short hub/hub/static/ui` was empty afterward.
Wrote the full reasoning into `tasks.md` under 5.4 rather than leaving a bare "reverted" note.

**Teardown:** killed the throwaway Hub (`taskkill /F /PID 22104`, confirmed gone via `tasklist`),
deleted `testbed/scratch/group5-drive/` entirely (playwright scripts, screenshots, sqlite db, logs
— nothing there was worth keeping past this iteration).

**Verification:** `npm run lint` clean (re-run after settling on the final diff).
`pytest hub/tests/test_a_task_nothing_will_move_holds_nobody.py -q` → 34 passed, unchanged (this
task never touched `hub/hub` or `hub/tests`, run again per habit, not because group 5 could plausibly
have moved it).

`git status --short` before commit showed exactly the three files touched: `hub/ui/src/__tests__/
loopsIndexTab.test.tsx`, `hub/ui/src/components/spec/LoopsIndexTab.tsx`, and this change's own
`tasks.md` — `hub/hub/static/ui` was clean after the revert, so it is not part of this commit.
Staged and committed explicitly (no `git add -A`): `e6d1c96`. Pushed.

**Group 5 is closed** (built and driven; the bundle ships on a later firing, once `:8000` is
restarted past `c18a87b` — the operator's call). `next_action` advances to
`unstaffed-group6-drive`, the queue's last item.

## Iteration 16 — unstaffed-group6-drive, verify + drive, queue finished (2026-09-23, this firing)

Branch/log reconciled before starting: head `8d4895f` matched STATE's implied position, working
tree had one uncommitted diff (`test-guide.md`, already re-derived against `_rung_3_reason` by a
prior, uncommitted firing) — verified against the shipped code (the A3 clause, the `_RUNG_3_REJECT`
string, every named test) rather than redone, then kept and committed with this iteration's work.

**6.0** (first run): `pytest hub/tests/test_a_task_nothing_will_move_holds_nobody.py -q` → 34
passed, matching every prior iteration's baseline.

**6.1**: `ruff check src/ hub/ tests/` all clean; `black --check --target-version py311 src/
hub/hub/ hub/tests/ tests/` → 594 unchanged; `mypy src/` → clean, 22 files; `cd hub/ui && npm run
lint` → clean.

**6.3 — driven live, real turns, real commits (port 8096, `profiles/drive0923/`,
`testbed/scratch/unstaffed-drive-032900/`, git-backed, never `:8000`/`:8010`).** A real flow (a
job whose loop carries `spec_document_id`, via `POST /project/documents` + `PUT .../content`);
`author` did two small pieces of real work as two real Haiku turns, each a real commit
(`d299021`, `92b85aa`, read from the task's own worktree branch — not asserted); a requirement
per task, evidence recorded against it naming that commit (`POST /project/spec/evidence`, operator-
recorded, which the code accepts on arrival). With `holder1`/`holder2`/`unreach` all booked via a
second live loop's reachable holdings, firing answered 409 with the exact rung-3 sentence — every
name, every held task, the "Land it"/"Rejecting booked tasks" remedy — and `GET history`'s row and
the `review_unstaffed` event's reason both matched the 409 detail byte-for-byte, all three surfaces
agreeing (A1/A2). **R8's two replacement claims, driven, not asserted:** (a) a holder whose only
task was unreachable was staffed directly as reviewer on the very next firing, no stall at all —
the observable proof an unreachable holding is free, not booked; (b) rejecting `holder1`'s one
reachable holding (its only booking) through the operator's own `PATCH` turned the next firing's
409 into a 200 that staffed `holder1` as reviewer — the remedy the sentence prints, watched
working. Both review turns ran for real and both approved. Full transcript: `scripts/drive/
FINDINGS.md`, D-6. Teardown: all three jobs `PATCH`ed `enabled: false`; `GET /jobs` confirmed zero
enabled. The setup script is kept, uncommitted-relevant: `scripts/drive/d6_0923_unstaffed_setup.py`.

**6.2** — `py -3.11 -m pytest hub/tests/ -q`, full, run **concurrently with 6.3's live drive**
(both in the same firing, to keep the window moving): **12 failed, 4644 passed, 86 skipped**
(2395s). All twelve failures were `test_pty_runner.py` (11) and
`test_lifespan_shutdown.py::test_hub_shutdown_kills_a_real_tracked_process` (1) — every one a
real-process-spawn/PID test. Neither DEAD-ENDS's F292 (`sqlite3 database is locked`) nor F314
(ordering) signature matches these names, so classified separately: re-run in isolation
immediately after the drive's teardown, nothing else on the machine spawning processes, all
twelve green (`32 passed`). Confirmed environmental (contention with the drive's real Haiku
subprocesses), not a regression — recorded as measured, not silently corrected. **Lesson for a
future window: do not run the full suite concurrently with a live drive that spawns real agent
processes on the same machine** — this cost a false-failure investigation this iteration could
have skipped by sequencing the two instead.

6.0's end-of-window re-run: 34 passed, unchanged.

**6.5** — `test-guide.md` re-derivation (found already done, verified: the five clauses, the
reachability filter, the removed archive remedy, the hold clause, A6's title-attribute note, all
checked against the shipped `_rung_3_reason` and the named tests, which exist).

**6.4 — archive.** `agent-flows`'s `## ADDED Requirements` delta appended to `openspec/specs/
agent-flows/spec.md`; `openspec validate agent-flows --strict` → "valid". Directory moved
(`git mv`) to `archive/2026-09-23-an-unstaffed-review-names-its-holders`. F352's status line
rewritten to `both halves fixed`, naming this change and D-6. F353/F334/F365/F367 already read
`fixed <sha>` from the sibling directory — confirmed, not re-written.

**Both changes in tonight's ORDER are now closed**
(`a-hub-that-was-not-told-which-database-refuses-to-open-one` and
`an-unstaffed-review-names-its-holders`). Staged explicitly (no `git add -A`) and committed.

**Decisions for the operator, unchanged from iteration 15 and still open:** the built UI bundle
(group 5's title attribute) ships on a later firing once `:8000` is restarted past `c18a87b` —
the operator's call, not this loop's.

**What to distrust:** none of this iteration's own work — every claim above was driven or
measured live, not asserted. The 12 pty/lifespan failures were investigated and reproduced as
environmental, not swept under the rug.

`next_action` set to null: the queue is finished, and the driver should unregister itself rather
than spend a firing rediscovering that there is nothing left to do.

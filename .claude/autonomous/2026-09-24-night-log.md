# 2026-09-24 night window

Newest entry at the BOTTOM. Playbook: `.claude/loops/night-window.md`.

Armed 2026-09-24T22:55:12+01:00 on `autonomous/2026-09-24-daily` (parent master@8a5d0ed), model opus.

## Iteration 1 — compose (2026-09-24 night)

CI verdict for inherited sha 8a5d0ed: `success` (run 17:12Z); armed commit ec5f690 in_progress at read time (age minutes, not hung). Branch `autonomous/2026-09-24-daily` matches STATE.

Backlog page refreshed (124 open findings, up from 104). APPROVALS `## 2026-09-24` is today's section and carries an `ORDER:`, taken verbatim as the queue: F133, tool-server, pressing-run, checkpoint-handover, event-announce, run-id-event, flows-own-moves, then a drive item. Tree-green check on the local suite not run this iteration (compose only); CI success on the inherited sha stands in. First build item's firing runs `tests/` before its commit.

## Iteration 2 — f133-impl (2026-09-24 night)

CI verdict for previous push sha 13eca69: `in_progress` at read time (inherited 8a5d0ed: `success`). Branch matched STATE.

Built F133: `select_turn(entries, hop_budget, cap)` and `entry_kind` (was `_entry_kind`) now live in `hub/hub/inbound_queue.py`; `_attempt_turn` and `GET /queue/{agent}/status` both call it, so the status route's token-budget test reads the turn's entries, not every queued entry. Two tests added in `hub/tests/test_accounting_budget.py` (autonomous-then-operator keeps the reason; operator-first control). Failing first (status returned null), green after. Mutation: dropping the conversation filter in `select_turn` turns the new test red; reverted. Code commit c418cb8; FINDINGS F133 marked fixed.

Verified: full hub suite 4776 passed, 86 skipped (37 min run, on the tree with the mutation reverted); CLI `tests/` 553 passed except that `test_cli_symlink_privilege::test_the_probe_leaves_nothing_behind` failed once while a Hub suite ran concurrently and passes alone, before and after my change (unverified cause: probable temp-dir race between the two concurrent runs). ruff, black, mypy clean. Not driven on a live Hub: the night-drive item covers this surface.

## Iteration 3 — tool-server-impl (2026-09-24 night)

CI verdict for previous push sha 933114f: `success`. Branch matched STATE. The tree arrived dirty with the previous firing's unfinished tool-server work; I verified and finished it.

Built: `hub/hub/tool_server.py` (`ToolServerPin`, `PIN`, `pinned_server_path`), `agent_trigger.py` uses it and answers 409 with the materialize sentence on `OSError`, `main.py` lifespan pins, logs and prunes, `.claude/rules/mcp-server.md` rewritten. Verified: pin + trigger + stdio-surface tests 72 passed; neighbouring mcp/runner/codex tests 341 passed; ruff, black, mypy clean. NOT done: full hub suite (37 min, not run; CLI `tests/` not run), tasks 0.x (rounds, F354 decision row) and 3.x (live drive, deferred to night-drive). Not archived.

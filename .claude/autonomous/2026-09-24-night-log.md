# 2026-09-24 night window

Newest entry at the BOTTOM. Playbook: `.claude/loops/night-window.md`.

Armed 2026-09-24T22:55:12+01:00 on `autonomous/2026-09-24-daily` (parent master@8a5d0ed), model opus.

## Iteration 1 — compose (2026-09-24 night)

CI verdict for inherited sha 8a5d0ed: `success` (run 17:12Z); armed commit ec5f690 in_progress at read time (age minutes, not hung). Branch `autonomous/2026-09-24-daily` matches STATE.

Backlog page refreshed (124 open findings, up from 104). APPROVALS `## 2026-09-24` is today's section and carries an `ORDER:`, taken verbatim as the queue: F133, tool-server, pressing-run, checkpoint-handover, event-announce, run-id-event, flows-own-moves, then a drive item. Tree-green check on the local suite not run this iteration (compose only); CI success on the inherited sha stands in. First build item's firing runs `tests/` before its commit.

## Iteration 2 — f133-impl (2026-09-24 night)

CI verdict for previous push sha 13eca69: `in_progress` at read time (inherited 8a5d0ed: `success`). Branch matched STATE.

Built F133: `select_turn(entries, hop_budget, cap)` and `entry_kind` (was `_entry_kind`) now live in `hub/hub/inbound_queue.py`; `_attempt_turn` and `GET /queue/{agent}/status` both call it, so the status route's token-budget test reads the turn's entries, not every queued entry. Two tests added in `hub/tests/test_accounting_budget.py` (autonomous-then-operator keeps the reason; operator-first control). Failing first (status returned null), green after. Mutation: dropping the conversation filter in `select_turn` turns the new test red; reverted. Code commit c418cb8; FINDINGS F133 marked fixed.

Verified: full hub suite 4776 passed, 86 skipped (37 min run, on the tree with the mutation reverted); CLI `tests/` 553 passed except that `test_cli_symlink_privilege::test_the_probe_leaves_nothing_behind` failed once while a Hub suite ran concurrently and passes alone, before and after my change (unverified cause: probable temp-dir race between the two concurrent runs). ruff, black, mypy clean. Not driven on a live Hub: the night-drive item covers this surface.

## Iteration 3 — tool-server-impl (2026-09-24 night)

CI verdict for previous push sha 933114f: `success`. Branch matched STATE. The tree arrived dirty with the previous firing's unfinished tool-server work; I verified and finished it.

Built: `hub/hub/tool_server.py` (`ToolServerPin`, `PIN`, `pinned_server_path`), `agent_trigger.py` uses it and answers 409 with the materialize sentence on `OSError`, `main.py` lifespan pins, logs and prunes, `.claude/rules/mcp-server.md` rewritten. Verified: pin + trigger + stdio-surface tests 72 passed; neighbouring mcp/runner/codex tests 341 passed; ruff, black, mypy clean. NOT done: full hub suite (37 min, not run; CLI `tests/` not run), tasks 0.x (rounds, F354 decision row) and 3.x (live drive, deferred to night-drive). Not archived.

## Iteration 4 — pressing-run-impl (2026-09-25 night)

CI verdict for previous push sha 06e0cec: `in_progress` at read time (933114f: `success`). Branch matched STATE, tree clean.

Built `pressing-run-names-the-reason-that-held`: `LoopBusyRefusal` + `BUSY_*` constants and `_loop_flow_busy_refusal` in `scheduler.py` (`_loop_flow_busy_reason` is now the wrapper); `run_job` names only the condition that held, answers from a row only where it wrote or counted into it (`skipped` → 409, `failed` → 500), and re-asks the guard unconditionally below those arms. 13 new tests in `hub/tests/test_pressing_run_names_the_reason.py`; the two moved assertions in `test_board_agent_role.py` (task 1.6). Mutation: with the old `jobs.py` restored, 8 new tests and both moved assertions fail; the four controls pass. Verified: 64 passed across the four neighbouring files; ruff, black, mypy clean. Full hub suite (2.5) was started in the background at commit time and NOT read: unverified. CLI `tests/` not run (no CLI code touched). Tasks 3.x (drive), 4.x (findings, archive) left to night-drive.

## Iteration 5 — checkpoint-handover-impl, part 1 of 2 (2026-09-25 night)

CI verdict for previous push sha 8c6cb54: `in_progress` at read time (06e0cec also `in_progress`; 933114f `success`). Branch matched STATE, tree clean. `/tmp/hubsuite.txt` from iteration 4 stopped at 2% (the run was killed with its process): task 2.5 of pressing-run stays UNVERIFIED and unticked.

Split, as the queue detail allowed: this firing built the fix, the migration and the sequential + race tests; the next takes the trigger tests, the route test, the D6 tests and the drive.

Built: `Checkpoint.cut_over_to_conversation_id` plus partial unique index `ix_checkpoints_one_handover_per_conversation` (model + migration `0106`, guarded, per-row backfill in `sequence` order, first handover wins); `cut_over` refuses first on the recorded handover (D4 texts), captures plain ids, then flushes and claims with a conditional UPDATE (`synchronize_session=False`), rolls back and re-reads on a miss or `IntegrityError` (re-raises an `IntegrityError` that names no handed-over checkpoint); `CheckpointSummary.cut_over_to_conversation_id`; trigger returns `payload["checkpoint_id"]` on refusal (2.6) and declines a handed-over conversation after the dismissed/final backstop (2.7 code). New `hub/tests/test_checkpoint_handover.py` (6 tests: F293, D2 second checkpoint, D1 record, chain control, and both races with the barrier inside a patched `archivable` and a rollback spy); 4 migration tests (backfill, fork/duplicate/quoted/missing, guard, downgrade); the two moved/extended assertions in `test_checkpoint_cutover.py` (1.7 not extended — see below).
Mutations: unique index off → the two-checkpoint race fails, the same-checkpoint race passes; the `IS NULL` predicate dropped from the claim → the same-checkpoint race fails, the other passes. Both reverted.
Verified: 179 passed, 1 skipped over cutover, handover, migrations, project_persistence and checkpoint_access with `claude` stripped from PATH; ruff, black, mypy clean. Not run: CLI `tests/` (no CLI code touched), the full hub suite, the fail-first record for 1.1–1.5 (mutations stand in).
NOT done: 1.7's extension (the after-unarchive cutover), 1.8, 1.10–1.13, 2.7's tests (so 2.7 unticked), 3.7 (F329 parity — covered by the migrations file passing, tick after a named run), group 4. Not archived.

## Iteration 6 — checkpoint-handover-impl, part 2 of 2 (2026-09-25 night)

CI verdict for previous push sha 1f79ee8: `success`. Branch matched STATE; the tree arrived dirty with the previous firing's unfinished tests (1.8, 1.10–1.13, 1.7 extension), which I verified and committed.

Verified: cutover + handover files 56 passed (claude stripped from PATH); F329 parity test passes (3.7); ruff, black, mypy clean; full hub suite 4815 passed, 86 skipped (35:48). Mutation for 1.13: a decline placed above the dismissed/final backstop turns `final_warning` red (1 failed, 3 passed); reverted, file unchanged. Tasks 1.x, 2.7, 3.7, 4.1–4.3 ticked. CLI `tests/` not run (no CLI code touched). NOT done: 4.4 (drive → night-drive), 4.5 (findings footnote), archive.

## Iteration 7 — event-announce-impl (2026-09-25 night)

CI verdict for previous push sha 35fe156: `success`. Branch matched STATE; the tree arrived dirty with the previous firing's finished-but-uncommitted build, which I verified and committed.

Built `an-event-is-announced-only-once-its-write-is-committed`: `SSEManager.publish` (sync) with `broadcast` delegating; `defer_broadcast` plus `after_commit` / `after_transaction_end` listeners in `hub/hub/sse.py`; `resolve_divergences_for_task` now defers (F335); new `hub/tests/test_an_event_is_announced_after_commit.py` (with the AST guard rules 1 and 2); the residual note in the refused-review test updated; F335 marked fixed in FINDINGS. 2.4b n/a (B10 not landed).
Verified: new file + test_sse + test_operator_events + refused-review file 56 passed; ruff, black clean; full hub suite 4822 passed, 86 skipped (30:43, `claude` stripped from PATH). CLI `tests/` not run (no CLI code touched). Not driven live (night-drive). 0.3 (operator approval) and 3.2 (reconcile on archive) left open. Not archived.

## Iteration 8 — run-id-event-impl (2026-09-25 night)

CI verdict for previous push sha e3f7272: `in_progress` at read time (35fe156 and 1f79ee8: `success`). Branch matched STATE, tree clean.

Built `run-id-in-an-event-always-names-a-run`: `run_id` → `job_run_id` at all nine `scheduler.py` sites (`job_fired` ×4, `job_run_skipped` ×4, `job_run_failed` ×1) and `jobs.py` (`_record_job_run_failure`, `run_job`'s answer, local renamed); UI response type in `hub/ui/src/api/jobs.ts`. New `hub/tests/test_run_id_names_a_run.py` (5 tests, 1.1-1.5): all 5 fail with `scheduler.py`/`jobs.py` reverted, pass after.
Verified: neighbouring scheduler/pressing-run/flow-width/jobs files 156 passed; full hub suite 4827 passed, 86 skipped (30:29, `claude` off PATH); UI `npm test` 1622 passed, `npm run lint` clean; ruff, black clean. CLI `tests/` not run (no CLI code touched). UI bundle not refreshed (type-only edit; unverified that build output is unchanged). Tasks 1.x, 2.x ticked. NOT done: 0.3 (operator's D1 record), 3.1 (drive → night-drive), agent-stream-events spec reconcile on archive.

## Iteration 9 — flows-own-moves-impl (2026-09-25 night)

CI verdict for previous push sha d4c7c73: `in_progress` at read time (033f1e1 and e3f7272: `failure`, the test_openspec_task_evidence break that d4c7c73 fixed; 35fe156: `success`). Branch matched STATE; the tree arrived dirty with the previous firing's finished-but-uncommitted build, which I verified and committed.

Built `a-flows-own-moves-are-recorded-as-the-flows`: `ORIGIN_JOB` and `job_id` in `apply_transition` (validated pair), `enter_selected_task(..., origin, job_id)`, migration `0107` (`job_id` on `task_transitions` and `inbound_queue_entries`), scheduler callers and `agent_trigger` reading the cause from delivered entries, `_transition_view` gains `job_id/job_name/job_kind`, UI `TaskTransitionHistory` renders `Flow/Loop <name> moved`, bundle refreshed (`index-C9AJfuRV.js`). New `hub/tests/test_a_flows_moves_are_the_flows.py`; AST guard in `test_task_transitions.py`.
Verified: targeted files 191 passed, 1 skipped (`claude` off PATH); full hub suite 4841 passed, 86 skipped (38:05); UI history vitest 5 passed, lint clean; ruff, black, mypy clean. CLI `tests/` not run (no CLI code touched). Not driven live (night-drive). Left open: 0.3 DECISIONS.md entry, 3.2 drive, 3.3 archive.

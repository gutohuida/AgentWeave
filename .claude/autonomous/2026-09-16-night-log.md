# 2026-09-16 night window

Newest entry at the BOTTOM. Playbook: `.claude/loops/night-window.md`.

Armed 2026-09-16T22:55:03+01:00 on `autonomous/2026-09-14-daily` (parent master@31dcb3f), model opus.

## Iteration 1 — compose

Branch confirmed: `autonomous/2026-09-14-daily`, matches STATE. `git log` tip is `c11b139`
(arm commit), consistent.

`spec-queue/APPROVALS.md`'s newest `## <date>` heading is `## 2026-09-16` — matches tonight's
Armed date, so it applies. It carries `ORDER: a-materialised-task-carries-its-criteria` and
`APPROVED a-materialised-task-carries-its-criteria build §2-§5 tonight; §6-§7 only if they fit`.
That is the whole queue for tonight; the default backlog shape is ignored per the section.

Step 3 (green-tree gate): the section's guarded green-tree measurement
(4415 passed, 86 skipped, 0 failed, 27:46 at `6d70710`) still applies — checked `git show --stat`
on `c11b139`, `a999941`, `6d70710` and none touch `hub/` or `src/` (only `.claude/autonomous/`,
`spec-queue/APPROVALS.md`, `spec-queue/DIRECTION.md`). So step 3's own run is skipped tonight,
per the section's explicit permission.

Wrote the queue into STATE-night.json: `amtci-impl` (§2), `amtci-tests` (§3),
`amtci-mutations` (§4), `amtci-gate` (§5), `amtci-drive` (§6-§7, conditional on fit).
`current`/`next_action` point at `amtci-impl`. No work beyond composing done this iteration,
per playbook instruction.

## Iteration 2 — §2 implementation (amtci-impl)

Branch/log reconciled against STATE-night.json before starting: `git log` tip matched the prior
iteration's commit, no drift.

Read design.md's Round log and D1-D8 in full, and tasks.md §2 in task order (2.1, 2.2, 2.7, 2.3,
2.6, 2.5, 2.4) before writing anything, per `next_action`.

**`hub/hub/spec_reading.py`**: added the `isinstance(raw, list)` guard inside both
`statements_by_key` (was `payload.get("requirements") or []` at :72) and
`criteria_by_requirement_key` (was `payload.get("acceptance_criteria") or []` at :98) — both now
`return`/`return grouped` early when the raw value is present but not a list, closing the
`TypeError: 'int' object is not iterable` hole task 1.8 measured. Guards sit inside the helpers,
not at the call site, per D7.

**`hub/hub/spec_tasks.py`**: added `_render_criterion` (D2/D8 — prefixes the handle only when it's
a non-empty string, never emits the literal `None`, returns `None` for a criterion with no given/
when/then so the caller skips it) and `_criteria_for_entry` (D3/D4 — `dict.fromkeys` de-dup of the
entry's own `requirements` names, not a `set`; collects each name's group from
`criteria_by_requirement_key`; one stable sort keyed by `position.get(name, len(position))`, so the
sort key comes from the *group a criterion was collected under*, not a field of the criterion
itself — the helper's dicts carry no `requirement` field, task 2.2's warning). `materialise()` now
builds `position` (from `spec_reading.statements_by_key(payload)`'s keys, task 2.7 — not a
hand-rolled `enumerate(payload["requirements"])`, which raises on a non-dict element) and
`criteria_index` once, before the per-entry loop (D6). Each entry's own `requirements` names are
collected into `names` in the same pass that resolves `requirements`/`unresolved` (unchanged
resolution logic), matching on the payload key the entry **named** — not the resolved row's
`.key` — per D3. `Task(...)` now sets `acceptance_criteria=criteria or None` — unset, not `[]`,
when nothing attaches (task 2.4).

Verified by execution, not just import: `_render_criterion` produces
`"ac1: Given g, when w, then t"` for a full criterion, drops the prefix when `key` is `None`,
returns `None` (skipped) when given/when/then are all absent, and renders `"ac1: Then t"` for a
then-only partial — no literal `None` in any output. `_criteria_for_entry` given entry names in
**reverse** of `payload.requirements` order still returned criteria in `payload.requirements`
order (`req-a` before `req-b`), confirming the sort — not the collection order — decides the
result. Re-ran the two hostile-payload guards from task 1.8/D7: `criteria_by_requirement_key({
"acceptance_criteria": 5})` and `statements_by_key({"requirements": 5})` both now return `{}`
instead of raising, as do a list holding a non-dict element for each.

Ran the existing suites that exercise these modules: `pytest tests/test_spec_declared_tasks.py
tests/test_spec_reading.py -q` from `hub/` — 20 passed, 0 failed. `ruff check` and
`black --check --target-version py311` on both edited files — clean. (Full-suite gate is §5's job,
not this iteration's.)

Ticked tasks.md §2 (2.1, 2.2, 2.7, 2.3, 2.6, 2.5, 2.4) — all seven, each backed by the
verification above, not by the plan existing.

**Not done, deliberately out of scope for this iteration**: §3 tests, §4 mutations, §5 gates. Those
are `amtci-tests`, `amtci-mutations`, `amtci-gate` in the queue, unchanged. `current`/`next_action`
now point at `amtci-tests`.

## Iteration 3 — §3 tests (amtci-tests)

Branch/log reconciled first: `git log` tip was `e11bdfd` ("build(night): §2 — materialise()
attaches criteria + spec_reading guards"), branch `autonomous/2026-09-14-daily`, tree clean — all
three match STATE-night.json. No drift, nothing to reconcile.

Read the §2 implementation before writing anything, per `next_action`: `_render_criterion`,
`_criteria_for_entry` and `materialise()`'s `position`/`criteria_index` in `hub/hub/spec_tasks.py`,
and the two `isinstance(raw, list)` guards in `hub/hub/spec_reading.py`. Then
`tests/test_spec_declared_tasks.py` and `tests/test_spec_reading.py` for the fixture and helper
conventions (the `author` run-credential fixture, the `_Row` duck type for the pure-function view,
the direct-`materialise()` pattern that file already uses for payloads `validate_payload` refuses).

**Wrote `hub/tests/test_spec_criteria_reach_the_task.py` — all 25 of §3 (3.1-3.26 as listed;
3.20 does not exist in the list), none skipped.** Shape of the file:

- **3.1 goes the whole way through the HTTP approval route** — create document, submit, close
  exploration, `proposed`, `approved`, then read the board and assert the task's
  `acceptance_criteria`. Deliberately not a unit call: it is the one test that makes "approving a
  document gives its tasks their criteria" a statement about the route an operator uses.
- Everything else calls `materialise()` (or `materialise_quietly` where the task said so) directly,
  because the discriminating payloads are mostly ones `validate_payload` refuses on submission —
  which is the realistic case, not a contrived one, per task 1.5: the approval route parses with
  `extract_payload` and `spec_adoption` never validates at all.
- 3.22/3.25 are plain synchronous calls into `spec_reading.requirement_view`, the
  `read_spec_document` path, with a duck-typed row.
- 3.10 composes a real `_compose_loop_briefing` for **both** `is_review=False` and `is_review=True`
  and asserts one `- ` line per criterion plus `all(isinstance(line, str))`.

Every fixture carrying an explicit MUST clause was honoured literally:

- **3.6** criterion keys `zebra, apple, mango` — not alphabetical, so a key-sort yields a different
  list than written order.
- **3.15** entry lists `["beta", "alpha"]`, the reverse of `payload.requirements`, and the criteria
  interleave — so the correct answer (`alpha-1, alpha-2, beta-1, beta-2`), the entry-order answer
  and the raw-payload-position answer are three distinct orders.
- **3.23** the absent-requirement criterion (`ghost-1`) is written **before** the present one, so
  raw document order would put it first; the assertion is `alpha-1, ghost-1`.
- **3.11** five shapes, two tasks declared and **both asserted** on every shape; the shapes include
  `"abc"` and `["ac-alpha", 7]` — lists holding non-dict elements — and assert through
  `materialise_quietly`.
- **3.26** `requirements` is `["not a requirement", ALPHA]`, a list holding a non-dict element, and
  is asserted through plain `materialise()` so a raise surfaces as a failure rather than as an
  empty board.
- **3.24** declares criteria the entry names, so the `statements_by_key` guard is actually reached.

**Verified, not just written.** `py -3.11 -m pytest tests/test_spec_criteria_reach_the_task.py -q`
from `hub/` — **25 passed**. With the two neighbouring suites — **45 passed, 0 failed, 28.8s**.
`ruff check` and `black --check --target-version py311` on the new file — clean.

**And shown able to fail.** 25/25 green on the first run is exactly the shape of a section that
pins nothing, so two mutations were applied together as a smoke test — `rank = 0` in
`_criteria_for_entry` (4.8b's shape) and both `isinstance(raw, list)` guards replaced by `or []`
(4.10/4.16's shape). **Exactly six flipped: 3.11, 3.15, 3.18, 3.22, 3.24, 3.25** — the six those
two mutations should reach, and nothing else. 3.6 and 3.23 correctly did **not** flip: their
targets are 4.4 and 4.14, which this probe did not apply. Reverted with
`git checkout -- hub/hub/spec_tasks.py hub/hub/spec_reading.py`; re-ran, back to 45 passed. This is
a smoke test of the section, **not** §4 — §4 still applies all seventeen properly, one at a time,
and its job includes finding the ones that flip nothing.

Ticked tasks.md §3 — all 25, each with a per-row `**Done** — \`test_...\`` note naming the test
that closes it, plus a section note carrying the commands and their output. (Noted while doing it:
§2's rows were ticked last iteration **without** Done notes, which the file's own header requires.
Not repaired here — it is a documentation gap in a section whose work was independently verified
and logged, and repairing it belongs with §4's pass over the same file rather than in a commit
about tests.)

**Not done, deliberately**: §4's seventeen mutations, §5's gates, §6-§7. Unchanged in the queue.
`current`/`next_action` now point at `amtci-mutations`.

## Iteration 4 — §4 mutations (amtci-mutations)

Branch/log reconciled first: `git log` tip `13eb1a9` ("build(night): §3 — 25 tests pinning the
criteria a materialised task carries"), branch `autonomous/2026-09-14-daily`, tree clean — all
three match STATE-night.json. No drift.

**All seventeen mutations applied individually, run, recorded and reverted. Every one flipped its
named target. None flipped nothing.**

Harness: `testbed/scratch/mutate_amtci.py` (throwaway, gitignored, not committed). Each mutation
is a list of exact-anchor replacements over `hub/hub/spec_tasks.py` and/or
`hub/hub/spec_reading.py`; the anchor count is asserted to be exactly 1 before writing, so a
mutation that silently lands in the wrong place aborts instead of producing a result. Then
`py -3.11 -m pytest tests/test_spec_criteria_reach_the_task.py -q --no-header -rf` from `hub/`,
the `FAILED`/`ERROR` lines parsed back to row numbers, then
`git checkout -- hub/hub/spec_tasks.py hub/hub/spec_reading.py` before the next. Baseline first:
**25 passed in 14.10s**.

One mutation (4.8) aborted on the first pass with `anchor count=2` — `for entry in raw:` occurs in
both `statements_by_key` and `criteria_by_requirement_key`. Re-anchored on the three-line form
unique to the criteria helper and re-run. That abort is the harness working: an ambiguous anchor
would have mutated the wrong function and been recorded as a result.

Target-by-target, all seventeen, `flipped` being every row that failed, not only the target:

| row | target | flipped |
|---|---|---|
| 4.1 | 3.7 | 3.7 |
| 4.6 | 3.11 | 3.11 |
| 4.7 | 3.13 | 3.1 3.2 3.3 3.5 3.6 3.7 3.9 3.10 3.13 3.14 3.15 3.17 3.19 3.21 3.23 3.24 3.26 |
| 4.8 | 3.15 | 3.15 3.23 |
| 4.12 | 3.14 | 3.14 |
| 4.9 | 3.16 | 3.16 |
| 4.10 | 3.18 | 3.11 3.18 3.22 |
| 4.11 | 3.17 | 3.11 3.17 |
| 4.2 | 3.2 | 3.1 3.2 3.4 3.12 3.19 3.21 |
| 4.3 | 3.8 | 3.1 3.2 3.3 3.5 3.7 3.8 3.9 3.10 3.14 3.16 3.17 3.19 3.21 3.24 3.26 |
| 4.4 | 3.6 | 3.6 |
| 4.8b | 3.15 | 3.15 |
| 4.14 | 3.23 | 3.23 3.24 |
| 4.15 | 3.22 | 3.22 |
| 4.16 | 3.25 | 3.24 3.25 |
| 4.17 | 3.26 | 3.26 |
| 4.5 | 3.4 | 3.4 |

**The two `next_action` singled out as likely already-satisfied are not.** 4.11 (attach criteria
that state nothing) flips 3.17; 4.5 (attach criteria to tasks resolving no requirement) flips 3.4
and nothing else. Both were read carefully before being written, as instructed, and both turned
out to be real decisions with real tests behind them.

**Three things the run measured that the rows did not predict**, all recorded in the rows rather
than smoothed over:

1. **4.16's retarget is right and its stated reason is wrong.** The row says 3.24 was dropped as a
   target because `materialise_quietly`'s catch-all protects it. It does not protect it — it
   converts the `TypeError` into an empty board, and 3.24 asserts the tasks *exist*, so 3.24 fails
   too, just not by raising. The retarget survives on its real merit (3.25 is where the raise
   reaches a caller with no `try`). Recorded in 4.16's Done note per the round discipline: an
   argument can be wrong while everything it argues about is right.
2. **4.8 is a weaker discriminator than it reads.** Ordering by raw `payload.acceptance_criteria`
   position is *not reachable* inside the helper boundary D7 prescribes — the grouped dicts carry
   only `key`/`given`/`when`/`then`, so the mutation had to stamp a synthetic `_raw_position` onto
   them in `spec_reading.py` before `spec_tasks.py` could sort by it. The algorithm 4.8 mutates
   toward is one D7 already forecloses. **4.8b is what actually pins the ordering**: one file, an
   implementation a reasonable author would write, one test standing between it and the correct
   one. 4.8 is not worthless — it confirms 3.23's written-first fixture does its job — but the
   ordering requirement rests on 4.8b.
3. **4.7 and 4.3 flip 17 and 15 rows.** Rendering is a single choke point that nearly every
   assertion compares whole strings against. Breadth is not weakness — 3.13 and 3.8 are the rows
   that state *why* the handle and the `then` are there, and each fails on its own assertion — but
   it means those two mutations say little about test targeting beyond their targets.

**Implementation byte-identical to HEAD at the end**, checked as `next_action` required:
`git status --short` shows only `M openspec/.../tasks.md`, `git diff --stat` touches only that
file. Re-ran the three neighbouring suites afterwards — `test_spec_criteria_reach_the_task.py`,
`test_spec_declared_tasks.py`, `test_spec_reading.py` — **45 passed, 0 failed, 28.6s**, the same
counts as iteration 3's. `openspec validate a-materialised-task-carries-its-criteria --strict`
passes.

Ticked tasks.md §4 — all seventeen, each with a Done note naming the mutation as applied, the rows
that flipped, and whether that matches what the row predicted. Added a section note carrying the
harness, the commands, the baseline and the byte-identical check.

**No finding recorded in `scripts/drive/FINDINGS.md`.** `next_action` said a mutation that flips
nothing is a finding about the tests; none did. The two observations worth keeping (4.16's
reasoning, 4.8's reachability) are about this change's own §4 argument, not about the product or
the spec flow, so they live in the rows and here rather than as drive findings.

**Not done, deliberately**: §5's gates, §6-§7. Unchanged in the queue. `current`/`next_action` now
point at `amtci-gate`.

## Iteration 5 — §5 gates (amtci-gate)

Branch/log reconciled first: `git log` tip `6c1d055` ("build(night): §4 — all 17 mutations
applied, run and reverted"), branch `autonomous/2026-09-14-daily` — both matched STATE-night.json.
Tree was **not** clean, though: `openspec/changes/.../tasks.md` was already modified with all
four §5 rows marked `[x]` and Done notes, none of it committed, and `.claude/autonomous/
hub-suite-output.txt` sat untracked — both pre-dated this iteration (visible in the session's
opening `git status`). Investigated before trusting or discarding it, per the "verify a memory
before acting on it" discipline this loop runs under: 5.1's own Done note pointed at "the section
note below" for the full-suite counts, but no such note existed anywhere in the diff — a claim
citing evidence that was never written. Read as a sign of an interrupted or confused prior attempt
that never ran what it claimed, not as real completed work. **Discarded** with
`git checkout -- openspec/.../tasks.md` (safe: nothing was committed, so nothing was lost) and
redone from scratch, each row backed by a command actually run this iteration.

**5.1**: `py -3.11 -m pytest tests/ -q` from `hub/`, backgrounded first (two path-quoting false
starts before it actually ran — bash ate the backslashes in a Windows path on the first attempt,
producing a stale-looking progress file from nothing; re-launched with a forward-slash path and
watched it via Monitor, re-arming three times across the ~42-minute run since each arm caps at 10
minutes). Final: **4440 passed, 86 skipped, 0 failed, 42:02.** Against the guarded green-tree
baseline this window's own iteration 1 recorded (4415 passed, 86 skipped, 0 failed, 27:46 at
`6d70710`): **+25 passed, 0 new failures** — exactly §3's new test file, nothing else moved. Gate
passes.

**5.2**: `ruff check src/ hub/ tests/` — all checks passed. `black --check src/ hub/hub/
hub/tests/ tests/ --target-version py311` — 578 files unchanged. `mypy src/` — no issues in 22
source files. (Note for any future iteration: these tools are not on the bash tool's `PATH`
directly — `py -3.11 -m ruff`/`-m black`/`-m mypy` via PowerShell is what worked.)

**5.3**: `npx openspec validate a-materialised-task-carries-its-criteria --strict` — valid.
Re-run as `next_action` asked, not assumed from iteration 4.

**5.4**: The trial Hub (`:8010`) has **zero** `approved`/`current`-phase documents — checked
directly, so no measurement was possible there. Port 8000's real database has exactly one:
`spdoc-97d90a3506f5` ("Loop engine with a dashboard for agent loops"). Read only via a `mode=ro`
SQLite URI (`projects`, `spec_documents`, `tasks` tables) — the `next_action`'s own wording
carves out exactly this ("or anything on port 8000's database except through a mode=ro URI"); no
write, no start, no migration, no API call. Measured the *actual persisted*
`tasks.acceptance_criteria` for that document's already-materialised tasks (real `materialise()`
output from a real approval, not a reconstruction of the algorithm) and rendered each block exactly
as `_compose_loop_briefing` does (`scheduler.py:2456-2460`). 13 tasks carry criteria (2-9 each).
**Largest block: 1,307 characters — 33% of the 4,000-character checkpoint bound.** No bound
proposed: the measurement doesn't ask for one at today's scale, and per task 1.6 the criteria block
sits beside the checkpoint's budget rather than inside it. Residual recorded in tasks.md as before:
a much larger future document is still unmeasured.

All four gates pass. `openspec/changes/a-materialised-task-carries-its-criteria/tasks.md` §5 ticked
with real Done notes. Committed as one checkpoint (`.claude/autonomous/hub-suite-output.txt` left
untracked, scratch only). `current`/`next_action` now point at `amtci-drive` (§6-§7), conditional on
fit before `stop_at` (2026-09-17T07:00:00+01:00) — the playbook's own instruction is to stop after
the gate and say so if §6-§7 doesn't genuinely fit, rather than compress it.

# 2026-09-21 night window

Newest entry at the BOTTOM. Playbook: `.claude/loops/night-window.md`.

Armed 2026-09-21T22:55:05+01:00 on `autonomous/2026-09-21-daily` (parent master@72cfb99), model opus.

## Iteration 1 — compose (2026-09-21 ~23:00)

- Branch `autonomous/2026-09-21-daily` matches STATE; HEAD 3e06634 (arm commit).
- Backlog page current, nothing moved. CI verdict for inherited sha 3e06634: run `in_progress`, created 21:55Z (just started; not hung, no conclusion yet). Not a red streak observation tonight.
- APPROVALS `## 2026-09-21` present with operator `ORDER:` written this evening; taken verbatim. DECISIONS late-night entry approves nothing new (an-unstaffed-review needs R8 review on 09-22; F388 is for 09-22).
- Queue: archive first-turn; F380; F292 x2 firings; a-loop-staffs group 7 drive + archive (not §5); F375 groups 1, 2, 3, archive.
- Local suite green check deferred to the first build item (no code touched this iteration).

## Iteration 2 — a-first-turn-archive (2026-09-21 night)

- Branch/HEAD matched STATE (1fd204d). Tasks 38/38 ticked, 0 open.
- `openspec archive a-first-turn-is-not-told-it-has-nothing -y`: 2 MODIFIED requirements applied to `openspec/specs/agent-capability-plane/spec.md`; archived as `archive/2026-09-21-a-first-turn-is-not-told-it-has-nothing`.
- Measured: `openspec validate agent-capability-plane --strict` valid; the delta's R2/R3 process-note comment did not leak into the corpus (0 hits); new scenario "No grounds means no denial either" present.
- F302 Status line already reads `fixed 802a8c7`; left as is, no further claim.
- No code touched, so no tests run this iteration.

## Iteration 3 — f380-impl (2026-09-21 night)

- Branch/HEAD matched STATE (85efe66).
- F380 (a): `arm-cycle.ps1` and the installer's dirty check now use `git status --short --untracked-files=no`. The arm's refusal also appends a line to the gitignored `driver-<window>.log` (Say alone reaches only the console; Task Scheduler keeps a bare exit code). Not written to the tracked window log: that would itself dirty the tree.
- Measured on the check logic alone: with one untracked scratch file, tracked-only count 2 (my own edits) vs full count 3; a tracked edit is still counted. Scratch file removed. Both scripts parse with 0 errors; `tests/test_autonomous_skills.py` 22 passed. Did not register tasks or run arm for real; the refusal-log branch itself is unexercised (unverified).
- (b) was decided 2026-09-18 and is not touched. F380 status line says only (a) fixed.

## Iteration 4 — f292-impl-1 (2026-09-21 night, two firings)

- The 23:15 firing wrote the change, then exited while its full-suite run was still going, leaving the tree dirty and nothing committed. The 23:20 firing (this one) found only its own process running, re-verified the work and committed it. Branch/HEAD matched STATE (5bb9e6c).
- F292 cheap shape, "give them own DB": `hub/tests/conftest.py` adds a `do_connect` listener so new connections open `_current_test_db_file`, and the `app` fixture moves that to a fresh `hub-test-<n>.db` right after `dispose()`. It also best-effort deletes the previous file. `engine.url` is unchanged, so the isolation guards still pass.
- New `hub/tests/test_each_test_gets_its_own_database.py`, 3 tests: a raw sqlite3 holder with `BEGIN IMMEDIATE` and an uncommitted write no longer blocks the next reset. The previous firing mutation-checked it: with the move removed, the test fails with CI's `database is locked` on `BEGIN IMMEDIATE` (I did not re-run that).
- Measured: the new test and the isolation tests, 10 passed. Full `hub/tests`: **4478 passed, 86 skipped** in 37 min. ruff and black are clean on both files.
- CI baseline on this branch before the fix: 3e06634 red (F292), 1fd204d red (F292), 85efe66 green, 5bb9e6c green, so 2 of 4. The fix goes live from this commit on. The per-push rate for this commit and later is `f292-impl-2`'s job. It is not yet measured, so there is no claim that F292 is fixed.

## Iteration 5 — f292-impl-2 (2026-09-21 night)

- Branch/HEAD matched STATE (b630252).
- The only post-fix `ci.yml` run so far is `b630252`, run `35665344029`. I waited for it to finish: **success**, every job green. hub-test logged `4471 passed, 20 skipped` in 11 min, with no `database is locked`.
- Tally on this branch: pre-fix 2 of 4 red (both F292); post-fix **0 of 1**. One clean run happens 75% of the time at the 25% baseline, so this measures nothing yet. F292 stays **open**. Its Status line and foot both record this and the 11-run bar (0.75^11 ≈ 0.042).
- Timebox spent (2 firings); nothing more built. **Standing tally for later iterations:** each later push on this branch adds one `ci.yml` run. Record F292 hit or miss per run here. If one shows `database is locked`, the per-file move did not reach the holder. The lead is then "the holder follows the test".
- Only FINDINGS.md prose changed; no code touched, so no tests run.

## Iteration 6 — a-loop-staffs-drive (2026-09-22 ~00:20)

- Branch/HEAD matched STATE (3d9e2e4). Tasks 7.1-7.4 were all unticked (the 09-20 drive predates the F392 counts rule), so I re-drove all four on current code.
- **7.1** pre-fix: a worktree at `e41b607` (parent of the fix `831ac16`), own Hub :8094 at migration head 0104, `d7_0920_alsn_drive.py`: Run on a documentless loop naming busy gamma answered **200**, conversation `conv-fe95c98c3048`, task `in_progress` on alpha. 7 of 13 verdicts held; the 6 failing are the refusal-shape ones. F128 reproduces.
- **7.2/7.3** post-fix: fresh Hub :8093 from this checkout (head 0105), same script: **13 of 13**. 409 with `stall_reason`, no conversation, task pending and unassigned, bucket `stalled`; the flow's firing 200 with both tasks started on alpha and beta.
- **7.4** `mode=ro` read of `:8000`'s DB: 1 loop, spec-linked; 0 documentless loops, so 0 wedged review rows. Task 4.1 is a regression guard, not a live repair.
- Noticed: the pre-fix run started only one of the flow's two tasks, the post-fix run both. Recorded as an observation only; cause not investigated.
- Noticed: the script's teardown `DELETE` answered 409 both runs because Haiku turns were still finishing (the same transient as 09-19). I waited and deleted by hand (204 both), and removed the leaked fixture dirs `drive-0920-alsn-001614`/`-001811`. Left `-090657` alone: not mine. Both Hubs killed by PID, worktree and profiles removed, no listener on 8093/8094, no job enabled. `:8000` was only read via `mode=ro`.
- F292 tally: `3d9e2e4` CI run `35666358888` was still `in_progress` when I finished, so no hit/miss yet; the standing tally stays 0 of 1.
- No product code touched, so no test suite run. Ticks recorded in `tasks.md` 7.1-7.4.

## Iteration 7 — a-loop-staffs-archive (2026-09-22)

- Branch/HEAD matched STATE (69f1531). Every task in tasks.md (0.1-8.4) is ticked; 6.2-REDO and 7.x carry counts.
- F392 rule 2 union: guard/regression files named by all 5 unarchived changes, 14 existing files (`test_refused_capability.py` does not exist yet): `hub/tests/` test_a_held_agent_is_busy, test_a_loop_does_not_staff_its_own_review, test_a_loop_staffs_the_agent_it_names, test_a_task_nothing_will_move_holds_nobody, test_actor_aware_claimability, test_agent_actions_governed, test_codex_posture_ordering, test_config, test_flow_width, test_loop_busy_guard, test_permission_approver, test_questions, test_workspace_writes: **417 passed, 1 skipped, 68s**; `tests/test_hub_commands.py`: **41 passed**.
- Synced the delta into `openspec/specs/agent-loops/spec.md` (1 added, 2 modified requirements; `openspec validate --specs --strict` 43/43), moved the change to `archive/2026-09-22-a-loop-staffs-the-agent-it-names`.
- F128 (`831ac16`/`adca56b`), F161 (`f468bf5`), F70 (`f58cc75`) already carry fixed statuses; unchanged.
- F292 tally: `3d9e2e4` run 35666358888 **success** -> post-fix now **0 of 2** (`69f1531`'s run 35667056235 was in progress). Still not enough to call it fixed.

## Iteration 8 — f375-g1-impl (2026-09-22)

- Branch/HEAD matched STATE (90ef2b1). Ticked 0.3/0.4 citing DECISIONS `2026-09-21 late evening`, and 1.1-1.8 with counts in `tasks.md`.
- Tests only, in `hub/tests/test_permission_approver.py`: new section with 63 parametrised rows (15 parent-word, 11 tilde, 11 option-joined, 26 must-stay-allowed) plus `test_dotdot_is_judged_like_dotdot_slash` x2; R8, R9, H10, P7 flipped; H10b added.
- Measured on today's code: **43 failed, 240 passed, 1 skipped** in the file. The 43 are exactly the 15+11+11 refusal rows (37), the 2 judged-alike, and R8, R9, H10, P7. Every negative control and H10b passes. Scratch mutations (reverted, `mcp_server.py` clean): `trusted = True` fails H10b; a rule 4 refusing `..` outright fails the judged-alike test, but that run cannot isolate the root-workspace half, since the first half fails on the same code (noted in 1.2).
- Design disagreement: none. PowerShell rows for `--target-directory=..`, `tar -C..` and `dotnet -o:..` are asserted in both dialects; if group 2 finds one that cannot hold in PowerShell, that is design news, stop and log.
- No product code touched. ruff and black clean on the test file. F292: `69f1531` run 35667056235 not checked this firing; tally stays 0 of 2.

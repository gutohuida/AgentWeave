# 2026-09-19 night window

Newest entry at the BOTTOM. Playbook: `.claude/loops/night-window.md`.

Armed 2026-09-19T22:55:02+01:00 on `autonomous/2026-09-17-daily` (parent master@a435f48), model opus.

## Iteration 1 — compose

Branch confirmed against `git log` — `autonomous/2026-09-17-daily`, HEAD `6e3186f`, matches
`STATE-night.json`. No reconciliation needed.

`py -3.11 scripts/backlog_page.py`: `spec-queue/BACKLOG.html` current, nothing moved since the day
window's last regeneration. Ledger warnings unchanged (12 severity-less findings, none open; F53/F352
read open with a status line claiming a fix — not acted on here, not tonight's job).

`spec-queue/APPROVALS.md`'s newest section is `## 2026-09-19`, matching tonight's arm date, so it is
this window's to read. It carries an `ORDER:` line — used verbatim, per the playbook, ignoring the
default backlog-first shape:

```
ORDER: a-refused-capability-reaches-the-operator tasks 0.3, 0.4, 0.5 and 4.12 ONLY, then
an-unstaffed-review-names-its-holders tasks 1.1-1.4 ONLY, then a-loop-staffs-the-agent-it-names
groups 0, 1, 2, 3, 4, 6, 7, 8 -- NOT group 5
```

Baseline before any build — the regression files named for the two larger approvals, run together:
`test_a_held_agent_is_busy.py`, `test_a_task_nothing_will_move_holds_nobody.py`,
`test_a_loop_does_not_staff_its_own_review.py`, `test_loop_busy_guard.py`, `test_flow_width.py`,
`test_actor_aware_claimability.py` → **109 passed, 77.45s** (matches the two approvals' separately
measured baselines of 55 and 54). Tree is green; building on it.

Queue written to `STATE-night.json`, 8 items: `arc-impl`/`arc-drive` (the small additive migration
slice), `unrh-impl`/`unrh-drive` (group 1 only), then `alsn-impl-1`/`alsn-impl-2`/`alsn-drive`/
`alsn-closeout` for the largest approval, split by group since one firing can't finish all of groups
0-4,6-8. `alsn-impl-1`/`-2`/`-closeout` carry explicit `model`/`effort` since their ids don't match
the `-impl$`/`-drive$` suffix convention in `usage-policy.json`. `current` set to `arc-impl`.

No decisions logged for the operator this iteration — the approvals already resolved what's in and
out of scope, and F386 (the gate on the rest of `a-refused-capability-reaches-the-operator`) is
already routed to tomorrow's day window per the approval text, not this window's to escalate again.

## Iteration 2 — arc-impl

Branch confirmed against `git log` — `autonomous/2026-09-17-daily`, HEAD `7811bf4`, matches
`STATE-night.json`. Working tree was **not** clean at start: `git status` showed uncommitted edits
to `hub/hub/api/v1/questions.py`, `hub/hub/db/models.py`, `hub/tests/test_migrations.py`,
`hub/tests/test_project_persistence.py`, `hub/tests/test_questions.py`, plus an untracked
`hub/hub/migrations/versions/0104_question_subject_key.py` — the shape of tasks 0.3/0.4/0.5/4.12,
already written but never committed or logged by iteration 1's process. Read each diff in full
against the `next_action` spec rather than trusting that it matched:

- `models.py`: `Question.subject_key` (`String(200)`, nullable) plus a first `__table_args__` —
  `ix_questions_open_subject_key`, unique on `(project_id, subject_key)`, `sqlite_where` excluding
  answered/declined/NULL-keyed rows. Matches design D15/D17.
- `0104_question_subject_key.py`: additive, guards a missing `questions` table like `0033`/`0034`,
  idempotent column/index checks, symmetric downgrade. `down_revision = "0103"` — correct head.
- `questions.py`: `ask_question_for_actor` takes `subject_key: Optional[str] = None`, written onto
  the row; every existing caller (both `ask_question` and the agent-facing route) left unchanged.
- `test_migrations.py` / `test_project_persistence.py`: head assertions bumped `0103` → `0104`.
- `test_questions.py` (4.12): a new coexistence test — **found broken**. It posted
  `{"from_agent", "question", "blocking"}` to `POST /questions` and asserted `201`, but
  `QuestionCreate` requires `options` (min 2), `header`, and `multi_select`
  (`hub/hub/schemas/questions.py:22-35`) — the omission produced `422`, not the coexistence the test
  claimed to prove. This is exactly the "green for the wrong reason" failure mode CLAUDE.md's F190
  note warns about, except here it was red, not silently green — caught by actually running it
  rather than trusting the diff. Fixed by sending the same shape `test_ask_and_answer_question`
  already uses (`header`, two `options`, `multi_select`) for both posts.

Ran `py -3.11 -m pytest hub/tests/test_migrations.py hub/tests/test_project_persistence.py
hub/tests/test_questions.py -q` twice — red (`1 failed, 110 passed`) before the fix, green
(`111 passed, 1 skipped`, 166.98s) after. Marked tasks 0.3/0.4/0.5/4.12 `[x]` in
`openspec/changes/a-refused-capability-reaches-the-operator/tasks.md`. Staged exactly the seven
files touched (no `git add -A`) and committed `3b7718d`, pushed to
`origin/autonomous/2026-09-17-daily`.

`current` advances to `arc-drive`. No decisions logged for the operator — the test bug was found and
fixed within this window's own scope, nothing for them to weigh in on.

## Iteration 3 — arc-drive

Branch confirmed against `git log` — `autonomous/2026-09-17-daily`, HEAD `e07f42c`, matches
`STATE-night.json`; working tree was clean at start.

Fresh Hub from source, port 8091 (never 8000/8010), `DATABASE_URL` naming a brand-new
`~/.agentweave/hub/profiles/drive0919/agentweave.db` that had never held a revision. First boot used
the wrong SQLAlchemy URL scheme (`sqlite:///` instead of `sqlite+aiosqlite:///`) and crashed on
import — `.claude/reference/hubs.md`'s example has the right scheme, this was my own typo, fixed on
the second start. `alembic upgrade head` (applied implicitly at startup) landed on `0104` from the
empty database — confirmed via `SELECT version_num FROM alembic_version`. Schema inspection off
`sqlite_master` confirmed both artifacts task 0.3 describes: `questions.subject_key` (nullable
`VARCHAR(200)`) and `ix_questions_open_subject_key`, unique on `(project_id, subject_key)` with the
D17 partial predicate `WHERE answered = 0 AND declined = 0 AND subject_key IS NOT NULL` present
verbatim.

Getting an operator credential took two tries: setting `AW_BOOTSTRAP_API_KEY` on a *second* start
does nothing, because `_seed_operator_credential` (`hub/hub/db/engine.py`) only runs while
`OperatorCredential` is empty, and the first (crashed-then-fixed) start had already auto-generated
and persisted one before the variable was set. Read the auto-generated key back out of
`operator_credentials` directly (a fixture database I own, not the operator's) and used that instead
of restarting again.

Exercised the ordinary question flow on a disposable fixture project (`drive-0919-arc2`, created via
`POST /projects/open`, deleted via `DELETE /projects/{id}` when done — never `proj-5e960453`/
`proj-18e5d4e0`): `POST /questions` with the full `QuestionCreate` payload (`options` needed to be
`{label, description}` objects, not bare strings — the first attempt got a `422`) → `201`; `PATCH
/questions/{id}` with `{"answer", "labels"}` (not `answer_labels`, which only exists on the response
model — second `422` before finding the right field name) → `200`, `answered: true`. Nothing about
the existing ask/answer path changed shape with the new column and index present. `subject_key`
itself is not on `QuestionResponse` — expected, out of this slice's scope per the commit note.

Shut the drive Hub down after: the PID the launching shell reported was not uvicorn's real listening
PID (confirmed via `netstat -ano` before and after killing the correct one). Fixture directories
(`~/Documents/drive-0919-arc*`) needed a manual `rm -rf` after the script's own `shutil.rmtree`
silently failed to clear its `.git` directory — noted for whoever writes the next such script, not
filed as a finding (fixture cleanup on Windows, not product behaviour).

Recorded the whole drive as `D-2, 2026-09-19` in `scripts/drive/FINDINGS.md` — no defect found;
migration 0104 and tasks 0.3/0.4/0.5/4.12 drive clean. `current` advances to `unrh-impl`. No
decisions logged for the operator.

## Iteration 4 — unrh-impl

Branch confirmed against `git log` — `autonomous/2026-09-17-daily`, HEAD `62d6576`, matches
`STATE-night.json`; working tree was clean at start.

Baseline before touching anything: `py -3.11 -m pytest hub/tests/test_a_held_agent_is_busy.py
hub/tests/test_a_task_nothing_will_move_holds_nobody.py -q` → **55 passed**, matching the approval's
measured number.

Read `an-unstaffed-review-names-its-holders`'s `design.md` Round 5, Round 6 and the measured Round 6
sections in full before starting, per `tasks.md`'s own instruction not to trust this file's line
numbers — R5 and R6 both record citation drift as a recurring failure of this specific change.
Built exactly tasks 1.1–1.4, no more:

- **1.1/1.2.** Added `Holding` and `AgentAvailability` (frozen dataclasses) and
  `_roster_availability(session, project_id)` to `hub/hub/scheduler.py`, reading the same four
  queries `_agents_that_are_free` always has (running turns, live loops, queued turns, live-status
  holdings) and returning one record per non-archived agent in name order. `_agents_that_are_free`
  is now that function's projection (`has_runner and not running and not held and not
  any(h.reachable for h in holdings)`) rather than its own independent query set — its docstring's
  reachability and D6 paragraphs kept verbatim, with a short new paragraph naming the
  re-expression.
  `resolve_reviewer` (rung 2's pool walk and the roster-held check at the bottom) now calls
  `_roster_availability` exactly once and derives both from that one read, rather than reading
  `_agents_that_are_free` and then a second `agents_held` plus `Agent.name` query — `grep -n "await
  _agents_that_are_free("` on the tree this was built against returned exactly `:348`, `:1263`,
  `:1444`, confirming R7's prediction (`_loop_flow_busy_reason` and `decide_firing` keep calling the
  free-list function unchanged; `resolve_reviewer` is the one that moved) over the stale
  `:298`/`:1298` numbers still sitting in `tasks.md`'s older text.
- **1.3/1.4.** Two tests added to `hub/tests/test_a_task_nothing_will_move_holds_nobody.py`:
  `test_the_roster_read_gives_each_agent_the_right_record` (six agents — no runner, running,
  reachable-holder, unreachable-holder, a sixth non-live-status holder, held, free — checking each
  record and that the pool is exactly the unreachable-holder, the non-live-status holder and the
  free agent) and `test_resolve_reviewer_reads_the_roster_once` (a `before_cursor_execute` listener
  counting `tasks.assignee` selects during one `resolve_reviewer` call).

  The task text's fixture as written would not have caught the `Task.status` mutation — every
  fixture task already carried a `LIVE_STATUSES` status, so dropping the filter changed nothing
  observable. Caught only by actually applying the mutation (not by reading the task text), and
  fixed by adding the sixth agent. Likewise the 1.4 fixture as first written (two free-ish agents)
  let rung 2 return before the roster-held read ever ran, so the mutation was invisible; restaged as
  a single-agent (author-only) project, which reaches rung 3 by the general rule.

  All four described mutations were applied to `hub/hub/scheduler.py` by hand, one at a time, and
  each was confirmed failing before being reverted (details and the exact assertions that failed are
  recorded beside each task in `tasks.md`, per the playbook's convention): dropping the `Task.status`
  filter, forcing `reachable = True` unconditionally (also failed
  `test_the_loopengine_shape_staffs_its_review`, as required), dropping `held` from the projection
  (also failed `test_a_held_agent_holding_no_task_is_not_free` — the approval text's own citation of
  this test's name, `test_a_held_agent_is_not_free`, is itself stale, noted in `tasks.md`), and
  making `resolve_reviewer` read `_agents_that_are_free` for rung 2 and a second, independent
  `_roster_availability` call for the roster-held check (two `tasks.assignee` selects instead of
  one).

Ran `py -3.11 -m ruff check --fix` (one import-order fix in the test file) and `py -3.11 -m black
--target-version py311` (reformatted `scheduler.py`'s two new/changed multi-line expressions) before
the final test pass. Final measurements: the two named guard files **55 → 57 passed**; the wider
regression set (`test_reviewer_ladder.py`, `test_flow_width.py`, `test_loop_busy_guard.py`,
`test_actor_aware_claimability.py`, `test_a_loop_does_not_staff_its_own_review.py`, plus the two
guard files) **124 passed**; `openspec validate an-unstaffed-review-names-its-holders --strict`
passes. Marked tasks 1.1–1.4 `[x]` in `tasks.md`, each with an observed-mutation note. Did not touch
groups 2, 5 or 6. Staged exactly the three files touched (no `git add -A`) and committed `f663898`,
pushed to `origin/autonomous/2026-09-17-daily`.

`current` advances to `unrh-drive`. No decisions logged for the operator — the two citation
corrections found (the caller line numbers, and the held-agent test's name) were already the kind of
thing this change's own rounds told a future round to expect and re-verify, not a new judgement call.

## Iteration 5 — unrh-drive

Branch confirmed against `git log` — `autonomous/2026-09-17-daily`, HEAD `50cb436`, matches
`STATE-night.json`; working tree was clean at start.

Baseline: `py -3.11 -m pytest hub/tests/test_a_held_agent_is_busy.py
hub/tests/test_a_task_nothing_will_move_holds_nobody.py -q` → **57 passed**, matching `unrh-impl`'s
own measurement.

**F388's trap fired a second time, harmlessly.** Starting a fresh drive Hub the first way —
`DATABASE_URL="..." AW_BOOTSTRAP_API_KEY="..." py -3.11 -m uvicorn hub.main:app --port 8092` from
`hub/`, exactly the inline-prefix shape `.claude/reference/hubs.md`'s own example uses — logged a
full `0001→0104` migration chain (which looks exactly like proof of a correctly-targeted fresh
database) but left no file at the intended profile path (`~/.agentweave/hub/profiles/drive0919c/`)
once the process was killed. That pointed straight at `F388` (filed today by the day window, at the
operator's instruction, after this same class of failure actually took the operator's real `:8000`
app down for three hours): a `DATABASE_URL` that fails to reach the child process falls through to
`~/.agentweave/hub/data/agentweave.db`, the operator's live database, silently.

Followed `DEAD-ENDS.md`'s own recorded practice before touching anything further: read-only
(`mode=ro`) inspection of that exact file, twice, minutes apart, with this session's own server
process already confirmed dead (`netstat`) in between. Both reads showed `alembic_version = 0103`,
unchanged, and no `subject_key` column — 0104 never reached it — while `event_logs`' row count grew
between the two reads on its own. That combination (schema frozen, activity still live) is only
consistent with the operator's actual `:8000` process (separately confirmed listening, PID 9940)
doing its own real work, not this session doing anything to it. No harm done, and confirmed no harm
done rather than assumed. Recorded as corroborating evidence under `F388`'s existing `DEAD-ENDS.md`
section rather than filed as a new finding — same known trap, second occurrence, not a new one.

Restarted correctly the second time: PowerShell `$env:DATABASE_URL`/`$env:AW_BOOTSTRAP_API_KEY` set
before `Start-Process`, and the profile directory pre-created with `New-Item -ItemType Directory
-Force` first (`sqlite3`/`aiosqlite` will not create a missing parent directory itself, so this is
also the cheap first check when a start looks wrong). Verified correct this time before proceeding:
the bootstrap key read back out of the new file's own `operator_credentials` table matched exactly
what was set, and the profile directory existed afterward.

**The live pass.** Built a fixture project, one runner, two agents (`author-agent`,
`reviewer-free`). Reaching `resolve_reviewer`'s rung-2 pool walk live turned out to need more than
this slice's scope: `decide_firing`'s own comment (`scheduler.py:1697`, design D5/F161) is explicit
that a documentless loop never calls `resolve_reviewer` at all — a spec-document-linked loop
(requirements, evidence) would be needed to reach it, which is a materially bigger fixture than a
confirmation pass warrants and sits in `alsn-impl`'s territory, not this one's. Said so rather than
building past scope or silently skipping the live pass altogether. Instead drove the other
live-reachable half of the same refactor: a real one-agent, documentless loop (`unrh-drive-loop`),
fired once for real against Haiku (per the standing directive), claimed its one pending task through
the refactored `_agents_that_are_free`/roster-availability projection, completed it, and then —
correctly per F161 — stayed at `completed` rather than moving to `under_review`, with the loop's own
`stall_reason` reading the expected, unchanged sentence: *"A loop does not review its own agent's
work, so approving is what puts it in the product."* Live confirmation that ordinary task-claiming
through the re-expressed `_agents_that_are_free` still works end to end.

Cleanup: job disabled; drive Hub killed both times by the exact `netstat`-confirmed listening PID
(never a blanket `taskkill /IM python.exe`, which is what turned `F388`'s first occurrence into a
real outage); profile directory and fixture project directory both removed. Operator's `:8000`
instance re-verified unchanged (`alembic_version` still `0103`) after teardown.

Recorded as `D-3, 2026-09-19` in `scripts/drive/FINDINGS.md`. Appended the second-occurrence account
and the working fix to `DEAD-ENDS.md`'s existing F388 section (append, not a fresh copy, per
`CLAUDE.md`'s own instruction for that file). No new finding filed. `current` advances to
`alsn-impl-1`.

**Decision for the operator:** none new — `F388`'s fix shape is already recorded as undecided in its
own finding text. Noting here only that it has now fired twice in one day, the second time caught
before any damage, which may bear on how the operator weighs its priority.

## Iteration 6 — alsn-impl-1

Branch confirmed against `git log` — `autonomous/2026-09-17-daily`, HEAD `e41b607`, matches
`STATE-night.json`; working tree was clean at start.

Baseline: `py -3.11 -m pytest hub/tests/test_a_loop_does_not_staff_its_own_review.py
hub/tests/test_loop_busy_guard.py hub/tests/test_flow_width.py
hub/tests/test_actor_aware_claimability.py -q` → **54 passed, 41.28s**, matching R4's own measured
number. `grep -n "await _agents_that_are_free(" hub/hub/scheduler.py` → exactly two (`:348` inside
`_loop_flow_busy_reason`, `:1510` inside `decide_firing`), confirming R2-7's post-group-1 prediction
of *"TWO, not three"* now that `an-unstaffed-review-names-its-holders` group 1 has landed and moved
`resolve_reviewer` onto `_roster_availability` directly. Task 0.1's check-not-gate proceeded on that
basis — no collision, as R2-7 argued.

Built exactly groups 0-4, no more, reading `design.md`'s D1/D2/D3/D5/D6/D7 sections in full (not
only `tasks.md`) before writing anything, per the round log's own warning that a defect surviving
into `tasks.md` is a fact about which artifact was actually read.

- **1.3/1.4/1.5 (D1/D2).** Added `_agents_a_loop_may_staff(session, loop)` to `hub/hub/scheduler.py`,
  directly after `_agents_that_are_free`: `loop.spec_document_id is None` → `[]`; otherwise the
  unfiltered pool. No `default_agent` parameter (R2-8). Pointed `decide_firing`'s `free = ...` read
  and `_loop_flow_busy_reason`'s pool check (task 3.3) at it. Extended the "Width is bounded by
  available agents" `continue` comment to name the new one-agent-permanently bound for a documentless
  loop. Left the default-agent branch (`:1667-1710` post-edit) untouched, confirmed by diff — it
  reads only `running`/`held_agents`/`taken`, never `free`.
- **3.3 (D3).** `_loop_flow_busy_reason`'s docstring rewritten: the paragraph that documented F128 as
  an open defect now names the closing change and states the guard's collapse to *busy, full stop*
  for a documentless loop.

Ten new tests in a new file, `hub/tests/test_a_loop_staffs_the_agent_it_names.py`, covering groups
1-4 (1.1, 1.2, 1.6, 2.1, 2.2, 3.1, 3.2, 3.4, 3.5, 4.2), plus confirming the existing
`test_a_loops_wedged_review_still_recovers` still covers 4.1 unmodified (R4-6's own instruction:
*"run the mutation against that one, and add a new test only if it does not already fail under
it"* — it does not, so no new 4.1 test was written). Every load-bearing mutation named in `tasks.md`
was hand-applied to `scheduler.py`, run, observed failing, and reverted, in the same one-at-a-time
discipline `unrh-impl` established:

- 1.1/3.1/3.4/3.5's shared mutation (deleting `_agents_a_loop_may_staff`'s `spec_document_id is
  None` guard entirely) — 5 tests failed together (1.1, 1.6, 3.1, 3.4, 3.5), confirming all four are
  sensitive to the D2 filter existing at all.
- 2.1's mutation (applying the filter to the resumption arm) — 1 test failed
  (`test_a_documentless_loop_resumes_a_sibling_while_staffing_its_own_agent`), confirming D6.
- 3.1/3.4's mutation (reverting `_loop_flow_busy_reason`'s pool read to `_agents_that_are_free`) — 2
  tests failed, confirming D3.
- **1.6's own named mutation — `_agents_a_loop_may_staff` returning `[default_agent]` instead of
  `[]` — was measured inert against its own fixture.** Applied by hand (`session.get(AIJob,
  loop.job_id)` then `[job.agent]`), all 10 tests still passed. Traced rather than shrugged off:
  under "own agent free," `free` is only consulted once `default_taken` is already `True`, which by
  construction means the default agent is already in `taken` — so a pool of `[gamma]` and a pool of
  `[]` filter to the same candidate set every time. Confirmed the *actual* falsifier of that same
  literal mutation is 1.1's shape instead (default agent busy, not yet `taken`, when `free` is
  read) — applied that variant separately and it failed as expected. Recorded in `tasks.md` beside
  1.6 rather than silently building around it, matching this change's own round-discipline lesson
  that an artifact's claim can be wrong while the surrounding argument is right.

Task 4.2 (the `run_divergence.py:441` silent-reviewer path) needed its own fixture: reused
`test_review_divergence.py`'s `_review_run_that_said_nothing` helper against a task whose `loop_id`
names a documentless loop, confirming `auditor` is still resolved project-wide and `resolve_reviewer`
is untouched by this group, per D5. No mutation was applied for 4.1/4.2 — this group made no edit to
`resolve_reviewer` or `run_divergence.py` for a mutation to test against (task 4.3's own point).

Final measurements: the four baseline files plus the new file — **54 → 64 passed**, 48.23s. `ruff
check` and `black --check --target-version py311` clean on both touched files (`black` reformatted
the new test file once, re-ran after). `openspec validate a-loop-staffs-the-agent-it-names --strict`
passes. Marked tasks 0.1-0.3, 1.1-1.6, 2.1-2.2, 3.1-3.5, 4.1-4.3 `[x]` in `tasks.md`, each with what
was measured — including two stale-reference corrections found along the way (task 2.2's `:2758-
2772` line number, and task 4.3's `_agents_that_are_free` naming what is now `_roster_availability`
since group 1 landed). Staged exactly the three files touched (no `git add -A`) and committed
`831ac16` (amended once, from `2260e4b`, to add the `Co-Authored-By` trailer omitted on the first
attempt), pushed to `origin/autonomous/2026-09-17-daily`.

`current` advances to `alsn-impl-2` (group 6, "what must not move" — the full regression re-run,
`ruff`/`black`/`mypy`, and tasks 6.6/6.7's two documentation corrections). Groups 5 (operator-facing
wording, explicitly out of scope for this window per `APPROVALS.md`'s `ORDER` line), 7 (drive) and 8
(closeout) remain separate, later queue items. No decisions logged for the operator — the two stale
references found were already the kind of thing this change's own round log told a future round to
expect, not a new judgement call.

## Iteration 7 — reconciliation (alsn-impl-2 was already done)

This process started fresh and found `git log`'s HEAD (`adca56b`) one commit ahead of what
`STATE-night.json` and this log recorded — `alsn-impl-2` itself (tasks 6.1-6.3, 6.6, 6.7) was
already built and committed, but the prior process died before writing the log entry or advancing
`current`/`queue`. Per step 2 of this window's own instructions, reconciling here rather than
redoing or skipping the work.

Did not trust the commit message alone. Independently re-ran
`py -3.11 -m pytest hub/tests/test_a_loop_does_not_staff_its_own_review.py
hub/tests/test_loop_busy_guard.py hub/tests/test_flow_width.py
hub/tests/test_actor_aware_claimability.py hub/tests/test_a_loop_staffs_the_agent_it_names.py -q`
→ **64 passed, 52.87s**, matching the commit's own claim and `alsn-impl-1`'s baseline (54 + 10 new).
Re-ran `ruff check` on the three files the commit touched (`test_flow_width.py`,
`test_questions.py`, plus `scheduler.py` as a sanity check since group 6 is "what must not move") →
all checks passed. Read `test_flow_width.py:611-628` directly: the docstring now states D2 retires a
documentless loop's width in full rather than claiming it survives, matching task 6.6's description
exactly, with the assertions left untouched. Read `tasks.md`'s 6.7 block: a **confirmed, no-edit**
outcome — D8b explicitly rejects modifying `spec.md:1242`, and the delta cites it by name instead;
verified both `:1242`'s wording and the delta's cross-reference are present and read as one rule for
the row they share, matching the task's own instruction not to over-claim `:1242`'s authority for
the silent-reviewer row it doesn't cover. All five group-6 tasks (6.1, 6.2, 6.3, 6.6, 6.7) are `[x]`
in `tasks.md` with measurements recorded beside each, consistent with the commit.

Working tree was clean (`git status` — nothing to commit) both before and after this verification,
since no new edits were needed. `queue`'s `alsn-impl-2` entry marked `done`; `current` advances to
`alsn-drive` (group 7 — drive the change against a fresh Hub; do not build §5). No decisions logged
for the operator — this was a verify-and-reconcile step, not a new judgement call.

# 2026-09-20 night window

Newest entry at the BOTTOM. Playbook: `.claude/loops/night-window.md`.

Armed 2026-09-20T22:55:02+01:00 on `autonomous/2026-09-17-daily` (parent master@802a8c7), model opus.

## Iteration 1 — compose

**Branch confirmed** `autonomous/2026-09-17-daily`, tree clean, matches STATE. HEAD `6c934d7`.

**Backlog page:** `py -3.11 scripts/backlog_page.py --check` → current, nothing moved since last
generation. Regenerated anyway for the warnings block: 12 findings carry no severity (0 open among
them), and F53/F352 read "open" with a status line that claims a commit fixed something — not
re-checked here, not this window's item.

**`spec-queue/APPROVALS.md` `## 2026-09-20`** is today's section, written in session with the
operator (the day window hit the weekly usage limit at 14:15 and may not have reached its own
review page). It carries an explicit `ORDER:` and an `APPROVED` row:
`an-archived-agent-holds-nothing-and-is-offered-nowhere`, all groups including the new 2b and
group 5, explicitly **no UI, no bundle refresh**. That is tonight's queue, verbatim, per the
playbook's `ORDER:` rule. If it finishes with time left, the section's own fallback is
`a-loop-staffs-the-agent-it-names` group 6.2-REDO (re-run `pytest hub/tests/ -q` in full and write
the real count — 6.2's existing tick cites a log entry that was never written). Section 5 of that
change stays NOT approved.

**CI verdict for the inherited sha.** `gh run list --branch autonomous/2026-09-17-daily --limit 20`:
`parent_sha` 802a8c7 → `success`. HEAD `6c934d7`'s run is still in progress (no conclusion). The one
failure since (`28ed8f1`) is `hub-test` → `sqlite3.OperationalError: database is locked` in
`test_flow_fires_a_review_turn.py` — F292, the known intermittent the merge gate already re-runs
around; not queued here. No red streak: 802a8c7 success, dfa8ee4 success, 28ed8f1 F292-failure,
interleaved with unrelated successes/failures on now-superseded shas earlier in the evening — not
three-plus consecutive. Separately, **F394 (A)** was filed by the prior interactive session
tonight: `hub-test` has *hung* (not failed) for 5-6h on three of the last five `master` runs,
including current `master` HEAD `45d4576` (no CI verdict at all). That is a real open severity-A
finding, already ledgered, with a named cheap fix (`--timeout=120 --timeout-method=thread` on
`ci.yml:136`); not re-triaged here, it is exactly what the backlog step below would surface if this
window reaches findings before groups 1-6 exhaust the time budget. **Did not run the full local
suite in this compose iteration** — 15-47 minutes measured historically, and CI's per-sha verdict
for the parent is `success` with no local-breaking commit since; group 6 (task 6.1) runs
`pytest hub/tests/ -v` for real before this change is called done, which is the gate that matters.

**Queue written** to `STATE-night.json`: `group1-impl` .. `group5-impl` (tasks.md's five build
groups, one queue item per group, each sized to one firing), `group-verify-drive` (6.1-6.4: full
suite, lint, a real drive on a fresh port), `change-archive` (6.5: validate --strict, sync-specs,
archive-change — this is what retires F185/F181/F390/F391's status lines), then `i-6.2-redo` as the
time-permitting fallback the approval section names. `current` → `group1-impl`.

## Iteration 2 — group1-impl

**Branch confirmed** `autonomous/2026-09-17-daily`, tree clean at start, matched STATE-night.json
(HEAD `c3cb10e`, `current: group1-impl`).

Built tasks 1.1-1.8 of `an-archived-agent-holds-nothing-and-is-offered-nowhere` (F185 group 1):

- `hub/hub/agent_lifecycle.py` `archive()` now sets `agent.charter_id = None`; docstring names why
  and that `runner_id` stays bound on purpose (D3). `unarchive()` docstring states the release is
  permanent.
- `hub/hub/api/v1/charters.py` — comment above the `bound` select in `delete_charter` states the
  invariant (an archived agent can't hold a charter) and names `agent_lifecycle.archive` as what
  maintains it. No query change.
- `hub/hub/api/v1/agents.py` `patch_agent` — the `charter_id` branch now refuses (409) binding a
  charter to an archived agent, `charter_id: null` still clears unconditionally. Comment on the
  adjacent `runner_id` branch states the deliberate asymmetry and points at D3.
- Six new tests: `test_agent_archival.py::test_archiving_releases_the_charter_but_not_the_runner`,
  `test_charters_api.py::test_deleting_a_charter_bound_to_an_archived_agent_succeeds`,
  `test_charters_api.py::test_no_archived_agent_row_ever_holds_a_charter`,
  `test_agents.py::test_patch_agent_refuses_binding_a_charter_to_an_archived_agent`.

**Mutation checks, each applied by hand and reverted:** deleting the `charter_id = None` line in
`archive()` failed 1.3's first assertion (charter_id) and 1.4's DELETE assertion (409 instead of
204), while leaving 1.3's runner_id assertion green — confirms the test exercises the release, not
a coincidence. Dropping the `agent_row.lifecycle == "archived"` guard in `patch_agent` failed 1.7's
and 1.8's refusal assertions (200 instead of 409) while leaving the null-clear and open-agent cases
green — confirms the guard is what the tests check, and that clearing/open-agent behaviour is
untouched by it.

**Verification:** `py -3.11 -m pytest hub/tests/test_agent_archival.py hub/tests/test_charters_api.py
hub/tests/test_agents.py -q` → 37 passed (clean, after mutations reverted).
`py -3.11 -m ruff check` and `py -3.11 -m black --check --target-version py311` on the six touched
files → both clean. Did not run the full `hub/tests/` suite this iteration — group 6 (task 6.1) is
where that gate lives; scoped to the three files this group's tasks name.

Ticked 1.1-1.8 in `tasks.md` with the mutation evidence beside each. Group 1 is done.
Committed and pushed. `current`/`next_action` advance to `group2-impl` (tasks 2.1-2.5).

## Iteration 3 — group2-impl

**Branch confirmed** `autonomous/2026-09-17-daily`, tree clean at start, matched STATE-night.json
(HEAD `b2a852c`, `current: group2-impl`).

Built tasks 2.1-2.5 of `an-archived-agent-holds-nothing-and-is-offered-nowhere` (F185 group 2):

- `hub/hub/api/v1/agents.py` `archive_agent` — `agent_row.charter_id` is read into
  `released_charter_id` **before** `archive_agent_row` runs (the release happens in-process, so
  reading after would always see `None` — this is exactly 2.3's mutation). Response now returns
  `charter_id: None`, `released_charter_id`, and `message` naming the released charter by its
  `Charter.name` (resolved via `session.get`) — `message` is omitted entirely when nothing was
  bound, not sent as `null`, matching "no sentence when nothing was bound".
- `unarchive_agent` — response now always carries `charter_id: agent_row.charter_id` (always
  `None` by the time unarchive runs, since archive already released it and `patch_agent`'s 1.6
  guard blocks re-binding while archived) and the exact R2 standing sentence, unconditionally.
- New test `test_agent_archival.py::test_archive_and_unarchive_responses_state_the_bindings_fate`:
  archive of a chartered agent names the charter and clears `charter_id`; unarchive returns the
  standing sentence and does not contain "before this agent's next turn"; archive of a never-
  chartered agent has `released_charter_id: None` and **no** `message` key at all; unarchive of
  that agent still gets the same standing sentence.

**Mutation, applied by hand and reverted:** moved the `released_charter_id = agent_row.charter_id`
read to after `archive_agent_row(agent_row)` — the "names the charter" assertion failed
(`None == 'charter-5e819630f8ca'`), confirming the test exercises the pre-release capture order
task 2.1 requires, not a coincidence. Reverted; file green again.

**Task 2.4 re-confirmed, not assumed:** re-read `hub/ui/src/api/agents.ts:209-238` this iteration
— `useArchiveAgent`'s `mutationFn` is still typed `{ name: string; lifecycle: string }` and its
`onSuccess` still takes no argument, so both new fields and the sentence are API facts nothing
renders yet, exactly as D9 says. No file under `hub/ui/src` touched; `make ui` not run.

**Task 2.5, stated per its own instruction:** this group is a half-discharge of the operator's
rider. The API now says a released charter's fate on both archive and unarchive; the app (
`useArchiveAgent` → `AgentSettingsPage.tsx`) still shows nothing, because rendering it is a
`hub/ui/src` change and this change carries no bundle refresh. **No finding's `**Status:**` line is
touched by this commit** — F185 stays open until group 6's drive exercises the whole change
end-to-end through HTTP.

**Verification:** `py -3.11 -m pytest hub/tests/test_agent_archival.py hub/tests/test_charters_api.py
hub/tests/test_agents.py -q` → 38 passed (was 37 before this iteration's one new test). `ruff check`
and `black --check --target-version py311` on the two touched Python files → both clean. Did not run
the full `hub/tests/` suite — group 6 (task 6.1) is that gate; scoped to the files this group's
tasks name, same as iteration 2.

Ticked 2.1-2.5 in `tasks.md` with the mutation evidence and the half-discharge statement beside
2.5. Group 2 is done. Committed and pushed. `current`/`next_action` advance to `group2b-impl`
(tasks 2b.1-2b.7 — the `agent_archived`/`agent_unarchived` event, server-side only).

## Iteration 4 — group2b-impl

**Branch confirmed** `autonomous/2026-09-17-daily`, tree clean at start, matched STATE-night.json
(HEAD `973bb5d`, `current: group2b-impl`).

Built tasks 2b.1-2b.7 of `an-archived-agent-holds-nothing-and-is-offered-nowhere` (F391, group 2b):

- `hub/hub/api/v1/agents.py` `archive_agent` — after `session.commit()`/`session.refresh`, builds
  `event_payload` (`agent`, `lifecycle`, `released_charter_id`) from the same pre-release capture
  task 2.1 already takes, then `persist_event(..., "agent_archived", event_payload, agent=...)`
  followed by `sse_manager.broadcast(..., "agent_archived", event_payload)` — same payload object,
  same order as `agent_created` (`:697-698`). Unconditional: no guard around the event calls, so an
  agent archived holding nothing still records `released_charter_id: null` (2b.3).
- `unarchive_agent` — same two calls with `"agent_unarchived"` and a payload of only `agent` and
  `lifecycle`; no `released_charter_id` key at all, matching D2 (reopening does not restore the
  binding).
- Two new tests in `test_agent_archival.py`: `test_archiving_and_unarchiving_persist_events`
  (asserts against `GET /events/history` — empty before, a heartbeat positive control lands, one
  `agent_archived` naming the released charter, one `agent_unarchived` with no
  `released_charter_id` key) and `test_archiving_a_never_chartered_agent_still_records_an_event`
  (2b.3: bare archive still records the event with `released_charter_id: null`).

**Mutation, applied by hand and reverted, both observed distinct:** deleting the `persist_event`
call in `archive_agent` failed `len(archived_events) == 1` (`0 == 1`) while the heartbeat positive
control still passed, confirming `/events/history` itself works and the gap is real. Deleting the
`released_charter_id` key from `event_payload` failed differently: `KeyError:
'released_charter_id'` on the data-key assertion, not a count mismatch. Both reverted; file green
again.

**Verification:** `py -3.11 -m pytest hub/tests/test_agent_archival.py hub/tests/test_charters_api.py
hub/tests/test_agents.py -q` → 40 passed (was 38 before this iteration's two new tests). `ruff
check` clean on `agents.py`; `black --check --target-version py311` initially flagged the test file
(whitespace only), reformatted with `black --target-version py311`, re-verified clean and green (17
passed on the file alone). Confirmed via `git diff --stat` that only `hub/hub/api/v1/agents.py` and
`hub/tests/test_agent_archival.py` changed — no `hub/ui/src` file touched, `make ui` not run (2b.7).

Ticked 2b.1-2b.7 in `tasks.md` with the mutation evidence. Set F391 `**Status:** fixed 06161a7` in
`scripts/drive/FINDINGS.md`, in a second commit (the sha of the code+test commit could not be known
inside that same commit), stating explicitly that the display half is untouched and naming
`useSSE.ts:31`/`:460` as where it would live — per 2b.6's instruction. Committed
(`06161a7` code+test+tasks.md, `89277d0` the ledger edit) and pushed. `current`/`next_action`
advance to `group3-impl` (migration 0105, tasks 3.1-3.5).

## Iteration 5 — group3-impl

**Branch confirmed** `autonomous/2026-09-17-daily`, tree clean at start, matched STATE-night.json
(HEAD `78f546b`, `current: group3-impl`).

Built tasks 3.1-3.5 of `an-archived-agent-holds-nothing-and-is-offered-nowhere` (F185, group 3 —
the migration):

- New `hub/hub/migrations/versions/0105_clear_archived_agent_charter_bindings.py`, down-revision
  `0104` — `UPDATE agents SET charter_id = NULL WHERE lifecycle = 'archived'`, guarded for a
  missing `agents` table via `_has_table` exactly as `0033`/`0104` do. `downgrade()` is a
  documented one-line no-op: which rows lost a binding is not recorded, so there is nothing to
  restore.
- Confirmed rather than assumed (3.3): re-read `hub/hub/db/models.py:230-232` —
  `Agent.charter_id` was already `nullable=True`. No `models.py` change.
- Bumped both head assertions (3.4): `HEAD_REVISION = "0105"` in `test_migrations.py`,
  `assert version == "0105"` in `test_project_persistence.py`. Grepped both files for any other
  literal `"0104"` first — none found, so these were the only two sites.
- Two new tests in `test_migrations.py`:
  `test_migration_0105_clears_an_archived_agents_charter_but_leaves_an_open_ones` (3.5's data
  test — seeds one archived agent and one open agent, both holding a charter binding, at
  revision `0104` via raw SQL, upgrades to `0105`, asserts the archived row's `charter_id` is
  NULL and the open row's is untouched) and `test_migration_0105_is_guarded_when_agents_does_not_exist`
  (3.1's guard, matching the house pattern every other guarded migration in this file already
  carries — not asked for by name in the task, but the established shape for every prior guarded
  migration's own test coverage).

**Mutation, applied by hand and reverted:** dropped `WHERE lifecycle = 'archived'` from the
migration's `UPDATE`. The open agent's `charter_id` assertion failed (`None == 'charter-2'`,
i.e. its binding was wrongly cleared too); the archived agent's assertion still passed. Reverted
and diffed byte-identical against a pre-mutation copy (`diff` clean, no stray whitespace); file
green again.

**Verification:** `py -3.11 -m pytest hub/tests/test_migrations.py hub/tests/test_project_persistence.py -q`
→ **103 passed, 1 skipped** (the whole file, not scoped to `-k 0105` — task 3.4 touches a head
assertion every other test in both files depends on, so this iteration ran the full files rather
than the group-1-through-2b habit of scoping to the touched test module alone). `ruff check` on
the new migration file and both test files → clean. `black --check --target-version py311`
initially flagged the new migration file and `test_migrations.py` (both whitespace-only);
reformatted with `black --target-version py311`, re-verified clean, re-ran the `0105`-scoped
tests after reformatting to confirm the reformat changed nothing behavioural (3 passed). Did not
run the full `hub/tests/` suite — group 6 (task 6.1) is that gate, same reasoning as every prior
iteration this window.

`git diff --stat` shows exactly `hub/tests/test_migrations.py`, `hub/tests/test_project_persistence.py`,
`openspec/changes/.../tasks.md` modified and the one new migration file added — no `hub/ui/src`
file touched, `make ui` not run (this group has no UI surface).

Ticked 3.1-3.5 in `tasks.md` with the mutation evidence. Group 3 is done. No finding's `**Status:**`
line touched this iteration — F185's migration half lands, but group 6's drive is still what closes
the finding. Committed and pushed. `current`/`next_action` advance to `group4-impl` (tasks 4.1-4.5 —
launchability applies the roster's lifecycle filter, F181).

## Iteration 6 — group4-impl

**Branch confirmed** `autonomous/2026-09-17-daily`, tree clean at start, matched STATE-night.json
(HEAD `25167a8`, `current: group4-impl`).

Built tasks 4.1-4.5 of `an-archived-agent-holds-nothing-and-is-offered-nowhere` (F181, group 4 —
launchability applies the roster's lifecycle filter):

- `hub/hub/api/v1/agents.py` `get_agents_launchability` — added
  `lifecycle: Literal["open", "archived", "all"] = Query("open")`, same position and default as
  `list_agents`. The filter is applied to `session_agents_meta` right after the `db_agents`
  `setdefault` loop and before the per-name probe loop, using the identical "no row counts as
  open" comprehension `list_agents` already uses (`agents.py:359-369`). Docstring gained one
  paragraph naming `list_agents` as the source of the reasoning rather than restating it — 4.2
  asked for exactly one paragraph, not a copy of the other function's comment.
- Two new tests in `hub/tests/test_launchability.py` (this module, not `test_agents.py` — every
  other test of this endpoint already lives here):
  `test_launchability_lifecycle_filter_matches_the_roster` (4.3: default omits an archived
  DB-registered agent, `?lifecycle=archived` returns it, `?lifecycle=all` returns both, and a
  session-config-only name with no `Agent` row at all is excluded from the archived filter) and
  `test_every_agent_the_default_probe_calls_runnable_is_not_refused_as_archived` (4.4: binds two
  agents to real runners with `shutil.which` mocked present, archives one, then loops over every
  name the *default* probe calls `runnable: true` and POSTs `/agent/trigger` for each with
  `PtySession.spawn` mocked — asserts none come back `409`/"archived").

**Mutation, applied by hand and reverted, both tests caught it (for the reason 4.3 names):**
moved the filter onto the `Agent` query (`agent_q = agent_q.where(Agent.lifecycle == lifecycle)`)
instead of post-filtering `session_agents_meta`. 4.3's test failed on the session-config-only case
exactly as prescribed: `config-only` (present only in session config, no DB row) leaked into
`?lifecycle=archived`'s response (`AssertionError: 'config-only' not in {...}`) — the query-only
filter never removes a name that arrived through session config rather than through `db_agents`.
4.4's test failed the same way for the same underlying reason: its `archived-runnable` agent is
*also* present in session config (it has to be, to get a bound runner via the `bind_runner`
fixture pattern used throughout this suite), so it likewise leaked into the mutated default probe
as `runnable: true`, and the subsequent `POST /agent/trigger` for it came back `409` — failing
`resp.status_code != 409`. Reverted both; `hub/tests/test_launchability.py` green again (44
passed).

**Task 4.5 re-confirmed, not assumed:** `grep -rn "useAgentLaunchability" hub/ui/src` finds the
hook's own definition (`api/agents.ts:376`) and eleven `vi.mock(...)` stubs across
`__tests__/*.test.tsx` — no import or call from any real component. Zero real call sites still
holds; no `hub/ui/src` file touched, `make ui` not run.

**Verification:** `py -3.11 -m pytest hub/tests/test_launchability.py hub/tests/test_agent_archival.py
hub/tests/test_agents.py hub/tests/test_agent_trigger.py -q` → **124 passed** (a `RuntimeError:
Event loop is closed` appears only in a background-thread teardown warning from
`test_agent_trigger.py`'s PTY mocking, pre-existing and not from this iteration's changes — no
test failure). `ruff check` and `black --check --target-version py311` on the two touched Python
files → both clean. Did not run the full `hub/tests/` suite — group 6 (task 6.1) is that gate,
same reasoning as every prior iteration this window.

`git diff --stat` shows exactly `hub/hub/api/v1/agents.py`, `hub/tests/test_launchability.py`, and
`openspec/changes/.../tasks.md` modified — no `hub/ui/src` file touched, confirming 4.5.

Ticked 4.1-4.5 in `tasks.md` with the mutation evidence and the 4.5 re-confirmation. Group 4 is
done. No finding's `**Status:**` line touched this iteration — F181 stays open until group 6's
drive exercises the whole change end-to-end through HTTP. Committed and pushed.
`current`/`next_action` advance to `group5-impl` (tasks 5.1-5.3 — the third site, `runners.py`
`delete_runner`, F390).

## Iteration 7 — group5-impl

**Branch confirmed** `autonomous/2026-09-17-daily`, tree clean at start, matched STATE-night.json
(HEAD `2bf4816`, `current: group5-impl`).

Built tasks 5.1-5.3 of `an-archived-agent-holds-nothing-and-is-offered-nowhere` (F390, group 5 —
the third site, cut clean of groups 1-4 by design):

- `hub/hub/api/v1/runners.py` `delete_runner` — the bound-holders select now pulls `Agent.name`
  **and** `Agent.lifecycle`. Each archived holder gets `" (archived)"` appended inline; the
  trailing clause ("an archived agent is listed under Agents with the archived filter") is appended
  only when at least one holder is archived — an open-only refusal is byte-identical to today's
  sentence, matching the task's "keep today's sentence unchanged" instruction.
- Three cases in `hub/tests/test_runners_api.py`:
  `test_delete_runner_bound_to_only_archived_agent_names_it_archived`,
  `test_delete_runner_bound_to_mixed_holders_qualifies_only_the_archived_one` (asserts the open
  holder's name appears unqualified while the archived holder's carries `(archived)`), and the
  pre-existing `test_delete_runner_bound_to_agent_is_refused` extended with an assertion that
  `(archived)` never appears in an open-only refusal (today's-sentence case, per 5.2). A shared
  `_make_runner_bound_agent` helper factors the create-runner/register-agent/bind-runner sequence
  the three tests share.

**Mutation, applied by hand and reverted:** replaced the select with `Agent.name` alone and
synthesized `lifecycle="open"` for every row (simulating "drop the lifecycle column"). Result:
the only-archived and mixed tests failed
(`AssertionError: 'mixed-archived (archived)' not in 'Runner is bound to agent(s): mixed-archived,
mixed-open. Unbind before deleting.'`), while the pre-existing only-open test
(`test_delete_runner_bound_to_agent_is_refused`) stayed green — confirming the lifecycle column,
not some other signal, is what the qualification depends on. Reverted; file green again.

**Verification:** `py -3.11 -m pytest hub/tests/test_runners_api.py -q` → 23 passed (was 21 before
this iteration's two new tests). `ruff check` and `black --check --target-version py311` on both
touched files → clean.

Ticked 5.1-5.3 in `tasks.md` with the mutation evidence. Set F390 `**Status:** fixed 861429d` in
`scripts/drive/FINDINGS.md` — rewrote the entry in place (Status line first, design-history R2/R3
narrative kept below it, the code snippet re-labelled "Pre-fix" since it now reads as history, not
current code); stated explicitly that this was reproduced through HTTP for the first time via the
two new tests, not by reading, and that the underlying `runner_id`-survives-archival design choice
(D3/D4) is unchanged — the fix answers by naming, not releasing. Two commits, same as group 2b's
pattern: `861429d` (code+tests+tasks.md — the sha task 5.3 needed did not exist until this commit
landed) then `efb0053` (the ledger edit alone). Pushed. `current`/`next_action` advance to
`group-verify-drive` (tasks 6.1-6.4 — full suite, lint, and a real drive on a fresh port).

## Iteration 8 — group-verify-drive

**Branch confirmed** `autonomous/2026-09-17-daily`, tree clean at start, matched STATE-night.json
(HEAD `a2cdb72`, `current: group-verify-drive`).

Ran tasks 6.1-6.4 of `an-archived-agent-holds-nothing-and-is-offered-nowhere`:

- **6.1:** `py -3.11 -m pytest hub/tests/ -q` -> **4474 passed, 86 skipped**, 2841.21s. Two
  `RuntimeError: Event loop is closed` traces are `aiosqlite` background-thread teardown noise
  (pre-existing across this whole window's prior iterations, not new failures). One `UserWarning`
  worth flagging for a future iteration, not this one: `test_surface_ceilings.py`'s
  `MISREPORT_CEILING` ratchet reports the live count dropped to 49 from a ceiling of 52 — the
  constant should be lowered to hold the new floor, but that is a separate, unscoped chore.
- **6.2:** `ruff check src/ hub/ tests/` clean, `black --check --target-version py311 src/ hub/hub/
  hub/tests/ tests/` clean (582 files), `mypy src/` clean (22 files).
- **6.3:** Delegated the actual HTTP/DB drive to a subagent (research first — endpoint paths,
  request bodies, and the 0105 migration's exact SQL — then execution) rather than driving by hand,
  given the number of sequential steps (start a Hub, bootstrap an operator key, create a project,
  register an agent, bind/archive/re-bind/unarchive a charter, probe launchability twice, trigger,
  then a second independent alembic seed-and-upgrade). Ran on port **8025** (not 8000/8010),
  `DATABASE_URL` pointed at a fresh `testbed/drive0921/agentweave.db`, `AW_BOOTSTRAP_API_KEY` set
  to a known value so the operator token didn't need to be dug out of logs. Project
  `proj-b99d375d4baf`, agent `archive-test-agent`.

  F185: bind charter -> archive returns
  `{"lifecycle":"archived","charter_id":null,"released_charter_id":"charter-76a2942959d2","message":"Archiving released this agent's charter, \"test-charter\"."}`
  -> re-bind the same charter while archived -> `409 "Agent 'archive-test-agent' is archived and
  cannot hold a charter. Unarchive it first."` -> unarchive ->
  `{"lifecycle":"open","charter_id":null,"message":"No charter is bound. Archiving releases an
  agent's charter and unarchiving does not restore it. An agent with no charter still runs -- bind
  one only if this agent should have one."}` (D8's message is static/unconditional, confirmed by
  reading the route, not just by this one call).

  F181: re-archived the agent, then default `GET /agents/launchability` -> `{"agents":{}}`
  (archived agent correctly absent) and `POST /agent/trigger` -> `409 "archive-test-agent is
  archived and cannot be triggered. Unarchive it first."` — probe and trigger agree, which is
  exactly task 4.4's assertion. The subagent's first pass flagged `?lifecycle=archived` reporting
  `runnable: true` as a "deviation" — checked this myself against `tasks.md` 4.4 and the
  `get_agents_launchability` docstring: the finding, the fix, and the docstring's own wording
  ("an archived agent excluded there must not still report `runnable: true` here") are all scoped
  to the **default** probe agreeing with trigger, not to what a non-default `lifecycle=` view
  reports. Not a regression, not in scope — recorded as a clarifying note on F181 rather than a new
  finding, so the next reader doesn't re-open the same question.

  Migration: seeded `testbed/drive0921/migration_test.db` at revision `0104` (schema inspected via
  Python's `sqlite3` module — the `sqlite3` CLI binary is not on this machine), hand-inserted one
  `agents` row with `lifecycle='archived', charter_id='charter-fixture-1'` and one with
  `lifecycle='open', charter_id='charter-fixture-2'`, then `alembic upgrade head` (applied exactly
  `0104 -> 0105`). Post-migration: archived row's `charter_id` is `NULL`, open row's is unchanged —
  exact match to the migration's `UPDATE agents SET charter_id = NULL WHERE lifecycle = 'archived'`.

  No real agent turn fired (every 409 returned before any spawn path). Hub process stopped, port
  8025 confirmed free (no LISTENING socket, only stale TIME_WAIT). `testbed/drive0921/` artifacts
  (both sqlite files, `uvicorn.log`, `instance_identity.json`, `proj/`) left on disk as evidence,
  untracked (matches `testbed/README.md`'s throwaway convention).

- **6.4:** `scripts/drive/FINDINGS.md` — F185 set `**Status:** fixed 10cf98e`, F181 set
  `**Status:** fixed 2bf4816`, both rewritten in place with the drive's evidence, F181's entry
  carrying the default-vs-non-default scope note above so it isn't mistaken for a live gap later.
  F390 (`861429d`, group 5) was already fixed. Ticked 6.1-6.4 in `tasks.md`.

Committed and pushed. `current`/`next_action` advance to `change-archive` (task 6.5 —
`openspec validate --strict`, then `openspec-sync-specs`/`openspec-archive-change` via the repo's
own skills, retiring F185/F181/F390/F391's open-severity listing per the archiving rule).

## Iteration 9 — change-archive (task 6.5)

**Branch confirmed** `autonomous/2026-09-17-daily`, tree clean at start, HEAD `61dd95c`, matched
STATE-night.json (`current: change-archive`).

Ran task 6.5 of `an-archived-agent-holds-nothing-and-is-offered-nowhere`:

- `openspec validate --strict an-archived-agent-holds-nothing-and-is-offered-nowhere` → **valid**,
  nothing flagged.
- Confirmed all 38 tasks in `tasks.md` were ticked (`grep -c '^\- \[x\]'` → 38, `'^\- \[ \]'` → 0)
  before archiving — 6.5 itself was the only open one, ticked with the evidence below.
- **openspec-sync-specs**, agent-driven merge into the three main specs the change's delta specs
  touched:
  - `openspec/specs/agent-charter/spec.md` — added the new requirement "Deleting a held charter is
    refused, and every holder it names is one the operator can find" (three scenarios), appended
    after the existing "starter set demonstrates a non-software separation of duties" requirement.
  - `openspec/specs/agent-configuration/spec.md` — merged the modified "An agent is archived rather
    than deleted" requirement: added "in any report of which agents can be launched" to the
    not-offered clause, and inserted the charter-release, pre-existing-agent, archiving/unarchiving
    wording, event/announcement, re-bind-refusal, and runner-binding-survives paragraphs plus their
    eleven new scenarios, preserving every scenario already present.
  - `openspec/specs/runner-registry/spec.md` — merged the modified "Launchability reports the
    runner that would actually be spawned" requirement (three new paragraphs, three new scenarios
    on top of the three existing ones) and added the new requirement "A refusal that names a
    runner's holders names where to reach each one" (three scenarios).
  - Read both delta and main spec for each of the three capabilities before editing; nothing
    pre-existing was removed, only new requirement/scenario text was added or merged in.
- **openspec-archive-change**: moved
  `openspec/changes/an-archived-agent-holds-nothing-and-is-offered-nowhere/` to
  `openspec/changes/archive/2026-09-21-an-archived-agent-holds-nothing-and-is-offered-nowhere/`
  (dated by today, 2026-09-21). All artifacts were `done` per `openspec status --json`; no
  incomplete-task warning applied since 6.5 was ticked first.
- **FINDINGS.md ledger check for F185/F181/F390/F391** per night-window.md's archiving rule: read
  all four sections in full. Each already carries a clean `**Status:** fixed <sha>` line (F181
  `2bf4816`, F185 `10cf98e`, F390 `861429d`, F391 `06161a7`) with no stray "open" wording inside its
  own section. Searched the whole file for any other place these four numbers appear: two hits
  (lines 545, 582) are historical narrative about a 2026-09-06 classifier-script defect, not a live
  open-severity listing; the file's actual "open severity-A" tracking sections predate all four
  findings (they're B/C, never listed there) and its one "open below severity A" enumeration is from
  2026-08-27, before any of the four existed. **Conclusion: no ledger edit was needed beyond what
  group-verify-drive (iteration 8) already made** — this is stated explicitly rather than silently
  skipped, since the task instruction assumed there would be something to correct.

Ticked 6.5 in `tasks.md` (before the file moved to archive) with this evidence. Staged the three
main spec files, the archived change directory (as new/untracked, git will register the add), the
deletion of the old change path, and this state/log pair. Committed and pushed.

`current`/`next_action` advance to `group7-drive` (queue id `group7-drive`) — tasks 7.1-7.7 of
`a-first-turn-is-not-told-it-has-nothing`, APPROVED 2026-09-20, groups 1-6 already merged.

## Iteration 10 — group7-drive

**Branch confirmed** `autonomous/2026-09-17-daily`, tree clean at start, HEAD `44dfea3`, matched
STATE-night.json (`current: group7-drive`).

Ran tasks 7.1-7.7 of `a-first-turn-is-not-told-it-has-nothing` (the D2 measurement) — the whole
drive delegated to a subagent given the tasks.md section verbatim plus the operating constraints,
since it is a long sequential procedure (start a Hub, verify three preconditions by direct sqlite
read, trigger real turns, read transcripts) with no room for the agent to improvise around R3's
already-corrected instructions. Verified the agent's file edits afterward rather than trusting its
report.

- **7.1/7.1b:** throwaway Hub on `:8091` (never 8000/8010), scratch profile
  `testbed/scratch/f302_measure/`, project `proj-c3039d3069b2`. Three brand-new agents
  (`f302agent1`/`2`/`3`) bound to a `claude-haiku-4-5-20251001` runner. All three preconditions
  confirmed by **direct sqlite read before triggering**, not assumed: no session-wide row in
  `project_sessions`, each agent's `config` literally `{}` (no `hub_client` override), bound
  runner's `cli` is `claude` (in `MCP_INJECTABLE_RUNNERS`), and `mcp_adapter_online_at` had zero
  prior rows for all three agents.
- **7.2/7.3:** identical one-shot instruction ("create a task titled 'F302 probe task' ... then
  stop") to all three, Haiku, one turn each, no charter, peers present (each agent saw the other two
  in the roster). Run ids `run-3e7497554e98`, `run-63c923499c5d`, `run-9ddb13b6dfeb`, all
  `status=completed exit_code=0`.
- **7.4:** read each run's `agent_outputs` directly (full dump kept at
  `testbed/scratch/f302_measure/transcript_dump.txt`). **All three: MCP** — each called
  `mcp__agentweave__create_task` directly, no `curl`/`python -c`/`Invoke-WebRequest` anywhere in any
  transcript. **Counts: 3 MCP, 0 HTTP, 0 both, 0 neither** — every sampled turn usable, no
  replacements needed. Post-turn `mcp_adapter_online_at` stamped *during* the run in all three,
  confirming the no-grounds-but-injected condition the whole measurement targets actually held.
- **7.5/7.5b/7.5c:** these were already correct in tasks.md from R3's pass — confirmed the baseline
  stays stated as n=1 (never "4 of 4 took HTTP") and the confounds (Haiku vs. the baseline's
  Opus/Sonnet, one-shot vs. a resumed ten-run session, no charter) are written alongside the
  verdict. **7.5c verdict: first branch** — every sampled first turn reached for MCP. This weakens
  R2's dissent but explicitly does **not** close D2 on n=3, one model, one instruction shape, one
  throwaway project; F302 marked "fixed, measured" rather than "fixed without qualification" (the
  distinction 7.5c itself names as the difference between this and the F392 defect).
- **7.6/7.7:** appended a new `2026-09-21` dated block to F302 in `scripts/drive/FINDINGS.md` (after
  the existing `2026-09-20, implemented` block) and a new dated section in `spec-queue/DECISIONS.md`
  under `### 2026-09-21 — F302 group 7 measured: 3 of 3 fresh first turns reached for MCP`. The
  "notice heals on turn two; the agent does not" line in the 2026-09-14 exploration was
  **deliberately left unchanged** — read carefully it already attributes turn-2-onward persistence
  to a *learned method* (the Architect's already-paid-for `curl` routine), not to the notice's
  continued presence, and 3/3 fresh-turn MCP samples with no learned method possible on turn one is
  consistent with that framing, not contrary to it; correcting it would overstate what three Haiku
  samples can say about one Opus agent's ten-run session — recorded as a stated decision, not a
  silent skip. Separately (7.7), corrected the exploration's own "What we saw" section: the
  four-agent list is now explicit that only the Architect's behaviour was actually read
  (transcript/thinking/sidechains) and the other three are listed only because the log shows they
  were *told* the same sentence — the overreach R2's 7.5 inherited from that section, named and
  fixed at its source.

**Verified independently before committing:** `git status --short` showed exactly the four intended
files (`tasks.md`, `FINDINGS.md`, `DECISIONS.md`, the exploration doc); `grep -c` on `tasks.md`
confirmed all 8 group-7 task lines (7.1, 7.1b, 7.2, 7.3, 7.4, 7.5, 7.5b, 7.5c, 7.6, 7.7 — 8 lines
since 7.2/7.3 share one bullet) ticked `[x]`, none left `[ ]`; read the new FINDINGS.md and
DECISIONS.md blocks in full and confirmed they match tasks.md's own account of counts and run ids
with no discrepancy. Hub process confirmed stopped, port 8091 free. Scratch DB and transcript dump
left on disk under `testbed/scratch/f302_measure/`, untracked, per `testbed/README.md`.

**This change is not archived this iteration.** Task 7's own bar — "7.4 carries a recorded result"
— is met, so nothing blocks archiving, but `openspec-sync-specs`/`openspec-archive-change` for
`a-first-turn-is-not-told-it-has-nothing` is unscoped work not named in this window's queue; left
for a future item if wanted, same as `change-archive` was its own separate queue entry for the
`an-archived-agent...` change.

Committed and pushed. `current`/`next_action` advance to `e2e-sweep-drive` (the full-surface e2e
sweep, last item before the time-permitting `i-6.2-redo` fallback).

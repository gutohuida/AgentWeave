# 2026-09-01 day window

Newest entry at the BOTTOM. Playbook: `.claude/loops/day-window.md`.

Armed 2026-09-01T08:55:01+01:00 on `autonomous/2026-09-01-daily` (parent master@556bb37), model opus.

---

## Iteration 1 — 2026-09-01T09:01:20+01:00 — compose the queue

Playbook `.claude/loops/day-window.md`, "Iteration 1 — compose the queue", done in full. No other
work this iteration, as `next_action` required.

### The limits this window runs under

Stated here so a later firing inherits them even if this process dies mid-thought. They are also in
`STATE-day.json:limits`.

- Stay on `autonomous/2026-09-01-daily`. No commit, merge or rebase onto `master`. Never auto-merge.
- Nothing outward-facing: no publish, no release, no PR or issue, no force-push, no history rewrite.
  Push the branch every iteration; that is what makes the work durable.
- Nothing destructive: no deleting projects, databases or kept reproductions.
- Do not browse the open web. Research is `AgentWeaveResearch`'s job.
- Every claim measured or labelled unverified.
- Operator's decisions go to `decisions_for_user` / `spec-queue/DECISIONS.md`, never guessed.
- Stage explicit paths, never `git add -A`. Never commit `kimichanges.md` / `kimiwork.md`.
- `py -3.11`, never bare `python`. `black --target-version py311`.
- **This window does not implement.** No edits under `hub/hub/` or `src/agentweave/`. The one
  exception is a drive fixture, which belongs in `scripts/drive/`.
- Drive Hub is **8011**. Never `proj-5e960453` or `proj-18e5d4e0`. Port **8000** is the operator's
  real usage and must never be touched. Every real agent turn binds `claude-haiku-4-5`. Never leave
  a job enabled.

### 1. The branch — settled, and the previous cycle **is** merged

`git branch --show-current` → `autonomous/2026-09-01-daily`. `git merge-base master HEAD` →
`556bb37`, which is master's own tip, the merge commit `merge: the 2026-09-01 overnight run`. So
the night's 47 commits are **in master**, this branch was re-cut from that merge, and it carries
exactly one commit of its own (`c0a2e8b`, the arming commit).

**The branch spans one day.** There is no unmerged predecessor: `git branch -a | grep daily` finds
only this name, local and remote, and `git branch --merged master` lists every earlier autonomous
branch. The review page's first section therefore has no "unmerged cycle" warning to carry — and
that is a measurement, not an assumption.

### 2. What last night did — read from the log and the diff, not remembered

- **Product code: exactly one commit.** `4ce24fc`, `fastmcp>=2.0,<4` in all three declarations plus
  `hub/tests/test_fastmcp_api_contract.py` (six tests naming each FastMCP API the tool surface
  stands on). +171/−3 across three files, and the night log states that is the whole product diff.
- **Seven openspec changes archived** with specs synced; no `.py`/`.ts`/`.tsx` touched.
- **A full-surface e2e sweep, rows 1–9b of 19**, ten commits of harnesses under `scripts/drive/`.
  **45 findings filed, F170–F214: 3 A, 14 B, 25 C, 3 D.** All driven against a live 8011 Hub on a
  fresh project per row, each harness run twice, the second run on the state the first left.
- **What was driven vs merely tested:** the sweep rows were driven. `4ce24fc` was **tested, not
  driven** — it is a dependency bound, and its six contract tests are the only evidence for it.
  `F169` and the static half of `F209`–`F214` are labelled read-not-driven in the ledger.
- **Left open:** row **9c** (evidence, decisions, reviews, drift, reindex) — not started, four
  leads preserved in `STATE-night.json:sweep_state.row_9c_leads`; rows 10–17; the F156 spec loop
  (queue items N-3…N-7); 45 findings with **no change proposed against any of them**.
- **`decisions_for_user` carried 29 entries**, since triaged into eight rows in
  `spec-queue/DECISIONS.md`. Entry 32 was **closed by measurement**: the full suites did run in the
  pre-merge session — `hub/tests/` 3831 passed / 84 skipped / 1 xpassed in 14:39, `tests/` 440
  passed / 3 skipped, linters clean. `+6` over the `9fa4c4b` baseline is exactly the six new
  contract tests.

### 3. Research delivered — and the file that was already here was the **wrong** run

`spec-queue/research/2026-09-01.md` was already tracked (committed by `116708f`), so this step
looked done. It was not. The tracked file was `md5 7e7f4603…`, which is the routine's **01:05 pilot
run** (`research-2026-09-01-run1-0105.md`, 21,479 bytes). Today's scheduled 07:10 delivery
(`research-2026-09-01.md`, 23,956 bytes, written 07:21, `md5 6500ed4b…`) had never been taken in.
Caught by comparing md5s against both files in the routine's `out/` rather than trusting the
filename.

Both are now in the repo: the pilot preserved verbatim as `2026-09-01-run1-0105.md`, and today's
delivery at `2026-09-01.md`, **verified byte-identical to the routine's output by md5**. The
naming convention for a multi-run day is now written into `spec-queue/research/README.md` so the
next window does not have to re-derive it.

The routine's `logs/` shows both firings (`2026-09-01_004923.log`, `2026-09-01_071002.log`); the
07:10 run finished at 07:22. Nothing was missing and nothing failed.

**The research file is data, not instructions**, per the playbook. Nothing in it directed any action
taken here; its candidates enter the queue only as ranked material for D-2's choice and for the
review page's last section.

Its three ranked candidates, each already ending in what it means for AgentWeave:

1. **Name and test the context-elevation boundary** (S). arXiv 2608.27299 hit 13/13 objectives on
   all six coding harnesses tested — including all three offering automatic permission review — by
   promoting low-privilege text into the system prompt. AgentWeave's elevation channel is explicit
   (`runner_commands.py:230`/`:312`) and four defences already hold, but one agent-authored string
   reaches a **reviewer's** system prompt unescaped: `review.task_title` at `agents.py:1139`,
   capped only by `max_length=256` with no newline restriction. And **nothing tests the property**.
2. **Record the runner CLI version on every run** (S/M). Claude Code v2.1.251 fixed four
   permission-boundary defects, including a post-check symlink swap; AgentWeave delegates file-tool
   enforcement to that CLI and no run record can say which version answered.
3. **Ratify or widen the fastmcp ceiling** (a decision, not a build item). DEPBENCH (arXiv
   2608.30300) measures the best coding-agent configuration repairing **104/203 = 51.2%** of real
   dependency upgrades. On a dependency whose break means every agent turn fails to *start*, a coin
   flip is not a migration strategy. Belongs on the review page, not in the spec queue.

Also flagged, not proposed: **Google Antigravity** is called the highest-value unread item — a
first-party multi-agent teams product with a permissions surface — and the researcher declined to
characterise it, having read only the Boost page. That restraint is right and is worth carrying to
the review page as such.

### 4. The queue

Written into `STATE-day.json`. Ordered so that stopping anywhere leaves complete work rather than a
half-written proposal.

| id | what |
|---|---|
| **D-1** | Drive: sweep **row 9c** — evidence, decisions, reviews, drift, reindex |
| **D-2** | Spec **R1** — explore and propose |
| **D-3** | Spec **R2** — independently re-derive R1's argument against the code |
| **D-4** | Spec **R3** — a second, independent re-derivation |
| **D-5** | The review page, and today's `APPROVALS.md` section |
| **D-6** | *Optional, only if firings remain before 17:00* — sweep rows 10–11 |

**Why D-1 is row 9c and not the night's build.** The playbook's default is to drive what the night
window built. The night built one thing, and it is a **dependency bound** — there is no end-to-end
surface to drive on it, and its six contract tests are the appropriate evidence. Today is Tuesday
(`(Get-Date).DayOfWeek`), so no Monday sweep is owed and the last sweep is hours old, not seven days.
Row 9c is therefore both the highest-value drive available and the one the night explicitly
preserved leads for — the sharpest being (a): 9a measured `reindex` answering **200 with
`index.written: null`** on a corpus with no home recorded, so drive **drift** against a corpus that
reindex has "succeeded" on without writing.

**What D-2 will spec, and the rule if the drive changes the answer.** Default target is **F173 (A)**
— runner management is free-typed text that swallows its own refusal, while
`openspec/specs/runner-registry/spec.md:72-73` is a **shipped** requirement saying it must offer the
catalog's models. It is the strongest of the three A findings to spec rather than merely fix: the
product is out of conformance with its own spec, and the repair has genuine design surface, because
`RunnersPage.tsx` has no error surface at all for a mutation (`api/runners.ts:69-88` declare
`onSuccess` and no `onError`; `main.tsx:8-15` configures no `MutationCache` `onError` and no
toast), so a refused create or edit reaches nothing. That absence is systemic, not local to runners.

The playbook ranks a finding *this window's drive* produced above a ledger finding. So: **if D-1
files a new A-severity finding, D-2 specs that instead** — and either way D-2 records the choice in
the log, because R2 and R3 need a target that does not move under them.

`F188` (a repairable workspace fault destroys the operator's message) and `F190` (no turn can ever
report stopped/failed/interrupted; the status map is built backwards from a payload sorted the other
way) stay unproposed and go on the review page as the two A findings the night window could take
directly — both are close to one-line repairs, and spending three rounds on a one-liner is the cost
without the benefit.

**Not attempted today, deliberately:** the F156 spec loop (night queue N-3…N-7) and rows 10–17. A
realistic day is one drive plus one change through three rounds; the round discipline is expensive
by design and collapsing it to fit more in is the failure this loop exists to prevent.

---

## Iteration 2 — 2026-09-01T09:10–09:30 +01:00 — D-1: the drive, sweep row 9c

**Row 9 is closed.** Evidence, decisions, reviews, drift and reindex driven against the `:8011`
trial Hub (beta profile, PID 22908, started 01:48 — checked first: no `.py` under `hub/hub` or `src`
is newer than the process start, so this is this branch's code). Four fixture projects, all deleted;
project count back to **23**; **zero enabled jobs** in `ai_jobs`.

### What was run

| harness | result |
|---|---|
| `scripts/drive/t_sweep_row9c_evidence.py` (operator plane) | **91 passed / 3 failed**, the second run on the state the first left |
| `scripts/drive/t_sweep_row9c_agent_plane.py` (agent plane, **real Haiku turns**) | **21 passed / 0 failed, twice** |

Six real `claude-haiku-4-5` turns across the two agent-plane runs, every one `completed`. There is
no shortcut to that plane — `get_agent_actor` resolves a run credential whose plaintext exists only
in the spawned process's memory — and 9b's lead was right that this is the one part of row 9 with a
real agent surface, because the finding it produced (F217) *is* the difference between the two
planes and could not exist without both being driven.

### Four findings filed — F215 (B), F216 (C), F217 (C), F218 (D)

- **F215 (B) — the operator's screen tells them evidence is waiting and gives them nothing to
  press.** All nine of 9c's routes are absent from the served bundle (1,472,146 bytes, read as
  bytes) *and* from `hub/ui/src`, with `spec/coverage` carried as a **control needle** reading 1.
  What makes this more than F206's shape a third time: `SpecCoverageBar.tsx:10-27` ships two
  operator-facing sentences — *"Someone needs to say which one was wrong"* and *"nobody has decided
  about it yet"* — both present in the bundle exactly once, on a bar mounted at
  `SpecDocumentPanel.tsx:240`. The actions they name are `POST /spec/drift/{id}/resolve` and
  `POST /spec/evidence/{id}/decision`, neither of which the bundle contains. **An operator using
  only the product cannot accept their own project's evidence.** With F206 (5) and F211 (3) the spec
  flow now has **17 routes with no operator surface**, measured.
- **F216 (C) — the drift candidate names nothing its reader has ever seen.** `GET /spec/drift`
  projects `requirement_id: spreq-…` and `evidence_id: ev-…` and no identifier, document, actor or
  locator. F212's shape one router over — and the product already fixed it in the other direction,
  with the reason written down at `agent_actions.py:1141-1143`.
- **F217 (C) — an agent's accepted evidence is never drift-checked in practice; an operator's for
  the same requirement is.** One commit to `README.md` on `main`, two accepted rows on the same
  document: operator footprint `main` → 1 candidate; agent footprint `agentweave/<agent>` → **0**.
  `detect_drift`'s docstring documents the branch-basis choice as a *general* deferral; measured, it
  is not general — an agent is always footprinted in its worktree, so agent evidence is *always* the
  blind case. And the Hub records `reachable_from_main: true` on the footprint it then declines to
  compare against main.
- **F218 (D)** — `POST /spec/evidence` answers `review_state: accepted, latest_review: null` while
  the next GET on the same row carries the review. F71's fix one field over, on the same call.

### 9a's sharpest lead is answered, and it is a re-confirmation

9a asked what downstream assumes an index exists because reindex answered 200 with
`index.written: null`. Driven: **nothing new does.** `detect_drift` never reads the manifest at all
— footprints and git trees only — and answered 200 on the unindexed corpus; coverage answered 200.
The one route that refuses is `arrange`, and that refusal is **F208**, already filed, unchanged.

### Two clean negatives worth as much as the reds

- **F204/F210's shape is absent from 9c**, measured rather than read off the signatures. The one
  route whose fields all default (`reindex`) answers **200** bodyless; the four that refuse each
  have a genuinely required field and name it the moment the caller sends `{}`.
- **An added file is not drift.** `_changed` walks the baseline's paths. This cost the harness its
  first run, and chasing it is what produced the negative.

### The method note that mattered

**A zero only means something next to a control.** The first draft's six zero-hit rows looked
identical to the ones a broad `grep -il evidence` immediately contradicted with 21 files — the UI
says the *words* (`evidence_count`, `drifting`, `evidence_awaiting_review`) while calling none of
the *routes*. Only the narrow needle plus the `spec/coverage` control tells those apart, and F215
would have been either missed or overstated without it.

**Next:** D-2, spec loop R1. The override rule in the queue applies — D-1 filed a new **B**, not a
new A, so the default target stands: **F173**.

---

## Iteration 3 — 2026-09-01T09:35–10:05 +01:00 — D-2: spec loop ROUND 1

**TARGET: F173 (A)** — recorded here so R2 and R3 have one that does not move. The queue's override
rule did not fire: D-1 filed a new **B** (F215), not a new A, so the default stands.

**Change written:** `openspec/changes/runner-model-is-chosen-from-the-catalog/` —
`proposal.md`, `design.md`, `tasks.md` (24 tasks), and a `specs/runner-registry/spec.md` delta
(one requirement MODIFIED, one ADDED). `openspec validate --strict` **passes**.

### The scope call, made deliberately and written into design.md

The queue's note said the missing-error-surface absence "is systemic rather than local to runners".
Round 1 **measured how systemic**, because that answer decides the shape:

```
55 mutate call sites under hub/ui/src
12 files already using readableApiError
 6 files with a mutation and no error surface at all
```

So the app is **not** missing an idiom — it has one, and `AgentCreateDialog.tsx` already uses both
halves this change needs (a catalog `<Select>` at line 222, a mutation-error alert at line 235).
`RunnersPage` is the outlier, not the frontier. **The change stays inside runner management.**

Two alternatives rejected in writing:

- **A global `MutationCache.onError` toast** — it would double-report against twelve existing local
  surfaces and put the sentence somewhere other than beside the control that failed.
- **Generalising the shipped requirement** `agent-conversation-workspace:2051` ("The operator reads
  why a submission was refused") to cover every write surface. That is the cleaner end state and is
  named as a follow-up, not done: the six surfaces above have never been driven, so the general
  requirement would ship as prose the code breaches on the day it lands.

**F215 is deliberately NOT folded in**, as the queue asked me to decide: it is a 17-route operator
surface gap across three findings, a much larger change, and widening this one would bury an
A-severity repair inside it.

### Round 1 found a second defect by asking a question a picker forces — F219 (C), filed

A free-text box lets the operator *believe* they cleared the model. A select has to name the unset
choice out loud, so R1 asked whether the API can honour one. **It cannot, and answers `200` anyway.**

Measured live against `:8011`, fixture project created and deleted:

```
PATCH /projects/<p>/runners/<r> {"model": null}
  -> 200  {"model": "claude-fable-5", ...}      <- unchanged, reported as a success
PATCH /projects/<p>/runners/<r> {"model": ""}
  -> 400  '' is not a model 'claude' declares
```

`update_runner` (`runners.py:136-141`) gates every field on `is not None`, and Pydantic gives an
explicit `null` and an absent key the same value — so *clear this* is indistinguishable from *do not
touch this*, and the only other spelling is refused. **Once a runner has a model, it has one
forever.** An unset model is a real state: both seeded runners have `model: null` (measured) and
`_build_claude_command` emits `--model` only `if model` (`runner_commands.py:199-200`).

It is in this change rather than filed away because shipping the picker without it would deliver an
unset choice that silently does nothing — the same defect class, in a new place.

### The reproduction is durable, not a transcript

`scripts/drive/t_f219_runner_model_clear.py` — **10 passed / 2 failed, twice**, the two reds being
exactly F219. It creates and deletes its own fixture project, so it never reads `AW_PROJECT` and
cannot touch a real one; it refuses `:8000` outright. Project count back to **23**, no fixture left.

### Other measured facts R1 put in the proposal

- `GET /model-catalog` on the running Hub: **claude 4 models, codex 6**. Not read off the source.
- `flags` is already effectively clearable (`agent_trigger.py:976` reads `list(runner.flags or [])`,
  so `[]` and `None` are the same state at spawn) — so the fix is scoped to `model` alone.
- **The existing UI test pins the defect in place.** `runnersUi.test.tsx:84-85` types into the
  free-text placeholders, and the shared `modelCatalogFixture.ts` declares `models: []` for both
  providers while being imported by **9** test files. Both are tasks, with the blast radius named.
- `Runner` in `api/runners.ts` does not even declare `model_unrecognised`, so the field the backend
  computes for the requirement's third scenario cannot be read by the UI at all.
- **Not** `components/agents/ModelPicker.tsx`: it opens `bottom-full` (upward, wrong in a centred
  modal) and cannot express "no model" — `current` falls back to the provider default descriptor, so
  unset and default-selected render identically.

**Next:** D-3, spec loop R2 — independently re-derive this argument against the code. Not a re-read
of the above. Ask what each route *returns* when the function it calls raises.

---

## Iteration 4 — 2026-09-01T09:45–10:35 +01:00 — D-3: spec loop ROUND 2

**Target unchanged and unmovable: F173 (A)**, change
`openspec/changes/runner-model-is-chosen-from-the-catalog`. This round did **not** re-read round 1's
reasoning. It opened the files round 1 cited, re-derived each claim, and drove the questions round 1
never asked. `openspec validate --strict` passes.

**This round changed the proposal.** Four corrections, one of them the kind the discipline exists
for, plus two new findings.

### The catch: round 1's change would ship a screen where a legacy runner cannot be saved

`_reject_undeclared_model(cli, model)` takes a provider and a model string. **It cannot see the
runner's stored model.** Its docstring (`runners.py:25-27`) claims it can — *"a model is refused only
when it is being newly set — an already-stored, unrecognised model … is left alone"* — and that
claim is true today **only by accident**: the free-text box sends `model: model || undefined`, which
`JSON.stringify` drops, so an untouched model arrives as an absent key.

A picker cannot do that. Its selected value *is* the stored model, and round 1's own task 2.3
requires the edit path to keep sending `model` explicitly. Measured:

```
GET   /runners/<r>                                   -> model_unrecognised: true
PATCH {"name": "renamed"}                            -> 200, legacy model survives
PATCH {"name": "renamed", "model": "claude-3-legacy-9"}   <- the runner's OWN model
                                                     -> 400 'claude-3-legacy-9' is not a model 'claude' declares
```

So round 1's task 4.4 (*"saving unchanged submits that same model"*) asserts the opposite of what
the code does, and shipping it would breach the **shipped** `runner-registry` scenario *"Existing
runners keep working"* that this change is otherwise strengthening — a legacy runner would become
uneditable in every respect, because `name` and `model` travel in the same PATCH. This is the
2026-08-28 shape exactly: **the argument was wrong while everything it argued about was right.**

Repaired in the proposal: new task **1.3** (`update_runner` does not refuse a model identical to the
one already recorded; a *different* undeclared model and any undeclared model on create stay
refused), new test task **1.5**, a normative clause and a new scenario in the spec delta, and task
4.4 rewritten to say the assertion is unsatisfiable without 1.3. Filed as **F220 (C)**.

### Three smaller corrections, all from reading rather than trusting

- **The exemplar is only half an exemplar.** Round 1 said `AgentCreateDialog.tsx` "already uses both
  halves". The model half is real (`useModelCatalog()` 149, providers 168, `<select>` 222). The
  error half is **not** `readableApiError`: line 235 calls a private `errorDetail` at lines 10-18
  that is a near-twin of the `extractErrorDetail` this change deletes. The same bespoke helper has
  been written twice — which sharpens the case for the shared idiom, and means task 3.2 must copy
  its placement and explicitly not its helper.
- **"Copy `AgentCreateDialog.tsx:180`" was wrong for runners.** Its `handleProviderChange` resets to
  a concrete default *model id* because an agent must have one (`canSubmit` requires `!!model`). A
  runner must reset to the **unset** option; choosing a model on the operator's behalf is the same
  mistake as silently re-pointing a legacy runner. Task 2.4 rewritten.
- **The error surface must be reset, not only rendered.** `useCreateRunner()`/`useUpdateRunner()` are
  called in `RunnersPage` (36-38), so they outlive `RunnerForm`. React Query holds a mutation's last
  result until the next `mutate` or `reset()` — so reopening "New Runner" after a refusal shows the
  previous refusal before anything is submitted. `AgentCreateDialog` has the same shape and no
  reset, so copying it copies the defect. New tasks **3.4** and **4.6**. *(Derived from React
  Query's documented state lifetime and the call sites, labelled not-driven in design.md.)*

### F221 (D), found and deliberately left open

The catalog **declares** `opus` — as an alias of `claude-opus-5` (`model_catalog.py:154`) — but
`ProviderDescriptor.model()` matches `m.id` only (113-117) and nothing outside that module resolves
aliases. So F173's exact reproduction is refused with *"'opus' is not a model 'claude' declares"*
about a string the catalog lists and `claude --model opus` accepts. **The sentence the operator
reads is not true.** Named out of scope in design.md with the reason: the picker submits ids, so
after this change it is unreachable from the screen and API-only, and widening an A repair to chase
a D is how the repair stops shipping.

### What round 2 checked and did **not** change — recorded on purpose

- **What does the route leave behind when it raises?** `update_runner` assigns `runner.name` (133)
  before `_reject_undeclared_model` can raise (137). Driven twice with two different undeclared
  models: **the name is unchanged both times.** `get_session` yields inside
  `async with async_session_factory()` and never commits on the exception path. Round 1 never asked;
  the answer is clean and the change does not touch it.
- A refused create leaves **no row** (3 → 3).
- **`flags` measured, not read.** Round 1 read `[]`-is-clearable off `agent_trigger.py:976`.
  Measured: `PATCH {"flags": []}` → `[]`; `PATCH {"flags": null}` → **no-op**. So `flags` carries
  `model`'s exact defect and escapes it only because `[]` is a reachable spelling meaning the same
  thing at spawn. That asymmetry is now the stated reason `model` is fixed and `flags` is not.
- **`useDeleteRunner` throws a real `ApiError`** (`fetchWithAuth`, `client.ts:24-27`). Checked
  because `readableApiError` returns its *fallback* for anything else, so task 3.5's swap could have
  silently replaced the delete refusal's sentence — the one naming the agents to unbind — with a
  generic one. It does not.
- **`model_fields_set` is the right mechanism**, verified against the real `extra="forbid"`
  `RequestModel`: explicit null → `{'model'}`, absent → `set()`. Task 1.1 stands.
- **ModelPicker confirmed unusable**, now for two independent reasons rather than round 1's one:
  `current`'s fallback at line 55, *and* `onChangeModel: (modelId: string) => void` (line 44) has no
  spelling for "unset" to emit.
- **F219 re-measured independently** — `PATCH {"model": null}` → 200, unchanged; `""` → 400.

### The harness

`scripts/drive/t_r2_runner_update_semantics.py` — **18 passed / 1 failed, twice**, the single red
being F220. Creates and deletes its own fixture project (never reads `AW_PROJECT`, refuses `:8000`);
project count back to **23**; no jobs created. Q5 needed a runner whose stored model the catalog does
not declare, which **no API path can produce**, so the harness writes that one row directly into the
fixture runner via sqlite — the pre-catalog state the shipped scenario is about.

**Observation, not acted on:** the harness identified the database by looking up its own fixture row,
and the Hub on **8011 serves `~/.agentweave/hub/profiles/beta/agentweave.db`** — the same file
CLAUDE.md records for 8010. Both trial Hubs appear to share one database. Added to the open
`trial-hub-8010-staleness` decision rather than guessed at.

**Next:** D-4, spec loop R3 — a second, independent re-derivation. Do not start from this entry;
compare the proposal against the code afresh, and check it against the **shipped** specs in
`openspec/specs/`, not only the implementation.

---

## Iteration 5 — 2026-09-01T10:05+01:00 — D-4: spec loop R3, the second independent re-derivation

**This round changed the proposal.** Not its argument — round 2's clause is right and every one of
its measurements re-confirmed — but the spec text it left behind, which is the artefact that becomes
a test.

### The headline: round 2's carve-out falsified a scenario that was left standing

R2 added *"A legacy runner can still be saved"* to the `MODIFIED` requirement and kept
*"An undeclared model is refused"* verbatim beside it. The second scenario's `WHEN` is an **instance
of** the first's:

> An undeclared model is refused — WHEN a runner is submitted with a model its provider does not
> declare → **refused**.
> A legacy runner can still be saved — WHEN the operator saves it with that model still selected →
> **accepted**.

The requirement's normative prose resolves this four paragraphs later (*"refused where it is newly
set, and only there"*), but scenarios are what get turned into tests, and this pair cannot both be
turned into a passing one. An implementer reading top-to-bottom builds task 1.3's opposite.

That is exactly the shape the state note asked R3 to look for under (b) — *the relaxation weakens a
shipped `SHALL`; the delta must say so rather than leaving two rules that disagree*. It said so in
one paragraph and contradicted itself in another. Two narrowing edits, no additions:

- the bare absolute now reads *"The Hub SHALL refuse a request that **sets** a runner's model to one
  its provider does not declare"* — a runner already carrying it is not having it set;
- the scenario gains `AND the runner does not already record that model`.

### Second: a scenario with no failing case, beside normative text with no scenario

`A request that changes nothing is not reported as a change` was written for F219, **before** task
1.3 existed. Once 1.3 lands, a `PATCH` carrying the runner's own stored model is accepted `200` with
the model unchanged — that scenario's `WHEN` read literally, with the outcome the change wants. So
it now forbids what 1.3 requires.

Meanwhile *"A request that carries no model at all leaves the runner's model as it was; these are
different requests and the Hub SHALL distinguish them"* had **no scenario**, while task 1.4 already
tests precisely it. Replaced the first with the second; the guard it was carrying (the answer must
not report the old model) moved into the clearing scenario as an `AND`, where it can be asserted.

### Third: task 2.6 asked for something no requirement demanded

`The runner list row marks a runner whose model_unrecognised is true` was in `tasks.md` and in
proposal.md's *What Changes* and in **no** requirement — the shipped scenario says *when editing it*,
and R2's delta scoped the marking to the offered choices. **Backed rather than dropped**: what makes
a legacy runner safe is that it is legible, and the list is where runners are seen; the file is
already reading `model_unrecognised` for the picker. One clause and one scenario `AND` now carry it,
so every task traces to a requirement again.

### Fourth: task 4.2 would have broken the test it rewrites

It read *"the two `getByPlaceholderText` assertions on the model input (lines 84-85) go"*. Line 84 is
`'e.g. Claude Opus'` — the **Name** field. `Save` is `disabled={isPending || !name.trim()}`
(`RunnersPage.tsx:246`), so deleting it leaves the button disabled and the test's own `createMutate`
assertion fails. Only line 85 goes. Rewritten with the reason.

### Checked and not changed — recorded so the next round is not made to look cheap

- **Both providers gate `--model`.** The "unset is spawnable" claim cited only
  `_build_claude_command:199-200`; `_build_codex_command:313-314` does the same. Holds for the whole
  catalog, not the cited half.
- **Every reader of `runner.flags` treats `[]` and `None` alike** — exactly two:
  `agent_trigger.py:976` and `codex_appserver.uses_app_server:87` (reached from `agents.py:226`),
  both `or []`. So the fix-model-not-flags asymmetry is sound, and leaving `flags` alone does **not**
  breach the sibling shipped requirement *"A runner whose flags are unset SHALL receive the Hub's
  default transport"* — `[]` reaches that state.
- **`RunnersPage` is the only writer of runners** — one call site each for `useCreateRunner` /
  `useUpdateRunner`. Task 1.3's relaxation is reachable from the picker or a direct API caller and
  nowhere else. (state note (b))
- **(d) both of R1's unre-checked measurements hold**: `useModelCatalog` is a `useQuery`, so
  `isLoading`/`isError` exist for task 2.5, and `runnersUi.test.tsx:66` already mocks it;
  `modelCatalogFixture.ts` declares `models: []` and `grep -rl` finds exactly **9** importers.
- **(a) no shipped requirement already governs the ADDED one.** `agent-conversation-workspace:1849`
  and `:2051` are scoped to *"a submission to an agent"* — conversation input, not runner CRUD;
  `hub-interaction-feedback` is pointer/focus states only; `hub-api-request-contract` governs
  undeclared **fields**, and this change adds none. Sibling, not duplicate.
- **(e) `t_r2_runner_update_semantics.py` re-run: 18 passed / 1 failed**, the same single red.
  Fixture project created and deleted, count back to **23**. Nothing moved underneath.

`openspec validate --strict runner-model-is-chosen-from-the-catalog` passes. No finding filed: these
are proposal defects, not product ones. The spec loop is **complete** — F173 is ready to implement,
and this window does not implement.

**Next:** D-5, the review page — `spec-queue/review/review-2026-09-01.html` per the six-section
contract in `.claude/loops/day-window.md#D-5`, then today's section in `spec-queue/APPROVALS.md`.
Section 4 must say what R2 and R3 **each** changed; both changed something, so neither gets the
"changed nothing" sentence.

---

## Iteration 6 — 2026-09-01T10:10–10:20 +01:00 — D-5: the review page, and today's APPROVALS section

**Branch and history reconciled.** `git branch --show-current` = `autonomous/2026-09-01-daily`,
HEAD `f1c5991`, tree clean, `git log` matches `STATE-day.json` commit for commit. Nothing to
reconcile.

**Written:** `spec-queue/review/review-2026-09-01.html`, 35 kB, six sections in the contract's
order plus one clearly-labelled decisions block placed between 5 and 6 so the six required sections
stay in sequence.

### Verified rather than assumed

The page claims "every number is measured or labelled otherwise", so the claims it makes about
*itself* were measured too:

| Claim | How it was checked |
|---|---|
| Self-contained | Regex sweep for `<link href>`, `<script>`, `@import`, `src="http`, `url(http`, `fonts.googleapis` — **zero hits** |
| Opens as a file | `<!DOCTYPE html>`, `<html`, `<head>`, `<body>`, `</body>`, `</html>` all present |
| Theme-aware | bare `:root {` present, `@media (prefers-color-scheme: dark)` present, `body { … background: var(--bg) }` matched by regex |
| Well-formed | `html.parser` tag-balance walk — **no unclosed tags at EOF, no mismatches** |
| "no product code on this branch" | `git diff --stat 556bb37..HEAD -- hub/hub src tests hub/tests hub/ui pyproject.toml hub/pyproject.toml` → **empty** |
| "29 tasks" | counted in `tasks.md` — the state file still said R1's **24**; R2 and R3 added five |
| "0 of 54" / "0 of 29" for the archive step | `grep -c` on both `tasks.md` files, checked and unchecked separately |
| "226 findings" | `grep -c '^## F'` on `FINDINGS.md` |

**One number in the research was wrong and is corrected on the page rather than repeated.** The
research file says AgentWeave's tool surface is "27 declared tools". Measured:
`grep -c '^@mcp.tool()' hub/hub/mcp_server.py` = **26**, 25 agent-callable, which is also what
`CLAUDE.md` records. The page states 26 and names the discrepancy in-line, because a positioning
line built on a wrong count is worse than no line.

### Two self-corrections while writing APPROVALS.md, the second of them material

1. The first draft told the operator to write the status token *"replacing the leading `-`
   marker"*. The contract in `spec-queue/README.md` is `- APPROVED  <change-name>` — the `-` stays
   and the token follows it. Corrected.
2. **The corrected version then contained a fenced example reading
   `- APPROVED  runner-model-is-chosen-from-the-catalog`** — inside today's own section, in a file
   whose sole reader is the FIX window. A parser that scans for status tokens would have found the
   day window approving its own proposal, and the whole point of the split is that it cannot. The
   example is now `- <APPROVED|REVISING|REJECTED>  …`, with the reason written beside it, and a
   `grep` for a real token under today's heading returns **nothing**.

That second one is the kind of defect this loop exists to catch, produced by the loop itself: a
plausible-reading artefact that would have mis-fired at 23:00 with nobody awake.

### What the page says, in one line each

1. **Branch** — 1 day, predecessor merged, no warning to carry, and that is measured.
2. **The night** — one product commit (fastmcp `<4`), *tested and not driven*, said so plainly;
   seven archives; rows 1–9b driven. The night's own "what to distrust" list carried over intact.
3. **Today's drive** — F215 (B), F216 (C), F217 (C), F218 (D) from row 9c, plus F219 (C), F220 (C),
   F221 (D) from the spec loop. Severity, one sentence, `file:line` each. Two leads recorded as
   *closed* rather than carried.
4. **The change** — problem, argument, then a card each for what R2 and R3 changed. Both changed
   something, so neither gets the "changed nothing" sentence; R3's card is explicit that it changed
   the spec *text* and re-confirmed the argument, which is a different kind of outcome from R2's.
   Cost stated as 29 tasks, the files, the bundle refresh, and the three findings it retires.
5. **The default queue** — the three steps from `spec-queue/README.md`, each resolved against
   today's tree: step 1 is **empty** (measured), step 2 is where it lands, step 3 needs the
   operator. Plus the one caution worth having: F173 is *both* the top open A finding *and* the
   thing that was just specced, so approving nothing sends the night at F173 without the design.
6. **The research** — three ranked candidates, each ending in what it means for AgentWeave; the
   drift surface noted as reinforced; five "seen, not proposed" items including Antigravity as the
   highest-value unread thing next run should start from.

Both carried decisions are on the page as **decisions, not work**: the `fastmcp<4` ratification
with DEPBENCH's 104/203 attached, and F188/F190 staying unproposed as direct repairs. The three
older open decisions are named in one small paragraph pointing at `spec-queue/DECISIONS.md`,
including round 2's measurement that 8010 and 8011 appear to share the beta database.

**Not run, and it did not need to be:** no test suite, no Hub, no fixture project. This iteration
wrote two documents and measured its own claims about them. No `.py`, `.ts` or `.tsx` touched, so
nothing could have gone red.

**Next:** D-6 is OPTIONAL — sweep rows 10–11 (Jobs+Loops, Questions), and only if a firing remains
before `stop_at` 17:00. If it files anything, it must edit section 3 of the review page as well as
`FINDINGS.md`: a review page that omits a finding filed after it was written is worse than one
written later.

---

## Iteration 7 — 2026-09-01T10:20–10:33 +01:00 — D-6: the optional drive, sweep row 10 (Jobs + Loops)

**Branch and history reconciled.** `git branch --show-current` = `autonomous/2026-09-01-daily`,
HEAD `e84a38f`, tree clean, `git log` matches `STATE-day.json` commit for commit. Nothing to
reconcile. Clock 10:20 against `stop_at` 17:00 — a full iteration remained, so D-6's gate opened.

**Hub confirmed current before anything was concluded.** The `:8011` process started
2026-09-01T01:48:32; **zero** `.py` files under `hub/hub` or `src` have a mtime newer than that
(measured, not assumed). Project count 23 at the start.

### What was driven

Row 10 of the 19-row matrix — the twelve routes in `hub/hub/api/v1/jobs.py` (8) and
`hub/hub/api/v1/loops.py` (4). Harness kept: `scripts/drive/t_sweep_row10_jobs_loops.py`,
**66 passed / 9 failed**, run **four** times (twice, then twice more after leg 9 was corrected),
failure set identical apart from ids and timestamps. Fixture `proj-d8d207e6e845`, created and
deleted; count back to 23. Four real agent turns, all `claude-haiku-4-5-20251001`, measured off
`run_started` events rather than assumed from the runner row. Project-wide sweep afterwards:
**zero enabled jobs anywhere**, across all 23 projects and 61 jobs.

Safety measure worth reusing: every job the harness creates uses the cron `0 0 1 1 *`, so the
scheduler cannot fire one behind it. Firing is done by hand through `POST /run`. The `finally`
disables and archives every job it created and then re-reads the project to prove it.

### Five findings, F222–F226

| | | |
|---|---|---|
| **F222** | **B** | An **archived** job is put back to work by one `PATCH {"enabled": true}` — accepted, re-registered with the scheduler, still absent from `GET /jobs`. `update_job` reads `archived_at` nowhere (`jobs.py:1039,1063`); `run_job` doesn't either (`:1219`). |
| **F223** | C | `GET /jobs/{id}` reports `source: "hub"` for a job stored `"local"` — the detail route hand-builds its dict, omits the key, and `JobResponse.source`'s default fills the hole. The list route is correct, so the two disagree. |
| **F224** | C | A loop archived through its job is told *"this loop is still running"* — permanently. `archive_job` sets `archived_at` and never `ending_state`; `archive_loop` checks `ending_state` first. |
| **F225** | C | Neither loop mutation route (`/control`, `/archive`) has a call site in the UI source **or** the served bundle. `loops.ts` is 56 lines, two queries, no mutation. |
| **F226** | D | The detail route's embedded history drops `error_summary` and `tick_count`. Latent — but `JobCard.tsx:359` already prefers the embedded copy and `:205` renders `error_summary`. |

**F222 was driven to its end rather than filed at the `200`.** The interesting part is not that the
PATCH is accepted; it is what happened next. From the project's own event log:

```
09:25:08.342  job_archived        {"id": "job-cd5dde4f77d0"}
09:25:08.757  run_triggered       {"agent": "r7a102303", "run_id": "run-9a090650dc99"}
09:25:14.463  run_started         {"runner": "claude", "model": "claude-haiku-4-5-20251001"}
09:25:16.903  run_completed       {"exit_code": 0}
```

The archived loop applied its staged definition edit (`loop_edit_applied`), claimed
`task-bec308ca4d24`, moved it to `in_progress` and spent a real turn on it — 0.4 s after being
archived, and invisible in both `GET /jobs` and `GET /loops`. `archive_loop`'s own docstring calls
this "the exact governance failure loops exist to make impossible (D17)". Held to **B** rather than
A only because the shipped UI cannot reach it: `useJobs` never sends `include_archived`, so no
screen shows an archived job next to the enable control that would resurrect it.

### One assertion was wrong, and saying so is the point

Leg 9's first version asserted *"the served bundle reaches `/control`"* and went red at bundle 0 /
source 5. That reads exactly like F215's stale-bundle shape. It is not one: **all five source hits
are the words "model/control catalog" in comments.** The check was measuring a word, not a route.
Rewritten to match `/loops/${…}/control|archive` as a caller would have to write it — 0 in source,
0 in the bundle — and the last two of the four runs are the corrected form. Filed inside F225 so the
next reader knows the finding survived a check that could have manufactured a different, false one.

### Negatives recorded on purpose

Row 10 is a well-defended router and most of what was probed held:

- **All four documented loop opt-in refusals fire and leave no row behind** — `work_needs_evidence`
  without a loop, `session_mode=resume` on a loop, an ambiguous cron, an invalid cron. F54's
  "a refusal creates nothing" rule verified for all four in one assertion.
- **The staged-edit machinery (D11) is exactly as specified** — accepted, reported as `pending_edit`
  with `staged_by`/`staged_at`, live `purpose` untouched, `loop_edit_staged` on that loop and no
  other.
- **F212's shape is ABSENT.** `LoopSummary` carries its own `id` and `GET /tasks?loop_id=` really
  does accept it — driven, not read.
- **F204/F210's shape is absent too.** Bodyless probes on all six mutating routes: the two with a
  required body answer 422, the two with none work, and the refused `PATCH` left the job untouched.
- **Cross-project isolation holds** on all five routes tried, and every unknown id is a 404.
- `DELETE /jobs/{id}` refuses with a stated remedy — and answers 404 first for an unknown id.
- `GET /history` refuses `limit=0` and `limit=1001` rather than clamping.

**The boundary question, asked once of this router and dropped:** `GET /jobs` and `GET /loops` take
no `limit` and are unbounded; the detail route's embedded history is hard-limited to 10 with no
parameter; only `/history` is bounded and parameterised. Not at risk here, and not where that would
first hurt.

### The review page was edited, not left behind

`spec-queue/review/review-2026-09-01.html` — section 3 gains a row-10 paragraph and five table rows
below a labelled rule, the running total moves 226 → **231**, and "rows 10–17 remain untouched"
becomes "rows 11–17". Section 6's *"pattern with a name"* paragraph now names F225 alongside F215,
with the reason F225 is the harder instance: the jobs UI is complete, so two missing loop actions
read as a gap in a feature rather than a feature with no surface. The page's own claims were
re-measured after the edit, not assumed: zero external references, full DOCTYPE/html/head/body
wrapper, `:root` + `prefers-color-scheme` + `body{background:var(--bg)}`, and an `html.parser`
tag-balance walk with no unclosed tags and no mismatches.

`spec-queue/APPROVALS.md` is **not** edited — no new change was proposed, and the day window must
not write a status token into its own section.

**Next:** the window's queue is complete. D-1 through D-6 are all done and the required work (the
review page and today's APPROVALS section) was already published in iteration 6. Row 11 (Questions)
is the next row of the sweep and is the natural start for the next window that has a drive slot.

---

## Iteration 8 — 2026-09-01, 10:35 → 11:1x — D-7: sweep row 11, Questions

`next_action` said the queue was complete and named one conditional unit: **row 11 of the sweep,
Questions, and only if a full iteration remained.** It did — 10:35 against a 17:00 `stop_at` — so
row 11 was taken. Branch and `git log` matched `STATE-day.json` exactly; nothing to reconcile.

**Row 11 is CLOSED.** Two harnesses, both kept:

| | passed / failed | runs | turns |
|---|---|---|---|
| `scripts/drive/t_sweep_row11_questions.py` — operator plane | 55 / 5 | three, identical | none |
| `scripts/drive/t_sweep_row11_batch.py` — the batch half | 28 / 1 | twice, identical | one real Haiku turn each |

Three findings filed: **F227 (B)**, **F228 (C)**, **F229 (D)**.

### What was driven, and what was deliberately not

`next_action` warned that row 11 has prior coverage under an older numbering. It does — four files,
all read before anything was written: `t_row13_questions.py` (setup), `t_row13_operator_question.py`
(a blocking and a non-blocking operator question and whether the answer is delivered),
`t_row13_timeout.py` (a real agent's blocking question expiring) and `t_row19_crash_question.py` (a
question outliving a Hub crash). None of them touches the **decline** route in any form, what the
operator's two lists contain after a decline, or whether the two resolutions are symmetric. That is
what was driven, rather than re-ploughing.

### The finding, in one line: the operator declines, then answers, and a turn is spent

`POST /questions/{id}/decline` refuses an already-answered question with a 409 and a sentence saying
why. `PATCH /questions/{id}` never reads `question.declined` at all, so the same pair of actions in
the other order is accepted silently and the row ends up **`answered=True` and `declined=True`** —
two of the three outcomes `ask_user`'s own docstring enumerates as distinct.

That would be a C if it sat there inertly. It does not:

```
GET /projects/<p>/queue/asker
  {"id": "entry-a122898689f8",
   "state": "delivered",
   "delivered_in_run_id": "run-993559fe8a6e",
   "content": "Question: r11-DL1-decline-first: which way should this go?  Answer: right"}
```

A real run consumed the answer to a question the operator had already closed — to an agent that
`ask_user` had already told *"Nothing further is coming… Decide it yourself."* The control is in
the same leg: an ordinary answer queues exactly one entry, so the delivery path is what is being
measured. **F227 (B).**

The reachable path to it is **F228 (C)**: `QuestionsPanel` makes exactly two calls, `?answered=false`
and `?answered=true`, and the route filters on the `answered` column alone. A declined question has
`answered=False`, so it never leaves the list the page labels *Unanswered* — and **no component in
`hub/ui/src/components` reads a question's `declined` flag at all** (0 hits, measured, while
`api/questions.ts:34` declares it). Every row renders an `AnswerForm`. Shown twice: on a hand-made
question, and on a real three-question `ask_user` batch that was answered twice, declined once and
**delivered** — the batch is finished, the agent has its result, and the declined row is still
listed as unanswered.

**F229 (D)** is the surface half: the route *is* in the served bundle (1 source call site, 1 in
`hub/hub/static/ui`, matched as a caller has to write it), so F215/F225's stale-bundle shape is
**absent** here. Its only call site is `AgentOutputPanel`'s in-run card; `QuestionsPanel.tsx`
contains the string `declin` zero times.

### The batch design came through almost untouched

`2026-08-13-answers-arrive-together` and `2026-08-11-declining-a-question` had never been driven,
because there is no way to make a batch except a real agent turn — `POST /projects/{id}/questions`
leaves `batch_id` NULL and `POST /agent-actions/questions/batch` is agent-authenticated. Driven
non-blocking on purpose: a blocking batch is held open by `ask_user`'s own poll loop and the answers
return as the tool result, so the queue delivery path is skipped by design. Every claim held:

- one `batch_id`, `batch_index` 0/1/2 in ask order, `batch_size` 3 on every row;
- **answering 1 of 3 queues nothing. Answering 2 of 3 queues nothing.** The interruption batching
  exists to prevent does not happen;
- a **decline completes the batch** and releases the two answers already given — one entry, not one
  per answer, at `hop_depth` 0, `origin_type` operator, addressed to the asking agent;
- the declined one is **named**, not omitted, exactly as D4 requires:

```
You asked 3 questions. The operator has now resolved all of them.

1. …first: What color should the submit button be?
   Answer: the first one
2. …second: What should we name the configuration file?
   Answer: the second one
3. …third: Should Save come before or after Delete in the menu?
   Declined — the operator saw this and chose not to answer it.
```

### Two of this harness's own assertions were wrong, and both are recorded as such

Row 10's lesson applied twice in one iteration, so it is written down again rather than quietly
fixed:

1. **The bodyless-probe loop declined the question before asserting nothing had changed.**
   `POST /{id}/decline` takes no body, so bodyless is its *normal* call; putting it in the probe
   list meant the "the refused calls left the question exactly as it was" check was asserting
   against this harness's own action. Moved after the check, with the reason in a comment.
2. **The batch harness counted its own trigger message as a delivery.** The run marker has to appear
   in the prompt — it is what tells the agent to use it — so matching the marker alone made legs 3
   and 4 read `1 entry` when the real answer was `0`. That would have filed *"answering 1 of 3 wakes
   the agent"*, a finding against a design that in fact works. Replaced by a `deliveries()` helper
   that identifies an entry by what only `_batch_delivery_text` writes.

A third assertion was **misconceived rather than wrong**: "the thread is named from the first
question". `name_conversation` is a documented no-op on a thread that already has a title, and the
delivery reuses the agent's open thread — which this harness's own trigger had just titled. So the
rule is unreadable in this setup and asserting it would have been asserting against the setup.
Replaced with what *can* be read: the answers land in an existing open thread rather than opening a
second one, and that thread carries a title.

### Negatives recorded on purpose

- **F215/F225's stale-bundle shape is ABSENT** from this router — the decline route reaches the
  served bundle. Measured as a route, not as a word.
- **F204/F210's shape is absent.** Bodyless probes on all three mutating routes: the two with a
  required body answer 422 and leave the row untouched, and the third works because it has no body.
- **Cross-project isolation holds on all five routes**, and every unknown id is a 404 — including
  `decline`, which answers 404 for an unknown id rather than the 409.
- **Schema refusals fire**: 1 option, 9 options, an empty label, and each of the three mandatory
  fields dropped in turn — 422 every time, and an undeclared field is refused too.
- `?limit=0`, `?limit=1001` and `?offset=-1` are refused rather than clamped; `offset` shifts the
  window by exactly one row.
- **Declining is idempotent** and the second decline does not move `declined_at`.
- The detail route and the create response agree field-for-field, and `asker_waiting` is `True` for
  a question with no recorded asker — design D5's presumption, not a stale constant (F80).

### Fixtures, cost and cleanliness

Two fixture projects created (`proj-8288dbd815ea`, `proj-c4444ce0ecda`) and both **deleted**;
project count re-measured at **23**. A project-wide sweep afterwards: 23 projects, 45 jobs,
**0 enabled** — this harness creates no job. Both fixture directories removed. Every real turn bound
to `claude-haiku-4-5`. Confirmed before drawing any conclusion that **no `.py` under `hub/hub` or
`src` is newer than the Hub process start** (pid 22908, started 01:48:32), so the running Hub is the
code that was read.

One fixture problem worth carrying, because it cost a whole harness run: **a project whose git
repository has no commits cannot run a turn.** `git worktree add … HEAD` fails with *"fatal: invalid
reference: HEAD"*, and `POST /agent/trigger` answers **200** with `run_id: null`,
`status: "queued"` and the reason in `waiting_reason`. The product is behaving correctly and says so
clearly; the harness was reading `code == 200` as "the turn started". Not filed — an honest 200 with
a stated reason is the shipped design — but any future fixture needs a `git commit` before its first
turn.

### The review page was edited, not left behind

`spec-queue/review/review-2026-09-01.html` — section 3 gains a row-11 narrative paragraph, a
labelled rule and three table rows; the running total moves **231 → 234**; *"Rows 11–17 remain
untouched"* becomes *"Rows 12–17"*. Section 6's *"pattern with a name"* argument gains the
**counter-example**: row 11 is the case where the route *is* in the bundle and the feature is still
split across two screens that disagree, so a repair scored on "is there a call site anywhere" would
mark this router green. The page's own claims were re-measured after the edit rather than assumed:
zero external link/script/`@import`/http-src/font references, full DOCTYPE/html/head/body wrapper,
`:root` + `prefers-color-scheme` + `body { background: var(--bg) }`, and an `html.parser`
tag-balance walk with no mismatches and nothing unclosed. 42,601 bytes.

`spec-queue/APPROVALS.md` is **not** edited. No new change was proposed this iteration, and the day
window must not write a status token into its own section.

**Next:** the queue is complete again and row 11 is closed. If another firing remains before 17:00,
row 12 — Permissions — is the next row of the sweep, and it has prior coverage under the old
numbering too (`t_row13_row14.py`, `t_row19_crash_card.py`); read those first.

---

## Iterations 9–10 — 2026-09-01, 11:05 → 11:55 +01:00 — D-8: sweep row 12, Permissions

**Iteration 9 died mid-drive and left its work uncommitted.** It claimed the iteration at 11:05
(`cd65a7b`), built the harness, ran it once at ~11:22 (the finding text quotes files stamped
`112238`), edited `FINDINGS.md` and the review page, ran `--teardown`, and then the process ended
before it committed anything. What iteration 10 found on disk was a clean Hub (23 projects, both
fixture directories gone) and a dirty tree: an untracked 703-line harness, three new findings, and
85 changed lines of review page.

**Nothing was taken on trust.** The claims were re-driven from nothing rather than read: the
fixture directories were rebuilt, and the harness was run **four complete times** — twice on the
fixture as it stood and, after a full teardown, twice more starting from an empty disk. Every run:
**83 passed / 1 failed, the same single red.** Four real `claude-haiku-4-5` turns per run (deny,
expiry, allow, and the default-posture comparison), sixteen turns in all.

### What row 12 had, and what it did not

Prior coverage was read before anything was written. `t_row13_row14.py` presses *Allow* once;
`t_row19_crash_card.py` kills the Hub with a card on screen. Between them: allow, and Hub-killed.
Never driven at all, and driven here:

- **Deny.** The operator refuses, the write does not land, the run ends rather than hanging, and
  **exactly one** `permission_denied` event is written rather than two — `_operator_already_refused`
  suppresses the run's own report of a refusal the operator already made, and the only way to see
  that working is to count. It counted 1, four times.
- **Expiry on the agent's own configured timeout.** `Agent.permission_timeout_seconds` was set to
  its 10s floor, the card was left alone, and it expired in **~10s, not the 120s default** — so
  `AW_DECISION_TIMEOUT` really does reach the spawned process, which nothing had ever checked. The
  run's refusal sentence quotes the number back: *"no operator answered within 10s, so this was not
  approved"*, and an expiry is **not** attributed to an operator who never answered.
- **Dismiss**, shipped in migration `0062` and never exercised. A live card is a 409 —
  *"answer it rather than clearing it away"*. An expired one dismisses, keeps its terminal status,
  is idempotent on a second dismiss, and leaves even the `include_expired` list.
- **The `permission_timeout_seconds` bounds**: 5s and 601s refused, 10s accepted and stated back,
  and `null` really clears it — **F219's shape is ABSENT on this route** (the runner route's
  `PATCH {"model": null}` answers 200 and changes nothing; here the loop is `if field in body`).

### Three findings, and two of them are what the greens compare

**F230 (C)** — *Allow crosses the workspace boundary the product enforces everywhere else, and the
card cannot say so.* Driven on **one path under both postures**, same project, same agent, same
tool. Default posture: refused by `mcp_server._decide` with *"…is outside your workspace"*, no card,
no file. `manual`: a card, one click, and the file is on disk in the project root — outside the
agent's worktree. That is the design (`approve_tool_call` routes to `_ask_operator` *instead of*
`_decide`), and it is defensible; what is not is that the operator cannot see which side of the line
they are on. The card schema, read from the live `/openapi.json` rather than from source, has eleven
fields and none of them is the workspace, so `PermissionRequestCard.describe()` renders a bare
absolute path and `requestKind()` labels it *File change* — the same label an in-worktree write
gets. The Hub computed that boundary itself (`agent_trigger.py:1018` sets `AW_WORKSPACE_DIR`); it
just never sends it with the card.

**F231 (C)** — *an approval leaves no record any screen can retrieve.* Measured after a real allowed
turn: no event is written (`record_permission_decision` states outright that only refusals are
persisted, and the fixture project's whole event history after three manual turns contains no
`permission_requested` and no allowed anything), and neither of the two URLs the app ever issues
returns a decided card. The parameter that would, `pending_only=false`, appears **0 times in
`hub/ui/src` and 0 times in the served bundle**. A refusal reaches the timeline; an approval reaches
nothing — while the database has held `status='allowed'`, `decided_by='operator'` and `decided_at`
the whole time. One query parameter away from being answerable, which is why it is filed rather than
argued.

**F232 (D)** — the harness's single red. `dismiss_permission_request`'s docstring is explicit:
*"Only an expired request may be dismissed… An answered one is not shown in the first place."* The
guard refuses `pending` and nothing else, so a `denied` card answers **200** and takes a
`dismissed_at`. Inert on screen — no list the app fetches contains an answered card (F231) — so it
is a D: the contract and the code disagree and only an API client can act on the difference.

### The harness's own defects, found by re-driving rather than by reading

Two, both fixed in the file rather than worked around:

1. **It could not set up its own fixture.** `ensure_project` assumed the directory already existed
   as a git repository with a commit — true only because iteration 9 had made one by hand, and
   `--teardown` deletes it. The next run of the file would have hit iteration 8's carried lesson
   (a repository with no commits cannot run a turn; `git worktree add … HEAD` fails and the trigger
   answers an honest 200 with `run_id: null`). Now `ensure_repo()` creates the directory, `git init`s
   it and makes the initial commit, with the reason in the docstring.
2. **Teardown left `.git` behind on Windows.** `shutil.rmtree(..., ignore_errors=True)` reported
   `exists=True` with nothing but `.git/objects` remaining — git marks loose objects read-only and
   `os.remove` raises on them. Measured, not guessed. Replaced with an `onerror` handler that clears
   the read-only bit and retries; the next teardown reported `exists=False` for both.

A third correction: the module docstring claimed **two** real turns per run. There are **four**.

### A defect in the review page itself, caught by re-reading it

Iteration 9's F230 row contained the path `C:\…\row12-default.txt` with its `\r` collapsed into an
actual line break — so the page rendered *"'C:\… ow12-default.txt'"*, a path with a character
silently eaten. Repaired, and the page's own claims re-measured afterwards rather than assumed: zero
external `link`/`script`/`@import`/http-`src`/font/remote-`url()` references, full
DOCTYPE/html/head/body wrapper, `:root` + `prefers-color-scheme` + `body { background: var(--bg) }`,
an `html.parser` tag-balance walk with no mismatches and nothing unclosed, and **no stray CR anywhere
in the file**. 48,140 bytes.

### Negatives recorded on purpose

- **F215/F225's stale-bundle shape is ABSENT.** All three route strings (`permission-requests`,
  `include_expired`, `/dismiss`) are in the served bundle. Row 12 is the *third* variant of that
  pattern and the subtlest: the router is fully surfaced and what is missing is a **query**, not a
  route. Grepping for orphaned routes finds nothing here; grepping for orphaned *parameters* would.
- **F204/F210's shape is absent.** `decide` with no body at all is a 422 and leaves the card
  pending; the four probes before the deny assertion were checked to have changed nothing.
  Iteration 8's lesson was applied in the file itself: `dismiss` takes **no** body, so a bodyless
  call is its *normal* call and is deliberately kept out of the probe loop, with the reason in a
  comment.
- **Cross-project isolation holds**: the other project can neither decide nor dismiss the card, and
  it is not in that project's list at any width.
- Deciding twice is a 409 (*"already denied; the run has moved on"*), not a silent re-answer;
  deciding an expired card is a 409; every unknown id is a 404.
- The old spelling `{"decision": "allow"}` is refused naming both fields; `allow: null` and
  `allow: "banana"` are 422. `allow: "yes"` **is** coerced to `True` by Pydantic and reaches the
  route — framework-wide lax-bool behaviour, not something this router chose, and recorded as a note
  rather than filed.
- A denied and an expired card both leave the default list; the row is kept and `pending_only=false`
  returns it.

### Cleanliness

Fixtures created and deleted four times over; final state **23 projects**, and a project-wide sweep
afterwards: **23 projects, 23 jobs, 0 enabled**. Both fixture directories confirmed gone from disk.
Every real turn bound to `claude-haiku-4-5`. Confirmed before drawing any conclusion that **no `.py`
under `hub/hub` or `src` is newer than the Hub process start** (pid 22908, started 01:48:32) — the
running Hub is the code that was read. One process mistake, recorded: a run was piped through
`head -20` and died of a broken pipe mid-drive, leaving two fixture projects behind; they were torn
down and the run redone in full, which is why there were four complete runs and not three.

**Review page:** section 3 gains a row-12 narrative paragraph, a second paragraph naming what the
greens compare, and three table rows; the running total moves **234 → 237** (re-counted from
`FINDINGS.md`, not carried: `grep -c "^## F[0-9]"` returns 237); *"Rows 12–17"* becomes *"Rows
13–17"*; section 6 gains the third-variant paragraph and names the cheapest single repair — a screen
that answers *"what have I allowed?"*, since the read side already exists and already works over
HTTP.

`spec-queue/APPROVALS.md` is **not** edited. No new change was proposed, and the day window must not
write a status token into its own section.

**Next:** row 12 is closed and the queue is complete again. Row 13 — Checkpoints — is next if a
firing remains before 17:00.

---

## Iteration 11 — D-9: sweep row 13, Checkpoints

**Claimed at 12:00:21+01:00.** State matched `HEAD` (`ada9fe5`), tree clean, five hours to
`stop_at`. Row 12 was closed and the queue complete, so this took the fourth optional drive — row
13, the last one `next_action` named.

### Prior coverage read first, and deliberately not re-ploughed

Four harnesses already own the checkpoint spine and were read before anything was written:
`t_row15_cutover.py` (generate, render, cut over, continue), `t_row15_chain.py` (lineage, and the
empty span that became F130), `t_continue_branches.py` (`continue`'s refusal and waiting branches),
`t_f131_start_reported_to_its_own_input.py` (cutover queues without scheduling). What none of them
touches:

* **The warning and its dismissal.** `POST /conversations/{id}/dismiss-checkpoint-warning` had
  never been called with anything but a nonexistent id — `t_sweep_conversations.py:92` probes its
  404 and nothing else. The whole `due → dismissed → final` machine was undriven.
* **The agent plane.** `list_checkpoints`, `read_checkpoint`, `recall`, `submit_checkpoint_notes`.
  Grepped across `scripts/drive`: **zero** harnesses set `can_read_checkpoints` or `can_recall`.
  F88 measured this intersection closed on the visibility side in August and it was repaired;
  nothing has exercised it since.

### The harness

`scripts/drive/t_sweep_row13_checkpoints.py`, **70 passed / 5 failed, run twice with an identical
failure set** — thirteen legs, one project with three agents plus a second for isolation, **ten
real `claude-haiku-4-5` turns and four checkpoint generations per run**. **Six** complete runs in
all: two on an earlier version, two more after the assertion corrections below, then a full
teardown and the final pair — fresh, and then again on the state the fresh one left.

### Four findings

**F235 (C) — "Still bounded by each checkpoint's own visibility" is a bound that cannot exist.**
That sentence is the hint under the operator's own checkbox (`AgentSettingsControls.tsx:397`).
`may_read_checkpoint` computes capability ∩ visibility, and the visibility half is open for every
checkpoint there has ever been: exactly two call sites take the argument and **both take it as a
default**, no route sets it, there is no PATCH, and `SELECT visibility, count(*) FROM checkpoints`
on the drive Hub returns `[('project', 14)]`. So `can_read_checkpoints` is all-or-nothing across
the project — driven, not inferred: `peer` was granted read alone and listed and opened an
`author` checkpoint from a conversation it had never taken part in. The same shape as **F221**,
told in the permissive direction, which is the worse one.

**F233 (D) — a warning can be dismissed before it is shown, and that silences it for good.** The
route's only guard is for `final`; a conversation at `NULL` is written straight to `dismissed`,
which `checkpoint_trigger:192` then reads as *"already crossed its threshold, already been offered
a checkpoint, and already declined"* — none of which happened. Driven twice on a conversation held
below its threshold: dismissed at 200, threshold lowered to 2000 tokens, one real turn reading
34,159 tokens, and it never reached `due`.

**F234 (D) — taking the checkpoint answers `due` and `final`, but not a dismissal.** `dismissed` is
missing from the tuple at `checkpoints.py:190`, so the conversation goes on being treated as
"already declined" about a checkpoint that now exists — the exact sentence the comment gives as its
reason for clearing `final`.

**F236 (D) — a stranded note is handed to a later checkpoint as though it were fresh.**
`pending_notes` takes the most recent *unconsumed* note and retires only that one. Three notes, two
checkpoints: the first took the newest, the second took one written two turns *before it*.
`CheckpointNote`'s own docstring gives the reason it should not.

### The control leg, added because half an argument is not one

Legs 3–5 measured that a dismissal is sticky. On its own that says a code path differs, not that
two operators diverge — so leg 12 runs the identical conversation with no dismissal at all. Same
project, same threshold, same button:

| | author (dismissed once) | control (never dismissed) |
|---|---|---|
| crosses threshold | `due` | `due` |
| operator dismisses | `dismissed` | — |
| takes a checkpoint | still `dismissed` | `null` |
| one more turn, crosses again | still `dismissed`, **no offer** | `due`, offered again |

### A B that was three lines from being filed, and was wrong

The first reading of leg 11 was that the cut-over banner filters on
`trigger === 'context_pressure'` (`AgentOutputPanel.tsx:491`) while the operator's own button
writes `trigger="operator"`, and that under `offered` mode — the mode named for offering — nothing
could ever be offered. That is **wrong**. `handleCheckpoint` calls `writeCheckpoint`, which takes
the checkpoint *and cuts over* in one operation (`checkpointOperationStore.ts:70`) without
consulting that banner at all; the banner is for `automatic` mode, where the Hub generated one
unasked. The assertion was rewritten to state the fact rather than the inference, and the same
correction is what moved F233 and F234 from C down to **D** — the app's only take-path also cuts
over, so a live conversation never sits on a stale dismissal, and both defects are API-only today.
Recorded because the wrong version was already drafted.

### Three harness defects of my own, found by re-driving

1. **The evidence channel was the model's prose.** Each agent-plane turn was asked to transcribe
   its tool results into `submit_checkpoint_notes`. One turn in three wrote the marker and forgot
   the transcript, which made *"reading it by id is refused"* pass on an empty string. Replaced
   with `tool_calls()`, which reads `agent_outputs.payload` — the Hub already stores every call and
   its verbatim result under a shared `call_id`. The Hub's record, not the model's account of it.
2. **Three refusal assertions matched words the product never says.** They looked for `not found`;
   the sentence is *"No checkpoint by that id is available to you."* Rewritten to match the `(404)`
   a caller actually sees.
3. **The file was not re-runnable, and only the second run on the first's state could show it.**
   Legs 7 and 8 grant `peer` both capabilities and nothing ever took them back, so the next run
   arrived with `peer` already granted and *"both reader grants are closed by default"* failed on
   the harness's own leftovers — a red that says nothing about the product. Two repairs, because
   one would not have been enough: the default-closed claim moved to a throwaway agent created in
   the leg itself, whose defaults are genuinely fresh every time, and `peer` is explicitly reset to
   ungranted before the ungranted leg runs. The reset earned an assertion of its own — **a grant
   can be revoked, not only conferred** — which nothing had checked either. This is exactly what
   the run-it-twice rule is for, and it is the first time in this window that the rule caught the
   harness rather than the product.

### Negatives recorded on purpose

* **The access model holds, and this is the row's real headline.** Both grants closed by default;
  an ungranted peer's list is `[]` and both `read_checkpoint` and `recall` answer 404,
  indistinguishably from a missing id. Read-granted-but-not-recall is separately enforced: the peer
  read the whole checkpoint, saw `out-…` named in its own *Recorded observations*, and was still
  refused `recall` on that exact id. Granting recall returned the author's text verbatim.
* **F215/F225's stale-bundle shape is absent, and this time the check was built to say so.** All
  four route strings were measured in source *and* in the served bundle and they **agree** —
  including `/rendered`, which has 0 source call sites and is absent from the bundle. The earlier
  version asserted bundle-presence alone and manufactured a red for a route the app simply does not
  use; D-6's lesson, re-learned in a new shape.
* F204/F210's shape is absent; `cutover` with no body at all is a 404, not a 500, and the dismissal
  route — which takes no body — is deliberately kept out of that probe.
* Cross-project isolation holds on all five routes tried.
* Dismissing twice is idempotent; `offered` mode warns rather than spending a model call (no
  checkpoint existed until the operator asked); a checkpoint taken with no notes says so in its own
  body, and one taken with them does not.
* The backend does not distinguish a checkpoint's trigger: an `operator`-triggered one cuts over
  happily and archives its predecessor.
* **`final` is unreachable in a drive** and is recorded as untested rather than passed:
  `needs_final_warning` requires ≥92% of the window and a Haiku fixture turn reads 17%.

### Cleanliness

Fixtures created and deleted twice over; project count back to 23 after each teardown; a
project-wide sweep afterwards reporting **0 enabled jobs**; every real turn bound to
`claude-haiku-4-5`; no `.py` under `hub/hub` or `src` newer than the Hub process (pid 22908,
started 01:48:32), so the running Hub is the code that was read. `git diff --stat 556bb37..HEAD`
over `src`, `hub/hub` and `hub/ui/src` is still empty — no product code on this branch.

### Review page

Section 3 gains three row-13 narrative paragraphs, a separator and four table rows; the running
total moves **237 → 241** (re-counted from `FINDINGS.md`, not carried); *"Rows 13–17"* becomes
*"Rows 14–17"*. **A defect in the page itself was found and fixed:** F226, a jobs finding, was
sitting inside the row-11 questions block — iteration 8 inserted its rows ahead of it instead of
after it — so an operator reading the questions block met a jobs row. Moved back into row 10.

The page's own checks are now a **script rather than a habit**: `scripts/drive/check_review_page.py`
(self-containment, full wrapper, `:root` + dark scheme + body background, an `html.parser`
tag-balance walk, no stray CR, and one id per finding row scoped to section 3). It was written
after the same checks had been retyped by hand for every edit today and one of them had already
slipped through once. It passes on the current page: 54,419 bytes, 22 findings F215–F236.

`spec-queue/APPROVALS.md` is **not** edited. No new change was proposed, and the day window must
not write a status token into its own section.

**Next:** row 13 is closed and the queue is complete again. Row 14 — Accounting — is next if a
firing remains before 17:00.

---

## Iteration 12 — 2026-09-01T12:36+01:00 → D-10, sweep row 14 (ACCOUNTING)

**Position on arrival.** Branch `autonomous/2026-09-01-daily`, clean, `4e906bb` at HEAD — exactly
what `STATE-day.json` claimed. Iteration 11 had released the heartbeat at 11:53 with the queue
complete and row 14 named as the next unit "only if a FULL iteration remains". At 12:35 against a
`stop_at` of 17:00, it did. Nothing new had been queued by the operator. Limits re-read from state
and honoured: cycle branch only, nothing outward-facing, `:8011` and a fresh fixture, Haiku on every
real turn, no job left enabled, explicit paths staged.

**Prior coverage read first, as the four drives before this one did.** Two harnesses already touch
accounting and neither reads the operator's surface: `t_row18_budget_reason.py` owns the budget as a
*waiting reason* (it PATCHes and clears the budget, parks an autonomous entry, and compares the
scheduler's predicate with `GET /queue/{agent}/status`'s) and never calls `GET /accounting` at all;
`t_sweep_surface.py:161-166` probes `GET /accounting` for a 200 and `PUT /settings` for three
budget shapes. F92 — the crash-reconciliation finding, fixed eight days ago — closed with a
question it recorded for the operator rather than driving: `worker_invocations` are "real model
calls made on the operator's behalf [that] appear on no aggregate surface and in no budget."

So the drive went where none of them had: the per-conversation rollup (zero call sites anywhere),
the two budget write paths against each other, and what the budget's own promise is worth.

**The harness.** `scripts/drive/t_sweep_row14_accounting.py`, ten legs, **73 passed / 4 failed, run
twice with an identical failure set** on the state the first run left. Three real
`claude-haiku-4-5` turns, one checkpoint generation and one generated conversation title per run,
plus a live SSE subscriber held open across both budget writes — a ticket fetched and a stream
opened exactly as a browser does, because "what is a listening dashboard told" is a measurement,
not a reading of `useSSE.ts`.

### Four findings, F237–F240

* **F240 (B) — with the budget exhausted and "spending is paused" on screen, the Hub spent $0.042
  that reaches no total and no budget.** Driven in that state (`{"limit_tokens": 17070,
  "used_tokens": 34140, "remaining_tokens": 0, "exhausted": true}`): one checkpoint, two real
  `claude-haiku-4-5` calls, `42,157 usd_micros` over 732 tokens, `outcome: ok` on both. The project
  total did not move, `budget.used_tokens` did not move, no `turn_usage` row was written. The
  conversation titler is the same hole *without even the audit row* — a real call produced the
  title *"Agent outputs ROW14D as instructed"* and wrote nothing anywhere at all. This is F92's
  parenthetical question driven, with the titler added (F92 did not name it) and with the fact that
  makes it a defect rather than a design question: **the gap is not bounded by the budget.**
* **F237 (B) — two controls set the token budget and one leaves every surface that displays it
  stale.** `ProjectSettingsPanel.tsx:144` → `PUT /settings` → `project_settings_updated`, which
  `useSSE.ts:551` answers by invalidating `['projects']` and never `['project', id, 'accounting']`.
  Measured on the wire: `PATCH` → `['accounting_budget_updated']`, `PUT` →
  `['project_settings_updated']`. The server is right; the always-mounted `BudgetExhaustionNotice`
  is not, while `redrain_queued_agents` has already applied the new budget.
* **F238 (C) — half of all budget changes are missing from the project's activity history.**
  `accounting.py:57` persists; `projects.py:526-529` broadcasts without persisting. One new history
  row after the PATCH, none after the PUT.
* **F239 (C) — the per-conversation rollup answers 200 with a zeroed summary** for
  `conv-does-not-exist`, and the identical body for a real conversation belonging to a second
  project. Isolation holds; the refusal is what is missing. Same shape as F216, and every sibling
  conversation route 404s.

### Negatives recorded on purpose

The per-turn accounting is **sound**, and the row is worth reading for that first. One measured row
per turn with the runner, the model and a real token total; the project total is exactly the sum of
the measured rows and `measured_turns` counts them; **every ended run has exactly one accounting
outcome and no run has two — F92's invariant, re-driven**; an empty project reports `null` totals
and `preferred_display: {"kind": "unavailable"}` rather than `$0.00`; `remaining_tokens` clamps at
zero rather than going negative; cross-project totals do not mix and neither do the agent lists; an
operator message really does still run with the budget exhausted and its cost really is counted;
both routes refuse an unauthenticated caller and an unknown project is a 404. `preferred_display`
carried genuine rate-limit telemetry — `five_hour`, `status: allowed`, `overageStatus: rejected`,
`out_of_credits` — which `accountingDisplayLabel` renders in operator language rather than as raw
JSON.

### Three harness defects of my own, all found by re-driving

The run-it-twice rule earned its place for the second window running.

1. **The event-history rows key on `type`, not `event_type`.** The first version read the wrong key
   and printed `[None]` for every event — which looks exactly like a Hub that records nothing, and
   would have been filed as one.
2. **Six assertions were absolute where the fixture is cumulative.** `measured_turns == 1`,
   `total == the row`, `the rollup matches the project total` — all true on a fresh project and all
   red on the second run against four turns of history. Rewritten against a baseline captured in
   the leg.
3. **A false PASS, which is worse than a red.** *"The newest history entry is an
   `accounting_budget_updated`"* is satisfied by the **previous** write's row — every budget change
   persists the same type — so it reported a pass for a route that had persisted nothing at all. It
   only became visible because run 3 flipped it green while the SSE assertion beside it stayed red.
   Fixed by comparing rows with their timestamps, and written into the finding, because the wrong
   version was already drafted.

Mistake #2 from the standing list also fired and was caught: the title check asked whether the
generated title *lacked my marker*, and the titler quoted the marker — *"Agent outputs ROW14D as
instructed"* is a perfectly good generated title that the harness called a failure. The predicate
is now "the title is no longer the truncated message", which is what the fallback actually is.

### Cleanliness

Two fixture projects plus a throwaway created and deleted; **project count back at 23**; a
project-wide sweep afterwards reporting **0 enabled jobs**; every real turn bound to
`claude-haiku-4-5`; no `.py` under `hub/hub` or `src` newer than the Hub process (pid 22908,
started 01:48:32), so the running Hub is the code that was read. `git diff --stat 556bb37..HEAD`
over `src`, `hub/hub` and `hub/ui/src` is still empty — no product code on this branch. The
harness resets the budget, the checkpoint configuration and the title mode in leg 10, and leg 1's
empty-project assertions run against a project created and destroyed inside the leg, because a
fixture is only empty once.

### Review page

Section 3 gains four row-14 narrative paragraphs and four table rows; the running total moves
**241 → 245**; *"Rows 14–17"* becomes *"Rows 15–17"* and *"Rows 10, 11, 12 and 13"* becomes *"Rows
10 through 14"*. `py -3.11 scripts/drive/check_review_page.py` passes: 61,073 bytes, 26 findings
F215–F240, total stated 245. Every inserted block was re-read on disk afterwards.

`spec-queue/APPROVALS.md` is **not** edited. No new change was proposed, and the day window must
not write a status token into its own section.

**Next:** row 14 is closed and the queue is complete again. Row 15 — Worktrees — is next if a
firing remains before 17:00, and only if a full iteration does.

---

## Iteration 13 — 2026-09-01T13:00+01:00 — D-11: drive row 15 (worktrees)

**Claimed** at 13:00 with the queue already complete. `next_action` named row 15 — Worktrees,
per-agent isolation and conflicts — as the next optional drive if a *full* iteration remained. It
did: `stop_at` is 17:00 and the branch heartbeat had been released at the end of iteration 12.
Branch and `git log` matched `STATE-day.json` exactly (`3a1a552`, clean tree).

### The unit

Harness kept: **`scripts/drive/t_sweep_row15_worktrees.py`**, **40 passed / 14 failed, twice, the
second run on the state the first left.** Nine legs, three real `claude-haiku-4-5` turns per run,
against the `:8011` Hub (pid 22908, started 01:48:32; `Get-ChildItem hub\hub,src -Filter *.py`
where `LastWriteTime > StartTime` returns **0**, so the running Hub is the code that was read).
Two fixture projects created by the script and destroyed by `--teardown`: `aw-drive-row15`, a git
repository seeded with `calc.py`, and `aw-drive-row15-nogit`, deliberately not a repository.

**Prior coverage was read first, as `next_action` required.** `t_row16_worktrees.py` exists under
the old numbering and drove the mechanism on 2026-08-29 (`FINDINGS.md:8093`). It prints and
asserts nothing, and it stops at the point where the conflict report exists. This file starts
there and asks the five questions it did not: who reads the report, what it is compared against,
what happens to it when a task ends, what the `/conflicts` route does to an agent named
`conflicts`, and whether the un-isolated mode the settings page describes is reachable.

### Ten findings, F241–F250 — running total 245 → 255

* **F241 (B)** — the conflict report is computed, correct, and **read by nothing**. Zero call
  sites for `worktrees/conflicts` under `hub/ui/src`; absent from the served bundle, while the
  sibling *list* route the panel does call is present in the same file. The Hub knew two task
  branches conflicted on `calc.py`; no screen shows it. The requirement's own words are *"divergent
  changes surface as a conflict"*.
* **F242 (B)** — one `PATCH /agents/{name} {"config": {"read_only": true}}` moves an agent off
  isolation mid-task, and its next **task-bound** turn writes uncommitted into the operator's
  checkout. Three measured consequences: the live task checkout vanishes from the agent's own
  settings page while the directory stays on disk; the turn runs in the project root; nothing
  commits it, so the board reads `completed` and the only copy is a working-tree modification.
  `AgentSettingsPage.tsx:290` declines to offer this control **and says why, for this exact
  reason** — the API offers it with none of the protection that comment says it would need.
* **F243 (C)** — `{"config": {}}` and `{"config": null}` both answer 200 and clear nothing; only
  an explicit `{"read_only": false}` reverses the flip. **Exactly F219's shape on a second route.**
* **F244 (C)** — `OperatorAgentResponse` carries no `config`, so an agent moved off isolation is
  indistinguishable on the roster from every other one.
* **F245 (B)** — the conflict check **never compares a task branch against the branch it will
  merge into**. With an operator commit on `master`, `git merge-tree` reports `calc.py` against
  both task branches and the endpoint reports only task-versus-task.
* **F246 (C)** — finishing a task takes its conflict off the report while the divergence remains.
  The release event written in the same transaction carries `unmerged_commits` at severity `warn`.
* **F247 (C)** — `GET /worktrees/{agent}` invents a workspace for an agent that does not exist.
  Third instance of this shape in three days, after F216 and F239.
* **F248 (C)** — an agent legally named `conflicts` can never have its workspace read: the
  `/conflicts` route is declared first, on purpose, and hands that agent's settings page an array.
* **F249 (D)** — the worktrees list cannot say *"not a repository"*; `[]` is the same answer a
  healthy empty repository gives, while the per-agent route has `unavailable_reason` and uses it.
* **F250 (D)** — nothing invalidates `['project', id, 'worktrees']`, so the panel does not notice
  a checkout being created.

### Negatives recorded on purpose

The mechanism **holds**, and the row is worth reading for that first. Reading the surface
provisions nothing — three reads left the checkout directories identical and `provisioned` matched
`isdir` of the very path reported. Two parallel task-bound turns each got their own checkout on
their own branch, each agent's function present on its own branch and absent from the other's and
from `master`. `GET /worktrees/conflicts` named both **task** workspaces and `calc.py`, and its
answer is *equal to what `git merge-tree --write-tree --name-only` produces* — checked against git
rather than against itself. Release keeps the branch and records what it leaves unmerged. A
**self-registered** `read_only` agent shares the project checkout exactly as designed. And the
one-run-per-task refusal earned its keep by accident: an agent holding two live tasks read the
wrong one off `list_tasks`, implemented it, and was refused with *"A run finishes the task it took,
and takes at most one"* — which it read correctly and acted on.

### Two harness defects of my own, both found by re-driving

The run-it-twice rule earned its place for the third window running, and this time the harness bug
**was** the finding.

1. **The teardown leg cleared a config with `{"config": {}}` and the clear did nothing.** Run 2
   therefore measured an agent that was still flipped: no task checkout, a turn in the project
   root, work uncommitted. That is F243 causing F242 to be visible, and neither would have been
   found by a single run. The reset is now `{"read_only": false}` and leg 9 asserts the agent is
   isolated again.
2. **A dirty board makes the next run measure the wrong work.** Run 2's `beta` held two live tasks
   and chose the wrong one. Leg 4 now sweeps every non-terminal task belonging to the drive's
   agents to a terminal status before creating its own, and leg 9 restores the project checkout
   with `git checkout -- calc.py` so the next run's leg 6 does not commit this run's stray work
   onto the base branch — which run 2 did, and which cost twenty minutes to trace.

Mistake #7 from the standing list (write no absolute counts) was designed out from the start:
every count in this harness is a delta or a match against disk.

### Cleanliness

Two fixture projects created and deleted; **project count back at 23**; a project-wide sweep
afterwards reporting **0 enabled jobs**; every real turn bound to `claude-haiku-4-5`; no `.py`
under `hub/hub` or `src` newer than the Hub process. `git diff --stat 556bb37..HEAD` over `src`,
`hub/hub` and `hub/ui/src` is still empty — no product code on this branch.

### Review page

Section 3 gains four row-15 narrative paragraphs and ten table rows; the running total moves
**245 → 255**; *"Rows 15–17"* becomes *"Rows 16–17"* and *"Rows 10 through 14"* becomes *"Rows 10
through 15"*. `py -3.11 scripts/drive/check_review_page.py` passes: 73,830 bytes, 36 findings
F215–F250, total stated 255. Every inserted block was re-read on disk afterwards.

`spec-queue/APPROVALS.md` is **not** edited. No new change was proposed, and the day window must
not write a status token into its own section.

**Next:** row 15 is closed. Row 16 — Logs, Events and SSE — is next if a firing remains before
17:00, and only if a full iteration does.

---

## Iteration 14 — 2026-09-01T13:25+01:00 — D-12: drive row 16 (logs, events, SSE)

**Position on arrival.** Branch `autonomous/2026-09-01-daily` at `6d8ebbc`, clean, matching
`STATE-day.json` exactly. The required queue has been complete since iteration 6; iterations 7–13
took six optional drives (rows 10–15). `next_action` named row 16 — Logs, Events and SSE — *only
if a full iteration remained*. At 13:25 against a 17:00 stop, it did. Taken.

**Prior coverage, read first.** `grep` over `scripts/drive` for SSE, `/events`, `/logs` and
`event_logs`: **nothing**. No harness in the directory has ever opened an SSE connection; every one
of them polls the database or the REST surface. `FINDINGS.md` had no finding against `events.py`,
`logs.py` or `sse.py`. So this row was genuinely unploughed, and the harness is the first client on
the stream.

**Harness.** `scripts/drive/t_sweep_row16_logs_events_sse.py`, ten legs, **44 passed / 15 failed**,
run **twice on the state the first run left with an identical failure set** (four runs in total —
the first two were on earlier revisions of the file, and each change between them was an *added*
check, not a moved goalpost). One fixture git repository the harness creates and destroys, one
second project that exists only to be deleted while two streams watch, one real
`claude-haiku-4-5` turn per run. Its SSE client re-implements `feedSSEChunk` from `useSSE.ts` so
what it counts is what the shipped client would see.

### Seven findings, F251–F257

* **F251 (B)** — the Hub broadcasts **17 event names the shipped client discards** before any
  listener runs. `useSSE.ts:335` gates dispatch on a hand-written 44-name array; a frame whose name
  is not on it is dropped — not dispatched, not buffered, not counted as activity. Measured against
  the **served bundle** (whose array matches the source member for member) and every literal name
  passed to `sse_manager.broadcast`. Three were **watched crossing the wire**: `job_archived` and
  `project_deleted` beside `job_created`/`job_updated`, which survive; `conversation_updated` after
  a real rename. **Nine of the seventeen have UI code written for them** — `useCheckpoints`
  subscribes to `checkpoint_ready`/`conversation_cut_over` and its docstring names the exact failure
  it was added to fix, and `eventSummary.ts` has an operator-facing sentence for six more. The same
  defect was patched name-by-name once before (`useSSE.ts:530`, six loop events) and the test suite
  has two tests of that shape; **no test compares the array against the Hub's vocabulary.**
* **F252 (B)** — the Logs screen renders the **oldest 500 events a project ever recorded**.
  `useLogs` asks `limit=500` with no offset; `list_logs` orders `timestamp.asc()`. Live mode
  refetches the same oldest 500. Not synthetic: on `proj-18e5d4e0`, a real project the drive only
  read, the screen's request ends **2026-08-24 11:26** and the newest event is **2026-08-27 04:15**.
  `/events/history`, over the same table, returns the newest correctly.
* **F253 (B)** — a subscriber that falls behind **loses events silently**: 1,426 of 3,000 dropped,
  the same count on both runs, `QueueFull` at 256 deep. No `id:` on the wire, no sequence, no gap
  marker, and no disconnect — so `onSseReconnect`'s `invalidateQueries`, the client's only catch-up
  path, never fires. Half a lost stream looks exactly like an idle project.
* **F254 (C)** — an **SSE ticket outlives the project it names**: after deletion the ticket still
  opens a 200 stream while the header path answers 404 and no new ticket can be minted. Fourth
  instance in three days of a route answering about an entity that does not exist (F216, F239,
  F247).
* **F255 (C)** — a malformed `since` is caught and ignored, so the route answers the whole window as
  though no filter had been asked for. C rather than B because no shipped caller sets `since`.
* **F256 (D)** — `/logs/agents` offers names that are on no roster; `POST /logs` takes `agent` from
  the body unchecked and the route unions the roster with every distinct string ever logged.
* **F257 (D)** — filtering by a severity that cannot exist answers 200 with an empty list, so a typo
  reads as *"no events of that kind"* rather than *"no such kind"*.

### Negatives recorded on purpose

The transport is sound where it was checked. The instance envelope is stamped with the project that
produced the event and a `project_id` **forged inside the payload does not become it**. The project
stream and the instance stream both deliver the same write; `/logs` and `/events/history` both
persist and return it. Ticket binding holds on five refusals — a project ticket refused on another
project's stream and on the instance stream, a raw API key in `?token=` refused, an operator ticket
refused on a project stream and accepted on the instance one. Every event kind a **real turn**
produced (seven: `run_started`, `run_completed`, `agent_output`, `agent_session_changed`,
`context_warning`, `queue_entry_queued`, `queue_entry_delivered`) is on the allowlist and reaches a
screen — which is why the allowlist gap has survived: the happy path is covered and the edges are
not. An unknown severity on the write path is normalised to `warn`. Deleting a project leaves **no
orphaned `event_logs` row**.

### One harness correction, and why the run-it-twice rule earned its place again

The first `--- LEG 6` attempt pushed 700 events at a stalled subscriber and **lost nothing** — the
socket buffers absorbed the burst and the assertion passed. That would have been a false negative
published as "the drop path could not be reached". Raising the burst to 3,000 forced it, twice,
with an identical count. A pass that came from not pushing hard enough is worth exactly as little
as a red that came from a broken fixture.

Also corrected between runs: leg 10's clean-tree assertion counted `.agentweave/` as stray work. It
is the Hub's own project marker, written by `POST /projects/open` — the product's, not the
harness's — so the assertion now ignores it and still fails on anything else.

One limit was respected narrowly rather than by accident: the "busiest other project" probe would
have made an API call against `proj-18e5d4e0`, which the standing limit forbids **driving**. The
check now mirrors the route's own query read-only in SQL for the two protected ids and requests it
for any other — sound only because the API and the mirror were shown to agree on the fixture, and
they did, to the microsecond.

### Cleanliness

Two fixture projects created and deleted; **project count back at 23**; `enabled=1` jobs across the
whole instance: **0**; orphaned `event_logs` rows: **0**; `event_logs` total back to 4,764, the
number it held before the drive. Every real turn bound `claude-haiku-4-5`. No `.py` under `hub/hub`
or `src` is newer than the Hub process start (01:48:32), checked before any conclusion was drawn.
`git diff --stat 556bb37..HEAD` over `src`, `hub/hub` and `hub/ui/src` is still empty — no product
code on this branch.

### Review page

Section 3 gains six row-16 narrative paragraphs and seven table rows; the running total moves
**255 → 262**; *"Rows 10 through 15"* becomes *"Rows 10 through 16"* and *"Rows 16–17 … remain
untouched"* becomes *"Row 17 … is the only one left untouched"*.
`py -3.11 scripts/drive/check_review_page.py` passes: 83,841 bytes, 43 findings F215–F257, total
stated 262. Every inserted block was re-read on disk afterwards, including the codepoints of the
two em dashes in the rewritten sentence.

`spec-queue/APPROVALS.md` is **not** edited. No new change was proposed, and the day window must not
write a status token into its own section.

**Next:** row 16 is closed. **Row 17 — Messages — is the last row of the sweep**, and is next if a
firing remains before 17:00 and a full iteration fits.

---

## Iteration 15 — 2026-09-01T13:50+01:00 — D-13: drive row 17 (messages), the last row of the sweep

**Position on arrival.** Branch `autonomous/2026-09-01-daily` at `d3310e3`, clean, matching
`STATE-day.json` exactly. The required queue has been complete since iteration 6; iterations 7–14
took seven optional drives (rows 10–16). `next_action` named row 17 — Messages, the last row — only
if a full iteration remained. At 13:50 against a 17:00 stop, it did. Taken.

**Prior coverage, read first.** Across the 95 files in `scripts/drive`, exactly one line had ever
touched the messages route surface: `t_hop.py:38`, a bare `GET /projects/{P}/messages` whose body is
printed and never asserted on. `FINDINGS.md` had no finding against `api/v1/messages.py`. The
*queue* half of peer mail is thoroughly ploughed (T-HOP, row 7, `t_queue_attrition.py`); the
*messages* half never had been.

**Harness.** `scripts/drive/t_sweep_row17_messages.py`, eleven legs, **82 passed / 0 failed**, run
**twice on the state the first run left with an identical assertion set** (four runs in total; the
two earlier ones were on earlier revisions). Two fixture projects with the same agent names, three
agents, one archived agent, one real `claude-haiku-4-5` exchange per run — alpha calls
`send_message`, bravo's turn starts. Both fixtures deleted at the end; project count back at 23.

### Seven findings, F258–F264

* **F258 (B)** — every message the operator sends through `POST /messages` is **born one hop past
  the budget**. `hop_depth = hop_budget + 1` is the default, corrected only when the caller supplies
  a live `run_id`, which only a running agent has. 201 Created, `queue_chain_suspended` at *warn*,
  depth 7 against a budget of 6, no turn. Not a race: five such entries were still queued at depth 7
  after the recipient ran **two** later turns for other entries. The queue panel shows them and
  explains them as *"hop budget exhausted"* — a chain that never hopped — and attributes the
  operator's own send to an agent. Releasing the entry re-bases it to 0 and the turn starts
  immediately (measured twice), so the defect is an undocumented second call, not lost mail.
  `docs/reference/hub-api.md:25` advertises the route.
* **F259 (B)** — **nothing in the product ever marks a message read.** `msg.read = True` occurs once
  in the whole Hub; its only caller is `useMarkRead` → `MessageCard` → `MessagesFeed`, a screen that
  ships in no bundle. Delivery does not mark read either — the live turn's message was delivered,
  answered, and left `read = 0`. The StatusBar renders the unread count as its `N msgs` chip beside
  `N tasks` (active) and `N questions` (unanswered), both of which fall. Measured read-only across
  the instance: **82 messages in four pre-existing projects, 0 read**, oldest unread 2026-08-22.
* **F264 (B)** — `_pending_loop_request` (`scheduler.py:415`) selects the loop's pending-request
  reason out of `messages` with **no `project_id` filter**, so the row whose content becomes the
  operator-facing reason can belong to another project. F259 removes the only other bound: the
  `read == False` predicate excludes nothing. **Mirrored read-only, not driven** — the branch needs
  an agent-created loop — and the mirror was validated against the scoped form first.
* **F260 (C)** — row 17's whole operator surface is unreachable: three routes, three hooks, three
  components (358 lines), no route, no sidebar entry, tree-shaken out of the served bundle. It took
  the only caller of the read flag and of both query filters with it.
* **F261 (C)** — the recipient is checked against the roster and **the sender is not**, so a name
  nobody registered becomes a listed agent: `GET /agents` falls back to "names seen in 24h of
  activity" with message senders among its sources, and a synthesised name has no row to archive. It
  lists with runner `native` on the roster every agent-picker reads, while sending *to* that same
  name answers 404. F256's shape one route over.
* **F262 (C)** — `?conversation=` means an agent **pair** split on `":"`; a real conversation id has
  no colon, so the filter is dropped rather than refused and the route answers with the project's
  entire mail (26 of 26). F255's shape one row over.
* **F263 (D)** — `sort` reverses only for the exact literal `desc`; `DESC`, `descending`, `newest`
  and `()` all answer 200 ascending, so the default page is the **oldest** hundred messages a
  project ever recorded. F252's shape on a second route.

### Negatives recorded on purpose

Peer mail sent by a real agent works end to end: attributed to the run that sent it, sender derived
by the Hub rather than supplied, entry inside the hop budget, delivered, recipient's turn started.
Isolation holds everywhere it was checked except F264's query — one project's mail is not listed by
another, a cross-project `PATCH /read` is refused 404, a foreign `conversation_id` 404, a foreign
`task_id` refused. So are an unknown message type, a body past 10,000 characters, an archived
recipient (409, with the sender's own content handed back), an archived conversation, and
`conversation_id` combined with `start_new_thread`. `PATCH /read` itself is correct — idempotent,
stamps `read_at`, drops the message from the inbox listing, moves the status count.

### Two harness corrections, both found by the run-it-twice rule

1. Setup aborted on the second run: leg 5 archives an agent, and `GET /agents` excludes archived
   rows by default, so `ensure_agent` believed the agent it had archived itself no longer existed.
   Now asks `?lifecycle=all`.
2. Leg 2 asserted `pending == total` — true only on a virgin project, false on the second run
   because the *first* run had marked one message read deliberately. Rewritten as a delta against a
   baseline captured in the leg: `pending == total - (reads this harness made earlier)`. The
   instance-wide claim carries the "nobody else has ever read one" half.

Both are the same rule as D-12's leg-10 correction: an assertion that only holds on a fresh fixture
is a goalpost, not a measurement.

### Cleanliness

Two fixture projects created and deleted; **project count back at 23**; `enabled=1` jobs across the
whole instance **0**; orphaned `messages` rows **0**; orphaned `inbound_queue_entries` rows **0**.
Every real turn bound `claude-haiku-4-5`. No `.py` under `hub/hub` or `src` is newer than the Hub
process start (pid 22908, 01:48:32), checked before any conclusion was drawn. The two protected
project ids were read in SQL only and never called. `git diff --stat 556bb37..HEAD` over `src`,
`hub/hub` and `hub/ui/src` is still empty — no product code on this branch.

### Review page

Section 3 gains seven row-17 narrative paragraphs, a `— row 17, messages —` separator and seven
table rows; the running total moves **262 → 269**; the closing sentence changes from *"Row 17 … is
the only one left untouched"* to **every row of the sweep now closed**, all 17 feature areas driven
at least once. `py -3.11 scripts/drive/check_review_page.py` passes: 96,224 bytes, 50 findings
F215–F264, total stated 269. Every inserted block was re-read on disk afterwards, and the
running-total anchor had to be built from `chr(8212)` — the paragraph's em dashes are literal U+2014
in the file, not `&mdash;` entities, and the first attempt matched nothing.

`spec-queue/APPROVALS.md` is **not** edited. No new change was proposed, and the day window must not
write a status token into its own section.

**Next:** the sweep is complete — rows 1 through 17 have all been driven. Nothing is outstanding.

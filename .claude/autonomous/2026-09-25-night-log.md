# 2026-09-25 night window

Newest entry at the BOTTOM. Playbook: `.claude/loops/night-window.md`.

Armed 2026-09-25T22:55:03+01:00 on `autonomous/2026-09-25-daily` (parent master@fca808d), model opus.

## Iteration 1 — compose (2026-09-25 night)

CI verdict for inherited sha 7a2bb38: `in_progress`, created 21:55Z (minutes old, not hung). Recorded; next firing re-reads.
Branch `autonomous/2026-09-25-daily` matches STATE; log matches `git log` (HEAD 7a2bb38).
Backlog page refreshed (`backlog_page.py`): 124 open findings (+F457).
Tree check: `py -3.11 -m pytest tests/ -q` → 553 passed, 3 skipped, 1:06. Hub suite not run (too long; per-item runs follow).
APPROVALS `## 2026-09-25` is today's section and carries an `ORDER:` of seven changes → queue is that ORDER verbatim, each as `-impl` then `-drive` (which also archives). The two R1 changes are APPROVED but explicitly not tonight's.

## Iteration 2 — a-task-is-attended-only-by-a-turn-that-will-reach-it IMPL

CI for 7a2bb38 / 053493c: both `in_progress` when read (minutes old); next firing re-reads.
Built per tasks.md: `run_task_binding.task_attendance` (`Attending`, `TaskAttendance`; head read through `select_turn`) replaces the two old helpers; `decide_firing` reads the pair (F154 surfacing, F70 guard with `wedge_deferred`, resume arm without the held qualifier, new refused-work arm, `surfaced_rows`); `_roster_availability` uses `has_turn`; `_refused_review_reason`, `_refused_work_reason` (title-then-refusal fit), `_wedged_review_reason` reworded. Old predicate tests rewritten against `task_attendance` (D6).
New file `hub/tests/test_a_task_is_attended_only_by_a_turn_that_will_reach_it.py`: 25 tests through the real firing (`_fire` x3) — F370 both insertion orders + control, F368 (schedule_agent patched), refused head surfaced/not re-briefed/held/behind-another-task/withdraw exit, running-no-task control, F371 legs, wedge_deferred, unit cases for attendance, sentence fit.
Verified: hub/tests 4865 passed 86 skipped (claude stripped from PATH), tests/ 553 passed, ruff and black clean. NOT done: tests 1.4c budget/paused legs, 1.12 extra case, 1.14 (noted in tasks.md); new tests were not run against the old code (they import the new API), so "fails today" is by the R1 measurements only. Behaviour drive is the next item.

## Iteration 3 — a-task-is-attended-only-by-a-turn-that-will-reach-it DRIVE

CI for 9fbfb96 was `in_progress` when read (4 min old); prior pushes green. Next firing re-reads.
Drive Hub :8031 from source, fresh profile `drive0926` (`drive0925` already existed from an earlier drive, so not reused), operator key set by env. Harness `scripts/drive/d0926_attendance.py` (API for project/runner/agents/flow/task/presses/withdraw; direct SQLite only for the provider-hold row and queue entries in states a real refusal leaves — labelled in the file).
- F370: held `dev`, peer entry queued first then dev's briefing; 3 presses → dev's job entries stayed 1, each press 409 "held until ...". 
- F368 (refused head): idle `dev2`, briefing entry `delivery_attempts=1` + waiting_reason; 3 presses → no new entry, 409 naming the task, the refusal's words and "withdraw that input", recorded as one skipped JobRun with tick_count 3.
- Exit: DELETE the entry, press once → 200, a Haiku run bound to the task ran to completed.
- F371 (review arm) NOT driven live; covered by 25 real-firing tests only. Tasks 1.4c(b,c), 1.12, 1.14 still unwritten.
Archived (`openspec archive -y`, specs synced into agent-flows and agent-loops). FINDINGS: F368/F370 fixed 9fbfb96, F371 fixed in code, not driven. All jobs disabled; drive Hub stopped.

## Iteration 4 — a-review-no-reviewer-can-approve-goes-to-the-operator IMPL

CI for 7a2bb38, 053493c, 9fbfb96, 15cf887: all `success` (read via `gh run list`).
Built: `requirement_gate.ApprovalHold` + `approval_held_for_operator` (DB short-circuit before `evaluate`; savepoint; `TimeoutExpired`/`OSError` = held "could not ask git"; any other raise = warning + not held; grant read through `requirement_evidence.may_accept`; drifting/diagnostics operator-only always). `task_integration.GIT_TIMEOUT_SECONDS` ContextVar (60 default, 5 for the predicate); the gate's four git call sites (`is_repository`, `branch_exists`, `task_branch_tip` via `merge_targets`, `would_conflict`) run through `asyncio.to_thread`. `_answer_failed_review` asks it for declared and availability paths and surfaces with D4's sentence; `decide_firing`'s F154 branch: refused delivery → approval held (`_approval_held_reason`, remedy first, title trimmed to 500) → wedge sentence.
New file `hub/tests/test_a_review_no_reviewer_can_approve_goes_to_the_operator.py`: 12 tests (1.1, 1.2, 1.3, 1.6, 1.6b, 1.6c x4, 1.6d, 1.7, 1.7c) staged with real git/evidence via the F122 helpers; 10 of 12 fail against the pre-change hub/hub (stash check), the other two are controls.
Verified: hub/tests with claude stripped from PATH, `-n auto`: 4876 passed, 86 skipped, 2 failed (`test_database_named_before_opening ...real_process`, `test_pty_runner ...terminate_stops_a_long_running_process`); both pass when run alone (41 s, 36 passed) — load flakes, and pty_runner also passes on the stashed tree. ruff, black clean. `tests/` (CLI) not run: no `src/` change.
NOT done: tests 1.4, 1.5, 1.7b, 1.8 (unwritten, marked in tasks.md); D3's `Waiting: {pieces}` clause omitted; no live drive yet (next item). Not run against a real hung git.

## Iteration 5 — a-review-no-reviewer-can-approve-goes-to-the-operator DRIVE

CI: 15cf887 success; a8a0b32 in_progress when read (minutes old). Branch and log match STATE.
Drive Hub :8033, fresh profile drive0926b, named key. `scripts/drive/d0926_review_hold.py` (reuses t_d0915's setup): alpha authored with evidence naming a commit (left awaiting), beta and gamma created idle, flow fired, real Haiku beta reviewed and ended with no verdict.
- Result: task stayed `under_review`/beta; gamma's queue empty; `run_diverged` outcome `surfaced` carrying the gate sentence and "Nobody else has been asked to review it"; the flow card's stall_reason "Approval of <task> waits on you: accept or reject the evidence ...". Exit: operator accepted the evidence, PATCH approved → 200.
- New F458: the divergence reason also carries the gate's "still running the turn ... clears itself" arm, false at that moment.
- Not driven: hung-git (1.6c/1.6d) and the UI; unit tests only. Tests 1.4/1.5/1.7b/1.8 remain unwritten.
Archived (agent-flows synced). F374 marked fixed. Drive Hub stopped; job disabled.

## Iteration 6 — why-queued-input-waits-is-told-truthfully IMPL

CI: 9e393d0 and a8a0b32 both `success`. Branch and log match STATE. The tree held the impl and new test file uncommitted from the prior firing; verified and committed here.
Built: D1 `held_by_hop_budget` + `delivery_note` on both message routes and `mcp_server.send_message`; D3 `checkout_held_sentence` shared with `agent_trigger`, live holder check in `get_queue_status` in one try falling through to D4; D4 labelled fallback. 1.6 built as "starts with label, stored sentence inside" (D4 wraps it).
Verified (claude was still on PATH for the targeted run; full run with it stripped): hub/tests 4888 passed, 86 skipped, 2 failed (same two load flakes as iteration 4, pass alone: 36 passed). Targeted 7 files 131 passed. ruff, black clean. `tests/` not run (no `src/` change).
NOT done: test 1.8b (review controlling entry skips D3) unwritten; no live drive yet (next item).

## Iteration 7 — why-queued-input-waits-is-told-truthfully DRIVE

CI: 9e393d0 success; 2988870 in_progress when read. Branch and log match STATE.
Drive Hub :8035, fresh profile drive0926c, named key. `scripts/drive/d0926_queue_truth.py`: holder runs a real Haiku turn on a task, challenger's entry staged (SQLite, labelled) with the D8 sentence.
- F289: during the holder's turn `GET /queue/chall/status` named the holder; the sentence is identical to the stored one, so that leg alone does not discriminate. The holder's end re-drained the challenger (correct). Then a stored D8 sentence with no live holder → `the last delivery attempt was refused: holder is already running a turn ...` (D4 fallback), measured.
- NOT driven: F361 (D1 hop-budget note; needs a chain past the budget) and the UI line; unit tests only. Test 1.8b still unwritten.
Archived (agent-loops/agent-tool-surface specs synced). F289 fixed; F361 fixed in code, not driven. Drive Hub stopped; job disabled.

## Iteration 8 — a-live-view-that-fell-behind-is-told-and-catches-up IMPL

CI: 2988870 / 3620519 (DRIVE why-queued) were in_progress when read (minutes old); earlier pushes success. Branch and log match STATE.
Built: `sse.SubscriberQueue` (`dropped`), both `publish` loops count `QueueFull`, `make_gap_event`, `stream_frames` (gap after each yielded message, count reset); both routes in `events.py` yield from it. UI: `useSSE.ts` handles `stream_gap` before the allowlist (dispatch, then `fireReconnect`, docstring widened); `ActivityLog` lets a gap past the project filter; `eventSummary` sentence. 2.3b n/a (F251 change not landed). Bundle refreshed via `refresh_ui_bundle.py`.
Tests: `hub/tests/test_a_stream_that_fell_behind_is_told.py` (6), useSSE.test.tsx +2 (one hook mounted), ActivityLog.test.tsx +1, eventSummary-gap.test.ts +1. The four UI tests fail on the stashed old UI source and pass now; backend tests not run against old code (they import the new API).
Verified: hub/tests 4895 passed, 86 skipped, 1 failed (`test_database_named_before_opening ...real_process`, the known load flake; re-run alone noted above). UI npm test 1624 passed, 3 failed under load (a different set each run; dependencyBoard passes alone, 31). ruff, black, eslint, build clean. `tests/` not run (no `src/` change). claude was still on PATH for the hub run (PATH strip did not take).
NOT done: live drive (3.1 stalled-subscriber, 3.2 human check) — next item.

## Iteration 9 — a-live-view-that-fell-behind-is-told-and-catches-up DRIVE

CI: dd28818 (DRIVE why-queued) was `failure`: one ruff error, unused `timedelta` import in `scripts/drive/d0926_queue_truth.py` (scripts bug-rule pass). Fixed here; `ruff check scripts/ --select ...` clean. 8179c93 in_progress when read.
Drive Hub :8037, fresh profile drive0926d, named key. `scripts/drive/d0926_stream_gap.py`: raw-socket operator-stream subscriber, 512-byte RCVBUF, stalled while 3,000 events were pushed via POST /logs, then drained.
- Measured (2 runs; second recorded): one `stream_gap` frame `{"dropped": 1362, "severity": "warn"}`, no `project_id`; received 1638 + dropped 1362 = 3000. (First run's parse failed in my script, not the Hub.)
- NOT driven: the UI half (3.2, ActivityLog line and catch-up in a browser) — unit tests only.
Archived (local-project-workspace synced). F253 marked fixed. Drive Hub stopped.

## Iteration 10 — evidence-is-decided-after-the-run-that-recorded-it IMPL

CI: dd28818 was `failure` (ruff, fixed in iteration 9); 8179c93/8317a65 in_progress when read. Branch and log match STATE.
Built (D1-D7): `decide` refuses `recording_run_live` (409) after the value/grant/self checks and notes an agent waiter; `run_liveness` waiters; `record` revises the same run's awaiting same-digest row in place (200 `revised: true`, `produced_at` bumped, footprint re-applied with `outside_writes`), `duplicate_of(run_id=)`, D3 agent sentence, D5 dirty-checkout pass (recorded dir under task/worktree roots only); `recording_run_live` on `_evidence_view` and the agent record response; D6 both decision routes build the answer before integration (F426); `agent_trigger._wake_decision_waiters` after both registry releases + `_queue_decision_notes` (queue origin `evidence`, refusing conversation, workspace copied from the delivered entry); migration `0108` + models CHECKs; `format_turn_prompt` renders `AgentWeave`; mcp docstrings.
Tests: new `hub/tests/test_evidence_waits_for_its_run.py` (13, pass); `test_duplicate_evidence.py` two tests moved (they re-recorded from the same run, which now revises; now use a later run). Head assertions bumped to 0108. hub/tests full: 4909 passed, 86 skipped, 0 failed. ruff, black clean. `tests/` not run (no `src/` change).
NOT done: tests 1.7b, 1.9, 1.12, 1.12a, 1.13, 1.16, 1.17(c,e) unwritten; no live drive (3.1, 3.2) — next item. Tests were not run against the old code (they import the new API).

## Iteration 11 — evidence-is-decided-after-the-run-that-recorded-it DRIVE

CI: 6268b0e still in_progress when read (previous head 8317a65 success). Branch and log match STATE.
Drive Hub :8041, fresh profile drive0926e, named key; `scripts/drive/d0926_evidence_waits.py`, real Haiku builder/tester.
- 3.1 measured: builder recorded FR-1 (row `recording_run_live: true`); operator decide mid-turn -> 409 `{"code":"recording_run_live","message":"ev-… was recorded by run run-3b0a…, which is still running. … Decide once the run has ended. If that run is stuck … stopping it ends it, and the decision is then open."}`; after the run ended -> 200 accepted.
- 3.2 measured (2nd project, run 2b7b… held ~135s via a 120s sleep loop): tester's `decide_evidence` -> 409 (hub log), builder ended 02:28:53, queue entry origin `evidence` delivered into the same conversation (`conv-a7fac…`): "You tried to decide ev-61670acfca8a while run run-2b7ba925fc27 was still recording them, and the Hub held the decision. That run has ended; ev-61670acfca8a now names commit 28e6bca. They are open for a decision now — read them again first if the commit changed what you were judging."; tester then decided 200. (First attempt missed the window: Haiku shortened `sleep 75`, tester decided after the builder ended. Not counted.)
- NOT driven: F426's merge-failure 500 (unit tests only); unwritten tests 1.7b/1.9/1.12/1.12a/1.13/1.16/1.17c,e remain unchecked in the archived tasks.md.
Archived (requirement-traceability synced, 3 requirements). F358 and F426 marked fixed. Drive Hub stopped.

# 2026-09-21 day window

Newest entry at the BOTTOM. Playbook: `.claude/loops/day-window.md`.

Armed 2026-09-21T08:55:02+01:00 on `autonomous/2026-09-17-daily` (parent master@802a8c7), model opus.

## Iteration 1 — compose (09:02)

**Backlog page:** `py -3.11 scripts/backlog_page.py --check` → current, nothing moved since last
generation.

**Merge gate (step 1, morning fallback).** Branch is `autonomous/2026-09-17-daily`, not `master`.
`git rev-list --count HEAD..master` = **0** (genuine fast-forward candidate), branch is **24**
commits ahead of master. Tree clean, `HEAD` = `@{u}` = `66779df`. **CI for `66779df` is still
`in_progress`** (run `35575245187`) — condition 3 fails, gate does not open here; today's `D-7`
carries it as usual. Recorded per playbook: master last moved 2026-09-20T21:33+01, HEAD (the arm
commit) is from 2026-09-21T08:55+01 — under a day behind, not a multi-day gap. The prior sha
`cd7a4ed` (last night's final commit) failed CI with two `ERROR at setup` — both
`sqlite3.OperationalError: database is locked`, the F292 signature — but `HEAD` has since moved past
it, so no re-run applies to a superseded sha; only `66779df`'s own conclusion will matter at close.

**Branch settled** — already on `autonomous/2026-09-17-daily`, matches STATE; no new branch needed,
none merged into master yet.

**What last night did** (`.claude/autonomous/2026-09-20-night-log.md`, iterations 1-12): shipped and
archived `an-archived-agent-holds-nothing-and-is-offered-nowhere` (F185, F181, F390, F391 closed,
driven both mid-flow and at handoff); measured group 7 of `a-first-turn-is-not-told-it-has-nothing`
(3/3 fresh Haiku first turns reached MCP); ran a full-surface e2e sweep, **10 of 19 matrix rows
driven**, Flows/Checkpoints/Resilience **not reached**, filed **F395** (B, `POST /messages`
mislabels operator-sent messages as agent hops); filed **F396** (B, MISREPORT ratchet keyed by
`file:line` has decayed, already stale when its ceiling was frozen) and queued an operator decision
`F396-rekey` in `DECISIONS.md`'s `## Open`; wrote 6.2-REDO's real full-suite count (4474 passed, 86
skipped, 47:37) into the change's own tasks.md, closing F392's complaint for this instance. Queue
ended empty, `next_action: null`, ~4 hours of window unspent because every remaining change is
gated (REVISING, unapproved, or awaiting a re-derivation round) and the night window may not
propose.

**Research delivered.** `research-2026-09-21.md` present at the operator's routine output, copied to
`spec-queue/research/2026-09-21.md`. Two ranked candidates: **#1 (S)** the cross-page "waiting for
you" attention rail (`conversation_attention`) is correct but 94/101 `sse_manager.broadcast` sites
omit `conversation_id`, including all eleven question/permission lifecycle sites, so the amber dot
never appears or clears live; **#2 (M)** `ghcp` (GitHub Copilot runner) — CLI profile already
written, Hub side a deliberate 501, and Copilot CLI shipped four MCP-reliability fixes this week on
exactly the injection path a Hub implementation would use.

**DIRECTION.md.** Newest dated section is `## 2026-09-20`, not today — absence of a 2026-09-21
section is not an instruction; composing the default queue shape.

**Drain count** (changes with an unticked task and no APPROVED/REJECTED token in APPROVALS.md's
newest section): **4** — `a-hub-that-was-not-told-which-database-refuses-to-open-one`,
`a-loop-staffs-the-agent-it-names`, `a-refused-capability-reaches-the-operator`,
`an-unstaffed-review-names-its-holders`. **2 or more → no spec loop today.** All slots go to the
draining column.

**Queue written** (draining shape, D-1..D-7): D-1 scoped drive of what night built end to end; D-2
finish the sweep's three not-reached rows (last sweep is hours old, not 7-day-stale, so this
completes it rather than re-running it); D-3 the no-spec repair carve-out on **F394** (A,
`hub-test` hangs 6h on `master` instead of erroring — add `--timeout=120` to `ci.yml:136`'s pytest
invocation, per the finding's own suggested fix); D-4 one more no-spec repair or a bounded
status-line sweep of `FINDINGS.md`; D-5 the review page; D-6 spare repair slot if time remains; D-7
the merge gate, last. Full detail in `STATE-day.json`.

`current` set to `D-1`, matching `next_action`.

## Iteration 2 -- D-1: drove what night built (archived-agent change + group 7)

Branch and HEAD matched STATE. Fresh Hub from source on `:8097`, fresh profile `drive0921`, all real
turns on `claude-haiku-4-5-20251001`. Hub stopped afterward; no jobs created; nothing on 8000/8010.

**Archived-agent change** (`scripts/drive/d1_0921_archived_agent_drive.py`): 16 of 17 checks OK, past
the change's own 6.3 drive -- the `agent_archived` event is really persisted with
`released_charter_id`, the F390 runner wall tags archived holders, F181's probe and trigger agree,
unarchive then a real Haiku turn works. The one BAD was my script's assumption: archive-mid-turn is
refused 409 with a remedy (a gate that held). Filed **F397 (D)**: re-archiving an archived agent
re-stamps `archived_at` and writes a duplicate event.

**Group 7 replication** (`d1_0921_g7_mcp_first_turn.py`): 3 brand-new Haiku agents, hub_client unset,
same instruction as 7.2. **3 MCP / 0 HTTP / 0 neither**, read from `agent_outputs` (ToolSearch then
`mcp__agentweave__create_task`, no shell-out). Independent n=3 agreeing with last night's n=3.

Two harness traps hit: `GET /events` is an SSE stream (blocks forever; history is
`/events/history`), and the first script invocation piped through `tail` hid a hang for 10 minutes.

## Iteration 3 -- D-2: drove the three not-reached sweep rows (Flows, Checkpoints, Resilience)

Fresh Hub from source on `:8097`, profile `drive0921b`, Haiku only. **Trap:** a bash `$HOME` path
(`/c/Users/...`) in `DATABASE_URL` made the Hub create its database at `C:\c\Users\huida\...\drive0921b\`
(a stray dir, left; nothing on the real profile or `:8000` touched) -- pass `C:/Users/...` instead.

- **Row 12 Flows** (`t_row12_flows.py`): 1/1 held -- parallel firing, both tasks moved out of the
  active band (F140), work landed in each task checkout; job disabled and archived.
- **Row 15 Checkpoints** (`t_sweep_row13_checkpoints.py`): 70 pass / 5 fail. Filed **F398 (C)**
  (dismiss accepts a never-warned conversation and silences its first warning) and **F399 (D)**
  (taking a checkpoint never clears `dismissed`). The fifth fail (second checkpoint consumed a note
  older than the first) is recorded as unverified, not filed.
- **Row 19 Resilience** (`t_row19_crash.py`): 4/4 PASS -- Hub force-killed mid-run, no orphan child,
  restart on the same DB, agent triggerable again, post-crash turn ran to idle.

Hub stopped; port 8097 dead; no job left enabled. `spec-queue/BACKLOG.html` was already modified at
iteration start (regenerated page) and is committed with this.

## Iteration 4 -- D-3: F394 CI hang mitigation

Added `--timeout=300 --timeout-method=thread` to ci.yml's hub-test pytest step and `pytest-timeout>=2.3` to hub dev extras (commit 3491580). Verified: flag accepted on a real 54-test run; a 30s sleep with --timeout=2 is killed with a Timeout stack; tests/test_dev_constraints.py passes. Not yet seen on CI; F394 Status says so. Mitigation only -- root cause unknown.

## Iteration 5 -- D-4: F304 no-spec repair (empty run.error on cancellation)

Status sweep first: of 222 open/missing-status entries, only F304 was a real, still-reproducing defect with a fix smaller than its argument (the other commit-message hits were mentions, not fixes). Fix: `agent_trigger.py:1916` `run.error = str(exc) or type(exc).__name__`. New test `test_a_cancelled_run_records_a_reason_rather_than_an_empty_string` fails without the fix (measured) and passes with it; full test_agent_trigger.py 55 passed; ruff and black clean. Not driven against a live Hub -- the tail is unit-reached directly. F304 Status set to fixed.

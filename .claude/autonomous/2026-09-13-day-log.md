# 2026-09-13 day window

Newest entry at the BOTTOM. Playbook: `.claude/loops/day-window.md`.

Armed 2026-09-13T08:55:02+01:00 on `autonomous/2026-09-12-daily` (parent master@eac213c), model opus.

---

## Iteration 1 — compose the queue; the merge gate stays shut on F292 (09:00–09:15)

The unit for this firing was `day-window.md`'s "Iteration 1 — compose the queue". Times come from
PowerShell `Get-Date`.

### The limits, restated so a later firing inherits them even if this one dies mid-thought

- **Stay on the cycle branch.** Only the merge gate may touch `master`, and only under the four
  conditions it checks itself, with `--ff-only`.
- **Nothing outward-facing.** Push, do not open PRs.
- **Nothing destructive.**
- **Do not browse the open web.**
- **Every claim is measured or labelled unverified.**
- **Operator decisions go to `decisions_for_user`**, and are not guessed.
- **Stage explicit paths**, never `git add -A`.
- **Tooling:** `py -3.11`, never bare `python`; `black --target-version py311`.
- **Never drive `proj-5e960453` or `proj-18e5d4e0`**, and never touch port 8000.
- **Every real agent turn binds Haiku**, and no job is left enabled.

### Step 1 — the merge gate did NOT open: condition 3 failed, on F292

Following yesterday's finding, this firing **wrote nothing** until the arming commit's CI
concluded. At 09:00, run `34746396329` for `000fcbf` was `in_progress`: every job was green except
`hub-test`. `gh run watch` held until the run concluded, and it concluded **`failure`**.

| condition | measured | verdict |
|---|---|---|
| branch is not `master`; `HEAD..master` is 0 | `autonomous/2026-09-12-daily`; `HEAD..master` **0**, `HEAD..origin/master` **0**; the branch is 44 commits ahead | pass |
| clean tree; `HEAD` equals `@{u}` | `git status --short` empty; both `000fcbf5ca36` | pass |
| CI `success` for **this exact sha** | `000fcbf` → CI `completed failure`. `hub-test` finished **4212 passed, 18 skipped, 1 error** in 12:38. Every other job was green | **FAIL** |
| no line-initial `HOLD MERGE` in the newest `DIRECTION.md` section | the newest is `## 2026-09-13`; `grep -n '^HOLD MERGE'` over the whole file finds nothing | pass |

**The one error, classified from its own `--log-failed`, is F292.**
`ERROR tests/test_reviewer_is_not_the_author.py::test_assigning_a_reviewer_and_sending_to_review_in_one_patch_is_accepted`
failed with `sqlalchemy.exc.OperationalError: (sqlite3.OperationalError) database is locked`,
`[SQL: BEGIN IMMEDIATE]`. The commit under test (`000fcbf`, the arm) touches only this log and
state files. That makes it the **eighth** F292 red in the last 19 runs on this branch. The night
counted 7 in 18.

**I did not re-run the failed job.** A re-run could produce a green for this same sha and open the
gate, but no window has done that before (grep over every day log and `.claude/loops/` finds no
precedent). Re-running a flake until it passes is exactly the priority call the night put to the
operator in its fourth `decisions_for_user` item: how far up F292 should go. So it goes to
`decisions_for_user` as a question and is not taken here.

**The gate gets one more attempt this window, and only one.** `gh run list` shows CI takes about
13 minutes here, and the driver fires every 5 (`arm-cycle.ps1:39`). The next firing would therefore
find this commit's CI still in flight. So `gate-retry` is its own queue item, first after this one.
It waits from a clean tree for this commit's CI and writes nothing until that concludes, then
re-measures all four conditions. It lands on `success` and records the failure otherwise. Either
way it is closed after that one attempt, so the gate never eats more than one firing. If it
lands, the playbook's "start today's branch from the new `master`" means cutting
`autonomous/2026-09-13-daily`. `STATE-day.json`'s `branch` has to be rewritten in the same firing,
because `run-iteration.ps1:124` stops the driver on a mismatch.

### Step 2 — the branch

`autonomous/2026-09-12-daily`, which is **unmerged**. It was cut 2026-09-12 from `eac213c` and now
spans **two days** (2026-09-12 and 2026-09-13), 44 commits ahead of `master`. The review page
says so first.

### Step 3 — what last night did

There are 44 commits in `eac213c..000fcbf`, and the night's own close is the bottom of
`2026-09-12-night-log.md`. It built and **archived** both approved changes:

- **`a-url-is-not-a-path`** fixes F300, F312, F321, F323 and F331. Product commit `612b9c9`
  rewrote `_decide`'s shell reading, adding about 410 lines to `mcp_server.py`.
- **`a-refused-review-leaves-nothing-behind`** fixes F319 and F320. Product commits are `6ebcd9f`
  and `997ff5d`, in `turn_scheduler.py`. `agent_trigger.py` changed only in a comment.

**Driven:**
- Both changes were driven on a live Hub with real Haiku agents, pre-fix tree first and then the
  fixed one.
- F300, F312, F321, F323: 15/15 on the fixed tree.
- F319, F320: 37 ok, plus 6 ok in Chromium for the UI check.

**Tested, not driven:**
- F331 on POSIX. CI's Linux `hub-test` is the only evidence.
- 17 + 12 mutations.

**Inferred, not seen — the gaps D-1 takes:**
- the **PowerShell `tool_name` path** of the new reader (design D10 item 5: *"that a spawned run's
  approver receives `PowerShell` as `tool_name` … is unverified"*);
- **F333's** UI line, never rendered;
- **F335's** feed line (`eventSummary.ts:142`).

**Distrusted by the night itself:**
- r4's one-off 10m16s hang of `test_giving_up_lets_the_agent_accept_new_input`, still
  unclassified;
- the r8 §5.4 UI absence check, never shown to fail.

**New findings:** F331 (A, fixed), F332 (A, bash `$'..\x2f..'` on POSIX, allowed before the fix
and after), F333 (B), F334 (B), F335 (D). `FINDINGS.md` grew by 967 lines in this cycle.

**The night's `decisions_for_user`, four of them, go onto the review page verbatim:**
1. D11a: four departures from R2's reader;
2. F333 and F334, propose them together or fold one into the other;
3. the human-only S6 and S7 checks, which are unticked in the two *archived* changes. They are
   archived, so the drain count does not count them;
4. F292's priority.

### Step 4 — the research was delivered on time

`research-2026-09-13.md` was written at 07:33, from the run that started at 07:10:01 (`logs/2026-09-13_071001.log`). It is
copied byte-identical (`cmp`) to `spec-queue/research/2026-09-13.md`, committed here, and read as
data. Its three ranked candidates bear directly on today's spec loop:

1. **Re-drive F299 before R1 proposes.** On `claude` 2.1.269, measured on this machine this
   morning, F299's own configuration exits 1 at spawn: *"MCP server blocked by enterprise
   policy"*, then *"--permission-prompt-tool … not found"*. It spends zero tokens. On that build,
   verdict 1b would swap this truthful, free failure for a paid turn whose writes are all denied,
   unless it also passes `--permission-prompts none`. That flag is an unknown option before
   v2.1.259.
2. **`acceptEdits` is path-confined by the harness itself**, which contradicts four places in the
   repository, one of them 1b's own reason for rejecting that posture (`DECISIONS.md:772-773`).
3. **The Hub's MCP "grounds" are positive-only and permanent** (`launchability.py:232-259`), even
   though the harness reports a policy block on every run.

R1 must treat all three as inputs to measure. None of them licenses re-deciding 1b, and a
conflict with the verdict goes to the operator.

### Step 5 — `DIRECTION.md` has a section for today, and it sets the shape

`## 2026-09-13` has these parts:
- **D-1** drives what the night built, both changes.
- **The first spec loop is F299** (A), verdict `DECISIONS.md` 1b (`:745`).
- **The second loop is F327** (B), only at a drain count of 0. It starts from R2's option (b), the
  dispatch stages and the flow stops staging first, and from R3's warning that the divergence
  restaff collides with D9. It will MODIFY `agent-flows`.
- **The collision rule:** if F299's R1 finds a file shared with F327's expected blast radius
  (`scheduler.py`, `run_divergence.py`, the review staffing path), F299 runs alone.
- **Candidates for the D-6 carve-out:** F328 (D) and F326 (D).

### Step 6 — the drain count is 0, so two spec loops

The playbook's loop over `openspec/changes/*/` found **no** change with an unticked task.
`openspec/changes/` holds only `archive/`. **0 means two loops**, D-2/D-3/D-4 (F299), then
D-2b/D-3b/D-4b (F327), then D-5.

**The drive is scoped, not a sweep.** `scripts/drive/SURVEY.md:144` records the 17-row sweep as
complete, and `spec-queue/ROADMAP.md:283` says "No sweeps". Both outrank the calendar, as the last
three day windows each re-measured. Today's `DIRECTION.md` names the drive's scope anyway.

**A guard on the second loop, written into its R1 item.** Eleven items remain for about 7h45m. If
`d2b-r1` comes up later than **14:30**, it and its two rounds are skipped in favour of D-5. The log
has to say why. That is the playbook's own escape ("or run one loop and say why in the log"),
used so that a window which runs out still leaves a review page. Without it, the day ends with a
proposal and nothing that presents it.

### The queue

`gate-retry`, `d1-drive`, `d2-r1`, `d3-r2`, `d4-r3` (F299), `d2b-r1`, `d3b-r2`, `d4b-r3` (F327),
`d5-review`, `d6-repair` (F328 or F326, if the carve-out holds), and `d7-ledger`. The ledger item
files research candidates 2 and 3 as findings, unless F299's rounds have already done so.

**Next:** `gate-retry`.

---

## Iteration 2 — gate-retry: the gate stays shut, this time on F314 (09:15–09:30)

The unit was `gate-retry`: one attempt, then closed. Times come from PowerShell `Get-Date`.
Branch and `HEAD` matched the state file on entry (`b798ff6` on `autonomous/2026-09-12-daily`).

**Nothing was written until CI for `HEAD` concluded.** Run `34747152049` for `b798ff6` was
`in_progress` at 09:15. The first `gh run watch` hit its 580s timeout, the second returned, and
the run concluded **`failure`** at about 09:28.

| condition | measured | verdict |
|---|---|---|
| branch is not `master`; `HEAD..master` is 0 | `autonomous/2026-09-12-daily`; after `git fetch`, `HEAD..master` **0**, `HEAD..origin/master` **0**; 45 ahead | pass |
| clean tree; `HEAD` equals `@{u}` | `git status --short` empty; both `b798ff60cb5b` | pass |
| CI `success` for **this exact sha** | `b798ff6` → CI `completed failure`. `hub-test`: **1 failed, 4212 passed, 18 skipped** in 14:25; the other eight jobs green | **FAIL** |
| no line-initial `HOLD MERGE` in the newest `DIRECTION.md` section | sections run newest-first after the template; the newest is `## 2026-09-13` (:28–:83); 0 matches there and 0 in the whole file | pass |

**The red, classified from its own `--log-failed`, is F314, not F292.**
`FAILED tests/test_flow_holds_the_loop_requirements.py::test_a_wide_flows_state_is_still_one_call`
with `RuntimeError: <asyncio.locks.Lock object at 0x7f3d87981050 [locked]> is bound to a different
event loop`. The scheduler logged `Could not start req-third for run run-8bfc6fcf7988` with the same
error (`scheduler.py:3054`). That is F314's signature exactly, as `FINDINGS.md` `## F314 (B)`
(:25439, **Status:** open) and `DEAD-ENDS.md:141` state it. The commit under test touches only this
log, `STATE-day.json` and `spec-queue/research/2026-09-13.md`, with no Python, so the red is not
that commit's.

**Not re-run**, for the same reason iteration 1 gave: re-running a flake until it goes green is
the operator's F292 priority call. It now covers F314 as well. The branch's last 20 CI runs are
**8 failure, 12 success**, and every classified red is one of the two known intermittents.

**What this means for the day.** The gate has now failed on two consecutive state-only commits,
one per intermittent, so the problem is not F292 alone. The `decisions_for_user` entry is widened
to name both. `autonomous/2026-09-12-daily` stays unmerged and now spans two days at 45 commits.
The review page must lead with that. No branch was cut, and `STATE-day.json`'s `branch` is
unchanged.

`gate-retry` is closed (done, did not land). **Next:** `d1-drive`.

---

## Iteration 3 — d1-drive: PowerShell's name verified, F333 rendered, F335 narrowed, F336 filed (09:35–09:58)

Branch and `HEAD` matched the state file on entry (`4521625` on `autonomous/2026-09-12-daily`,
clean). Times come from PowerShell `Get-Date`.

**The drive Hub.** Port **8023**, chosen after `netstat -ano` showed only 8000 listening among
80xx. Fresh profile `drive0913d`, migrated to `0102` at launch. Started from `hub/` with
`py -3.11 -m uvicorn`, from source at `4521625`. Process start was 09:37:14. No `.py` under `hub/hub`
or `src` was newer than the key file written just before the launch. Operator key via
`AW_BOOTSTRAP_API_KEY`, kept in `testbed/scratch/d1_0913/key.txt` (ignored). The served bundle and
`hub/ui/src` were last changed in the same commit (`7a0e5bc`). Claude Code **2.1.269**. Every run in
every drive project joined to `claude-haiku-4-5-20251001`. Three fresh projects: `proj-9e567eeff7a2`
(PowerShell), `proj-c7a96aab02c6` (render D+F) and `proj-0ace0a0911e3` (render Q). Afterwards: 0
running runs, 0 queued entries, 0 rows in `ai_jobs`/`job_runs`/`apscheduler_jobs`. The Hub was
stopped at 09:55 and the profile kept.

### Priority 1 — the PowerShell `tool_name` path: verified, no fallback fired (19/19)

`scripts/drive/t_d1_0913_powershell_tool_name.py`. Two agents on one Haiku runner, each ask a fresh
session. All five turns used the PowerShell tool (`tool_use.tool == "PowerShell"` in every
transcript).

| ask | posture | command | approver's verdict, from the record |
|---|---|---|---|
| w1 | workspace | `Set-Content -Path "ps_inside.txt" -Value "hi"` | allowed; file landed in the worktree |
| w2 | workspace | `Set-Content -Path "..\ps_stray.txt" -Value "hi"` | `permission_denied`, **`tool_name='PowerShell'`**, `'..\ps_stray.txt' is outside your workspace`; no stray |
| w3 | workspace | `Invoke-WebRequest -UseBasicParsing https://example.com/ …` | `permission_denied`, **`tool_name='PowerShell'`**, D5's network text verbatim |
| w4 | workspace | `Invoke-RestMethod "$env:HUB_URL/health"` | **allowed**; one `permission-decisions` POST, then the Hub logged `GET /health`; no denied row |
| m1 | manual | `Set-Content -Path "ps_manual.txt" -Value "hi"` | request row `tool_name='PowerShell'`, `tool_input={"command": "Set-Content -Path \"ps_manual.txt\" -Value \"hi\"", "description": …}`; allowed by the harness through the API; file landed |

**Why w4 settles it.** The Hub persists only refusals (`agent_actions.py:891-912`), so an allowed
call names no tool anywhere. w4 was built to discriminate, and checked against `_decide` locally
before any turn ran. Read as `PowerShell`, `$env:HUB_URL` is the run's own Hub. Read by `Bash`, or
by the both-ways fallback that any other name gets (including `powershell` in lower case), it is a
variable beside a separator, and refused as "cannot be checked". w4 was allowed, so the approver
was handed exactly `PowerShell`. m1 shows the same directly, with the script in
`tool_input["command"]`. **D10 item 5 of `a-url-is-not-a-path` is verified on Windows and Claude
Code 2.1.269. D8(g)'s fallback did not fire.** Evidence: `%TEMP%\d1_0913\ps\transcripts\`.

### Priority 2 — the renders (Chromium, served bundle)

`scripts/drive/t_d1_0913_render.py` (phases `D`, `Q`, `F`) and `t_d1_0913_feed_probe.py`.
Screenshots are in `%TEMP%\d1_0913\render\shots\`. They are evidence for the operator's human-only
checks, and **they tick none of them**.

- **F333, rendered: the finding's prediction holds exactly.** After Continue was pressed in the
  UI on H's conversation, with H queued at 2 attempts and refused by the author guard, H went to
  `withdrawn` at 3 and M was delivered. The notice read *"Not started — this conversation had
  nothing queued. Reply with the single word pong. Call no tool. started instead."* The same
  screen's timeline shows H's **NOT DELIVERED — delivery failed 3 times (Cannot move task …)** one
  row above. The Status line is updated in FINDINGS.
- **F335, narrowed. Two predictions were wrong in how they reach the operator.**
  - (1) The operator route's `409` never stages. `review_dispatch_refusal`
    (`agent_trigger.py:1461`) answers first, so a live `409` with the Activity tab open produced
    no broadcast and no line.
  - (2) A review queued behind the reviewer's own turn, then refused at delivery (pruned while it
    waited), **does** broadcast. `curl -N /api/v1/events` captured
    `run_divergence_resolved {"task_id":"task-1275228c05dd","count":1}` while the divergence stayed
    open and no row was written.
  - (3) **The feed renders neither the false broadcast nor a true one.** A real operator resolution,
    with the page open, rendered its `task_updated` line live and no resolution line. The cause is
    F251: `useSSE.ts`'s allowlist drops `run_divergence_resolved`.
  - So F335 is latent behind F251, and F251 must not be fixed without it. Both sections say so.
    Phase D's assertion was inverted after it measured (its first run's FAIL *is* the
    measurement). Phase Q's check is now a tripwire for the day F251 lands alone.
- **The 409 in the dispatch control (archived change 6.1): the control does not exist. Filed as
  F336 (C).**
  - No code in `hub/ui/src` or the served bundle sends `review_task_id` (0 matches each).
  - The card's only agent control is **Start work** (`TaskCard.tsx:259-280`, posting `task_id`).
    Its items on a completed task were `Start <agent> on this` ×3.
  - By reading, not driving, it starts a work turn bound to the completed task, not a review
    (`run_task_binding.py:441-443`). I did not press it.
  - Over the API the `409` reads *"commit … is not present in this repository, so there is nothing
    to check out for review"*, and the task row is unchanged.
  - Test-guide check 1 and 6.1 ask for an action only the API can take.

**Seen and not examined:** H's conversation showed "Working · 1s", **Stop turn** and "au094433 is
responding…" while the running turn was M's, in another conversation. The status is per-agent by
construction (`AgentOutputPanel.tsx:306`, `isRunning = agent.status === 'running'`). Whether
**Stop turn** there stops another conversation's turn was not tested. It is not filed.

### Priority 3 — r4's stall: 0 in 5, still unclassified

`hub/tests/test_failed_run_returns_input.py` alone, under `py -3.11`, with `timeout 600` per run,
the drive Hub already stopped. rc 0 each time, **19 passed** each. Wall times: **29.2s, 11.2s,
11.0s, 11.1s, 10.8s**. pytest's own times: 8.50s, 8.77s, 8.57s, 8.60s, 8.38s. The first run's
extra ~18s is outside pytest's own timing, so it is interpreter start-up, not the test. With the
night's 8/8, the file alone is **13 clean runs of 13**. The one hang came on the first run of the
~315-test set, not on the file. That is F292's shape, and nothing here classifies it.

### Findings touched

- **F336 (C), new.** No control in the app dispatches a review.
- **F333, F335, F251.** Status lines updated with what was measured, and nothing else.
- **For D-5's page:** D10 item 5 verified; F335 latent behind F251; F336 answers 6.1's question
  ("no UI surface shows it").

`d1-drive` is closed. **Next:** `d2-r1` (F299 R1).

## Iteration 4 — d2-r1: F299 R1; the measurement contradicts 1b's premise, so there is an operator question; the second loop is skipped on collision (10:05–10:25)

Branch and `git log` matched the state file (`00ba182`, clean tree). `claude --version` → **2.1.269**
at 10:05 and again at 10:21. The change is `openspec/changes/an-absent-approver-is-not-named/`,
written for `DECISIONS.md` 1b **as decided**. `openspec validate --strict` passes.

### The re-drive, on the Hub's own spawn rather than the research's

`scripts/drive/t_d2_0913_f299_harness.py`. **Two things differ from the research's method, and
both were checked against the code:**
- argv comes from `runner_commands.build_command` itself;
- the spawn goes through `hub.pty_runner.PtySession`, because Claude runs are PTY-spawned
  (`agent_trigger.py:2034`).

The research cited `pty_runner.py:357` (`stderr=STDOUT`) as the Hub's merge. **That is
`PipeSession`, Codex's spawn.** Under the PTY the two streams share one terminal anyway, so the
conclusion holds but the citation was wrong.

Each condition ran in two environments: `inherit` (this process's `CLAUDE*` variables kept, which is
what a Hub started inside Claude Code passes) and `plain` (stripped). The run used Haiku, a `%TEMP%`
directory, F299's own prompt, and `deniedMcpServers` via `--settings` as the policy. Evidence is in
the change's `evidence/`.

| cond | shape | both environments |
|---|---|---|
| A_hub (Q4b) | today's argv, policy on | **exit 1, no model call**; `Warning: MCP server blocked by enterprise policy: agentweave` + `Error: … approve_tool_call … not found` |
| A_repro (P4) | F299's repro flags | exit 1, no model call, the `not found` line |
| B_1b | A_hub minus the approver flag = **1b's actual shape** (the research's Q5 dropped `--mcp-config` too) | exit 0, 2 turns, ~$0.023, `['Write']` denied, *"please approve"* |
| B_q5 (Q5) | `manual` only | exit 0, `['Write']`, *"please approve"* |
| C_1b_none | B_1b + `--permission-prompts none` | *"no approval surface"* in both; **plain retried via PowerShell** (3 turns) |
| C_q3 (Q3) | `manual` + `--permission-prompts none` | *"no approval surface"*, no retry |

- **The research's candidate 1 reproduces.**
- **The inherited `CLAUDE*` environment is ruled out** as the cause of F299's 2026-09-09 start. The
  build remains the likely cause, and it is unmeasured.
- **Operator question** at the top of `proposal.md`, with options (a), (b), or 1b as written. It is
  added to `decisions_for_user`. Not picked.

### What R1 found that the verdict did not say

1. **The verdict cannot be implemented on its own signal at its own grain.**
   `harness_has_honoured_mcp` is false for **every agent's first run**, as well as for F299's agent.
   A literal *"no grounds → no flag"* would refuse writes on every new agent's first turn on every
   permitted machine. That breaches *"A newly created agent can edit files in its own workspace"*.
   *"From the second run"* needs a fact the Hub does not record: that it gave an earlier run its
   server, and that the harness got as far as starting. So the design adds `Run.mcp_server_injected`
   (migration `0103`, NULL is not evidence) and a third state, *untested*, which keeps today's flag.
2. **As written, the verdict newly breaches *"A refusal is recorded wherever it is decided"*.**
   Claude refusals are recorded only through `approve_tool_call`. A 1b-shaped run starts, is
   refused, and leaves a clean record. The change therefore records `permission_denials` from the
   harness's result line for every approver-less Claude run, keyed on the spawned argv. That is a
   MODIFIED sandboxing requirement, and it reaches beyond F299's configuration on purpose
   (`design.md` D6). R2 should attack the scope.
3. **A failed adapter announce now costs a turn** (D9). R2 should also attack D8, the argument that
   `agent-capability-plane` needs no delta.

### Collision check → d2b/d3b/d4b are SKIPPED

Files the change touches:
- `hub/hub/runner_commands.py`
- `hub/hub/launchability.py`
- `hub/hub/api/v1/agent_trigger.py` (`:1000` access-path block, `:1190` `Run(...)`, `:2140`
  `_flush_line`)
- `hub/hub/runner_parsing.py`
- `hub/hub/db/models.py`
- migration `0103`
- `hub/tests/test_migrations.py`
- `docs/reference/permission-postures.md`

**`agent_trigger.py` is shared with F327's review-dispatch path** (`review_dispatch_refusal` `:452`,
called at `:1461`). The regions differ, but `day-window.md` step 6's rule is *"shares no file"*.
So F299 runs alone, as DIRECTION says to when this happens.

*"Take the one after it"* does not rescue a second loop either. DIRECTION's item 3 (F301's notice)
lives in `launchability.py` and `agent_trigger.py`'s access-path block, both of which this change
touches. Item 4 is three unrelated leftovers with no single finding.

### Recorded

- **F299's ledger entry:** a dated re-measurement note. Status stays open.
- **Research candidates 2 and 3** are *named* in design D10/D9 and **not filed**, so D-7 still
  files both.
- **Friction with the flow:** none new. The spec went in openspec as DIRECTION says.

**Next:** `d3-r2`.

## Iteration 5 — d3-r2: F299 R2; R1's "no model call" was a misreading, D3 admitted typos, D6 now joins on tool_use_id, and a third requirement needed its exception written (10:27–10:50)

Branch and `git log` matched the state file (`54c47b3`, clean tree). `claude --version` → **2.1.269**
at 10:25 and again at 10:38. The change is `openspec/changes/an-absent-approver-is-not-named/`.
`openspec validate --strict` passes. Both MODIFIED headers match `openspec/specs` character for
character, and no shipped line is missing from either MODIFIED body (checked by script).

### R1's measurement, re-run

`t_d2_0913_f299_harness.py B_1b A_hub --env plain`:
- **B_1b:** exit 0, 2 turns, `['Write']` denied, *"Please approve"*, $0.0082 (R1: $0.0236, cache).
- **A_hub:** exit 1, both harness lines.

Both outcomes reproduce.

### What R2 found (all in the change's `design.md` "What round 2 changed", evidence `r2-harness-results.json`)

1. **R1's argument was wrong while every outcome it recorded was right.** R1 wrote that F299's
   configuration *"exits 1 before any model call, spending nothing"*. It read that from the missing
   `subtype`/`num_turns`, but those fields sit on the result line. **Every A transcript holds two
   `assistant` lines and a `Write` tool_use, and no result line.** That is R1's own three captures
   (10:08–10:09) and R2's rerun (10:25).

   What 2.1.269 does:
   - A turn needing no approval **completes** (measured: exit 0, `ok`).
   - At the first approval-needing call, the `not found` error comes back as the tool result, and
     the process dies with no result line. The Hub then records the turn's usage as *unavailable*
     (`usage_accounting.py:36`).

   Rewritten to match: the **operator question's premise** (the options are unchanged, none is
   picked, and it still answers in one line), design H-2.1.269, R1's table rows, D7, D8's evidence,
   the test guide, and F299's ledger note (a dated correction below R1's).
2. **D3 counted a typo as a refutation.** On a *permitted* 2.1.269 (`t_d3_0913_f299_early_exit.py`,
   a stand-in server that writes a marker when started):

   | failure | exit | `init` | server started |
   |---|---|---|---|
   | unknown runner flag | 1 | none | never |
   | `--resume` of an unseen session | 1 | none | 1.2 s before exit, racing the announce |
   | unknown model | 1 | yes | `connected` |
   | bad API key | 1 (10 retries, 182 s) | yes | `connected` |

   R1's conditions (injected, `exit_code` set, failed, no report) admit the first two. One flag
   typo would have removed a working approver.

   The fix is a new condition: **the harness's `init` line was seen**, recorded as
   `Run.harness_init_at` (a second column in `0103`, set by the read loop from a new
   `ParsedLine.harness_init`). `Run.session_id` can't be used, because it is seeded from
   `resume_session_id` (`agent_trigger.py:1194`). This also excludes Codex's synthetic exit code
   and names `interrupted`.

   Checked that it can fire: the Hub's own read loop, replayed over a raw PTY capture
   (`t_d3_0913_f299_init_line.py`), parses the `init` line as JSON.
3. **D6's argv key made a false spec claim.** It said an approver-named run *"already has each
   refusal recorded"*. That is false for three cases:
   - F299's own 09-09 first run;
   - a swallowed `_report_decision`;
   - refusals the harness decides alone.

   **Measured** with a stand-in approver that refuses everything (`t_d3_0913_f299_approver_denials.py`):
   the approver's own refusal appears in `permission_denials` under the **same `tool_use_id`** the
   approver was handed. Both existing refusal writers already store that id.

   So D6 now records every Claude run's harness-reported refusals, **joined on `tool_use_id`**,
   which is the codebase's own *"the card is the join"* pattern. This dissolves R1's D6 scope
   question. The argv now only picks the reason's wording.
4. **A third shipped requirement, not cited by R1, contradicted the new one.**
   *"The default posture lets an agent work inside its own workspace"* says the Hub MUST NOT impose,
   by default, a posture that only an absent answerer can resolve. The new requirement mandates
   exactly that for *refuted* runs, and today's code already breaches it on a blocking harness.
   1b decided the case (*"It does not restore the run's ability to work"*, and `acceptEdits` is
   rejected). So the change now **MODIFIES that requirement**, adding:
   - an exception paragraph;
   - a qualified *"newly created agent"* scenario;
   - one new scenario.

   New D11. *"Introducing an enforced posture…"*'s *"only where the mechanism answering them is
   present"* is cited, not modified. **R3 should attack D11 hardest**, because it narrows a
   shipped scenario.
5. **The F108 question — the path is not on screen.** The activity line is `<agent> refused <tool>:
   <reason>` (`eventSummary.ts:114`, `EventRow.tsx:56`), and it never renders `detail`. The spec
   scenario and the test guide said the activity names the path. Both now say what shows, and the
   path is in the record. **Filed F337 (C)**, covering that and the sentence naming the wrong actor.
   It predates this change and affects Codex too.
6. **Task 6.1 had no source for the argv.** The Hub neither logs nor stores `cmd`. 6.1 now reads the
   live process's `Win32_Process.CommandLine`. Added drive 6.2b and test-guide check 2b (a typo is
   not evidence), and tests 2.2 (the `init`-less row, mutation-checked), 3.3 (ii-b) and
   4.3 (b)–(d), mutation-checked both ways.

D8 was attacked as R1 asked. No H on 2.1.269 lets the named-absent approver allow more than 1b's
shape: both allow no-approval turns, and neither allows the `Write`. What differs is whether the
turn survives the refusal. D8 stands.

The collision check is unchanged: R2 added no file to the change's blast radius. So d2b/d3b/d4b stay
SKIPPED.

**Spend:** ~9 Haiku spawns, about $0.10 in total, from the result lines that exist (the A rows
have none).

**Friction with the flow:** none new. The spec is in openspec, as DIRECTION says.

**Next:** `d4-r3`.

## Iteration 6 — d4-r3: F299 R3; R2's D3 argument was wrong where it went unmeasured, so D3 now reads what `init` says; D11's exception stops promising what the change cannot deliver (10:52–11:08)

Branch and `git log` matched the state file (`fd2de09`, clean tree). `claude --version` → **2.1.269**
at 10:50 and in every result line after. Change: `openspec/changes/an-absent-approver-is-not-named/`.
`openspec validate --strict` passes. Both MODIFIED bodies keep every shipped line (checked by
script), and the headers match `openspec/specs` character for character.

### R2's measurement, re-run

`t_d3_0913_f299_early_exit.py E_badflag E_ok`:
- **E_badflag:** exit 1, no `init`, server never started, `error: unknown option`.
- **E_ok:** exit 0, `ok`, `connected`.

Both reproduce.

### What R3 measured that R2's argument rested on

`scripts/drive/t_d4_0913_f299_slow_server.py`, a stand-in server that sleeps or dies before
serving. Permitted harness, Haiku, `PtySession`, `plain`. Evidence:
`evidence/r3-harness-results.json`.

| cond | server | `init` at | says of `agentweave` | turn |
|---|---|---|---|---|
| S_0 | serves at 3.7 s | 4.3 s | `connected` | success, rc 0 |
| S_8 | serves at 11.6 s | 12.6 s | `connected` | success, rc 0 |
| **S_45** | never serves | **33.6 s** | **`failed`** | **success, rc 0** |
| S_8_nb | S_8 + `MCP_CONNECTION_NONBLOCKING=true` | 12.7 s | `connected` | success, rc 0 |
| **S_crash** | exits 1 at start | 4.3 s | **`failed`** | **success, rc 0** |

- The real adapter imports in 0.95–0.99 s from a warm start (three runs). Its announce waits at most
  10 s.
- R2's raw A_hub capture has no `agentweave` entry in `init.mcp_servers` under the policy, and it
  carries `claude_code_version: 2.1.269`.
- No `pending` status was seen on this build.

### What R3 changed

1. **R2's D3 argument was wrong where it was unmeasured, and every outcome R2 recorded was right.**
   R2 wrote that with the `init` condition, *"that leaves one way for a test on a permitted harness
   to show no report: a lost announce"*. S_45 and S_crash are two more. Each completes, reports
   `init`, and leaves no announce. R2's four failure modes all used a server that started at once.
   - **Fix:** replace `Run.harness_init_at` with **`Run.harness_mcp_status`**. It holds what `init`
     said of the Hub's server: its status verbatim, or `absent`. A test requires a value that is
     neither NULL nor `connected`.
   - That also **removes R1's own D9 risk, the lost announce**. `connected` means the approver was
     there.
   - What remains is S_45, a server the harness gave up on. It costs one bad turn and corrects
     itself. With the measured adapter it needs a start about 20 s slower than measured. D9 states
     it.
   - S_crash is a *true* refutation, because the approver was absent too.
   - Every status other than `connected` counts, because the 09-09 build's naming of a blocked
     server is unmeasured.
   - Updated to match: the ADDED requirement's SHALL line and evidence paragraph; one new scenario
     (*"A server the harness reported started is not evidence against it"*); tasks 1.1, 1.5 (a
     shared server-name constant, three captured `init` fixtures), 2.1, 2.2 (a `connected` row and
     a `failed` row, with mutation checks on both halves of the condition), 3.3 (ii-c), 6.1, 6.2
     and 6.2b; the proposal; and D2, D8 and D9.
2. **D11, attacked hardest as asked. Three defects, each fixed.**
   - (a) The exception is stated per harness, so it covers the *untested* first run and a *grounds*
     agent whose harness blocks later. On 2.1.269 both die before their result line, so *"those
     refusals are recorded"* was false for them. It now says *"each refusal its harness reports is
     recorded"*.
   - (b) It asserted *"no posture gives that run both a workspace check and the ability to work"*.
     1b said only *"may be"*, and research candidate 2 measured `acceptEdits` confined, for edits.
     The disputed claim would have become normative. The paragraph now gives 1b's own reason: the
     trade is the operator's.
   - (c) *"State the agent's access path"* was ambiguous. Stating the tool-protocol path brings the
     approver back, and the run then dies. The paragraph now names the path that does not use the
     tool protocol. The qualified *"newly created agent"* scenario had also dropped agents given no
     server. It now reads *"given no Hub tool server, or its harness starts the one it is given"*.
   - **Does it quietly license the untested first run?** It licenses it openly. That is within 1b,
     which changes only *"the second run"* and says *"On a harness that blocks MCP there may be no
     posture…"*. The *grounds*-agent case is covered by the text and was **not** decided by 1b.
     D11 now names it and leaves it to candidate 3.
3. **Option (a) of the operator question overstated its cost.** It said *"adds a version probe"*.
   A refuted agent has, by construction, a run whose `init` line was seen, and that line carries
   `claude_code_version`. The option text now says so, with the downgrade caveat. It is **not
   picked**, and the question still answers in one line. Its statement of 2.1.269's behaviour
   stands: R2 measured it, and R3 re-read the capture.

**D6's `tool_use_id` join was attacked and holds.**
- Both refusal writers store `run_id` and `tool_use_id` in `EventLog.data`
  (`agent_actions.py:897-905`, `permissions.py:113-126`).
- The approver reports before it answers (`mcp_server.py:1470`), so 4.3(b)'s ordering is the real
  path's order.
- The Hub runs on SQLite only, so `data["run_id"].as_string()` compiles to `json_extract`. It is
  the codebase's **first** JSON-path filter. 4.3(b) and 4.3(d) go through the real routes, so a
  query that matched nothing would fail them.
- `cmd` is `_execute_run`'s parameter, so it is in scope for `_flush_line`.
- The join also dedupes D6's own rows, so a second result line would not double-record.
- Empty `tool_use_id`: every 2.1.269 entry carried one, and an empty id is recorded unjoined.
- Written up in the design as *"What round 3 changed"*.

**"Nothing is widened"** still holds on 2.1.269. The withheld flag leaves `manual`, which `init`
reports as `default`, and B_1b's `Write` is refused. R3 did not re-run B_1b, because R2 did at
10:25.

**Collision:** unchanged. R3 added no file to the blast radius. The new constant sits in
`runner_commands.py` and its import in `runner_parsing.py`, both already listed. So d2b/d3b/d4b
stay SKIPPED.

**F299 ledger:** a dated R3 note. The policy leaves the server out of `init`, unlike a timeout or a
crash. Status is still open.

**For D-7:** research candidate 3 is **not absorbed**. The change reads `init`'s status only to
decide what counts as a test, never to take grounds away, and `runner_parsing.py` still has no
system-message branch on the tree. D-7 files it, noting the overlap.

**Spend:** 7 Haiku spawns, about $0.09 from the result lines.

**Friction with the flow:** none new. The spec is in openspec, as DIRECTION says.

**Next:** `d5-review`.

## Iteration 7 — d5-review: the review page and the APPROVALS section (11:10–11:20)

Branch and `git log` matched the state file (`f5f0091`, clean tree).

**Written:** `spec-queue/review/review-2026-09-13.html` and a `## 2026-09-13` section at the top of
`spec-queue/APPROVALS.md` (newest first, above 2026-09-12).

**The branch goes first, as a banner.** `git rev-list --count master..HEAD` at `f5f0091` → **50**.
`eac213c..4acebf4` → 43 (the 2026-09-12 cycle), `4acebf4..HEAD` → 7 (today). `git diff --shortstat
master..HEAD` → 69 files, +18238 −383. The page states the gate's two failures (F292 at `000fcbf`,
F314 at `b798ff6`, condition 3 only) in a per-attempt table.

**What the page carries, and where each part came from:**
- **§2**, the night's build, is from `APPROVALS.md`'s two BUILT sections and this log's iteration 1.
  It includes the legs today's D-1 moved from inferred to driven (PowerShell's `tool_name`, F333) or
  narrowed (F335).
- **§3** lists today's findings with file:line: F333, F335/F251, F336, F337. F333's
  `AgentOutputPanel.tsx:759-769` was re-read and is the `Not started — ${waiting_reason}` notice.
- **§4 is F299's change:**
  - the OPERATOR QUESTION, quoted in substance from `proposal.md:1-38`;
  - the argument;
  - a per-round table with R1's findings and error, R2's four corrections, and R3's D3/D11/option (a);
  - the cost: 24 tasks, 22 plus H.1–H.2. That count is from the checkbox lines, not the proposal.
  - round spend: R1 ~$0.19 summed from `r1-harness-results.json`'s 8 result lines; R2 and R3 from
    this log. The A spawns have no result line and are not counted, and the page says so.
- **F327's non-run** is stated with the `agent_trigger.py` collision, and that F301 collides too.
- **§5** walks the night's default queue against today's tree. `scripts/classify_findings.py` gives
  open A: F299, F301, F325, F332, plus a CONFLICT on F52. With nothing approved, no feature lands.
- **§6:** research candidates 1–3, each ending in what it means for AgentWeave. 2 and 3 are marked
  as D-7's.
- **The decisions box** holds seven: this window's three (F299's question plus approval, the gate
  and F292/F314, F336) and the night's four as the night wrote them. The night's #4 (F292's
  priority) is noted as answered by #2.

**APPROVALS rows.** One paragraph for `an-absent-approver-is-not-named`, with no status token.
Checked by grep: no line-initial `APPROVED`/`REVISING`/`REJECTED`/`ORDER:`/`NOTHING TONIGHT` in the
new section. There is no `ORDER:` line, deliberately. F327 has no row, and the section says why.

**Looked at in Chromium** (Playwright, `file://`), light and dark:
- body background `rgb(251,251,250)` / `rgb(22,22,26)`;
- **0 external requests** in either.
- **Defect seen and fixed.** The first render's gate table ran off the right edge, because the long
  test node ids pushed the retry column out. Fixed with `table-layout: fixed` on that table and
  `overflow-wrap: anywhere` on table code.
- Re-measured, 1100 px: document `scrollWidth` equals viewport, and no element overflows. At 420 px
  the page no longer scrolls sideways; wide tables scroll inside themselves.

Screenshots were looked at: the top of the page in both themes, §1 (light and dark), §4 (light) and
§5 (dark). They are in `%TEMP%\rv0913\`, not kept.

**Friction with the flow:** none new.

**Next:** `d6-repair` (F328 or F326, only if D-6's carve-out holds).

## Iteration 8 — d6-repair: F328 fixed on both sides, driven 3/8 → 0/24; F326 not taken; F338 filed (11:20–12:13)

Branch and `git log` matched the state file (`7b8a33f`, clean tree).

**Candidate chosen: F328.** The D-6 conditions for each candidate:
- **Requirements.** F328 touches none. `agent-conversation-workspace` already says a pass whose
  input *"was withdrawn while the attempt was being made"* gave up on nothing (spec.md :2329-2334),
  so the repair brings the code to the spec.
- **Migration, API or schema.** F328 has none. The `DELETE` route's `409` is its existing sentence.
- **A finding with a reproduction.** Test O2, kept in `testbed/scratch/opusf319/`.
- **Overlap with F299's blast radius** (`runner_commands.py`, `launchability.py`,
  `agent_trigger.py`, `runner_parsing.py`, `db/models.py`):
  - F328's files, `turn_scheduler.py` and `inbound_queue.py`, **do not overlap**. Grepped: the
    change names neither.
  - F326's three refusals are in `agent_trigger.py`, which **overlaps**. F326 was therefore not
    taken, and stays open.

**Reproduced first.** O2 was copied into `hub/tests`, run once and deleted, on `7b8a33f`. At delay
0.3: `withdrew: True`, the entry `('withdrawn', 3, …, 'delivery failed 3 times …')`, and one
`queue_entry_abandoned`.

**What the reproduction showed that the finding did not say.** That end state is reached with the
*operator's* write landing last:
- `withdraw_entry` read the row as `queued`, then waited on the lock.
- The Hub's give-up committed while it waited.
- Its unconditional `UPDATE` then ran and returned success.

So the finding's named repair, a counting write conditioned on `queued`, fixes only one of the two
writers.

**The repair** (commit `a5cb384`):
- One rule: every write that takes an entry out of `queued` without delivering it is a single
  `UPDATE … WHERE state = 'queued'`.
- `inbound_queue._withdraw_if_queued` now serves `withdraw_entry` and `withdraw_refused_entry`.
  - It commits on both branches. A rollback would expire the caller's rows, and the refused-request
    path holds some, so the next read of one would be `MissingGreenlet`.
  - It re-reads with `populate_existing`.
- The scheduler's count and give-up are one conditional `UPDATE` per entry, inside the batch's
  single transaction.
  - Give-up is decided from the computed count, not read back off the in-memory row.
  - It mirrors onto the session's copy with `set_committed_value`, because a rider carried by the
    pass's next turn has its count stated in `format_turn_prompt`.
- The re-read comment and 1.8b's docstring were corrected to match.

**Tests:** three in `hub/tests/test_a_withdrawal_and_a_give_up_do_not_both_win.py`. All fail on
`7b8a33f` and pass on the fix, 5 of 5 repeats.

| mutation | what fails it |
|---|---|
| scheduler's `state == 'queued'` dropped | test 1, the deterministic seam at `other_input_would_have_run_elsewhere` |
| withdrawal's condition dropped | tests 2 and 3: the stale-copy test, and the route test with a flushed write holding the lock and a concurrent real `DELETE` |
| `set_committed_value` loop dropped | the existing `test_failed_run_returns_input.py::test_a_terminal_pre_spawn_refusal_abandons_the_entry_after_the_limit` |

**Test runs:**
- The 30 test files that touch the queue: **465 passed**.
- The full Hub suite: **4221 passed, 86 skipped**, 26 min.
- Lint: `ruff check src/ hub/ tests/` clean, `black --check --target-version py311` clean (after
  reformatting the new test file), `mypy src/` clean.

**Driven:** `scripts/drive/t_d6_0913_f328_withdraw_race.py`. Real routes, no row inserts, one Haiku
turn per Hub, no job.
- **Setup.** N tasks, each with evidence at a commit that is then pruned (F319's B1). N reviews are
  queued behind the reviewer's own 60 s turn. At every attempt-2 head, `POST …/continue` and
  `DELETE …/queue/entries/{head}` are fired together, at offsets 0–250 ms.
- **Pre-fix Hub:** a `7b8a33f` worktree, port 8025, profile `drive0913v`. **3 of 8 inconsistent:**
  `DELETE 200`, 3 attempts, the Hub's reason, and both `queue_entry_abandoned` and
  `queue_entry_withdrawn` for one entry. F328 is live, not only at unit level.
- **Fixed Hub:** this checkout, port 8024, profile `drive0913w`. **0 of 24 inconsistent** over two
  runs: 7 went to the operator (`200`), 17 to the Hub (`409`). 20 `DELETE`s overlapped their pass,
  the longest waiting 198 ms.
- **Afterwards:** both Hubs stopped, the worktree removed, and the profiles and `%TEMP%\f328-*`
  repos kept. Zero jobs, zero running runs, zero queued entries in either database.

**Filed F338 (D, read only):** `deliver_entries_with_run` has the same check-then-write, and a plain
turn reads before holding the lock. Not measured. The drive script is the pattern for measuring it.

**F328 ledger:** Status `fixed a5cb384`, with a dated note. APPROVALS 2026-09-13 and review page
§5 each got one dated line.

**Friction with the flow:** none new. This is openspec-side and needed no spec.

**Next:** `d7-ledger`.

## Iteration 9 — d7-ledger: research candidates 2 and 3 filed as F339 (B) and F340 (B); the queue is done (12:15–12:35)

Branch and `git log` matched the state file (`ae5766f`, clean tree).

**Not duplicates.** F330–F338 cover other ground. The F299 rounds cite both candidates and absorb
neither: design D10 sends candidate 2 here, and D9 says of candidate 3 *"This change inherits that
unchanged … D-7 of today's window files it."*

**Candidate 2 became F339 (B).**
- **The four texts were checked against their current wording.** All four still say `acceptEdits`
  checks no path: `permission-postures.md:33` (and `:39`, `:64`), `agent-capability-plane/spec.md:256-259`,
  `runner_commands.py:63-64`, and `DECISIONS.md:772-774`. The ledger repeats it at F299 `:24342`
  and F281 `:21078`.
- **Reproduced Q1.** `scripts/drive/t_d7_0913_accept_edits_confined.py`, run twice.
  - The argv is `build_command(runner="claude", mcp_command=None)`, asserted to carry
    `--permission-mode acceptEdits` and no server or approver. The spawn is `PtySession`, with
    `CLAUDE*` stripped, in `%TEMP%`.
  - `init` said 2.1.269 and `acceptEdits`.
  - Both runs: the inside Write landed. A `../` Write, an `echo > ../` and an absolute-path Write
    outside were each in `permission_denials`, and none landed. Cost $0.028 and $0.015.
- **The Hub records none of it.** `grep -rn permission_denials hub/hub` returns nothing.
- **Operator question:** does 1b's rejection of `acceptEdits` on no grounds stand on its remaining
  reason? It is added to `decisions_for_user`, to APPROVALS 2026-09-13 as one dated line with no
  token, and to review page §6 as one dated paragraph. The page was not corrected, because the
  queue item said to file it, not repair it.

**Candidate 3 became F340 (B).**
- **Code verified.**
  - `launchability.py:251-260` is `.limit(1)`, with no ordering or recency.
  - The only writer is `agent_actions.py:442`, and it only sets the column.
  - `parse_claude_line` has no `system` branch; a `system` line falls through to `:371`.
  - A non-JSON stderr line becomes text at `:235-238`.
- **Permanence measured.** A one-off test planted one stamped 2020 run and five later unstamped
  `failed` runs; `harness_has_honoured_mcp` returned True. The test was then deleted.
- **Harness statuses.** Taken from R3's `r3-harness-results.json` (`connected`, and `failed` for
  both a crashed and a slow server) and R2's `ABSENT` rows (policy).
- **Scoped to today's tree.** The grounds decide only the *described* path
  (`agent_trigger.py:1001-1005`). The approver is named regardless, so the killed-at-first-write
  consequence belongs to F299 today, and becomes F340's once F299's change gates the approver on
  grounds. That is why it is B now.
- **Not measured.** The research's Q4c shape (missing binary with approver) under the PTY.

**Also:** the new script passes `ruff check` and `black --check` (one SIM105 fixed before the
second run).

**Friction with the flow:** none new.

**The queue is done.** `next_action` is `null`. The two skipped F327 rounds stay first in line for
tomorrow's loop. Window stop is 17:00; this ends it at 12:35.

# Triage — the 14 in-flight openspec changes

**Method.** For every change: read `tasks.md`'s open items in full, then check whether each open
item is (a) already resolved in code with an explanation left in place, (b) a genuine coding gap, or
(c) an operator judgement call a loop cannot answer. Per `next_action`'s warning, no open-task count
was trusted on its own — q1 already proved one `tasks.md` had fallen behind its real implementation.
Cross-referenced against `.claude/autonomous/2026-08-15-judgement-evidence.md` (q3) for which
judgement calls already have their artefact captured.

**Verdict key.** `archive (pending d1)` = every open item is a human judgement call; code is done;
nothing for the loop to do; blocked only on the operator clearing d1 in
`2026-08-15-judgement-evidence.md`. `resume` = real, uncaptured coding work remains. `drop` = the
change should not be archived as-is; its live remainder should be re-cut into new change(s).

| Change | Open/Total | Verdict | Reason |
|---|---|---|---|
| `2026-07-30-hub-native-experience` | 69/188 | **drop** (as a single change) | See detail below — almost entirely superseded by later archived changes, documented in the file's own reconciliation notes; only a small genuine remainder (13.4, 13.9, 13.11, part of 13.3, 15.1–15.3, and all of 14 needs re-scoping) should survive, as new change(s) — this is **d2**, the operator's call. |
| `2026-08-10-blocked-and-conversation-binding` | 4/42 | archive (pending d1) | 8.10–8.13 are all judgement calls ("does the board read as...", "does X surprise you"). **Captured in q3** (driven live 19:41, see judgement-evidence.md). |
| `2026-08-11-declining-a-question` | 2/31 | archive (pending d1) | 6.8–6.9 are judgement calls about how dismissal/staleness *feels*. **Captured in q3** (driven live 19:27, see judgement-evidence.md). |
| `2026-08-12-hub-owns-the-spec-document` | 8/92 | **resume** (small) | 12.3 (default spec-charter binding) was deliberately left unimplemented — the obvious approach makes "no charter" unreachable during spec work; D9 needs amending before this is coded, not the code itself. 16.8 (refusal of event modification/deletion) is a documented test-only gap — no code path offers either, so there's nothing to fix, only a test asserting absence to write. The remaining six (17.1–17.8 minus 12.3/16.8) are judgement calls, **partially captured** in q3 (17.6 half-answered, 5.2 done, 5.3 open). |
| `2026-08-12-run-without-a-git-repository` | 4/31 | archive (pending d1) | 5.1–5.5 are judgement calls (does the no-repo project behave sensibly, does the workspace note read as informative). **Captured in q3** except 5.3 (driven live 18:13; 5.3 is a pure visual read with no API proxy — stays open regardless). |
| `2026-08-13-a-document-earns-its-name` | 4/42 | archive (pending d1) | 9.1–9.4 are judgement calls (placeholder, rename timing, panel behaviour, table readability). **Captured in q3.** |
| `2026-08-13-a-gate-that-only-evidence-opens` | 4/34 | archive (pending d1) | 5.1–5.4 are judgement calls (refusal actionability, demotion, whether `contract` earns its keep, gating point). **Captured in q3** (driven live 18:46, see judgement-evidence.md). |
| `2026-08-13-a-posture-that-survives-the-handoff` | 4/23 | archive (pending d1) | 4.1–4.4 are judgement calls (unprompted self-verification, posture surviving a hop, workspace boundary, wider execution surface). **Captured in q3** (driven live 18:31, see judgement-evidence.md — this drive also found and fixed a real documentation bug in this change's own tasks.md/user test guide, filed under q4). |
| `2026-08-13-a-requirement-knows-its-work` | 5/53 | archive (pending d1) | 8.1–8.5 are judgement calls (coverage legibility, drift tone, migration correctness on real data, retention policy, which agent holds `can_accept_evidence`). **Captured in q3.** |
| `2026-08-13-answers-arrive-together` | 7/32 | archive (pending d1) | 1.4 and 4.6 are resolved-with-explanation, not real gaps (1.4: the pre-fix behaviour no longer exists to reproduce, evidenced instead by the live database row named in the proposal; 4.6: the concurrency guarantee was established not to exist, so no test could assert it meaningfully — left open and named on purpose). 5.1–5.5 are judgement calls. **Captured in q3** (driven live 18:59, see judgement-evidence.md). |
| `2026-08-13-the-hubs-procedure-outranks-an-installed-one` | 3/25 | archive (pending d1) | 5.3–5.5 are judgement calls (Claude-agent parity, real-`openspec/` project behaviour, whether naming a competing workflow is useful or noise). **Captured in q3** (driven live 19:16, see judgement-evidence.md). |
| `2026-08-13-the-interview-is-a-conversation` | 5/24 | archive (pending d1) | 5.1–5.5 are judgement calls. **Captured in q3** — 5.1/5.2/5.5 written up 17:54 from an already-run turn (`out-e058d01e`); 5.3/5.4 driven live 19:57 (charter-less agent, genuine either/or fork). |
| `2026-08-13-the-spec-tool-reaches-the-agent` | 3/27 | archive (pending d1) | 6.1, 6.4, 6.5 are judgement/decision calls (document readability, the ten-minute turn timeout, no-`conversation_id` behaviour). 6.2/6.3 are now **done**, not judgement calls — both answered directly by q2's `aw-loop10` drive (a Claude agent ran the flow; the document went through propose/approve/build/merge for real) and ticked in `tasks.md`. **Gap fixed 2026-08-15 ~20:1x (q8 review)**: this row and the change itself were missing from `judgement-evidence.md` entirely — two prior iterations (14:54, and this triage row itself) flagged the omission but neither one added it; now has a full section. |
| `2026-08-13-the-tool-list-matches-the-tools` | 3/26 | archive (pending d1) | 5.1–5.3 are judgement calls (watch `submit_spec_document` get called, read the rendered document, confirm no-document agents aren't misled). **Captured in q3** — this is the change q1 closed the false "17 open" reading of; its remaining 3 were always judgement calls, never code. |

---

## Detail: `2026-07-30-hub-native-experience`

This is the only change needing more than a line, because it's the only one where the open-task
count (69) is actively misleading rather than just judgement-gated. The file has been reconciled
against its successors multiple times (2026-08-02 through 2026-08-12), and each reconciliation left
a dated note **in place**, above the relevant section, rather than silently checking boxes — so the
evidence for every claim below is already written into `tasks.md` itself, not asserted fresh here.

**Sections closed by real, archived successor implementations** (checkboxes intentionally left
unchecked under the file's own "reconciliation rule" — the successor's task list is authoritative):
- **9. Accounting and budgets** — closed by `archive/2026-08-03-accounting-and-budgets`.
- **10. Multi-project support and navigation** — closed by the local multi-project workspace
  successor (dated update 2026-08-04).
- **11. Composer, first cut** — closed by `archive/2026-08-03-composer-intelligence` plus the
  conversation-workspace change.
- **12. Composer controls** — closed by the same two successors (dated update 2026-08-03).

**Section 13 (agent identity, charters, skills) — marked SUPERSEDED, genuinely partial.** A
2026-08-12 reconciliation pass checked every item against the tree rather than assuming closure:
- Done via successors: 13.1 (`Runner`), 13.2 (closed 2026-08-12, the unique-index race fix),
  13.8 (roster injection), 13.10 (`request_agent` + budget), 13.12 (`roles.py` etc. removed),
  13.13 (stronger than asked — `init` doesn't exist at all).
- Partly done: 13.3 (the `Charter` record exists as name+content; the scope/default-skills structure
  does not).
- **Genuinely open, confirmed against the tree, not assumed:** 13.4 (scope enforcement — no scope
  field exists anywhere), 13.9 (single-agent projects still emit an empty `### Team` block instead
  of omitting it), 13.11 (no composition-inspection surface exists).
- **Not assessed by that pass, still unknown:** 13.5, 13.6, 13.7, 13.14.

**Section 14 (specification traceability and authoring) — marked SUPERSEDED, "do not implement from
this list."** This is exactly the territory that `2026-08-12-hub-owns-the-spec-document`,
`2026-08-13-a-requirement-knows-its-work`, `2026-08-13-a-gate-that-only-evidence-opens`, and several
of the other 2026-08-13 changes went on to deliver piece by piece. **Update (2026-08-15, later
iteration): the line-by-line mapping this note originally recommended is now done** — see "Section 14
mapping" below the change-by-change table. Net: 9 of 19 items fully delivered, 2 delivered but
distributed across several successors, 2 partially delivered with a small named gap, 1 superseded by
a different design with its intent covered elsewhere, 1 blocked only on 16.2's sync bookkeeping (not
new work), 1 standing, and 3 genuinely undelivered — 14.11, 14.12 (in-place proposal/authoring), and
14.14 (scope-discipline enforcement).

**Section 15 (approval gates in the conversation) — genuinely open, not just superseded.** The
2026-08-12 note is explicit that this is easy to misread as closed and isn't: permission/question
cards exist and are closed by other successors, but 15.1's subject is **task-lifecycle and
specification-gate** decisions reaching the operator inline in the composer, which nothing does
today — task transitions still live only on `TasksBoard.tsx`. 15.2 is half-blocked on section 14.
15.3 references a `spec-traceability` spec that doesn't exist under that name.

**Section 16 (closeout)** is a bookkeeping phase (sync every delta spec into `openspec/specs/` under
its current name) blocked on 16.2, which is itself blocked on deciding what happens to 13/14/15.

**Recommendation for d2:** don't resume this change as a whole — most of the checkbox list is dead
weight now that its content lives in five archived successors. Don't archive it either — 13.4, 13.9,
13.11, part of 13.3, all of 15, and 14's three genuinely undelivered items (14.11, 14.12, 14.14, plus
14.5's and 14.13's small remainders — see "Section 14 mapping" below) are real, undelivered work. The
shape that matches every other section here (9–13, split cleanly into focused successors) suggests:
split whatever remains live across 13/14/15 into one or two new dated changes, then archive this
umbrella once nothing in it is left unaccounted for. That is a proposal, not a decision — d2 is the
operator's.

---

## Section 14 mapping — the pass the previous entry left undone

**Method.** For each of 14.1–14.19, grepped the nine 2026-08-13-dated changes' `specs/*/spec.md`
and `tasks.md` for the requirement it maps to (not just proposal.md prose), the same code-first
discipline as the section-13 pass. Checked `openspec/specs/` to see which delta specs are actually
synced versus still living only in a change directory.

| Item | Verdict | Delivered by / reason |
|---|---|---|
| 14.1 identifiers, stable across rewording/retirement | **delivered** | `2026-08-13-a-requirement-knows-its-work`, requirement "A requirement is addressable outside its document" — active/retired, digest, anchor, `observed_at`. |
| 14.2 task declares requirements it serves | **delivered** | same change, requirement "Work is linked to the requirements it serves". |
| 14.3 evidence carries kind/origin/time/actor, refuses anonymous | **delivered** | same change, requirement "Evidence names what produced it and what it was produced against". |
| 14.4 per-requirement verification state, inline | **delivered** | same change, requirement "Coverage is one computation with one precedence". |
| 14.5 stale evidence, distinguish stale/absent, retain superseded, mark editorial | **partial** | staleness-by-digest and retired-not-deleted are both delivered (tasks 1.4, 4.11, 7.5). The specific "operator marks a change editorial" affordance does not exist anywhere — grepped `editorial` across the change's tasks.md and spec.md, no match. |
| 14.6 drift detection, deliberate resolution | **delivered** | same change, requirement "A changed implementation raises a candidate, never an edit" — `requirement_drift`, candidate/resolved/superseded. |
| 14.7 bidirectional navigation | **delivered** | same change, requirement "A requirement and its work are navigable in both directions". |
| 14.8 project verification coverage | **delivered** | same change's coverage computation, read by `2026-08-13-a-gate-that-only-evidence-opens` for the gate itself. |
| 14.9 rigor declaration, promotion/demotion, preserved on demotion | **delivered** | `2026-08-13-a-gate-that-only-evidence-opens`, requirements "A document declares how strictly it is enforced" + "Only the operator changes rigor" + "Rigor is only raised on a document that can be enforced"; demotion preservation is explicit (tasks 2.4, 2.5, 4.2, 4.4). |
| 14.10 enforce the gate | **delivered** | same change, requirement "Approval is refused while a gated requirement is unverified". |
| 14.11 direct edits on sketches, proposals on contracts/gates, dual attribution | **not delivered** | grepped all nine changes' specs and the Hub's `spec.py`/`mcp_server.py` for a proposal/direct-edit distinction gated by rigor — no match anywhere. Nothing in any successor implements this. |
| 14.12 in-position, individually-acceptable proposals | **not delivered** | same search, no route or tool exists for a proposal that lands in-place in a rendered document and can be accepted/rejected without residue. |
| 14.13 on-ramps: derive from implementation, grow from conversation, start from template | **partial** | "grow from conversation" is delivered and is in fact the *only* on-ramp — `2026-08-12-hub-owns-the-spec-document`'s `spec-chat-session` requirement states creating a document "MUST NOT require... choosing a template." "Derive from implementation" and "start from template" do not exist as alternate paths. |
| 14.14 scope authoring to specs; discovered implementation work is proposed not performed | **not delivered as an enforced requirement** | `2026-08-13-the-hubs-procedure-outranks-an-installed-one` *observed* an agent correctly declining to invent scope in one live run and the charter carries prose asking for this, but nothing in any successor's spec.md asserts or tests it — it is charter guidance, not a built control. |
| 14.15 make aw-spec-* skills reachable; invert aw-verify onto requirements | **superseded by a different design** | the named skills were deleted outright (`hub-owns-the-spec-document` task 12.5), not made reachable — the Hub replaced the skill channel with a conversation-embedded procedure. The underlying intent of the second half (evidence attaches to requirements) **is** delivered, by `a-requirement-knows-its-work`'s `record_evidence`/`requirement_id` linkage, just not via "aw-verify inverted." |
| 14.16 workspace to the same standard as the conversation | **delivered, distributed** | no single successor owns this; it's the combined effect of `a-document-earns-its-name` (naming), `the-interview-is-a-conversation` (interview quality), and `hub-owns-the-spec-document` (the workspace existing at all). |
| 14.17 plain/portable; reconcile external edits without losing links/evidence | **delivered** | `hub-owns-the-spec-document` task 17.7 (external edit is detected and reported, changes nothing silently) plus `requirement-traceability`'s digest explicitly tracks "whether the change arrived through the Hub or from an external edit" — identity survives because it's content-addressed, not actor-trusted. |
| 14.18 verify against `spec-traceability` and `spec-authoring` | **cannot close, not a coding gap** | no spec is named either `spec-traceability` or `spec-authoring` anywhere. The nearest equivalents are `requirement-traceability` (fully written, but **not yet synced** into `openspec/specs/` — confirmed by listing the directory) and `spec-document-authority` (synced). This is the same 16.2 sync blocker section 16 already names, not new work. |
| 14.19 `/handoff` | **standing** | housekeeping checkpoint, not a feature — always "open" by design at the point a phase is mid-flight. |

**Net for d2.** Of 19 items: 9 fully delivered, 2 delivered-but-distributed-across-several-changes,
2 partially delivered with a named-and-small remaining gap (14.5's "mark editorial", 14.13's two
missing on-ramps), 1 superseded-by-a-different-design with its intent covered elsewhere, 1 blocked
only on the 16.2 sync bookkeeping, 1 standing, and **3 genuinely undelivered with no successor
touching them at all: 14.11, 14.12, and 14.14** — the in-place proposal/authoring mechanic and
scope-discipline enforcement. If the operator takes q5's split proposal, these three (plus 14.5's and
14.13's small remainders) are the concrete content of "whatever remains live in 14" that a new change
would need to cover — the rest of section 14 can be treated as closed by successors already archived
or in flight.

---

## What this closes and what it doesn't

**Closes:** the `next_action` instruction to triage all 14 changes, one line each, with the
q1-style code-verification discipline applied rather than trusting checkbox counts. Also closes the
follow-up `next_action` (2026-08-15 later iteration) to map section 14's 19 items to whichever
2026-08-13 change delivered each — see "Section 14 mapping" above.

**Does not close:** d1 (the operator has to actually answer the ~40 judgement calls before any of
the 13 "archive (pending d1)" rows can be archived) or d2 (hub-native-experience's split/drop/resume
call). Both were already recorded as non-blocking decisions_for_user entries before this pass; this
file gives d2 a concrete proposal to react to instead of an open question.

**New gap surfaced by this pass, not previously recorded:** `the-spec-tool-reaches-the-agent` is
entirely absent from q3's judgement-evidence file — neither answered nor listed as pending. The next
judgement-evidence session should add its 6.1–6.5 rather than assume q3 was exhaustive.

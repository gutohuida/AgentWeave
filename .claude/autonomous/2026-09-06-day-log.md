# 2026-09-06 day window

Newest entry at the BOTTOM. Playbook: `.claude/loops/day-window.md`.

Armed 2026-09-06T08:55:05+01:00 on `autonomous/2026-09-04-daily` (parent master@a38caee), model opus.

---

## Iteration 1 — 2026-09-06 09:00 — compose the queue

Fresh process. Read `.claude/loops/day-window.md` in full, then did its steps 1–6. **No product
code, no test code, no proposal was written this iteration** — that is the playbook's instruction
for iteration 1 and it was followed.

### Limits, restated so a later firing inherits them

Stay on `autonomous/2026-09-04-daily`; no commits, merges or rebases onto `master`; nothing
outward-facing (no publish, release, PR, force-push, history rewrite — push, do not open PRs);
nothing destructive; do not browse the open web; every claim measured or labelled unverified;
operator decisions go to `decisions_for_user`, never guessed; stage explicit paths, never
`git add -A`; tests under `py -3.11`, `black --target-version py311`; never drive `proj-5e960453`
or `proj-18e5d4e0`, never touch port 8000, drive Hub is 8011; every real agent turn binds
`claude-haiku-4-5`; never leave a job enabled; do not run the whole Hub suite in this window unless
something done here could plausibly have broken it.

### Step 1 — the merge gate: **dormant, and it would not have opened anyway**

**Condition zero is still unmet.** `STATE-day.json`'s `limits[0]`, seeded by
`.claude/loops/arm-cycle.ps1`, still reads *"No commits, merges or rebases onto master. Never
auto-merge."* A limit in the state file outranks the playbook, so the gate was checked and
reported, not executed. Nothing touched `master`.

The four conditions, checked anyway and measured at 09:00:

| # | condition | verdict | measurement |
|---|---|---|---|
| 1 | not on `master`, and `git rev-list --count HEAD..master` is 0 | **pass** | on `autonomous/2026-09-04-daily`; count `0` — a genuine fast-forward |
| 2 | `git status --short` empty, `HEAD` equals `@{u}` | **pass** | empty; both `f819dc6` |
| 3 | CI concluded `success` for **this exact sha** | **FAIL** | run `34020422896` for `f819dc6` is `in_progress` at 09:00 |
| 4 | newest `DIRECTION.md` section has no line-initial `HOLD MERGE` | **pass** | no `HOLD MERGE` anywhere in the file |

**So the gate fails on condition 3 independently of condition zero.** That is worth stating plainly
on the review page: even with the operator's seeded limit relaxed, this morning would not have
landed, and the reason is not a stale green — it is that CI had not finished.

Worse, and this is the part the operator should see: **the branch's two most recent *completed* CI
runs both failed.** `3a96ddb` (run `33998598976`) and `2da52ce` (run `33998517779`), each on the
`hub-test` job alone — every other job green on both, all six `test (os, py)` matrix legs, `ui-test`
and `build` included. Both are F292.

### The F292 instrumentation fired in CI overnight — and its answer is a negative

The night window (N-1) landed a checked-out-connection registry in `hub/tests/conftest.py`
specifically so that *"the next CI occurrence will name the holder instead of the victim"*, and
called waiting for it the cheapest next move. **That occurrence happened the same night.** From run
`33998598976`, `hub-test`, `3966 passed, 18 skipped, 1 error in 741.71s`:

```
ERROR at setup of test_assigning_a_reviewer_and_sending_to_review_in_one_patch_is_accepted
  sqlalchemy.exc.OperationalError: (sqlite3.OperationalError) database is locked
F292 DIAGNOSTIC: the schema reset failed. Who was holding the database:
  [before dispose]   pool.status()='Pool size: 5  Connections in pool: 0 Current Overflow: -5 Current Checked out connections: 0'
  [before dispose]   registry: no connection checked out
  [before drop_all]  pool.status()='Pool size: 5  Connections in pool: 0 Current Overflow: -5 Current Checked out connections: 0'
  [before drop_all]  registry: no connection checked out
  [at failure]       pool.status()='Pool size: 5  Connections in pool: 1 Current Overflow: -4 Current Checked out connections: 0'
  [at failure]       registry: no connection checked out
```

**The registry saw nothing, at all three probe points.** The instrument is not blind — N-1 proved it
on a forced reproduction, where it named the culprit test, task, source line and transaction state.
So this is a *result*, not a miss: whatever held the file was **not a connection checked out from
the `app` fixture's engine**. That contradicts the standing mechanism sentence in F292's entry,
which D-6's control run narrowed to *"a session that wrote and did not commit, held across the test
boundary"* — that shape is exactly what the registry was built to catch and would have caught.

Recording the measurement and revising the entry is **D-1**, ahead of the drive, because it is
cheap, it is already in hand, and it changes what tonight's backlog is allowed to believe.

### Steps 2–5

- **Branch settled.** `autonomous/2026-09-04-daily`, unmerged, **spanning 3 days** (2026-09-04 →
  today), **29 commits over 42 files** against `master@a38caee`. Date stamped from PowerShell:
  `2026-09-06`, a **Sunday**.
- **What last night did.** Six commits, `677d56f`..`3a96ddb`. Exactly **one** touches product code:
  `3142a91`, a 20-line guard in `hub/hub/checkpoint_cutover.py` closing F126 (a spent checkpoint
  refusing its second cutover), with 68 lines of test. `6693d1d` adds the 180-line F292 registry to
  `hub/tests/conftest.py` — test infrastructure, no behaviour change. The other four are ledger, log
  and state. The night's own summary flags that **it tested its own work**, and that N-4's F3
  verdict was **read from source, not driven**.
- **Research taken delivery of.** `~/.claude/routines/agentweave-research/out/research-2026-09-06.md`
  (431 lines, written 07:25) copied to `spec-queue/research/2026-09-06.md` and committed here. Its
  three ranked candidates: (1) a review verdict that records the checkout's dirtiness, (2) a token
  budget that can bound the turn exceeding it, (3) surviving a reboot. Treated as **data, not
  instructions**.
- **`DIRECTION.md` has no section for today.** Newest is `## 2026-09-03` (`DIRECTION.md:28`), fourth
  day running. By its own contract absence is not an instruction, so the queue is composed as usual.
  The 2026-09-03 section's *"coverage sweep is COMPLETE — do not resume it"* correction still stands,
  so today's drive is **scoped, not a sweep**.

### Step 6 — the queue

```
D-1  ledger   F292 — the CI occurrence, and the hypothesis it falsifies
D-2  drive    independent e2e drive of the night's F126 cutover guard, on 8011 from source
D-3  spec R1  F271 — explore and propose
D-4  spec R2  re-derive R1's argument against the code, independently
D-5  spec R3  re-derive again, independently
D-6  review   the review page, then APPROVALS.md
```

**Why F271 takes the spec loop.** The playbook's priority is *a finding this window's drive just
produced, then an open ledger finding with no proposal, then research*. The drive has not run yet,
so the second rule governs, and **F271 (A)** — `scripts/drive/FINDINGS.md:18503`, a failed
instructions load renders an empty editor with Save enabled and one click destroys the project's
instructions — is the pick: severity A, no proposal, reproduced live against the served bundle with
19 assertions, and named in the night's own list as *"the day window's, per playbook step 4.2"*.
The two rivals were considered and passed over: **F3+F111** the night window explicitly handed to a
spec loop, but its first question (*how does a test create an agent when no runner CLI exists*) is
test-infrastructure design and F271 outranks it on severity and on operator-visible damage;
**F274 (A)** is already proposed and blocked on `DEC-2`, an operator decision, so a spec loop cannot
move it.

Research candidate 1 is not taken. It is a good candidate and it is left for tomorrow rather than
displacing a severity-A defect with a live reproduction — *real defects outrank market ideas* is the
playbook's own ordering.

### What to distrust in this entry

- **The CI diagnostic is read from one run.** One failure, one negative. It falsifies the leading
  mechanism for that occurrence; it does not prove every F292 occurrence has the same cause.
- **Nothing was driven this iteration and no Hub was started.** 8011 is free; 8010 and 8000 were
  never approached. No job was created, so none was left enabled.
- **The branch's CI history is mixed**, not uniformly red: of the last 20 runs, 15 succeeded. The
  two failures are consecutive and both `hub-test`, which is consistent with F292's intermittency
  rather than with new breakage from `3142a91`.

---

## Iteration 2 — D-1: F292's instrument returned a negative, and one of the survivors was killed by measurement

**Ledger only. No product or test code touched; the diff is `scripts/drive/FINDINGS.md` alone,
108 insertions and 5 deletions.** No Hub was started, no job created, nothing on 8000/8010/8011.

### The logs were re-read first-hand, and the second run was not what the summary assumed

`gh run view <id> --log-failed` on both runs, not the iteration-1 summary. Run `33998598976`
(`3a96ddb`) is exactly as recorded: `3966 passed, 18 skipped, 1 error in 741.71s`, `ERROR at setup
of test_assigning_a_reviewer_and_sending_to_review_in_one_patch_is_accepted`, and the registry
printing `no connection checked out` at all three probe points.

Run `33998517779` (`2da52ce`) **did print the same diagnostic, byte-identical apart from
timestamps** — the previous entry only asked whether it had. But its failing test is a *different*
one: `test_a_wedged_review_is_restaffed_to_a_real_reviewer`, `3966 passed, 18 skipped, 1 error in
488.37s`. Both are in `tests/test_reviewer_is_not_the_author.py`, and in the progress stream they
sit **one line apart** — 3244 and 3245, both at `[ 66%]`. In one run the earlier test errors and the
later passes; in the other, the reverse. CI runs `pytest tests/ -v` with no randomisation, so the
order is fixed and the failure window **slid by one test between runs**. That is a timing-dependent
holder, not a particular test's leak — and it is a sharper localisation than the entry had.

### What the negative falsifies

The registry is not blind: N-1 proved it on a forced reproduction where it named culprit test, task,
source line and transaction state. So the negative is a *result*. It kills two readings this entry
carried:

- **D-6's narrowing** — *"a session that wrote and did not commit, held across the test boundary"* —
  is precisely the shape the registry catches. Not that, here.
- **`conftest.py`'s own mitigation comment**, which blames a live `JobScheduler` still holding a
  checked-out connection. The entry already doubted this on reading; it is now measured.

### One surviving hypothesis eliminated, one sharpened — both by reading code, not guessing

The instruction asked for "another engine on the same file" as a hypothesis. It is better than that
now, because the engines are countable. The Hub creates **exactly three**: `hub/hub/db/engine.py:34`
(the product engine, the only one the registry watches), `hub/hub/migrations/env.py:51`, and
`hub/hub/scheduler.py:2367`.

- **APScheduler's synchronous engine is eliminated for the suite.** It had to be checked rather than
  waved away, because `hub/hub/api/v1/jobs.py:497-517` documents it as a *real, measured* cause of
  exactly this error in the product — "a separate synchronous engine pointed at the same SQLite
  file … cannot take the write lock and raises `database is locked`". It is not live in tests:
  `JobScheduler.__init__` is inert, `_get_sync_engine` is reached only from `start()`, no test calls
  it (the suite's only reference sets `_scheduler_instance` to `None`), and `conftest.py:468` records
  that ASGITransport never triggers the lifespan.
- **Alembic's engine survives and is now the concrete lead.** `_run_alembic_upgrade`
  (`hub/hub/db/engine.py:54-107`) runs `command.upgrade` **in a worker thread from the loop's default
  executor** (`engine.py:104`) — threads that outlive the coroutine — and `env.py:51` builds its own
  async engine on the same `settings.database_url`, invisible to engine-level pool listeners by
  construction. The `app` fixture no-ops it *per test*, and that patch's own comment names
  `test_migrations.py::test_init_db_runs_alembic_for_file_db` as running the real thing without the
  fixture.
- **A non-SQLAlchemy writer is the weakest of the three**: `sqlite3.connect(` appears **zero** times
  under `hub/hub/`.

Each is labelled a hypothesis in the entry. None is claimed as the cause.

### What was also written down, so the negative is not over-read

The registry records connections **checked out** from the product engine. A connection of that
engine sitting idle *in the pool* while still holding a lock would also print "no connection checked
out" — but the fixture disposes between the first and second probe and `dispose()` closes the old
pool's idle connections, so that reading is weak rather than dead. The `Connections in pool: 1` at
failure is the `drop_all` attempt's own connection on the replacement pool, not a survivor.

The entry's Status line now says *open, and the standing mechanism is falsified for the two
occurrences that carried the instrument*, and points the reader at the D-1 section as superseding
the narrowing above it. The main CI table gained the two rows; its "three of the sixteen runs"
sentence was corrected in place to five rather than rewritten, and the two newest are the branch's
two most recent completed runs — which is why the merge gate's condition 3 cannot pass.

### What to distrust in this entry

- **Two occurrences, one negative each.** They falsify the mechanism *for those two*. The three
  earlier runs in the table ran before the instrument existed and are not covered.
- **Nothing was reproduced.** This is CI evidence plus code read in this repository. The alembic
  lead is a hypothesis about a `dispose()` not being complete or not being reached — it has not been
  instrumented, and `env.py` does call it.
- **DEC-1 still governs `hub/tests/conftest.py`.** Nothing in it was touched, and the alembic lead is
  recorded rather than acted on.

---

## Iteration 3 — D-2: the night's guard, driven independently — it holds where it was driven, and does not hold two steps either side

**A drive, not a re-run.** Fresh uvicorn from source on `8011` (`py -3.11 -m uvicorn hub.main:app`,
started from `hub/`, never `agentweave --port`), a fresh scratch database
(`.claude/autonomous/scratch/d2drive/d2.db`), a throwaway project `proj-d2drive`, agent `delta`.
Nothing on 8000; 8010 was listening throughout and was never addressed. No runner bound, no turn
spent, no job created — every probe here is HTTP against the cutover route and its neighbours.
**Staleness check before any conclusion:** `find hub/hub src -name '*.py' -newermt <process start>`
returned empty on both launches. The Hub was stopped at the end and `GET /jobs` returned `[]`.

New tracked fixtures: `scripts/drive/setup_d2_cutover.py` (prerequisites only — a checkpoint
otherwise costs a real Haiku turn, and none of these probes are about generation) and
`scripts/drive/t_d2_cutover_guard.py`. Nothing under `hub/hub/` or `src/` was touched.

### What the night's guard was asked that three presses in a row cannot ask

The night wrote the guard, its two tests and its own drive, and pressed the button three times from
one client. That is the whole reason this item existed. So the three probes were chosen to sit
*outside* that sequence:

1. **Follow the refusal's own advice.** The 409 ends `…if it was archived by hand, unarchive it
   first.` Do exactly that, then press again.
2. **Press twice at the same instant.** F126's own section says the way this reaches an operator is
   "a retried request after a network timeout" or "a second browser tab" — both concurrent.
3. **Hand a successor over in turn**, to check the guard does not refuse the feature.

### Two findings, both reproduced twice on two separate fresh databases

- **F293 (B)** — `unarchive` is documented *"Never refused — reopening obstructs nothing"*
  (`agent_chat.py:549`) and sets `lifecycle = "open"`, which is the only thing the guard reads
  (`checkpoint_cutover.py:97`). Press → 200; press → 409 telling the operator to unarchive;
  unarchive → 200; press → **200 and a second successor**. Two `handoff` conversations, same
  lineage, identical titles, both `open` and both returned by
  `GET /conversations?lifecycle=all`, plus two undelivered checkpoint queue entries. F126's rows,
  reached through the product's own error message.
- **F294 (B)** — two threads, one unspent checkpoint, presses issued without waiting:
  **200 and 200**, two successors, two entries, no 409 and no 500. The guard reads the lifecycle at
  `:97` and writes it at `:144`, with an `archivable` await, a successor and a queue entry in
  between, and nothing serialising the window — no claim (`take_checkpoint` has one three files
  away), no constraint. The predecessor is archived twice, which is harmless, so **the duplicate is
  silent**.

Written down in F294 rather than left implied: **shape (2) alone does not fix F294.** A
`cut_over_to_conversation_id` column read at `:97` and written at `:145` races in the identical
window. That fix needs a claim or a uniqueness constraint, not another field to read.

`F126`'s `**Status:**` line is amended in place — *narrowed, not closed* — rather than rewritten.
The night's own 13/13 reproduces exactly; nothing it claimed is retracted.

### The negative, which is a result too

Probe 3 held: cut over, let the successor's entry be consumed, checkpoint the successor, cut *that*
over → 200, two-hop chain, first hop `archived`, second `open`, one `lineage_id`, no stacked
`"Continued: Continued:"`. The guard does not over-refuse the feature it protects. **12 of 20
verdicts held; the 8 that did not are F293 and F294 and nothing else.**

### One thing the harness got wrong first, and what it cost

Probe 3's first run returned 409 `"This conversation has messages waiting to be delivered"` — that
is `archivable`'s pre-existing refusal, not the new guard, and it fired because nothing in this
fixture runs a turn, so the successor's checkpoint entry was still `queued`. **A fixture artefact,
recorded as such rather than filed.** The harness now withdraws that entry (standing in for the run
that would consume it) and re-presses, which is what isolates the guard from a refusal that
predates it. Two seams found while building it, neither a defect and both worth the next drive
knowing: operator routes authenticate against `OperatorCredential`, not `ApiKey` (`auth.py:88`),
and `Conversation`'s primary key is `sequence`, not `id` (`models.py:418`), so
`db.get(Conversation, "conv-…")` silently returns `None`.

### What to distrust

- `GET /conversations` does not carry `lineage_id`, so the lineage tallies are read from the row
  store; every press is real HTTP. The operator-visible half of F293 *is* checked over the route.
- Both findings are duplicates-of-rows, driven without an agent turn. That the second delivery
  *burns a turn* is F126's measurement from 2026-08-30, not a new one from today.
- n=2 on each. Both are deterministic mechanisms read out of the code, not timing flukes — but
  F294 is a race, and a race that reproduced twice is still a race.

**Record correction.** The commit for this iteration (`6f58c27`) carries a stray `@` as its
first line and another as its last: PowerShell here-string quoting (`-m @'…'@`) used by
mistake in a Bash `git commit`. It is already pushed, and the window's limits forbid
force-pushing or rewriting history, so it stays. `git log --oneline` shows this commit's
subject as `@`; the real subject is the line under it. Nothing about the work is affected.

---

## Iteration 4 — D-3: spec R1 for F271 — an unread editor cannot overwrite

State and git agreed on entry: branch `autonomous/2026-09-04-daily`, `iteration: 3`, HEAD `cf9045e`,
tree clean. Nothing to reconcile.

**Written:** `openspec/changes/2026-09-06-an-unread-editor-cannot-overwrite/` —
`proposal.md`, `design.md`, `tasks.md`, and a `specs/project-instructions/spec.md` delta.
`openspec validate --strict` passes; `openspec list` shows it at 0/19 tasks. No product code
touched — this window does not implement.

### The argument, in three sentences

`InstructionsPage.tsx:9` destructures `{ data, isLoading }` and nothing ever reads the query's error
state. The seeding effect at `:14-18` is `if (data)`, so a failed load leaves `content` at its
`useState('')` initial value — the same value a genuinely empty project produces — while
`isLoading = isPending && isFetching` is **false** in the error state, so the skeleton branch at
`:45` is not taken and the textarea renders. Save's `disabled` is `saveMutation.isPending` alone
(`:39`), so one click sends `{"content": ""}`, which the route accepts on purpose, and the project's
instructions are gone with no history, no undo and no confirmation.

### What R1 chose, and the one non-obvious call

The gate is **`data` present**, not `isError`. Built the four-state table from the installed
`@tanstack/query-core` rather than from memory
(`build/modern/queryObserver.js:310`, `isLoading = isPending && isFetching`): two of the four states
have nothing truthful to show and *both* currently fall through to the editor — the error state, and
a **disabled** query (`isPending && !isFetching`), which `useInstructions` can be, since it carries
`enabled: isConfigured && !!projectId` (`api/instructions.ts:14`). One condition covers both.

Whether the disabled state is reachable in the running app is **not** established — `App.tsx:484`
renders the page from a destination carrying `destination.projectId` while the hook reads
`selectedProjectId` off `useConfigStore`, a second source, and whether those can disagree while the
page is mounted was not driven. Labelled as derived-not-driven in `design.md`, and **no scenario in
the change depends on it**.

The ordering inside the gate is the part a reviewer should attack: `data` is checked **before**
`isError`, so a background refetch failure cannot take a loaded editor away from an operator who is
typing into it. The naive `isError`-first branch gets that wrong, so it is written as its own
scenario rather than left to implementation taste.

### Three references in F271 had drifted, and this round re-derived rather than carried them

`F271` cites `agents.py:1486-1490` and `:2072-2078` for the two consumers and names the second
`get_charter_content`. On today's code they are `:1529-1533` and `:2119-2121`, and the function is
`get_charter_context` (`:2108`); `get_charter_content` does not exist anywhere in `hub/hub`. The
finding's *claim* survives intact and is in fact slightly stronger than it stated: both consumers
gate on the content being **non-empty** (`if project_instructions:`,
`if instructions_row and instructions_row.content`), so blanking does not degrade the context — it
removes the section and the charter prepend entirely. Three more line numbers in the proposal's own
first draft were wrong by counting rather than reading (`:43`→`:45`, `:36`→`:39`, `:15`→`:14`) and
were corrected against the files before validating.

The component itself has **not** moved since the drive: last commit touching
`InstructionsPage.tsx`, `api/instructions.ts` or `api/v1/instructions.py` is `3430dbe`,
2026-08-29 — four days before the finding was filed. So the 2026-09-02 drive measured this code.

### Scope: one page, and the capability-wide requirement is rejected on the record

F271's own sweep counted 2 unguarded sites of 153; the other is `AgentOutputPanel.tsx:207`, a
different capability and still undriven. The tempting generalisation — put "a configuration section
that failed to load says so" into `project-environment-settings`, beside its existing *Saving
reports its outcome* — is **rejected in `design.md`**, because it is a promise about eight pages
(`App.tsx:483-490`) of which exactly one has been measured, and a corpus that describes seven
unmeasured pages wrongly is worse than one that describes a single page narrowly. Recorded as a
candidate for its own change after a sweep, so the next person does not re-derive the rejection.

### Deliberately not specced

Whether a PUT should refuse to replace non-empty stored content with `""` without a confirmation.
`F271` calls that a product decision for the operator and this round kept to it — the empty string
is a value the route accepts on purpose and "clear my instructions" is a legitimate ask. It is
recorded as `DAY-3` in `decisions_for_user` and goes on the review page as a question, not a
proposal. It is also noted in `design.md` as **not sufficient on its own**: it does nothing about the
misleading empty editor.

### What to distrust

- R1 is one round. The whole point of R2 and R3 is that this argument has not yet been re-derived by
  anyone who did not write it.
- The four-state table is read out of the installed query-core source and out of the React Query
  semantics, **not** driven in a browser. The two rows that matter for the driven defect (error, and
  success) are corroborated by `F271`'s live measurement; the disabled row is not corroborated by
  anything and is labelled as such.
- `tasks.md` claims the drive harness's 19 assertions currently pass because the page is broken.
  That is `F271`'s measurement from 2026-09-02, re-read here, **not** re-run today — this window did
  not start a Hub.

## Iteration 5 — D-4: spec R2 — the gate does not reach the button it needs to gate

State and git agreed on entry: branch `autonomous/2026-09-04-daily`, `iteration: 4`, HEAD `5891d5a`,
tree clean. Nothing to reconcile.

**This round changed something.** Round 2 opened the files R1 cites and re-derived the argument from
them before reading R1's prose. R1's central claim survives intact and every one of its citations
checked out — `InstructionsPage.tsx:9`, `:14-18`, `:39`, `:45`, `api/instructions.ts:14`,
`client.ts:74`, `main.tsx:12`, `instructions.py`'s `InstructionsUpdate` docstring,
`ProjectSettingsPanel.tsx:319`, `AgentCreateDialog.tsx:235`, `queryObserver.js:310`. Four things
changed: one real defect in the change, one open question closed, one scope call re-decided on
evidence, and three citation corrections. 0/19 tasks became 0/21.

### The defect R1 missed: Save is not inside the branch the change rewrites

`InstructionsPage` hands the Save button to `SettingsSection` as its **`actions`** prop (`:36-43`),
and `SettingsSection` renders `{actions}` in the section heading (`SettingsSection.tsx:58`) — a
sibling of `{children}` (`:60`), which is where the `isLoading` ternary at `:45` lives. The branch
governs the textarea and nothing else. Two consequences:

1. **The plan was not implementable as written.** `tasks.md` 1.4 said "not rendering Save in the
   failure block is an acceptable implementation of this". There is no failure block that contains
   Save; the three-branch rewrite in 1.2 leaves the button on screen in all three branches. Task 1.4
   now says so and names the two shapes that do work — move the control inside the gated region, or
   make `actions` conditional on the same `data` test.
2. **The loading state is a second one-click destruction path, live today.** While the skeleton is
   rendered, Save is present and enabled (`disabled` is `saveMutation.isPending` alone) and `content`
   is still `''`. A click there sends `{"content": ""}` for a project whose stored content has never
   been read. Short when the request fails fast (`retry: 1` plus backoff); **unbounded** when the
   request hangs rather than fails, because `isPending && isFetching` stays true. R1's four-state
   table calls the loading row "correct" — it is correct about the editor and wrong about the page.

The requirement text needed no change: *"for a project whose stored instructions have not been read
successfully"* already forbids it. What was missing was assertion. Added: a scenario (*A read still
in flight issues no write*), a unit test (2.3), and a drive column (4.1b — hold the GET open instead
of aborting it, and run it once against the pre-change bundle, where it is a reproduction).

Read from the code, **not driven** — `F271`'s harness has no in-flight column at all, which is
exactly why nobody had seen it.

### The open question R1 left, closed as far as reading can close it

R1 labelled the disabled-query row "derived, not driven" and left *whether the page's two project-id
sources can disagree while it is mounted* unestablished. They can, and the mechanism is nameable:
they are reconciled in an **effect** (`App.tsx:145-151`) that runs after the render it corrects,
while the store initialises `selectedProjectId` from `localStorage` and `isConfigured` from
`sessionStorage` synchronously (`configStore.ts:129-131`) and the destination is resolved from
`window.location` in a `useState` initialiser (`useWorkspaceNavigation.ts:25-27`). A first paint
where the destination names project A and the store still says `null` — or says B — is a committed
render, not a hypothesis. What remains unestablished is that such a window outlives the frame, and
no drive has put an operator in one, so **the verdict is unchanged**: the gate covers it, no
scenario asserts it. The `projectId = B, destination = A` variant (the page is A's while the hook
reads *and writes* B's) is recorded in `design.md` as an out-of-scope drive candidate, not filed as
a finding, because nothing measured it.

### The scope call, attacked rather than inherited — and it holds, now on evidence

R1 rejected a capability-wide requirement in `project-environment-settings` on the argument that it
would promise eight pages of which one was measured. R2 measured the other seven statically:
**not one of the eight pages `App.tsx:483-490` mounts reads a read query's error state.** `isError`
appears in exactly two of them — `AccountingPanel.tsx:137,147` (`updateBudget.isError`) and
`ProjectSettingsPanel.tsx:84` (`update.error ?? relocate.error`) — and both are *mutation* errors,
the save-side reporting the capability already requires. Zero of eight on the read side. That
confirms the rejection with a count instead of a caution, and it upgrades the recorded follow-up
from "someone should look" to a named gap. What stays unmeasured is whether any of the seven seeds a
write from the read it does not check; `F271`'s own 153-site sweep says only two sites in the whole
application do, so the other seven are likely misinforming rather than destroying.

### Corroboration, and three citations corrected

- The same observer that computes `isLoading` also ships `isLoadingError = isError && !hasData`
  (`queryObserver.js:331`) and `isRefetchError = isError && hasData` (`:335`). R1 argued the
  `data`-before-`isError` ordering from first principles; the library encodes exactly that split as
  two named flags, so the ordering is its distinction and not this change's taste.
- `@tanstack/react-query` is **5.90.21** installed; `package.json` declares `^5.62.16`. R1's table
  was captioned "React Query 5.62" — the floor, not what the bundle is built from. The table itself
  was read from the installed copy and is right.
- The proposal's summary bullet named an ADDED requirement *"A failed instructions load is stated,
  not rendered as an empty editor"*, which the delta does not contain — the heading is *"An unread
  instructions editor is not presented as the project's instructions"*. Corrected.

### The one thing R2 could not do

`tasks.md`'s claim that the harness's assertions "pass today because the page is broken" is still
`F271`'s 2026-09-02 measurement — no Hub was started this iteration. What R2 *could* check it did:
the harness asserts through `check(ok, label)` (`:60`) and the assertions carrying the claim are
readable in the source (`:196` skeleton gone, `:197` empty textarea, `:198` Save enabled, read-back
at `:217`/`:226`), so *"it asserts the defect"* is verified from the file and task 4.1's inversion
list is correct. Recorded in `design.md` with that distinction drawn explicitly.

**Verification:** `openspec validate --strict 2026-09-06-an-unread-editor-cannot-overwrite` passes;
`openspec list` shows 0/21. The MODIFIED requirement's heading still matches
`openspec/specs/project-instructions/spec.md:34` and all three of its original scenarios are
preserved, conditionalised rather than dropped. No product code touched — `git status` is four
modified files, all under the change directory.

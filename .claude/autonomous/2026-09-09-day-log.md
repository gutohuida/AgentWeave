# 2026-09-09 day window

Newest entry at the BOTTOM. Playbook: `.claude/loops/day-window.md`.

Armed 2026-09-09T09:09:38+01:00 on `autonomous/2026-09-08-daily` (parent master@755bdad), model opus.

---

## Iteration 1 — compose the queue

**Limits, restated so a later firing inherits them even if this one dies mid-thought.** Stay on the
cycle branch; the only permitted touch of `master` is iteration 1 step 1's `--ff-only` gate under its
four conditions. Nothing outward-facing — push, never open a PR, never force-push. Nothing
destructive. No open web. Every claim measured or labelled unverified. Operator decisions go to
`decisions_for_user`, not guessed. Stage explicit paths. Tests under `py -3.11`; `black` needs
`--target-version py311`. Drives bind `claude-haiku-4-5`, never touch `:8000`, never
`proj-5e960453`/`proj-18e5d4e0`, never leave a job enabled.

### Step 1 — the merge gate did NOT open. Condition 3 failed, and it is not a flake.

| Condition | Result |
|---|---|
| Not on `master`, `git rev-list --count HEAD..master` = 0 | **pass** — 0, genuine fast-forward available |
| `git status --short` empty, `HEAD` == `@{u}` | **pass** — clean, both `b3f4ba2` |
| CI concluded `success` for `b3f4ba2` | **FAIL** — `in_progress` at 09:2x |
| No line-initial `HOLD MERGE` in `DIRECTION.md`'s newest section | **pass** — none |

The playbook says a queued or in-progress run is not a pass, so the gate stays shut for this firing.
But the honest reading is worse than "wait for it to finish," and the number is the point:

**The last twelve completed CI runs on this branch all concluded `failure`.** Newest success is
`b246017`; every completed run since `3a1e6bc` is red. The failing job is `hub-test` in all of them,
and run `34325989834` (`0ecfc38`) names the cause:

```
tests/test_mcp_adapter_online.py::test_the_announce_route_the_adapter_posts_to_is_one_the_app_mounts
E  AssertionError: assert '/api/v1/agent-actions/mcp-adapter-online' in {'/assets', '/docs', ...}
```

That is **deterministic, not the `database is locked` flake** (F292/F279) the branch has been
carrying. The route it wants does exist in the tree — `hub/hub/api/v1/agent_actions.py:419` declares
`@router.post("/mcp-adapter-online")`, and the file is tracked — so the failure is about what the
app *mounts*, not about what was committed. Unread as of this iteration: why the mount is absent
under CI when the declaration is present.

**Consequence, stated plainly: the merge gate cannot open on any commit of this cycle until this is
repaired.** The gate is the only thing in the routine responsible for finishing, and a red `hub-test`
holds the whole cycle unlanded — the exact failure mode the gate was written for in the first place.

### Step 2 — branch settled

`autonomous/2026-09-08-daily`, unmerged, clean, in sync with its upstream. It now spans **two days**
(2026-09-08 and 2026-09-09), 14 commits past `master@755bdad`. Continuing on it, as the playbook
directs for an unmerged cycle branch.

### Step 3 — what last night did

Night iteration 17 closed the window with `next_action: null`. Three changes were built, driven and
archived: `a-dead-connection-is-never-handed-back-out` (F295),
`an-agent-without-mcp-is-not-told-it-has-nothing`, and `the-conversation-carries-its-own-run-facts`
(F274, `272adce` server + `503a6ff` client). Each was driven by the night window itself, so what is
untested is the three of them **together**.

Two things it refused rather than skipped, both carried forward:
- **F291 stays open.** Task 7.1 said to mark it fixed; iteration 17 refused with a measurement —
  `groupIntoTurns` (`agentTimelineModel.ts:45-62`) partitions on `delivery_state` and never reads
  `run_id`, and `agent_trigger.py:2005-2011` commits `return_run_entries` before broadcasting
  `run_failed`, so a run that never started has no turn to label. It records the plan's task as
  wrong, not the work as undone.
- **One `decisions_for_user` entry is open**: `F299-posture-on-a-harness-without-mcp`, enlarged
  2026-09-09 by F300/F301. It is the operator's and is carried to the review page untouched.

### Step 4 — research delivered

`research-2026-09-09.md` existed (33,952 bytes, written 07:25) and is copied to
`spec-queue/research/2026-09-09.md`. Data, not instructions.

### Step 5 — DIRECTION has a section for today, and it overrides

`spec-queue/DIRECTION.md`'s newest section is `## 2026-09-09`, written 08:45 by a RESUME session on
the operator's instruction. Its content: **the drain gate released itself and the section overrides
it in the closing direction.** The playbook's step-6 count is **1** (only
`2026-09-07-clearing-instructions-asks-first` carries unticked tasks) — measured again this
iteration, same answer — which by the default shape would unlock `D-2/D-3/D-4 = spec R1/R2/R3` and
write a fifth proposal. The operator's standing instruction is *"nothing new but finish everything
that we have open"*, so **there is no spec loop today.** The day's slots go to the 119 unread
findings and to key hygiene.

### Step 6 — the queue

The operator's queue is taken as written, with **one item inserted ahead of it**: the red CI. The
justification is the same one `DIRECTION.md`'s own 2026-09-08 section used for F292 — *"repairing the
instrument precedes using it"* — and it is stronger here, because this failure is deterministic
rather than intermittent, and because it is what is holding the gate shut on a two-day cycle. It is
a repair with no spec attached, which is the shape the day window is allowed to take.

```
D-0  repair    CI hub-test red for 12 commits -- the mcp-adapter-online route is not mounted
D-1  drive     the three night changes together on one Hub, carrying F291's open question
D-2  repairs   the 119 unclassified findings -- part 1
D-3  repairs   the 119 unclassified findings -- part 2
D-4  repairs   key hygiene -- recount the aw_live_ literals, then fix
D-5  review    the review page and today's APPROVALS section
D-6  repairs   if the day has room
```

No other work this iteration, per `next_action`.

---

## Iteration 2 — D-0: the red CI, which is a test reading a data structure that no longer exists

**Repaired.** `hub/tests/test_mcp_adapter_online.py:159` read `create_app().routes` directly. The
route it looks for is mounted; the *iteration* is what stopped working.

### What it actually was

`starlette` 1.x stopped flattening `include_router` into `app.routes`. It now holds
`_IncludedRouter` wrappers whose inner `APIRoute`s carry **relative** paths, so a direct
comprehension over `app.routes` sees only the routes declared on the app object itself.

| | starlette | fastapi | `{r.path for r in create_app().routes}` |
|---|---|---|---|
| this machine | **0.52.1** | 0.136.3 | 161 paths, target present |
| CI (`pip install -e ./hub[dev]`, resolved fresh) | **1.6.0** | 0.141.1 | **7 paths**, target absent |

The seven, measured in a venv built to CI's resolution: `/assets`, `/docs`,
`/docs/oauth2-redirect`, `/health`, `/openapi.json`, `/redoc`, `/{full_path:path}` — **byte-identical
to the set CI's assertion message printed**, truncation aside. That is the reproduction, and it
rules out flake, ordering, and anything about what was committed.

So the test could never have passed on CI and could never have failed here. It was not a regression
at `3a1e6bc`; it was **wrong on the commit that introduced it**, and the dev machine could not say so.

### The part worth keeping

`hub/tests/_routing.py` already exists for exactly this, and its docstring already names two tests
that made the same mistake — `test_mcp_body_contract` and `test_request_strictness`. This is the
**third**, and the first to reach CI unnoticed. The fix is to use it:

```python
assert "/api/v1/agent-actions/mcp-adapter-online" in api_route_paths(create_app())
```

### Verification — measured, not reasoned

Built a throwaway venv at CI's exact versions (`py -3.11 -m venv`, `pip install -e ./hub[dev]`,
`pip install -e .` -> starlette 1.6.0 / fastapi 0.141.1) rather than trusting that three passing
siblings implied a fourth would pass.

| Check | Result |
|---|---|
| Old expression under 1.6.0 | 7 paths, target **absent** — failure reproduced |
| New expression under 1.6.0 | 161 paths, target **present** |
| Negative control (`/api/v1/agent-actions/no-such-route`) under 1.6.0 | **absent** — the assertion still has teeth |
| `test_mcp_adapter_online` + `test_request_strictness` + `test_mcp_body_contract` under 1.6.0 | **28 passed** |
| `test_mcp_adapter_online.py` under 0.52.1 (this machine) | **9 passed** — no regression here |
| `ruff check src/ hub/ tests/` | clean |
| `black --check --target-version py311 src/ hub/hub/ hub/tests/ tests/` | 557 files unchanged |

The whole Hub suite was **not** run, per `next_action`. It did not need to be: CI's own run at
`0ecfc38` reported `1 failed, 4031 passed, 18 skipped` — this was the single failure in 4,032 tests,
and the three commits since touch only log and state files.

### Scope

No spec, and the D-6 carve-out shape holds on inspection: one test file changed, no product code,
no openspec requirement, no migration, no API shape, no schema the UI reads. The route at
`hub/hub/api/v1/agent_actions.py:419` was correct throughout and is untouched.

### Two things left deliberately undone

- **No guard against a fourth occurrence.** A test that greps `hub/tests/` for `app.routes` would
  false-positive on the three files whose *comments* document this trap, including `_routing.py`
  itself. Writing a brittle guard in a hurry is how the trap gets disarmed and re-armed. Recorded
  here as a candidate rather than half-built.
- **The version gap itself is not closed.** Nothing pins starlette, so CI resolves the newest
  allowed on every run and this machine keeps 0.52.1 until something upgrades it. **Any test that
  reads a framework data structure is currently unverifiable locally by default.** That is a
  standing hazard, not today's repair, and it is the operator's call whether to pin.

Appended both to `.claude/handoffs/DEAD-ENDS.md` (Hub test patterns), with the venv recipe, so the
next window does not re-derive it.

### The gate

Still shut for this firing — the fix is being pushed now, so the earliest commit that can satisfy
condition 3 is this one, once its run completes. Nothing else about the gate has changed.

---

## Iteration 3 — D-1: the three changes on one Hub, and the first turn of every agent is told a falsehood

One Hub on `:8011` from source, a **fresh** database (`…/Temp/aw0909/aw0909.db`, migrated to head
`0102`), a fresh project `proj-dd368932acc8` outside this repository, every agent on a
`claude` / `claude-haiku-4-5-20251001` runner. `:8000` was PID `10556` before the drive and PID
`10556` after; nothing listened on `:8010` at any point. `GET /jobs` and `GET /loops` both `[]` at
teardown, and the Hub is stopped.

### What the three changes did, together

| | measured |
|---|---|
| **mcp-notice** — first turn of a fresh agent | HTTP rendering, naming `HUB_URL`, `/api/v1/agent-actions`, `AW_RUN_TOKEN`, **no credential value** |
| **mcp-notice** — second turn, same agent | MCP rendering, on grounds earned by turn 1's own adapter announcing itself |
| **F274** — a conversation whose runs are no longer recent | **2 turn boundaries, 2 stat lines** (`Worked for 13s · 33,892 tokens`, `Worked for 10s · 34,594 tokens`) with two later runs sitting in *other* conversations |
| **F274 + F295** — a run killed by a Hub shutdown, seen after restart | **`Turn failed`**, `Worked for 12s`, on screen, from the conversation's own facts |
| **F295** — 30 ordinary requests immediately after cancelling a run mid-flight | 30/30 answered, **none slower than 3 s**, no failure |
| **F295** — Ctrl-Break with a real Haiku turn in flight | shutdown complete in **0.5 s**, no *"did not settle"* warning, no `Event loop is closed` |
| **F291** — three deliveries to a pinned-text-file CLI | 3 `failed` runs, 3 `abandoned` entries, `runs=3` in the chat payload, **0 turn boundaries on screen**, 1 `NOT DELIVERED` |

The notice A/B is the part no single-change drive could have produced: it needs two turns of one
agent on one Hub, because the grounds are a row written by the **first** run and read by the
**second**. Both readings came out of the agent's own mouth — the message asked it to quote the
`[AgentWeave]` lines back — not out of a code path re-run in Python.

### F291's open question — confirmed, both halves, and one citation in the ledger is wrong

`groupIntoTurns` partitions on `delivery_state === 'delivered'` before anything else and reads
`run_id` only inside the delivered branch. The pre-spawn `except` runs `return_run_entries` →
`db.commit()` → `_report_abandoned_entries` → `_broadcast_run_lifecycle(..., "run_failed")`. Both
as `STATE-day.json` stated them.

**The path in the question is wrong**: the model is `hub/ui/src/lib/agentTimelineModel.ts`, not
`hub/ui/src/components/agents/`. Line numbers correct, directory not. Corrected in `FINDINGS.md`
rather than in the dated paragraphs that carry it.

### F302 — the new finding, and it is in the ordinary deployment, not the blocked one

**An agent's first turn is told `no MCP tools this turn` while it is holding them.**
`described_access_path` grants the MCP rendering only on grounds, and the only observable ground is
`Run.mcp_adapter_online_at` on a *previous* run of the same agent — so a brand-new agent never has
any. The docstring says this is deliberate and costs "convenience for one turn". What the drive
adds is that the branch does not under-describe, it **asserts**: `no MCP tools this turn` is a
positive claim about the run's tool list, and the requirement it shipped against never asked for it
(*"No grounds means no assertion"* requires the HTTP form be described and tools not be claimed —
nothing more).

Two fresh agents, one turn each, same Hub:

- `cold094500`, asked to enumerate `mcp__agentweave__*` tools → **`NONE-PRESENT`**;
- `cold2094600`, told to try `create_task` first → `ToolSearch`, then
  `mcp__agentweave__create_task`, and **`task-f0dcea69c7ac` exists**.

Both runs have `mcp_adapter_online_at` set. The first row is **not** read as a controlled
measurement of the notice's effect — that harness defers tool schemas behind `ToolSearch`, which is
an equally good explanation of `NONE-PRESENT`. What the pair establishes is that the sentence is
false while the run is being told it. Severity **B**, because F300/F301 measured that the HTTP path
the notice steers it to is one a `claude` run cannot use under its own power.

The workaround is measured, not reasoned: `cold3094700`, created with
`config: {"hub_client": "mcp"}`, quoted the MCP rendering on turn 1 with no prior run anywhere —
the *"operator's own statement is honoured"* scenario, driven.

Filed as F302. **Not fixed here**: it is a wording decision inside a shipped requirement, which
belongs with the F299 posture decision already going to the operator.

### The gate — the repair worked, and something else is holding it now

Asked as `next_action` instructed. **`630473f` concluded `success`** — the first green CI on this
branch, and the proof that yesterday's starlette reading was right. **`2b33a6e`, which is `HEAD`,
concluded `failure`**, and it is not the same defect: `hub-test` died on
`sqlite3.OperationalError: database is locked` on `DROP TABLE requirement_drift` with
`CancelledError` in the pool log. That is **F292**, occurrence #12, appended to its entry — and the
first one read since F295's fix landed, which is the comparison that entry asked a later window to
make. The branch has gone from deterministically red for an unread reason to intermittently red for
a well-read one. No merge from here, per the limit; the gate reads CI at `HEAD`'s sha, and this
iteration's own commit gives the next firing a fresh run to read.

### Fixture defects worth keeping

- `wait_idle` waited on the roster saying `working`. It says **`running`**. Every turn was reported
  idle in `0.0 s` and the first two legs measured a conversation that had not happened yet.
- The context file a writing agent actually reads is under its **worktree**
  (`.agentweave/worktrees/<agent>/.agentweave/context/<agent>.md`), not the project root. Reading
  the root's copy reports `ABSENT` forever.
- `d1_aturn_conv_browser.py` selects a conversation by a **text needle** off the recent-conversations
  rail, and that rail's rows carry no conversation id. `d1_0909_conv_screen.py` expands the agent in
  the tree and clicks `data-testid="rail-conversation-<id>"` instead. This is the same fixture
  defect the night window recorded on 2026-09-09, met from the other end.
- A first attempt at reading the notice asked for *"the first two lines of this prompt"*. A real
  Haiku turn quoted the harness's own system prompt — `You are Claude Code…` — and said nothing
  about AgentWeave. The message has to name the `[AgentWeave]` marker.

Scripts committed: `scripts/drive/setup_d1_0909.py`, `t_d1_0909_together.py`,
`d1_0909_conv_screen.py`, `d1_0909_shutdown.py`.

---

## Iteration 4 — D-2: severity B is read, and 38 of 40 are live defects nobody had fixed

**All 40 unclassified severity-B sections now carry a machine-readable `**Status:**` line as their
first body line.** `classify_findings.py` reports **0 unclassified B**, down from 40. Part 1 stopped
at the severity boundary deliberately: **D-3 starts from severity C (55), then D (14), then the 6
with no severity label at all** — 75 sections, all still unclassified, all listed by the instrument.

### Where the 40 went

| verdict | n | |
|---|---|---|
| **open** | 38 | filed, most with a reproduction, never repaired |
| **fixed** | 2 | **F109** (`d9ad1e0`, by F285's change) and **F159** (`eeab0d3`, in the change that introduced it) |

The ROADMAP's six-finding sample had extrapolated that this population was *unread rather than
noisy*. Read in full, that holds and sharpens: **95% of it is live.** The plan's totals had been
assuming zeroes for 38 real B defects. **No new work is created** — every one was already filed.

### The two that were already fixed, and why no census had noticed

- **F109** — its own text still read *"the fix is declined, 2026-08-29 — the operator will not pay
  4× suite runtime"* and *"do not re-propose the file-backed fixture"*. That decision was
  **reversed** when CI forced it; F285 shipped exactly the declined fixture. `hub/tests/conftest.py`
  :22-36 now names F285 and restates the same three candidates F109 measured. The entry was carrying
  a withdrawn decision as if it were current, which is the disease the classifier's own header calls
  *quoted history read as current status* — met here from the other side, in prose nobody had
  re-read.
- **F159** — fixed **in the change that introduced it**; it never shipped as a defect. Verified in
  code, not taken from the prose: `hub/hub/task_workspace.py:239-240` scopes the `approved` filter
  to `not evidence_governs`, and both named guards exist (`test_turn_workspace.py:907` and `:547`).

### Four sections answered neither open nor fixed, which sampling could not have found

Sampling asks *is it real*. Reading asks *what is it now*.

- **F119 and F138 are halves.** Each shipped one half and left a named residual. F119's Hub copy was
  deduplicated (`scheduler.py:57` calls `redact_secrets`); the CLI's broad rule stands on an operator
  question never asked. F138's three hard-wired harnesses were fixed in the filing commit and
  `aw.py`'s `KEY` default went on 2026-09-07 — but **`HUB` still defaults to `8010`**, the one
  instance a drive must not disturb. Both read `open` and both are cheaper than that label suggests.
- **F259's headline is struck and its substance survives.** The 2026-09-02 amendment withdrew the
  consequence (`StatusBar.tsx` is in no bundle) without withdrawing the finding, which lands on F264.
- **F264 is half-driven** — code half established, live pass **fired the branch and did not
  reproduce the leak**, and it names the drive still owed. `open` overstates it. The honest verdict
  is `unverified`, and **the instrument has no such bucket**; recorded as friction, not worked around.

### Seven statuses rest on a code check taken today, not on the ledger

F109 (conftest), F111 (`POST /agents/register` **still live** at `agents.py:2144`), F119
(`scheduler.py:57`), F129 (**no** `drift/detect` or `requirement_drift` reference anywhere in
`hub/ui/src` — still unreachable), F138 (`aw.py:18`), F159 (`task_workspace.py:239-240`), and F178,
where the check **sharpened the finding**: `useAgentLaunchability` has **no non-test caller** in
`hub/ui/src`. Its only consumers are three `__tests__` mocks. So the report reaches no screen — and
three test files mock a hook no component renders.

### One collateral defect, made and caught inside this task

The first draft of F111's status line put the word *"closed"* on the same line as a reference to
`F3`, and **F3 immediately flipped from `OPEN` to `CONFLICT`** — the cross-section arm reading a
sentence about F111 as a resolution of F3. That is exactly the failure mode
`classify_findings.py`'s header names as *the single failure mode behind every error*: a sentence
that mentions finding X while resolving finding Y.

**Writing into the ledger can corrupt the ledger's own census.** The only reason it was caught is
that every non-B bucket was diffed against the pre-edit run, line by line, and F3 was the one row
that moved. Reworded; re-verified; every non-B bucket is now byte-identical to before the edit.
**D-3 must do the same diff.** The `next_action` says so.

### Verification

- `classify_findings.py`: B unclassified **40 → 0**; B open 19 → 57, B resolved 54 → 56. `+38 open,
  +2 fixed = 40`, and 302 sections before and after — no section created or destroyed.
- Every non-B bucket **identical** to the pre-edit run (A: 2/3/47; C: 3/17/20/3/55; D: 5/1/14;
  ?: 7/6), after the F3 repair.
- `tests/test_classify_findings.py` — **15 passed**. The instrument's own guards still hold on a
  ledger 169 lines longer.
- `ruff` and `black --target-version py311` clean on the new script. (`scripts/` is outside CI's lint
  paths — run deliberately, not because a gate asked.)

### Kept

`scripts/drive/_d2_write_status.py` — the 40 status texts and the placer, so **D-3 extends one list
rather than inventing a second format**. It refuses to write where a `**Status:**` line already
leads a section, so re-running it is a no-op rather than a duplicate. Counts went to
`spec-queue/ROADMAP.md`, never into `FINDINGS.md` — that file is the instrument's input, and
publishing a census into it changes the census.

---

## Iteration 5 — D-3: the ledger is fully classified, and 8 of the last 75 were already answered

**`classify_findings.py` now reports `UNCLASSIFIED: 0`.** The 75 sections part 1 left — severity C
(55), D (14) and the 6 with no severity label — were read and given a machine-readable
`**Status:**` line as their first body line. **67 open, 8 already answered.** Both parts together
put the ledger at **144 open / 143 resolved / 9 conflict / 6 resolved-elsewhere**, across the same
302 sections that existed before either part ran.

### Where the 75 went

| | C | D | ? | total |
|---|---|---|---|---|
| **open** | 51 | 14 | 2 | **67** |
| **resolved** | 4 | 0 | 4 | **8** |

89 % live, against B's 95 %. The difference is entirely the eight, and only two of them are
ordinary repairs.

### The eight, and the two things they say that a count cannot

- **F270 — fixed `3b1b8f0`, by the side effect it predicted.** The entry said
  `a-turn-says-how-it-ended` task 2.2 would close it as a consequence of persisting the terminal
  status row for every run. It did: `agent_trigger.py:2353-2363` now writes that row through
  `record_agent_output` on either runner, so `lastRunSettled`'s first signal is no longer
  Claude-only. **A finding can be closed by a change that never mentions it**, and nothing in this
  repository looks for that case.
- **F280 — fixed `c063e28`** in the iteration that filed it. Verified: `footprint_view` at
  `agent_actions.py:1137,1145`. Named nowhere outside the ledger.
- **F66 and F131 — fixed 2026-08-30** (`f5b46e9`, `5958200`), both verified in code today rather
  than believed from their own prose.
- **F137, F144, F145, F148 — not product defects at all.** Drive-harness records, each repaired in
  its filing commit. This is a species the census had no bucket for: four of the 75 were never
  claims about the product, so counting them as unknown defects overstated the backlog by exactly
  as much as leaving the other four uncounted understated the progress.

### The instrument cannot read the ledger's own strongest verdicts

Worth more than the eight. **Two of the four already-fixed sections declared it in their first body
line and were still `UNCLASSIFIED`:**

```
## F131 ...
**FIXED 2026-08-30** — openspec/changes/continue-starts-what-it-names, three spec-loop rounds
## F66 ...
**Status:** **closed 2026-08-30 — the question was answered in code
```

`STRONG_PAT` requires the literal `Status:` before `FIXED`, so F131's bare declaration misses; and
`closed` is in-section vocabulary nowhere — `EXT_WORD` recognises it only *across* sections, which
is the arm the file's own header says to treat as a lead and never as a verdict. So the ledger's two
plainest statements of resolution are invisible to the instrument reading it.

**Deliberately not fixed here.** Widening the pattern moves verdicts, and moving verdicts in the
same pass that writes 75 of them would make the bucket diff unreadable — and the bucket diff is the
only thing that catches a self-inflicted re-verdict. Recorded in `ROADMAP.md` as the instrument's
next round, to be mutation-checked like the seven guards already there.

### Severity C is not "unread" — part of it is decided and dropped

Six of the 51 open C findings have a remedy already chosen in writing and nothing behind it: F181
(`DECISIONS.md:536`), F195 and F201 (`:263-264`, sized at ~8 and 9 sites, `:545` deciding the narrow
repair and the sweep ship together), F198 (`:825`, *remove the route*), F209 (`:820` and
`ROADMAP.md:337`, whose R-3.1 re-check records *HOLDS, exactly*), F212 (`:527`). Reading the
population creates no work for these. It shows the decision was the easy half.

**And 35 of the 75 are named nowhere outside `FINDINGS.md`** — measured by a cross-reference sweep
over `openspec/`, `spec-queue/`, `hub/`, `src/`, `scripts/` and `tests/`. For those, the ledger is
the only copy. F280 is one of them, and it is fixed.

### The trap, turned into a guard

D-2's collateral defect — a status line naming `F3` beside the word *closed*, flipping F3 from
`OPEN` to `CONFLICT` — is now checked rather than remembered. `_refuse_cross_section_verdicts()`
rejects any line that carries an `F<n>` reference and a resolution word together, before anything is
written. **It did not fire on this batch**, so the bucket diff was taken anyway: a guard that has
never fired has not been shown to work.

### Verification

- `classify_findings.py`: `UNCLASSIFIED` **75 → 0**; C `OPEN 17→68, RESOLVED 20→24`; D
  `OPEN 0→14`; `?` `OPEN 0→2, RESOLVED 7→11`. `+67 open +8 resolved = 75`.
- **Every bucket set-diffed against the pre-edit run by finding number**, ignoring line numbers:
  **no untouched finding moved bucket.** CONFLICT (9), OPEN A/B (3 and 57) and RESOLVED_ELSEWHERE
  (6) are identical member-for-member. 302 sections before and after.
- **41 of the 67 open verdicts rest on a code check taken today**, cited at file and line. The rest
  rest on the ledger plus the cross-reference sweep and say which.
- `tests/test_classify_findings.py` — **15 passed** on a ledger 335 lines longer.
- `ruff` and `black --target-version py311` clean on both scripts.

### Kept

`scripts/drive/_d2_write_status.py`, now carrying both batches: D-2's 40 entries skip (already
classified, counted, and reconciled against the dictionary so a mis-targeted insert cannot hide
among them), D-3's 75 insert, and four sections whose own words were already a verdict were
relabelled `**Status as filed:**` so a classification can be told from the author's prose. Counts
went to `ROADMAP.md`, never into `FINDINGS.md`.

---

## Iteration 6 — D-4: the count was right to the digit and the sentence beside it was false

**Filed as `F303` (B), fixed in this commit. Both halves.**

`next_action` said to recount before fixing, and not to trust the stale figure. The recount
returned **exactly what the stale figure said**: 10 real-shaped `aw_live_` literals in 8 files.
Ten of ten, eight of eight. Nothing was clean for free.

**And the recount was still worth its cost, for a reason the instruction did not anticipate.** The
`ROADMAP` 6.3 row does not only carry a number, it carries a sentence — *"10 literals in 8 files
remain, **all outside `scripts/drive/`** except `FINDINGS.md`'s own transcript"* — and that half was
false. Four scripts in that directory still carried
`KEY = os.environ.get("AW_KEY", "aw_live_<33 characters>")` and sent it into
`Authorization: Bearer`: `t_d1_catalog_is_the_only_door.py:44`,
`t_d4_instructions_failed_load.py:59`, `t_n3_runner_model_picker_ui.py:38`,
`t_n4_runner_refusal_reaches_the_operator.py:45`.

### Why three instruments in a row missed them

The literal is 33 characters, not hex, one character repeated 28 times — a hand-written
placeholder. Every instrument pointed at this problem asks about **shape**:
`aw_live_[0-9a-f]{32}`, a real key's shape. The recount asks it. `tests/test_drive_key_guard.py`
asks it. So the placeholder is not a disclosed secret — and *that is exactly what made it
invisible*.

**A count of disclosed secrets and a guard against a bad practice are two different questions, and
only one of them is about shape.** The behaviour here is the one `require_key()` was written to
stop, in `aw.py`'s own words: an unset `AW_KEY` sends a bogus Bearer token, the Hub answers **401**,
and the script carries on with **exit code 0**. A placeholder produces that outcome exactly as well
as a real key does.

This is the third successive count of one directory (`scripts/drive/`), each right about what it
measured and each attached to a claim wider than it: the row said eight scripts, the 2026-09-08
test counted nine, and today's shape-independent sweep found four more.

### Fixed by changing the question

All four now `from aw import require_key` / `KEY = require_key()`. The new gate,
`test_no_drive_script_defaults_the_key_to_anything`, rejects any
`os.environ.get("AW_KEY", <value>)` whose default is not empty — **of any shape**. Mutation-checked
four ways, on a real file in the real directory, restored afterwards:

| mutation | old shape-based guard | new default-based guard |
|---|---|---|
| the placeholder-shaped literal these four carried | **passes — blind** | **fails** |
| a real `aw_live_` + 32 hex literal | fails | fails |
| `os.environ.get("AW_KEY", "")` | passes | passes |
| `os.environ.get("AW_KEY")` | passes | passes |

Driven, not just tested: each of the four run with `AW_KEY` unset exits **1** with the local
refusal, before any HTTP. Positive control — each run with a key set passes the key check and
reaches the *next* statement, the `:8000` refusal, so the gate is on emptiness and not on use.

### The second half: the rotated key still had somewhere to come back from

6.3 records the disclosed key as rotated by the operator on 2026-09-08, and its ten surviving
literals as *"all dead against a rotated key"*. Both true, and together not the whole answer.

- **No database on this machine honours any of them.** The ten sites carry three distinct values;
  each was checked against `operator_credentials` and `api_keys` in all three local databases
  (`trial`, `live`, `f274p0`) — **no rows**. Done on copies; the `live` profile serving :8000 was
  read and never touched, and its own credential is a different value.
- **`hub/.env` still held the disclosed key** — untracked, gitignored, mtime 2026-03-31, the only
  file outside the repository's own text on this machine that did.
- **That file is loaded by the documented trial-Hub launch.** `hub/hub/config.py:21` reads `.env`
  relative to the working directory, and CLAUDE.md's launch command runs from `hub/`. Measured:
  `Settings()` with `cwd=hub` returns the file's value.
- **A fresh database mints it verbatim.** `_seed_operator_credential`
  (`hub/hub/db/engine.py:336-341`) auto-generates only when the configured key is empty or is the
  documented `a1b2c3…` placeholder. Measured on a throwaway in-memory database, with the
  placeholder as a negative control.

So the key was dead in every store and **one fresh-database launch from the documented directory
away from being alive again** — which is precisely what the 2026-09-07 trial rebuild was. It did not
happen; nothing prevented it. Rotated in `hub/.env` today under the operator's 2026-09-08 standing
permission: one line, same length, no other line touched, file still untracked so no literal enters
the repository by the fix. `Settings()` re-read afterwards returns the new value.

### The eight survivors, said out loud, since the brief asked which kind each is

- **Documentation, keep** — the `a1b2c3…` placeholder at `hub/.env.example:18`,
  `hub/hub/db/engine.py:34`, `hub/tests/test_setup.py:58`. It is asserted upon, and it is the value
  the seeding path refuses to mint.
- **Regression fixtures, keep** — `hub/tests/test_operator_is_told_the_truth.py:41,59,128`. They
  exist *because* the key leaked; deleting them deletes the regression.
- **The record, keep** — two historical loop documents, this ledger's own transcript line at
  `FINDINGS.md:1511`, and one archived UI mock's invented value. The key is public in git history
  already, so scrubbing the working tree would change nothing about disclosure and would destroy
  the account of it. The correct answer to a disclosed key is revocation, and it is revoked.

### Verification

- `tests/test_drive_key_guard.py` — 4 passed (3 before). `tests/` — **461 passed, 3 skipped**.
- `hub/tests/test_setup.py`, `test_operator_is_told_the_truth.py`, `test_project_persistence.py` —
  46 passed.
- `scripts/classify_findings.py`: 303 sections, `UNCLASSIFIED` **0**, severity B `RESOLVED 56→57`.
  Every other bucket identical to iteration 5's run.
- CI's lint set clean: `ruff check src/ hub/ tests/`,
  `black --check --target-version py311 src/ hub/hub/ hub/tests/ tests/`. `ruff` clean on the four
  edited drive scripts too, though `scripts/` is in neither CI path list — which is why
  `tests/test_drive_key_guard.py` is the only gate on that directory at all.
- No value was printed at any point; literals were classified by length, character class and
  SHA-256 prefix.

---

## Iteration 7 — D-5: the review page, and the CI story it was told to check had moved

**Written:** `spec-queue/review/review-2026-09-09.html` (42,328 bytes, 705 lines) and an addendum
inside the existing `## 2026-09-09` section of `spec-queue/APPROVALS.md`.

### The instruction said to re-read CI rather than trust the sentence, and that was right

`next_action` carried a three-step account of the gate — 12 red, repaired, then `2b33a6e` red on
F292. Re-read from `gh` at 10:52, **four more commits had completed and the picture had changed
shape**:

| Commit | CI |
|---|---|
| `ed7d3ef` compose | failure — the **thirteenth** red, not the twelfth; the compose commit was red too |
| `630473f` the repair | **success** |
| `2b33a6e` approvals | failure — F292 `database is locked`, occurrence #12 |
| `c807e4d` drive | **success** |
| `99c1368` classify | **success** |
| `65caf69` classify | **success** |
| `6ccb737` keys (HEAD) | in progress, started 09:45Z |

So the page says something the state file could not: **four of the five completed runs since the
repair are green**, and the gate is waiting on a run rather than on a fault. The count was corrected
from twelve to thirteen for the same reason — iteration 1 read it before its own commit had
finished.

### The APPROVALS instruction had to be read against the file, not obeyed literally

D-5 says *"append today's section to `APPROVALS.md`"*. **A section for today already existed**,
written 08:45 by a RESUME session on the operator's instruction, carrying
`ORDER: F142, 2026-09-07-clearing-instructions-asks-first`. The night window reads **only the newest
dated section** and takes an `ORDER:` line **verbatim**. Appending a second `## 2026-09-09` heading
would have created two candidates for "newest section" and could have cost the operator their own
order line — the exact trap that section was written to remove from the 2026-09-08 one.

So the addendum went **inside** that section, above its closing `---`, and adds no line-initial
`APPROVED`/`REVISING`/`REJECTED`, no `ORDER:`, no `NOTHING TONIGHT`. Verified after writing: today's
section still holds **one** status row (the pre-existing one), **one** `ORDER:` line (the
operator's), and the file still has **one** `## 2026-09-09` heading.

**The addendum carries one correction the classification produced.** That section calls `F142`
*"the last open severity-A finding"*. The instrument now reads open severity-A as **three** — F299,
F300, F301 — and `F142` as `CONFLICT`. The practical meaning survives (those three are the DAY-3
decision, not buildable work) and the order stands; the count is what changed, and it is written
where the reader of that line will see it.

### The page

Answers the playbook's six questions in order. §1 the branch and the gate, first and unskimmable;
§2 what the night built and that all three were driven by the night itself; §3 today's drive,
including F302 and F303; **§4 nothing specced, and why** — plus a §4b for what the day did instead,
which is what makes §5 usable; §5 what tonight does with no further approval, which is *not* the
default queue because an `ORDER:` line already exists; §6 the three research candidates, each ending
in what it means for AgentWeave; §7 the three decisions collected — DAY-1 pin starlette, DAY-2 the
F302 wording, DAY-3 the carried F299 posture.

### Verification — the page was rendered, not just parsed

- **Self-contained**: 0 occurrences each of `http://`, `https://`, `<script`, `<link`, `@import`,
  `src=`.
- **Well-formed**: full `<!DOCTYPE>`/`<html>`/`<head>`/`<body>` wrapper, exactly one of each;
  `HTMLParser` walk closes every tag — empty unclosed stack, zero mismatches.
- **Rendered in both themes** with `scripts/uishot.py` at 1100 px against a `file://` URL:
  9,315 px tall in each. Background sampled at the corner **and** at mid-page —
  dark `(22,22,26)` = `#16161a`, light `(251,251,250)` = `#fbfbfa`, i.e. the explicit `body`
  background resolves from the bare `:root` and from the `prefers-color-scheme: dark` block. The
  dark capture was read as an image: header, section rules, severity pills, cards and the three
  `.ask` blocks all render.

The two failed heredocs on the way in are worth one line: the body had to be appended in five
chunks, because a ~30 kB `cat` heredoc exceeds the Windows spawn limit and fails with
`ENAMETOOLONG` or a truncated quote. Nothing was written on either failure — checked before
retrying, not assumed.

### Not done here

D-6 remains: one more repair that needs no spec, under the day-window conditions. It is the last
item in the queue.

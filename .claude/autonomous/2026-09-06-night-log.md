# 2026-09-06 night window

Newest entry at the BOTTOM. Playbook: `.claude/loops/night-window.md`.

Armed 2026-09-06T22:55:07+01:00 on `autonomous/2026-09-04-daily` (parent master@ab60cf3), model opus.

---

## Iteration 1 — compose the queue

**Branch reconciled.** `git branch --show-current` = `autonomous/2026-09-04-daily`, `git log -1` =
`da5072d arm(night): 2026-09-06 cycle`, both matching `STATE-night.json`. Parent `master@ab60cf3` is
still the branch point. No disagreement to reconcile.

**`spec-queue/APPROVALS.md`, `## 2026-09-06`: the operator sat down and approved.** No `ORDER:` line,
no `NOTHING TONIGHT`. One row, and it carries a status token:

> `- APPROVED  2026-09-06-an-unread-editor-cannot-overwrite   F271 (A). 24 tasks. UI-only, no migration.`

`spec-queue/DIRECTION.md` is unchanged since 2026-09-02 and does not answer DAY-2, so the F292 gate
question stays open and is not this window's to decide.

**The backlog above the approved row is genuinely empty, checked rather than assumed.** `openspec
list` reports both open changes at **0/24** and **0/44** tasks — so playbook step 1 (unarchived
changes that are implemented and only need archiving) has nothing in it. Step 2's open severity-A
findings all sit in the *needs-a-proposal* state the playbook explicitly routes to the day window
rather than to here. So tonight is the `APPROVED` row, start to finish, and the queue below is that
change's five phases split at commit boundaries.

**Green baseline, measured, not assumed.** The change is UI-only — no Python path is touched — so the
relevant chunk is `hub/ui`, not the 15–25 minute Hub suite:

| Command | Result |
|---|---|
| `npm run test` (vitest) | **144 files, 1498 tests, all passed**, 115.48s |
| `npm run lint` (eslint, `--max-warnings 0`) | **clean**, no output |

Nothing inherited is red, so any breakage from here is this window's own. The `Error: boom` stack
traces in the vitest output are `ErrorBoundary.test.tsx`'s deliberate throw being logged by React,
not a failure — the summary line is unambiguous.

**Ordering note that shapes the queue.** Task 4.1b requires the new in-flight drive column to be run
**once before the fix**, as a reproduction of the second destruction path. That has to happen while
`hub/hub/static/ui` still holds the pre-change bundle, so `n1-repro` is first and the bundle rebuild
(`n4-bundle`) cannot move ahead of it. Getting this backwards would silently convert the
reproduction into a post-fix pass.

Queue written: `n1-repro`, `n2-impl`, `n3-units`, `n4-bundle`, `n5-drive`, `n6-retry`, `n7-close`,
`n8-wrap`. Eight items, ~8h of window remaining at 23:04.

---

## Iteration 2 — n1-repro: the in-flight column, run once against the pre-change bundle

**Branch reconciled.** `git branch --show-current` = `autonomous/2026-09-04-daily`, `git log -1` =
`a644eb6 queue(night): compose tonight's queue`, both matching `STATE-night.json`. No disagreement.

**The ordering constraint this item exists to honour.** Task 4.1b's column is a reproduction only
while `hub/hub/static/ui` holds the pre-change page. Run it after `n4-bundle` and it silently becomes
a pass. So the first thing checked was that the served bundle really is pre-change, and it was
checked rather than assumed: `ui-build-stamp.json` records `src_commit c063e28`, and the only
`hub/ui/src` commit since is `5a5e026`, which touches `lib/eventSummary.ts` and its test — nothing
near `InstructionsPage.tsx`, whose last change is `8ed52f7`, an ancestor of the stamp. Working tree
clean for both `hub/ui/src` and `hub/hub/static/ui`.

**Instrument.** Fresh uvicorn from source started from `hub/`
(`py -3.11 -m uvicorn hub.main:app --port 8011`, never `agentweave --port`), fresh database
`C:/Users/huida/AppData/Local/Temp/aw0906n1/n1.db`, key minted from `/setup/token`, two throwaway
fixture projects created and deleted by the harness. Nothing on 8000, 8010 not addressed.
`find hub/hub src -name '*.py' -newermt 2026-09-06T23:10:54` empty before and after. Hub stopped at
the end (pid 19936 killed, `/health` refuses).

**What was added.** Column `D` in `scripts/drive/t_d4_instructions_failed_load.py`, plus the module
docstring's fourth question. A/B/C's expectations are **untouched** — 4.1's inversion belongs to
`n5-drive`, and inverting it here would have destroyed the very reproduction B1 provides. The
instrument is the same route interception the other columns use, with one difference: the GET route
is **parked**, never resolved, so the page sits on its skeleton for the whole observation window.

**It reproduced.** 24 passed / 3 failed, and the three failures are exactly the new assertions:

| | |
|---|---|
| `skeleton: true`, `textarea: false` | the editor has never held the stored text |
| `save_visible: true`, `save_disabled: false` | **FAIL** — Save is live over a skeleton |
| one click | **FAIL** — 1 PUT issued |
| stored before → after | **FAIL** — `'ALPHA PROJECT RULES\n…'` → `''` |

The wider claim this settles: F271's destruction does not need a failure at all. B1 reaches it
through a dropped connection; D reaches it through **latency**, and that window opens on every
visit. It also confirms by measurement what task 1.4 only argued from the code — `skeleton: true`
and `save_visible: true` were observed *simultaneously*, so the three-branch render of task 1.2
cannot close this column. Save is `SettingsSection`'s `actions`, a sibling of `{children}`.

Column **C is still not driven**, unchanged from 2026-09-02: `'Switch project' controls on screen: 0`.
Recorded, not asserted; task 2.6 covers that shape at the unit level.

**Instrument noise, recorded so nobody chases it.** Playwright prints an
`asyncio.exceptions.CancelledError` traceback on stderr when the parked route is torn down at page
close. Noted at the handler in the harness.

**Not touched, deliberately:** `InstructionsPage.tsx`, the bundle, and A/B/C's expectations.
`ruff check` clean on the harness. Task 4.1b's box is left **open** — half of it (the post-fix run)
is `n5-drive`'s — with the pre-fix result written under it.

---

## Iteration 3 — n2-impl: the component, tasks 1.1–1.7

**Branch reconciled.** `git branch --show-current` = `autonomous/2026-09-04-daily`, `git log -1` =
`fd32657 night(n1-repro)`, tree clean — both matching `STATE-night.json`. No disagreement.

**What changed.** `hub/ui/src/components/instructions/InstructionsPage.tsx` only. No Python path,
no `hub/hub/api/v1/instructions.py` (1.7), no bundle rebuild, no unit-test file — those are
`n4-bundle` and `n3-units`.

- **1.2 — three branches, `data` first.** `data ? editor : isError ? failure : skeleton`, the shape
  of `WorktreesPanel.tsx:44-62` with its deliberate divergence: that panel tests `error` first
  because its list is read-only; this one tests `data` first because a background refetch failure
  must not take a loaded editor away from an operator mid-edit. The session-boundary disclaimer
  stays *inside* the gated block, so it travels with the textarea and is correctly absent from the
  other two states.
- **1.4 — the one that was measured to be necessary.** `actions` is now `data ? (…) : undefined`.
  Iteration 2 observed `skeleton:true` and `save_visible:true, save_disabled:false` *simultaneously*
  in a real browser, so the 1.2 branch alone leaves Save live over the skeleton; `actions` is
  rendered in `SettingsSection`'s **heading** (`:58`), a sibling of `{children}` (`:60`). Gating
  `actions` on the same `data` test closes both `data === undefined` routes by construction rather
  than by a `disabled` attribute.
- **1.3 — the failure block.** `role="alert"` naming what could not be loaded, the sentence from
  `readableApiError(error, …)`, and a Retry calling `refetch()`.
- **1.5** — the failure lives inside `{children}`; the whole `<SettingsSection>` is never replaced,
  so *A configuration section states what it governs* still holds in the failure state.
- **1.6** — `saveMutation.isError` is now read and stated in a `role="alert"` beside the textarea,
  the way `ProjectSettingsPanel.tsx:319` does. This closes a **shipped** requirement the component
  had always breached (*Saving reports its outcome*), not a new one.

**Two deliberate divergences from the letter of the tasks, both recorded rather than silent.**

1. **1.1 asks for `isLoading` to be destructured; it is not.** In a `data`-first render
   `!data && !isError` *is* the not-yet-answered state — in flight, or the query disabled because no
   project is selected — so `isLoading` carries no information the third branch needs, and binding
   it unused fails `--max-warnings 0`. `isError`, `error` and `refetch` are all read as 1.1 intends.
   Stated in the component's doc comment.
2. **1.3 says the *fallback string* must say nothing stored has been changed.** Instead that
   sentence is a static line in the alert, always rendered, with the fallback carrying only
   "The Hub did not return a reason." This is strictly stronger: the reassurance then also holds for
   an error *response* that carries a body and so displaces the fallback, not only for the bodiless
   dropped-connection case the scenario names. The scenario "A failed read reports something useful
   with no server response to quote" is satisfied either way.

**Verified by measurement, not by a green suite.**

| Check | Result |
|---|---|
| `npm run lint` (`--max-warnings 0`) | clean |
| `npx tsc --noEmit` | exit 0 |
| `npm run test` (vitest) | **144 files / 1498 tests passed** — identical to the pre-change baseline |

A green suite proves nothing about a branch no test enters, so the component was **rendered** in a
throwaway harness (`zzScratchInstructions.test.tsx`, deleted after the run — the kept eight tests
are `n3-units`' and this does not pre-empt them). Five behavioural checks, all passing against the
fix: failed read renders no textarea, no Save, an announced failure, and clicking *every* button on
screen issues **no PUT**; a read left permanently in flight renders the skeleton with no Save and
again no PUT from any click; Retry restores the pre-filled editor; the success path still issues
exactly one PUT with the edited content; a rejected save states the failure while keeping the typed
content.

**Mutation-checked, not reasoned about.** The component was `git stash`ed and the same five re-run
against the pre-change page: **4 failed, 1 passed.** The four that fail are the four guards; the one
that passes is the success path, which is the evidence that it is unchanged. This reproduces at the
unit level exactly what iteration 2 reproduced in a browser, including the in-flight column.

**Still owed, and not claimed.** No browser has seen this — `hub/hub/static/ui` still serves the
**pre-change** bundle, so nothing an operator can reach has changed yet (`n4-bundle`, then
`n5-drive`; F274 is precisely the rule that a UI change driven anywhere but a real browser against
the served bundle is not driven). Tasks 1.1–1.7 checked off in `tasks.md`; 4.1b stays open.

---

## Iteration 4 — n3-units: `instructionsUnreadEditor.test.tsx`, tasks 2.1–2.8

**Branch reconciled.** `git branch --show-current` = `autonomous/2026-09-04-daily`, `git log -1` =
`c7e615c night(n2-impl)`, tree clean — matching `STATE-night.json`. No disagreement.

**What changed.** One new file, `hub/ui/src/__tests__/instructionsUnreadEditor.test.tsx` (8 tests),
plus the 2.1–2.8 boxes and their evidence note in the change's `tasks.md`. No product code: the
component was `git checkout`ed back and forth for the mutation checks and is byte-identical to
`c7e615c` at commit time. No bundle rebuild (`n4-bundle`), no drive (`n5-drive`).

**Harness.** The shape iteration 3's throwaway proved, kept: a partial `vi.mock('@/api/client')`
via `importOriginal` replacing only `getJson`/`putJson` — `readableApiError` stays **real**, or every
"states why" assertion would be a test of a stub — a `QueryClientProvider` with `retry: false` on
both queries and mutations, and `useConfigStore.setState({ isConfigured: true, selectedProjectId })`
in `beforeEach`. Two things had to be added on top of it:

- `vi.clearAllMocks()` first in `beforeEach`. These are module-factory `vi.fn()`s and the shared
  `setup.ts`'s `restoreAllMocks` does not empty their call history — the first run had 2.4 asserting
  `getJson` twice and seeing **six**, the leftovers of the three tests before it. Left unfixed,
  *"no PUT was issued"* would silently have been an assertion about the whole file's history.
- A `stored` object standing in for the row the Hub keeps, written by the `putJson` mock, so a PUT is
  observable as **a write** and not only as a call. 2.2/2.3 assert `stored[ALPHA]` is still the
  original text.

**The guard is the mutation, never the markup.** 2.2 and 2.3 do not look for a `disabled`
attribute — a markup assertion passes against a page that renders the error *and* the textarea.
They call `interactWithEverything`, which types into **every** textbox on screen and clicks **every**
button, whatever they happen to be, then asserts `putJson` was not called at all.

**Mutation-checked by reverting the component, not by reasoning.** `git checkout c7e615c^ --
hub/ui/src/components/instructions/InstructionsPage.tsx`, re-run, restore. **6 of 8 fail**, and
after one correction each fails on *its own* claim:

| Test | Failure against the pre-change page |
|---|---|
| 2.1 failed load renders no editor | `Unable to find role="alert"` |
| 2.2 no PUT after a failed read | `putJson` **called 1 time** — the emptiness really is written |
| 2.3 no PUT while still in flight | `putJson` **called 1 time** — no failure needed, just latency |
| 2.4 Retry | `Unable to find role="alert"` — there is no Retry to press |
| 2.6 project switch | `expected [ <textarea …> ] to have a length of +0 but got 1` — alpha's text on beta's page |
| 2.8 rejected save | `Unable to find role="alert"` |

**Two tests initially failed for the wrong reason, and were rewritten.** 2.2 and 2.6 first waited on
`findByRole('alert')`, so against a page that renders no alert they failed as *copies of 2.1* rather
than on the write and the stale editor they exist to catch. Both now wait on
`client.getQueryState(...)?.status === 'error'` — a settle condition true of both versions — and only
then assert. The table above is the corrected run.

**2.5 needed a second mutation and initially survived it — the finding of this iteration.** Its
mutation is not the pre-change page (which passes it) but an `isError`-first ordering, applied as
`data && !isError`. It **passed anyway**: React Query notifies only on result fields a render
actually read, and a `data`-first branch never reads `isError` while data is present, so the failed
refetch re-rendered nothing and the wrong ordering was invisible. 2.5 now asserts the query really
reached `error` with its data retained, then **keeps typing** — which forces the re-render — and it
fails against that mutation with `Unable to find an accessible element with the role "textbox"`.
Worth carrying: *a React Query render-path test can pass because nothing re-rendered.* The masking is
in the observer, so it applies to any test of a branch on a field the happy path short-circuits past.

**2.5 and 2.7 pass before and after, stated rather than glossed.** 2.7 is the success path and its
being unchanged is the evidence the fix took nothing away; 2.5's mutation is the ordering, above.

**2.6 is closed by construction.** `data` is per query key (`['project', projectId, 'instructions']`),
so a newly selected project's key has no data and the editor branch cannot be reached holding the
previous project's text. There is no explicit guard, and the test says so.

**Measured.** `npm run lint` (`--max-warnings 0`) clean; `npx tsc --noEmit` exit 0; full vitest
**145 files / 1506 tests passed** — the 144/1498 baseline plus exactly these eight.

**Still owed, unchanged by this iteration.** `hub/hub/static/ui` still serves the **pre-change**
bundle, so nothing an operator can reach has changed (`n4-bundle`, then `n5-drive`). Task 4.1b stays
open until its post-fix run. No browser has seen the fix.

---

## Iteration 5 — n4-bundle: the served bundle finally holds the fix (task 3.1)

**Branch reconciled.** `git branch --show-current` = `autonomous/2026-09-04-daily`, `git log -1` =
`eb1d1d7 night(n3-units)`, tree clean — matching `STATE-night.json`. No disagreement. The stamp read
before touching anything recorded `src_commit c063e28`, built 2026-09-04, i.e. the **pre-change**
bundle, exactly as `next_action` required be confirmed first.

**What changed.** `hub/hub/static/ui` only — the committed build artefact — plus task 3.1's box and
its evidence in `tasks.md`. No component edits, no test edits, no Hub started, nothing driven.

**Measured, not assumed.** The instruction was explicit that a byte-identical rebuild is a hard
stop, so the check is content and not the fact that a build ran:

| Check | Result |
|---|---|
| `cd hub/ui && npm run build` | built in 11.20s |
| `py -3.11 scripts/refresh_ui_bundle.py` | refreshed + stamp written (only this script writes it) |
| Stamp `src_commit` | `c063e28` → **`eb1d1d7`** — HEAD, which contains `c7e615c`'s fixed component |
| Served JS | `index-BXA4wQsi.js` → **`index-C9-G-wn9.js`**, and `index.html` rewritten to point at it |
| Fix string in **new** bundle | `This project's instructions could not be loaded` — **1** occurrence |
| Fix string in **old** bundle (`git show HEAD:…index-BXA4wQsi.js`) | **0** occurrences |
| Control string `instructions` | matches in **both** — so the zero above is a real absence, not a broken grep |
| `AW_CHECK_UI_BUNDLE=1 pytest hub/tests/test_ui_build_stamp.py` | **13 passed**, strict bundle-matches-source included |

The control-string row is the part worth keeping: "the marker is absent from the old bundle" is
worthless on its own, because a grep that cannot match anything in a 1.1 MB minified file returns
zero just as readily. Both greps were run against a string that *does* match before the absence was
believed.

**"Commit `hub/ui/src` and `hub/hub/static/ui` together" — stated rather than glossed.** There was
nothing to stage from `hub/ui/src`: the component landed at `c7e615c` and the tests at `eb1d1d7`, on
this same branch, two commits ago. The rule exists so the two never diverge in what is pushed; the
bundle catches up in the immediately following commit on the same branch, so they do not. This is
the one deviation from the literal wording of the task, and it is a deviation of ordering only.

**Still owed.** Nothing has been *driven*. A browser has still not seen the fix — but from this
commit on, a Hub started from source serves the fixed page, which is what `n5-drive` needs and what
made this iteration a prerequisite rather than housekeeping (F274). Task 4.1b stays open until its
post-fix run.


---

## Iteration 6 — n5-drive: a browser sees the fix, and n6-retry folded in (tasks 4.1–4.4)

**Branch reconciled.** `git branch --show-current` = `autonomous/2026-09-04-daily`, `git log -1` =
`676eba4 night(n4-bundle)`, tree clean — matching `STATE-night.json`. No disagreement. The served
bundle was checked before anything was started, not assumed: stamp `src_commit eb1d1d7`, and the
fix's own string occurs once in `index-C9-G-wn9.js`.

**Instrument.** Fresh uvicorn from source started from `hub/`
(`py -3.11 -m uvicorn hub.main:app --port 8011`, never `agentweave --port`), fresh database
`%TEMP%/aw0906n5/n5.db`, key minted from `GET /api/v1/setup/token`. `find hub/hub src -name '*.py'
-newermt <process start>` empty, so the running Hub is this tree. Fixtures `proj-2f2559e39c35` /
`proj-f2ec075f8ff8`, both deleted by the harness and confirmed absent from `GET /projects`. Nothing
on `:8000`, `:8010` never addressed, no agent turn so nothing bound a model. Hub stopped, `8011`
free.

**4.1 / 4.2 — the two failure columns inverted, and the read-back turned around.** B1 and B2 now
assert the requirement rather than the defect: no editor, Save absent-or-inert, the failure stated
in a `role="alert"`, a Retry control on screen. **31 passed / 0 failed.** What B1 actually observed:

| | |
|---|---|
| `textarea` / `save_visible` | `false` / `false` — the button is not merely inert, it is gone |
| the alert | *This project's instructions could not be loaded. / The Hub did not return a reason. / Nothing stored has been changed…* |
| PUT attempts | **0** |
| stored before → after | `'ALPHA PROJECT RULES
…'` → **byte-identical** |

Two things were added rather than just flipped. The read-back is a **preservation** check driven by
a shared `interact()` helper that types into *every* textbox on screen and clicks *every* Save,
whatever they happen to be — never a `disabled`-attribute assertion, because a page rendering the
error *and* the editor would satisfy that. And B2 gained an assertion the pre-fix harness had no use
for: a 500 carries an `ApiError`, so the server's own sentence (`boom`) must be quoted rather than
the dropped-connection fallback. It is. Said plainly rather than glossed: post-fix `interact()`
types into **0** textboxes and clicks **0** buttons, because there is nothing there. The guard is
the absence, not a refused interaction.

**The inverted harness was mutation-checked, which is the part that makes the 31/0 mean anything.**
An inverted expectation that happens to be trivially true is exactly how a drive reports success
over a broken page. So the *same* harness was re-run against the **pre-change** bundle
(`git checkout 676eba4^ -- hub/hub/static/ui`; served JS confirmed back to `index-BXA4wQsi.js` on
the wire, not just on disk): **19 passed / 12 failed.**

| Column | Against the pre-change bundle |
|---|---|
| A baseline | **both checks pass, identically to the post-fix run** — so the 12 are this fix and nothing else |
| B1 | editor present, `save_visible=True/disabled=False`, `alerts: []`, no Retry, **1 PUT**, stored → `'TYPED BY THE DRIVE'` |
| B2 | same shape under a 500; no server sentence to find because there is no alert at all |
| D | `save_visible=True` over the skeleton, **1 PUT**, stored → `''` |

The `'TYPED BY THE DRIVE'` row is worth keeping: pre-fix the drive's own typing reached the stored
row, which is a **stronger** destruction than the original one-click blanking and proves the
instrument can see a write when there is one to see. Bundle restored afterwards; `git diff HEAD --
hub/hub/static/ui` empty (the checkout leaves the superseded assets behind — they were unstaged and
removed).

**4.1b's post-fix half — column D passes unmodified.** Not a rewritten expectation. The three
assertions that failed in iteration 2 (`Save absent-or-inert`, `no PUT was issued`, `byte-identical
afterwards`) now pass with the column's text untouched. That is the return on having run it before
the fix, and its box is now closed.

**4.4 folded in (`n6-retry`) — `scripts/drive/t_d4_retry_by_hand.py`, 15 passed / 0 failed.** No
route interception anywhere in it: a real uvicorn is started, stopped and started again on the same
database with a real browser sitting on the page. **The wrinkle that shapes the whole script: the
SPA is served by the Hub**, so "stop the Hub, open the page" is not a scenario an operator can
reach — with it down there is no app at all. The reachable form is the one driven: load the app on
Environment → Diagnostics so nothing is cached for the instructions key, stop the Hub, then click
**Instructions** in the sidebar — client-side navigation into a query whose GET meets a socket that
refuses. Then restart and press Retry.

Two results from it that no unit test reaches. The page **does not heal on its own** when the Hub
comes back — asserted explicitly between the restart and the click, because a page that silently
recovered would make the Retry control untestable and the assertion vacuous. And one click on Retry
brings back the textarea holding the stored text byte-for-byte, alert gone, Save enabled again.
Screenshot `retry-02-hub-down.png` read by eye: the section's title and its statement of what it
governs are still in the heading above the alert, which is task 1.5's requirement holding in the
failure state.

**Column C still not driven**, unchanged since 2026-09-02: `'Switch project' controls on screen: 0`.
Recorded, not asserted; task 2.6 covers that shape at the unit level and closes it by construction.

**Measured this iteration.** `ruff check` clean on both harnesses. No product code touched — the
only edits are `scripts/drive/`, and `hub/hub/static/ui` is byte-identical to `HEAD` after the
mutation check.

**Still owed.** `n7-close`: F271's status line and the index paragraph in `FINDINGS.md`, the UI
lint, the spec sync and the archive. Then `n8-wrap`.

---

## Iteration 7 — n7-close: the change is closed, and the ledger's census was wrong (tasks 5.1–5.3)

**Branch reconciled.** `autonomous/2026-09-04-daily`, `git log -1` = `f664f45 night(n5-drive)`, tree
clean — matching `STATE-night.json`. No disagreement. Docs-and-specs iteration as planned: no Hub
started, nothing driven, no product code touched.

**5.1 — UI lint clean, and the Python set named rather than skipped.**
`eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0` exits 0 with no output.
`ruff check src/ hub/ tests/`, `black --check` and `mypy src/` were **deliberately not run**, said
here rather than passed over in silence: the change's diff is `InstructionsPage.tsx`, a new
`hub/ui/src/__tests__/` file, the committed `hub/hub/static/ui` bundle, `scripts/drive/` harnesses
(already `ruff`-clean in their own iterations) and markdown. No file the Python lint set covers is
modified by the change itself.

**5.3 — synced and archived.** The MODIFIED requirement replaced *Hub UI provides instructions
editor* in full — its three scenarios now carry the read-succeeded condition, the disclaimer
included — and both ADDED requirements were appended. The delta's change-relative narration
(*"this change repairs"*, *"R3 found"*) was dropped and its durable rationale kept: a main spec
states current behaviour, not a diff. `openspec validate --all --strict` → **44 passed, 0 failed**.
Change moved to `openspec/changes/archive/2026-09-06-an-unread-editor-cannot-overwrite`.

**Only `project-instructions` was synced, and that was checked rather than assumed.** Task 1.6
closes a requirement that lives in `project-environment-settings` — *Saving reports its outcome*,
`openspec/specs/project-environment-settings/spec.md:70`. Read in full: it was already shipped and
binding, and the component simply breached it. No delta exists for it and its text needs no edit.
The change directory holds exactly one delta spec.

**5.2 — F271 closed, with its evidence separated rather than averaged.** `**Status:** fixed
`c7e615c`` (component), `676eba4` (bundle), `f664f45` (drive). The section now carries a three-row
table because the three destruction paths are closed on **different strengths of evidence** and
reporting one number would hide that:

| Path | Closed by |
|---|---|
| B1 / B2 — failed load, dropped connection and 500 | **measurement** — no editor, Save *gone* not inert, `role="alert"`, 0 PUTs, stored row byte-identical |
| D — read still in flight | **measurement against an expectation written before the fix existed** — column D passes *unmodified* |
| C — cross-project | **construction, not measurement** — still not drivable (no in-page switcher); per-query-key `data` plus unit 2.6, recorded as the weaker evidence it is |

The stale *"Status of F271 after this iteration: still open"* line at the section tail was
re-anchored to the iteration it was actually about, instead of being left to contradict the new head.

**The index correction is not the one the queue anticipated, and this is the iteration's real
finding.** `next_action` said to correct the open severity-A list on the assumption that removing
F271 takes it from six to five. Re-running the index's **own** mechanical scan first, rather than
doing the arithmetic, falsified that: there are **41** severity-A headings now, not the 40 the
2026-09-04 recount scanned, because **F295** was filed earlier the same day out of F292 and has
never appeared on any list on that page. `**Status:** open, not specced, not fixed`.

**So the list is still six — F274, F140, F142, F154, F155, F295 — with F271 leaving it and F295
joining it.** The count is unchanged by coincidence. Writing "five" would have been a *different*
error from the one that section spent 2026-09-04 correcting: not a stale definition, a stale
**census**.

**And the scan's first pass reported five, wrongly, which is written into the file too.** The
classifier tested for the substring `fixed` in each status line — and F295's *"open, not specced,
not fixed"* matches it. **A substring test for `fixed` reads "not fixed" as fixed.** It was caught
only because the heading count came back 41 against a remembered 40, i.e. by a check that was not
looking for it. Same class as the definition drift above: an instrument agreeing with the expected
answer.

**Measured this iteration.** UI lint exit 0. `openspec validate project-instructions --strict` valid;
`--all --strict` 44/44. The severity-A scan re-run to completion and its full status table read by
eye, not sampled.

**Not measured, and not claimed.** Nothing was driven tonight and no test suite was re-run — the
product evidence for this change is iterations 5 and 6's, unchanged. F140, F142, F154, F155 were not
touched; their banner condition is still unmet. F295 was read, not investigated.

**Still owed.** `n8-wrap` only.

---

## Iteration 8 — n8-wrap: closing the window

**Branch reconciled.** `autonomous/2026-09-04-daily`, `git log -1` = `15ea64d night(n7-close)`, tree
clean — matching `STATE-night.json` (iteration 7, `current: n8-wrap`). No disagreement. This entry is
the only artefact of this iteration: no Hub, nothing driven, no product code, no tests re-run.

**The queue emptied.** Eight items composed at 23:04, eight done. The window built exactly the one
`APPROVED` row in `spec-queue/APPROVALS.md` — `2026-09-06-an-unread-editor-cannot-overwrite`, F271
(A), 24 tasks — start to finish, and archived it. No proposals were written, which is correct: this
window does not write them.

### What changed

Six commits, and the split between product code and paper matters for how closely to read them:

| Commit | Carries |
|---|---|
| `fd32657` n1-repro | **drive harness only** — column D added to `scripts/drive/`, run once against the *pre-change* bundle |
| `c7e615c` n2-impl | **product code** — `hub/ui/src/components/.../InstructionsPage.tsx`, the three-branch render. Read this one closely. |
| `eb1d1d7` n3-units | **tests** — `hub/ui/src/__tests__/instructionsUnreadEditor.test.tsx`, 8 tests |
| `676eba4` n4-bundle | **committed build artefact** — `hub/hub/static/ui`, rebuilt + `refresh_ui_bundle.py` stamp. Generated, not authored; review the stamp, not the JS. |
| `f664f45` n5-drive | **drive harnesses** — inverted failure columns, plus `t_d4_retry_by_hand.py` |
| `15ea64d` n7-close | **docs and specs** — spec sync into `openspec/specs/project-instructions/`, archive move, `FINDINGS.md` F271 closure and index revision. No executable path. |

One product-code file changed all night. Everything else is tests, generated output, harnesses and
prose.

### What was proven, with evidence — and, separately, what was only inferred

**Proven by a browser against the bundle the Hub actually served** (the strong evidence, and the only
kind F274 accepts for a UI change):

- 31 passed / 0 failed on a fresh `:8011` from source, fresh DB, served JS `index-C9-G-wn9.js`,
  stamp `eb1d1d7`. Failed load and 500: **no editor, Save gone (not merely inert), a stated failure
  in a `role="alert"`, a Retry beside it, 0 PUTs, the stored row byte-identical afterwards.**
- **Mutation-checked at the drive level, which is what makes the 31 mean anything**: the *same*
  inverted harness re-run against the pre-change bundle (`676eba4^`, served JS confirmed back to
  `index-BXA4wQsi.js` on the wire) gives 19 passed / **12 failed**, with column A's baseline checks
  passing identically in both runs. The 12 are this fix and nothing else. Pre-fix, the drive's own
  typing reached the stored row (`TYPED BY THE DRIVE`) — the instrument can see a write.
- Column D (read in flight) passes **unmodified** against an expectation written before the fix
  existed, which is the strongest form available here.
- **Retry driven by hand with no route interception** (`t_d4_retry_by_hand.py`, 15/0): a real uvicorn
  started, stopped, started again on the same DB. Asserted explicitly that the page does **not** heal
  on its own when the Hub returns — otherwise the Retry assertion would have been vacuous.
- The fix reached the bundle **by content**: the new string occurs once in the new bundle and zero
  times in the old, both greps validated against a control string matching in both.
  `AW_CHECK_UI_BUNDLE=1 pytest hub/tests/test_ui_build_stamp.py` → 13 passed, strict assertion on.

**Proven only by tests** (weaker — a green suite is not behaviour): 8 unit tests, each
mutation-checked by `git checkout c7e615c^ -- InstructionsPage.tsx`; **6 of 8 fail** against the old
component, each on its own claim. 2.5 and 2.7 pass before and after, and the entry for iteration 4
says which and why rather than rounding up to 8.

**Inferred, not measured, and labelled as such in `FINDINGS.md` rather than folded into the 31/0**:
F271's **cross-project variant (path C)** is closed *by construction* — per-query-key `data` plus
unit 2.6 — because there is no in-page project switcher to drive. If one lands, drive it.

**Not claimed at all:** nothing tonight touched F140, F142, F154, F155 or F295; no Python suite was
run (no Python path is in the diff); the Hub's own 15–25 minute suite was never exercised.

### What is open

- **F293 + F294 want one change between them, not two.** Both are severity B, both filed 2026-09-06
  from the same drive, both about F126's cutover guard. F126's deferred column **plus** a claim or
  uniqueness constraint answers both; **the column alone races identically**, so a proposal that
  stops at the column has not fixed F294. Neither is specced.
- **The open severity-A list is six: F274, F140, F142, F154, F155, F295** — and **five of the six are
  unproposed.** F140/F142/F154/F155 each have an *archived, never-driven* change and an unmet
  re-drive banner. **F295** is severity A, was filed 2026-09-06 out of F292, and had never appeared
  on any list on the index page before tonight. **F274 is the sixth and is different**: it is fully
  specced — `openspec/changes/2026-09-05-the-conversation-carries-its-own-run-facts`, 44 tasks, 0
  done, the only unarchived change — and sits in `APPROVALS.md:99` **with no verdict token in front
  of it**. It is waiting on the operator, not on a spec loop.
- **F292's gate question (DAY-2)** is untouched by this window and still open.

### Decisions waiting for the operator

1. **`2026-09-05-the-conversation-carries-its-own-run-facts` (F274) has no verdict.** Write
   `APPROVED`, `REVISING` or `REJECTED` in front of it in `spec-queue/APPROVALS.md`. Until then no
   night window may build it, and it is the one change already ready to build.
2. **DAY-1** (relax the seeded no-merge line) and **DAY-2** (spend a night slot on F292, or let the
   gate tolerate a re-run) are carried over unanswered from the 2026-09-06 day window.
3. `decisions_for_user` is otherwise empty — this window made no decision that was the operator's.

### What to distrust

- **I tested my own work throughout.** The drive harnesses, the unit tests and the component were all
  written by this window. The mitigation used everywhere was mutation-checking against the *previous*
  commit rather than reasoning — accept a claim here only where the entry names which artefact failed
  when the fix was reverted. Where it does not, treat it as untested.
- **The instrument note, and it is the transferable one: a substring test for `fixed` reads
  "not fixed" as fixed.** That is how n7-close's first severity-A recount reported five instead of
  six. It was caught only by a check that was not looking for it — a heading count coming back 41
  against a remembered 40. **Any future recount must not repeat it**, and more generally: an
  instrument that agrees with the expected answer has not been validated.
- **`hub/hub/static/ui` is generated.** Do not read its diff as authored code; the thing to verify is
  the stamp and that `hub/ui/src` was committed alongside it (it was — `hub/ui/src` had nothing left
  to stage at `676eba4` because `c7e615c`/`eb1d1d7` already carried it, so the pair cannot diverge
  across a push).
- **The branch is now 42 commits deep, spans four days, and is still unmerged.** The merge gate did
  not open tonight and this window never touches `master`.
- **`APPROVALS.md` for 2026-09-06 reasons off the superseded two-count** for open severity-A rows. It
  is deliberately left as written — it is the record of a decision session — but read the index
  revision in `FINDINGS.md` alongside it.

**Queue empty. `next_action` set to null so the driver unregisters rather than spending an invocation
rediscovering there is nothing to do.**

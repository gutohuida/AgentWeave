# conversation-checkpoint Specification

## Purpose

A checkpoint is the Hub's durable record of where a conversation got to: a structured envelope it computes and can therefore verify, carrying one markdown body only a model can write. It replaces a design in which the agent was asked to write a file — observed twice producing an artifact in an unreachable place and then producing nothing at all, both times reported as ready because readiness meant the run had ended.

## Requirements

### Requirement: A checkpoint is a durable Hub record, not a file an agent is asked to write

The Hub SHALL persist each checkpoint as a durable record attached to the conversation it describes,
and SHALL NOT depend on an agent writing a file to disk.

The prior mechanism instructed the agent to invoke a skill AgentWeave never installs and to write
beneath a path outside the agent's own working directory. Both instructions were unsatisfiable: one
runtime was refused by its sandbox, the other silently created a second nested workspace tree
nothing reads. Where an artifact was produced at all, it was produced by a skill belonging to the
operator's personal environment rather than to the product.

A checkpoint MUST NOT require any skill, template, or directory to be installed into a project.

#### Scenario: A checkpoint is produced without an installed skill

- **WHEN** a checkpoint is requested for a conversation
- **THEN** the Hub produces it without invoking any project-installed skill
- **AND** the record is attached to the conversation

#### Scenario: No filesystem location is required

- **WHEN** a checkpoint is produced
- **THEN** no path outside the Hub's own storage is required for it to succeed
- **AND** an agent's working-directory restrictions do not affect whether it succeeds

### Requirement: Checkpoint generation runs on a worker with an operator-chosen runner and model

Checkpoint generation SHALL run as a Hub-side worker invocation whose runner and model are chosen by
the operator, using a prompt owned and versioned by the Hub.

Generation is out-of-band: it does not occupy an agent turn, does not require the agent's
cooperation, and cannot be refused by it. Reusing runner records means the operator manages
generation through a surface that already exists, and lets a checkpoint be generated by an
inexpensive model while the work itself runs on an expensive one.

A worker invocation MUST NOT be built as an agent turn. It carries no streaming protocol, no tool
server, no permission posture, and no injected project context, because it reads one prompt and
returns one structured answer.

A worker failure — non-zero exit, timeout, or unparseable output — MUST NOT raise into the caller,
and MUST be recorded.

#### Scenario: The operator selects the generating runner and model

- **WHEN** an operator configures checkpoint generation
- **THEN** the runner and model are selected from the registered runners and the model catalog
- **AND** generation uses that selection rather than the agent's own runner

#### Scenario: A worker failure is recorded rather than raised

- **WHEN** a worker invocation exits non-zero, times out, or returns unparseable output
- **THEN** the failure is recorded against the invocation
- **AND** no exception propagates to the caller

#### Scenario: Every invocation is accountable

- **WHEN** a worker invocation completes
- **THEN** its prompt version, runner, model, duration, and outcome are recorded

### Requirement: Fields the Hub can determine are computed, never requested from a model

A checkpoint's identity, lineage, changed files, ledger, and runtime state SHALL be computed by the
Hub, and the generating model SHALL be asked only for judgement.

The division is verifiability. What the Hub can check, it must not delegate: a model asked for a
timestamp it could not obtain invented one, and a model asked for pending work reported none from a
worktree that is always clean because the Hub commits every turn.

Computed: conversation, agent, runner, model, timestamps, trigger, lineage, files changed derived
from the conversation's own commits, tasks assigned to the agent, open questions, permission
decisions, and runtime overrides in force.

Written by the model: objective, current state, decisions and their rejected alternatives, dead ends
and their symptoms, next actions whose first step is executable without a further decision, and
risks not to repeat.

Tasks are project-scoped and carry no conversation, so the task list a checkpoint carries is the
agent's whole list. The record MUST state this rather than imply the list is conversation-specific.

#### Scenario: The envelope is complete even when generation produces nothing

- **WHEN** the worker returns no usable output
- **THEN** the computed fields are still populated
- **AND** the record states that its written half is absent

#### Scenario: A computed field is not solicited from the model

- **WHEN** a checkpoint is generated
- **THEN** the prompt does not ask the model for changed files, tasks, questions, or timestamps

### Requirement: A checkpoint is anchored on its predecessor rather than regenerated

Generation SHALL read the preceding checkpoint and the turns since it, and SHALL NOT re-read the
whole conversation from its start.

Regenerating a summary from scratch each time loses information gradually, and re-reading a full
transcript costs the worker its full price on every checkpoint. Anchoring also makes the chain a
first-class object rather than a by-product, which is what allows checkpoints to serve tracking
rather than only resumption.

#### Scenario: A later checkpoint anchors on the earlier one

- **WHEN** a conversation that already has a checkpoint produces another
- **THEN** generation reads the preceding checkpoint and only the turns since
- **AND** the new checkpoint records its predecessor

#### Scenario: A first checkpoint has no predecessor

- **WHEN** a conversation produces its first checkpoint
- **THEN** it records no predecessor
- **AND** generation reads the conversation from its start

### Requirement: The agent contributes notes that the Hub treats as input rather than as the artifact

The Hub MAY request brief structured notes from the agent before a checkpoint, and SHALL produce the
checkpoint whether or not those notes arrive.

Generation from the record cannot recover what never reached it: what the agent was about to do
next, what it suspects but did not verify, and what it would warn a successor away from. The agent
is asked for those, and only those. It MUST NOT be asked for anything the Hub computes.

Notes MUST be requested through a tool call rather than parsed from prose, so that the request, the
response, and the absence of a response are all observable.

Notes MUST be requested before the conversation is succeeded, because the provider session does not
survive the transition. They SHALL be requested at a configured point earlier than the point at
which the conversation is succeeded, so they are not composed from an already-exhausted context.

#### Scenario: Notes are unavailable

- **WHEN** the agent does not answer, refuses, or returns unusable notes
- **THEN** the checkpoint is still produced
- **AND** the record states that agent notes were not incorporated

#### Scenario: Notes are requested before exhaustion

- **WHEN** automatic checkpointing is configured
- **THEN** notes are requested at the configured notes point
- **AND** that point precedes the point at which the conversation is succeeded

### Requirement: A checkpoint that contradicts the Hub's own records is reported as failed

The Hub SHALL verify a generated checkpoint against the records it already holds, and SHALL report a
checkpoint whose claims contradict them as failed.

Readiness previously meant only that a run had stopped. A run that produced no artifact and returned
a question to the operator was reported ready, which is the defect this capability exists to remove.

Verification compares the checkpoint's claims against determinable truth: which files changed, which
tasks are assigned, which questions are unanswered. These are the claims that summarisation is
weakest at preserving, and they are the claims the Hub can settle without a judge.

Readiness SHALL mean that a checkpoint record exists and passed verification. It MUST NOT be derived
from a run ending.

A checkpoint MUST be reportable as failed both when it is absent and when it is wrong.

#### Scenario: A contradicted claim fails the checkpoint

- **WHEN** a generated checkpoint's claims about changed files, assigned tasks, or unanswered
  questions disagree with the Hub's records
- **THEN** the checkpoint is reported as failed
- **AND** the disagreement is recorded

#### Scenario: A run ending does not confer readiness

- **WHEN** a run stops without a checkpoint record having been produced
- **THEN** no checkpoint is reported ready

#### Scenario: A successor can be resumed from the checkpoint alone

- **WHEN** a checkpoint is verified
- **THEN** its next actions and state are sufficient to continue the work without the preceding
  conversation

### Requirement: A checkpoint cites observations that remain exactly recoverable

A checkpoint SHALL carry citations to the recorded observations it summarises, and the Hub SHALL
return the cited observation exactly on request.

Recorded output is already retained verbatim under stable identifiers and is discarded at a
transition today. Citing it gives a summary an exact escape hatch: the narrative carries what
matters, and anything it compressed away remains recoverable without re-running a tool and without
similarity search.

Recall SHALL be scoped to the conversation the checkpoint describes.

#### Scenario: A cited observation is recovered exactly

- **WHEN** a permitted reader requests a cited observation
- **THEN** the recorded content is returned unmodified

#### Scenario: Recall is bounded to the cited conversation

- **WHEN** a reader requests an observation outside the conversation the checkpoint describes
- **THEN** the request is refused

### Requirement: Reading a checkpoint and recalling raw observations are separate permissions

The Hub SHALL treat permission to read a checkpoint and permission to recall raw observations as
independent grants, both closed by default.

A checkpoint is curated and bounded. A recorded observation is raw tool output that may contain file
contents, command output, or credentials from another agent's working directory. An agent that
benefits from understanding how work was done — including its dead ends — does not thereby need read
access to another agent's workspace.

Grants SHALL be held as structured configuration on the agent, and MUST NOT be expressed in a
charter. A charter is editable prose describing behaviour; allowing it to widen access would let an
edit to a paragraph change what an agent can read.

Authorisation SHALL be resolved from the identity bound to the running turn, and MUST NOT be
accepted from a request body or header.

A checkpoint MAY additionally restrict itself, in which case access requires both the reader's grant
and the checkpoint's own visibility.

#### Scenario: A reader granted checkpoints is refused observations

- **WHEN** an agent permitted to read another agent's checkpoints requests a cited observation
- **AND** it holds no recall grant
- **THEN** the observation request is refused
- **AND** the checkpoint remains readable

#### Scenario: Access is closed without a grant

- **WHEN** an agent requests a checkpoint belonging to another agent with no grant
- **THEN** the request is refused

#### Scenario: Charter text cannot widen access

- **WHEN** a charter describes an agent as permitted to read other agents' work
- **THEN** that description does not grant access

### Requirement: Lineage is recorded and participation is derived

The Hub SHALL record a checkpoint's position in its conversation's lineage, and SHALL derive
cross-agent participation from existing run attribution rather than recording it separately.

These are different graphs and conflating them produces an identifier that means two things. Lineage
is linear and belongs to one agent: each checkpoint records its predecessor, and shares a lineage
identifier so a chain can be selected without walking it.

Participation is cross-agent and belongs to a unit of work. Every mutation already records the run
that made it, and every run records its agent and conversation, so which agents and conversations
touched a task is answerable from existing attribution.

#### Scenario: A chain is selected without walking it

- **WHEN** the checkpoints of one conversation lineage are requested
- **THEN** they are returned by lineage identifier
- **AND** ordering is available from each checkpoint's recorded predecessor

#### Scenario: Participation is answered from run attribution

- **WHEN** the agents that contributed to a task are requested
- **THEN** they are derived from the runs that created or updated it
- **AND** no separate participation record is maintained

### Requirement: Automatic checkpointing is configured as a threshold in proportion or in tokens

The Hub SHALL let an operator configure whether checkpoints are produced automatically, and SHALL
accept the threshold either as a proportion of the context window or as an absolute token count.

Context windows differ enough between models that an absolute instruction is often the meaningful
one. An absolute threshold also requires no context window at all, so it remains usable for a model
whose window is unknown, where a proportion cannot be evaluated.

A threshold SHALL be expressed as a mode together with a value, so that exactly one interpretation
can ever apply. Token thresholds are entered in thousands.

Configuration SHALL resolve from the agent, then the project, then a built-in default. An agent's
threshold replaces the project's entirely, mode and value together; the two MUST NOT be combined
field by field, which would produce a threshold neither was configured to mean.

A token threshold at or above a known context window SHALL be refused, because it can never be
reached.

#### Scenario: A token threshold is evaluated without a known window

- **WHEN** an agent's context window is unknown
- **AND** its threshold is expressed in tokens
- **THEN** the threshold is evaluated against the observed token count

#### Scenario: A proportional threshold requires a known window

- **WHEN** an agent's context window is unknown
- **AND** its threshold is expressed as a proportion
- **THEN** no automatic checkpoint is triggered from that unresolved window

#### Scenario: An agent threshold replaces the project threshold whole

- **WHEN** a project configures a proportional threshold
- **AND** an agent configures a token threshold
- **THEN** the agent's mode and value both apply
- **AND** the project's mode is not combined with the agent's value

#### Scenario: An unreachable token threshold is refused

- **WHEN** an operator sets a token threshold at or above a known context window
- **THEN** the configuration is refused

### Requirement: A produced checkpoint is visible to the operator

The Hub SHALL present a produced checkpoint to the operator in the conversation it describes, and
SHALL make it readable through the API.

A checkpoint the operator cannot inspect reproduces the defect this capability removes: a signal
reported as successful that nothing examines. Visibility is what makes verification meaningful to
the person relying on it.

#### Scenario: A checkpoint appears in its conversation

- **WHEN** a checkpoint is produced for a conversation
- **THEN** it is presented within that conversation
- **AND** its verification outcome is presented with it

#### Scenario: A failed checkpoint is visibly failed

- **WHEN** a checkpoint fails verification
- **THEN** it is presented as failed rather than omitted

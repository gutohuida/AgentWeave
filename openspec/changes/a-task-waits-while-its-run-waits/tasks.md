## 1. Reproduce both halves as failing tests first

- [x] 1.1 Add `hub/tests/test_a_task_waits_while_its_run_waits.py`. Build F14's shape: an agent, a run bound to a task in `in_progress`, and a blocking question asked through `POST /questions` on the agent-facing router (`agent_actions.ask_operator_question`) with the run's own credential. Assert the current behaviour — `task.status == "in_progress"`, `blocked_reason is None`, `question.blocked_task_id is None` — and run it against unmodified code to confirm it passes. A reproduction that does not pass first is not a reproduction.
- [x] 1.2 Add F60's shape to the same file: the same setup, then the run moves the task to `completed` while the question is still unanswered. Assert what F60 measured — `tasks.status == "completed"`, `blocked_reason is None`, `questions.answered is False`, `declined is False`, `blocked_task_id is None` — and that nothing on the task response says a decision was made without the operator. Confirm it passes against unmodified code.
- [x] 1.3 Add the batch shape: `POST /questions/batch` with two blocking questions, and assert that today neither records `blocked_task_id` and the task does not move.

## 2. Park at ask time

- [x] 2.1 Move `_announce_block` from `hub/hub/run_divergence.py:539-568` to `hub/hub/run_task_binding.py`, beside `block_task_for_question`. Keep `severity="info"` and its docstring's reasoning. Update the `run_divergence` call site to import it.
- [x] 2.2 In `hub/hub/api/v1/agent_actions.py`, after `ask_question_for_actor` returns, park the asking run's bound task: resolve `Run` from `actor.run_id`, take `run.task_id`, and call `block_task_for_question` with the run, task and question. Do it for the single-question route and for every question of the batch route, in batch order. **Commit it.** `ask_question_for_actor` commits and refreshes before it returns (`hub/hub/api/v1/questions.py:193-195`), so the park is a second write with no commit of its own; in the batch route it would otherwise be flushed by accident by the next question's create. And the ask must still succeed if the park fails — the question is already committed by then, and a park that raises must not cost the agent the question it just asked or the wait it is about to enter. Catch, log, return the question.
- [x] 2.3 Only for `blocking=True`. A non-blocking ask is the agent leaving a note; parking on one would make the status mean an agent mentioned something (`unanswered_blocking_question`'s own rule).
- [x] 2.4 Do **not** put this in `ask_question_for_actor`. That helper is shared with the operator-facing `POST /questions`, which has no run and no binding, and a park there would be a park with no asker. Add a comment saying so, because the helper is the obvious wrong place.
- [x] 2.5 Announce it: call the moved `_announce_block` when — and only when — the transition actually happened, matching `evaluate_run_end`'s existing condition, so a batch of four announces once.
- [x] 2.6 Correct the **four** comments that now state something false — round 1 counted two. `hub/hub/api/v1/tasks.py:336-343` (`_attach_awaiting_answer`'s docstring), `hub/hub/schemas/tasks.py:315-322` (`awaiting_answer_reason`'s comment), `hub/ui/src/api/tasks.ts:34-43` (the same sentence on the TypeScript field) and `hub/ui/src/components/tasks/TaskCard.tsx:115-119` ("does not park its task until it *ends*"). All four say a task reaches `blocked` only when the asking run ends. Say what the field is still for instead — the two cases in design D9.
- [x] 2.6a No UI *behaviour* change is needed for half (a), and the reason is worth a test rather than a comment: `TaskCard` already coalesces (`const waitingReason = task.blocked_reason ?? awaitingAnswer`, `isWaitingOnOperator = isBlocked || Boolean(awaitingAnswer)`), so a task that is now both `blocked` and carrying `awaiting_answer_reason` renders one wait, not two. Assert it in `hub/ui/src/__tests__/taskBlockedTreatment.test.tsx`.
- [x] 2.7 Flip task 1.1's assertion: the task is `blocked` with a `blocked_reason` derived from the question, `blocked_task_id` is set, and the transition is `origin='runtime'` naming the run. Add the batch assertion from 1.3 flipped: the first question transitions, the second records `blocked_task_id` without a second transition.
- [x] 2.8 Add the case the park must refuse: a run bound to a task in `under_review` asks a blocking question. No transition, **`blocked_task_id` is NOT recorded**, and `awaiting_answer_reason` reports the wait. Round 1 asserted the opposite and was wrong about the code it cited: `block_task_for_question` records `blocked_task_id` on the non-transitioning branch *only* when `task.status == STATUS_BLOCKED` (`hub/hub/run_task_binding.py:625-628`). That is also correct — `run-task-binding:663` is scoped to "a question asked about a task that is **already waiting**" — so the test asserts the code, and D2's "reused unchanged" survives. This is the case that keeps `awaiting_answer_reason` alive.

## 2b. Close the way out that needs no report

Round 3 (design D10). Without this, everything in groups 4–8 is bypassable by one call the agent is
already told it may make, and the ADDED requirement in `task-lifecycle-governance` is unenforceable.

- [x] 2b.1 Reproduce it first, against **unmodified** code plus group 2: park a task at ask time, then have the run PATCH it to `in_progress` and then to `completed`. Both succeed today. That test is the defect; it becomes the guard's test when inverted.
- [x] 2b.2 In `hub/hub/api/v1/tasks.py`, beside the existing `status == STATUS_BLOCKED and not actor.is_operator` refusal at `:1157`, refuse a **non-operator** moving a task whose current status is `blocked` to anything else. Same place and same shape as the entering guard, because it is the same rule read the other way: `task-lifecycle-governance:445` says "enter **or leave**". The message must name what does end a wait — the operator's answer or decline, or the run's own expired wait — so the agent is not left guessing at a 403.
- [x] 2b.3 Derive it from `task.status` before the transition, not from `body.status`, and put it before `apply_transition` so nothing is staged when it refuses.
- [x] 2b.4 Confirm the three legitimate releases still pass, by actor rather than by inspection: `release_block_for_question` uses `operator()` (`run_task_binding.py:687`) and does not go through this route at all; the expiry release is run-attributed with `origin=runtime` and likewise does not; the operator's own PATCH is exempt by `actor.is_operator`. A test per path.
- [x] 2b.5 Invert 2b.1: `blocked -> in_progress` by the bound run is `403`, `blocked -> completed` is still `409` as an illegal edge, and the two refusals are distinguishable.
- [x] 2b.6 `GET /tasks/transitions/allowed` needs no change and must not get one: it is hardcoded to `ACTOR_OPERATOR` (`tasks.py:1301`, *"The operator's own view of the transition map"*), and the operator is exactly who this guard exempts. So `test_task_transitions_api.py:385`'s `transitions["blocked"] == ["assigned", "in_progress", "rejected"]` stays true and its sibling contract — the control never offers a move that then fails — is not broken. Assert that rather than assume it, because a guard that made the operator's own control lie would be a worse defect than the one it fixes.
- [x] 2b.7 Check `mcp_server.py` needs no change: `TaskStatus` already withholds `blocked`, and `in_progress` must stay in it because it is the ordinary claim. The refusal belongs at the route, which is where the entering one is and for the reason its docstring gives.

## 3. Prove that blocked-while-running breaks nothing

One test per row of the proposal's table that a reviewer could reasonably doubt. These are guards,
not reproductions; they must pass before and after.

- [ ] 3.1 A run whose task was parked at ask time ends with the question still unanswered: no divergence is recorded, the task is still `blocked`, and nothing was started in response (`run-task-binding:594`).
- [ ] 3.2 A loop firing does not claim a task parked at ask time, and the loop's board still shows it as the current item (`jobs.py:354`).
- [ ] 3.3 A second run binding to a task parked at ask time leaves it parked (`run-task-binding:618`).
- [ ] 3.4 `_free_agents` still excludes the agent whose run is waiting.
- [x] 3.5 **Do this with group 7, not without it** (design D5): a board that reads a waiting task as "flagged, not stopped" while the gate can still stop it permanently states something false, and ungating without this leaves a resumable task rendered `gated`. Fix `dependency_state` in `hub/hub/api/v1/tasks.py:317`: `running_on_regressed` is derived from `response.status == "in_progress"` and must also hold for `blocked`, which is reachable only from `in_progress` and therefore has always started. Add a test for a parked task with a regressed prerequisite, and a comment recording that this was already wrong before ask-time parking and is only widened by it.

## 3a. Who is on a blocked task while a run is on it

Round 3 (design D11). The one row the proposal's table did not have.

- [x] 3a.1 Reproduce: a task parked at ask time with its run still `running`, read through the loop summary. `attribution.capacity` is `assigned` today — "nothing is running" — about an agent mid-turn on that task.
- [x] 3a.2 In `hub/hub/task_attribution.py`, consult `live.task_ids` before the final `CAPACITY_ASSIGNED` fall-through, not only inside the `unstaffable` branch. Keep the sources separate — `live` is the runs table, `staffing` is the firing — because `agent-loops` requires that no source is asked a question it does not answer.
- [x] 3a.3 Correct `CAPACITY_ASSIGNED`'s comment, which says "this is the `blocked` case". After this it is the blocked case *with nothing running*, which is the case the requirement was written for.
- [x] 3a.4 Test both halves: blocked with a running bound run reads `working`; blocked with no running run still reads `assigned`. And the existing capacity tests still pass — this must not re-merge anything.
- [x] 3a.5 The `agent-loops` delta splits the shipped scenario in two. Do not archive without it.
- [x] 3a.6 `LIVE_STATUSES` is **not** touched. Record in the test module why the two answers differ: `LIVE_STATUSES` asks whether anyone is accountable for the work, `task_attribution` asks who is on the task and in what relation. Leaving them consistent-by-accident is what produced the collision.

## 4. The wait's deadline, recorded

- [ ] 4.1 Add `wait_expires_at` to `Question` in `hub/hub/db/models.py`, nullable `UTCDateTime`, with a comment saying what it is for and **why it is stored rather than derived at read time**: the operator can edit `Agent.question_timeout_seconds` while the run waits, so a deadline recomputed later would describe a wait that never happened. Design D3 — this is the one place this change knowingly declines the prefer-derived default.
- [ ] 4.2 New migration in `hub/hub/migrations/versions/`, guarded for a missing table the way `0033`/`0034` are, adding this column and `wait_ended_at` (5.1) together. Bump the head assertions in `hub/tests/test_migrations.py` **and** `hub/tests/test_project_persistence.py`.
- [ ] 4.3 **Round 3 replaced this task.** It used to accept `wait_seconds` on the agent-facing ask schemas; it must not, because that puts the threshold of D4's refusal in the hands of the run being refused (design D3). Instead add a Hub-side resolver — `effective_question_wait(agent_row)` in `hub/hub/api/v1/agent_trigger.py`, beside the code that writes the env var, since that is the fact it must agree with: `agent_row.question_timeout_seconds` when set, else the Hub's own `AW_QUESTION_TIMEOUT` parsed under the same rules, else 240. Restate `MIN_WAITING_SECONDS`/`MAX_WAITING_SECONDS`/240 the way `mcp_server.py:801-802` already does, and add the test that asserts the two agree — that test is what makes the restatement safe.
- [ ] 4.3a Set `wait_expires_at = now + effective_question_wait(...)` in the agent-facing ask routes, for blocking questions only, in the same second write as the park (2.2). No schema field, no request body value, and nothing an older or forged caller can influence.
- [ ] 4.3b Test the agreement directly rather than by inspection: with `Agent.question_timeout_seconds` set, the recorded deadline matches it; with it null and `AW_QUESTION_TIMEOUT` set in the Hub's environment, it matches that; with neither, 240; with an out-of-range or unparseable value, 240 — the same fallbacks `_configured_wait` takes, because the tool will take them.
- [ ] 4.4 **Round 3 deleted this task.** `hub/hub/mcp_server.py`'s ask is unchanged. Confirm that by running `hub/tests/test_mcp_tool_schemas.py` and diffing the module: nothing in this change may touch it except 5.5.

## 5. The end of the wait

- [ ] 5.1 Add `wait_ended_at` to `Question`, nullable, in the same migration as 4.1.
- [x] 5.2 Add `POST /questions/wait-ended` to the agent-facing router in `hub/hub/api/v1/agent_actions.py`, taking a list of question ids. Not an `@mcp.tool()` — it is not a capability, and design D4 says why.
- [x] 5.3 Refuse per question, silently skipping rather than erroring the batch: not asked by the calling run; no `wait_expires_at`; `wait_expires_at` not yet passed; already answered or declined. Return which ids were accepted so the tool's own behaviour is testable.
- [x] 5.3a Decide and test what the route returns when the release raises. A `TransitionRefusedError` from one question's release must not lose the `wait_ended_at` writes for the others, and must not 500 — the tool swallows the failure (5.5) and the agent proceeds, so a 500 here is indistinguishable to the caller from the report never having been sent, which 5a exists to catch. Catch, log, report the id as not accepted, commit what succeeded.
- [x] 5.4 Set `wait_ended_at` on each accepted question, then release the task once if it is `blocked` — a new `release_block_for_expired_wait` in `run_task_binding.py` beside the other two release paths, attributed to the run with `origin='runtime'` and calling `release_reason`.
- [x] 5.5 In `hub/hub/mcp_server.py`, call it from `ask_user` immediately after the wait loop, with the ids of the questions that expired — the `expired` list already computed at `:411`, not `unanswered`, so a decline is never reported as an expiry. Failure to reach the Hub must not raise: the tool still returns the agent its answers.
- [x] 5.6 Test the whole path against the reproduction from 1.2: the task is `blocked`, the expiry is reported, the task is `in_progress` again with no `blocked_reason`, and `update_task(completed)` then succeeds. Assert the transition history reads `in_progress → blocked → in_progress → completed`, which is the requirement that no history states a task was completed while waiting.
- [x] 5.7 Test each refusal in 5.3 individually, and the unreported case: the run ends without reporting, and the task is still `blocked`.

## 5a. The report is not the only signal

Round 3 (design D4). Rounds 1 and 2 merged "the report was never sent" with "the report was sent and
did not land" and concluded that a missing report means nobody proceeded. Task 5.5 requires the
second to be swallowed, so in that case somebody did. The shipped analogue in the same router
already has both halves — `expire_permission_request`: *"The run reports and the run's end sweeps."*

- [x] 5a.1 Reproduce it, and make the reproduction the honest one: park at ask time, let the deadline pass, make the wait-ended POST fail (patch the tool's `_hub_request`, or stop the route from committing), have the agent then call `update_task(completed)`. It is refused, the task is stranded `blocked`, and the run ends. With group 2b in place the agent has no way out at all, which is the correct behaviour and the reason this sweep is required rather than optional.
- [x] 5a.2 In `run_divergence.evaluate_run_end`, before the existing park: where the run's outstanding blocking question has a `wait_expires_at` that has passed and is neither answered nor declined, treat the wait as ended — set `wait_ended_at`, release the task if it is `blocked`, through the same `release_block_for_expired_wait` the endpoint uses, so there is one release and not two.
- [x] 5a.3 Order it against 6.1 deliberately and comment the ordering: 6.1 makes `unanswered_blocking_question` skip a question whose wait ended, so the sweep must find its candidate before that exclusion applies to it, and must be idempotent when the report already arrived.
- [x] 5a.4 It must never fire early. Assert it: a run that ends while its recorded deadline is still in the future leaves the task `blocked` and sets nothing. That is the `An unreported wait that could not have expired leaves the task waiting` scenario and it is the guard against turning the sweep into the timer D4 rejected.
- [x] 5a.5 Assert the swept task then carries the same permanent statement (group 8) as a reported one. A wait that ended is a wait that ended; how the Hub found out must not change the record.

## 6. An expired wait is not an open one

- [x] 6.1 Add `Question.wait_ended_at.is_(None)` to `unanswered_blocking_question` (`run_task_binding.py:574-602`), with the reasoning from design D6 in the docstring beside the existing `blocking`/`declined` paragraphs.
- [x] 6.2 Add the same condition to `_attach_awaiting_answer`'s query (`hub/hub/api/v1/tasks.py:362-376`).
- [x] 6.3 Test the case that matters: a run whose wait expired, that reported it, and that then ends **without** moving its task. It must be divergent as normal, and the task must not be re-parked. Without 6.1 this test fails by recording a wait that had already ended.
- [x] 6.4 **Round 3: three more surfaces read the same idea from `answered = False` and now describe an ended wait wrongly** (design D6). Exclude it in `hub/hub/conversations.py:424` — `conversation_attention`'s question arm, which marks the conversation "waiting on the operator" and by its own docstring outranks `running`. Match the shape of the permission arm immediately below it, which already excludes `expired`; the two arms answer the same question and only one had an expired state to exclude. Carries the `agent-conversation-workspace` delta.
- [x] 6.5 Exclude it in `hub/hub/api/v1/jobs.py:385` — a loop's open-question count, which the operator reads as work still needing them. Carries the `agent-loops` delta.
- [x] 6.6 **Do the opposite** in `hub/hub/checkpoints.py:286` (`_open_questions_for`): keep the question, and add that its wait ended. This is the one reader whose audience is the successor *agent*, which needs to know a decision was already taken without the operator — and it is the stated cover for the task a checkpoint omits during a wait (`LIVE_STATUSES`, task 3a.6), so it must not be silently dropped. Carries the `conversation-checkpoint` delta.
- [x] 6.7 Test each of 6.4–6.6 against the same fixture: one run, one expired wait, then read the conversation rail, the loop summary and a generated checkpoint. Two must stop saying somebody is waiting; the third must still list the question and say the wait ended.
- [x] 6.8 Leave `scheduler._pending_loop_request` (`scheduler.py:376-382`) alone and keep design's note about it. It is a fifth reader of the same family and a real defect — no `declined` exclusion — but it is about a loop's stop reason, not about who is waiting, and folding it in would put an unrelated fix inside a reproduction that cannot cover it.

## 7. Ungate the resume

- [x] 7.1 In `hub/hub/task_transition_service.py:371-383`, skip the dependency gate for `blocked -> in_progress`. Derive it from the task's status at the transition rather than from a flag the caller passes, so no caller can forget it and no caller can abuse it.
- [x] 7.2 Comment it with design D5's reasoning: the gate asks whether work may *start*, `blocked` is reachable only from `in_progress`, so this work started and this edge resumes it.
- [x] 7.3 Test all three releases — answer, decline, expiry — against a task whose prerequisite regressed while it waited. All three release; none is refused. The expiry case is the one that would otherwise leave an agent unable to complete finished work.
- [x] 7.4 Test that the gate still refuses `pending -> in_progress` and `assigned -> in_progress` unchanged, so this does not read as a general weakening.
- [x] 7.5 Overturn the shipped test that asserts the old rule, rather than working around it: `hub/tests/test_dependency_gate.py:185` `test_the_blocked_resume_edge_is_gated_the_same_way` must become its inverse, and the module docstring (`:6-7`) and section comment (`:155`) must stop saying "including the `blocked -> in_progress` resume edge". Its sibling `test_the_blocked_resume_edge_succeeds_once_the_prerequisite_is_approved` stays true and stays. Also correct the gate's own two docstrings, which state the old rule in prose: `hub/hub/dependency_gate.py:7-9` and `hub/hub/task_transition_service.py:370-378`.
- [x] 7.6 Add the case round 2 named that no earlier round did: a dependency **declared** on a task while it is waiting (`task-dependencies:262`) no longer stops it resuming, and the unmet prerequisite is reported against the task instead. This is the honest cost of the ungating and it must have a test rather than only a paragraph.

## 8. Say it on the task, permanently

- [x] 8.1 Add a sibling of `reason_from_question` in `run_task_binding.py` producing the "proceeded without your answer" statement, trimmed by the same `_REASON_LIMIT`, so the two surfaces cannot spell it differently.
- [x] 8.2 Add `proceeded_without_answer_reason` to `TaskResponse` in `hub/hub/schemas/tasks.py`, with a comment saying it is derived and why it is permanent.
- [x] 8.3 Derive it in `hub/hub/api/v1/tasks.py` beside `_attach_awaiting_answer`: a question with `wait_ended_at IS NOT NULL` and either `blocked_task_id == task.id` **or** asked by a run bound to that task. **No condition on `answered`** — design D7, and the reason belongs in the code as a comment because it is the non-obvious half. **Round 3 added the second arm**: `blocked_task_id` is recorded only where the task parked, so keying on it alone misses a run bound to an `under_review` task that waits out the full deadline and decides for itself — F60 with a different starting status. Drop `_attach_awaiting_answer`'s `Run.status == "running"` condition here, deliberately: that belongs to a live wait, and this record is permanent and looks backwards.
- [x] 8.3a Test that second arm on its own: a run bound to a task in `under_review` asks, the wait expires and is reported, no park ever happened, `blocked_task_id` is null — and the task still carries the statement.
- [x] 8.4 Render it on `TaskCard.tsx` and `TaskDetailDrawer.tsx`, distinct from the waiting treatment: waiting is a live ask, this is a decision already taken. Reuse the existing `Icon` component; do not add an icon source.
- [x] 8.5 UI tests in `hub/ui/src/__tests__/` alongside `taskBlockedTreatment.test.tsx`: a completed task carrying the statement renders it, and a task with an answered question does not.
- [x] 8.6 Backend tests: the statement appears on a `completed` task, survives `under_review` and `approved`, and is **still there** after the operator answers the question afterwards. That last one is F60's compounding half and is the assertion this requirement exists for.
- [x] 8.7 If `hub/ui/src` changed: `cd hub/ui && npm run build`, then `py -3.11 scripts/refresh_ui_bundle.py`, and commit `hub/ui/src` and `hub/hub/static/ui` together.

## 9. Gates, suites and a live drive

- [ ] 9.1 `ruff check src/ hub/ tests/` · `black --check --target-version py311 src/ hub/hub/ hub/tests/ tests/` · `mypy src/` · `cd hub/ui && npm run lint`.
- [ ] 9.2 Full Hub suite. Baseline before this change is what the previous iteration measured; the difference must be exactly the tests this change adds.
- [ ] 9.3 CLI suite and UI suite.
- [ ] 9.4 A drive harness under `scripts/drive/`, against a Hub on 8011 started from current source, binding Haiku: a real agent asks a real blocking question about a real task, and the task is observed `blocked` **while the run is still `running`** — the assertion no unit test can make. Then leave it unanswered, let the timeout expire, and assert the task returns to `in_progress`, the agent completes it, and the completed task carries the statement. Restart the Hub first and confirm it serves the code under test.
- [ ] 9.4a Extend the drive to the round-3 findings, because three of them are exactly the kind no unit test reaches. With the task parked and the run still `running`: read the loop summary and confirm the capacity says the agent is working it (3a); read the conversation rail and confirm it says waiting, then read it again after the expiry and confirm it does not (6.4); and have the agent attempt `update_task(in_progress)` on its own blocked task and confirm the 403 with a message it can act on (2b).
- [ ] 9.5 `openspec validate a-task-waits-while-its-run-waits --strict`, then sync **all five** deltas — `task-lifecycle-governance`, `task-dependencies`, `agent-loops`, `agent-conversation-workspace` and `conversation-checkpoint` — and archive the change. `--strict` does not compare capabilities, so it will not tell you if one is missed. Check by grepping the archived corpus: "Resuming is gated" must be gone; "unanswered, non-declined questions" must be gone from `agent-loops`; "holds an unanswered question" must be gone from `agent-conversation-workspace`; and `agent-loops` must carry two waiting-on-a-person capacity scenarios, not one.
- [ ] 9.6 Update `scripts/drive/FINDINGS.md`: F14 and F60 both fixed, naming the commits and anything the implementation overturned.

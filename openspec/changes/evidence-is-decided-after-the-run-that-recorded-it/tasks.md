## 0. Rounds — no task below may start until R2 and R3 are recorded in design.md's round log

- [x] 0.1 R1: explore and propose (2026-09-24, bundle B5)
- [x] 0.2 R2: an independent re-derivation against `requirement_evidence.py` (`record`, `duplicate_of`, `decide`, `restamp_run_footprints`), `run_liveness.py`, `agent_trigger.py`'s finalize order, both decision routes and both record routes. In particular: is the registry entry really popped only after the restamp commit (re-read `_execute_run` and the app-server path); does D2's digest guard agree with `requirement_coverage`'s staleness; answer design Open Question 2
- [x] 0.3 R3: a second independent re-derivation, not starting from R2's notes. `openspec validate evidence-is-decided-after-the-run-that-recorded-it --strict` passes
- [x] 0.3a Operator review fixes applied (`spec-queue/tracks/reviews/B5-2026-09-24.md` §2, operator decision 2): D7 re-queue, D1's stop remedy, D2's `produced_at` bump, D6 named as F426's fix
- [x] 0.4 The operator approves (APPROVALS.md, 2026-09-25 row)

## 1. Tests first — each fails on today's code unless marked as a control

New file `hub/tests/test_evidence_waits_for_its_run.py`. Stage liveness the way `test_approval_waits_for_the_turn.py:50-120` does (a stand-in session in `run_liveness.active_ptys`, popped in a finaliser). Reuse `builder`, `make_repo`, `make_document`, `record_as_agent` from `test_conflict_refusal_names_what_clears_it.py`.

- [x] 1.1 (D1) An agent records evidence under a run registered live. The operator's `POST /spec/evidence/{id}/decision {"decision":"accepted"}` answers **409** with `code == "recording_run_live"` and the run id in the message; `GET /spec/evidence/{id}/reviews` is empty; `review_state` is `awaiting`; the message also says stopping the run ends it (review, LOW). FAILS today (200, accepted)
- [x] 1.2 (D1) The same on the agent plane: a second agent granted `can_accept_evidence` decides it → 409 `recording_run_live` (not 403). FAILS today
- [x] 1.3 (D1) Pop the registry entry, decide again → 200 and one review. Control for the "clears by itself" half; passes before and after
- [x] 1.4 (D1) Control: a run whose row says `running` but is not in the registry → the decision is recorded. Passes before and after (it is what every existing decision test stages)
- [x] 1.5 (D1) Ordering: with the run live and an ungranted agent deciding → 403 `acceptance_not_granted`, not 409; with `decision: "maybe"` → 422. Pins D1's placement
- [x] 1.6 (D4) `GET /spec/requirements/FR-1?document=…` while live: the evidence row has `recording_run_live: true`; after popping, `false`. FAILS today (key absent)
- [x] 1.7 (D2) Record FR-1 twice from the same live run with different summaries and nothing committed between: the second answers **200** with the first's id, `revised: true`, and the second summary; exactly one `RequirementEvidence` row exists; its `produced_at` is later than the first record's (operator review: a revision bumps it), and with a second row for the same task recorded between the two records, `commit_for_task_review` returns the revised row's commit. FAILS today (409 `duplicate_evidence`)
- [x] 1.8 (D2, D5) Control: two different runs of the same agent at the same commit, checkout **clean** → the second is still refused `duplicate_evidence`. Passes before and after
- [ ] 1.9 (D2) Reword the requirement between the two records of 1.7 → the second creates a new row. FAILS today (duplicate refusal)
- [x] 1.10 (D3) 1.8's refusal message does not contain `commit it first`; an operator-recorded duplicate's still does. FAILS today on the first half
- [ ] 1.12 (D5) As 1.8, but the second run's checkout has an uncommitted change → the second record answers 201 with a new id; two rows exist. FAILS today (409 `duplicate_evidence`)
- [ ] 1.12a (D5) As 1.12, but the run is a review turn in a review checkout (dirty) → still refused `duplicate_evidence`. Control, passes before and after; FAILS if D5 ignores the snapshot condition
- [ ] 1.13 (D5) Control: an operator records the same duplicate with a dirty checkout → still refused, and the message still says `commit it first`
- [ ] 1.7b (D2, R3) Run A records FR-1 (clean) and ends (its row re-pointed at its snapshot). Run B, same agent and task, starts at that snapshot with an uncommitted change: records FR-1 (D5 → new row), then records FR-1 again with a new summary → **200** `revised: true` on **B's** row; exactly two rows exist (A's, B's). FAILS if D2 checks the same-run condition on `duplicate_of`'s oldest match (a third row appears)
- [x] 1.14 (D6, R3, F426) Both decision routes, with an approved task waiting on the evidence and integration raising: patch `task_integration.tasks_awaiting_this_commit` to **load** that `Task` through the session it is given (so a transaction is open, as in production) and `task_transition_service.retry_integration` to raise. Operator `POST /spec/evidence/{id}/decision {"decision":"accepted"}` → **200**, `review_state: accepted`; a granted agent's `POST /agent-actions/spec/evidence/{id}/decision` → **200**; the stored row reads `accepted`. FAILS today (`MissingGreenlet` at `spec.py:926`, a 500 — measured by R3). A stub that returns a task without querying does not open a transaction and passes on today's code: not evidence
- [x] 1.15 (D7) An agent `tester` (granted, in a conversation, its run registered live or not) decides a row whose recording run R is registered live → 409; then the transport's end is simulated the way the finally does it — pop R, call `agent_trigger._wake_decision_waiters(project_id, R)`, await the dispatched task. Exactly one `InboundQueueEntry` for `tester` exists: `state == "queued"`, `origin_type == "evidence"`, `origin_agent is None`, `hop_depth == 0`, `conversation_id` == the refusing run's conversation, content naming the evidence id, R, and the row's re-pointed commit. FAILS today (no entry; `_wake_decision_waiters` absent)
- [ ] 1.16 (D7) The refusing run was a review turn (an entry delivered into it carries `review_task_id=T`) → the wake entry carries `review_task_id=T`. A directly-triggered refusing run (no delivered entry) → both task columns `None`. FAILS today
- [x] 1.17 (D7; written: a, b, d — c and e unwritten) Controls, each FAILS if D7 over-reaches: (a) the operator is refused → nothing queued at wake; (b) the row is decided (by the operator) after the pop and before the wake task runs → nothing queued; (c) `tester` is refused twice on two rows of R → one entry naming both; (d) the waiters are cleared (`run_liveness.decision_waiters.clear()`, a restart's effect) → nothing queued and the decision succeeds; (e) the refusing conversation is archived → nothing queued, a warning logged
- [x] 1.18 (D7) Structural, in the manner of `test_evidence_restamp.py:353-371`: in `agent_trigger`'s source, each registry release (`active_ptys.pop(run_id` and `active_app_server_runs.discard(run_id)` inside a `finally`) is followed by `_wake_decision_waiters(` before the block's first `await`. FAILS today
- [x] 1.19 (D7) The origin: `inbound_queue.new_entry(origin_type="evidence", …)` persists through the migrated schema (both CHECK constraints), and `format_turn_prompt` renders it as `AgentWeave`, not `Agent "None"`. FAILS today (`ValueError`, then the CHECK)
- [ ] 1.11 Run `test_requirement_evidence.py`, `test_conflict_refusal_names_what_clears_it.py`, `test_approval_waits_for_the_turn.py`, `test_review_briefing_names_the_evidence_gate.py` before group 2 and record the counts

## 2. The fix

- [x] 2.1 (D1) `requirement_evidence.decide`: the liveness refusal after the value, grant and self-acceptance checks, immediately before the review is built. Import `run_liveness` at module scope (it imports only `db.models` and `pty_runner`; confirm no cycle)
- [x] 2.2 (D2) `record`: the same-run, awaiting, same-digest revise branch, re-applying the footprint with `outside_writes` read from the run and setting `produced_at` to now; return a marker the routes read (a `revised` attribute set on the instance, or a small result type — R2 picks)
- [x] 2.3 (D2) `agent_actions.record_evidence`: 200 with `revised: true` when revised; still broadcasts `spec_updated`
- [x] 2.4 (D3) The agent sentence in the duplicate refusal
- [x] 2.4a (D5) The agent-only dirty-checkout pass on the duplicate path, using this module's `_git`
- [x] 2.4b (D2, R3) The same-run lookup: `duplicate_of` gains `run_id=` (or a sibling query) so this run's matching row is found before the oldest match; D5's recorded-directory condition reads `Run.workspace_dir` (`recorded_workspace_dir`), not `footprint_root`
- [x] 2.4c (D6, closes F426) Both decision routes build their response before `integrate_what_was_waiting_for_this_evidence` and return it after
- [x] 2.5 (D4) `_evidence_view` and the agent-plane record response gain `recording_run_live`
- [x] 2.8 (D7) `run_liveness`: `DecisionWaiter`, `decision_waiters`, `note_decision_waiter`, `take_decision_waiters` (synchronous; no new imports). `decide` notes an agent waiter in the same synchronous stretch as the liveness check (design D1's snippet)
- [x] 2.9 (D7) `agent_trigger._wake_decision_waiters(project_id, run_id)`: synchronous launcher (take waiters; `create_task` held in a module set, as `checkpoint_handover.py:301-331`); the task opens its own session, drops rows decided since, resolves the refusing conversation with `get_conversation_by_id`, copies `task_id`/`review_task_id` from an entry delivered into the refusing run, `new_entry(origin_type="evidence")`, commits, then `schedule_agent`. Called right after the pop at `agent_trigger.py:2707` and the discard at `:3251`, before `outside_writes.flush()`. Exceptions logged, never raised
- [x] 2.10 (D7) Migration at the next free revision (read `.claude/rules/db-migrations.md` first): add `evidence` to `ck_inbound_queue_origin_type` and `ck_inbound_queue_origin_agent` (with `origin_agent IS NULL`) by table recreation as `0058` does; the same in `db/models.py:651-666` with a comment in the existing style; bump the head assertions in `test_migrations.py` and `test_project_persistence.py`
- [x] 2.11 (D7) `inbound_queue.new_entry`'s allowed set and message; `format_turn_prompt`'s `evidence` branch
- [x] 2.6 `mcp_server.py`: `record_evidence` docstring says a same-turn re-record revises; `decide_evidence` docstring names `recording_run_live`, says to decide after the run ends, and that the Hub sends a note when it does. Read `.claude/rules/mcp-server.md` first (stdlib + fastmcp only)
- [x] 2.7 Group 1 passes; the 1.11 counts are unchanged except the tests this change moved; ruff, black `--target-version py311`

## 3. Drive

- [ ] 3.1 On a trial Hub (`:8010`, from source), one Haiku turn records evidence and, mid-turn, the operator decides it through the route: record the 409 body verbatim. After the run ends, decide again: 200
- [ ] 3.2 (D7) On the trial Hub, a second Haiku agent granted acceptance is sent a message to decide the row while the first's turn is still running: it is refused 409, and after the first turn ends it is woken with the `evidence` note in the same conversation and its decision is recorded. Record the note's text verbatim

## 1. Constants & Configuration

- [x] 1.1 Add `"codex"` to `RUNNER_TYPES` in `constants.py`
- [x] 1.2 Add `RUNNER_CONFIGS["codex"]` with all fields: `cli`, `subcommand`, `session_flag`, `output_format`, `session_source`, `session_id_field`, `session_event_type`, `model_flag`, `context_flag`, `mcp_add_cmd`
- [x] 1.3 Add `AGENT_RUNNER_DEFAULTS["codex"] = "codex"` in `constants.py`
- [x] 1.4 Add `CODEX_MODEL_CONTEXT_LIMITS` dict (`gpt-5.5`: 272000, `gpt-4o`: 128000) in `constants.py`
- [x] 1.5 Add `"runner_options"` to `VALID_AGENT_CONFIG_KEYS` in `constants.py`

## 2. Session Layer

- [x] 2.1 Add `get_runner_options(agent)` method to `Session` — returns `runner_options` dict, defaulting to `{}`
- [x] 2.2 Update `sync_from_yaml()` in `session.py` to read and persist `runner_options` from `agentweave.yml`

## 3. Watchdog — Codex Command Builder

- [x] 3.1 Add `_build_codex_cmd(agent, prompt, session)` in `watchdog.py` — builds `codex exec [resume <id>] --json [-c model_instructions_file=<path>] [--model <m>] [-c memory_mode=disabled] "<prompt>"`
- [x] 3.2 Update the watchdog's JSONL session-ID parser to use `session_id_field` and `session_event_type` from `RUNNER_CONFIGS` (data-driven, not hardcoded per runner)
- [x] 3.3 Add stderr filter for Codex runner: suppress lines matching `"failed to record rollout items"`

## 4. Watchdog — Context Usage Reporting

- [x] 4.1 Parse `turn.completed` events in the Codex output pipeline to extract `input_tokens`, `cached_input_tokens`, `output_tokens`
- [x] 4.2 Compute `percent` using `CODEX_MODEL_CONTEXT_LIMITS` (fallback 128000 for unknown models)
- [x] 4.3 Write context_usage file to `.agentweave/shared/context_usage/<agent>.json` after each completed turn

## 5. Watchdog — Reset Context (Codex path)

- [x] 5.1 In the watchdog's `new_session_request` handler, add a branch for Codex runner: delete `.agentweave/agents/<agent>-session.json` directly instead of sending an inbox message

## 6. CLI Commands

- [x] 6.1 Add `codex` branch in `cmd_agent_configure()` — set runner, warn if `CODEX_API_KEY` not in environment
- [x] 6.2 Add `codex` branch in `cmd_mcp_setup()` — use `codex mcp add` command from `RUNNER_CONFIGS["codex"]["mcp_add_cmd"]`
- [x] 6.3 Update `agentweave switch` / `agentweave run` display logic to handle `codex` runner label correctly
- [x] 6.4 Add commented `runner_options` example to the `agentweave.yml` template generated by `agentweave init`

## 7. Hub Backend

- [x] 7.1 Expose `runner_options` field in the agent response schema (`hub/hub/api/v1/agents.py`) and Pydantic schemas
- [x] 7.2 Update `post_new_session_request` endpoint to emit a transport event the watchdog can act on directly (rather than only an inbox message) — or confirm existing event is already polled by watchdog

## 8. Hub UI — AgentCard

- [x] 8.1 Add `codex` entry to `RUNNER_CONFIG` in `AgentCard.tsx` with color `#10b981` (OpenAI green) background mix
- [x] 8.2 Add `opencode` entry to `RUNNER_CONFIG` in `AgentCard.tsx` (currently falls through to manual grey)

## 9. Hub UI — Mission Control

- [x] 9.1 Add `runner_options?: Record<string, unknown>` to `AgentSummary` interface in `agents.ts`
- [x] 9.2 In `MissionControlPage.tsx` `MissionCard`, replace Compact button with "Auto-managed" badge when `agent.runner === "codex"` — include tooltip text
- [x] 9.3 Verify Reset Context flow works end-to-end for Codex (Hub → watchdog → session file deletion → fresh ping)

## 10. Tests

- [x] 10.1 Unit test: `_build_codex_cmd` — first ping, resume, with/without context file, with/without model, memory disabled
- [x] 10.2 Unit test: JSONL parser reads `thread_id` from `thread.started` using `session_id_field` / `session_event_type`
- [x] 10.3 Unit test: context usage computed correctly from `turn.completed` token counts for known and unknown models
- [x] 10.4 Unit test: `get_runner_options` returns correct dict; `sync_from_yaml` persists `runner_options`
- [x] 10.5 Unit test: `cmd_mcp_setup` produces correct `codex mcp add` command for Codex agent

## 0. Rounds — no task below may start until R2 and R3 are recorded in design.md's round log

- [x] 0.1 R2: an independent re-derivation of the proposal against `hub/hub/api/v1/jobs.py` (`run_job`, `_loop_in_flight_decision`, `_held_in_flight_reasons`, the board's `:357` re-ask), `hub/hub/scheduler.py` (`_loop_flow_busy_reason`, `_loop_has_open_task`, `_agents_a_loop_may_staff`, `_stall_run_to_increment`, every `return False` in `_fire_job_internal`), and `openspec/specs/agent-loops/spec.md` (the requirement at `:1499` and the busy guard's own at `:793`). R2 checks in particular: rebuild design's decline-path table from `grep` without reading it first; check whether `:793`'s first sentence, which names two of the guard's three conditions, must join this delta; and re-run `scripts/drive/d2b_0923_run_answer_probe.py` (copy it under `hub/tests/`, run it with `-s`, delete the copy). Record the result in `design.md` `## Round log`. **Done 2026-09-23 (D-3b)**: two decline paths R1 did not describe (a firing that raises before / after its row exists) measured and folded into D3; `held` renamed `condition`; tasks 1.7/1.8 made non-vacuous; 1.12, 1.13 added; `:793` needs no delta; F412 filed (Open Question 3)
- [x] 0.2 R3: a second independent re-derivation, which must not start from R2's notes; record it. `openspec validate pressing-run-names-the-reason-that-held --strict` passes. **Done 2026-09-23 (D-4b)**. It re-derived the ways out of `_do_fire_job` before reading R2, and found an eleventh: a raise after `run` was discarded. It measured three crash cases that D3 cannot see, filed as F413 (Open Question 4). D3's `else → 500` became named statuses, because a concurrent tick's `in_progress` row answers 500 today (measured; task 1.14). The precedence is now a total order. The existing 500/409 pins were checked, and none moves beyond 1.6's two. Task 2.2 gained the function-local import
- [x] 0.3 The operator answers design Open Question 1 (fold F411 in, or keep it a separate finding) and Open Question 4 (fold F413 in, or keep it a separate finding), and, if they want, Open Question 2 (the scope clause's wording); record the answers in `spec-queue/DECISIONS.md`. **Answered 2026-09-24** (design `## Operator review, 2026-09-24`; `spec-queue/tracks/reviews/2026-09-23-changes-2026-09-24.md` §2): approved with defaults. F411, F412 and F413 stay separate findings, and the proposed scope-clause wording stands. Left unticked until the answers are also recorded in `spec-queue/DECISIONS.md`

## 1. Tests first — each must fail on today's code unless marked as a control

Put the route cases in a new `hub/tests/test_pressing_run_names_the_reason.py`. Reuse `_make_loop_job`, `_running_turn` (`test_loop_busy_guard.py`), `_roster` (`test_review_turn.py`), and the `live_scheduler` fixture and `_no_spawn` (`test_a_task_nothing_will_move_holds_nobody.py`). Where a case needs a flow, declare a `SpecDocument` on the loop, as `_guard_case` does.

- [x] 1.1 (D2, F400) A documentless loop: its agent is mid-turn, it holds one pending task, and a second roster agent is free. `POST /jobs/{id}/run` answers 409. The detail contains the busy agent's sentence, `this loop's work goes only to <agent>, the agent its job names`, and `Nothing was started`. It does **not** contain `no other agent is free`. Record that it FAILS today (measured detail: `…and no other agent is free to take this loop's work.`)
- [x] 1.2 (D2) The same loop with nobody else on the roster: same scope clause, same absence. Record that it FAILS today
- [x] 1.3 (D2) A documentless loop with an empty queue, its agent mid-turn, and a sibling free. The detail contains `this loop's queue holds no open task`, and does **not** contain `for another agent to take` or `no other agent is free`. Record that it FAILS today
- [x] 1.4 (D2) Control, PASSES today and must keep passing: a flow with an empty queue, its agent mid-turn. The detail still reads `…this loop's queue holds no open task for another agent to take. Nothing was started.` exactly
- [x] 1.5 (D2) Control, PASSES today and must keep passing: a flow with an open task, its agent mid-turn, nobody else free. The detail reads `…and no other agent is free to take this loop's work. Nothing was started.`, and does not contain `goes only to`
- [x] 1.6 (D2) **Move two existing assertions, deliberately.** In `hub/tests/test_board_agent_role.py`, `test_running_a_loop_whose_agent_is_mid_turn_answers_409_not_500` (`:385`) and `test_running_a_loop_whose_agent_is_held_names_the_hold` (`:420`) stage a documentless loop. Replace `assert "no other agent is free" in detail` with the scope clause, and add `assert "no other agent is free" not in detail`. Keep every other assertion. Add a comment naming this change and design D2. Record that both FAIL after the edit and before group 2
- [x] 1.7 (D3, F373) A flow: its only task is `in_progress` and assigned to the job's agent, which is mid-turn. A second roster agent is free, so the guard passes. The newest `JobRun` is an hour-old `skipped` row reading `loop queue is stalled: 1 still awaiting a prerequisite's approval`, with `requested_by_run_id="run-sentinel"` seeded (an operator's press sends no run identity, so an unseeded row compares `None` with `None`). Run answers 409 with `Every task on this loop's queue is already being worked`. The detail does **not** contain `awaiting a prerequisite`, and the earlier row's `tick_count` and `requested_by_run_id` are unchanged. Record that it FAILS today (measured detail: the earlier row's reason)
- [x] 1.8 (D3) Control, PASSES today and must keep passing: a continuing stall (a documentless loop whose only task is `blocked`, its agent idle), pressed twice. Both answers are 409 with the stall reason. There is one `JobRun` and its `tick_count` is 2. Between the presses, set that row's `requested_by_run_id` to `"run-sentinel"`; after the second press it still reads `"run-sentinel"` (measured by R2 on today's code: it does)
- [x] 1.9 (D3) A counted stall is answered from its row, by the `skipped` arm, before the busy re-ask is reached (the re-ask itself is unconditional below the `skipped` and `failed` arms; design D3, Operator review 2026-09-24). **This cannot be staged with real state**: the firing's own guard runs before the stall walk (in `_do_fire_job`, `scheduler.py:3045` at `8bc926f`), so a loop whose agent is busy never reaches a counted stall. The race is the agent becoming busy between the firing's guard and the route's re-ask. Simulate it the only deterministic way: after 1.8's first press, patch `hub.scheduler._loop_flow_busy_refusal` (the firing reaches it through the `_loop_flow_busy_reason` wrapper, the route directly; both resolve the module attribute at call time) with a side effect that returns `None` on its first call (the firing's) and a `LoopBusyRefusal` on its second (the route's). Press again, and assert the stall reason and the absence of `is already running a turn`. Record that it FAILS today with the equivalent patch on `_loop_flow_busy_reason` (today's gate is `not wrote_row`, so the route re-asks and answers busy)
- [x] 1.9a Control, PASSES today and must keep passing: the same patch around a **first** stall (the press writes the row). The answer is the stall reason. R1 found no existing test for the requirement's scenario *"A firing that recorded its own refusal is answered from that record"* (`grep` of `hub/tests` for the busy guard patched mid-press finds none), so this is its first
- [x] 1.10 (D1) Unit: `_loop_flow_busy_refusal` returns `condition == BUSY_EMPTY_QUEUE`, `BUSY_LOOP_SCOPE` and `BUSY_NO_FREE_AGENT` for the three stagings above, and `None` where the agent is idle. `_loop_flow_busy_reason` returns exactly `refusal.reason` in each case. Record that it FAILS today (the function does not exist)
- [x] 1.11 (D1) Control, PASSES today and must keep passing: `test_the_guard_agrees_with_the_walk`, `test_run_still_answers_409_when_the_holding_is_in_a_live_loop`, `test_run_on_a_busy_agents_empty_loop_names_the_empty_queue` and `test_the_board_does_not_read_the_busy_sentence` (`test_a_task_nothing_will_move_holds_nobody.py`), and all of `test_loop_busy_guard.py`. Run them before group 2 and record the counts
- [x] 1.12 (D3, R2) A firing that raises before its row exists is not answered with an earlier firing's reason. A documentless loop, its agent idle, `session_mode="resume"` with a `last_session_id`, and an hour-old `skipped` row with a seeded `requested_by_run_id`. Patch `hub.scheduler.conversation_for_provider_session` to raise. Run answers **500** `Failed to fire job`; the detail does not contain the earlier row's reason; no row is written; the earlier row's `tick_count` and requester are unchanged. Record that it FAILS today (measured by R2: 409 with the earlier row's stall)
- [x] 1.13 (D3, R2) A firing that writes `failed` after its turn started is answered from that row. A documentless loop, its agent idle, one pending task. Patch `hub.turn_scheduler.schedule_agent` (**not** inside `_no_spawn`, which patches the same name and shadows it) with a coroutine that inserts a `running` `Run` for the agent and then raises. Run answers **500** with the row's `error_summary`, and the detail does not contain `already being worked` or `nothing is wrong`. Record that it FAILS today (measured by R2: 409 *"Every task on this loop's queue is already being worked…"*)
- [x] 1.14 (D3, R3) A row that is not a decline's is not read as one. Use a documentless loop, its agent mid-turn, and one pending task. Patch `JobScheduler._fire_job_internal` with a coroutine that commits an `in_progress` `JobRun` for the job in its own session and returns `False`. That is a cron tick that fired while the press was refused. Run answers **409** with the busy sentence and the scope clause, not 500 `Failed to fire job`. Record that it FAILS today (measured by R3: 500 `Failed to fire job`)

## 2. The fix

- [x] 2.1 (D1) In `hub/hub/scheduler.py`, add `BUSY_EMPTY_QUEUE`, `BUSY_LOOP_SCOPE`, `BUSY_NO_FREE_AGENT`, `LoopBusyRefusal` and `_loop_flow_busy_refusal`, holding today's body of `_loop_flow_busy_reason` with its returns labelled. Reduce `_loop_flow_busy_reason` to the wrapper. Leave its callers (the firing, in `_do_fire_job`, and the board's re-ask in `jobs.py`, `:3045` and `:389` at `8bc926f`) untouched
- [x] 2.2 (D2) In `run_job`, call `_loop_flow_busy_refusal` instead of `_loop_flow_busy_reason`, and choose `why` from `refusal.condition` and `loop.spec_document_id` per design D2's table. Keep the import inside the function, as today's guard import is. Task 1.9's patch reaches the route only that way. Delete the route's own `_loop_has_open_task` call and its import. Rewrite the D8 comment to name design D2 of this change
- [x] 2.3 (D3) In `run_job`, copy `earlier_ticks` as an `int` beside `earlier_run_id`, with a comment naming the identity trap (design Context, last measured row). Compute `counted` and `answered_by_row`. Where `answered_by_row`, answer from the row: `skipped` → 409 with its reason, `failed` → 500 with its reason (design D3, R2). Name both statuses. There is no `else`: any other status falls through, as though nothing was written (design D3, R3; task 1.14). Below those two arms, with no further gate (not `not answered_by_row`, not `not wrote_row`; design D3, Operator review 2026-09-24): the busy re-ask, then `_loop_in_flight_decision`, then 500 `Failed to fire job` (drop its now-dead `wrote_row` read of `latest_run.error_summary`). Comment on that final 500 that it is also reached where an in-flight firing's work finished before the re-ask (design Risks). Leave the requester stamp on `wrote_row`
- [x] 2.4 Run group 1; every row passes. Record the counts inline **Done 2026-09-25 (night iteration 4)**: 13 new tests in `test_pressing_run_names_the_reason.py` pass; against the old route 8 of them and the two moved `test_board_agent_role.py` assertions fail, the controls (1.4, 1.5, 1.8, 1.9a) pass
- [x] 2.5 Run `py -3.11 -m pytest hub/tests/ -q` and record the full-suite count inline, or do not tick. Any assertion that moves other than 1.6's two is named and explained; any unexplained move is a defect — Group 1 (`test_tool_server_pin.py`, `test_agent_trigger.py`, `test_mcp_server_stdio_surface.py`): 72 passed. Full `hub/tests/` at `920d54c` (2026-09-25, claude stripped from PATH): 4829 passed, 86 skipped, 12 failed — all 12 in `test_pty_runner.py`/`test_lifespan_shutdown.py` with `WinptyError: The system cannot find the file specified`, caused by the PATH filter, and those two files pass 32/32 with the normal PATH, so 4841 passed. No assertion moved
- [x] 2.6 `ruff check hub/`, `black --check --target-version py311 hub/hub/ hub/tests/`, clean

## 3. Drive it

- [x] 3.1 On a trial Hub from source (a fresh port and profile, never `:8000`; runbook `.claude/reference/hubs.md`), create a documentless loop whose agent binds `claude-haiku-4-5`, give its queue one task, start a turn for that agent, and press Run through the API while a second agent is idle. Record the 409's detail verbatim. Then do the same for F373's staging (design Context). Record both details and the `JobRun` rows before and after. Disable every job you created before leaving — Driven (trial Hub 8026, profile `drive0925morning`, Haiku, 2026-09-25 morning): documentless loop, author busy, bravo/charlie free → 409 "author is already running a turn, and this loop's work goes only to author, the agent its job names. Nothing was started." F373 staging (flow, only task in_progress under bravo mid-turn, hour-old `skipped` row, charlie free) → 409 "Every task on this loop's queue is already being worked. Nothing was started, and nothing is wrong — the next firing picks up whatever finishes." JobRun rows identical before and after. Busy held by a `Run(status=running)` row after two real-turn races were lost. Jobs disabled
- [x] 3.2 Call MCP `run_job` from an agent turn on the same staging, and record what the tool result says. This is the one surface that displays the answer today (F411) — Driven (trial Hub 8026, profile `drive0925morning`, Haiku, 2026-09-25 morning): MCP `run_job` from a Haiku turn on the F373 staging → is_error, "Hub rejected POST /jobs/job-82f8e8479fe7/run (409): Every task on this loop's queue is already being worked. Nothing was started, and nothing is wrong — the next firing picks up whatever finishes."

## 4. Close it out

- [x] 4.1 Update F400 and F373 in `scripts/drive/FINDINGS.md` with the fix commit and the drive's evidence, and close both. F411 stays open unless Q1 folded it in — Both closed 2026-09-25 with `8c6cb54` and the morning drive; F411 stays open
- [x] 4.2 Sync the delta into `openspec/specs/agent-loops/spec.md` and archive the change

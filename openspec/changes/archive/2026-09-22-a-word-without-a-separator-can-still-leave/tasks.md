## 0. Rounds — no task below may start until R2 and R3 are recorded in design.md's round log

- [x] 0.1 R2: an independent re-derivation of the proposal against `hub/hub/mcp_server.py` (`_words`, `_judge_word`, `_where`, `_judge_path`, `_lex`'s NUL decode) and the `agent-run-sandboxing` spec; record in `design.md` `## Round log` — done 2026-09-21: APPROVE WITH FIXES, R2-1 to R2-11 applied in place (the rule widened to option-joined values, D7; three more existing rows flip)
- [x] 0.2 R3: a second independent re-derivation, which must not start from R2's notes; record it. R3 checks D7 (option-joined values) and D3's quoted-`~` over-refusal in particular — done 2026-09-21: APPROVE WITH FIXES, R3-1 to R3-9 applied in place (the `~` check narrowed to the shapes a shell substitutes; the colon-joined `..` check read in both dialects and for `--name:`; D9's PowerShell residuals named; spec scenarios corrected). Task rows 1.1-1.8 measured: each fails or passes today as stated, and passes after
- [x] 0.3 The operator answers design Open Questions 1, 3, 4 and 5 (file D5, D6 and brace expansion as findings; D7 in scope, with R3's widening; superseding archived D9 for rows R8 and R9 and its two PowerShell residuals; R3's narrowed `~` check); record the answers in `spec-queue/DECISIONS.md` — done 2026-09-21 ~19:50: recorded in DECISIONS.md `2026-09-21 late evening` (f375-tilde, f375-d7, f375-d9, f375-findings)
- [x] 0.4 Before editing `hub/hub/mcp_server.py`, tell the operator: the Hub starts each run's MCP server from this checkout's file, so `:8000`'s next run is judged by the edited rule, committed or not (design R2-7) — done: satisfied by the same approval (DECISIONS f375-8000)

## 1. Tests first — each must fail on today's code

- [x] 1.1 In `hub/tests/test_permission_approver.py`, add parametrised rows over both dialects that are refused as outside, each naming the word: `cp notes.md ..`, `Copy-Item notes.md ..`, `cp notes.md '..'`, `cp notes.md ."".`, `cp notes.md \.\.` (Bash), `cp --target-directory=.. notes.md`, `ls ..`, `cd .. && cat notes.md`; run them on today's code and record that each FAILS (allow). Do not use `.""."`: it is an unbalanced quote bash refuses to parse, and the lexer's run-to-end reading would pass it for a reason unrelated to quote joining (R2-3) — done 2026-09-22: 8 commands, both dialects except `\.\.` (Bash only): 15 rows, all FAIL on today's code (allow)
- [x] 1.2 Add a "judged alike" test: for each dialect, `_decide` on `cp notes.md ..` and on `cp notes.md ../` return the same `allow`; record that it FAILS today. Add a second case with `AW_WORKSPACE_DIR` set to `os.path.abspath(os.sep)` (a filesystem root; `_decide` reads no file, so nothing is created): both commands are allowed. Record that it PASSES today, and that it FAILS against a scratch implementation of rule 4 that returns `_refuse(word, _OUTSIDE)` for `..` instead of calling `_judge_path` (design D2, R2-6); that failure is what makes it evidence — done: `test_dotdot_is_judged_like_dotdot_slash` x2 dialects FAIL today (`..` allowed vs `../` refused). The root-workspace half PASSES today; against a scratch rule 4 that refuses `..` outright the test fails (2 failed) — but that run also trips the first half's already-failing assertion, so the second half alone is not isolated (unverified)
- [x] 1.3 Add rows refused as uncheckable, where the reason contains "cannot be checked" and not "outside your workspace": `cp notes.md ~`, `cp notes.md ~root`, `cp notes.md ~-`, `cp notes.md ~+`, `Copy-Item notes.md ~`, and PowerShell `Copy-Item notes.md -Destination:~`; record that each FAILS today (R3 measured: each allowed today, refused after) — done: 11 rows (5 commands x 2 dialects + PowerShell `-Destination:~`), all FAIL today
- [x] 1.4 Flip `_ANSI_C` row P7 (`cp notes.md $'..\x00x'`) to refused as outside, and rewrite its comment to say this change closes it; record that it FAILS today — done: P7 flipped to refused as outside, comment rewritten; FAILS today
- [x] 1.5 Add rows for option-joined values refused as outside, each naming the whole word (design D7): Bash `cp -t.. notes.md` (`'-t..'`), Bash `cp -xt.. notes.md` (`'-xt..'`, a run of short options), Bash and PowerShell `tar -C.. -xf a.tar` (`'-C..'`), PowerShell `Copy-Item notes.md -Destination:..` (`'-Destination:..'`) and `Copy-Item notes.md -Dest:'..'` (`'-Dest:..'`: the quotes are removed, so the reason names the word as the shell passes it on), Bash and PowerShell `dotnet publish -o:..` (`'-o:..'`) and `dotnet publish --output:..` (`'--output:..'`) (R3-2), Bash `cp -t$'..\x00x' notes.md`; record that each FAILS today — done: 11 rows, all FAIL today
- [x] 1.6 Flip rows R8 (`cd .. && echo hi > stray.txt`) and R9 (`git -C .. status`) of `_TABLE` to `False, _outside("..")`, and add a comment above them saying this change supersedes the archived `a-url-is-not-a-path` D9 for these two rows; record that each FAILS today (R2-1) — done: R8 and R9 flipped with the superseding comment; each FAILS today
- [x] 1.7 Change H10's expected reason from `_UNCHECKED` to `_outside("..")`, and add a row H10b, `HUB_URL=sub ; cat $HUB_URL/x`, `False, _UNCHECKED`, which carries H10's old job of pinning `_read_command`'s `trusted` guard. Record that H10 FAILS today (wrong reason), that H10b PASSES today, and that H10b FAILS when `trusted` is forced to `True` in a scratch run (R2-1) — done: H10 flipped (FAILS today, wrong reason); H10b PASSES today and FAILS with `trusted = True` forced in a scratch run (reverted)
- [x] 1.8 Add negative controls that must stay allowed, and record that each PASSES today. Each one names the wrong implementation it catches: `cp notes.md .` and `cp notes.md ...` (a prefix match on `..`), `git diff a..b` and `git log main..HEAD` (a substring match on `..`), `git log HEAD~1` (a substring match on `~`), `ls -la`, `git log -1` and `cp -t... notes.md` (a glued-option rule that judges more than an exact `..`), Bash `cp -t~ notes.md` (the `~` check applied to a glued short option), PowerShell `Copy-Item notes.md -Destination:sub`, `Get-ChildItem -Filter:*.md` and `Get-ChildItem -Recurse:$true` (a parameter rule that refuses every value), `echo a,b`. R3-1 and R3-2 add: Bash `git commit -m "cuts time by ~30%, ~2x, ~13 files"` and the heredoc form `git commit -m "$(cat <<'EOF'` / `speeds it up by ~30%` / `EOF` / `)"` (a `~` check on every word that begins with `~`), PowerShell `Copy-Item notes.md -Destination:~5` (a `~` check on every colon value; measured: PowerShell writes a file named `~5` inside the workspace), Bash `cp notes.md -x:~` (the `~` check applied to a colon value in bash) — done: 26 rows across both dialects where applicable, all PASS today

## 2. The fix

- [x] 2.1 Change rule 4 of `_judge_word` (`hub/hub/mcp_server.py:1158`) as in design D1 (R3's version): a colon-joined value (`-name:` or `--name:`, both dialects) is checked for `..`, and in PowerShell also for `~`; otherwise the whole word is checked for `~`, then a glued short-option value (both dialects) for `..`. A `~` check is `_TILDE_PREFIX_RE.fullmatch` → `_refuse(word, _UNCHECKED)`; a value whose text before its first NUL is `..` → `_judge_path("..", root, word, argument, continues)`; anything else → `None`. Add `_COLON_OPTION_RE`, `_GLUED_OPTION_RE` and `_TILDE_PREFIX_RE` beside the other patterns (`:953-981`) with a comment citing D3 and D7. Update the rule-4 comment and the block comment above `_SEPARATORS` if it restates the rule — done: matches design D1 verbatim (`hub/hub/mcp_server.py:975-982, 1164-1178`); found already written on disk, uncommitted, at this iteration's start (a prior firing wrote it and exited before verifying/committing) — re-derived and confirmed against design.md before proceeding, not taken on trust. The `_SEPARATORS` block comment does not restate rule 4, so left unchanged
- [x] 2.2 Run group 1's rows; every row passes. Record the count inline — done: `test_permission_approver.py` alone: 283 passed, 1 skipped (240 passed + 43 formerly-failing rows now pass; 0 failed)
- [x] 2.3 Run the whole of `hub/tests/test_permission_approver.py` plus `hub/tests/test_workspace_writes.py` and `hub/tests/test_codex_posture_ordering.py`; record the counts inline. Expected changes against today are exactly P7, R8, R9 and H10 (R2 measured this with the rule patched in: 4 failed, 380 passed across the eight test files that reach the judge; R3 re-measured R3's rule from a patched copy of the whole package, which also runs the tests that spawn `mcp_server.py`: the same four across ten files and 453 tests); any other change is a defect — done: measured directly (not taken from the design doc's scratch-copy numbers). All three files together, code unfixed (`git stash` on `mcp_server.py` only): **43 failed, 293 passed, 1 skipped** (337 total). Same three files, fix restored: **336 passed, 1 skipped** (337 total). Exactly the 43 rows flip from fail to pass (the 4 flipped table rows P7/R8/R9/H10 plus group 1's new rows, all designed to fail today); 293 stay passed, 0 regressions, 0 unaccounted changes
- [x] 2.4 Run `py -3.11 -m pytest hub/tests/ -q` (F392 rule 1); record the full-suite count inline, or do not tick — done: **4544 passed, 86 skipped**, 0 failed, in 39m12s. A `RuntimeError: Event loop is closed` traceback appears in the teardown warnings after the summary line; it is an aiosqlite background-thread warning at interpreter shutdown, not a test failure (exit code 0, 0 failed)
- [x] 2.5 Run `ruff check src/ hub/ tests/` and `black --check --target-version py311 src/ hub/hub/ hub/tests/ tests/`; both clean — done: ruff clean (0 errors); black clean, 0 files would reformat

## 3. Real shells — the evidence that the rule matches what the shell does

- [x] 3.1 In a scratch directory (not the repo root, not `testbed/` leftovers), repeat the design's real-landing rows in Git Bash and PowerShell: `cp notes.md ..`, `$'..\x00x'`, `~`, `cp -t.. notes.md`, `cp -xt.. notes.md`, `Copy-Item notes.md -Destination:..`; confirm each lands outside, which justifies its refusal. Record the shell versions — done: scratch dirs `~/f375-scratch-bash/workspace` (Git Bash) and `$env:TEMP/f375-scratch-ps` (PowerShell), both removed after. Git Bash `GNU bash, version 5.2.37(1)-release (x86_64-pc-msys)`: `cp notes.md ..` landed at `../notes.md`; `cp notes.md $'..\x00x'` also landed at `../notes.md` (bash truncates the ANSI-C word at its embedded NUL before exec, so the shell-visible word is `..`); `cp notes.md ~` landed at `~/notes.md` (home dir); `cp -t.. notes.md` and `cp -xt.. notes.md` both landed at `../notes.md`. PowerShell `5.1.26100.9444`: `Copy-Item notes.md -Destination:..` landed one level above the scratch dir (confirmed present, then removed) — outside the workspace. All six rows confirmed landing outside; each refusal is justified.
- [x] 3.2 Confirm the negative controls `...`, `'.. '`, `C:` and `cp -t~ notes.md` do not land outside in Git Bash, that `echo ~30% ~2x ~13` prints them literally there, and that PowerShell's `Copy-Item notes.md -Destination:~5` writes a file named `~5` inside the workspace; record the result — done: in Git Bash, `cp notes.md ...` and `cp notes.md '.. '` created files named literally `...` and `.. ` inside the workspace (not outside); `C:` as a bare word is a no-op (bash has no such special-file/drive semantics, cwd unchanged); `cp -t~ notes.md` errored `cp: failed to access '~': No such file or directory` (bash does not glue-expand `~` after `-t`, so nothing lands in home) — confirms the negative control holds by construction, not by writing anywhere outside; `echo ~30% ~2x ~13` printed `~30% ~2x ~13` literally (no tilde expansion, since none of the three forms is a bare `~`, `~user`, `~+` or `~-`). In PowerShell, `Copy-Item notes.md -Destination:~5` created a file literally named `~5` inside the scratch workspace (confirmed via `Get-ChildItem`), not an expansion to any home path.

## 4. Close

- [x] 4.1 Update F375's Status line in `scripts/drive/FINDINGS.md` to `fixed <sha>`, and regenerate the backlog with `py -3.11 scripts/backlog_page.py` — done: Status line reads `fixed a5e5a49`; backlog regenerated, F375 no longer open (open findings 217→215, fixed 166→167)
- [x] 4.2 `openspec validate a-word-without-a-separator-can-still-leave --strict` passes; then archive with `openspec-archive-change` — done: valid, archived this commit

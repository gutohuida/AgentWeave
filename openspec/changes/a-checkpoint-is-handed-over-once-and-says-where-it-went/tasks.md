## 0. Rounds — no task below may start until R2 and R3 are recorded in design.md's round log

- [x] 0.1 R1: explore and propose (2026-09-24, bundle B8). F293 and F294 re-measured open on `404c7d5`
- [x] 0.2 R2: an independent re-derivation against `hub/hub/checkpoint_cutover.py`, `hub/hub/api/v1/checkpoints.py`, `hub/hub/checkpoint_trigger.py`, `hub/hub/conversations.py` (`archivable`, `unarchive`), `hub/hub/db/models.py` (`Checkpoint`) and `hub/hub/db/engine.py`. In particular: re-derive design D3's claim that pysqlite's legacy transaction mode serialises the two writers (it rests on reading, not measurement); check that no third caller of `cut_over` exists; and re-run the F293/F294 reproduction (copy task 1.1 and 1.2 into a `hub/tests/test_zz_*.py`, run with `-s`, delete the copy). **Done 2026-09-24**: F293 → 2 successors, F294 → 2 successors; D3 measured in both journal modes (design D3, *Measured in R2*)
- [x] 0.3 R3: a second independent re-derivation, not starting from R2's notes. `openspec validate a-checkpoint-is-handed-over-once-and-says-where-it-went --strict` passes. **Done 2026-09-24**: two callers, no MCP/agent path. Added D6 and task 2.7, split 1.8 into 1.8 (sequential, D6) and 1.12 (race, the rollback path), explicit flush and IntegrityError re-raise in 2.3, and recorded a fourth `0106`
- [x] 0.3a Operator review (2026-09-24, Opus adversarial review): D2 approved; D6 amended to decline only the billed steps (the final warning still fires). Fixes applied to 1.2, 1.4, 1.8, 1.11, 2.3, 2.7, 3.2, 3.5; task 1.13 and the `conversation-checkpoint` MODIFIED delta added (design, *Operator review*)
- [ ] 0.4 The operator approves (APPROVALS.md), having seen design D2 (per conversation, not only per checkpoint), D6 (the trigger declines a handed-over conversation) and Open Questions 0 and 1

## 1. Tests first — in `hub/tests/test_checkpoint_cutover.py`; each must fail on today's code unless marked as a control

- [x] 1.1 (F293) `test_reopening_the_predecessor_does_not_re_arm_its_checkpoint`: `cut_over`, `unarchive(conversation)` + commit, `cut_over` again with the same checkpoint. The second raises `CutoverRefusedError` whose text contains the first successor's id. Assert on rows: exactly one `origin="handoff"` conversation and one `origin_type="checkpoint"` entry. Record that it FAILS today (R1 measured two successors)
- [x] 1.2 (F294) `test_two_simultaneous_presses_mint_one_successor`: two `async_session_factory()` sessions each load the conversation (`get_conversation_by_id`) and checkpoint (`get_checkpoint_by_id`), then `cut_over` under `asyncio.gather(..., return_exceptions=True)`. **The barrier is inside `cut_over`, not before it** (operator review, 2026-09-24): patch `hub.checkpoint_cutover.archivable` with a wrapper that awaits one shared `asyncio.Barrier(2)` and then returns the real `archivable(db, conversation)`. `archivable` runs at `checkpoint_cutover.py:105`, after every pre-check, so both presses have passed task 2.2's pre-check query before either writes. A barrier before the call let the second press's pre-check read the first's commit and refuse sequentially, so the compare-and-set and its rollback never ran and the test was green with or without them. Also wrap each session's `rollback` in a counting spy and assert the refused session rolled back exactly once (it went through design D3 step 3, not a pre-check). Assert exactly one result is a `(Conversation, str)` and exactly one is a `CutoverRefusedError` naming that successor; **no other exception type** (an `OperationalError` here falsifies design D3's locking argument, so stop and report). One successor, one entry. Record that it FAILS today (R1 measured two successors). This runs under the suite's `journal_mode=WAL` (`conftest.py:112-115`), not production's rollback journal; task 1.11 covers that
- [x] 1.3 (D2) `test_a_second_checkpoint_cannot_hand_the_conversation_over_again`: cut over with C1, unarchive, `_ready_checkpoint` again (C2), `cut_over` with C2. Refused, and the text names the first successor and C1. One successor. Record that it FAILS today
- [x] 1.4 (D2, D3 backstop) `test_two_checkpoints_raced_mint_one_successor`: as 1.2 (the barrier inside the patched `archivable`, and the rollback spy: the loser rolled back through design D3 step 4), but one session cuts over C1 and the other C2 (both ready, same conversation). Exactly one success and one refusal. Record that it FAILS today. **Mutation check at IMPL**: with the partial unique index removed from both model and migration, this test fails and 1.2 still passes. With the compare-and-set replaced by a plain assignment, 1.2 fails and this one still passes. Record both
- [x] 1.5 (D1) `test_the_checkpoint_records_where_it_went`: after `cut_over`, a **fresh** session's `get_checkpoint_by_id` reads `cut_over_to_conversation_id == successor.id`, and a checkpoint that was not cut over reads `None`. Record that it FAILS today (no attribute)
- [x] 1.6 Control, PASSES today and must keep passing: `test_a_chain_is_still_handed_over_hop_by_hop`: cut over P→S1, deliver S1's checkpoint entry (`deliver_entries_with_run`, as existing tests do) so `archivable` passes, take a checkpoint on S1, cut over S1→S2. Both succeed, the titles hold no `Continued: Continued:`, and one `lineage_id` runs throughout. The index is keyed on `conversation_id`, so a chain must not trip it
- [x] 1.7 (D4) Control, PASSES today and must keep passing: `test_a_conversation_archived_by_hand_is_not_cut_over_either` (existing, `:399-420`) still matches `unarchive it first`. **Extend** it: after `unarchive` + commit, `cut_over` succeeds with one successor. The extension PASSES today too. It pins that the hand-archive advice is true
- [ ] 1.8 (D6, trigger path, sequential) `test_the_trigger_does_not_spend_a_checkpoint_on_a_conversation_already_handed_over`: under `automatic` (`_configured_project`'s defaults, which set `checkpoint_runner_id="runner-1"`, `test_checkpoint_cutover.py:452`; do not override it, or `_resolve_runner` returns early at `checkpoint_trigger.py:148-149, 293-300` and the test passes for the wrong reason), cut the conversation over with C1, `unarchive` + commit, then give it a new run and call `consider(...)` past the threshold, with `hub.worker.resolve_executable` patched to the identity (`monkeypatch.setattr("hub.worker.resolve_executable", lambda cmd: cmd)`, as `:530` does, so a CI runner without `claude` does not turn the spawn into a `FileNotFoundError` and an `unwritten` checkpoint, `pty_runner.py:127-128`, `worker.py:355`) and `subprocess.run` patched to **explode** (pattern: `test_under_offered_nothing_is_made_and_nothing_is_cut_over`, `:559-576`). **The load-bearing assertion is no new checkpoint row** (only C1 exists). Also assert: `consider` returns `None`, no broadcast, and still one successor. Repeat under `offered` with the warning `None`, at a notes-point reading (72%) and a threshold reading (85%): no notes entry, no `checkpoint_due`, `checkpoint_warning` still `None`. Record that it FAILS today: under `automatic` the exploding spawn raises out of `consider` (or, where a spawn failure is swallowed, an `unwritten` C2 row is written). It never mints a second successor, because an exploded CLI never makes a `ready` checkpoint (operator review corrected R3's note). Under `offered` it fails on the notes entry and the `due` warning. (R3: R2's version of this test reached the refusal sequentially. Task 2.2's pre-check refuses before any write, so there is no rollback and no expiry, and that version passed with or without task 2.6. The rollback path is task 1.12)
- [x] 1.9 **Move one existing assertion, deliberately.** `test_a_spent_checkpoint_cannot_be_cut_over_a_second_time` (`:354-396`) matches `already archived`. Change it to match the successor's id, and add a comment naming this change and design D4. Its row assertions stay. Record that it FAILS after the edit and before group 2
- [ ] 1.10 Route: in the same file (or `hub/tests/test_checkpoint_access.py` if it holds the route fixtures), `POST …/checkpoints/{id}/cutover` → 200; `POST …/conversations/{c}/unarchive` → 200; the cutover again → **409** whose `detail` contains the successor id. Then `GET …/conversations/{c}/checkpoints` has a row, **picked by `id`, not by position**, carrying `cut_over_to_conversation_id == successor`. Record that it FAILS today (200 on the third call)
- [ ] 1.11 (D3, production journal mode) `test_simultaneous_presses_serialise_under_sqlites_default_journal`: run 1.2's and 1.4's races (the barrier inside the patched `archivable`, and the rollback spy, as in 1.2) on a **separate** engine built with `engine.py:36-40`'s arguments on a `tmp_path` file (`create_all`), so the suite's `do_connect`/`connect` listeners in `conftest.py` do not apply. Assert `PRAGMA journal_mode` reads `delete`, then the same one-success/one-refusal/no-other-exception outcome for both races. R2 measured 10/10 clean with a stand-in for the compare-and-set. Record that it FAILS today (two successors)

- [ ] 1.12 (trigger path, the rollback) `test_an_automatic_cutover_that_loses_the_race_keeps_its_checkpoint_and_returns_its_id`: under `automatic`, with the CLI patched as `test_crossing_the_threshold_under_automatic_generates_and_cuts_over` does (`:517-556`), and C1 a ready checkpoint of the same conversation. Patch `hub.checkpoint_cutover.archivable` with a wrapper. On its first call it opens a **separate** `async_session_factory()` session, runs `cut_over` of C1 there, commits, and then returns `None`. It sets its once-only flag **before** the nested `cut_over`, so that call's own `archivable` reaches the real function. So the trigger's pre-checks have passed, and its claim or index must refuse. Assert: `consider(...)` **returns C2's id without raising**; the broadcast is `checkpoint_ready` with `cutover_refused` naming C1's successor; C2's row exists; one successor and one checkpoint entry. Record that it FAILS today (two successors). **Mutation at IMPL**: revert task 2.6 (return `checkpoint.id`) and this test fails with `MissingGreenlet`. Record it. The wrapper reaches the real concurrent route D2 names, an operator pressing C1 while the trigger cuts over C2, without a barrier

- [ ] 1.13 (D6 as amended, control) `test_a_handed_over_conversation_still_gets_its_final_warning`: under `offered`, cut the conversation over with C1, `unarchive` + commit, set `checkpoint_warning = "dismissed"` + commit, give it a new run, patch `subprocess.run` to explode, and call `consider(..., percent=99.0)`. Assert: returns `None`; `checkpoint_warning == "final"`; one `checkpoint_due` broadcast with `final: True`; no checkpoint row beyond C1; still one successor. Then, on a second conversation of the same shape (handed over, reopened, dismissed), a reading at 85%: no notes entry, no `due`, no new row. PASSES today (the backstop at `checkpoint_trigger.py:192-234` runs before anything billed). **Mutation at IMPL**: move task 2.7's decline up to directly after `:187-190` (R3's placement) and this test fails. Record it

## 2. The fix

- [x] 2.1 `Checkpoint.cut_over_to_conversation_id` and `ix_checkpoints_one_handover_per_conversation` in `hub/hub/db/models.py` (design, migration checklist item 1), with a comment naming F293/F294 and this change
- [x] 2.2 In `cut_over`: before the lifecycle guard, one query for the checkpoint of `predecessor.id` whose `cut_over_to_conversation_id IS NOT NULL`; refuse with D4's first or second text. Then the lifecycle guard with D4's third text
- [x] 2.3 In `cut_over`: capture `checkpoint.id`, `predecessor.id` and `checkpoint.conversation_id` as plain strings before any write (design D3 step 0). The rollback expires every instance, and reading one afterwards raises `MissingGreenlet`, which the route would answer as a 500. Then, after the adds and `archive(predecessor)`, the compare-and-set `update(Checkpoint).where(Checkpoint.id == checkpoint_id, Checkpoint.cut_over_to_conversation_id.is_(None)).values(cut_over_to_conversation_id=successor_id).execution_options(synchronize_session=False)` (design D3 steps 2–5). Require `result.rowcount == 1`, or use `.returning(Checkpoint.id)` and treat `None` as the miss. `synchronize_session=False` because the default `"auto"` evaluates the predicate against the loser's stale in-memory row and writes the successor id into it (operator review). Miss → rollback, re-read, refuse. `IntegrityError` → rollback, re-read, refuse. Add a comment at the rollback: both callers arrive with nothing pending (design *Context*). R3: call `await db.flush()` explicitly before the compare-and-set, rather than relying on the ORM update's autoflush. If step 4's re-read finds no handed-over checkpoint, re-raise the original `IntegrityError`; never build a refusal naming `None` (design D3, *R3 additions*)
- [x] 2.4 `CheckpointSummary.cut_over_to_conversation_id` in `hub/hub/api/v1/checkpoints.py`, set in `.of`
- [x] 2.5 Update `cut_over`'s docstring (`:71-82`). It says the lifecycle guard is *"what makes a cutover happen at most once per conversation"*, and that is no longer true
- [x] 2.6 In `checkpoint_trigger.consider`'s `except CutoverRefusedError` branch (`:329-332`), return an id captured before `cut_over` (`payload["checkpoint_id"]` is already one), not `checkpoint.id`. Task 1.12 pins it (R3: not 1.8, which never rolls back)
- [ ] 2.7 (D6, as amended by the operator 2026-09-24) In `checkpoint_trigger.consider`, **after** the dismissed/final backstop branch (`:192-234`) and **before** `should_request_notes` (`:236`), one query for a checkpoint of this conversation with `cut_over_to_conversation_id IS NOT NULL`. If one exists, `_declined(conversation_id, "already handed over to {successor}; its line continues there")` and return `None`, ahead of the notes, `due` warning and generation branches. **Not** directly after the lifecycle decline (`:187-190`): that is above the backstop and would silence the free final warning (design D6, operator's amendment). Tasks 1.8 and 1.13 pin it. `worker-spend-counts-against-the-budget` also edits the automatic branch below this point. That is a textual merge only

## 3. The migration

- [x] 3.1 `hub/hub/migrations/versions/0106_checkpoint_cut_over_to.py` (renumber to the next free number at IMPL: `agents-no-longer-register-themselves` and `worker-spend-counts-against-the-budget` also name `0106`, and so does `a-footprint-names-the-line-of-work-its-commit-is-on` (R3)): guarded add-column, backfill, guarded create-index; downgrade drops both. The docstring says it reaches the operator's live database and that the backfill wrote zero rows there when measured (2026-09-24)
- [x] 3.2 Backfill per design D5: preamble-prefixed `origin_type='checkpoint'` entries only, walked **one row at a time** in `InboundQueueEntry.sequence` order (not one bulk `UPDATE`). Per entry take the **first** `^# Checkpoint (ckpt-\S+)$` match only. Skip (and count) an id that matches no `checkpoints` row. Set it only if that checkpoint is still NULL and no sibling (same `checkpoints.conversation_id`) has been set by an earlier row; first handover wins. Log the set/skipped counts
- [x] 3.3 Bump `HEAD_REVISION` (`hub/tests/test_migrations.py:40`) and `hub/tests/test_project_persistence.py:227` to `0106`
- [x] 3.4 `test_migration_0106_backfills_the_successor_from_the_delivered_checkpoint`: build to `0105`, seed a project, a conversation, a ready checkpoint, a successor, a delivered cutover entry (preamble + `# Checkpoint ckpt-a`), and a **notes-request** entry on the same conversation. Upgrade to `0106`: `ckpt-a` points at the successor; the notes entry changed nothing
- [x] 3.5 `test_migration_0106_keeps_the_first_handover_when_the_database_already_holds_a_fork`: seed two checkpoints of one conversation, each with a delivered entry (the F293-era fork), plus a second delivery of the first checkpoint (the F126-era duplicate). The upgrade **succeeds**, only the earliest entry's checkpoint is set, and the index exists. **Reverse-order case** (operator review): C1's entry, then the F126-era duplicate delivery of C1 to a second successor, then C2's entry. C1 points at the **first** successor (not the duplicate's), and C2 stays NULL. Also seed one preamble entry naming a `ckpt-` id with no row (skipped and counted), and one whose body quotes a second `# Checkpoint ckpt-…` line after its first (only the first id is used)
- [x] 3.6 `test_migration_0106_is_guarded_when_checkpoints_does_not_exist` (pattern: `test_migration_0105_is_guarded_when_agents_does_not_exist`, `:3619`) and a downgrade test (column and index gone)
- [ ] 3.7 Run `test_an_empty_database_migrated_alone_gets_the_schema_init_db_builds` (F329 parity). It must pass. A mismatch between the model's index and the migration's shows up here

## 4. Verify

- [ ] 4.1 `py -3.11 -m pytest hub/tests/test_checkpoint_cutover.py hub/tests/test_migrations.py hub/tests/test_project_persistence.py hub/tests/test_checkpoint_access.py -q` with `claude` stripped from PATH (DEAD-ENDS). Then the full `hub/tests/`; record counts
- [ ] 4.2 The CLAUDE.md lint block (ruff, black, mypy)
- [ ] 4.3 The mutation checks of 1.4 (repeat them under 1.11) and of 1.12, recorded in the round log
- [ ] 4.4 Re-drive `scripts/drive/t_d2_cutover_guard.py` on a **trial** Hub (`:8010`, from source, never `:8000`). Probe 1's third press is now 409 naming the successor, and probe 2 gives one 200 and one 409. Adapt the harness's `unarchive it first` check to the new spent-checkpoint text in the same commit, and record the verdict counts verbatim
- [ ] 4.5 Footnote F293 and F294 in `scripts/drive/FINDINGS.md` with the commit and the drive; regenerate the backlog

## 0. Rounds and decision

- [x] 0.0 R1: interactive explore with the operator (2026-09-25; decisions marked *(operator)* in design.md) and this proposal
- [x] 0.1 R2: independent re-derivation against `schemas/jobs.py`, `api/v1/jobs.py` (PATCH, `LoopSummary`, `_pending_loop_edit`), `scheduler.py` (`_stage_pending_loop_edit`, busy guard, `decide_firing`), `LoopTab.tsx`, `SpecPhaseBar.tsx`, `api/jobs.ts`, `api/loops.ts`. **Done 2026-09-25**: design "Round 2" lists ten corrections. The main ones are D2a (staging after the busy guard could not apply a switch away from a held agent), agent edits being operator-only (**operator to confirm**), the loop lookup for an `agent`-only PATCH, the revert case, task 3.1 answered, loop-cache freshness after a create, and migration `0108`
- [x] 0.1a Operator: confirm design D2's operator-only rule for `agent` (403 from an agent's run), or say agents may re-point jobs. **Decided 2026-09-25: operator only** (`DECISIONS.md` `R1-agent-operator-only`)
- [x] 0.2 R3: second independent re-derivation; `openspec validate a-flow-is-configured-from-its-own-tab --strict` passes. **Done 2026-09-25**: design "Round 3" lists ten corrections. The main ones: the firing-active helper lives in `scheduler.py` (import cycle); an applied agent change must not resume the old agent's session; the Spec-destination link must navigate without the document, or the document tab is in front; the `except` path is fixed here (unbound `acting_agent`, no `loop_edit_applied`); `loop_edit_applied` invalidates jobs; test 1.4's mutation corrected
- [ ] 0.3 Build only after B10's `a-loop-is-stopped-archived-and-delegated-from-its-own-tab` is in the tree (design, Risks); stop and log if it is not

## 1. Tests first — `hub/tests/test_a_flow_is_configured_from_its_own_tab.py`

- [ ] 1.1 PATCH `{"agent": "B"}`, with **no other field**, on a loop whose job names A: 200, `job.agent` still `A`, `loop.pending_agent == "B"`, `pending_edit.agent == "B"`, a `loop_edit_staged` event whose `changes` include `agent`. FAILS today (422). The body carries no loop field, so it fails too on a version that forgets to look the loop up when `loop_fields_supplied` is false (design D2)
- [ ] 1.2 The next firing applies it. Drive `_do_fire_job` (not the helper alone) with no firing active: afterwards `job.agent == "B"`, `pending_agent` is null, and `loop_edit_applied` carries `agent`. FAILS today
- [ ] 1.2a A loop whose job is in resume mode (seed `session_mode="resume"` directly; the route refuses to set it on a loop), with `last_session_id` set and a conversation of A's holding that provider session, and `pending_agent="B"`. One firing: the queued entry is for B, in a conversation that is not A's, with `session_id` null. The `JobRun`'s `session_id` is null, and so is `job.last_session_id`. Mutation: leave the conversation looked up at `:3056` in place, and the entry lands in A's thread with A's session (design D2, R3)
- [ ] 1.3 On a loop that declares **no document**: while `pending_agent` is set and A's firing is active (a `JobRun` `in_progress` whose conversation has A's `Run` `running`), `POST /jobs/{id}/run` answers 409 busy and `pending_agent` is still `"B"`. Mutation: apply the agent immediately in the PATCH, or ungate D2a's early application, and watch this test fail. Do not use a flow with a free agent here: D12 lets that firing proceed whoever the job names
- [ ] 1.3a D2a, the case R1's version could not fire: a documentless loop naming A, **no** firing active, A held (`provider_hold` seeded as `hub/tests/test_a_held_agent_is_busy.py` does), `pending_agent="B"`. Give the loop one open, unassigned task and a `stop_at` in the future, so the firing neither stops nor proceeds empty. One tick: B is applied before the guard, the firing is not refused as busy, and it is queued for B. FAILS on R1's version (refused every tick until A's reset)
- [ ] 1.3b D2a, the busy-refused path: A running some other conversation's turn (not this loop's firing), `pending_agent="B"`, B also running. The tick is refused busy **for B**; `job.agent == "B"`; `loop_edit_applied` was emitted once despite the early return. Mutation: drop the emit on the busy path
- [ ] 1.3c D2a's `except` path (R3): stage an edit, and monkeypatch `_loop_stop_reason` to raise. One tick: no `UnboundLocalError` escapes `_do_fire_job`, the `JobRun` is `failed`, the applied edit is committed, and `loop_edit_applied` was emitted once. FAILS today (the handler reads `acting_agent` before it is bound), and fails on a version that forgets the emit
- [ ] 1.4 Unknown and archived agents, with a roster seeded (the check passes silently on an empty roster): 400 with `_check_agent_exists`'s sentence, nothing staged. Send `{"agent": "nobody", "name": "renamed"}` and assert the name is unchanged too. `update_job` commits once and `get_session` rolls back on a raise, so this fails only if the check is moved after the commit. That is its mutation, and ordering within the transaction is not observable here (R3)
- [ ] 1.4a A plain job PATCHed with `{"agent": "B", "purpose": "p"}` becomes a loop and names B at once, with nothing staged. On an existing loop, `{"agent": "B", "purpose": "p"}` makes exactly one `loop_edit_staged`, whose `changes` hold both (design D2, R3)
- [ ] 1.5 Plain resume-mode job with `last_session_id` set: PATCH `{"agent": "B"}` applies at once and clears `last_session_id`. FAILS today (422)
- [ ] 1.6 Tasks assigned to A and inbound entries queued for A are unchanged after the agent change is applied; `Checkpoint.loop_id` rows unchanged
- [ ] 1.7 `GET /loops` and `GET /loops/{id}` carry `spec_document_id` (a flow: its document; a plain loop: null). FAILS today. Same file: archive a flow, create a second on the same document; the default `GET /loops` returns only the second, oldest-first order holds, and `include_archived=true` returns both
- [ ] 1.8 Migration test: upgrade to `0108` adds nullable `loops.pending_agent` with rows preserved; downgrade removes it. Seed from a copy of `:8000`'s real `loops` DDL (read `mode=ro`)
- [ ] 1.9 Operator-only: the agent-plane `PATCH /agent-actions/jobs/{id}` with `{"agent": "B"}` from a running run of B, `allow_agent_jobs` on, answers 403 with the sentence in design D2; `job.agent` and `pending_agent` unchanged. The same route with `{"name": "x"}` still answers 200 (control). FAILS today (422, not 403)

## 2. Backend

- [ ] 2.1 `JobUpdate.agent` (`max_length=64`). In `update_job`, at the top beside the `work_needs_evidence` refusal: 403 when agent headers are present, then `_check_agent_exists`. Look the loop up for `agent` independently of `loop_fields_supplied`, and before that branch can create one; a loop created by this PATCH takes the agent at once. Staged on a loop that already existed (`pending_agent`, `pending_edit_actor/at`, event `changes`); immediate on a plain job, with `last_session_id = None` when the name changes
- [ ] 2.2 `Loop.pending_agent` (`String(64)`, nullable) + migration `0108_loop_pending_agent.py` modelled on `0080` (guarded `op.add_column`, `op.drop_column` on downgrade); bump `HEAD_REVISION` in `hub/tests/test_migrations.py` and the assertion in `hub/tests/test_project_persistence.py`; update the `pending_edit_at` comment ("one of the four")
- [ ] 2.3 `_stage_pending_loop_edit(loop, job)` applies `pending_agent` to the job and clears `job.last_session_id` when it does; `loop_edit_applied` carries it; `_pending_loop_edit` reports it. In `_do_fire_job`, a payload carrying `agent` drops the `conversation`/`resume_session_id` looked up for the old agent, and `run.session_id` on the late path
- [ ] 2.3a D2a: factor `_batch_loop_summaries`' firing-active query into `scheduler._jobs_with_active_firing(session, job_ids)`. It goes in `scheduler.py` because `jobs.py` imports `scheduler` at module level. In `_do_fire_job`, apply a staged edit before the busy guard when no firing of the job is active; `:3179` keeps any payload already made; emit `loop_edit_applied` after the busy-refusal and skip commits too. Bind `pending_edit_payload = None` and `acting_agent = job.agent` before the first statement that can raise, and have the `except` handler emit `loop_edit_applied` after its commit
- [ ] 2.4 `LoopSummary.spec_document_id` in `_batch_loop_summaries` and the detail view

## 3. UI

- [ ] 3.1 Navigation (R2 found it; design D4). Reuse `usePanelTabsStore.getState().openTab(projectId, loopTabId(loop.id))`, as `ConversationRow.tsx:290` and `LoopFiringGroup.tsx:94` do. Thread `onOpenLoop?: (loop) => void` through `SpecDocumentPanel` → `SpecPhaseBar` like `onOpenTasks`. `ConversationView.tsx:384` passes `openTab` alone. `SpecPage` gets the prop, and `App.tsx:428-446` passes `openTab` followed by `navigateTo(agentDestination(projectId, loop.agent, null, null))`: no document, or `ConversationView`'s mount effect (`:220-233`) makes the document's tab the active one, in front of the loop (R3). With no handler, or an empty `agent` on the Spec destination, render the label as text
- [ ] 3.2 `api/loops.ts`: `useUpdateLoopSettings(jobId)`, `useCreateFlow()` (both invalidate loops and jobs **on settle**) and `useDocumentFlow(documentId)` (over `useLoops()`). `LoopSummary.spec_document_id` and `LoopPendingEdit.agent` in the TS types; `JobCreate.spec_document_id` in `api/jobs.ts`. `useSSE.ts`: `job_created`/`job_updated`/`job_deleted` invalidate `['project', pid, 'loops']`, and `loop_edit_applied` also invalidates `['project', pid, 'jobs']`
- [ ] 3.3 `LoopTab.tsx` Settings section (design D1). Read view including default agent, message and cadence (`useJob(loop.job_id)`). Edit/Save, where each input opens on the staged value if there is one and a field is sent when it differs from what it opened on. Read-only when ended or archived. The "creator control follows the agent" note when `control == "creator"`. The route's `detail` on a refusal. A "Default agent" row in `stagedFields`. Add `useUpdateLoopSettings` to the `@/api/loops` mock factories in `loopTab.test.tsx` and `loopPendingEdit.test.tsx` (B10 task 2.5 already adds its own there), and mock `@/api/jobs` and `@/api/agents`
- [ ] 3.4 `StartFlowDialog.tsx` (design D5: host-agnostic, `useDialogFocus`, refuses to submit with no stop condition) and `SpecPhaseBar.tsx` Flow link / Start a flow… (design D4), `change-spec` documents at `approved` only. Add `@/api/loops` to `specPhaseBar.test.tsx`'s mocks
- [ ] 3.5 Vitest. (a) Settings sends only changed fields: assert on the request body. (b) Revert: with `pending_edit.agent: 'B'` and `agent: 'A'`, choosing A sends `{agent: 'A'}`. (c) An agent edit shows as pending, not as in force. (d) The dialog's POST body has `spec_document_id`, `stop_when_queue_empties: true` and `purpose: ''`, and no `work_needs_evidence`; with both stops cleared, nothing is sent. (e) The phase bar, fed a `GET /loops`-shaped list (no archived rows, oldest first, a documentless loop before the flow), shows **Flow: <label>** when a loop declares the document. It shows the same for an ended, unarchived one, and **Start a flow…** for an empty list. Clicking the link calls `onOpenLoop` with that loop. (e2) The Spec destination's handler, rendered through `App`, calls `openTab` with the loop's tab and navigates to the agent with `document: null`. Mutation: pass the document path, and the assertion on the destination fails. (f) After a successful create, `invalidateQueries` was called with the loops key
- [ ] 3.6 `npm run lint`, `npm test`; `make ui`; commit `hub/ui/src` and `hub/hub/static/ui` together, in the same commit as section 2 (the bundle needs the new fields)

## 4. Verify

- [ ] 4.1 Full `hub/tests/` with `claude` stripped from PATH; the CLAUDE.md lint block
- [ ] 4.2 Drive on a throwaway Hub (fresh profile, free port, never `:8000`/`:8010`, Haiku on any real turn, no job left enabled): start a flow from an approved document **on the Spec destination and from the conversation panel**, and see the document name the flow at once in both. Change its cadence and agent from the tab, see the agent pending, let one firing apply it, see it in force
- [ ] 4.3 Tell the operator about the `:8000` skew (design, Risks) before the bundle commit; sync deltas and archive

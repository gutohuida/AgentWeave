## 0. Rounds. No task below may start until every round is recorded in design.md's Round log

- [x] 0.1 R2: an independent re-derivation of the proposal against `format_turn_prompt`, `turn_scheduler`'s call to `trigger_agent_directly`, `_trigger_agent_directly` (notices and prompt assembly), `_batch_delivery_text`, `scheduler` (`_compose_loop_briefing` and both job firing sites), `run_divergence._queue_response` and `checkpoint_cutover.cut_over`, and the `agent-run-sandboxing` spec. **Done 2026-09-23 (R2-1 to R2-9 in the Round log). The tokeniser rerun gave 41/41 rows match (exit 0, fresh marker). Scope widened to the one-shot workers (D7), and the D6 trigger moved.**
- [x] 0.2 R3: a second independent re-derivation. **Done 2026-09-23 (R3-1 to R3-4 in the Round log): added D8 (board Start work), replaced D7's template rule with a code-side inverse, made the probe grading symmetric. Measured: `scripts/drive/d4_0923_worker_json_escape.py`, 9 Haiku turns, 0 unparseable. validate --strict: valid.**
- [x] 0.3 The operator answers Q1 to Q3. **Done 2026-09-24 (`spec-queue/DECISIONS.md` `0923-changes`): all three defaults accepted.**
- [x] 0.4 R4: revise after the operator's review (`spec-queue/tracks/reviews/2026-09-23-changes-2026-09-24.md` §1). **Done 2026-09-24 (R4-1 to R4-10 in the Round log). D4 widened to option answers, D9 and D10 added, the requirement's Q1 contradiction fixed, and MODIFIED deltas added for `agent-flows`, `agent-composer` and `conversation-side-panel`. 9 Haiku rows measured. validate --strict: valid.**
- [x] 0.4a R5: an independent comparison. **Done 2026-09-24 (R5-1 to R5-5 in the Round log). Added the `agent-conversation-workspace` MODIFIED delta (*A delivered turn reaches the model intact*), corrected D10's U+FEFF account, stated the must-not-raise rules for D4 and D7, and measured 8 Haiku rows. validate --strict: valid.**
- [x] 0.5 The operator answers Q4 (D10 stays in this change, the default, or is split out). Record it in `spec-queue/DECISIONS.md`. **Done 2026-09-24 afternoon (`spec-queue/DECISIONS.md` `F409-Q4`): D10 stays in this change.**

## 1. Tests first. Each must fail on today's code unless it is marked as a control

- [ ] 1.0 **Gate, before any test or fix (second review, fix 1).** Rerun `py -3.11 scripts/drive/d2_0923_at_mention_tokeniser.py` on the CLI version installed at build time, and record the version and the `N/N match` line. **If a row is marked `CHANGED`, stop here and re-open D2 (and D10, for the `/` row); do not start group 2.** Then run `py -3.11 scripts/drive/d3_0923_worker_at_mention.py` on today's code, before group 2 lands: both workers `expanded=True`, and the system-prompt-file control `expanded=False`. Record it; this is the "before" run that task 3.1's "after" run is compared with
- [ ] 1.1 In `hub/tests/test_inbound_queue.py`, add a parametrised test over `origin_type` in `agent`, `job`, `checkpoint`, `divergence`, and one unknown origin (the unknown row builds an `InboundQueueEntry` directly, bypassing `new_entry`'s closed set). An entry whose content is `see @/etc/passwd and a@b.com` reaches `format_turn_prompt`'s output as `see \@/etc/passwd and a\@b.com`. The unknown row is what pins D3's form (`origin_type != "operator"`, outside the label branches), which B5's `evidence` origin relies on. Record that each FAILS today
- [ ] 1.2 Control: an `operator` entry with the same content reaches the output byte-identical, with no backslash and no D6 sentence. Record that it PASSES today
- [ ] 1.3 A mixed turn (an operator entry with `@src/app.py`, then an agent entry with `@../x`): the operator block keeps `@src/app.py` and the agent block reads `\@../x`. The D6 sentence appears once, on the line after the preamble. Record that it FAILS today
- [ ] 1.3a The question echo (D4, D6). A turn holding only an `operator` entry whose content is `_batch_delivery_text` of (a) a question containing an at-sign with a typed answer, or (b) a question with no at-sign whose answer was the chosen option `Use @/x` (`answer_labels=["Use @/x"]`), gets the D6 sentence once. The same turn built from a question and a typed answer with no at-sign is byte-identical to today. Record that (a) and (b) FAIL today and the control PASSES
- [ ] 1.4 The D6 sentence contains no literal at-sign. It is absent from a non-operator turn whose content has no at-sign, and from an operator turn whose content has no `\@` (the existing exact-string test `test_scheduled_job_origin_is_typed_and_has_no_origin_agent` must keep passing unchanged). Record both
- [ ] 1.5 The escape does not collapse an existing escape: content that already contains `\@` becomes `\\@`. Assert it, and assert that the character before every at-sign in the output is a backslash (the property D2 relies on). Include U+FEFF, U+3000 and a newline before the at-sign. Record that it FAILS today
- [ ] 1.6 (D4, rewritten in R4) In `hub/tests/test_question_batch_delivery.py` (or the questions test that already builds `_batch_delivery_text` rows), for the single form and for a batch of two, one row each:
  - (a) An at-sign in `question`, with a typed answer (`answer_labels=[]`, `answer="see @src/app.py"`): the question is escaped and the answer is byte-identical. FAILS today on the question half.
  - (b) **Labels set:** `answer_labels=["Use @/home/u/.ssh/id_rsa"]`, `answer="Use @/home/u/.ssh/id_rsa"`: the delivered answer reads `Use \@/home/u/.ssh/id_rsa`. Multi-select: `answer_labels=["Keep it", "Use @/x"]`, `answer="Keep it, Use @/x"`: it reads `Keep it, Use \@/x`. FAILS today.
  - (c) **Typed answer, control:** `answer_labels=[]` and an at-sign in `answer`: unchanged. PASSES today and must keep passing.
  - (d) **Mixed:** `answer_labels=["Option A"]`, `answer="Option A, and see @/x"`: the whole answer is escaped (`\@/x`). FAILS today.
  - (e) The batch form with one answered row whose answer was an option and one declined row: the declined wording is unchanged and the answered row is escaped.
  - (f) (R5) An answered row whose `answer` is `None`, for both forms: `_batch_delivery_text` does not raise, and it renders the answer exactly as today's f-string does. The call runs after the route's commit, so a raise would return 500 for a stored answer that is never delivered. PASSES today and must keep passing.

  - (g) **Through the route, in the UI's payload shape (second review, fix 7).** A non-blocking question with an option `Use @/x`, answered through the answer route with the body the UI sends when an option is clicked (`labels=["Use @/x"]`, `answer="Use @/x"`), queues an inbound entry whose content reads `\@/x` for the answer. This is the only row that proves the `populate_existing` re-read in `_completed_batch` hands `_batch_delivery_text` the stored `answer_labels`; the other rows build `Question` rows by hand (F190). FAILS today.

  Also assert, through the route, that `answer_question`'s stored `Question.answer` and `answer_labels` keep the unescaped text. Record each
- [ ] 1.7 (D7) `build_worker_command` and `build_title_command`, for `cli` in `claude` and `codex`: given a prompt containing `see @/etc/passwd`, the **prompt argument** is `see \@/etc/passwd`. For Claude that is the element after `-p`, and for Codex the last element. The test asserts on that element only, so the titler's `--tools ""` and any flag B7 adds do not affect it, and it asserts that every other element is unchanged from the builder's output for an at-sign-free prompt. Record that each FAILS today
- [ ] 1.8 (D7) Control, which PASSES today and must keep passing: `_GENERATION_PROMPT`, `_PROBE_PROMPT` and `conversation_titles._PROMPT` contain no at-sign, so neutralising a whole worker prompt never alters Hub text
- [ ] 1.9 (D7) `grade_probe` with the envelope's `files_changed` = `['packages/@scope/x.ts', 'node_modules/@types/y.d.ts', '@root/z.md']`. Each of these answers grades `passed` with no findings: the plain form; the escaped form `packages/\@scope/x.ts`; the Windows-separator form `node_modules\@types\y.d.ts`; and the leading `\@root/z.md`. Grade each both as given and after `restore_file_mentions`. Record that the escaped and Windows rows FAIL today
- [ ] 1.10 (D7) `restore_file_mentions(neutralise_file_mentions(t)) == t` for `t` in `a@b`, `a\@b`, `a\\@b`, `@x`, and text with no at-sign. `worker._interpret`, given a Claude envelope whose answer is a JSON object whose raw text is `"state": "see packages/\\@scope/x and ops\\@example.com"`, returns a parsed `CheckpointBody` whose `state` has no backslash before either at-sign. `generate_conversation_title`'s stored title has none either, given a titler output `Fix \@scope build`. Record that the `_interpret` and title rows FAIL today. **(R5)** `_interpret` is also given a payload that holds a number, a boolean, a `null` and a nested list of strings beside the escaped strings. It returns the same outcome it returns today, with only the strings restored, and it does not raise. Control: `CHECKPOINT_PROMPT_VERSION` stays `checkpoint/1`
- [ ] 1.11 (D8) A UI unit test for `useStartWorkOnTask`, or for the message builder it is refactored to call: a task titled `Fix @/etc/passwd for ops@x.io` posts `message` equal to `Work on task <id>: Fix \@/etc/passwd for ops\@x.io`, and a title with no at-sign is byte-identical to today. Record that the first FAILS today
- [ ] 1.12 (D9) In `hub/tests/test_launchability.py`: `spec_turn_notice("exploring", path="spec/a @x/y.html", is_unwritten=True)` contains `spec/a \@x/y.html` at both interpolations, contains no unescaped at-sign, and ends with the D6 sentence. The same call with `path="spec/pale-otter.html"` is byte-identical to today's output. Record that the first FAILS today and the second PASSES
- [ ] 1.13 (D10) In `hub/ui/src/__tests__/composerTriggerSources.test.ts` and a new `fileMentions` test:
  - `isSafeMentionValue` is true for `src/app.py`, `packages/@scope/x.ts`, `node_modules/@types/y.d.ts` and `@root/z.md`. It is false for `x @/home/u/.ssh/id_rsa`, `notes@home.md` and `x﻿@y`.
  - `resolveTriggerResults` for a `path` trigger with query `''`, over a listing holding all seven, returns exactly the four safe ones. The same holds for a `skill` trigger over `.claude/skills/ok/SKILL.md` and `.claude/skills/x @/y/SKILL.md`.
  - `FileTab`, rendered for `x @/home/u/.ssh/id_rsa`, has no `file-tab-insert` control. Rendered for `src/app.py`, it has one.
  - (second review, fix 5) The `Composer`, given an `insertPathRequest` for `x @/home/u/.ssh/id_rsa`, leaves its text unchanged; given one for `src/app.py`, it inserts the mention as today. This row fails if 2.3d's insert-effect guard is reverted, which nothing else catches.

  Record that the unsafe rows FAIL today
- [ ] 1.14 Control: the existing composer tests (`composerTrigger`, `composerTriggerSources`, the Composer insert test) keep passing unchanged for paths with no at-sign

## 2. The fix

- [ ] 2.1 Add `hub/hub/file_mentions.py` with `neutralise_file_mentions(text: str) -> str` (`text.replace("@", "\\@")`), `restore_file_mentions` (the exact inverse) and the D6 sentence as a constant. Add a comment citing design D2 and the U+FEFF row
- [ ] 2.2 `format_turn_prompt`: neutralise `entry.content` unless `entry.origin_type == "operator"`, as one test outside the label branches (D3, default-deny; B5's `evidence` branch must not move it). If B5's `evidence-is-decided-after-the-run-that-recorded-it` has already landed, add an `evidence` row to 1.1 (its design.md carries the matching note). Add the D6 sentence after the preamble line when the composed blocks contain `\@`
- [ ] 2.3 `_batch_delivery_text`: neutralise `row.question` at both interpolations. Neutralise `row.answer` at both interpolations when `row.answer_labels` is non-empty, whole, and leave it as it is otherwise (D4, R4)
- [ ] 2.3a (D7) `build_worker_command` and `build_title_command` neutralise the prompt argument in both CLI branches. Apply `restore_file_mentions` recursively to the payload's strings in `worker._interpret` before `model_validate`. Whichever of this change and B7's `worker-spend-counts-against-the-budget` lands second keeps that order. Apply it to the titler's output before `title_from_output` in `generate_conversation_title`. In `_normalise`, after mapping `\` to `/`, apply `re.sub(r"/+@", "@", ...)`. The templates and both prompt versions are unchanged
- [ ] 2.3b (D8) Add `hub/ui/src/lib/fileMentions.ts` with `neutraliseFileMentions` and a comment naming `hub/hub/file_mentions.py`, and use it on `title` in `useStartWorkOnTask`
- [ ] 2.3c (D9) `spec_turn_notice`: neutralise `path` at both interpolations, and when that changed it, append the D6 constant as the notice's last line
- [ ] 2.3d (D10) Add `isSafeMentionValue(value)` to `hub/ui/src/lib/fileMentions.ts`: true when every at-sign in `value` is at index 0 or directly after `/`. Filter `path` and `skill` results in `resolveTriggerResults` with it, applied to the value that is inserted (for `path`, the path; for `skill`, the name `skillNameFromPath` returns), and **before** `.slice(0, MAX_RESULTS)`, so refused values do not take up the 50 slots. In `FileTab`, render the insert action only for a safe path. In `Composer`'s `insertPathRequest` effect, ignore an unsafe path. Comment each use with design D10 and the measured `picker_quoted_nested` row
- [ ] 2.3e UI: run `cd hub/ui && npm run lint` and the UI tests. Run `cd hub/ui && npm run build` (the refresh script copies `dist/` and does not build it). Then refresh the bundle (`make ui` or `scripts/refresh_ui_bundle.py`) and commit `hub/ui/src` and `hub/hub/static/ui` together, per `.claude/rules/`
- [ ] 2.4 Run group 1; every row passes. Record the counts inline
- [ ] 2.5 Run `py -3.11 -m pytest hub/tests/ -q` and record the full-suite count inline, or do not tick. Any existing assertion that moves is named and explained. Any unexplained move is a defect
- [ ] 2.6 Run `ruff check src/ hub/ tests/` and `black --check --target-version py311 src/ hub/hub/ hub/tests/ tests/`. Both are clean

## 3. The CLI: evidence that the rule matches what the harness does

- [ ] 3.1 After group 2, rerun `py -3.11 scripts/drive/d3_0923_worker_at_mention.py`: it shows the fix (both workers `expanded=False`, the system-prompt-file control still `False`). Record it beside task 1.0's "before" run. (The tokeniser gate and the "before" run moved to 1.0, so they run before the build.)
- [ ] 3.1a Add R4's rows to the tokeniser probe, with their measured values: `label_answer` E, `label_answer_escaped` —, `multi_label_answer` E, `batch_label_answer` E, `picker_quoted_nested` E, `picker_unquoted_bom` E, `picker_quoted_slash_at` —, `spec_notice_path` E, `spec_notice_path_escaped` —. Add R5's rows too: `picker_quoted_bom` E, `skill_dollar_at` —, `skill_dollar_quoted_index0` —, `picker_quoted_index0` —, `small_commercial_at` —, `escaped_quoted` —, `bom_then_escaped` —. Use the prompt shapes in design.md's "R4's rows" and "R5's rows" tables
- [ ] 3.2 Add a note to `scripts/drive/README.md` saying that a `claude` CLI upgrade reruns both probes. Name the rows D2 depends on (`escaped`, `escaped_twice`), the rows D10 depends on (`after_002f`, `picker_quoted_slash_at`, `double_at`, `skill_dollar_at`, `picker_quoted_index0`), and the system-prompt-file control, which is why the context file is left alone

## 4. Drive: the product, end to end (every real turn binds `claude-haiku-4-5`)

- [ ] 4.1 Set up a scratch Hub (never `:8000`, never `:8010`'s real projects), with a fresh project in `testbed/scratch/` and two Claude agents, A and B, in the default "Workspace only" posture. Plant a random marker in a file outside B's workspace. Have the operator ask A to `send_message` B a text containing `@<that absolute path>`, and ask B to report any line starting with MARKER.
  - **Before the fix** (or with task 2.2 reverted), record whether B's CLI session transcript has an `"attachment":{"type":"file"` for the marker file.
  - **After the fix**, confirm it has none and that the marker appears nowhere in B's run output
- [ ] 4.2 Positive control on the same Hub: the operator sends B `@<a file inside B's workspace>` from the composer. Confirm the attachment is still made (the picker's contract survives)
- [ ] 4.2a (D7) On the same Hub, have B write an `@<outside path>` into its reply in a conversation, then create a checkpoint for that conversation. Confirm:
  - the checkpoint worker's CLI transcript has no `"attachment":{"type":"file"`;
  - the stored checkpoint body does not contain the marker;
  - the probe status is not `failed` for a reason naming that path
- [ ] 4.2b (D8) On the same Hub, have A `create_task` with a title containing `@<outside path>`, then press Start work on that card from the served bundle in a browser, assigning B. Confirm that B's CLI transcript has no `"attachment":{"type":"file"` for the marker file, and that the chat shows the title with `\@`
- [ ] 4.2c (D4, R4) On the same Hub, have B call `ask_user` with `blocking=false` and an option labelled `Use @<outside path>`. Click that option in the conversation. Confirm that B's next turn's CLI transcript has no `"attachment":{"type":"file"` for the marker file, that the chat's echo shows the answer with `\@`, and that the Questions destination shows the option as it was offered
- [ ] 4.3 Record all five in `scripts/drive/FINDINGS.md` under the drive's dated section. Tear down the scratch Hub by exact PID and remove the scratch project

## 5. Close

- [ ] 5.1 Update F409's Status line to `fixed <sha>`, and regenerate the backlog with `py -3.11 scripts/backlog_page.py`
- [ ] 5.2 `openspec validate an-at-mention-an-agent-wrote-reads-no-file --strict` passes. Archive with `openspec-archive-change`
